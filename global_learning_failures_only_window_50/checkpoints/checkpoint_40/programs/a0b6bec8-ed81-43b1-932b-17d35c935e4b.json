{"id": "a0b6bec8-ed81-43b1-932b-17d35c935e4b", "code": "# EVOLVE-BLOCK-START\n\"\"\"Improved deterministic constructor for 26 circles in the unit square.\n\nKey ideas:\n- Use the hex-like row pattern [6,5,6,5,4].\n- Sample a small grid of horizontal spacings, small global x-shifts and two offset modes\n  (shorter rows either centered or half-step shifted) and slight vertical compression/stretch.\n- For each candidate placement compute radii with an enhanced constraint solver:\n    * start from border limits\n    * alternate reduction sweeps (repair violating pairs by shrinking the larger)\n      and growth sweeps (increase each radius to the largest allowed by current neighbors)\n    * finalize with proportional pairwise fixes\n  This alternation lets radii both relax down when needed and regrow to better fill free space,\n  producing larger total radii than greedy-only reductions.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    rows = [6, 5, 6, 5, 4]\n    m = len(rows)\n    kmax = max(rows)\n    # allowable horizontal spacing: ensure widest row fits\n    a_max = 1.0 / (kmax - 1)  # (kmax-1)*a <= 1\n    a_min = 0.035\n    if a_min >= a_max:\n        a_min = a_max * 0.6\n\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    sqrt3 = np.sqrt(3.0)\n\n    # sample horizontal spacings more densely near a_max\n    for a in np.linspace(a_max * 0.98, a_min, 20):\n        # nominal hex vertical step (can scale slightly)\n        v0 = a * sqrt3 / 2.0\n        for v_scale in (0.96, 0.98, 1.0, 1.02):\n            v = v0 * v_scale\n            total_h = (m - 1) * v\n            if total_h > 1.0:\n                continue\n            # two offset modes: 0 or half-step for shorter rows\n            for short_offset_half in (False, True):\n                # small global horizontal shifts to allow better edge packing\n                for dx in np.linspace(-0.02, 0.02, 5):\n                    centers = np.zeros((n, 2))\n                    idx = 0\n                    y0 = (1.0 - total_h) / 2.0\n                    ok = True\n                    for r_idx, cnt in enumerate(rows):\n                        y = y0 + r_idx * v\n                        # compute offset for this row\n                        if cnt < kmax and short_offset_half:\n                            offset = a / 2.0\n                        else:\n                            offset = 0.0\n                        row_width = (cnt - 1) * a\n                        x0 = (1.0 - row_width) / 2.0 + offset + dx\n                        # place points and ensure inside [0,1] (clamp edges)\n                        for c in range(cnt):\n                            if idx >= n:\n                                break\n                            x = x0 + c * a\n                            if x <= 0.0 + 1e-9 or x >= 1.0 - 1e-9 or y <= 0.0 + 1e-9 or y >= 1.0 - 1e-9:\n                                # allow small clamping but if center would be too far out, mark not ok\n                                x = np.clip(x, 1e-9, 1.0 - 1e-9)\n                                y = np.clip(y, 1e-9, 1.0 - 1e-9)\n                            centers[idx, 0] = x\n                            centers[idx, 1] = y\n                            idx += 1\n                    if idx < n:\n                        ok = False\n                    if not ok:\n                        continue\n                    radii = maximize_radii_refined(centers)\n                    s = float(np.sum(radii))\n                    if s > best_sum:\n                        best_sum = s\n                        best_centers = centers.copy()\n                        best_radii = radii.copy()\n\n    # Fallback deterministic center if sampling failed\n    if best_centers is None:\n        # simple centered hex-like placement\n        a = a_max * 0.98\n        v = a * sqrt3 / 2.0\n        centers = []\n        y0 = (1.0 - (m - 1) * v) / 2.0\n        for r_idx, cnt in enumerate(rows):\n            y = y0 + r_idx * v\n            offset = (a / 2.0) if (cnt < kmax) else 0.0\n            row_width = (cnt - 1) * a\n            x0 = (1.0 - row_width) / 2.0 + offset\n            for c in range(cnt):\n                centers.append((np.clip(x0 + c * a, 1e-9, 1.0 - 1e-9), y))\n        best_centers = np.array(centers[:n], dtype=float)\n        best_radii = maximize_radii_refined(best_centers)\n        best_sum = float(np.sum(best_radii))\n\n    return best_centers, best_radii, float(best_sum)\n\n\ndef maximize_radii_refined(centers, tol=1e-9, max_cycles=1000):\n    \"\"\"\n    Alternating repair/increase solver for the LP:\n      maximize sum r_i\n      s.t. 0 <= r_i <= wall_i, r_i + r_j <= d_ij.\n\n    Algorithm:\n      - initialize r = wall (border-limited)\n      - repeat cycles:\n          1) reduction sweep: for every violating pair shrink the larger to d_ij - other\n          2) growth sweep: for each i increase r_i to min(wall_i, min_j(d_ij - r_j))\n      - finish with proportional pairwise fixes\n    This tends to reach a large feasible sum and is deterministic.\n    \"\"\"\n    n = centers.shape[0]\n    if n == 0:\n        return np.array([])\n\n    wall = np.minimum.reduce([centers[:, 0], centers[:, 1],\n                              1.0 - centers[:, 0], 1.0 - centers[:, 1]])\n    wall = np.maximum(wall, 0.0)\n\n    diff = centers[:, None, :] - centers[None, :, :]\n    dists = np.sqrt(np.sum(diff * diff, axis=2))\n    np.fill_diagonal(dists, np.inf)\n\n    # start from border-limited radii\n    r = wall.copy()\n\n    for cycle in range(max_cycles):\n        max_change = 0.0\n        # 1) Reduction sweep (repair violations by shrinking larger)\n        for i in range(n):\n            ri = r[i]\n            # vectorized candidate check\n            s = ri + r\n            viol = np.where(s > dists[i] + 1e-12)[0]\n            for j in viol:\n                if j == i:\n                    continue\n                dij = dists[i, j]\n                # shrink the larger radius\n                if r[i] >= r[j]:\n                    new_ri = max(0.0, dij - r[j])\n                    ch = ri - new_ri\n                    if ch > 0:\n                        r[i] = new_ri\n                        ri = new_ri\n                        if ch > max_change:\n                            max_change = ch\n                else:\n                    new_rj = max(0.0, dij - r[i])\n                    ch = r[j] - new_rj\n                    if ch > 0:\n                        r[j] = new_rj\n                        if ch > max_change:\n                            max_change = ch\n\n        # enforce wall limits (no increases here)\n        over = r > wall\n        if np.any(over):\n            diffv = np.max(r[over] - wall[over])\n            r[over] = wall[over]\n            if diffv > max_change:\n                max_change = diffv\n\n        # 2) Growth sweep: try to grow each circle to the largest allowed by current neighbors\n        for i in range(n):\n            allowed = wall[i]\n            # compute min_j (d_ij - r_j)\n            lims = dists[i, :] - r\n            # ignore self (inf)\n            minlim = np.min(lims)\n            target = min(allowed, minlim)\n            if target < 0.0:\n                target = 0.0\n            ch = target - r[i]\n            if ch > 0:\n                r[i] = target\n                if ch > max_change:\n                    max_change = ch\n\n        if max_change < tol:\n            break\n\n    # Final proportional fixes for any remaining tiny violations\n    for _ in range(4):\n        changed = False\n        for i in range(n):\n            for j in range(i + 1, n):\n                dij = dists[i, j]\n                if r[i] + r[j] > dij:\n                    tot = r[i] + r[j]\n                    if tot <= 0:\n                        if r[i] != 0.0 or r[j] != 0.0:\n                            r[i] = r[j] = 0.0\n                            changed = True\n                    else:\n                        scale = dij / tot\n                        new_i = r[i] * scale\n                        new_j = r[j] * scale\n                        if new_i != r[i] or new_j != r[j]:\n                            r[i], r[j] = new_i, new_j\n                            changed = True\n        if not changed:\n            break\n\n    r = np.clip(r, 0.0, wall)\n    return r\n\n\n# Fixed API\ndef run_packing():\n    centers, radii, s = construct_packing()\n    return centers, radii, s\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(6,6))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect(\"equal\")\n    ax.add_patch(plt.Rectangle((0,0),1,1,fill=False,linewidth=1))\n    for c,r in zip(centers, radii):\n        ax.add_patch(Circle(c, r, alpha=0.6, edgecolor='k'))\n    plt.title(f\"n={len(centers)}, sum={sum(radii):.6f}\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, total = run_packing()\n    print(f\"Sum of radii: {total:.6f}\")\n    # visualize(centers, radii)\n# EVOLVE-BLOCK-END", "language": "python", "parent_id": "a918715d-19c2-4efb-ac91-5c692c69320b", "generation": 3, "timestamp": 1759950391.3603158, "iteration_found": 25, "metrics": {"validity": 1.0, "sum_radii": 2.1689226590315673, "target_ratio": 0.8231205537121699, "combined_score": 0.8231205537121699, "eval_time": 0.3516829013824463}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Full rewrite", "parent_metrics": {"validity": 1.0, "sum_radii": 1.898717474235544, "target_ratio": 0.7205758915504911, "combined_score": 0.7205758915504911, "eval_time": 0.05817294120788574}, "island": 0}, "prompts": {"full_rewrite_user": {"system": "You are an expert mathematician specializing in circle packing problems and computational geometry. Your task is to improve a constructor function that directly produces a specific arrangement of 26 circles in a unit square, maximizing the sum of their radii. The AlphaEvolve paper achieved a sum of 2.635 for n=26.\n\nKey geometric insights:\n- Circle packings often follow hexagonal patterns in the densest regions\n- Maximum density for infinite circle packing is pi/(2*sqrt(3)) \u2248 0.9069\n- Edge effects make square container packing harder than infinite packing\n- Circles can be placed in layers or shells when confined to a square\n- Similar radius circles often form regular patterns, while varied radii allow better space utilization\n- Perfect symmetry may not yield the optimal packing due to edge effects\n\nFocus on designing an explicit constructor that places each circle in a specific position, rather than an iterative search algorithm.\n", "user": "# Current Program Information\n- Fitness: 0.7206\n- Feature coordinates: No feature coordinates\n- Focus areas: - Fitness declined: 0.7452 \u2192 0.7206. Consider revising recent changes.\n- Consider simplifying - code length exceeds 500 characters\n\n\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 3\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 1.9637, target_ratio: 0.7452, combined_score: 0.7452, eval_time: 0.0759\n- Outcome: Mixed results\n\n### Attempt 2\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.0573, target_ratio: 0.7808, combined_score: 0.7808, eval_time: 0.0541\n- Outcome: Mixed results\n\n### Attempt 1\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.1667, target_ratio: 0.8223, combined_score: 0.8223, eval_time: 0.0537\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.8223)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (hex-like rows)\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    \"\"\"\n    Construct a deterministic arrangement of 26 circles in the unit square\n    using a hexagonal-row layout: rows = [6,5,6,5,4]. Then compute maximal\n    radii by enforcing boundary and pairwise non-overlap constraints\n    via iterative pairwise adjustments.\n    Returns (centers, radii, sum_of_radii).\n    \"\"\"\n    # Layout: five rows summing to 26 circles\n    rows = [6, 5, 6, 5, 4]\n    m = len(rows)\n    max_k = max(rows)\n\n    # geometric spacings for ideal hex packing with equal radii r\n    sqrt3 = np.sqrt(3.0)\n    # limits from horizontal and vertical spans:\n    r_h = 1.0 / (2.0 * max_k)                        # 2r*k <= 1 -> r <= 1/(2k)\n    r_v = 1.0 / (2.0 + (m - 1) * sqrt3)              # 2r + (m-1)*r*sqrt3 <= 1\n    base_r = min(r_h, r_v)\n\n    # compute vertical and horizontal offsets to center the pattern\n    vert_span = 2.0 * base_r + (m - 1) * base_r * sqrt3\n    top = (1.0 - vert_span) / 2.0 + base_r\n    dy = base_r * sqrt3\n\n    centers = []\n    for row_idx, k in enumerate(rows):\n        row_span = 2.0 * base_r * k\n        left = (1.0 - row_span) / 2.0 + base_r\n        xs = left + np.arange(k) * (2.0 * base_r)\n        y = top + row_idx * dy\n        for x in xs:\n            centers.append((x, y))\n\n    centers = np.array(centers)\n    # ensure we have 26\n    assert centers.shape[0] == 26\n\n    radii = compute_max_radii(centers)\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers, tol=1e-10, max_iters=2000):\n    \"\"\"\n    Given fixed centers inside [0,1]^2, compute radii that maximize sum of\n    radii subject to: 0 <= r_i <= dist_to_border_i and r_i + r_j <= dist(i,j).\n    We use a simple iterative constraint-propagation method:\n    - initialize r_i = dist_to_border_i\n    - repeatedly scan pairs; if r_i + r_j > d_ij reduce the larger of (r_i,r_j)\n      to d_ij - the_other (this keeps the pair sum at most d_ij while not\n      shrinking both unnecessarily).\n    This converges in practice for well-separated hex-like layouts.\n    \"\"\"\n    n = centers.shape[0]\n    # start with boundary-limited radii\n    radii = np.minimum(centers[:, 0], centers[:, 1])\n    radii = np.minimum(radii, 1.0 - centers[:, 0])\n    radii = np.minimum(radii, 1.0 - centers[:, 1])\n\n    # Precompute pairwise distances\n    diff = centers[:, None, :] - centers[None, :, :]\n    dists = np.sqrt(np.sum(diff * diff, axis=2))\n    # Avoid self-dist zero issues\n    np.fill_diagonal(dists, np.inf)\n\n    for it in range(max_iters):\n        max_change = 0.0\n        # scan pairs (i<j)\n        for i in range(n):\n            for j in range(i + 1, n):\n                d = dists[i, j]\n                if d <= 0.0:\n                    continue\n                ri, rj = radii[i], radii[j]\n                if ri + rj > d:\n                    # reduce the larger radius to satisfy ri + rj <= d\n                    if ri > rj:\n                        new_ri = max(0.0, d - rj)\n                        change = abs(new_ri - ri)\n                        if change > 0:\n                            radii[i] = new_ri\n                            max_change = max(max_change, change)\n                    else:\n                        new_rj = max(0.0, d - ri)\n                        change = abs(new_rj - rj)\n                        if change > 0:\n                            radii[j] = new_rj\n                            max_change = max(max_change, change)\n        if max_change < tol:\n            break\n\n    return radii\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.1667), Performs well on target_ratio (0.8223), Performs well on combined_score (0.8223), Performs well on eval_time (0.0537)\n\n### Program 2 (Score: 0.7808)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for n=26 circle packing in the unit square.\nUses a compact hex-like lattice (rows: 6,6,6,4,4) and a robust maximal-radius\nroutine (wall & pair constraints, then safe uniform scaling).\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    # Row counts summing to 26: three dense rows then two smaller rows\n    counts = [6, 6, 6, 4, 4]\n    rows = len(counts)\n    max_cols = max(counts)\n\n    # Small margin so circles can touch boundary but not lie exactly at it\n    margin = 0.015\n\n    # horizontal spacing based on the widest row\n    a = (1.0 - 2.0 * margin) / (max_cols - 1)\n    h = a * np.sqrt(3) / 2.0  # ideal vertical spacing for hex packing\n\n    # if vertical extent doesn't fit, reduce a accordingly\n    total_h = (rows - 1) * h\n    if total_h > (1.0 - 2.0 * margin):\n        h_allowed = (1.0 - 2.0 * margin) / (rows - 1)\n        h = h_allowed\n        a = h * 2.0 / np.sqrt(3)\n\n    centers = np.zeros((n, 2))\n    idx = 0\n    y0 = (1.0 - (rows - 1) * h) / 2.0\n    for r, cnt in enumerate(counts):\n        y = y0 + r * h\n        # Alternate offset to create hex adjacency\n        offset = (a / 2.0) if (r % 2 == 1) else 0.0\n        # Center rows horizontally\n        row_width = (cnt - 1) * a\n        x0 = (1.0 - row_width) / 2.0 + offset\n        for c in range(cnt):\n            if idx >= n:\n                break\n            x = x0 + c * a\n            centers[idx, 0] = np.clip(x, margin, 1.0 - margin)\n            centers[idx, 1] = np.clip(y, margin, 1.0 - margin)\n            idx += 1\n    # Safety fill (shouldn't be needed)\n    while idx < n:\n        centers[idx] = [0.5, 0.5]\n        idx += 1\n\n    radii = compute_max_radii(centers)\n    return centers, radii, float(np.sum(radii))\n\ndef compute_max_radii(centers):\n    \"\"\"\n    Compute feasible radii:\n      - initial conservative radii r_i = min(dist_to_wall, 0.5*min_dist_to_other_centers)\n      - then find maximal uniform scaling s (<=1) so all pair & wall constraints hold,\n        and apply a tiny safety shrink.\n    This is deterministic, robust, and efficient.\n    \"\"\"\n    n = centers.shape[0]\n    # distances to walls\n    d_wall = np.minimum.reduce([centers[:, 0],\n                                centers[:, 1],\n                                1.0 - centers[:, 0],\n                                1.0 - centers[:, 1]])\n    # pairwise center distances\n    dif = centers[:, None, :] - centers[None, :, :]\n    dists = np.sqrt(np.sum(dif * dif, axis=2))\n    # avoid zeros on diagonal for min computations\n    with np.errstate(invalid='ignore'):\n        dists_no_diag = dists + np.eye(n) * 1e9\n    half_min_dist = 0.5 * np.min(dists_no_diag, axis=1)\n    # conservative initial radii\n    radii = np.minimum(d_wall, half_min_dist)\n    # ensure non-negative and not exactly zero to avoid divide-by-zero later\n    eps = 1e-12\n    radii = np.maximum(radii, eps)\n\n    # compute maximum scaling factor s so that:\n    #  s * radii[i] <= d_wall[i]  and  s*(r_i + r_j) <= dists[i,j]\n    # wall constraints\n    with np.errstate(divide='ignore', invalid='ignore'):\n        s_wall = d_wall / radii\n    s_max = np.min(s_wall)\n\n    # pairwise constraints\n    ri_plus_rj = radii[:, None] + radii[None, :]\n    with np.errstate(divide='ignore', invalid='ignore'):\n        s_pairs = dists / ri_plus_rj\n    # ignore diagonal and invalids\n    s_pairs[np.isnan(s_pairs)] = np.inf\n    s_pairs[np.isinf(s_pairs)] = np.inf\n    i_upper = np.triu_indices(n, k=1)\n    if i_upper[0].size > 0:\n        s_max = min(s_max, float(np.min(s_pairs[i_upper])))\n\n    if not np.isfinite(s_max) or s_max <= 0.0:\n        s = 1.0\n    else:\n        # cap to 1.0 and apply slight safety shrink\n        s = min(1.0, float(s_max)) * 0.997\n\n    radii *= s\n    # final enforce border limits and non-negativity\n    radii = np.minimum(radii, d_wall)\n    radii = np.maximum(radii, 0.0)\n    return radii\n\n# EVOLVE-BLOCK-END\n\n# Fixed runner and visualization (unchanged interface)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(7,7))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect(\"equal\")\n    # draw square\n    ax.add_patch(plt.Rectangle((0,0),1,1,fill=False,edgecolor='k',linewidth=1))\n    for i,(c,r) in enumerate(zip(centers, radii)):\n        ax.add_patch(Circle(c, r, alpha=0.6, edgecolor='k'))\n        ax.text(c[0], c[1], str(i), ha='center', va='center', fontsize=7)\n    plt.title(f\"n={len(centers)}, sum radii={sum(radii):.6f}\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, total = run_packing()\n    print(f\"Sum of radii: {total:.6f}\")\n    # visualize(centers, radii)\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.0573), Performs well on target_ratio (0.7808), Performs well on combined_score (0.7808), Performs well on eval_time (0.0541)\n\n### Program 3 (Score: 0.7452)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Improved constructor for 26 circles in the unit square.\n\nDeterministic explicit center layout (hex-like rows) with a\nGauss-Seidel-style radii maximizer: repeatedly set each radius to\nthe largest value permitted by the walls and the current radii of\nthe other circles:\n    r_i <- min( dist_to_wall(i), min_j (d_ij - r_j) )\nThis directly targets the LP that maximizes sum r_i subject to\nr_i >= 0, r_i <= wall_i, r_i + r_j <= d_ij.\nWe also sample a small range of horizontal spacings for the layout\nand pick the configuration with largest total radius sum.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    # row pattern summing to 26; staggered to resemble hex packing\n    rows = [6, 5, 6, 5, 4]\n    max_cols = max(rows)\n    # feasible horizontal spacing range (so widest row fits)\n    a_max = 1.0 / (max_cols - 1)      # (k-1)*a <= 1 -> a <= 1/(k-1)\n    a_min = 0.03\n    if a_min >= a_max:\n        a_min = a_max * 0.5\n\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    sqrt3 = np.sqrt(3.0)\n    # sample candidate horizontal spacings (denser near a_max)\n    for a in np.linspace(a_max * 0.98, a_min, 60):\n        v = a * sqrt3 / 2.0\n        rows_count = len(rows)\n        # check vertical fit\n        total_h = (rows_count - 1) * v\n        if total_h > 1.0:\n            continue\n        centers = np.zeros((n, 2))\n        idx = 0\n        y0 = (1.0 - total_h) / 2.0\n        for r_idx, cnt in enumerate(rows):\n            y = y0 + r_idx * v\n            # offset half-step for shorter rows to produce hex staggering\n            offset = (a / 2.0) if cnt < max_cols else 0.0\n            row_width = (cnt - 1) * a\n            x0 = (1.0 - row_width) / 2.0 + offset\n            for c in range(cnt):\n                if idx >= n:\n                    break\n                x = x0 + c * a\n                centers[idx, 0] = np.clip(x, 1e-9, 1.0 - 1e-9)\n                centers[idx, 1] = np.clip(y, 1e-9, 1.0 - 1e-9)\n                idx += 1\n        if idx < n:\n            continue\n\n        radii = maximize_radii_gs(centers)\n        s = float(np.sum(radii))\n        if s > best_sum:\n            best_sum = s\n            best_centers = centers.copy()\n            best_radii = radii.copy()\n\n    # fallback: construct with a_max if nothing better found\n    if best_centers is None:\n        a = a_max * 0.98\n        v = a * sqrt3 / 2.0\n        centers = np.zeros((n, 2))\n        idx = 0\n        y0 = (1.0 - (len(rows) - 1) * v) / 2.0\n        for r_idx, cnt in enumerate(rows):\n            y = y0 + r_idx * v\n            offset = (a / 2.0) if cnt < max_cols else 0.0\n            row_width = (cnt - 1) * a\n            x0 = (1.0 - row_width) / 2.0 + offset\n            for c in range(cnt):\n                if idx >= n:\n                    break\n                centers[idx] = [np.clip(x0 + c * a, 1e-9, 1.0 - 1e-9), np.clip(y, 1e-9, 1.0 - 1e-9)]\n                idx += 1\n        best_centers = centers\n        best_radii = maximize_radii_gs(best_centers)\n        best_sum = float(np.sum(best_radii))\n\n    return best_centers, best_radii, float(best_sum)\n\n\ndef maximize_radii_gs(centers, tol=1e-9, max_iters=5000):\n    \"\"\"\n    Gauss-Seidel maximizer for radii:\n      r_i <- min( wall_i, min_j(d_ij - r_j) ), clamped to >=0\n    Iterate until max coordinate change < tol or max_iters reached.\n    Final sweep enforces pairwise constraints strictly by proportional scaling.\n    \"\"\"\n    n = centers.shape[0]\n    # wall limits\n    wall = np.minimum.reduce([centers[:, 0], centers[:, 1], 1.0 - centers[:, 0], 1.0 - centers[:, 1]])\n    wall = np.maximum(wall, 0.0)\n    # pairwise distances\n    diff = centers[:, None, :] - centers[None, :, :]\n    dists = np.sqrt(np.sum(diff * diff, axis=2))\n    np.fill_diagonal(dists, np.inf)\n\n    # start from zeros (this encourages growth to fill available slack)\n    r = np.zeros(n, dtype=float)\n\n    for it in range(max_iters):\n        max_change = 0.0\n        # iterate circles in fixed order (deterministic)\n        for i in range(n):\n            # compute the tightest upper bound from neighbors given current r_j\n            # allowed = min_j (d_ij - r_j)\n            limits = dists[i, :] - r\n            # ignore self (dists diag is inf)\n            allowed = np.min(limits)\n            new_ri = min(wall[i], allowed)\n            if new_ri < 0.0:\n                new_ri = 0.0\n            change = abs(new_ri - r[i])\n            if change > 0:\n                r[i] = new_ri\n                if change > max_change:\n                    max_change = change\n        if max_change < tol:\n            break\n\n    # final safety: enforce pairwise constraints by proportional reductions where violated\n    # multiple passes to fully remove any remaining tiny violations\n    for _ in range(4):\n        changed = False\n        for i in range(n):\n            for j in range(i + 1, n):\n                dij = dists[i, j]\n                if r[i] + r[j] > dij:\n                    tot = r[i] + r[j]\n                    if tot <= 0:\n                        r[i] = r[j] = 0.0\n                    else:\n                        scale = dij / tot\n                        new_i = r[i] * scale\n                        new_j = r[j] * scale\n                        if new_i != r[i] or new_j != r[j]:\n                            r[i], r[j] = new_i, new_j\n                            changed = True\n        if not changed:\n            break\n\n    # enforce wall limits and non-negativity\n    r = np.clip(r, 0.0, wall)\n    return r\n\n# EVOLVE-BLOCK-END\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.65, edgecolor=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (1.9637), Performs well on target_ratio (0.7452), Performs well on combined_score (0.7452), Performs well on eval_time (0.0759)\n\n\n\n## Diverse Programs\n\n### Program D1 (Score: 0.7378)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (improved explicit constructor)\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square.\n    Uses a compact hexagonal-like layout adapted to the square, then\n    computes maximal feasible radii via an iterative constraint enforcement\n    that tries to maximize the sum of radii (heuristic but effective).\n    Returns:\n        centers: np.array (26,2)\n        radii:   np.array (26,)\n        sum_radii: float\n    \"\"\"\n    n = 26\n\n    # Choose a left/right margin and compute horizontal spacing so that\n    # the densest (6-point) row spans [m, 1-m].\n    m = 0.04\n    s = (1.0 - 2.0 * m) / 5.0           # horizontal spacing for up to 6 points\n    v = s * (np.sqrt(3.0) / 2.0)       # vertical spacing for hex packing\n\n    # Row structure (counts sum to 26)\n    row_counts = [5, 6, 5, 6, 4]  # 5 rows -> 5+6+5+6+4 = 26\n    rows = len(row_counts)\n\n    # center the vertical stack in the unit square\n    total_h = (rows - 1) * v\n    y0 = (1.0 - total_h) / 2.0\n\n    centers = []\n    for r, count in enumerate(row_counts):\n        y = y0 + r * v\n        if count == 6:\n            # 6 points: start at m, step s\n            xs = [m + i * s for i in range(6)]\n        elif count == 5:\n            # 5 points: offset by s/2 for hex packing\n            xs = [m + s / 2.0 + i * s for i in range(5)]\n        elif count == 4:\n            # 4 points: center them (offset a bit more)\n            xs = [m + s + i * s for i in range(4)]\n        else:\n            # general fallback (shouldn't happen here)\n            xs = [m + i * s for i in range(count)]\n        for x in xs:\n            centers.append((x, y))\n\n    centers = np.array(centers[:n], dtype=float)\n\n    # Clip to ensure tiny numerical issues don't push a center out of [0,1]\n    centers = np.clip(centers, 1e-6, 1.0 - 1e-6)\n\n    radii = compute_max_radii(centers)\n    sum_radii = float(np.sum(radii))\n\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers):\n    \"\"\"\n    Heuristic solver for the linear program:\n        maximize sum r_i\n        s.t. 0 <= r_i <= b_i (distance to border),\n             r_i + r_j <= d_ij for all i != j\n    Approach:\n      - initialize r_i = distance to nearest border\n      - repeatedly enforce pair constraints by reducing the larger radius\n        in any violating pair until no violations remain (or max iters).\n    This greedy reduction tends to produce a large total radius sum and\n    is fast and deterministic.\n    \"\"\"\n    n = centers.shape[0]\n    # initial upper bounds by border distances\n    radii = np.minimum.reduce([\n        centers[:, 0], centers[:, 1], 1.0 - centers[:, 0], 1.0 - centers[:, 1]\n    ]).copy()\n\n    eps = 1e-9\n    max_iters = 2000\n\n    # Precompute pairwise distances\n    diffs = centers[:, None, :] - centers[None, :, :]\n    dists = np.sqrt(np.sum(diffs * diffs, axis=2))\n    # Make diagonal large to avoid self-constraint\n    np.fill_diagonal(dists, np.inf)\n\n    for it in range(max_iters):\n        changed = False\n        # scan pairs; reduce the larger radius when r_i + r_j > d_ij\n        for i in range(n):\n            ri = radii[i]\n            # vectorized over j\n            # find j where constraint violated\n            # r_i + r_j > d_ij  =>  r_j > d_ij - r_i\n            limits = dists[i] - ri\n            viol = np.where(radii > limits + 1e-12)[0]\n            for j in viol:\n                if j == i:\n                    continue\n                # ensure non-negative target\n                target = max(0.0, dists[i, j] - radii[j])\n                # decide which to reduce: reduce the larger one (greedy)\n                if radii[i] > radii[j] + eps:\n                    if target < radii[i] - eps:\n                        radii[i] = target\n                        changed = True\n                else:\n                    # reduce j instead\n                    target_j = max(0.0, dists[i, j] - radii[i])\n                    if target_j < radii[j] - eps:\n                        radii[j] = target_j\n                        changed = True\n        if not changed:\n            break\n\n    # Final safety: enforce border and pairwise constraints strictly\n    radii = np.minimum(radii, np.minimum.reduce([\n        centers[:, 0], centers[:, 1], 1.0 - centers[:, 0], 1.0 - centers[:, 1]\n    ]))\n    for i in range(n):\n        for j in range(i + 1, n):\n            if radii[i] + radii[j] > dists[i, j]:\n                # split remaining allowance proportionally to current radii (stable)\n                tot = radii[i] + radii[j]\n                if tot <= 0:\n                    radii[i] = radii[j] = 0.0\n                else:\n                    scale = dists[i, j] / tot\n                    radii[i] *= scale\n                    radii[j] *= scale\n\n    return radii\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.5)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    visualize(centers, radii)\n```\nKey features: Alternative approach to validity, Alternative approach to sum_radii\n\n### Program D2 (Score: 0.7280)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for 26 circles in the unit square using tuned hex-like rows.\nSearches a small range of lattice spacings and picks the layout that yields the largest\nsum of radii under non-overlap and boundary constraints.\n\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    n = 26\n    counts = [5, 6, 5, 5, 5]  # rows summing to 26\n    rows = len(counts)\n    max_cols = max(counts)\n\n    margin = 0.01\n    # allowable horizontal spacing range (upper bound from width, lower bound small)\n    a_max = (1.0 - 2 * margin) / (max_cols - 1)\n    a_min = 0.03\n    if a_min >= a_max:\n        a_min = a_max * 0.5\n\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    # sample several spacings to accommodate vertical fit and edge effects\n    for a in np.linspace(a_max, a_min,  forty := 40):\n        h = a * np.sqrt(3.0) / 2.0\n        # check vertical fit\n        if (rows - 1) * h > (1.0 - 2 * margin):\n            continue\n\n        centers = np.zeros((n, 2))\n        idx = 0\n        y0 = (1.0 - (rows - 1) * h) / 2.0\n        for r, cnt in enumerate(counts):\n            y = y0 + r * h\n            offset = (a / 2.0) if cnt < max_cols else 0.0\n            row_width = (cnt - 1) * a\n            x0 = (1.0 - row_width) / 2.0 + offset\n            for c in range(cnt):\n                if idx >= n:\n                    break\n                x = x0 + c * a\n                # keep strictly inside boundaries by a tiny epsilon\n                centers[idx, 0] = np.clip(x, margin, 1.0 - margin)\n                centers[idx, 1] = np.clip(y, margin, 1.0 - margin)\n                idx += 1\n\n        # compute maximal uniform scaling radii for these centers\n        radii = compute_max_radii(centers)\n        s = float(np.sum(radii))\n        if s > best_sum:\n            best_sum = s\n            best_centers = centers.copy()\n            best_radii = radii.copy()\n\n    # fallback: if no candidate found (very unlikely), create one with a_max\n    if best_centers is None:\n        a = a_max\n        h = a * np.sqrt(3.0) / 2.0\n        centers = np.zeros((n, 2))\n        idx = 0\n        y0 = (1.0 - (rows - 1) * h) / 2.0\n        for r, cnt in enumerate(counts):\n            y = y0 + r * h\n            offset = (a / 2.0) if cnt < max_cols else 0.0\n            row_width = (cnt - 1) * a\n            x0 = (1.0 - row_width) / 2.0 + offset\n            for c in range(cnt):\n                if idx >= n:\n                    break\n                centers[idx] = [np.clip(x0 + c * a, margin, 1.0 - margin), np.clip(y, margin, 1.0 - margin)]\n                idx += 1\n        best_centers = centers\n        best_radii = compute_max_radii(centers)\n        best_sum = float(np.sum(best_radii))\n\n    return best_centers, best_radii, float(best_sum)\n\n\ndef compute_max_radii(centers):\n    \"\"\"\n    Given fixed centers, compute per-circle radii by:\n      1) taking conservative per-circle limits: distance to wall and half minimum inter-center distance;\n      2) computing the largest uniform scale s (<=1) that preserves wall and pairwise constraints;\n      3) applying a tiny safety shrink.\n    This yields non-overlapping circles and is deterministic.\n    \"\"\"\n    n = centers.shape[0]\n    # distance to nearest wall\n    d_wall = np.minimum.reduce([centers[:, 0], 1.0 - centers[:, 0], centers[:, 1], 1.0 - centers[:, 1]])\n    # pairwise distances\n    dif = centers[:, None, :] - centers[None, :, :]\n    dists = np.sqrt(np.sum(dif * dif, axis=2))\n    # avoid self-zero by setting diag to large\n    np.fill_diagonal(dists, np.inf)\n    half_min = 0.5 * np.min(dists, axis=1)\n\n    # initial conservative radii\n    radii = np.minimum(d_wall, half_min)\n    radii = np.maximum(radii, 1e-12)\n\n    # compute max uniform scaling s so that s*r_i <= d_wall[i] and s*(r_i + r_j) <= dists[i,j]\n    with np.errstate(divide='ignore', invalid='ignore'):\n        s_wall = d_wall / radii\n        ri_rj = radii[:, None] + radii[None, :]\n        s_pairs = dists / ri_rj\n    s_wall[np.isnan(s_wall)] = np.inf\n    s_pairs[np.isnan(s_pairs)] = np.inf\n    s_wall_min = np.min(s_wall)\n    # consider only upper triangle of pairs\n    iu = np.triu_indices(n, k=1)\n    if iu[0].size > 0:\n        s_pairs_min = np.min(s_pairs[iu])\n    else:\n        s_pairs_min = np.inf\n\n    s_max = min(s_wall_min, s_pairs_min)\n    if not np.isfinite(s_max) or s_max <= 0.0:\n        s = 1.0\n    else:\n        # allow small expansion up to s_max but cap at 1.0 (don't blow up initial conservative radii)\n        s = min(1.0, float(s_max)) * 0.9995\n\n    radii = radii * s\n    return radii\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nKey features: Alternative approach to validity, Alternative approach to sum_radii\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 0.6490, Type: Alternative)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (improved explicit constructor)\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square\n    using an explicit hex-like layered layout and a robust radius\n    relaxation that maximizes radii subject to non-overlap and border constraints.\n\n    Returns:\n        Tuple of (centers, radii, sum_of_radii)\n    \"\"\"\n    n = 26\n    centers = np.zeros((n, 2))\n\n    # We choose a layered, roughly hexagonal layout (5 rows with counts 5,6,6,6,3)\n    # This balances interior density with edge accommodation.\n    row_counts = [5, 6, 6, 6, 3]\n    num_rows = len(row_counts)\n\n    # Small margin from the square boundary to avoid degenerate zero border distances\n    margin = 0.06\n\n    # Vertical positions evenly spaced between margin and 1-margin\n    ys = np.linspace(margin, 1.0 - margin, num_rows)\n\n    # For horizontal positioning we use two spacings so rows with 6 are denser.\n    # This produces alternating offsets similar to a hex lattice.\n    x_dense = np.linspace(margin, 1.0 - margin, 6)\n    # For rows with fewer points center them in the same footprint\n    idx = 0\n    for r, y in enumerate(ys):\n        k = row_counts[r]\n        if k == 6:\n            # alternate offset to mimic hex packing (shift every other 6-row)\n            offset = (0.5 * (x_dense[1] - x_dense[0])) if (r % 2 == 1) else 0.0\n            xs = x_dense + offset\n            # clip to remain inside [margin, 1-margin]\n            xs = np.clip(xs, margin, 1.0 - margin)\n        else:\n            # center k points within the same horizontal interval\n            xs = np.linspace(margin + 0.5 * (x_dense[1] - x_dense[0]),\n                             1.0 - margin - 0.5 * (x_dense[1] - x_dense[0]),\n                             k)\n        for x in xs:\n            if idx < n:\n                centers[idx, 0] = x\n                centers[idx, 1] = y\n                idx += 1\n    # Safety: if due to indexing we didn't fill all, fill remaining near center\n    while idx < n:\n        centers[idx] = [0.5, 0.5]\n        idx += 1\n\n    # Compute maximal feasible radii for these fixed centers\n    radii = compute_max_radii(centers)\n\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers, tol=1e-6, max_iter=10000):\n    \"\"\"\n    Compute maximal radii for fixed centers inside unit square:\n    - r_i <= distance_to_border(center_i)\n    - for each pair i,j: r_i + r_j <= dist(center_i, center_j)\n\n    We use an iterative relaxation: start with border-limited radii,\n    then repeatedly repair pairwise violations by shrinking the larger\n    circle in any conflicting pair. This greedy-reduction converges\n    quickly and yields a feasible (often near-maximal) solution.\n    \"\"\"\n    n = centers.shape[0]\n    # initial radii limited by border distance\n    radii = np.minimum(centers[:, 0], centers[:, 1])\n    radii = np.minimum(radii, 1.0 - centers[:, 0])\n    radii = np.minimum(radii, 1.0 - centers[:, 1])\n\n    if n <= 1:\n        return radii\n\n    # Precompute pairwise distances\n    diff = centers[:, None, :] - centers[None, :, :]\n    dists = np.sqrt(np.sum(diff * diff, axis=2))\n    # Prevent self-dist being zero from causing division problems\n    np.fill_diagonal(dists, np.inf)\n\n    it = 0\n    while it < max_iter:\n        it += 1\n        max_violation = 0.0\n        # For each unordered pair check overlap\n        for i in range(n):\n            # vectorized over j could be faster, but explicit loop keeps control of shrinking policy\n            for j in range(i + 1, n):\n                dij = dists[i, j]\n                if dij <= 0:\n                    # identical centers -> force one to zero to break symmetry\n                    if radii[j] > radii[i]:\n                        radii[j] = 0.0\n                    else:\n                        radii[i] = 0.0\n                    continue\n                s = radii[i] + radii[j]\n                if s > dij + tol:\n                    # violation amount\n                    viol = s - dij\n                    max_violation = max(max_violation, viol)\n                    # shrink the larger radius preferentially (greedy)\n                    if radii[i] >= radii[j]:\n                        # new ri = max(dij - rj, 0), but also not larger than border distance (already enforced)\n                        new_ri = max(dij - radii[j], 0.0)\n                        # numerical safety clamp\n                        if new_ri < radii[i]:\n                            radii[i] = new_ri\n                    else:\n                        new_rj = max(dij - radii[i], 0.0)\n                        if new_rj < radii[j]:\n                            radii[j] = new_rj\n\n        # After pairwise repairs, ensure border limits still hold (they shouldn't increase)\n        border_limits = np.minimum.reduce([\n            centers[:, 0],\n            centers[:, 1],\n            1.0 - centers[:, 0],\n            1.0 - centers[:, 1],\n        ])\n        # numerical safety: never exceed border limits\n        radii = np.minimum(radii, border_limits)\n\n        if max_violation <= tol:\n            break\n\n    # Final pass: small positive clamp\n    radii = np.maximum(radii, 0.0)\n    return radii\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\", linewidth=0.5)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    visualize(centers, radii)\n```\nUnique approach: Modification: Full rewrite, Excellent validity (1.000), Excellent sum_radii (1.710)\n\n### Inspiration 2 (Score: 0.7378, Type: Alternative)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (improved explicit constructor)\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square.\n    Uses a compact hexagonal-like layout adapted to the square, then\n    computes maximal feasible radii via an iterative constraint enforcement\n    that tries to maximize the sum of radii (heuristic but effective).\n    Returns:\n        centers: np.array (26,2)\n        radii:   np.array (26,)\n        sum_radii: float\n    \"\"\"\n    n = 26\n\n    # Choose a left/right margin and compute horizontal spacing so that\n    # the densest (6-point) row spans [m, 1-m].\n    m = 0.04\n    s = (1.0 - 2.0 * m) / 5.0           # horizontal spacing for up to 6 points\n    v = s * (np.sqrt(3.0) / 2.0)       # vertical spacing for hex packing\n\n    # Row structure (counts sum to 26)\n    row_counts = [5, 6, 5, 6, 4]  # 5 rows -> 5+6+5+6+4 = 26\n    rows = len(row_counts)\n\n    # center the vertical stack in the unit square\n    total_h = (rows - 1) * v\n    y0 = (1.0 - total_h) / 2.0\n\n    centers = []\n    for r, count in enumerate(row_counts):\n        y = y0 + r * v\n        if count == 6:\n            # 6 points: start at m, step s\n            xs = [m + i * s for i in range(6)]\n        elif count == 5:\n            # 5 points: offset by s/2 for hex packing\n            xs = [m + s / 2.0 + i * s for i in range(5)]\n        elif count == 4:\n            # 4 points: center them (offset a bit more)\n            xs = [m + s + i * s for i in range(4)]\n        else:\n            # general fallback (shouldn't happen here)\n            xs = [m + i * s for i in range(count)]\n        for x in xs:\n            centers.append((x, y))\n\n    centers = np.array(centers[:n], dtype=float)\n\n    # Clip to ensure tiny numerical issues don't push a center out of [0,1]\n    centers = np.clip(centers, 1e-6, 1.0 - 1e-6)\n\n    radii = compute_max_radii(centers)\n    sum_radii = float(np.sum(radii))\n\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers):\n    \"\"\"\n    Heuristic solver for the linear program:\n        maximize sum r_i\n        s.t. 0 <= r_i <= b_i (distance to border),\n             r_i + r_j <= d_ij for all i != j\n    Approach:\n      - initialize r_i = distance to nearest border\n      - repeatedly enforce pair constraints by reducing the larger radius\n        in any violating pair until no violations remain (or max iters).\n    This greedy reduction tends to produce a large total radius sum and\n    is fast and deterministic.\n    \"\"\"\n    n = centers.shape[0]\n    # initial upper bounds by border distances\n    radii = np.minimum.reduce([\n        centers[:, 0], centers[:, 1], 1.0 - centers[:, 0], 1.0 - centers[:, 1]\n    ]).copy()\n\n    eps = 1e-9\n    max_iters = 2000\n\n    # Precompute pairwise distances\n    diffs = centers[:, None, :] - centers[None, :, :]\n    dists = np.sqrt(np.sum(diffs * diffs, axis=2))\n    # Make diagonal large to avoid self-constraint\n    np.fill_diagonal(dists, np.inf)\n\n    for it in range(max_iters):\n        changed = False\n        # scan pairs; reduce the larger radius when r_i + r_j > d_ij\n        for i in range(n):\n            ri = radii[i]\n            # vectorized over j\n            # find j where constraint violated\n            # r_i + r_j > d_ij  =>  r_j > d_ij - r_i\n            limits = dists[i] - ri\n            viol = np.where(radii > limits + 1e-12)[0]\n            for j in viol:\n                if j == i:\n                    continue\n                # ensure non-negative target\n                target = max(0.0, dists[i, j] - radii[j])\n                # decide which to reduce: reduce the larger one (greedy)\n                if radii[i] > radii[j] + eps:\n                    if target < radii[i] - eps:\n                        radii[i] = target\n                        changed = True\n                else:\n                    # reduce j instead\n                    target_j = max(0.0, dists[i, j] - radii[i])\n                    if target_j < radii[j] - eps:\n                        radii[j] = target_j\n                        changed = True\n        if not changed:\n            break\n\n    # Final safety: enforce border and pairwise constraints strictly\n    radii = np.minimum(radii, np.minimum.reduce([\n        centers[:, 0], centers[:, 1], 1.0 - centers[:, 0], 1.0 - centers[:, 1]\n    ]))\n    for i in range(n):\n        for j in range(i + 1, n):\n            if radii[i] + radii[j] > dists[i, j]:\n                # split remaining allowance proportionally to current radii (stable)\n                tot = radii[i] + radii[j]\n                if tot <= 0:\n                    radii[i] = radii[j] = 0.0\n                else:\n                    scale = dists[i, j] / tot\n                    radii[i] *= scale\n                    radii[j] *= scale\n\n    return radii\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.5)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    visualize(centers, radii)\n```\nUnique approach: Modification: Full rewrite, Excellent validity (1.000), Excellent sum_radii (1.944)\n\n### Inspiration 3 (Score: 0.4464, Type: Experimental)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (hex-like layout)\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square\n    using a compact hexagonal-like layering (rows: 5,6,5,5,5). This\n    explicit placement yields much larger total radii than the\n    previous ring-based layout.\n    Returns:\n        centers: (26,2) array of (x,y)\n        radii:   (26,) array of radii (maximized given centers)\n        sum_radii: sum of radii\n    \"\"\"\n    n = 26\n    rows = [5, 6, 5, 5, 5]  # totals 26\n    # Choose a uniform target radius so that the widest row (6) fits exactly:\n    r_target = 1.0 / (2 * 6)  # a = 2*r_target spacing horizontally\n    v = np.sqrt(3.0) * r_target  # vertical center spacing for hex packing\n\n    centers = np.zeros((n, 2))\n    idx = 0\n\n    # Place rows top-to-bottom starting at y = r_target\n    y0 = r_target\n    for row_i, m in enumerate(rows):\n        y = y0 + row_i * v\n        # center each row horizontally\n        row_width = 2 * r_target * m\n        x_start = (1.0 - row_width) / 2.0 + r_target\n        for j in range(m):\n            x = x_start + j * 2 * r_target\n            centers[idx] = [x, y]\n            idx += 1\n\n    # Compute maximal feasible radii for these centers\n    radii = compute_max_radii(centers)\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers, max_iter=500, tol=1e-10):\n    \"\"\"\n    Given centers, compute maximal radii satisfying:\n      - each circle inside unit square: ri <= min(x, y, 1-x, 1-y)\n      - pairwise non-overlap: ri + rj <= dist(center_i, center_j)\n    We use an iterative projection-style method that\n    initializes by border distances and repeatedly enforces\n    pairwise constraints (scaling conflicting pairs) until convergence.\n    \"\"\"\n    n = centers.shape[0]\n    # distance to borders\n    border = np.minimum.reduce([centers[:, 0], centers[:, 1], 1 - centers[:, 0], 1 - centers[:, 1]])\n    radii = border.copy()\n\n    # Precompute pairwise distances\n    dif = centers[:, None, :] - centers[None, :, :]\n    dists = np.sqrt(np.sum(dif * dif, axis=2)) + np.eye(n)  # diagonal nonzero to avoid div0\n\n    for it in range(max_iter):\n        max_change = 0.0\n        # enforce pairwise constraints by scaling pairs that overlap\n        for i in range(n):\n            for j in range(i + 1, n):\n                d = dists[i, j]\n                if d <= 0:\n                    continue\n                s = radii[i] + radii[j]\n                if s > d:\n                    # scale both proportionally to avoid bias\n                    if s > 0:\n                        scale = d / s\n                        new_i = radii[i] * scale\n                        new_j = radii[j] * scale\n                    else:\n                        new_i = new_j = 0.0\n                    # apply and track max change\n                    max_change = max(max_change, abs(radii[i] - new_i), abs(radii[j] - new_j))\n                    radii[i], radii[j] = new_i, new_j\n        # re-impose border constraints (they may get violated by scaling)\n        over = radii > border\n        if np.any(over):\n            delta = np.max(np.abs(radii[over] - border[over]))\n            radii[over] = border[over]\n            max_change = max(max_change, float(delta))\n        if max_change < tol:\n            break\n    # ensure non-negative small numerical corrections\n    radii = np.maximum(radii, 0.0)\n    return radii\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nUnique approach: Modification: Full rewrite, Excellent validity (1.000), Excellent sum_radii (1.176)\n\n# Current Program\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for 26 circles in the unit square using a tuned hex-like layout.\nCenters are placed in rows [6,5,6,5,4], then radii are found by a fast greedy constraint-propagation\nthat enforces border and pairwise non-overlap constraints.\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    rows = [6, 5, 6, 5, 4]\n    m = len(rows)\n    kmax = max(rows)\n    sqrt3 = np.sqrt(3.0)\n\n    # compute an initial equal-radius hexagonal spacing that fits in unit square\n    r_h = 1.0 / (2.0 * kmax)                      # horizontal limit from widest row\n    r_v = 1.0 / (2.0 + (m - 1) * sqrt3)          # vertical limit from stacked rows\n    base_r = min(r_h, r_v)\n\n    # vertical layout\n    dy = base_r * sqrt3\n    vert_span = 2.0 * base_r + (m - 1) * dy\n    y0 = (1.0 - vert_span) / 2.0 + base_r\n\n    centers = []\n    for i, k in enumerate(rows):\n        # horizontal span for k circles of diameter 2*base_r\n        row_span = 2.0 * base_r * k\n        x0 = (1.0 - row_span) / 2.0 + base_r\n        # alternate offset for hex-like staggering on every other row with fewer entries\n        offset = 0.0\n        if k < kmax:\n            offset = base_r  # half-step = base_r\n        xs = x0 + np.arange(k) * (2.0 * base_r) + offset\n        y = y0 + i * dy\n        for x in xs:\n            centers.append((x, y))\n\n    centers = np.array(centers[:n], dtype=float)\n    assert centers.shape[0] == n\n\n    radii = compute_max_radii(centers)\n    return centers, radii, float(np.sum(radii))\n\ndef compute_max_radii(centers, tol=1e-10, max_iters=5000):\n    \"\"\"\n    Greedy iterative repair:\n      - start with each radius = distance to nearest border\n      - for each violating pair (ri + rj > d_ij) reduce the larger radius to d_ij - the_other\n    This is deterministic, fast, and works well for hex-like seeds.\n    \"\"\"\n    n = centers.shape[0]\n    if n == 0:\n        return np.array([])\n\n    # initial radii limited by border distances\n    radii = np.minimum.reduce([centers[:,0], centers[:,1], 1.0-centers[:,0], 1.0-centers[:,1]]).copy()\n    radii = np.maximum(radii, 0.0)\n\n    # pairwise distances\n    diff = centers[:, None, :] - centers[None, :, :]\n    dists = np.sqrt(np.sum(diff * diff, axis=2))\n    np.fill_diagonal(dists, np.inf)\n\n    for _ in range(max_iters):\n        max_change = 0.0\n        # scan pairs\n        for i in range(n):\n            ri = radii[i]\n            # vectorize check for speed\n            s = ri + radii\n            viol = np.where(s > dists[i] + tol)[0]\n            for j in viol:\n                if j == i:\n                    continue\n                dij = dists[i, j]\n                # reduce the larger radius\n                if radii[i] >= radii[j]:\n                    new_ri = max(0.0, dij - radii[j])\n                    change = ri - new_ri\n                    if change > 0:\n                        radii[i] = new_ri\n                        max_change = max(max_change, change)\n                        ri = new_ri\n                else:\n                    new_rj = max(0.0, dij - radii[i])\n                    change = radii[j] - new_rj\n                    if change > 0:\n                        radii[j] = new_rj\n                        max_change = max(max_change, change)\n        # enforce border limits (shouldn't increase radii)\n        border = np.minimum.reduce([centers[:,0], centers[:,1], 1.0-centers[:,0], 1.0-centers[:,1]])\n        radii = np.minimum(radii, border)\n        if max_change < tol:\n            break\n\n    # final safety: ensure all pairwise constraints hold (fix tiny numerical issues)\n    for i in range(n):\n        for j in range(i+1, n):\n            dij = dists[i,j]\n            if radii[i] + radii[j] > dij:\n                tot = radii[i] + radii[j]\n                if tot == 0:\n                    radii[i] = radii[j] = 0.0\n                else:\n                    scale = dij / tot\n                    radii[i] *= scale\n                    radii[j] *= scale\n\n    radii = np.maximum(radii, 0.0)\n    return radii\n\n# Fixed API\ndef run_packing():\n    centers, radii, s = construct_packing()\n    return centers, radii, s\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(6,6))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect(\"equal\")\n    ax.add_patch(plt.Rectangle((0,0),1,1,fill=False,linewidth=1))\n    for c,r in zip(centers, radii):\n        ax.add_patch(Circle(c, r, alpha=0.6, edgecolor='k'))\n    plt.title(f\"n={len(centers)}, sum={sum(radii):.6f}\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # visualize(centers, radii)\n# EVOLVE-BLOCK-END\n```\n\n# Task\nRewrite the program to improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: complexity, diversity\nDifferent solutions with similar fitness but different features are valuable.\nProvide the complete new program code.\n\nIMPORTANT: Make sure your rewritten program maintains the same inputs and outputs\nas the original program, but with improved internal implementation.\n\n```python\n# Your rewritten program here\n```", "responses": ["```python\n# EVOLVE-BLOCK-START\n\"\"\"Improved deterministic constructor for 26 circles in the unit square.\n\nKey ideas:\n- Use the hex-like row pattern [6,5,6,5,4].\n- Sample a small grid of horizontal spacings, small global x-shifts and two offset modes\n  (shorter rows either centered or half-step shifted) and slight vertical compression/stretch.\n- For each candidate placement compute radii with an enhanced constraint solver:\n    * start from border limits\n    * alternate reduction sweeps (repair violating pairs by shrinking the larger)\n      and growth sweeps (increase each radius to the largest allowed by current neighbors)\n    * finalize with proportional pairwise fixes\n  This alternation lets radii both relax down when needed and regrow to better fill free space,\n  producing larger total radii than greedy-only reductions.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    rows = [6, 5, 6, 5, 4]\n    m = len(rows)\n    kmax = max(rows)\n    # allowable horizontal spacing: ensure widest row fits\n    a_max = 1.0 / (kmax - 1)  # (kmax-1)*a <= 1\n    a_min = 0.035\n    if a_min >= a_max:\n        a_min = a_max * 0.6\n\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    sqrt3 = np.sqrt(3.0)\n\n    # sample horizontal spacings more densely near a_max\n    for a in np.linspace(a_max * 0.98, a_min, 20):\n        # nominal hex vertical step (can scale slightly)\n        v0 = a * sqrt3 / 2.0\n        for v_scale in (0.96, 0.98, 1.0, 1.02):\n            v = v0 * v_scale\n            total_h = (m - 1) * v\n            if total_h > 1.0:\n                continue\n            # two offset modes: 0 or half-step for shorter rows\n            for short_offset_half in (False, True):\n                # small global horizontal shifts to allow better edge packing\n                for dx in np.linspace(-0.02, 0.02, 5):\n                    centers = np.zeros((n, 2))\n                    idx = 0\n                    y0 = (1.0 - total_h) / 2.0\n                    ok = True\n                    for r_idx, cnt in enumerate(rows):\n                        y = y0 + r_idx * v\n                        # compute offset for this row\n                        if cnt < kmax and short_offset_half:\n                            offset = a / 2.0\n                        else:\n                            offset = 0.0\n                        row_width = (cnt - 1) * a\n                        x0 = (1.0 - row_width) / 2.0 + offset + dx\n                        # place points and ensure inside [0,1] (clamp edges)\n                        for c in range(cnt):\n                            if idx >= n:\n                                break\n                            x = x0 + c * a\n                            if x <= 0.0 + 1e-9 or x >= 1.0 - 1e-9 or y <= 0.0 + 1e-9 or y >= 1.0 - 1e-9:\n                                # allow small clamping but if center would be too far out, mark not ok\n                                x = np.clip(x, 1e-9, 1.0 - 1e-9)\n                                y = np.clip(y, 1e-9, 1.0 - 1e-9)\n                            centers[idx, 0] = x\n                            centers[idx, 1] = y\n                            idx += 1\n                    if idx < n:\n                        ok = False\n                    if not ok:\n                        continue\n                    radii = maximize_radii_refined(centers)\n                    s = float(np.sum(radii))\n                    if s > best_sum:\n                        best_sum = s\n                        best_centers = centers.copy()\n                        best_radii = radii.copy()\n\n    # Fallback deterministic center if sampling failed\n    if best_centers is None:\n        # simple centered hex-like placement\n        a = a_max * 0.98\n        v = a * sqrt3 / 2.0\n        centers = []\n        y0 = (1.0 - (m - 1) * v) / 2.0\n        for r_idx, cnt in enumerate(rows):\n            y = y0 + r_idx * v\n            offset = (a / 2.0) if (cnt < kmax) else 0.0\n            row_width = (cnt - 1) * a\n            x0 = (1.0 - row_width) / 2.0 + offset\n            for c in range(cnt):\n                centers.append((np.clip(x0 + c * a, 1e-9, 1.0 - 1e-9), y))\n        best_centers = np.array(centers[:n], dtype=float)\n        best_radii = maximize_radii_refined(best_centers)\n        best_sum = float(np.sum(best_radii))\n\n    return best_centers, best_radii, float(best_sum)\n\n\ndef maximize_radii_refined(centers, tol=1e-9, max_cycles=1000):\n    \"\"\"\n    Alternating repair/increase solver for the LP:\n      maximize sum r_i\n      s.t. 0 <= r_i <= wall_i, r_i + r_j <= d_ij.\n\n    Algorithm:\n      - initialize r = wall (border-limited)\n      - repeat cycles:\n          1) reduction sweep: for every violating pair shrink the larger to d_ij - other\n          2) growth sweep: for each i increase r_i to min(wall_i, min_j(d_ij - r_j))\n      - finish with proportional pairwise fixes\n    This tends to reach a large feasible sum and is deterministic.\n    \"\"\"\n    n = centers.shape[0]\n    if n == 0:\n        return np.array([])\n\n    wall = np.minimum.reduce([centers[:, 0], centers[:, 1],\n                              1.0 - centers[:, 0], 1.0 - centers[:, 1]])\n    wall = np.maximum(wall, 0.0)\n\n    diff = centers[:, None, :] - centers[None, :, :]\n    dists = np.sqrt(np.sum(diff * diff, axis=2))\n    np.fill_diagonal(dists, np.inf)\n\n    # start from border-limited radii\n    r = wall.copy()\n\n    for cycle in range(max_cycles):\n        max_change = 0.0\n        # 1) Reduction sweep (repair violations by shrinking larger)\n        for i in range(n):\n            ri = r[i]\n            # vectorized candidate check\n            s = ri + r\n            viol = np.where(s > dists[i] + 1e-12)[0]\n            for j in viol:\n                if j == i:\n                    continue\n                dij = dists[i, j]\n                # shrink the larger radius\n                if r[i] >= r[j]:\n                    new_ri = max(0.0, dij - r[j])\n                    ch = ri - new_ri\n                    if ch > 0:\n                        r[i] = new_ri\n                        ri = new_ri\n                        if ch > max_change:\n                            max_change = ch\n                else:\n                    new_rj = max(0.0, dij - r[i])\n                    ch = r[j] - new_rj\n                    if ch > 0:\n                        r[j] = new_rj\n                        if ch > max_change:\n                            max_change = ch\n\n        # enforce wall limits (no increases here)\n        over = r > wall\n        if np.any(over):\n            diffv = np.max(r[over] - wall[over])\n            r[over] = wall[over]\n            if diffv > max_change:\n                max_change = diffv\n\n        # 2) Growth sweep: try to grow each circle to the largest allowed by current neighbors\n        for i in range(n):\n            allowed = wall[i]\n            # compute min_j (d_ij - r_j)\n            lims = dists[i, :] - r\n            # ignore self (inf)\n            minlim = np.min(lims)\n            target = min(allowed, minlim)\n            if target < 0.0:\n                target = 0.0\n            ch = target - r[i]\n            if ch > 0:\n                r[i] = target\n                if ch > max_change:\n                    max_change = ch\n\n        if max_change < tol:\n            break\n\n    # Final proportional fixes for any remaining tiny violations\n    for _ in range(4):\n        changed = False\n        for i in range(n):\n            for j in range(i + 1, n):\n                dij = dists[i, j]\n                if r[i] + r[j] > dij:\n                    tot = r[i] + r[j]\n                    if tot <= 0:\n                        if r[i] != 0.0 or r[j] != 0.0:\n                            r[i] = r[j] = 0.0\n                            changed = True\n                    else:\n                        scale = dij / tot\n                        new_i = r[i] * scale\n                        new_j = r[j] * scale\n                        if new_i != r[i] or new_j != r[j]:\n                            r[i], r[j] = new_i, new_j\n                            changed = True\n        if not changed:\n            break\n\n    r = np.clip(r, 0.0, wall)\n    return r\n\n\n# Fixed API\ndef run_packing():\n    centers, radii, s = construct_packing()\n    return centers, radii, s\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(6,6))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect(\"equal\")\n    ax.add_patch(plt.Rectangle((0,0),1,1,fill=False,linewidth=1))\n    for c,r in zip(centers, radii):\n        ax.add_patch(Circle(c, r, alpha=0.6, edgecolor='k'))\n    plt.title(f\"n={len(centers)}, sum={sum(radii):.6f}\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, total = run_packing()\n    print(f\"Sum of radii: {total:.6f}\")\n    # visualize(centers, radii)\n# EVOLVE-BLOCK-END\n```"]}}, "artifacts_json": null, "artifact_dir": null}