{"id": "b0fea2a4-bac8-459f-8f9b-a13589d77055", "code": "# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for n=26 circles in the unit square.\n\nStrategy (compact, effective):\n - Try to build an equal-radius hexagonal grid by binary-searching the largest\n   equal radius r that yields >=26 grid centers inside [r,1-r], for two\n   horizontal alignment choices (no offset or offset=r) and a few vertical shifts.\n - From the best grid (largest r) select 26 centers by prioritizing centers\n   nearer the square center (center-first). Then run a Gauss-Seidel monotone\n   growth routine that sets r_i := min(wall_i, min_j (d_ij - r_j)) in sweeps,\n   producing varied radii and a large total sum.\n - If no grid is found, fall back to a deterministic hex-like row layout.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    sqrt3 = np.sqrt(3.0)\n\n    def gen_grid(r, use_offset_r, vert_shift):\n        pts = []\n        y = r + vert_shift\n        row = 0\n        while y <= 1.0 - r + 1e-12:\n            offset = (r if use_offset_r and (row % 2 == 1) else 0.0)\n            x = r + offset\n            while x <= 1.0 - r + 1e-12:\n                pts.append((x, y))\n                x += 2.0 * r\n            row += 1\n            y += sqrt3 * r\n        if not pts:\n            return np.empty((0,2))\n        pts = np.array(pts)\n        # center-first stable ordering: distance to center, then x\n        d2 = np.sum((pts - 0.5)**2, axis=1)\n        order = np.lexsort((pts[:,0], d2))\n        return pts[order]\n\n    def search_best(use_offset_r):\n        lo, hi = 1e-8, 0.5\n        best_r = 0.0; best_pts = None\n        for _ in range(40):\n            mid = 0.5*(lo+hi)\n            found = False\n            for frac in (0.0, 0.25, 0.5):\n                vs = frac * sqrt3 * mid\n                pts = gen_grid(mid, use_offset_r, vs)\n                if pts.shape[0] >= n:\n                    found = True\n                    break\n            if found:\n                best_r = mid\n                lo = mid\n            else:\n                hi = mid\n        if best_r > 0:\n            # pick a vertical shift that yields the most points / best symmetry\n            best_pts = None\n            for frac in np.linspace(0.0,0.5,9):\n                vs = frac * sqrt3 * best_r\n                pts = gen_grid(best_r, use_offset_r, vs)\n                if pts.shape[0] >= n:\n                    if best_pts is None or pts.shape[0] > best_pts.shape[0]:\n                        best_pts = pts\n            if best_pts is None:\n                best_pts = gen_grid(best_r, use_offset_r, 0.0)\n        return best_r, best_pts\n\n    best_global = {\"r\":0.0, \"pts\":None}\n    for flag in (False, True):\n        r_val, pts = search_best(flag)\n        if pts is not None and r_val > best_global[\"r\"]:\n            best_global[\"r\"] = r_val\n            best_global[\"pts\"] = pts.copy()\n\n    if best_global[\"pts\"] is None:\n        # fallback deterministic hex-like rows pattern [6,5,6,5,4]\n        rows = [6,5,6,5,4]\n        m = len(rows); kmax = max(rows)\n        a = min(1.0/(kmax-1), 2.0/(sqrt3*(m-1))) * 0.98\n        v = a * sqrt3 / 2.0\n        total_h = (m-1)*v\n        y0 = (1.0 - total_h)/2.0\n        centers = []\n        for ridx,cnt in enumerate(rows):\n            y = y0 + ridx * v\n            offset = (a/2.0) if cnt < kmax else 0.0\n            row_w = (cnt-1)*a\n            x0 = (1.0 - row_w)/2.0 + offset\n            for c in range(cnt):\n                centers.append((np.clip(x0 + c*a,1e-12,1-1e-12), y))\n        centers = np.array(centers[:n], dtype=float)\n    else:\n        pts = best_global[\"pts\"]\n        centers = pts[:n].copy()\n        centers = np.clip(centers, 1e-12, 1.0 - 1e-12)\n\n    radii = maximize_radii_gs(centers)\n    return centers, radii, float(np.sum(radii))\n\ndef maximize_radii_gs(centers, tol=1e-10, max_iters=5000):\n    n = centers.shape[0]\n    if n == 0:\n        return np.array([])\n    wall = np.minimum.reduce([centers[:,0], centers[:,1], 1.0-centers[:,0], 1.0-centers[:,1]])\n    wall = np.maximum(wall, 0.0)\n    diff = centers[:,None,:] - centers[None,:,:]\n    dists = np.sqrt(np.maximum((diff**2).sum(axis=2), 0.0))\n    np.fill_diagonal(dists, np.inf)\n    r = np.zeros(n, dtype=float)\n    order = np.argsort(np.sum((centers-0.5)**2, axis=1))  # center-first\n    for it in range(max_iters):\n        max_ch = 0.0\n        seq = order if (it%2==0) else order[::-1]\n        for i in seq:\n            limits = dists[i] - r\n            allowed = float(np.min(limits))\n            newr = min(wall[i], allowed)\n            if newr < 0.0:\n                newr = 0.0\n            ch = abs(newr - r[i])\n            if ch > 0:\n                r[i] = newr\n                if ch > max_ch: max_ch = ch\n        if max_ch < tol:\n            break\n    # proportional cleanup for tiny violations\n    for _ in range(6):\n        changed = False\n        for i in range(n):\n            for j in range(i+1,n):\n                dij = dists[i,j]\n                if r[i] + r[j] > dij + 1e-12:\n                    tot = r[i] + r[j]\n                    if tot <= 0:\n                        if r[i] != 0.0 or r[j] != 0.0:\n                            r[i]=r[j]=0.0; changed=True\n                    else:\n                        s = dij / tot\n                        ni, nj = r[i]*s, r[j]*s\n                        if ni != r[i] or nj != r[j]:\n                            r[i], r[j] = ni, nj\n                            changed = True\n        r = np.minimum(r, wall)\n        if not changed:\n            break\n    r = np.clip(r, 0.0, wall)\n    return r\n\n# EVOLVE-BLOCK-END\n\n# Fixed runner and visualization\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(7,7))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect(\"equal\")\n    ax.add_patch(plt.Rectangle((0,0),1,1,fill=False,edgecolor='k'))\n    for c,r in zip(centers, radii):\n        ax.add_patch(Circle(c, r, alpha=0.6, edgecolor='k'))\n    plt.title(f\"n={len(centers)}, sum radii={sum(radii):.6f}\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, total = run_packing()\n    print(f\"Sum of radii: {total:.6f}\")\n    # visualize(centers, radii)", "language": "python", "parent_id": "f91f8196-e27a-4e21-a54c-496d2cb55529", "generation": 4, "timestamp": 1759950635.912686, "iteration_found": 44, "metrics": {"validity": 1.0, "sum_radii": 1.837011123255163, "target_ratio": 0.6971579215389613, "combined_score": 0.6971579215389613, "eval_time": 0.05872082710266113}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Full rewrite", "parent_metrics": {"validity": 1.0, "sum_radii": 1.943847838292613, "target_ratio": 0.7377031644374243, "combined_score": 0.7377031644374243, "eval_time": 0.4150102138519287}, "island": 0}, "prompts": {"full_rewrite_user": {"system": "You are an expert mathematician specializing in circle packing problems and computational geometry. Your task is to improve a constructor function that directly produces a specific arrangement of 26 circles in a unit square, maximizing the sum of their radii. The AlphaEvolve paper achieved a sum of 2.635 for n=26.\n\nKey geometric insights:\n- Circle packings often follow hexagonal patterns in the densest regions\n- Maximum density for infinite circle packing is pi/(2*sqrt(3)) \u2248 0.9069\n- Edge effects make square container packing harder than infinite packing\n- Circles can be placed in layers or shells when confined to a square\n- Similar radius circles often form regular patterns, while varied radii allow better space utilization\n- Perfect symmetry may not yield the optimal packing due to edge effects\n\nFocus on designing an explicit constructor that places each circle in a specific position, rather than an iterative search algorithm.\n", "user": "# Current Program Information\n- Fitness: 0.7377\n- Feature coordinates: No feature coordinates\n- Focus areas: - Fitness declined: 0.8249 \u2192 0.7377. Consider revising recent changes.\n- Consider simplifying - code length exceeds 500 characters\n\n\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 3\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.1737, target_ratio: 0.8249, combined_score: 0.8249, eval_time: 0.4660\n- Outcome: Mixed results\n\n### Attempt 2\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.4090, target_ratio: 0.9142, combined_score: 0.9142, eval_time: 0.6519\n- Outcome: Mixed results\n\n### Attempt 1\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.4390, target_ratio: 0.9256, combined_score: 0.9256, eval_time: 0.0564\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.9256)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for n=26 circle packing in the unit square.\n\nStrategy:\n - Try to build a hexagonal grid of equal circles by binary-searching the largest\n   equal radius r that yields >=26 grid centers inside [r,1-r], for two\n   horizontal offset choices (0 or r) and several vertical shifts.\n - From the best grid (largest r), select 26 centers (center-first ordering),\n   then run a Gauss-Seidel-style monotone growth routine that maximizes each\n   r_i := min(dist_to_wall, min_j (d_ij - r_j)). This yields varied radii\n   and typically a large total radius sum.\n - Fallback to a deterministic row layout if no grid is found.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n_needed = 26\n    sqrt3 = np.sqrt(3.0)\n\n    def gen_grid(r, horiz_offset, vert_shift):\n        pts = []\n        # start at y = r + vert_shift, step sqrt3 * r\n        y = r + vert_shift\n        row = 0\n        while y <= 1.0 - r + 1e-12:\n            offset = (row % 2) * r + horiz_offset\n            x = r + offset\n            while x <= 1.0 - r + 1e-12:\n                pts.append((x, y))\n                x += 2.0 * r\n            y += sqrt3 * r\n            row += 1\n        if not pts:\n            return np.empty((0,2))\n        pts = np.array(pts)\n        return pts\n\n    def find_best_for_offset(h_offset_flag):\n        # h_offset_flag: None means horiz_offset = r, otherwise numeric offset\n        lo, hi = 1e-8, 0.5\n        best_r = 0.0\n        best_pts = None\n        for _ in range(40):\n            mid = 0.5 * (lo + hi)\n            found = False\n            for frac in np.linspace(0.0, 0.5, 5):\n                vert_shift = frac * sqrt3 * mid\n                ho = (mid if h_offset_flag is None else h_offset_flag)\n                pts = gen_grid(mid, ho, vert_shift)\n                if pts.shape[0] >= n_needed:\n                    found = True\n                    break\n            if found:\n                best_r = mid\n                lo = mid\n            else:\n                hi = mid\n        if best_r > 0:\n            # pick a vertical shift that gives a stable centered set\n            best_pts = None\n            for frac in np.linspace(0.0, 0.5, 9):\n                vert_shift = frac * sqrt3 * best_r\n                ho = (best_r if h_offset_flag is None else h_offset_flag)\n                pts = gen_grid(best_r, ho, vert_shift)\n                if pts.shape[0] >= n_needed:\n                    # sort by y descending, then x ascending to pick center-first\n                    pts_sorted = pts[np.lexsort((pts[:,0], -pts[:,1]))]\n                    if best_pts is None or pts_sorted.shape[0] > best_pts.shape[0]:\n                        best_pts = pts_sorted\n            if best_pts is None:\n                best_pts = gen_grid(best_r, (best_r if h_offset_flag is None else h_offset_flag), 0.0)\n        return best_r, best_pts\n\n    best_global = {\"r\": 0.0, \"pts\": None}\n    # try two horizontal choices: offset 0 or offset=r (encoded as None)\n    for h_flag in (0.0, None):\n        r_val, pts = find_best_for_offset(h_flag)\n        if pts is not None and r_val > best_global[\"r\"]:\n            best_global[\"r\"] = r_val\n            best_global[\"pts\"] = pts.copy()\n\n    if best_global[\"pts\"] is None:\n        # fallback deterministic row layout (guarantees feasibility)\n        rows = [6,5,6,5,4]\n        max_k = max(rows)\n        r_h = 1.0 / (2.0 * max_k)\n        r_v = 1.0 / (2.0 + (len(rows) - 1) * sqrt3)\n        base_r = min(r_h, r_v) * 0.999\n        vert_span = 2.0 * base_r + (len(rows) - 1) * base_r * sqrt3\n        top = (1.0 - vert_span) / 2.0 + base_r\n        dy = base_r * sqrt3\n        centers = []\n        for row_idx, k in enumerate(rows):\n            row_span = 2.0 * base_r * k\n            left = (1.0 - row_span) / 2.0 + base_r\n            xs = left + np.arange(k) * (2.0 * base_r)\n            y = top + row_idx * dy\n            for x in xs:\n                centers.append((x, y))\n        centers = np.array(centers[:n_needed], dtype=float)\n    else:\n        pts = best_global[\"pts\"]\n        # select first n_needed in stable center-first order\n        centers = pts[:n_needed].copy()\n        centers = np.clip(centers, 1e-12, 1.0 - 1e-12)\n\n    radii = maximize_radii_gs(centers)\n    return centers, radii, float(np.sum(radii))\n\n\ndef maximize_radii_gs(centers, tol=1e-10, max_iters=5000):\n    \"\"\"\n    Gauss-Seidel-style monotone growth:\n      r_i <- min( wall_i, min_j (d_ij - r_j) )\n    This finds the maximal feasible radii vector (pointwise) for fixed centers\n    under r_i >= 0, r_i <= wall_i, r_i + r_j <= d_ij.\n    \"\"\"\n    n = centers.shape[0]\n    wall = np.minimum.reduce([centers[:,0], centers[:,1], 1.0-centers[:,0], 1.0-centers[:,1]])\n    wall = np.maximum(wall, 0.0)\n    diff = centers[:, None, :] - centers[None, :, :]\n    dists = np.sqrt(np.sum(diff*diff, axis=2))\n    np.fill_diagonal(dists, np.inf)\n\n    r = np.zeros(n, dtype=float)\n    for it in range(max_iters):\n        max_change = 0.0\n        for i in range(n):\n            limits = dists[i] - r  # d_ij - r_j\n            allowed_by_neighbors = float(np.min(limits))\n            new_ri = allowed_by_neighbors\n            if new_ri > wall[i]:\n                new_ri = wall[i]\n            if new_ri < 0.0:\n                new_ri = 0.0\n            change = abs(new_ri - r[i])\n            if change > 0:\n                r[i] = new_ri\n                if change > max_change:\n                    max_change = change\n        if max_change < tol:\n            break\n\n    # final safety: remove any tiny violations by proportional scaling on violating pairs\n    for _ in range(4):\n        changed = False\n        for i in range(n):\n            for j in range(i+1, n):\n                dij = dists[i,j]\n                if r[i] + r[j] > dij:\n                    tot = r[i] + r[j]\n                    if tot <= 0:\n                        r[i] = r[j] = 0.0\n                    else:\n                        scale = dij / tot\n                        new_i = r[i] * scale\n                        new_j = r[j] * scale\n                        if new_i != r[i] or new_j != r[j]:\n                            r[i], r[j] = new_i, new_j\n                            changed = True\n        if not changed:\n            break\n    r = np.minimum(r, wall)\n    r = np.maximum(r, 0.0)\n    return r\n\n# EVOLVE-BLOCK-END\n\n# Fixed runner and visualization (unchanged interface)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(8,8))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect(\"equal\")\n    ax.add_patch(plt.Rectangle((0,0),1,1,fill=False,edgecolor='k',linewidth=1))\n    for i,(c,r) in enumerate(zip(centers, radii)):\n        ax.add_patch(Circle(c, r, alpha=0.6, edgecolor='k'))\n        ax.text(c[0], c[1], str(i), ha='center', va='center', fontsize=8)\n    plt.title(f\"n={len(centers)}, sum radii={sum(radii):.6f}\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, total = run_packing()\n    print(f\"Sum of radii: {total:.6f}\")\n    # visualize(centers, radii)\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.4390), Performs well on target_ratio (0.9256), Performs well on combined_score (0.9256), Performs well on eval_time (0.0564)\n\n### Program 2 (Score: 0.9142)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for 26 circles in the unit square.\n\nCombines two constructive strategies and picks the best:\n 1) Binary-search equal-radius hex-grid alignments (offset=0 or offset=r),\n    with small vertical/horizontal shifts; take first 26 centers and then\n    maximize per-center radii (allows radii to vary and increases sum).\n 2) Parametric hex-row layout rows=[6,5,6,5,4] with a spacing sweep and\n    small global x-shifts / vertical scaling; then maximize radii.\n\nRadii maximization: start from wall-limited radii, alternately repair pair\nviolations by shrinking the larger, and grow each circle to the max allowed\nby current neighbors; finish with proportional fixes. Deterministic.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n_needed = 26\n    sqrt3 = np.sqrt(3.0)\n\n    def gen_hex_grid(r, use_offset_r, vert_shift, dx):\n        pts = []\n        row = 0\n        y = r + vert_shift\n        while y <= 1.0 - r + 1e-12:\n            offset = (r if use_offset_r and (row % 2 == 1) else 0.0)\n            x = r + dx + offset\n            while x <= 1.0 - r + 1e-12:\n                pts.append((x, y))\n                x += 2.0 * r\n            row += 1\n            y += sqrt3 * r\n        if not pts:\n            return np.empty((0,2))\n        pts = np.array(pts)\n        # stable ordering: center-first (high y), then left-to-right\n        order = np.lexsort((pts[:,0], -pts[:,1]))\n        return pts[order]\n\n    def max_r_for_alignment(use_offset_r):\n        lo, hi = 1e-8, 0.5\n        best_r, best_pts = 0.0, None\n        vert_fracs = np.linspace(0.0, 0.5, 5)\n        dx_vals = np.linspace(-0.01, 0.01, 5)\n        for _ in range(40):\n            mid = 0.5*(lo+hi)\n            ok = False\n            for frac in vert_fracs:\n                vs = frac * sqrt3 * mid\n                for dx in dx_vals:\n                    pts = gen_hex_grid(mid, use_offset_r, vs, dx)\n                    if pts.shape[0] >= n_needed:\n                        ok = True\n                        break\n                if ok:\n                    break\n            if ok:\n                best_r = mid\n                lo = mid\n            else:\n                hi = mid\n        if best_r > 0:\n            # choose best vertical shift and dx giving most symmetric/centered result\n            best_pts = None\n            for frac in np.linspace(0.0, 0.5, 9):\n                vs = frac * sqrt3 * best_r\n                for dx in dx_vals:\n                    pts = gen_hex_grid(best_r, use_offset_r, vs, dx)\n                    if pts.shape[0] >= n_needed:\n                        pts_sorted = pts[np.lexsort((pts[:,0], -pts[:,1]))]\n                        if best_pts is None or pts_sorted.shape[0] > best_pts.shape[0]:\n                            best_pts = pts_sorted\n            if best_pts is None:\n                best_pts = gen_hex_grid(best_r, use_offset_r, 0.0, 0.0)\n        return best_r, best_pts\n\n    candidates = []\n\n    # Strategy A: try both offset modes\n    for flag in (False, True):\n        r_val, pts = max_r_for_alignment(flag)\n        if pts is not None:\n            centers = pts[:n_needed].copy()\n            centers = np.clip(centers, 1e-12, 1.0 - 1e-12)\n            radii = maximize_radii(centers)\n            candidates.append((float(np.sum(radii)), centers, radii))\n\n    # Strategy B: parametric hex-rows [6,5,6,5,4]\n    rows = [6,5,6,5,4]\n    m = len(rows)\n    kmax = max(rows)\n    a_max = 1.0 / (kmax - 1)\n    a_min = 0.035\n    if a_min >= a_max:\n        a_min = a_max * 0.6\n    dx_vals = np.linspace(-0.02, 0.02, 7)\n    for a in np.linspace(a_max*0.995, a_min, 28):\n        v0 = a * sqrt3 / 2.0\n        for v_scale in (0.96, 0.98, 1.0, 1.02):\n            v = v0 * v_scale\n            total_h = (m - 1) * v\n            if total_h > 1.0: continue\n            y0 = (1.0 - total_h) / 2.0\n            for short_offset_half in (False, True):\n                for dx in dx_vals:\n                    centers = []\n                    for r_idx, cnt in enumerate(rows):\n                        y = y0 + r_idx * v\n                        offset = (a/2.0 if (cnt < kmax and short_offset_half) else 0.0)\n                        row_width = (cnt - 1) * a\n                        x0 = (1.0 - row_width) / 2.0 + offset + dx\n                        for c in range(cnt):\n                            centers.append((x0 + c * a, y))\n                    centers = np.array(centers[:n_needed], dtype=float)\n                    if centers.shape[0] != n_needed:\n                        continue\n                    centers = np.clip(centers, 1e-12, 1.0 - 1e-12)\n                    radii = maximize_radii(centers)\n                    candidates.append((float(np.sum(radii)), centers, radii))\n\n    # Choose best candidate by sum of radii\n    if not candidates:\n        # fallback simple centered rows\n        a = a_max * 0.98\n        v = a * sqrt3 / 2.0\n        centers = []\n        y0 = (1.0 - (m - 1) * v) / 2.0\n        for r_idx, cnt in enumerate(rows):\n            y = y0 + r_idx * v\n            offset = (a/2.0) if (cnt < kmax) else 0.0\n            row_width = (cnt - 1) * a\n            x0 = (1.0 - row_width)/2.0 + offset\n            for c in range(cnt):\n                centers.append((np.clip(x0 + c * a, 1e-12, 1.0 - 1e-12), y))\n        centers = np.array(centers[:n_needed], dtype=float)\n        radii = maximize_radii(centers)\n        return centers, radii, float(np.sum(radii))\n    best = max(candidates, key=lambda t: t[0])\n    return best[1], best[2], best[0]\n\n\ndef maximize_radii(centers, tol=1e-10, max_cycles=2000):\n    n = centers.shape[0]\n    if n == 0:\n        return np.array([])\n    # wall distances\n    wall = np.minimum.reduce([centers[:,0], centers[:,1], 1.0-centers[:,0], 1.0-centers[:,1]])\n    wall = np.maximum(wall, 0.0)\n    # pairwise distances\n    diff = centers[:, None, :] - centers[None, :, :]\n    dists = np.sqrt(np.sum(diff*diff, axis=2))\n    np.fill_diagonal(dists, np.inf)\n    # start at wall limits\n    r = wall.copy()\n\n    for cycle in range(max_cycles):\n        max_change = 0.0\n        # Reduction sweep: for each pair that violates, shrink the larger\n        for i in range(n):\n            ri = r[i]\n            s = ri + r\n            viol = np.where(s > dists[i] + 1e-12)[0]\n            for j in viol:\n                if j == i: continue\n                dij = dists[i,j]\n                if r[i] >= r[j]:\n                    new_ri = max(0.0, dij - r[j])\n                    ch = ri - new_ri\n                    if ch > 0:\n                        r[i] = new_ri\n                        ri = new_ri\n                        if ch > max_change: max_change = ch\n                else:\n                    new_rj = max(0.0, dij - r[i])\n                    ch = r[j] - new_rj\n                    if ch > 0:\n                        r[j] = new_rj\n                        if ch > max_change: max_change = ch\n        # enforce wall\n        over = r > wall\n        if np.any(over):\n            diffv = float(np.max(r[over] - wall[over]))\n            r[over] = wall[over]\n            if diffv > max_change: max_change = diffv\n        # Growth sweep: increase each to min(wall_i, min_j(d_ij - r_j))\n        for i in range(n):\n            lims = dists[i,:] - r\n            target = min(wall[i], float(np.min(lims)))\n            if target < 0.0: target = 0.0\n            ch = target - r[i]\n            if ch > 0:\n                r[i] = target\n                if ch > max_change: max_change = ch\n        if max_change < tol:\n            break\n\n    # Final proportional fixes\n    for _ in range(6):\n        changed = False\n        for i in range(n):\n            for j in range(i+1, n):\n                dij = dists[i,j]\n                if r[i] + r[j] > dij:\n                    tot = r[i] + r[j]\n                    if tot <= 0:\n                        if r[i] != 0.0 or r[j] != 0.0:\n                            r[i] = r[j] = 0.0\n                            changed = True\n                    else:\n                        scale = dij / tot\n                        new_i = r[i] * scale\n                        new_j = r[j] * scale\n                        if new_i != r[i] or new_j != r[j]:\n                            r[i], r[j] = new_i, new_j\n                            changed = True\n        if not changed:\n            break\n    r = np.clip(r, 0.0, wall)\n    return r\n\n\n# Fixed API\ndef run_packing():\n    centers, radii, s = construct_packing()\n    return centers, radii, s\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(6,6))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect(\"equal\")\n    ax.add_patch(plt.Rectangle((0,0),1,1,fill=False,linewidth=1))\n    for c,r in zip(centers, radii):\n        ax.add_patch(Circle(c, r, alpha=0.6, edgecolor='k'))\n    plt.title(f\"n={len(centers)}, sum={sum(radii):.6f}\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, total = run_packing()\n    print(f\"Sum of radii: {total:.6f}\")\n    # visualize(centers, radii)\n# EVOLVE-BLOCK-END\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.4090), Performs well on target_ratio (0.9142), Performs well on combined_score (0.9142), Performs well on eval_time (0.6519)\n\n### Program 3 (Score: 0.8249)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for 26 circles in the unit square.\n\nStrategy:\n- Use the empirically good row pattern [6,5,6,5,4].\n- Sample a small family of hex-like layouts (horizontal spacing, slight vertical scale,\n  two offset modes and small global x-shifts) and pick the layout that yields the\n  largest sum of feasible radii.\n- Radii solver alternates deterministic reduction passes (repair violating pairs by\n  shrinking the larger) and growth passes (increase each radius to the largest\n  allowed by current neighbors and the wall). Finish with proportional fixes.\nThis is a compact, deterministic constructor designed to boost total radius sum.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    rows = [6, 5, 6, 5, 4]\n    m = len(rows)\n    kmax = max(rows)\n\n    # horizontal spacing feasible range (widest row must fit)\n    a_max = 1.0 / (kmax - 1)        # (kmax-1)*a <= 1 => a <= 1/(kmax-1)\n    a_min = 0.035\n    if a_min >= a_max:\n        a_min = a_max * 0.6\n\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    sqrt3 = np.sqrt(3.0)\n\n    # Sample a near the upper bound more densely\n    for a in np.linspace(a_max * 0.99, a_min, 28):\n        # nominal vertical step for hex packing, allow slight scaling\n        v0 = a * sqrt3 / 2.0\n        for v_scale in (0.96, 0.98, 1.0, 1.02):\n            v = v0 * v_scale\n            total_h = (m - 1) * v\n            if total_h > 1.0:\n                continue\n            # two offset modes for shorter rows: centered or half-step\n            for short_half in (False, True):\n                # small global horizontal shifts\n                for dx in np.linspace(-0.02, 0.02, 5):\n                    centers = np.zeros((n, 2), dtype=float)\n                    idx = 0\n                    y0 = (1.0 - total_h) / 2.0\n                    valid = True\n                    for r_idx, cnt in enumerate(rows):\n                        y = y0 + r_idx * v\n                        if cnt < kmax and short_half:\n                            offset = a / 2.0\n                        else:\n                            offset = 0.0\n                        row_width = (cnt - 1) * a\n                        x0 = (1.0 - row_width) / 2.0 + offset + dx\n                        for c in range(cnt):\n                            if idx >= n:\n                                break\n                            x = x0 + c * a\n                            # small clamp if numerical drift pushes out\n                            if x <= 0.0 + 1e-12 or x >= 1.0 - 1e-12 or y <= 0.0 + 1e-12 or y >= 1.0 - 1e-12:\n                                x = np.clip(x, 1e-12, 1.0 - 1e-12)\n                                y = np.clip(y, 1e-12, 1.0 - 1e-12)\n                            centers[idx, 0] = x\n                            centers[idx, 1] = y\n                            idx += 1\n                    if idx < n:\n                        valid = False\n                    if not valid:\n                        continue\n                    radii = maximize_radii_alt(centers)\n                    s = float(np.sum(radii))\n                    if s > best_sum:\n                        best_sum = s\n                        best_centers = centers.copy()\n                        best_radii = radii.copy()\n\n    # Fallback simple centered hex-like layout\n    if best_centers is None:\n        a = a_max * 0.98\n        v = a * sqrt3 / 2.0\n        centers_list = []\n        y0 = (1.0 - (m - 1) * v) / 2.0\n        for r_idx, cnt in enumerate(rows):\n            y = y0 + r_idx * v\n            offset = (a / 2.0) if (cnt < kmax) else 0.0\n            row_width = (cnt - 1) * a\n            x0 = (1.0 - row_width) / 2.0 + offset\n            for c in range(cnt):\n                x = np.clip(x0 + c * a, 1e-12, 1.0 - 1e-12)\n                centers_list.append((x, y))\n        best_centers = np.array(centers_list[:n], dtype=float)\n        best_radii = maximize_radii_alt(best_centers)\n        best_sum = float(np.sum(best_radii))\n\n    return best_centers, best_radii, float(best_sum)\n\n\ndef maximize_radii_alt(centers, tol=1e-9, max_cycles=1000):\n    \"\"\"Alternate reduction/growth solver for maximizing sum of radii under pair and wall constraints.\"\"\"\n    n = centers.shape[0]\n    if n == 0:\n        return np.array([])\n\n    wall = np.minimum.reduce([centers[:, 0], centers[:, 1],\n                              1.0 - centers[:, 0], 1.0 - centers[:, 1]])\n    wall = np.maximum(wall, 0.0)\n\n    diff = centers[:, None, :] - centers[None, :, :]\n    dists = np.sqrt(np.sum(diff * diff, axis=2))\n    np.fill_diagonal(dists, np.inf)\n\n    # start from wall-limited radii (greedy)\n    r = wall.copy()\n\n    for cycle in range(max_cycles):\n        max_change = 0.0\n        # Reduction sweep: repair violating pairs by shrinking the larger circle\n        for i in range(n):\n            ri = r[i]\n            # compute violation indices where ri + rj > d_ij\n            sum_with_i = ri + r\n            viol = np.where(sum_with_i > dists[i] + 1e-14)[0]\n            for j in viol:\n                if j == i:\n                    continue\n                dij = dists[i, j]\n                # shrink the larger of r[i], r[j] to satisfy r_i + r_j <= dij\n                if r[i] >= r[j]:\n                    new_ri = max(0.0, dij - r[j])\n                    ch = ri - new_ri\n                    if ch > 0:\n                        r[i] = new_ri\n                        ri = new_ri\n                        if ch > max_change:\n                            max_change = ch\n                else:\n                    new_rj = max(0.0, dij - r[i])\n                    ch = r[j] - new_rj\n                    if ch > 0:\n                        r[j] = new_rj\n                        if ch > max_change:\n                            max_change = ch\n\n        # enforce wall limits (prevent overshoot)\n        over = r > wall\n        if np.any(over):\n            diffv = np.max(r[over] - wall[over])\n            r[over] = wall[over]\n            if diffv > max_change:\n                max_change = diffv\n\n        # Growth sweep: try to grow each circle to allowed max given neighbors\n        for i in range(n):\n            allowed = wall[i]\n            limits = dists[i, :] - r\n            # min over neighbors gives the tightest bound (self diag is inf)\n            minlim = np.min(limits)\n            target = min(allowed, minlim)\n            if target < 0.0:\n                target = 0.0\n            ch = target - r[i]\n            if ch > 0:\n                r[i] = target\n                if ch > max_change:\n                    max_change = ch\n\n        if max_change < tol:\n            break\n\n    # Final proportional fixes for any remaining tiny violations\n    for _ in range(4):\n        changed = False\n        for i in range(n):\n            for j in range(i + 1, n):\n                dij = dists[i, j]\n                if r[i] + r[j] > dij:\n                    tot = r[i] + r[j]\n                    if tot <= 0:\n                        if r[i] != 0.0 or r[j] != 0.0:\n                            r[i] = r[j] = 0.0\n                            changed = True\n                    else:\n                        scale = dij / tot\n                        new_i = r[i] * scale\n                        new_j = r[j] * scale\n                        if new_i != r[i] or new_j != r[j]:\n                            r[i], r[j] = new_i, new_j\n                            changed = True\n        if not changed:\n            break\n\n    r = np.clip(r, 0.0, wall)\n    return r\n\n\n# Fixed API\ndef run_packing():\n    centers, radii, s = construct_packing()\n    return centers, radii, s\n\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(7, 7))\n    ax.set_xlim(0, 1); ax.set_ylim(0, 1); ax.set_aspect(\"equal\")\n    ax.add_patch(plt.Rectangle((0, 0), 1, 1, fill=False, linewidth=1))\n    for c, r in zip(centers, radii):\n        ax.add_patch(Circle(c, r, alpha=0.6, edgecolor='k'))\n    plt.title(f\"n={len(centers)}, sum={sum(radii):.6f}\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, total = run_packing()\n    print(f\"Sum of radii: {total:.6f}\")\n    # visualize(centers, radii)\n# EVOLVE-BLOCK-END\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.1737), Performs well on target_ratio (0.8249), Performs well on combined_score (0.8249), Performs well on eval_time (0.4660)\n\n\n\n## Diverse Programs\n\n### Program D1 (Score: 0.8231)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Improved deterministic constructor for 26 circles in the unit square.\n\nKey ideas:\n- Use the hex-like row pattern [6,5,6,5,4].\n- Sample a small grid of horizontal spacings, small global x-shifts and two offset modes\n  (shorter rows either centered or half-step shifted) and slight vertical compression/stretch.\n- For each candidate placement compute radii with an enhanced constraint solver:\n    * start from border limits\n    * alternate reduction sweeps (repair violating pairs by shrinking the larger)\n      and growth sweeps (increase each radius to the largest allowed by current neighbors)\n    * finalize with proportional pairwise fixes\n  This alternation lets radii both relax down when needed and regrow to better fill free space,\n  producing larger total radii than greedy-only reductions.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    rows = [6, 5, 6, 5, 4]\n    m = len(rows)\n    kmax = max(rows)\n    # allowable horizontal spacing: ensure widest row fits\n    a_max = 1.0 / (kmax - 1)  # (kmax-1)*a <= 1\n    a_min = 0.035\n    if a_min >= a_max:\n        a_min = a_max * 0.6\n\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    sqrt3 = np.sqrt(3.0)\n\n    # sample horizontal spacings more densely near a_max\n    for a in np.linspace(a_max * 0.98, a_min, 20):\n        # nominal hex vertical step (can scale slightly)\n        v0 = a * sqrt3 / 2.0\n        for v_scale in (0.96, 0.98, 1.0, 1.02):\n            v = v0 * v_scale\n            total_h = (m - 1) * v\n            if total_h > 1.0:\n                continue\n            # two offset modes: 0 or half-step for shorter rows\n            for short_offset_half in (False, True):\n                # small global horizontal shifts to allow better edge packing\n                for dx in np.linspace(-0.02, 0.02, 5):\n                    centers = np.zeros((n, 2))\n                    idx = 0\n                    y0 = (1.0 - total_h) / 2.0\n                    ok = True\n                    for r_idx, cnt in enumerate(rows):\n                        y = y0 + r_idx * v\n                        # compute offset for this row\n                        if cnt < kmax and short_offset_half:\n                            offset = a / 2.0\n                        else:\n                            offset = 0.0\n                        row_width = (cnt - 1) * a\n                        x0 = (1.0 - row_width) / 2.0 + offset + dx\n                        # place points and ensure inside [0,1] (clamp edges)\n                        for c in range(cnt):\n                            if idx >= n:\n                                break\n                            x = x0 + c * a\n                            if x <= 0.0 + 1e-9 or x >= 1.0 - 1e-9 or y <= 0.0 + 1e-9 or y >= 1.0 - 1e-9:\n                                # allow small clamping but if center would be too far out, mark not ok\n                                x = np.clip(x, 1e-9, 1.0 - 1e-9)\n                                y = np.clip(y, 1e-9, 1.0 - 1e-9)\n                            centers[idx, 0] = x\n                            centers[idx, 1] = y\n                            idx += 1\n                    if idx < n:\n                        ok = False\n                    if not ok:\n                        continue\n                    radii = maximize_radii_refined(centers)\n                    s = float(np.sum(radii))\n                    if s > best_sum:\n                        best_sum = s\n                        best_centers = centers.copy()\n                        best_radii = radii.copy()\n\n    # Fallback deterministic center if sampling failed\n    if best_centers is None:\n        # simple centered hex-like placement\n        a = a_max * 0.98\n        v = a * sqrt3 / 2.0\n        centers = []\n        y0 = (1.0 - (m - 1) * v) / 2.0\n        for r_idx, cnt in enumerate(rows):\n            y = y0 + r_idx * v\n            offset = (a / 2.0) if (cnt < kmax) else 0.0\n            row_width = (cnt - 1) * a\n            x0 = (1.0 - row_width) / 2.0 + offset\n            for c in range(cnt):\n                centers.append((np.clip(x0 + c * a, 1e-9, 1.0 - 1e-9), y))\n        best_centers = np.array(centers[:n], dtype=float)\n        best_radii = maximize_radii_refined(best_centers)\n        best_sum = float(np.sum(best_radii))\n\n    return best_centers, best_radii, float(best_sum)\n\n\ndef maximize_radii_refined(centers, tol=1e-9, max_cycles=1000):\n    \"\"\"\n    Alternating repair/increase solver for the LP:\n      maximize sum r_i\n      s.t. 0 <= r_i <= wall_i, r_i + r_j <= d_ij.\n\n    Algorithm:\n      - initialize r = wall (border-limited)\n      - repeat cycles:\n          1) reduction sweep: for every violating pair shrink the larger to d_ij - other\n          2) growth sweep: for each i increase r_i to min(wall_i, min_j(d_ij - r_j))\n      - finish with proportional pairwise fixes\n    This tends to reach a large feasible sum and is deterministic.\n    \"\"\"\n    n = centers.shape[0]\n    if n == 0:\n        return np.array([])\n\n    wall = np.minimum.reduce([centers[:, 0], centers[:, 1],\n                              1.0 - centers[:, 0], 1.0 - centers[:, 1]])\n    wall = np.maximum(wall, 0.0)\n\n    diff = centers[:, None, :] - centers[None, :, :]\n    dists = np.sqrt(np.sum(diff * diff, axis=2))\n    np.fill_diagonal(dists, np.inf)\n\n    # start from border-limited radii\n    r = wall.copy()\n\n    for cycle in range(max_cycles):\n        max_change = 0.0\n        # 1) Reduction sweep (repair violations by shrinking larger)\n        for i in range(n):\n            ri = r[i]\n            # vectorized candidate check\n            s = ri + r\n            viol = np.where(s > dists[i] + 1e-12)[0]\n            for j in viol:\n                if j == i:\n                    continue\n                dij = dists[i, j]\n                # shrink the larger radius\n                if r[i] >= r[j]:\n                    new_ri = max(0.0, dij - r[j])\n                    ch = ri - new_ri\n                    if ch > 0:\n                        r[i] = new_ri\n                        ri = new_ri\n                        if ch > max_change:\n                            max_change = ch\n                else:\n                    new_rj = max(0.0, dij - r[i])\n                    ch = r[j] - new_rj\n                    if ch > 0:\n                        r[j] = new_rj\n                        if ch > max_change:\n                            max_change = ch\n\n        # enforce wall limits (no increases here)\n        over = r > wall\n        if np.any(over):\n            diffv = np.max(r[over] - wall[over])\n            r[over] = wall[over]\n            if diffv > max_change:\n                max_change = diffv\n\n        # 2) Growth sweep: try to grow each circle to the largest allowed by current neighbors\n        for i in range(n):\n            allowed = wall[i]\n            # compute min_j (d_ij - r_j)\n            lims = dists[i, :] - r\n            # ignore self (inf)\n            minlim = np.min(lims)\n            target = min(allowed, minlim)\n            if target < 0.0:\n                target = 0.0\n            ch = target - r[i]\n            if ch > 0:\n                r[i] = target\n                if ch > max_change:\n                    max_change = ch\n\n        if max_change < tol:\n            break\n\n    # Final proportional fixes for any remaining tiny violations\n    for _ in range(4):\n        changed = False\n        for i in range(n):\n            for j in range(i + 1, n):\n                dij = dists[i, j]\n                if r[i] + r[j] > dij:\n                    tot = r[i] + r[j]\n                    if tot <= 0:\n                        if r[i] != 0.0 or r[j] != 0.0:\n                            r[i] = r[j] = 0.0\n                            changed = True\n                    else:\n                        scale = dij / tot\n                        new_i = r[i] * scale\n                        new_j = r[j] * scale\n                        if new_i != r[i] or new_j != r[j]:\n                            r[i], r[j] = new_i, new_j\n                            changed = True\n        if not changed:\n            break\n\n    r = np.clip(r, 0.0, wall)\n    return r\n\n\n# Fixed API\ndef run_packing():\n    centers, radii, s = construct_packing()\n    return centers, radii, s\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(6,6))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect(\"equal\")\n    ax.add_patch(plt.Rectangle((0,0),1,1,fill=False,linewidth=1))\n    for c,r in zip(centers, radii):\n        ax.add_patch(Circle(c, r, alpha=0.6, edgecolor='k'))\n    plt.title(f\"n={len(centers)}, sum={sum(radii):.6f}\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, total = run_packing()\n    print(f\"Sum of radii: {total:.6f}\")\n    # visualize(centers, radii)\n# EVOLVE-BLOCK-END\n```\nKey features: Alternative approach to validity, Alternative approach to sum_radii\n\n### Program D2 (Score: 0.8231)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for 26 circles in the unit square.\nImproved layout: use the hex-like row pattern [6,5,6,5,4], sweep a small set of\nhorizontal spacings, tiny global shifts and two offset modes, and for each\ncandidate compute near-maximal radii with an alternating repair/grow solver.\nThis yields larger total radii than a single fixed layout.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    rows = [6,5,6,5,4]\n    m = len(rows)\n    kmax = max(rows)\n    sqrt3 = np.sqrt(3.0)\n\n    # horizontal spacing bounds (ensure widest row fits)\n    a_max = 1.0 / (kmax - 1)   # (kmax-1)*a <= 1\n    a_min = 0.035\n    if a_min >= a_max:\n        a_min = a_max * 0.6\n\n    best_sum = -1.0\n    best_C = None\n    best_r = None\n\n    # sample spacings (denser near a_max) and a few vertical scales\n    a_vals = np.linspace(a_max * 0.98, a_min, 20)\n    v_scales = (0.96, 0.98, 1.0, 1.02)\n    dx_vals = np.linspace(-0.02, 0.02, 5)\n\n    for a in a_vals:\n        v0 = a * sqrt3 / 2.0\n        for v_scale in v_scales:\n            v = v0 * v_scale\n            total_h = (m - 1) * v\n            if total_h > 1.0 + 1e-12:\n                continue\n            for short_offset_half in (False, True):\n                for dx in dx_vals:\n                    C = np.zeros((n,2), dtype=float)\n                    idx = 0\n                    y0 = (1.0 - total_h) / 2.0\n                    ok = True\n                    for r_idx, cnt in enumerate(rows):\n                        y = y0 + r_idx * v\n                        offset = (a/2.0) if (cnt < kmax and short_offset_half) else 0.0\n                        row_width = (cnt - 1) * a\n                        x0 = (1.0 - row_width) / 2.0 + offset + dx\n                        for c in range(cnt):\n                            if idx >= n:\n                                break\n                            x = x0 + c * a\n                            # quick rejection if center far outside\n                            if x < -1e-6 or x > 1.0 + 1e-6 or y < -1e-6 or y > 1.0 + 1e-6:\n                                ok = False\n                                break\n                            C[idx,0] = np.clip(x, 1e-12, 1.0 - 1e-12)\n                            C[idx,1] = np.clip(y, 1e-12, 1.0 - 1e-12)\n                            idx += 1\n                        if not ok:\n                            break\n                    if not ok or idx < n:\n                        continue\n                    r = maximize_radii_refined(C)\n                    s = float(np.sum(r))\n                    if s > best_sum + 1e-12:\n                        best_sum = s\n                        best_C = C.copy()\n                        best_r = r.copy()\n\n    # fallback deterministic layout if sampling failed\n    if best_C is None:\n        a = a_max * 0.98\n        v = a * sqrt3 / 2.0\n        y0 = (1.0 - (m - 1) * v) / 2.0\n        centers = []\n        for r_idx, cnt in enumerate(rows):\n            y = y0 + r_idx * v\n            offset = (a/2.0) if (cnt < kmax) else 0.0\n            row_width = (cnt - 1) * a\n            x0 = (1.0 - row_width) / 2.0 + offset\n            for c in range(cnt):\n                centers.append((np.clip(x0 + c * a, 1e-12, 1.0-1e-12), y))\n        best_C = np.array(centers[:n], dtype=float)\n        best_r = maximize_radii_refined(best_C)\n        best_sum = float(np.sum(best_r))\n\n    return best_C, best_r, float(best_sum)\n\n\ndef maximize_radii_refined(C, tol=1e-9, max_cycles=800):\n    \"\"\"\n    Alternating shrink/grow solver:\n     - start r = wall (distance to boundary)\n     - reduction sweep: for each violating pair shrink the larger to d_ij - other\n     - growth sweep: increase each r_i to min(wall_i, min_j (d_ij - r_j))\n     - repeat until small change, finalize with proportional pairwise fixes\n    \"\"\"\n    n = C.shape[0]\n    if n == 0:\n        return np.array([])\n\n    wall = np.minimum.reduce([C[:,0], C[:,1], 1.0 - C[:,0], 1.0 - C[:,1]])\n    wall = np.maximum(wall, 0.0)\n\n    diff = C[:, None, :] - C[None, :, :]\n    dists = np.sqrt(np.sum(diff * diff, axis=2))\n    np.fill_diagonal(dists, np.inf)\n\n    r = wall.copy()\n\n    for cycle in range(max_cycles):\n        max_change = 0.0\n        # Reduction: repair violations by shrinking the larger radius\n        for i in range(n):\n            for j in range(i+1, n):\n                dij = dists[i,j]\n                if r[i] + r[j] > dij + 1e-12:\n                    # shrink the larger of the two\n                    if r[i] >= r[j]:\n                        new_i = max(0.0, dij - r[j])\n                        ch = r[i] - new_i\n                        if ch > 0:\n                            r[i] = new_i\n                            if ch > max_change: max_change = ch\n                    else:\n                        new_j = max(0.0, dij - r[i])\n                        ch = r[j] - new_j\n                        if ch > 0:\n                            r[j] = new_j\n                            if ch > max_change: max_change = ch\n\n        # enforce wall limits (shrink if necessary)\n        over = r - wall\n        if np.any(over > 1e-15):\n            mv = float(np.max(np.where(over>0, over, 0.0)))\n            r = np.minimum(r, wall)\n            if mv > max_change: max_change = mv\n\n        # Growth: try to increase each radius to local maximum allowed\n        for i in range(n):\n            allowed = wall[i]\n            lims = dists[i,:] - r\n            minlim = np.min(lims)\n            target = min(allowed, minlim)\n            if target < 0.0:\n                target = 0.0\n            ch = target - r[i]\n            if ch > max_change:\n                max_change = ch\n            if ch > 1e-14:\n                r[i] = target\n\n        if max_change < tol:\n            break\n\n    # Final proportional pairwise fixes for any remaining small violations\n    for _ in range(5):\n        changed = False\n        for i in range(n):\n            for j in range(i+1, n):\n                dij = dists[i,j]\n                if r[i] + r[j] > dij + 1e-12:\n                    tot = r[i] + r[j]\n                    if tot <= 0.0:\n                        if r[i] != 0.0 or r[j] != 0.0:\n                            r[i] = r[j] = 0.0\n                            changed = True\n                    else:\n                        scale = dij / tot\n                        new_i = r[i] * scale\n                        new_j = r[j] * scale\n                        if abs(new_i - r[i]) > 1e-14 or abs(new_j - r[j]) > 1e-14:\n                            r[i], r[j] = new_i, new_j\n                            changed = True\n        if not changed:\n            break\n\n    r = np.clip(r, 0.0, wall)\n    return r\n\n\n# Fixed API\ndef run_packing():\n    centers, radii, s = construct_packing()\n    return centers, radii, s\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(6,6))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect(\"equal\")\n    ax.add_patch(plt.Rectangle((0,0),1,1,fill=False,linewidth=1))\n    for c,r in zip(centers, radii):\n        ax.add_patch(Circle(c, r, alpha=0.6, edgecolor='k'))\n    plt.title(f\"n={len(centers)}, sum={sum(radii):.6f}\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, total = run_packing()\n    print(f\"Sum of radii: {total:.6f}\")\n# EVOLVE-BLOCK-END\n```\nKey features: Alternative approach to validity, Alternative approach to sum_radii\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 0.8205, Type: High-Performer)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for 26 circles in the unit square.\nStrategy:\n - Try two hex-grid horizontal alignments (no offset, offset = r).\n - For each alignment binary-search the largest equal radius r that yields >=26\n   grid points inside [r,1-r] (try a few vertical shifts).\n - Take the lexicographically-stable first 26 points, then run a robust\n   alternating shrink/grow radii optimizer to (heuristically) maximize sum(r_i).\nThis yields varied radii that better utilize the square than a fixed equal-radius\ngrid and is fully deterministic.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n_needed = 26\n    sqrt3 = np.sqrt(3.0)\n\n    def gen_grid(r, horiz_offset, vert_shift):\n        pts = []\n        y = r + vert_shift\n        row = 0\n        # place rows until out of bounds\n        while y <= 1.0 - r + 1e-12:\n            offset = (row % 2) * horiz_offset\n            x = r + offset\n            while x <= 1.0 - r + 1e-12:\n                pts.append((x, y))\n                x += 2.0 * r\n            y += sqrt3 * r\n            row += 1\n        if not pts:\n            return np.empty((0, 2))\n        pts = np.array(pts)\n        # stable ordering: center-first (high y), then left-to-right\n        order = np.lexsort((pts[:,0], -pts[:,1]))\n        return pts[order]\n\n    def best_grid_for_flag(offset_is_r):\n        # binary search for largest r such that some vertical shift produces >=n_needed\n        lo, hi = 1e-8, 0.5\n        best_r = 0.0\n        best_pts = None\n        # try a few vertical shift fractions per test\n        vs_fracs = np.linspace(0.0, 0.5, 5)\n        for _ in range(40):\n            mid = 0.5*(lo+hi)\n            found = False\n            for frac in vs_fracs:\n                vert_shift = frac * sqrt3 * mid\n                ho = mid if offset_is_r else 0.0\n                pts = gen_grid(mid, ho, vert_shift)\n                if pts.shape[0] >= n_needed:\n                    found = True\n                    break\n            if found:\n                best_r = mid\n                lo = mid\n            else:\n                hi = mid\n        if best_r > 0:\n            # choose best vertical shift producing the most symmetric result\n            best_pts = None\n            for frac in np.linspace(0.0, 0.5, 9):\n                vert_shift = frac * sqrt3 * best_r\n                ho = best_r if offset_is_r else 0.0\n                pts = gen_grid(best_r, ho, vert_shift)\n                if pts.shape[0] >= n_needed:\n                    if best_pts is None or pts.shape[0] > best_pts.shape[0]:\n                        best_pts = pts\n            if best_pts is None:\n                best_pts = gen_grid(best_r, (best_r if offset_is_r else 0.0), 0.0)\n        return best_r, best_pts\n\n    # try both alignment choices\n    candidates = []\n    for flag in (False, True):\n        r_val, pts = best_grid_for_flag(flag)\n        if pts is not None:\n            candidates.append((r_val, pts))\n    # fallback deterministic row layout if no grid found\n    if not candidates:\n        rows = [6,5,6,5,4]\n        max_k = max(rows)\n        m = len(rows)\n        r_h = 1.0 / (2.0 * max_k)\n        r_v = 1.0 / (2.0 + (m - 1) * sqrt3)\n        base_r = min(r_h, r_v)\n        vert_span = 2.0 * base_r + (m - 1) * (base_r * sqrt3)\n        top = (1.0 - vert_span) / 2.0 + base_r\n        dy = base_r * sqrt3\n        centers = []\n        for i,k in enumerate(rows):\n            row_span = 2.0 * base_r * k\n            left = (1.0 - row_span)/2.0 + base_r\n            offset = base_r if k < max_k else 0.0\n            xs = left + np.arange(k) * (2.0*base_r) + offset\n            y = top + i * dy\n            for x in xs:\n                centers.append((x,y))\n        centers = np.array(centers[:n_needed])\n    else:\n        # choose candidate with largest base r (prefer denser grid)\n        best = max(candidates, key=lambda c: c[0])\n        pts = best[1]\n        centers = pts[:n_needed].copy()\n        centers = np.clip(centers, 1e-12, 1.0 - 1e-12)\n\n    radii = compute_max_radii(centers)\n    return centers, radii, float(np.sum(radii))\n\n\ndef compute_max_radii(centers, tol=1e-10, max_pass=1000):\n    \"\"\"\n    Alternating shrink/grow heuristic:\n     - start r = border limits\n     - shrink_pass: for violating pairs scale the pair down proportionally\n       (keeps total sum large and propagates consistently)\n     - grow_pass: try to increase each r_i to min(border, min_j d_ij - r_j)\n    Repeat until stable.\n    \"\"\"\n    n = centers.shape[0]\n    if n == 0:\n        return np.array([])\n    # border limits\n    border = np.minimum.reduce([centers[:,0], centers[:,1],\n                                1.0-centers[:,0], 1.0-centers[:,1]])\n    border = np.maximum(border, 0.0)\n    # distances\n    diff = centers[:, None, :] - centers[None, :, :]\n    dists = np.sqrt(np.clip((diff**2).sum(axis=2), 0.0, np.inf))\n    np.fill_diagonal(dists, np.inf)\n\n    # initialize to border\n    r = border.copy()\n\n    def shrink_pass(radii):\n        changed = False\n        # proportional pairwise fixes\n        for i in range(n):\n            for j in range(i+1, n):\n                dij = dists[i,j]\n                if dij <= 0.0:\n                    if radii[j] > radii[i]:\n                        radii[j] = 0.0\n                    else:\n                        radii[i] = 0.0\n                    changed = True\n                    continue\n                ssum = radii[i] + radii[j]\n                if ssum > dij + 1e-12:\n                    if ssum <= 0.0:\n                        radii[i] = radii[j] = 0.0\n                    else:\n                        scale = dij / ssum\n                        new_i = radii[i] * scale\n                        new_j = radii[j] * scale\n                        if abs(new_i - radii[i]) > 1e-12 or abs(new_j - radii[j]) > 1e-12:\n                            radii[i], radii[j] = new_i, new_j\n                            changed = True\n        # enforce border\n        np.minimum(radii, border, out=radii)\n        return changed\n\n    def grow_pass(radii):\n        changed = False\n        for i in range(n):\n            allowed = border[i]\n            # neighbors limitation\n            limits = dists[i, :] - radii\n            mn = float(np.min(limits))\n            allowed = min(allowed, mn)\n            allowed = max(0.0, allowed)\n            if allowed > radii[i] + 1e-12:\n                radii[i] = allowed\n                changed = True\n        return changed\n\n    for _ in range(max_pass):\n        any_change = False\n        # multiple shrink sweeps to propagate constraints\n        for _ in range(2):\n            if shrink_pass(r):\n                any_change = True\n            else:\n                break\n        # then some growth sweeps\n        for _ in range(3):\n            if grow_pass(r):\n                any_change = True\n            else:\n                break\n        if not any_change:\n            break\n\n    # final strict cleanup\n    for _ in range(4):\n        if not shrink_pass(r):\n            break\n    r = np.maximum(r, 0.0)\n    return r\n\n# Fixed API\ndef run_packing():\n    centers, radii, s = construct_packing()\n    return centers, radii, s\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(6,6))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect(\"equal\")\n    ax.add_patch(plt.Rectangle((0,0),1,1,fill=False,linewidth=1))\n    for c,r in zip(centers, radii):\n        ax.add_patch(Circle(c, r, alpha=0.6, edgecolor='k'))\n    plt.title(f\"n={len(centers)}, sum={sum(radii):.6f}\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, total = run_packing()\n    print(f\"Sum of radii: {total:.6f}\")\n    # visualize(centers, radii)\n# EVOLVE-BLOCK-END\n```\nUnique approach: Modification: Full rewrite, Excellent validity (1.000), Excellent sum_radii (2.162)\n\n### Inspiration 2 (Score: 0.6726, Type: Alternative)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 using an explicit hex-like layout\nand a tight uniform-scaling of conservative per-circle radii to maximize sum.\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    n = 26\n    # Row counts chosen to create a dense central hex-like patch with edge accommodation\n    counts = [5, 6, 5, 5, 5]  # sums to 26\n    rows = len(counts)\n    max_cols = max(counts)\n\n    # small margin so circles sit inside boundary but may touch it\n    margin = 0.018\n\n    # horizontal spacing from widest row\n    a = (1.0 - 2 * margin) / (max_cols - 1)\n    h = a * np.sqrt(3.0) / 2.0\n\n    # if vertical extent too large, reduce 'a' to fit\n    if (rows - 1) * h > (1.0 - 2 * margin):\n        h_allowed = (1.0 - 2 * margin) / (rows - 1)\n        h = h_allowed\n        a = h_allowed * 2.0 / np.sqrt(3.0)\n\n    centers = np.zeros((n, 2))\n    idx = 0\n    y0 = (1.0 - (rows - 1) * h) / 2.0\n    for r, cnt in enumerate(counts):\n        y = y0 + r * h\n        # offset rows with fewer columns to mimic hex packing\n        offset = (a / 2.0) if cnt < max_cols else 0.0\n        row_width = (cnt - 1) * a\n        x0 = (1.0 - row_width) / 2.0 + offset\n        for c in range(cnt):\n            if idx >= n:\n                break\n            x = x0 + c * a\n            centers[idx, 0] = np.clip(x, margin * 0.5, 1.0 - margin * 0.5)\n            centers[idx, 1] = np.clip(y, margin * 0.5, 1.0 - margin * 0.5)\n            idx += 1\n\n    # safety fill if necessary\n    while idx < n:\n        centers[idx] = [0.5, 0.5]\n        idx += 1\n\n    radii = compute_max_radii(centers)\n    return centers, radii, float(np.sum(radii))\n\n\ndef compute_max_radii(centers):\n    \"\"\"Conservative per-circle radii followed by maximal uniform scaling.\"\"\"\n    n = centers.shape[0]\n    # distance to walls\n    d_wall = np.minimum.reduce([centers[:, 0], centers[:, 1], 1.0 - centers[:, 0], 1.0 - centers[:, 1]])\n    # pairwise distances\n    dif = centers[:, None, :] - centers[None, :, :]\n    dists = np.sqrt(np.sum(dif ** 2, axis=2))\n    # avoid zero on diagonal\n    np.fill_diagonal(dists, np.inf)\n\n    # initial conservative radii: limited by wall and by half nearest neighbor distance\n    half_min = 0.5 * np.min(dists, axis=1)\n    r0 = np.minimum(d_wall, half_min)\n    r0 = np.maximum(r0, 1e-12)\n\n    # compute maximal uniform scaling factor s such that\n    # s * r0[i] <= d_wall[i]  and  s*(r0[i] + r0[j]) <= dists[i,j]\n    # For walls:\n    with np.errstate(divide='ignore', invalid='ignore'):\n        s_wall = d_wall / r0\n    s_wall[np.isnan(s_wall)] = np.inf\n    s_max = np.min(s_wall)\n\n    # For pairs:\n    ri_plus_rj = r0[:, None] + r0[None, :]\n    with np.errstate(divide='ignore', invalid='ignore'):\n        s_pairs = dists / ri_plus_rj\n    s_pairs[np.isnan(s_pairs)] = np.inf\n    # consider only upper triangle\n    iu = np.triu_indices(n, 1)\n    if iu[0].size > 0:\n        s_max = min(s_max, float(np.min(s_pairs[iu])))\n\n    if not np.isfinite(s_max) or s_max <= 0.0:\n        s = 1.0\n    else:\n        # safety shrink to avoid numerical boundary contact\n        s = min(1.0, s_max) * 0.997\n\n    return r0 * float(s)\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(6, 6))\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    for i, (c, r) in enumerate(zip(centers, radii)):\n        ax.add_patch(Circle(c, r, alpha=0.6, edgecolor=\"k\"))\n        ax.text(c[0], c[1], str(i), ha=\"center\", va=\"center\", fontsize=7)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n# EVOLVE-BLOCK-END\n```\nUnique approach: Modification: Full rewrite, Excellent validity (1.000), Excellent sum_radii (1.772)\n\n### Inspiration 3 (Score: 0.8231, Type: High-Performer)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for 26 circles in the unit square.\nImproved layout: use the hex-like row pattern [6,5,6,5,4], sweep a small set of\nhorizontal spacings, tiny global shifts and two offset modes, and for each\ncandidate compute near-maximal radii with an alternating repair/grow solver.\nThis yields larger total radii than a single fixed layout.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    rows = [6,5,6,5,4]\n    m = len(rows)\n    kmax = max(rows)\n    sqrt3 = np.sqrt(3.0)\n\n    # horizontal spacing bounds (ensure widest row fits)\n    a_max = 1.0 / (kmax - 1)   # (kmax-1)*a <= 1\n    a_min = 0.035\n    if a_min >= a_max:\n        a_min = a_max * 0.6\n\n    best_sum = -1.0\n    best_C = None\n    best_r = None\n\n    # sample spacings (denser near a_max) and a few vertical scales\n    a_vals = np.linspace(a_max * 0.98, a_min, 20)\n    v_scales = (0.96, 0.98, 1.0, 1.02)\n    dx_vals = np.linspace(-0.02, 0.02, 5)\n\n    for a in a_vals:\n        v0 = a * sqrt3 / 2.0\n        for v_scale in v_scales:\n            v = v0 * v_scale\n            total_h = (m - 1) * v\n            if total_h > 1.0 + 1e-12:\n                continue\n            for short_offset_half in (False, True):\n                for dx in dx_vals:\n                    C = np.zeros((n,2), dtype=float)\n                    idx = 0\n                    y0 = (1.0 - total_h) / 2.0\n                    ok = True\n                    for r_idx, cnt in enumerate(rows):\n                        y = y0 + r_idx * v\n                        offset = (a/2.0) if (cnt < kmax and short_offset_half) else 0.0\n                        row_width = (cnt - 1) * a\n                        x0 = (1.0 - row_width) / 2.0 + offset + dx\n                        for c in range(cnt):\n                            if idx >= n:\n                                break\n                            x = x0 + c * a\n                            # quick rejection if center far outside\n                            if x < -1e-6 or x > 1.0 + 1e-6 or y < -1e-6 or y > 1.0 + 1e-6:\n                                ok = False\n                                break\n                            C[idx,0] = np.clip(x, 1e-12, 1.0 - 1e-12)\n                            C[idx,1] = np.clip(y, 1e-12, 1.0 - 1e-12)\n                            idx += 1\n                        if not ok:\n                            break\n                    if not ok or idx < n:\n                        continue\n                    r = maximize_radii_refined(C)\n                    s = float(np.sum(r))\n                    if s > best_sum + 1e-12:\n                        best_sum = s\n                        best_C = C.copy()\n                        best_r = r.copy()\n\n    # fallback deterministic layout if sampling failed\n    if best_C is None:\n        a = a_max * 0.98\n        v = a * sqrt3 / 2.0\n        y0 = (1.0 - (m - 1) * v) / 2.0\n        centers = []\n        for r_idx, cnt in enumerate(rows):\n            y = y0 + r_idx * v\n            offset = (a/2.0) if (cnt < kmax) else 0.0\n            row_width = (cnt - 1) * a\n            x0 = (1.0 - row_width) / 2.0 + offset\n            for c in range(cnt):\n                centers.append((np.clip(x0 + c * a, 1e-12, 1.0-1e-12), y))\n        best_C = np.array(centers[:n], dtype=float)\n        best_r = maximize_radii_refined(best_C)\n        best_sum = float(np.sum(best_r))\n\n    return best_C, best_r, float(best_sum)\n\n\ndef maximize_radii_refined(C, tol=1e-9, max_cycles=800):\n    \"\"\"\n    Alternating shrink/grow solver:\n     - start r = wall (distance to boundary)\n     - reduction sweep: for each violating pair shrink the larger to d_ij - other\n     - growth sweep: increase each r_i to min(wall_i, min_j (d_ij - r_j))\n     - repeat until small change, finalize with proportional pairwise fixes\n    \"\"\"\n    n = C.shape[0]\n    if n == 0:\n        return np.array([])\n\n    wall = np.minimum.reduce([C[:,0], C[:,1], 1.0 - C[:,0], 1.0 - C[:,1]])\n    wall = np.maximum(wall, 0.0)\n\n    diff = C[:, None, :] - C[None, :, :]\n    dists = np.sqrt(np.sum(diff * diff, axis=2))\n    np.fill_diagonal(dists, np.inf)\n\n    r = wall.copy()\n\n    for cycle in range(max_cycles):\n        max_change = 0.0\n        # Reduction: repair violations by shrinking the larger radius\n        for i in range(n):\n            for j in range(i+1, n):\n                dij = dists[i,j]\n                if r[i] + r[j] > dij + 1e-12:\n                    # shrink the larger of the two\n                    if r[i] >= r[j]:\n                        new_i = max(0.0, dij - r[j])\n                        ch = r[i] - new_i\n                        if ch > 0:\n                            r[i] = new_i\n                            if ch > max_change: max_change = ch\n                    else:\n                        new_j = max(0.0, dij - r[i])\n                        ch = r[j] - new_j\n                        if ch > 0:\n                            r[j] = new_j\n                            if ch > max_change: max_change = ch\n\n        # enforce wall limits (shrink if necessary)\n        over = r - wall\n        if np.any(over > 1e-15):\n            mv = float(np.max(np.where(over>0, over, 0.0)))\n            r = np.minimum(r, wall)\n            if mv > max_change: max_change = mv\n\n        # Growth: try to increase each radius to local maximum allowed\n        for i in range(n):\n            allowed = wall[i]\n            lims = dists[i,:] - r\n            minlim = np.min(lims)\n            target = min(allowed, minlim)\n            if target < 0.0:\n                target = 0.0\n            ch = target - r[i]\n            if ch > max_change:\n                max_change = ch\n            if ch > 1e-14:\n                r[i] = target\n\n        if max_change < tol:\n            break\n\n    # Final proportional pairwise fixes for any remaining small violations\n    for _ in range(5):\n        changed = False\n        for i in range(n):\n            for j in range(i+1, n):\n                dij = dists[i,j]\n                if r[i] + r[j] > dij + 1e-12:\n                    tot = r[i] + r[j]\n                    if tot <= 0.0:\n                        if r[i] != 0.0 or r[j] != 0.0:\n                            r[i] = r[j] = 0.0\n                            changed = True\n                    else:\n                        scale = dij / tot\n                        new_i = r[i] * scale\n                        new_j = r[j] * scale\n                        if abs(new_i - r[i]) > 1e-14 or abs(new_j - r[j]) > 1e-14:\n                            r[i], r[j] = new_i, new_j\n                            changed = True\n        if not changed:\n            break\n\n    r = np.clip(r, 0.0, wall)\n    return r\n\n\n# Fixed API\ndef run_packing():\n    centers, radii, s = construct_packing()\n    return centers, radii, s\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(6,6))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect(\"equal\")\n    ax.add_patch(plt.Rectangle((0,0),1,1,fill=False,linewidth=1))\n    for c,r in zip(centers, radii):\n        ax.add_patch(Circle(c, r, alpha=0.6, edgecolor='k'))\n    plt.title(f\"n={len(centers)}, sum={sum(radii):.6f}\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, total = run_packing()\n    print(f\"Sum of radii: {total:.6f}\")\n# EVOLVE-BLOCK-END\n```\nUnique approach: Modification: Full rewrite, Excellent validity (1.000), Excellent sum_radii (2.169)\n\n# Current Program\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for n=26 circles in the unit square.\n\nStrategy:\n- Try several hex-like row patterns and deterministic staggering modes.\n- For each pattern sweep horizontal spacing 'a' and a small vertical-compression\n  factor. Build explicit centers (centered rows, optional offsets).\n- For each center set compute radii via a monotone Gauss-Seidel growth:\n    r_i <- min(border_i, min_j (d_ij - r_j))\n  which tends to reach a maximal feasible vector for the linear constraints.\n- Keep the configuration that maximizes the sum of radii.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    # Candidate row patterns summing to 26 (diverse layouts)\n    patterns = [\n        [6, 5, 6, 5, 4],\n        [6, 6, 5, 5, 4],\n        [5, 6, 5, 6, 4],\n    ]\n    sqrt3 = np.sqrt(3.0)\n\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    # deterministic staggering modes to trade edge vs. interior packing\n    offset_modes = [\"alt\", \"short\", \"none\"]  # alt: alternate rows; short: rows with fewer cols offset\n\n    for rows in patterns:\n        rows = list(rows)\n        R = len(rows)\n        max_cols = max(rows)\n        # maximal horizontal step so widest row fits (allow touching boundary)\n        a_max1 = 1.0 / (max_cols - 1) if max_cols > 1 else 1.0\n        # vertical fit for ideal hex spacing: (R-1)*(sqrt3/2*a) <= 1\n        a_max2 = 2.0 / (sqrt3 * (R - 1)) if R > 1 else a_max1\n        a_max = min(a_max1, a_max2) * 0.9999\n        a_min = max(0.02, a_max * 0.40)\n\n        # sample a more densely near upper feasible bound\n        for a in np.linspace(a_max, a_min, 48):\n            # allow small vertical compression/expansion around ideal sqrt3/2\n            for v_scale in (0.98, 1.0, 1.02):\n                h = a * (sqrt3 / 2.0) * v_scale\n                total_h = (R - 1) * h\n                if total_h > 1.0 + 1e-12:\n                    continue\n                y0 = (1.0 - total_h) / 2.0\n                for mode in offset_modes:\n                    centers = np.zeros((n, 2), dtype=float)\n                    idx = 0\n                    for r_idx, cnt in enumerate(rows):\n                        y = y0 + r_idx * h\n                        row_width = (cnt - 1) * a if cnt > 1 else 0.0\n                        # choose offset deterministically per mode\n                        if mode == \"alt\":\n                            offset = (a / 2.0) if (r_idx % 2 == 1) else 0.0\n                        elif mode == \"short\":\n                            offset = (a / 2.0) if cnt < max_cols else 0.0\n                        else:  # none\n                            offset = 0.0\n                        x0 = (1.0 - row_width) / 2.0 + offset\n                        for c in range(cnt):\n                            if idx >= n:\n                                break\n                            x = x0 + c * a\n                            centers[idx, 0] = np.clip(x, 1e-12, 1.0 - 1e-12)\n                            centers[idx, 1] = np.clip(y, 1e-12, 1.0 - 1e-12)\n                            idx += 1\n                    # safety fill (shouldn't be needed)\n                    while idx < n:\n                        centers[idx] = [0.5, 0.5]\n                        idx += 1\n\n                    radii = compute_max_radii(centers)\n                    s = float(np.sum(radii))\n                    if s > best_sum + 1e-12:\n                        best_sum = s\n                        best_centers = centers.copy()\n                        best_radii = radii.copy()\n\n    # final fallback (very unlikely)\n    if best_centers is None:\n        best_centers = centers\n        best_radii = compute_max_radii(centers)\n\n    return best_centers, best_radii, float(np.sum(best_radii))\n\n\ndef compute_max_radii(centers, tol=1e-9, max_iters=4000):\n    \"\"\"\n    Monotone Gauss-Seidel growth for radii:\n      r_i <- min(border_i, min_j (d_ij - r_j))\n    Iterate until convergence, then perform a few safety passes to remove tiny\n    violations (proportional reductions). Deterministic and effective.\n    \"\"\"\n    n = centers.shape[0]\n    # border limits\n    border = np.minimum.reduce([\n        centers[:, 0], centers[:, 1],\n        1.0 - centers[:, 0], 1.0 - centers[:, 1]\n    ])\n    border = np.maximum(border, 0.0)\n\n    # pairwise distances\n    diff = centers[:, None, :] - centers[None, :, :]\n    dists = np.sqrt(np.clip((diff ** 2).sum(axis=2), 0.0, np.inf))\n    np.fill_diagonal(dists, np.inf)\n\n    # initialize radii to zero and grow\n    r = np.zeros(n, dtype=float)\n    # process order: inner circles first (closer to center), deterministic tie-breaker\n    order = np.lexsort((np.arange(n), np.argsort(np.sum((centers - 0.5) ** 2, axis=1))))\n    # simpler order: indices sorted by distance to square center\n    order = np.argsort(np.sum((centers - 0.5) ** 2, axis=1))\n\n    for it in range(max_iters):\n        max_change = 0.0\n        # sweep in fixed order; alternate direction occasionally to avoid cycles\n        if (it % 2) == 0:\n            seq = order\n        else:\n            seq = order[::-1]\n        for i in seq:\n            # allowed by neighbors: min_j (d_ij - r_j)\n            limits = dists[i] - r  # includes inf at i -> ignored\n            allowed_by_neighbors = np.min(limits)\n            new_r = min(border[i], allowed_by_neighbors)\n            if new_r < 0.0:\n                new_r = 0.0\n            change = abs(new_r - r[i])\n            if change > 0:\n                r[i] = new_r\n                if change > max_change:\n                    max_change = change\n        if max_change < tol:\n            break\n\n    # final safety: remove any remaining tiny pairwise violations by local proportional reductions\n    for _ in range(6):\n        changed = False\n        for i in range(n):\n            for j in range(i + 1, n):\n                dij = dists[i, j]\n                if r[i] + r[j] > dij:\n                    tot = r[i] + r[j]\n                    if tot <= 0:\n                        if r[i] != 0 or r[j] != 0:\n                            r[i] = r[j] = 0.0\n                            changed = True\n                    else:\n                        scale = dij / tot\n                        new_i = r[i] * scale\n                        new_j = r[j] * scale\n                        if new_i != r[i] or new_j != r[j]:\n                            r[i], r[j] = new_i, new_j\n                            changed = True\n        # enforce wall limits strictly\n        r = np.minimum(r, border)\n        if not changed:\n            break\n\n    # clamp numeric artifacts\n    r = np.maximum(r, 0.0)\n    r = np.minimum(r, border)\n    return r\n\n# EVOLVE-BLOCK-END\n\n# Fixed runner and visualization (unchanged interface)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(7, 7))\n    ax.set_xlim(0, 1); ax.set_ylim(0, 1); ax.set_aspect(\"equal\")\n    ax.add_patch(plt.Rectangle((0, 0), 1, 1, fill=False, edgecolor=\"k\"))\n    for i, (c, r) in enumerate(zip(centers, radii)):\n        ax.add_patch(Circle(c, r, alpha=0.6, edgecolor=\"k\"))\n        ax.text(c[0], c[1], str(i), ha=\"center\", va=\"center\", fontsize=7)\n    plt.title(f\"n={len(centers)}, sum radii={sum(radii):.6f}\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, total = run_packing()\n    print(f\"Sum of radii: {total:.6f}\")\n    # visualize(centers, radii)\n```\n\n# Task\nRewrite the program to improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: complexity, diversity\nDifferent solutions with similar fitness but different features are valuable.\nProvide the complete new program code.\n\nIMPORTANT: Make sure your rewritten program maintains the same inputs and outputs\nas the original program, but with improved internal implementation.\n\n```python\n# Your rewritten program here\n```", "responses": ["```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for n=26 circles in the unit square.\n\nStrategy (compact, effective):\n - Try to build an equal-radius hexagonal grid by binary-searching the largest\n   equal radius r that yields >=26 grid centers inside [r,1-r], for two\n   horizontal alignment choices (no offset or offset=r) and a few vertical shifts.\n - From the best grid (largest r) select 26 centers by prioritizing centers\n   nearer the square center (center-first). Then run a Gauss-Seidel monotone\n   growth routine that sets r_i := min(wall_i, min_j (d_ij - r_j)) in sweeps,\n   producing varied radii and a large total sum.\n - If no grid is found, fall back to a deterministic hex-like row layout.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    sqrt3 = np.sqrt(3.0)\n\n    def gen_grid(r, use_offset_r, vert_shift):\n        pts = []\n        y = r + vert_shift\n        row = 0\n        while y <= 1.0 - r + 1e-12:\n            offset = (r if use_offset_r and (row % 2 == 1) else 0.0)\n            x = r + offset\n            while x <= 1.0 - r + 1e-12:\n                pts.append((x, y))\n                x += 2.0 * r\n            row += 1\n            y += sqrt3 * r\n        if not pts:\n            return np.empty((0,2))\n        pts = np.array(pts)\n        # center-first stable ordering: distance to center, then x\n        d2 = np.sum((pts - 0.5)**2, axis=1)\n        order = np.lexsort((pts[:,0], d2))\n        return pts[order]\n\n    def search_best(use_offset_r):\n        lo, hi = 1e-8, 0.5\n        best_r = 0.0; best_pts = None\n        for _ in range(40):\n            mid = 0.5*(lo+hi)\n            found = False\n            for frac in (0.0, 0.25, 0.5):\n                vs = frac * sqrt3 * mid\n                pts = gen_grid(mid, use_offset_r, vs)\n                if pts.shape[0] >= n:\n                    found = True\n                    break\n            if found:\n                best_r = mid\n                lo = mid\n            else:\n                hi = mid\n        if best_r > 0:\n            # pick a vertical shift that yields the most points / best symmetry\n            best_pts = None\n            for frac in np.linspace(0.0,0.5,9):\n                vs = frac * sqrt3 * best_r\n                pts = gen_grid(best_r, use_offset_r, vs)\n                if pts.shape[0] >= n:\n                    if best_pts is None or pts.shape[0] > best_pts.shape[0]:\n                        best_pts = pts\n            if best_pts is None:\n                best_pts = gen_grid(best_r, use_offset_r, 0.0)\n        return best_r, best_pts\n\n    best_global = {\"r\":0.0, \"pts\":None}\n    for flag in (False, True):\n        r_val, pts = search_best(flag)\n        if pts is not None and r_val > best_global[\"r\"]:\n            best_global[\"r\"] = r_val\n            best_global[\"pts\"] = pts.copy()\n\n    if best_global[\"pts\"] is None:\n        # fallback deterministic hex-like rows pattern [6,5,6,5,4]\n        rows = [6,5,6,5,4]\n        m = len(rows); kmax = max(rows)\n        a = min(1.0/(kmax-1), 2.0/(sqrt3*(m-1))) * 0.98\n        v = a * sqrt3 / 2.0\n        total_h = (m-1)*v\n        y0 = (1.0 - total_h)/2.0\n        centers = []\n        for ridx,cnt in enumerate(rows):\n            y = y0 + ridx * v\n            offset = (a/2.0) if cnt < kmax else 0.0\n            row_w = (cnt-1)*a\n            x0 = (1.0 - row_w)/2.0 + offset\n            for c in range(cnt):\n                centers.append((np.clip(x0 + c*a,1e-12,1-1e-12), y))\n        centers = np.array(centers[:n], dtype=float)\n    else:\n        pts = best_global[\"pts\"]\n        centers = pts[:n].copy()\n        centers = np.clip(centers, 1e-12, 1.0 - 1e-12)\n\n    radii = maximize_radii_gs(centers)\n    return centers, radii, float(np.sum(radii))\n\ndef maximize_radii_gs(centers, tol=1e-10, max_iters=5000):\n    n = centers.shape[0]\n    if n == 0:\n        return np.array([])\n    wall = np.minimum.reduce([centers[:,0], centers[:,1], 1.0-centers[:,0], 1.0-centers[:,1]])\n    wall = np.maximum(wall, 0.0)\n    diff = centers[:,None,:] - centers[None,:,:]\n    dists = np.sqrt(np.maximum((diff**2).sum(axis=2), 0.0))\n    np.fill_diagonal(dists, np.inf)\n    r = np.zeros(n, dtype=float)\n    order = np.argsort(np.sum((centers-0.5)**2, axis=1))  # center-first\n    for it in range(max_iters):\n        max_ch = 0.0\n        seq = order if (it%2==0) else order[::-1]\n        for i in seq:\n            limits = dists[i] - r\n            allowed = float(np.min(limits))\n            newr = min(wall[i], allowed)\n            if newr < 0.0:\n                newr = 0.0\n            ch = abs(newr - r[i])\n            if ch > 0:\n                r[i] = newr\n                if ch > max_ch: max_ch = ch\n        if max_ch < tol:\n            break\n    # proportional cleanup for tiny violations\n    for _ in range(6):\n        changed = False\n        for i in range(n):\n            for j in range(i+1,n):\n                dij = dists[i,j]\n                if r[i] + r[j] > dij + 1e-12:\n                    tot = r[i] + r[j]\n                    if tot <= 0:\n                        if r[i] != 0.0 or r[j] != 0.0:\n                            r[i]=r[j]=0.0; changed=True\n                    else:\n                        s = dij / tot\n                        ni, nj = r[i]*s, r[j]*s\n                        if ni != r[i] or nj != r[j]:\n                            r[i], r[j] = ni, nj\n                            changed = True\n        r = np.minimum(r, wall)\n        if not changed:\n            break\n    r = np.clip(r, 0.0, wall)\n    return r\n\n# EVOLVE-BLOCK-END\n\n# Fixed runner and visualization\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(7,7))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect(\"equal\")\n    ax.add_patch(plt.Rectangle((0,0),1,1,fill=False,edgecolor='k'))\n    for c,r in zip(centers, radii):\n        ax.add_patch(Circle(c, r, alpha=0.6, edgecolor='k'))\n    plt.title(f\"n={len(centers)}, sum radii={sum(radii):.6f}\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, total = run_packing()\n    print(f\"Sum of radii: {total:.6f}\")\n    # visualize(centers, radii)\n```"]}}, "artifacts_json": null, "artifact_dir": null}