{"id": "f4704674-456f-4a99-ad29-1281a9f2e993", "code": "# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for n=26 circle packing in the unit square.\n\nStrategy (compact, robust):\n - Build a best equal-radius hex lattice by binary-searching r that yields >=26\n   lattice centers inside [r,1-r] for two horizontal-offset modes.\n - Pick the 26 most central centers (by distance to square center) from that lattice.\n - Run a Gauss-Seidel monotone growth to maximize each r_i := min(wall_i, min_j (d_ij - r_j)).\n - Final proportional repairs to remove tiny violations.\nThis yields varied radii and typically a large total radius sum.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    sqrt3 = np.sqrt(3.0)\n\n    def gen_hex(r, horiz_offset, vert_shift):\n        pts = []\n        y = r + vert_shift\n        row = 0\n        while y <= 1.0 - r + 1e-12:\n            offset = ((row % 2) * r) + horiz_offset\n            x = r + offset\n            while x <= 1.0 - r + 1e-12:\n                pts.append((x, y))\n                x += 2.0 * r\n            y += sqrt3 * r\n            row += 1\n        return np.array(pts, dtype=float) if pts else np.empty((0,2))\n\n    def best_lattice_for_mode(hflag):\n        lo, hi = 1e-8, 0.5\n        best_r = 0.0\n        best_pts = None\n        for _ in range(40):\n            mid = 0.5*(lo+hi)\n            ok = False\n            # try a few vertical shifts to avoid aligning all rows with edges\n            for frac in (0.0, 0.25, 0.5):\n                vs = frac * sqrt3 * mid\n                ho = mid if hflag is None else hflag\n                pts = gen_hex(mid, ho, vs)\n                if pts.shape[0] >= n:\n                    ok = True\n                    break\n            if ok:\n                best_r = mid\n                lo = mid\n            else:\n                hi = mid\n        if best_r > 0:\n            # pick a shift that yields the most symmetric/central selection\n            best_pts = None\n            for frac in np.linspace(0.0, 0.5, 9):\n                vs = frac * sqrt3 * best_r\n                ho = best_r if hflag is None else hflag\n                pts = gen_hex(best_r, ho, vs)\n                if pts.shape[0] >= n:\n                    # sort by distance to center to pick most central points\n                    d2 = np.sum((pts - 0.5)**2, axis=1)\n                    order = np.argsort(d2)\n                    pts_sorted = pts[order]\n                    if best_pts is None or pts_sorted.shape[0] > best_pts.shape[0]:\n                        best_pts = pts_sorted\n            if best_pts is None:\n                best_pts = gen_hex(best_r, (best_r if hflag is None else hflag), 0.0)\n            return best_r, best_pts\n        return 0.0, None\n\n    best_global = {\"r\":0.0, \"pts\":None}\n    # try horizontal offset modes: aligned (0.0) and staggered (None means offset=r)\n    for mode in (0.0, None):\n        r_val, pts = best_lattice_for_mode(mode)\n        if pts is not None and r_val > best_global[\"r\"]:\n            best_global[\"r\"] = r_val\n            best_global[\"pts\"] = pts.copy()\n\n    if best_global[\"pts\"] is None:\n        # fallback deterministic row layout [6,5,6,5,4]\n        rows = [6,5,6,5,4]\n        max_k = max(rows)\n        a = 1.0 / (2.0 * max_k)\n        r0 = min(a, 0.08)\n        v = r0 * sqrt3\n        centers = []\n        top = (1.0 - ((len(rows)-1)*v + 2*r0))/2.0 + r0\n        for i,cnt in enumerate(rows):\n            y = top + i * v\n            offset = r0 if cnt < max_k else 0.0\n            row_width = (cnt-1)*2*r0\n            left = (1.0 - row_width)/2.0 + offset\n            for j in range(cnt):\n                centers.append((left + j*2*r0, y))\n        centers = np.array(centers[:n], dtype=float)\n    else:\n        pts = best_global[\"pts\"]\n        # choose most central 26 points\n        d2 = np.sum((pts - 0.5)**2, axis=1)\n        idx = np.argsort(d2)[:n]\n        centers = pts[idx].copy()\n        centers = np.clip(centers, 1e-12, 1.0-1e-12)\n\n    radii = maximize_radii_gs(centers)\n    return centers, radii, float(np.sum(radii))\n\ndef maximize_radii_gs(centers, tol=1e-10, max_iters=5000):\n    n = centers.shape[0]\n    if n == 0:\n        return np.array([])\n    wall = np.minimum.reduce([centers[:,0], centers[:,1], 1.0-centers[:,0], 1.0-centers[:,1]])\n    wall = np.maximum(wall, 0.0)\n    diff = centers[:,None,:] - centers[None,:,:]\n    d = np.sqrt(np.sum(diff*diff, axis=2))\n    np.fill_diagonal(d, np.inf)\n    r = np.zeros(n, dtype=float)\n    for it in range(max_iters):\n        maxc = 0.0\n        for i in range(n):\n            limits = d[i] - r\n            allowed = float(np.min(limits))\n            newri = min(wall[i], allowed)\n            if newri < 0.0:\n                newri = 0.0\n            change = abs(newri - r[i])\n            if change > 0:\n                r[i] = newri\n                if change > maxc:\n                    maxc = change\n        if maxc < tol:\n            break\n    # proportional pair fixes for any tiny violations\n    for _ in range(6):\n        changed = False\n        for i in range(n):\n            for j in range(i+1, n):\n                dij = d[i,j]\n                if r[i] + r[j] > dij:\n                    tot = r[i] + r[j]\n                    if tot <= 0:\n                        if r[i] != 0.0 or r[j] != 0.0:\n                            r[i] = r[j] = 0.0\n                            changed = True\n                    else:\n                        scale = dij / tot\n                        newi = r[i]*scale\n                        newj = r[j]*scale\n                        if newi != r[i] or newj != r[j]:\n                            r[i], r[j] = newi, newj\n                            changed = True\n        if not changed:\n            break\n    r = np.minimum(r, wall)\n    r = np.maximum(r, 0.0)\n    return r\n\n# EVOLVE-BLOCK-END\n\n# Fixed runner and visualization interface (unchanged)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(7,7))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect(\"equal\")\n    ax.add_patch(plt.Rectangle((0,0),1,1,fill=False,edgecolor='k',linewidth=1))\n    for i,(c,r) in enumerate(zip(centers, radii)):\n        ax.add_patch(Circle(c, r, alpha=0.6, edgecolor='k'))\n        ax.text(c[0], c[1], str(i), ha='center', va='center', fontsize=7)\n    plt.title(f\"n={len(centers)}, sum radii={sum(radii):.6f}\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, total = run_packing()\n    print(f\"Sum of radii: {total:.6f}\")\n    # visualize(centers, radii)", "language": "python", "parent_id": "1b6946c2-0ae3-4246-be23-f7f044239abd", "generation": 3, "timestamp": 1759950679.030771, "iteration_found": 48, "metrics": {"validity": 1.0, "sum_radii": 1.708720096586099, "target_ratio": 0.6484706248903602, "combined_score": 0.6484706248903602, "eval_time": 0.06042194366455078}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Full rewrite", "parent_metrics": {"validity": 1.0, "sum_radii": 1.959338634330761, "target_ratio": 0.7435820244139512, "combined_score": 0.7435820244139512, "eval_time": 0.09163594245910645}, "island": 0}, "prompts": {"full_rewrite_user": {"system": "You are an expert mathematician specializing in circle packing problems and computational geometry. Your task is to improve a constructor function that directly produces a specific arrangement of 26 circles in a unit square, maximizing the sum of their radii. The AlphaEvolve paper achieved a sum of 2.635 for n=26.\n\nKey geometric insights:\n- Circle packings often follow hexagonal patterns in the densest regions\n- Maximum density for infinite circle packing is pi/(2*sqrt(3)) \u2248 0.9069\n- Edge effects make square container packing harder than infinite packing\n- Circles can be placed in layers or shells when confined to a square\n- Similar radius circles often form regular patterns, while varied radii allow better space utilization\n- Perfect symmetry may not yield the optimal packing due to edge effects\n\nFocus on designing an explicit constructor that places each circle in a specific position, rather than an iterative search algorithm.\n", "user": "# Current Program Information\n- Fitness: 0.7436\n- Feature coordinates: No feature coordinates\n- Focus areas: - Fitness declined: 0.8249 \u2192 0.7436. Consider revising recent changes.\n- Consider simplifying - code length exceeds 500 characters\n\n\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 3\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.1737, target_ratio: 0.8249, combined_score: 0.8249, eval_time: 0.4660\n- Outcome: Mixed results\n\n### Attempt 2\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.4090, target_ratio: 0.9142, combined_score: 0.9142, eval_time: 0.6519\n- Outcome: Mixed results\n\n### Attempt 1\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.4390, target_ratio: 0.9256, combined_score: 0.9256, eval_time: 0.0564\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.9256)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for n=26 circle packing in the unit square.\n\nStrategy:\n - Try to build a hexagonal grid of equal circles by binary-searching the largest\n   equal radius r that yields >=26 grid centers inside [r,1-r], for two\n   horizontal offset choices (0 or r) and several vertical shifts.\n - From the best grid (largest r), select 26 centers (center-first ordering),\n   then run a Gauss-Seidel-style monotone growth routine that maximizes each\n   r_i := min(dist_to_wall, min_j (d_ij - r_j)). This yields varied radii\n   and typically a large total radius sum.\n - Fallback to a deterministic row layout if no grid is found.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n_needed = 26\n    sqrt3 = np.sqrt(3.0)\n\n    def gen_grid(r, horiz_offset, vert_shift):\n        pts = []\n        # start at y = r + vert_shift, step sqrt3 * r\n        y = r + vert_shift\n        row = 0\n        while y <= 1.0 - r + 1e-12:\n            offset = (row % 2) * r + horiz_offset\n            x = r + offset\n            while x <= 1.0 - r + 1e-12:\n                pts.append((x, y))\n                x += 2.0 * r\n            y += sqrt3 * r\n            row += 1\n        if not pts:\n            return np.empty((0,2))\n        pts = np.array(pts)\n        return pts\n\n    def find_best_for_offset(h_offset_flag):\n        # h_offset_flag: None means horiz_offset = r, otherwise numeric offset\n        lo, hi = 1e-8, 0.5\n        best_r = 0.0\n        best_pts = None\n        for _ in range(40):\n            mid = 0.5 * (lo + hi)\n            found = False\n            for frac in np.linspace(0.0, 0.5, 5):\n                vert_shift = frac * sqrt3 * mid\n                ho = (mid if h_offset_flag is None else h_offset_flag)\n                pts = gen_grid(mid, ho, vert_shift)\n                if pts.shape[0] >= n_needed:\n                    found = True\n                    break\n            if found:\n                best_r = mid\n                lo = mid\n            else:\n                hi = mid\n        if best_r > 0:\n            # pick a vertical shift that gives a stable centered set\n            best_pts = None\n            for frac in np.linspace(0.0, 0.5, 9):\n                vert_shift = frac * sqrt3 * best_r\n                ho = (best_r if h_offset_flag is None else h_offset_flag)\n                pts = gen_grid(best_r, ho, vert_shift)\n                if pts.shape[0] >= n_needed:\n                    # sort by y descending, then x ascending to pick center-first\n                    pts_sorted = pts[np.lexsort((pts[:,0], -pts[:,1]))]\n                    if best_pts is None or pts_sorted.shape[0] > best_pts.shape[0]:\n                        best_pts = pts_sorted\n            if best_pts is None:\n                best_pts = gen_grid(best_r, (best_r if h_offset_flag is None else h_offset_flag), 0.0)\n        return best_r, best_pts\n\n    best_global = {\"r\": 0.0, \"pts\": None}\n    # try two horizontal choices: offset 0 or offset=r (encoded as None)\n    for h_flag in (0.0, None):\n        r_val, pts = find_best_for_offset(h_flag)\n        if pts is not None and r_val > best_global[\"r\"]:\n            best_global[\"r\"] = r_val\n            best_global[\"pts\"] = pts.copy()\n\n    if best_global[\"pts\"] is None:\n        # fallback deterministic row layout (guarantees feasibility)\n        rows = [6,5,6,5,4]\n        max_k = max(rows)\n        r_h = 1.0 / (2.0 * max_k)\n        r_v = 1.0 / (2.0 + (len(rows) - 1) * sqrt3)\n        base_r = min(r_h, r_v) * 0.999\n        vert_span = 2.0 * base_r + (len(rows) - 1) * base_r * sqrt3\n        top = (1.0 - vert_span) / 2.0 + base_r\n        dy = base_r * sqrt3\n        centers = []\n        for row_idx, k in enumerate(rows):\n            row_span = 2.0 * base_r * k\n            left = (1.0 - row_span) / 2.0 + base_r\n            xs = left + np.arange(k) * (2.0 * base_r)\n            y = top + row_idx * dy\n            for x in xs:\n                centers.append((x, y))\n        centers = np.array(centers[:n_needed], dtype=float)\n    else:\n        pts = best_global[\"pts\"]\n        # select first n_needed in stable center-first order\n        centers = pts[:n_needed].copy()\n        centers = np.clip(centers, 1e-12, 1.0 - 1e-12)\n\n    radii = maximize_radii_gs(centers)\n    return centers, radii, float(np.sum(radii))\n\n\ndef maximize_radii_gs(centers, tol=1e-10, max_iters=5000):\n    \"\"\"\n    Gauss-Seidel-style monotone growth:\n      r_i <- min( wall_i, min_j (d_ij - r_j) )\n    This finds the maximal feasible radii vector (pointwise) for fixed centers\n    under r_i >= 0, r_i <= wall_i, r_i + r_j <= d_ij.\n    \"\"\"\n    n = centers.shape[0]\n    wall = np.minimum.reduce([centers[:,0], centers[:,1], 1.0-centers[:,0], 1.0-centers[:,1]])\n    wall = np.maximum(wall, 0.0)\n    diff = centers[:, None, :] - centers[None, :, :]\n    dists = np.sqrt(np.sum(diff*diff, axis=2))\n    np.fill_diagonal(dists, np.inf)\n\n    r = np.zeros(n, dtype=float)\n    for it in range(max_iters):\n        max_change = 0.0\n        for i in range(n):\n            limits = dists[i] - r  # d_ij - r_j\n            allowed_by_neighbors = float(np.min(limits))\n            new_ri = allowed_by_neighbors\n            if new_ri > wall[i]:\n                new_ri = wall[i]\n            if new_ri < 0.0:\n                new_ri = 0.0\n            change = abs(new_ri - r[i])\n            if change > 0:\n                r[i] = new_ri\n                if change > max_change:\n                    max_change = change\n        if max_change < tol:\n            break\n\n    # final safety: remove any tiny violations by proportional scaling on violating pairs\n    for _ in range(4):\n        changed = False\n        for i in range(n):\n            for j in range(i+1, n):\n                dij = dists[i,j]\n                if r[i] + r[j] > dij:\n                    tot = r[i] + r[j]\n                    if tot <= 0:\n                        r[i] = r[j] = 0.0\n                    else:\n                        scale = dij / tot\n                        new_i = r[i] * scale\n                        new_j = r[j] * scale\n                        if new_i != r[i] or new_j != r[j]:\n                            r[i], r[j] = new_i, new_j\n                            changed = True\n        if not changed:\n            break\n    r = np.minimum(r, wall)\n    r = np.maximum(r, 0.0)\n    return r\n\n# EVOLVE-BLOCK-END\n\n# Fixed runner and visualization (unchanged interface)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(8,8))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect(\"equal\")\n    ax.add_patch(plt.Rectangle((0,0),1,1,fill=False,edgecolor='k',linewidth=1))\n    for i,(c,r) in enumerate(zip(centers, radii)):\n        ax.add_patch(Circle(c, r, alpha=0.6, edgecolor='k'))\n        ax.text(c[0], c[1], str(i), ha='center', va='center', fontsize=8)\n    plt.title(f\"n={len(centers)}, sum radii={sum(radii):.6f}\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, total = run_packing()\n    print(f\"Sum of radii: {total:.6f}\")\n    # visualize(centers, radii)\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.4390), Performs well on target_ratio (0.9256), Performs well on combined_score (0.9256), Performs well on eval_time (0.0564)\n\n### Program 2 (Score: 0.9142)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for 26 circles in the unit square.\n\nCombines two constructive strategies and picks the best:\n 1) Binary-search equal-radius hex-grid alignments (offset=0 or offset=r),\n    with small vertical/horizontal shifts; take first 26 centers and then\n    maximize per-center radii (allows radii to vary and increases sum).\n 2) Parametric hex-row layout rows=[6,5,6,5,4] with a spacing sweep and\n    small global x-shifts / vertical scaling; then maximize radii.\n\nRadii maximization: start from wall-limited radii, alternately repair pair\nviolations by shrinking the larger, and grow each circle to the max allowed\nby current neighbors; finish with proportional fixes. Deterministic.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n_needed = 26\n    sqrt3 = np.sqrt(3.0)\n\n    def gen_hex_grid(r, use_offset_r, vert_shift, dx):\n        pts = []\n        row = 0\n        y = r + vert_shift\n        while y <= 1.0 - r + 1e-12:\n            offset = (r if use_offset_r and (row % 2 == 1) else 0.0)\n            x = r + dx + offset\n            while x <= 1.0 - r + 1e-12:\n                pts.append((x, y))\n                x += 2.0 * r\n            row += 1\n            y += sqrt3 * r\n        if not pts:\n            return np.empty((0,2))\n        pts = np.array(pts)\n        # stable ordering: center-first (high y), then left-to-right\n        order = np.lexsort((pts[:,0], -pts[:,1]))\n        return pts[order]\n\n    def max_r_for_alignment(use_offset_r):\n        lo, hi = 1e-8, 0.5\n        best_r, best_pts = 0.0, None\n        vert_fracs = np.linspace(0.0, 0.5, 5)\n        dx_vals = np.linspace(-0.01, 0.01, 5)\n        for _ in range(40):\n            mid = 0.5*(lo+hi)\n            ok = False\n            for frac in vert_fracs:\n                vs = frac * sqrt3 * mid\n                for dx in dx_vals:\n                    pts = gen_hex_grid(mid, use_offset_r, vs, dx)\n                    if pts.shape[0] >= n_needed:\n                        ok = True\n                        break\n                if ok:\n                    break\n            if ok:\n                best_r = mid\n                lo = mid\n            else:\n                hi = mid\n        if best_r > 0:\n            # choose best vertical shift and dx giving most symmetric/centered result\n            best_pts = None\n            for frac in np.linspace(0.0, 0.5, 9):\n                vs = frac * sqrt3 * best_r\n                for dx in dx_vals:\n                    pts = gen_hex_grid(best_r, use_offset_r, vs, dx)\n                    if pts.shape[0] >= n_needed:\n                        pts_sorted = pts[np.lexsort((pts[:,0], -pts[:,1]))]\n                        if best_pts is None or pts_sorted.shape[0] > best_pts.shape[0]:\n                            best_pts = pts_sorted\n            if best_pts is None:\n                best_pts = gen_hex_grid(best_r, use_offset_r, 0.0, 0.0)\n        return best_r, best_pts\n\n    candidates = []\n\n    # Strategy A: try both offset modes\n    for flag in (False, True):\n        r_val, pts = max_r_for_alignment(flag)\n        if pts is not None:\n            centers = pts[:n_needed].copy()\n            centers = np.clip(centers, 1e-12, 1.0 - 1e-12)\n            radii = maximize_radii(centers)\n            candidates.append((float(np.sum(radii)), centers, radii))\n\n    # Strategy B: parametric hex-rows [6,5,6,5,4]\n    rows = [6,5,6,5,4]\n    m = len(rows)\n    kmax = max(rows)\n    a_max = 1.0 / (kmax - 1)\n    a_min = 0.035\n    if a_min >= a_max:\n        a_min = a_max * 0.6\n    dx_vals = np.linspace(-0.02, 0.02, 7)\n    for a in np.linspace(a_max*0.995, a_min, 28):\n        v0 = a * sqrt3 / 2.0\n        for v_scale in (0.96, 0.98, 1.0, 1.02):\n            v = v0 * v_scale\n            total_h = (m - 1) * v\n            if total_h > 1.0: continue\n            y0 = (1.0 - total_h) / 2.0\n            for short_offset_half in (False, True):\n                for dx in dx_vals:\n                    centers = []\n                    for r_idx, cnt in enumerate(rows):\n                        y = y0 + r_idx * v\n                        offset = (a/2.0 if (cnt < kmax and short_offset_half) else 0.0)\n                        row_width = (cnt - 1) * a\n                        x0 = (1.0 - row_width) / 2.0 + offset + dx\n                        for c in range(cnt):\n                            centers.append((x0 + c * a, y))\n                    centers = np.array(centers[:n_needed], dtype=float)\n                    if centers.shape[0] != n_needed:\n                        continue\n                    centers = np.clip(centers, 1e-12, 1.0 - 1e-12)\n                    radii = maximize_radii(centers)\n                    candidates.append((float(np.sum(radii)), centers, radii))\n\n    # Choose best candidate by sum of radii\n    if not candidates:\n        # fallback simple centered rows\n        a = a_max * 0.98\n        v = a * sqrt3 / 2.0\n        centers = []\n        y0 = (1.0 - (m - 1) * v) / 2.0\n        for r_idx, cnt in enumerate(rows):\n            y = y0 + r_idx * v\n            offset = (a/2.0) if (cnt < kmax) else 0.0\n            row_width = (cnt - 1) * a\n            x0 = (1.0 - row_width)/2.0 + offset\n            for c in range(cnt):\n                centers.append((np.clip(x0 + c * a, 1e-12, 1.0 - 1e-12), y))\n        centers = np.array(centers[:n_needed], dtype=float)\n        radii = maximize_radii(centers)\n        return centers, radii, float(np.sum(radii))\n    best = max(candidates, key=lambda t: t[0])\n    return best[1], best[2], best[0]\n\n\ndef maximize_radii(centers, tol=1e-10, max_cycles=2000):\n    n = centers.shape[0]\n    if n == 0:\n        return np.array([])\n    # wall distances\n    wall = np.minimum.reduce([centers[:,0], centers[:,1], 1.0-centers[:,0], 1.0-centers[:,1]])\n    wall = np.maximum(wall, 0.0)\n    # pairwise distances\n    diff = centers[:, None, :] - centers[None, :, :]\n    dists = np.sqrt(np.sum(diff*diff, axis=2))\n    np.fill_diagonal(dists, np.inf)\n    # start at wall limits\n    r = wall.copy()\n\n    for cycle in range(max_cycles):\n        max_change = 0.0\n        # Reduction sweep: for each pair that violates, shrink the larger\n        for i in range(n):\n            ri = r[i]\n            s = ri + r\n            viol = np.where(s > dists[i] + 1e-12)[0]\n            for j in viol:\n                if j == i: continue\n                dij = dists[i,j]\n                if r[i] >= r[j]:\n                    new_ri = max(0.0, dij - r[j])\n                    ch = ri - new_ri\n                    if ch > 0:\n                        r[i] = new_ri\n                        ri = new_ri\n                        if ch > max_change: max_change = ch\n                else:\n                    new_rj = max(0.0, dij - r[i])\n                    ch = r[j] - new_rj\n                    if ch > 0:\n                        r[j] = new_rj\n                        if ch > max_change: max_change = ch\n        # enforce wall\n        over = r > wall\n        if np.any(over):\n            diffv = float(np.max(r[over] - wall[over]))\n            r[over] = wall[over]\n            if diffv > max_change: max_change = diffv\n        # Growth sweep: increase each to min(wall_i, min_j(d_ij - r_j))\n        for i in range(n):\n            lims = dists[i,:] - r\n            target = min(wall[i], float(np.min(lims)))\n            if target < 0.0: target = 0.0\n            ch = target - r[i]\n            if ch > 0:\n                r[i] = target\n                if ch > max_change: max_change = ch\n        if max_change < tol:\n            break\n\n    # Final proportional fixes\n    for _ in range(6):\n        changed = False\n        for i in range(n):\n            for j in range(i+1, n):\n                dij = dists[i,j]\n                if r[i] + r[j] > dij:\n                    tot = r[i] + r[j]\n                    if tot <= 0:\n                        if r[i] != 0.0 or r[j] != 0.0:\n                            r[i] = r[j] = 0.0\n                            changed = True\n                    else:\n                        scale = dij / tot\n                        new_i = r[i] * scale\n                        new_j = r[j] * scale\n                        if new_i != r[i] or new_j != r[j]:\n                            r[i], r[j] = new_i, new_j\n                            changed = True\n        if not changed:\n            break\n    r = np.clip(r, 0.0, wall)\n    return r\n\n\n# Fixed API\ndef run_packing():\n    centers, radii, s = construct_packing()\n    return centers, radii, s\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(6,6))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect(\"equal\")\n    ax.add_patch(plt.Rectangle((0,0),1,1,fill=False,linewidth=1))\n    for c,r in zip(centers, radii):\n        ax.add_patch(Circle(c, r, alpha=0.6, edgecolor='k'))\n    plt.title(f\"n={len(centers)}, sum={sum(radii):.6f}\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, total = run_packing()\n    print(f\"Sum of radii: {total:.6f}\")\n    # visualize(centers, radii)\n# EVOLVE-BLOCK-END\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.4090), Performs well on target_ratio (0.9142), Performs well on combined_score (0.9142), Performs well on eval_time (0.6519)\n\n### Program 3 (Score: 0.8249)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for 26 circles in the unit square.\n\nStrategy:\n- Use the empirically good row pattern [6,5,6,5,4].\n- Sample a small family of hex-like layouts (horizontal spacing, slight vertical scale,\n  two offset modes and small global x-shifts) and pick the layout that yields the\n  largest sum of feasible radii.\n- Radii solver alternates deterministic reduction passes (repair violating pairs by\n  shrinking the larger) and growth passes (increase each radius to the largest\n  allowed by current neighbors and the wall). Finish with proportional fixes.\nThis is a compact, deterministic constructor designed to boost total radius sum.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    rows = [6, 5, 6, 5, 4]\n    m = len(rows)\n    kmax = max(rows)\n\n    # horizontal spacing feasible range (widest row must fit)\n    a_max = 1.0 / (kmax - 1)        # (kmax-1)*a <= 1 => a <= 1/(kmax-1)\n    a_min = 0.035\n    if a_min >= a_max:\n        a_min = a_max * 0.6\n\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    sqrt3 = np.sqrt(3.0)\n\n    # Sample a near the upper bound more densely\n    for a in np.linspace(a_max * 0.99, a_min, 28):\n        # nominal vertical step for hex packing, allow slight scaling\n        v0 = a * sqrt3 / 2.0\n        for v_scale in (0.96, 0.98, 1.0, 1.02):\n            v = v0 * v_scale\n            total_h = (m - 1) * v\n            if total_h > 1.0:\n                continue\n            # two offset modes for shorter rows: centered or half-step\n            for short_half in (False, True):\n                # small global horizontal shifts\n                for dx in np.linspace(-0.02, 0.02, 5):\n                    centers = np.zeros((n, 2), dtype=float)\n                    idx = 0\n                    y0 = (1.0 - total_h) / 2.0\n                    valid = True\n                    for r_idx, cnt in enumerate(rows):\n                        y = y0 + r_idx * v\n                        if cnt < kmax and short_half:\n                            offset = a / 2.0\n                        else:\n                            offset = 0.0\n                        row_width = (cnt - 1) * a\n                        x0 = (1.0 - row_width) / 2.0 + offset + dx\n                        for c in range(cnt):\n                            if idx >= n:\n                                break\n                            x = x0 + c * a\n                            # small clamp if numerical drift pushes out\n                            if x <= 0.0 + 1e-12 or x >= 1.0 - 1e-12 or y <= 0.0 + 1e-12 or y >= 1.0 - 1e-12:\n                                x = np.clip(x, 1e-12, 1.0 - 1e-12)\n                                y = np.clip(y, 1e-12, 1.0 - 1e-12)\n                            centers[idx, 0] = x\n                            centers[idx, 1] = y\n                            idx += 1\n                    if idx < n:\n                        valid = False\n                    if not valid:\n                        continue\n                    radii = maximize_radii_alt(centers)\n                    s = float(np.sum(radii))\n                    if s > best_sum:\n                        best_sum = s\n                        best_centers = centers.copy()\n                        best_radii = radii.copy()\n\n    # Fallback simple centered hex-like layout\n    if best_centers is None:\n        a = a_max * 0.98\n        v = a * sqrt3 / 2.0\n        centers_list = []\n        y0 = (1.0 - (m - 1) * v) / 2.0\n        for r_idx, cnt in enumerate(rows):\n            y = y0 + r_idx * v\n            offset = (a / 2.0) if (cnt < kmax) else 0.0\n            row_width = (cnt - 1) * a\n            x0 = (1.0 - row_width) / 2.0 + offset\n            for c in range(cnt):\n                x = np.clip(x0 + c * a, 1e-12, 1.0 - 1e-12)\n                centers_list.append((x, y))\n        best_centers = np.array(centers_list[:n], dtype=float)\n        best_radii = maximize_radii_alt(best_centers)\n        best_sum = float(np.sum(best_radii))\n\n    return best_centers, best_radii, float(best_sum)\n\n\ndef maximize_radii_alt(centers, tol=1e-9, max_cycles=1000):\n    \"\"\"Alternate reduction/growth solver for maximizing sum of radii under pair and wall constraints.\"\"\"\n    n = centers.shape[0]\n    if n == 0:\n        return np.array([])\n\n    wall = np.minimum.reduce([centers[:, 0], centers[:, 1],\n                              1.0 - centers[:, 0], 1.0 - centers[:, 1]])\n    wall = np.maximum(wall, 0.0)\n\n    diff = centers[:, None, :] - centers[None, :, :]\n    dists = np.sqrt(np.sum(diff * diff, axis=2))\n    np.fill_diagonal(dists, np.inf)\n\n    # start from wall-limited radii (greedy)\n    r = wall.copy()\n\n    for cycle in range(max_cycles):\n        max_change = 0.0\n        # Reduction sweep: repair violating pairs by shrinking the larger circle\n        for i in range(n):\n            ri = r[i]\n            # compute violation indices where ri + rj > d_ij\n            sum_with_i = ri + r\n            viol = np.where(sum_with_i > dists[i] + 1e-14)[0]\n            for j in viol:\n                if j == i:\n                    continue\n                dij = dists[i, j]\n                # shrink the larger of r[i], r[j] to satisfy r_i + r_j <= dij\n                if r[i] >= r[j]:\n                    new_ri = max(0.0, dij - r[j])\n                    ch = ri - new_ri\n                    if ch > 0:\n                        r[i] = new_ri\n                        ri = new_ri\n                        if ch > max_change:\n                            max_change = ch\n                else:\n                    new_rj = max(0.0, dij - r[i])\n                    ch = r[j] - new_rj\n                    if ch > 0:\n                        r[j] = new_rj\n                        if ch > max_change:\n                            max_change = ch\n\n        # enforce wall limits (prevent overshoot)\n        over = r > wall\n        if np.any(over):\n            diffv = np.max(r[over] - wall[over])\n            r[over] = wall[over]\n            if diffv > max_change:\n                max_change = diffv\n\n        # Growth sweep: try to grow each circle to allowed max given neighbors\n        for i in range(n):\n            allowed = wall[i]\n            limits = dists[i, :] - r\n            # min over neighbors gives the tightest bound (self diag is inf)\n            minlim = np.min(limits)\n            target = min(allowed, minlim)\n            if target < 0.0:\n                target = 0.0\n            ch = target - r[i]\n            if ch > 0:\n                r[i] = target\n                if ch > max_change:\n                    max_change = ch\n\n        if max_change < tol:\n            break\n\n    # Final proportional fixes for any remaining tiny violations\n    for _ in range(4):\n        changed = False\n        for i in range(n):\n            for j in range(i + 1, n):\n                dij = dists[i, j]\n                if r[i] + r[j] > dij:\n                    tot = r[i] + r[j]\n                    if tot <= 0:\n                        if r[i] != 0.0 or r[j] != 0.0:\n                            r[i] = r[j] = 0.0\n                            changed = True\n                    else:\n                        scale = dij / tot\n                        new_i = r[i] * scale\n                        new_j = r[j] * scale\n                        if new_i != r[i] or new_j != r[j]:\n                            r[i], r[j] = new_i, new_j\n                            changed = True\n        if not changed:\n            break\n\n    r = np.clip(r, 0.0, wall)\n    return r\n\n\n# Fixed API\ndef run_packing():\n    centers, radii, s = construct_packing()\n    return centers, radii, s\n\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(7, 7))\n    ax.set_xlim(0, 1); ax.set_ylim(0, 1); ax.set_aspect(\"equal\")\n    ax.add_patch(plt.Rectangle((0, 0), 1, 1, fill=False, linewidth=1))\n    for c, r in zip(centers, radii):\n        ax.add_patch(Circle(c, r, alpha=0.6, edgecolor='k'))\n    plt.title(f\"n={len(centers)}, sum={sum(radii):.6f}\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, total = run_packing()\n    print(f\"Sum of radii: {total:.6f}\")\n    # visualize(centers, radii)\n# EVOLVE-BLOCK-END\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.1737), Performs well on target_ratio (0.8249), Performs well on combined_score (0.8249), Performs well on eval_time (0.4660)\n\n\n\n## Diverse Programs\n\n### Program D1 (Score: 0.8231)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Improved deterministic constructor for 26 circles in the unit square.\n\nKey ideas:\n- Use the hex-like row pattern [6,5,6,5,4].\n- Sample a small grid of horizontal spacings, small global x-shifts and two offset modes\n  (shorter rows either centered or half-step shifted) and slight vertical compression/stretch.\n- For each candidate placement compute radii with an enhanced constraint solver:\n    * start from border limits\n    * alternate reduction sweeps (repair violating pairs by shrinking the larger)\n      and growth sweeps (increase each radius to the largest allowed by current neighbors)\n    * finalize with proportional pairwise fixes\n  This alternation lets radii both relax down when needed and regrow to better fill free space,\n  producing larger total radii than greedy-only reductions.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    rows = [6, 5, 6, 5, 4]\n    m = len(rows)\n    kmax = max(rows)\n    # allowable horizontal spacing: ensure widest row fits\n    a_max = 1.0 / (kmax - 1)  # (kmax-1)*a <= 1\n    a_min = 0.035\n    if a_min >= a_max:\n        a_min = a_max * 0.6\n\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    sqrt3 = np.sqrt(3.0)\n\n    # sample horizontal spacings more densely near a_max\n    for a in np.linspace(a_max * 0.98, a_min, 20):\n        # nominal hex vertical step (can scale slightly)\n        v0 = a * sqrt3 / 2.0\n        for v_scale in (0.96, 0.98, 1.0, 1.02):\n            v = v0 * v_scale\n            total_h = (m - 1) * v\n            if total_h > 1.0:\n                continue\n            # two offset modes: 0 or half-step for shorter rows\n            for short_offset_half in (False, True):\n                # small global horizontal shifts to allow better edge packing\n                for dx in np.linspace(-0.02, 0.02, 5):\n                    centers = np.zeros((n, 2))\n                    idx = 0\n                    y0 = (1.0 - total_h) / 2.0\n                    ok = True\n                    for r_idx, cnt in enumerate(rows):\n                        y = y0 + r_idx * v\n                        # compute offset for this row\n                        if cnt < kmax and short_offset_half:\n                            offset = a / 2.0\n                        else:\n                            offset = 0.0\n                        row_width = (cnt - 1) * a\n                        x0 = (1.0 - row_width) / 2.0 + offset + dx\n                        # place points and ensure inside [0,1] (clamp edges)\n                        for c in range(cnt):\n                            if idx >= n:\n                                break\n                            x = x0 + c * a\n                            if x <= 0.0 + 1e-9 or x >= 1.0 - 1e-9 or y <= 0.0 + 1e-9 or y >= 1.0 - 1e-9:\n                                # allow small clamping but if center would be too far out, mark not ok\n                                x = np.clip(x, 1e-9, 1.0 - 1e-9)\n                                y = np.clip(y, 1e-9, 1.0 - 1e-9)\n                            centers[idx, 0] = x\n                            centers[idx, 1] = y\n                            idx += 1\n                    if idx < n:\n                        ok = False\n                    if not ok:\n                        continue\n                    radii = maximize_radii_refined(centers)\n                    s = float(np.sum(radii))\n                    if s > best_sum:\n                        best_sum = s\n                        best_centers = centers.copy()\n                        best_radii = radii.copy()\n\n    # Fallback deterministic center if sampling failed\n    if best_centers is None:\n        # simple centered hex-like placement\n        a = a_max * 0.98\n        v = a * sqrt3 / 2.0\n        centers = []\n        y0 = (1.0 - (m - 1) * v) / 2.0\n        for r_idx, cnt in enumerate(rows):\n            y = y0 + r_idx * v\n            offset = (a / 2.0) if (cnt < kmax) else 0.0\n            row_width = (cnt - 1) * a\n            x0 = (1.0 - row_width) / 2.0 + offset\n            for c in range(cnt):\n                centers.append((np.clip(x0 + c * a, 1e-9, 1.0 - 1e-9), y))\n        best_centers = np.array(centers[:n], dtype=float)\n        best_radii = maximize_radii_refined(best_centers)\n        best_sum = float(np.sum(best_radii))\n\n    return best_centers, best_radii, float(best_sum)\n\n\ndef maximize_radii_refined(centers, tol=1e-9, max_cycles=1000):\n    \"\"\"\n    Alternating repair/increase solver for the LP:\n      maximize sum r_i\n      s.t. 0 <= r_i <= wall_i, r_i + r_j <= d_ij.\n\n    Algorithm:\n      - initialize r = wall (border-limited)\n      - repeat cycles:\n          1) reduction sweep: for every violating pair shrink the larger to d_ij - other\n          2) growth sweep: for each i increase r_i to min(wall_i, min_j(d_ij - r_j))\n      - finish with proportional pairwise fixes\n    This tends to reach a large feasible sum and is deterministic.\n    \"\"\"\n    n = centers.shape[0]\n    if n == 0:\n        return np.array([])\n\n    wall = np.minimum.reduce([centers[:, 0], centers[:, 1],\n                              1.0 - centers[:, 0], 1.0 - centers[:, 1]])\n    wall = np.maximum(wall, 0.0)\n\n    diff = centers[:, None, :] - centers[None, :, :]\n    dists = np.sqrt(np.sum(diff * diff, axis=2))\n    np.fill_diagonal(dists, np.inf)\n\n    # start from border-limited radii\n    r = wall.copy()\n\n    for cycle in range(max_cycles):\n        max_change = 0.0\n        # 1) Reduction sweep (repair violations by shrinking larger)\n        for i in range(n):\n            ri = r[i]\n            # vectorized candidate check\n            s = ri + r\n            viol = np.where(s > dists[i] + 1e-12)[0]\n            for j in viol:\n                if j == i:\n                    continue\n                dij = dists[i, j]\n                # shrink the larger radius\n                if r[i] >= r[j]:\n                    new_ri = max(0.0, dij - r[j])\n                    ch = ri - new_ri\n                    if ch > 0:\n                        r[i] = new_ri\n                        ri = new_ri\n                        if ch > max_change:\n                            max_change = ch\n                else:\n                    new_rj = max(0.0, dij - r[i])\n                    ch = r[j] - new_rj\n                    if ch > 0:\n                        r[j] = new_rj\n                        if ch > max_change:\n                            max_change = ch\n\n        # enforce wall limits (no increases here)\n        over = r > wall\n        if np.any(over):\n            diffv = np.max(r[over] - wall[over])\n            r[over] = wall[over]\n            if diffv > max_change:\n                max_change = diffv\n\n        # 2) Growth sweep: try to grow each circle to the largest allowed by current neighbors\n        for i in range(n):\n            allowed = wall[i]\n            # compute min_j (d_ij - r_j)\n            lims = dists[i, :] - r\n            # ignore self (inf)\n            minlim = np.min(lims)\n            target = min(allowed, minlim)\n            if target < 0.0:\n                target = 0.0\n            ch = target - r[i]\n            if ch > 0:\n                r[i] = target\n                if ch > max_change:\n                    max_change = ch\n\n        if max_change < tol:\n            break\n\n    # Final proportional fixes for any remaining tiny violations\n    for _ in range(4):\n        changed = False\n        for i in range(n):\n            for j in range(i + 1, n):\n                dij = dists[i, j]\n                if r[i] + r[j] > dij:\n                    tot = r[i] + r[j]\n                    if tot <= 0:\n                        if r[i] != 0.0 or r[j] != 0.0:\n                            r[i] = r[j] = 0.0\n                            changed = True\n                    else:\n                        scale = dij / tot\n                        new_i = r[i] * scale\n                        new_j = r[j] * scale\n                        if new_i != r[i] or new_j != r[j]:\n                            r[i], r[j] = new_i, new_j\n                            changed = True\n        if not changed:\n            break\n\n    r = np.clip(r, 0.0, wall)\n    return r\n\n\n# Fixed API\ndef run_packing():\n    centers, radii, s = construct_packing()\n    return centers, radii, s\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(6,6))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect(\"equal\")\n    ax.add_patch(plt.Rectangle((0,0),1,1,fill=False,linewidth=1))\n    for c,r in zip(centers, radii):\n        ax.add_patch(Circle(c, r, alpha=0.6, edgecolor='k'))\n    plt.title(f\"n={len(centers)}, sum={sum(radii):.6f}\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, total = run_packing()\n    print(f\"Sum of radii: {total:.6f}\")\n    # visualize(centers, radii)\n# EVOLVE-BLOCK-END\n```\nKey features: Alternative approach to validity, Alternative approach to sum_radii\n\n### Program D2 (Score: 0.8231)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for 26 circles in the unit square.\nImproved layout: use the hex-like row pattern [6,5,6,5,4], sweep a small set of\nhorizontal spacings, tiny global shifts and two offset modes, and for each\ncandidate compute near-maximal radii with an alternating repair/grow solver.\nThis yields larger total radii than a single fixed layout.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    rows = [6,5,6,5,4]\n    m = len(rows)\n    kmax = max(rows)\n    sqrt3 = np.sqrt(3.0)\n\n    # horizontal spacing bounds (ensure widest row fits)\n    a_max = 1.0 / (kmax - 1)   # (kmax-1)*a <= 1\n    a_min = 0.035\n    if a_min >= a_max:\n        a_min = a_max * 0.6\n\n    best_sum = -1.0\n    best_C = None\n    best_r = None\n\n    # sample spacings (denser near a_max) and a few vertical scales\n    a_vals = np.linspace(a_max * 0.98, a_min, 20)\n    v_scales = (0.96, 0.98, 1.0, 1.02)\n    dx_vals = np.linspace(-0.02, 0.02, 5)\n\n    for a in a_vals:\n        v0 = a * sqrt3 / 2.0\n        for v_scale in v_scales:\n            v = v0 * v_scale\n            total_h = (m - 1) * v\n            if total_h > 1.0 + 1e-12:\n                continue\n            for short_offset_half in (False, True):\n                for dx in dx_vals:\n                    C = np.zeros((n,2), dtype=float)\n                    idx = 0\n                    y0 = (1.0 - total_h) / 2.0\n                    ok = True\n                    for r_idx, cnt in enumerate(rows):\n                        y = y0 + r_idx * v\n                        offset = (a/2.0) if (cnt < kmax and short_offset_half) else 0.0\n                        row_width = (cnt - 1) * a\n                        x0 = (1.0 - row_width) / 2.0 + offset + dx\n                        for c in range(cnt):\n                            if idx >= n:\n                                break\n                            x = x0 + c * a\n                            # quick rejection if center far outside\n                            if x < -1e-6 or x > 1.0 + 1e-6 or y < -1e-6 or y > 1.0 + 1e-6:\n                                ok = False\n                                break\n                            C[idx,0] = np.clip(x, 1e-12, 1.0 - 1e-12)\n                            C[idx,1] = np.clip(y, 1e-12, 1.0 - 1e-12)\n                            idx += 1\n                        if not ok:\n                            break\n                    if not ok or idx < n:\n                        continue\n                    r = maximize_radii_refined(C)\n                    s = float(np.sum(r))\n                    if s > best_sum + 1e-12:\n                        best_sum = s\n                        best_C = C.copy()\n                        best_r = r.copy()\n\n    # fallback deterministic layout if sampling failed\n    if best_C is None:\n        a = a_max * 0.98\n        v = a * sqrt3 / 2.0\n        y0 = (1.0 - (m - 1) * v) / 2.0\n        centers = []\n        for r_idx, cnt in enumerate(rows):\n            y = y0 + r_idx * v\n            offset = (a/2.0) if (cnt < kmax) else 0.0\n            row_width = (cnt - 1) * a\n            x0 = (1.0 - row_width) / 2.0 + offset\n            for c in range(cnt):\n                centers.append((np.clip(x0 + c * a, 1e-12, 1.0-1e-12), y))\n        best_C = np.array(centers[:n], dtype=float)\n        best_r = maximize_radii_refined(best_C)\n        best_sum = float(np.sum(best_r))\n\n    return best_C, best_r, float(best_sum)\n\n\ndef maximize_radii_refined(C, tol=1e-9, max_cycles=800):\n    \"\"\"\n    Alternating shrink/grow solver:\n     - start r = wall (distance to boundary)\n     - reduction sweep: for each violating pair shrink the larger to d_ij - other\n     - growth sweep: increase each r_i to min(wall_i, min_j (d_ij - r_j))\n     - repeat until small change, finalize with proportional pairwise fixes\n    \"\"\"\n    n = C.shape[0]\n    if n == 0:\n        return np.array([])\n\n    wall = np.minimum.reduce([C[:,0], C[:,1], 1.0 - C[:,0], 1.0 - C[:,1]])\n    wall = np.maximum(wall, 0.0)\n\n    diff = C[:, None, :] - C[None, :, :]\n    dists = np.sqrt(np.sum(diff * diff, axis=2))\n    np.fill_diagonal(dists, np.inf)\n\n    r = wall.copy()\n\n    for cycle in range(max_cycles):\n        max_change = 0.0\n        # Reduction: repair violations by shrinking the larger radius\n        for i in range(n):\n            for j in range(i+1, n):\n                dij = dists[i,j]\n                if r[i] + r[j] > dij + 1e-12:\n                    # shrink the larger of the two\n                    if r[i] >= r[j]:\n                        new_i = max(0.0, dij - r[j])\n                        ch = r[i] - new_i\n                        if ch > 0:\n                            r[i] = new_i\n                            if ch > max_change: max_change = ch\n                    else:\n                        new_j = max(0.0, dij - r[i])\n                        ch = r[j] - new_j\n                        if ch > 0:\n                            r[j] = new_j\n                            if ch > max_change: max_change = ch\n\n        # enforce wall limits (shrink if necessary)\n        over = r - wall\n        if np.any(over > 1e-15):\n            mv = float(np.max(np.where(over>0, over, 0.0)))\n            r = np.minimum(r, wall)\n            if mv > max_change: max_change = mv\n\n        # Growth: try to increase each radius to local maximum allowed\n        for i in range(n):\n            allowed = wall[i]\n            lims = dists[i,:] - r\n            minlim = np.min(lims)\n            target = min(allowed, minlim)\n            if target < 0.0:\n                target = 0.0\n            ch = target - r[i]\n            if ch > max_change:\n                max_change = ch\n            if ch > 1e-14:\n                r[i] = target\n\n        if max_change < tol:\n            break\n\n    # Final proportional pairwise fixes for any remaining small violations\n    for _ in range(5):\n        changed = False\n        for i in range(n):\n            for j in range(i+1, n):\n                dij = dists[i,j]\n                if r[i] + r[j] > dij + 1e-12:\n                    tot = r[i] + r[j]\n                    if tot <= 0.0:\n                        if r[i] != 0.0 or r[j] != 0.0:\n                            r[i] = r[j] = 0.0\n                            changed = True\n                    else:\n                        scale = dij / tot\n                        new_i = r[i] * scale\n                        new_j = r[j] * scale\n                        if abs(new_i - r[i]) > 1e-14 or abs(new_j - r[j]) > 1e-14:\n                            r[i], r[j] = new_i, new_j\n                            changed = True\n        if not changed:\n            break\n\n    r = np.clip(r, 0.0, wall)\n    return r\n\n\n# Fixed API\ndef run_packing():\n    centers, radii, s = construct_packing()\n    return centers, radii, s\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(6,6))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect(\"equal\")\n    ax.add_patch(plt.Rectangle((0,0),1,1,fill=False,linewidth=1))\n    for c,r in zip(centers, radii):\n        ax.add_patch(Circle(c, r, alpha=0.6, edgecolor='k'))\n    plt.title(f\"n={len(centers)}, sum={sum(radii):.6f}\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, total = run_packing()\n    print(f\"Sum of radii: {total:.6f}\")\n# EVOLVE-BLOCK-END\n```\nKey features: Alternative approach to validity, Alternative approach to sum_radii\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 0.6490, Type: Alternative)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (improved explicit constructor)\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square\n    using an explicit hex-like layered layout and a robust radius\n    relaxation that maximizes radii subject to non-overlap and border constraints.\n\n    Returns:\n        Tuple of (centers, radii, sum_of_radii)\n    \"\"\"\n    n = 26\n    centers = np.zeros((n, 2))\n\n    # We choose a layered, roughly hexagonal layout (5 rows with counts 5,6,6,6,3)\n    # This balances interior density with edge accommodation.\n    row_counts = [5, 6, 6, 6, 3]\n    num_rows = len(row_counts)\n\n    # Small margin from the square boundary to avoid degenerate zero border distances\n    margin = 0.06\n\n    # Vertical positions evenly spaced between margin and 1-margin\n    ys = np.linspace(margin, 1.0 - margin, num_rows)\n\n    # For horizontal positioning we use two spacings so rows with 6 are denser.\n    # This produces alternating offsets similar to a hex lattice.\n    x_dense = np.linspace(margin, 1.0 - margin, 6)\n    # For rows with fewer points center them in the same footprint\n    idx = 0\n    for r, y in enumerate(ys):\n        k = row_counts[r]\n        if k == 6:\n            # alternate offset to mimic hex packing (shift every other 6-row)\n            offset = (0.5 * (x_dense[1] - x_dense[0])) if (r % 2 == 1) else 0.0\n            xs = x_dense + offset\n            # clip to remain inside [margin, 1-margin]\n            xs = np.clip(xs, margin, 1.0 - margin)\n        else:\n            # center k points within the same horizontal interval\n            xs = np.linspace(margin + 0.5 * (x_dense[1] - x_dense[0]),\n                             1.0 - margin - 0.5 * (x_dense[1] - x_dense[0]),\n                             k)\n        for x in xs:\n            if idx < n:\n                centers[idx, 0] = x\n                centers[idx, 1] = y\n                idx += 1\n    # Safety: if due to indexing we didn't fill all, fill remaining near center\n    while idx < n:\n        centers[idx] = [0.5, 0.5]\n        idx += 1\n\n    # Compute maximal feasible radii for these fixed centers\n    radii = compute_max_radii(centers)\n\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers, tol=1e-6, max_iter=10000):\n    \"\"\"\n    Compute maximal radii for fixed centers inside unit square:\n    - r_i <= distance_to_border(center_i)\n    - for each pair i,j: r_i + r_j <= dist(center_i, center_j)\n\n    We use an iterative relaxation: start with border-limited radii,\n    then repeatedly repair pairwise violations by shrinking the larger\n    circle in any conflicting pair. This greedy-reduction converges\n    quickly and yields a feasible (often near-maximal) solution.\n    \"\"\"\n    n = centers.shape[0]\n    # initial radii limited by border distance\n    radii = np.minimum(centers[:, 0], centers[:, 1])\n    radii = np.minimum(radii, 1.0 - centers[:, 0])\n    radii = np.minimum(radii, 1.0 - centers[:, 1])\n\n    if n <= 1:\n        return radii\n\n    # Precompute pairwise distances\n    diff = centers[:, None, :] - centers[None, :, :]\n    dists = np.sqrt(np.sum(diff * diff, axis=2))\n    # Prevent self-dist being zero from causing division problems\n    np.fill_diagonal(dists, np.inf)\n\n    it = 0\n    while it < max_iter:\n        it += 1\n        max_violation = 0.0\n        # For each unordered pair check overlap\n        for i in range(n):\n            # vectorized over j could be faster, but explicit loop keeps control of shrinking policy\n            for j in range(i + 1, n):\n                dij = dists[i, j]\n                if dij <= 0:\n                    # identical centers -> force one to zero to break symmetry\n                    if radii[j] > radii[i]:\n                        radii[j] = 0.0\n                    else:\n                        radii[i] = 0.0\n                    continue\n                s = radii[i] + radii[j]\n                if s > dij + tol:\n                    # violation amount\n                    viol = s - dij\n                    max_violation = max(max_violation, viol)\n                    # shrink the larger radius preferentially (greedy)\n                    if radii[i] >= radii[j]:\n                        # new ri = max(dij - rj, 0), but also not larger than border distance (already enforced)\n                        new_ri = max(dij - radii[j], 0.0)\n                        # numerical safety clamp\n                        if new_ri < radii[i]:\n                            radii[i] = new_ri\n                    else:\n                        new_rj = max(dij - radii[i], 0.0)\n                        if new_rj < radii[j]:\n                            radii[j] = new_rj\n\n        # After pairwise repairs, ensure border limits still hold (they shouldn't increase)\n        border_limits = np.minimum.reduce([\n            centers[:, 0],\n            centers[:, 1],\n            1.0 - centers[:, 0],\n            1.0 - centers[:, 1],\n        ])\n        # numerical safety: never exceed border limits\n        radii = np.minimum(radii, border_limits)\n\n        if max_violation <= tol:\n            break\n\n    # Final pass: small positive clamp\n    radii = np.maximum(radii, 0.0)\n    return radii\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\", linewidth=0.5)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    visualize(centers, radii)\n```\nUnique approach: Modification: Full rewrite, Excellent validity (1.000), Excellent sum_radii (1.710)\n\n### Inspiration 2 (Score: 0.6726, Type: Alternative)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 using an explicit hex-like layout\nand a tight uniform-scaling of conservative per-circle radii to maximize sum.\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    n = 26\n    # Row counts chosen to create a dense central hex-like patch with edge accommodation\n    counts = [5, 6, 5, 5, 5]  # sums to 26\n    rows = len(counts)\n    max_cols = max(counts)\n\n    # small margin so circles sit inside boundary but may touch it\n    margin = 0.018\n\n    # horizontal spacing from widest row\n    a = (1.0 - 2 * margin) / (max_cols - 1)\n    h = a * np.sqrt(3.0) / 2.0\n\n    # if vertical extent too large, reduce 'a' to fit\n    if (rows - 1) * h > (1.0 - 2 * margin):\n        h_allowed = (1.0 - 2 * margin) / (rows - 1)\n        h = h_allowed\n        a = h_allowed * 2.0 / np.sqrt(3.0)\n\n    centers = np.zeros((n, 2))\n    idx = 0\n    y0 = (1.0 - (rows - 1) * h) / 2.0\n    for r, cnt in enumerate(counts):\n        y = y0 + r * h\n        # offset rows with fewer columns to mimic hex packing\n        offset = (a / 2.0) if cnt < max_cols else 0.0\n        row_width = (cnt - 1) * a\n        x0 = (1.0 - row_width) / 2.0 + offset\n        for c in range(cnt):\n            if idx >= n:\n                break\n            x = x0 + c * a\n            centers[idx, 0] = np.clip(x, margin * 0.5, 1.0 - margin * 0.5)\n            centers[idx, 1] = np.clip(y, margin * 0.5, 1.0 - margin * 0.5)\n            idx += 1\n\n    # safety fill if necessary\n    while idx < n:\n        centers[idx] = [0.5, 0.5]\n        idx += 1\n\n    radii = compute_max_radii(centers)\n    return centers, radii, float(np.sum(radii))\n\n\ndef compute_max_radii(centers):\n    \"\"\"Conservative per-circle radii followed by maximal uniform scaling.\"\"\"\n    n = centers.shape[0]\n    # distance to walls\n    d_wall = np.minimum.reduce([centers[:, 0], centers[:, 1], 1.0 - centers[:, 0], 1.0 - centers[:, 1]])\n    # pairwise distances\n    dif = centers[:, None, :] - centers[None, :, :]\n    dists = np.sqrt(np.sum(dif ** 2, axis=2))\n    # avoid zero on diagonal\n    np.fill_diagonal(dists, np.inf)\n\n    # initial conservative radii: limited by wall and by half nearest neighbor distance\n    half_min = 0.5 * np.min(dists, axis=1)\n    r0 = np.minimum(d_wall, half_min)\n    r0 = np.maximum(r0, 1e-12)\n\n    # compute maximal uniform scaling factor s such that\n    # s * r0[i] <= d_wall[i]  and  s*(r0[i] + r0[j]) <= dists[i,j]\n    # For walls:\n    with np.errstate(divide='ignore', invalid='ignore'):\n        s_wall = d_wall / r0\n    s_wall[np.isnan(s_wall)] = np.inf\n    s_max = np.min(s_wall)\n\n    # For pairs:\n    ri_plus_rj = r0[:, None] + r0[None, :]\n    with np.errstate(divide='ignore', invalid='ignore'):\n        s_pairs = dists / ri_plus_rj\n    s_pairs[np.isnan(s_pairs)] = np.inf\n    # consider only upper triangle\n    iu = np.triu_indices(n, 1)\n    if iu[0].size > 0:\n        s_max = min(s_max, float(np.min(s_pairs[iu])))\n\n    if not np.isfinite(s_max) or s_max <= 0.0:\n        s = 1.0\n    else:\n        # safety shrink to avoid numerical boundary contact\n        s = min(1.0, s_max) * 0.997\n\n    return r0 * float(s)\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(6, 6))\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    for i, (c, r) in enumerate(zip(centers, radii)):\n        ax.add_patch(Circle(c, r, alpha=0.6, edgecolor=\"k\"))\n        ax.text(c[0], c[1], str(i), ha=\"center\", va=\"center\", fontsize=7)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n# EVOLVE-BLOCK-END\n```\nUnique approach: Modification: Full rewrite, Excellent validity (1.000), Excellent sum_radii (1.772)\n\n### Inspiration 3 (Score: 0.7017, Type: Alternative)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Improved constructor-based circle packing for n=26 circles.\nSelects the best hex-lattice spacing from a candidate list, picks the 26\nmost central lattice points, then computes near-optimal radii by alternating\nrobust shrink/grow passes that maximize radii subject to border and pairwise\nnon-overlap constraints. Deterministic and fully constructor-based.\n\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    n = 26\n\n    def hex_points(s):\n        dy = s * np.sqrt(3.0) / 2.0\n        # choose range large enough to cover unit square after centering\n        m = int(np.ceil(1.0 / (s * 0.5))) + 4\n        pts = []\n        for j in range(-m, m + 1):\n            y = j * dy\n            offset = (s / 2.0) if (j % 2 != 0) else 0.0\n            for i in range(-m, m + 1):\n                x = i * s + offset\n                pts.append((x, y))\n        pts = np.array(pts, dtype=float)\n        # center grid at (0.5,0.5)\n        minxy = pts.min(axis=0)\n        maxxy = pts.max(axis=0)\n        span = maxxy - minxy\n        pts = (pts - (minxy + span / 2.0)) + 0.5\n        return pts\n\n    def choose_centers_for_s(s):\n        pts = hex_points(s)\n        r = s / 2.0\n        mask = np.logical_and.reduce((pts[:, 0] >= r, pts[:, 0] <= 1 - r,\n                                      pts[:, 1] >= r, pts[:, 1] <= 1 - r))\n        inside = pts[mask]\n        if inside.shape[0] < n:\n            # if too few by strict inside test, relax to clipped-in-square\n            inside = np.clip(pts, r, 1 - r)\n        d2 = np.sum((inside - 0.5) ** 2, axis=1)\n        idx = np.argsort(d2)[:n]\n        centers = inside[idx].copy()\n        return centers\n\n    # Candidate spacings: scan a range and pick the spacing that yields the largest sum of radii\n    s_candidates = np.linspace(0.06, 0.20, 31)  # dense enough search\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    for s in s_candidates:\n        centers = choose_centers_for_s(s)\n        radii = compute_max_radii(centers)\n        ssum = float(np.sum(radii))\n        if ssum > best_sum:\n            best_sum = ssum\n            best_centers = centers\n            best_radii = radii\n\n    # As a final improvement, try a slightly denser local tweak around best spacing\n    if best_centers is not None:\n        # derive approximate s from nearest neighbor distances\n        pairs = best_centers[:, None, :] - best_centers[None, :, :]\n        dists = np.sqrt(np.sum(pairs ** 2, axis=2) + np.eye(n) * 1e9)\n        approx_s = np.min(dists, axis=1).mean()\n        for s in np.linspace(max(0.055, approx_s * 0.8), min(0.24, approx_s * 1.2), 9):\n            centers = choose_centers_for_s(s)\n            radii = compute_max_radii(centers)\n            ssum = float(np.sum(radii))\n            if ssum > best_sum:\n                best_sum = ssum\n                best_centers = centers\n                best_radii = radii\n\n    # Safety fallback\n    if best_centers is None:\n        best_centers = choose_centers_for_s(0.08)\n        best_radii = compute_max_radii(best_centers)\n\n    sum_radii = float(np.sum(best_radii))\n    return best_centers, best_radii, sum_radii\n\n\ndef compute_max_radii(centers, tol=1e-8, max_pass=500):\n    \"\"\"\n    Robust alternating shrink/grow passes to approximate the LP that\n    maximizes sum(r_i) subject to 0 <= r_i <= border_i and r_i + r_j <= d_ij.\n    \"\"\"\n    n = centers.shape[0]\n    # border distances\n    border = np.minimum.reduce([centers[:, 0], centers[:, 1], 1.0 - centers[:, 0], 1.0 - centers[:, 1]])\n    border = np.maximum(border, 0.0)\n\n    # pairwise distances\n    dif = centers[:, None, :] - centers[None, :, :]\n    dists = np.sqrt(np.clip((dif ** 2).sum(axis=2), 0.0, np.inf))\n    for i in range(n):\n        dists[i, i] = np.inf\n\n    # initialize radii to border (aggressive start)\n    r = border.copy()\n\n    def shrink_pass(radii):\n        changed = False\n        # enforce r_i + r_j <= d_ij by proportional scaling of conflicting pairs\n        for i in range(n):\n            ri = radii[i]\n            for j in range(i + 1, n):\n                rj = radii[j]\n                dij = dists[i, j]\n                if dij <= 0:\n                    # coincident centers: split evenly under border\n                    allowed = min(border[i], border[j], 0.0)\n                    if radii[i] != allowed or radii[j] != allowed:\n                        radii[i] = radii[j] = allowed\n                        changed = True\n                    continue\n                ssum = ri + rj\n                if ssum > dij + 1e-12:\n                    # scale both down proportionally but respect individual borders\n                    if ssum == 0:\n                        # nothing to do\n                        continue\n                    scale = dij / ssum\n                    new_ri = max(0.0, ri * scale)\n                    new_rj = max(0.0, rj * scale)\n                    if abs(new_ri - ri) > 1e-12 or abs(new_rj - rj) > 1e-12:\n                        radii[i] = new_ri\n                        radii[j] = new_rj\n                        ri = new_ri\n                        changed = True\n        # clip to borders (shouldn't increase)\n        np.minimum(radii, border, out=radii)\n        return changed\n\n    def grow_pass(radii):\n        changed = False\n        # Attempt to raise each radius to the maximum allowed by current neighbors and border\n        # Greedy: increase those with most slack first (sorted by available slack)\n        slack = border - radii\n        order = np.argsort(-slack)  # try to grow biggest slack first\n        for i in order:\n            # maximum allowed by neighbors: min_j (d_ij - r_j)\n            limits = dists[i, :] - radii\n            # exclude self via large diag\n            allowed = min(border[i], np.min(limits))\n            allowed = max(0.0, allowed)\n            if allowed > radii[i] + tol:\n                radii[i] = allowed\n                changed = True\n        return changed\n\n    # alternate passes until convergence\n    for _ in range(max_pass):\n        any_change = False\n        # multiple shrink passes help to propagate reductions\n        for _ in range(3):\n            if shrink_pass(r):\n                any_change = True\n            else:\n                break\n        # multiple grow passes to fill gaps\n        for _ in range(4):\n            if grow_pass(r):\n                any_change = True\n            else:\n                break\n        if not any_change:\n            break\n\n    # final strict enforcement\n    for _ in range(5):\n        if not shrink_pass(r):\n            break\n    r = np.maximum(np.minimum(r, border), 0.0)\n    return r\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\", linewidth=0.6)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nUnique approach: Modification: Full rewrite, Excellent validity (1.000), Excellent sum_radii (1.849)\n\n# Current Program\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Improved deterministic constructor for n=26 circle packing in the unit square.\n\nStrategy:\n- Try a few staggered/hex-like row patterns.\n- For each pattern, sweep horizontal spacing 'a' (within feasible bounds).\n- Place centers deterministically (centered rows with alternating offsets).\n- For each center set, compute radii by coordinate-wise maximization:\n    repeatedly set r_i = min(border_i, min_j (d_ij - r_j)) clipped >= 0.\n  This monotone Gauss-Seidel growth converges to a maximal feasible radii\n  vector that (practically) maximizes the sum of radii under linear pairwise\n  constraints r_i + r_j <= d_ij and r_i <= border_i.\n- Pick the configuration with largest sum of radii.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    # Candidate row patterns (sum to 26). Diversity helps edge-effect tradeoffs.\n    patterns = [\n        [6, 5, 6, 5, 4],\n        [6, 6, 5, 5, 4],\n        [5, 6, 5, 6, 4],\n    ]\n\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    sqrt3 = np.sqrt(3.0)\n    # Small safety margin to avoid exact boundary placement numerical issues\n    margin = 1e-6\n\n    # For each pattern, sweep horizontal spacing 'a' in a feasible interval.\n    for rows in patterns:\n        rows = list(rows)\n        max_cols = max(rows)\n        R = len(rows)\n\n        # max horizontal step a that fits width (allow touching boundaries)\n        a_max = (1.0 - 2.0 * margin) / (max_cols - 1) if max_cols > 1 else 1.0\n        a_min = min(0.03, a_max * 0.4)\n        if a_min <= 0:\n            a_min = a_max * 0.1\n\n        # sample spacings (denser near a_max helps pack more tightly)\n        for a in np.linspace(a_max, a_min, 40):\n            v = a * (sqrt3 / 2.0)\n            # vertical fit check\n            if (R - 1) * v > (1.0 - 2.0 * margin):\n                continue\n\n            # build centers\n            centers = np.zeros((n, 2), dtype=float)\n            idx = 0\n            y0 = (1.0 - (R - 1) * v) / 2.0\n            for r_idx, cnt in enumerate(rows):\n                y = y0 + r_idx * v\n                # offset odd/even rows for hex adjacency: shift rows with fewer columns\n                offset = (a / 2.0) if (cnt < max_cols) else 0.0\n                row_width = (cnt - 1) * a if cnt > 1 else 0.0\n                x0 = (1.0 - row_width) / 2.0 + offset\n                for c in range(cnt):\n                    if idx >= n:\n                        break\n                    x = x0 + c * a\n                    centers[idx, 0] = np.clip(x, margin, 1.0 - margin)\n                    centers[idx, 1] = np.clip(y, margin, 1.0 - margin)\n                    idx += 1\n            # safety fill (should not be needed)\n            while idx < n:\n                centers[idx] = [0.5, 0.5]\n                idx += 1\n\n            # compute radii by monotone coordinate-wise growth\n            radii = compute_max_radii_by_growth(centers)\n            s = float(np.sum(radii))\n            if s > best_sum:\n                best_sum = s\n                best_centers = centers.copy()\n                best_radii = radii.copy()\n\n    # As a final defensive fallback, if nothing found (extremely unlikely), use a simple layout\n    if best_centers is None:\n        # fallback pattern\n        rows = [6,5,6,5,4]\n        a = (1.0 - 2.0 * margin) / (max(rows) - 1)\n        v = a * (sqrt3 / 2.0)\n        centers = []\n        y0 = (1.0 - (len(rows) - 1) * v) / 2.0\n        for r_idx, cnt in enumerate(rows):\n            y = y0 + r_idx * v\n            offset = (a / 2.0) if (cnt < max(rows)) else 0.0\n            row_width = (cnt - 1) * a if cnt > 1 else 0.0\n            x0 = (1.0 - row_width) / 2.0 + offset\n            for c in range(cnt):\n                centers.append((np.clip(x0 + c * a, margin, 1.0 - margin), y))\n        best_centers = np.array(centers[:n])\n        best_radii = compute_max_radii_by_growth(best_centers)\n        best_sum = float(np.sum(best_radii))\n\n    return best_centers, best_radii, float(best_sum)\n\n\ndef compute_max_radii_by_growth(centers, tol=1e-9, max_iters=2000):\n    \"\"\"\n    Monotone coordinate-wise maximization:\n      iterate i = 0..n-1 repeatedly, set\n        r_i := min(border_i, min_{j!=i} (d_ij - r_j))\n      clipped to >= 0. Continue until max change < tol.\n    This produces the maximal feasible radii vector (w.r.t. pointwise ordering)\n    and in practice yields a very large sum of radii for these center layouts.\n    \"\"\"\n    n = centers.shape[0]\n    # border limits\n    border = np.minimum.reduce([centers[:, 0], centers[:, 1], 1.0 - centers[:, 0], 1.0 - centers[:, 1]])\n    border = np.maximum(border, 0.0)\n\n    # pairwise distances\n    dif = centers[:, None, :] - centers[None, :, :]\n    dists = np.sqrt(np.sum(dif * dif, axis=2))\n    np.fill_diagonal(dists, np.inf)  # ignore self in mins\n\n    # initialize radii to zero and grow\n    r = np.zeros(n, dtype=float)\n\n    for it in range(max_iters):\n        max_change = 0.0\n        # sweep in natural order (Gauss-Seidel style)\n        for i in range(n):\n            # allowed by neighbors: min_j (d_ij - r_j)\n            allowed_by_neighbors = np.min(dists[i] - r)\n            allowed = min(border[i], allowed_by_neighbors)\n            if allowed < 0.0:\n                allowed = 0.0\n            change = allowed - r[i]\n            if change > 0:\n                r[i] = allowed\n                if change > max_change:\n                    max_change = change\n            else:\n                # if negative change (rare) we shrink; keep monotone by allowing shrink as needed\n                if abs(change) > max_change:\n                    max_change = abs(change)\n                r[i] = allowed\n        if max_change < tol:\n            break\n\n    # final safety: enforce pairwise and border constraints strictly by tiny scaling\n    # compute any violations and scale down uniformly if needed\n    # pairwise check\n    viol = False\n    for i in range(n):\n        if r[i] > border[i] + 1e-12:\n            viol = True\n    for i in range(n):\n        for j in range(i + 1, n):\n            if r[i] + r[j] > dists[i, j] + 1e-12:\n                viol = True\n    if viol:\n        # compute maximal uniform factor s in (0,1] so that constraints hold\n        s = 1.0\n        # walls\n        with np.errstate(divide='ignore', invalid='ignore'):\n            s = min(s, float(np.min(np.where(r > 0, border / r, np.inf))))\n        # pairs\n        ri_rj = r[:, None] + r[None, :]\n        with np.errstate(divide='ignore', invalid='ignore'):\n            s_pair = np.where(ri_rj > 0, dists / ri_rj, np.inf)\n        iu = np.triu_indices(n, k=1)\n        if iu[0].size > 0:\n            s = min(s, float(np.min(s_pair[iu])))\n        if not np.isfinite(s) or s <= 0.0:\n            s = 0.0\n        r *= max(0.0, s * 0.999999)\n\n    return r\n\n# EVOLVE-BLOCK-END\n\n# Fixed runner and visualization interface (unchanged)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(7,7))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect(\"equal\")\n    ax.add_patch(plt.Rectangle((0,0),1,1,fill=False,edgecolor='k',linewidth=1))\n    for i,(c,r) in enumerate(zip(centers, radii)):\n        ax.add_patch(Circle(c, r, alpha=0.6, edgecolor='k'))\n        ax.text(c[0], c[1], str(i), ha='center', va='center', fontsize=7)\n    plt.title(f\"n={len(centers)}, sum radii={sum(radii):.6f}\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, total = run_packing()\n    print(f\"Sum of radii: {total:.6f}\")\n    # visualize(centers, radii)\n```\n\n# Task\nRewrite the program to improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: complexity, diversity\nDifferent solutions with similar fitness but different features are valuable.\nProvide the complete new program code.\n\nIMPORTANT: Make sure your rewritten program maintains the same inputs and outputs\nas the original program, but with improved internal implementation.\n\n```python\n# Your rewritten program here\n```", "responses": ["```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for n=26 circle packing in the unit square.\n\nStrategy (compact, robust):\n - Build a best equal-radius hex lattice by binary-searching r that yields >=26\n   lattice centers inside [r,1-r] for two horizontal-offset modes.\n - Pick the 26 most central centers (by distance to square center) from that lattice.\n - Run a Gauss-Seidel monotone growth to maximize each r_i := min(wall_i, min_j (d_ij - r_j)).\n - Final proportional repairs to remove tiny violations.\nThis yields varied radii and typically a large total radius sum.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    sqrt3 = np.sqrt(3.0)\n\n    def gen_hex(r, horiz_offset, vert_shift):\n        pts = []\n        y = r + vert_shift\n        row = 0\n        while y <= 1.0 - r + 1e-12:\n            offset = ((row % 2) * r) + horiz_offset\n            x = r + offset\n            while x <= 1.0 - r + 1e-12:\n                pts.append((x, y))\n                x += 2.0 * r\n            y += sqrt3 * r\n            row += 1\n        return np.array(pts, dtype=float) if pts else np.empty((0,2))\n\n    def best_lattice_for_mode(hflag):\n        lo, hi = 1e-8, 0.5\n        best_r = 0.0\n        best_pts = None\n        for _ in range(40):\n            mid = 0.5*(lo+hi)\n            ok = False\n            # try a few vertical shifts to avoid aligning all rows with edges\n            for frac in (0.0, 0.25, 0.5):\n                vs = frac * sqrt3 * mid\n                ho = mid if hflag is None else hflag\n                pts = gen_hex(mid, ho, vs)\n                if pts.shape[0] >= n:\n                    ok = True\n                    break\n            if ok:\n                best_r = mid\n                lo = mid\n            else:\n                hi = mid\n        if best_r > 0:\n            # pick a shift that yields the most symmetric/central selection\n            best_pts = None\n            for frac in np.linspace(0.0, 0.5, 9):\n                vs = frac * sqrt3 * best_r\n                ho = best_r if hflag is None else hflag\n                pts = gen_hex(best_r, ho, vs)\n                if pts.shape[0] >= n:\n                    # sort by distance to center to pick most central points\n                    d2 = np.sum((pts - 0.5)**2, axis=1)\n                    order = np.argsort(d2)\n                    pts_sorted = pts[order]\n                    if best_pts is None or pts_sorted.shape[0] > best_pts.shape[0]:\n                        best_pts = pts_sorted\n            if best_pts is None:\n                best_pts = gen_hex(best_r, (best_r if hflag is None else hflag), 0.0)\n            return best_r, best_pts\n        return 0.0, None\n\n    best_global = {\"r\":0.0, \"pts\":None}\n    # try horizontal offset modes: aligned (0.0) and staggered (None means offset=r)\n    for mode in (0.0, None):\n        r_val, pts = best_lattice_for_mode(mode)\n        if pts is not None and r_val > best_global[\"r\"]:\n            best_global[\"r\"] = r_val\n            best_global[\"pts\"] = pts.copy()\n\n    if best_global[\"pts\"] is None:\n        # fallback deterministic row layout [6,5,6,5,4]\n        rows = [6,5,6,5,4]\n        max_k = max(rows)\n        a = 1.0 / (2.0 * max_k)\n        r0 = min(a, 0.08)\n        v = r0 * sqrt3\n        centers = []\n        top = (1.0 - ((len(rows)-1)*v + 2*r0))/2.0 + r0\n        for i,cnt in enumerate(rows):\n            y = top + i * v\n            offset = r0 if cnt < max_k else 0.0\n            row_width = (cnt-1)*2*r0\n            left = (1.0 - row_width)/2.0 + offset\n            for j in range(cnt):\n                centers.append((left + j*2*r0, y))\n        centers = np.array(centers[:n], dtype=float)\n    else:\n        pts = best_global[\"pts\"]\n        # choose most central 26 points\n        d2 = np.sum((pts - 0.5)**2, axis=1)\n        idx = np.argsort(d2)[:n]\n        centers = pts[idx].copy()\n        centers = np.clip(centers, 1e-12, 1.0-1e-12)\n\n    radii = maximize_radii_gs(centers)\n    return centers, radii, float(np.sum(radii))\n\ndef maximize_radii_gs(centers, tol=1e-10, max_iters=5000):\n    n = centers.shape[0]\n    if n == 0:\n        return np.array([])\n    wall = np.minimum.reduce([centers[:,0], centers[:,1], 1.0-centers[:,0], 1.0-centers[:,1]])\n    wall = np.maximum(wall, 0.0)\n    diff = centers[:,None,:] - centers[None,:,:]\n    d = np.sqrt(np.sum(diff*diff, axis=2))\n    np.fill_diagonal(d, np.inf)\n    r = np.zeros(n, dtype=float)\n    for it in range(max_iters):\n        maxc = 0.0\n        for i in range(n):\n            limits = d[i] - r\n            allowed = float(np.min(limits))\n            newri = min(wall[i], allowed)\n            if newri < 0.0:\n                newri = 0.0\n            change = abs(newri - r[i])\n            if change > 0:\n                r[i] = newri\n                if change > maxc:\n                    maxc = change\n        if maxc < tol:\n            break\n    # proportional pair fixes for any tiny violations\n    for _ in range(6):\n        changed = False\n        for i in range(n):\n            for j in range(i+1, n):\n                dij = d[i,j]\n                if r[i] + r[j] > dij:\n                    tot = r[i] + r[j]\n                    if tot <= 0:\n                        if r[i] != 0.0 or r[j] != 0.0:\n                            r[i] = r[j] = 0.0\n                            changed = True\n                    else:\n                        scale = dij / tot\n                        newi = r[i]*scale\n                        newj = r[j]*scale\n                        if newi != r[i] or newj != r[j]:\n                            r[i], r[j] = newi, newj\n                            changed = True\n        if not changed:\n            break\n    r = np.minimum(r, wall)\n    r = np.maximum(r, 0.0)\n    return r\n\n# EVOLVE-BLOCK-END\n\n# Fixed runner and visualization interface (unchanged)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(7,7))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect(\"equal\")\n    ax.add_patch(plt.Rectangle((0,0),1,1,fill=False,edgecolor='k',linewidth=1))\n    for i,(c,r) in enumerate(zip(centers, radii)):\n        ax.add_patch(Circle(c, r, alpha=0.6, edgecolor='k'))\n        ax.text(c[0], c[1], str(i), ha='center', va='center', fontsize=7)\n    plt.title(f\"n={len(centers)}, sum radii={sum(radii):.6f}\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, total = run_packing()\n    print(f\"Sum of radii: {total:.6f}\")\n    # visualize(centers, radii)\n```"]}}, "artifacts_json": null, "artifact_dir": null}