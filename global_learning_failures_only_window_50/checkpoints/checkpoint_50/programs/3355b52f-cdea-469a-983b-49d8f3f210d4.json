{"id": "3355b52f-cdea-469a-983b-49d8f3f210d4", "code": "# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for n=26 circle packing in the unit square.\n\nStrategy (compact):\n - Build hexagonal grids for two horizontal alignment choices (offset=0 and offset=r).\n - For each choice binary-search the largest equal radius r that yields >=26 grid centers\n   inside [r,1-r] for some small vertical shifts.\n - From best grid take 26 centers chosen center-first, then run a Gauss-Seidel\n   monotone growth to maximize each radius under wall and pairwise constraints.\nThis yields varied radii and typically a large total radius sum.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n_needed = 26\n    sqrt3 = np.sqrt(3.0)\n\n    def gen_grid(r, horiz_offset, vert_shift):\n        pts = []\n        y = r + vert_shift\n        row = 0\n        while y <= 1.0 - r + 1e-12:\n            offset = ((row % 2) * r) + horiz_offset\n            x = r + offset\n            while x <= 1.0 - r + 1e-12:\n                pts.append((x, y))\n                x += 2.0 * r\n            y += sqrt3 * r\n            row += 1\n        return np.array(pts) if pts else np.empty((0,2))\n\n    def search_alignment(hflag):\n        lo, hi = 1e-8, 0.5\n        best_r = 0.0\n        for _ in range(40):\n            mid = 0.5*(lo+hi)\n            found = False\n            for frac in np.linspace(0.0, 0.5, 5):\n                vert_shift = frac * sqrt3 * mid\n                ho = (mid if hflag is None else hflag)\n                pts = gen_grid(mid, ho, vert_shift)\n                if pts.shape[0] >= n_needed:\n                    found = True\n                    break\n            if found:\n                best_r = mid\n                lo = mid\n            else:\n                hi = mid\n        best_pts = None\n        if best_r > 0:\n            for frac in np.linspace(0.0, 0.5, 9):\n                vert_shift = frac * sqrt3 * best_r\n                ho = (best_r if hflag is None else hflag)\n                pts = gen_grid(best_r, ho, vert_shift)\n                if pts.shape[0] >= n_needed:\n                    # prefer centers nearer the square center\n                    center = np.array([0.5,0.5])\n                    d = np.sum((pts - center)**2, axis=1)\n                    order = np.argsort(d)  # smallest distance first\n                    pts_sorted = pts[order]\n                    if best_pts is None or pts_sorted.shape[0] > best_pts.shape[0]:\n                        best_pts = pts_sorted\n            if best_pts is None:\n                best_pts = gen_grid(best_r, (best_r if hflag is None else hflag), 0.0)\n        return best_r, best_pts\n\n    best_global = {\"r\":0.0, \"pts\":None}\n    for h in (0.0, None):  # None means horiz_offset = r (staggered)\n        r_val, pts = search_alignment(h)\n        if pts is not None and r_val > best_global[\"r\"]:\n            best_global[\"r\"], best_global[\"pts\"] = r_val, pts.copy()\n\n    if best_global[\"pts\"] is None:\n        # fallback deterministic centered row layout [6,5,6,5,4]\n        rows = [6,5,6,5,4]\n        kmax = max(rows)\n        r_h = 1.0 / (2.0 * kmax)\n        r_v = 1.0 / (2.0 + (len(rows)-1)*sqrt3)\n        base_r = min(r_h, r_v)*0.999\n        vert_span = 2.0*base_r + (len(rows)-1)*base_r*sqrt3\n        top = (1.0 - vert_span)/2.0 + base_r\n        dy = base_r * sqrt3\n        centers = []\n        for ri, cnt in enumerate(rows):\n            y = top + ri*dy\n            row_span = 2.0*base_r*cnt\n            left = (1.0 - row_span)/2.0 + base_r\n            xs = left + np.arange(cnt)*(2.0*base_r)\n            for x in xs:\n                centers.append((x,y))\n        centers = np.array(centers[:n_needed], dtype=float)\n    else:\n        pts = best_global[\"pts\"]\n        centers = pts[:n_needed].copy()\n        centers = np.clip(centers, 1e-12, 1.0-1e-12)\n\n    radii = maximize_radii_gs(centers)\n    return centers, radii, float(np.sum(radii))\n\n\ndef maximize_radii_gs(centers, tol=1e-10, max_iters=5000):\n    n = centers.shape[0]\n    if n == 0:\n        return np.array([])\n    wall = np.minimum.reduce([centers[:,0], centers[:,1], 1.0-centers[:,0], 1.0-centers[:,1]])\n    wall = np.maximum(wall, 0.0)\n    diff = centers[:,None,:] - centers[None,:,:]\n    dists = np.sqrt(np.sum(diff*diff, axis=2))\n    np.fill_diagonal(dists, np.inf)\n    r = np.zeros(n, dtype=float)\n    for it in range(max_iters):\n        max_change = 0.0\n        for i in range(n):\n            limits = dists[i] - r\n            allowed = float(np.min(limits))\n            newri = min(allowed, wall[i])\n            if newri < 0.0:\n                newri = 0.0\n            change = abs(newri - r[i])\n            if change:\n                r[i] = newri\n                if change > max_change:\n                    max_change = change\n        if max_change < tol:\n            break\n    # proportional repairs\n    for _ in range(4):\n        changed = False\n        for i in range(n):\n            for j in range(i+1,n):\n                dij = dists[i,j]\n                if r[i] + r[j] > dij:\n                    tot = r[i] + r[j]\n                    if tot <= 0:\n                        if r[i]!=0 or r[j]!=0:\n                            r[i]=r[j]=0.0; changed=True\n                    else:\n                        scale = dij / tot\n                        ni, nj = r[i]*scale, r[j]*scale\n                        if ni!=r[i] or nj!=r[j]:\n                            r[i], r[j] = ni, nj; changed=True\n        if not changed:\n            break\n    r = np.minimum(r, wall)\n    r = np.maximum(r, 0.0)\n    return r\n\n# Fixed API\ndef run_packing():\n    centers, radii, s = construct_packing()\n    return centers, radii, s\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(6,6))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect(\"equal\")\n    ax.add_patch(plt.Rectangle((0,0),1,1,fill=False,linewidth=1))\n    for c,r in zip(centers, radii):\n        ax.add_patch(Circle(c, r, alpha=0.6, edgecolor='k'))\n    plt.title(f\"n={len(centers)}, sum={sum(radii):.6f}\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, total = run_packing()\n    print(f\"Sum of radii: {total:.6f}\")\n# EVOLVE-BLOCK-END", "language": "python", "parent_id": "a0b6bec8-ed81-43b1-932b-17d35c935e4b", "generation": 4, "timestamp": 1759950628.645323, "iteration_found": 42, "metrics": {"validity": 1.0, "sum_radii": 1.708720096586099, "target_ratio": 0.6484706248903602, "combined_score": 0.6484706248903602, "eval_time": 0.05622720718383789}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Full rewrite", "parent_metrics": {"validity": 1.0, "sum_radii": 2.1689226590315673, "target_ratio": 0.8231205537121699, "combined_score": 0.8231205537121699, "eval_time": 0.3516829013824463}, "island": 0}, "prompts": {"full_rewrite_user": {"system": "You are an expert mathematician specializing in circle packing problems and computational geometry. Your task is to improve a constructor function that directly produces a specific arrangement of 26 circles in a unit square, maximizing the sum of their radii. The AlphaEvolve paper achieved a sum of 2.635 for n=26.\n\nKey geometric insights:\n- Circle packings often follow hexagonal patterns in the densest regions\n- Maximum density for infinite circle packing is pi/(2*sqrt(3)) \u2248 0.9069\n- Edge effects make square container packing harder than infinite packing\n- Circles can be placed in layers or shells when confined to a square\n- Similar radius circles often form regular patterns, while varied radii allow better space utilization\n- Perfect symmetry may not yield the optimal packing due to edge effects\n\nFocus on designing an explicit constructor that places each circle in a specific position, rather than an iterative search algorithm.\n", "user": "# Current Program Information\n- Fitness: 0.8231\n- Feature coordinates: No feature coordinates\n- Focus areas: - Fitness unchanged at 0.8231\n- Consider simplifying - code length exceeds 500 characters\n\n\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 3\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.1689, target_ratio: 0.8231, combined_score: 0.8231, eval_time: 0.3517\n- Outcome: Mixed results\n\n### Attempt 2\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.1737, target_ratio: 0.8249, combined_score: 0.8249, eval_time: 0.4660\n- Outcome: Mixed results\n\n### Attempt 1\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.4390, target_ratio: 0.9256, combined_score: 0.9256, eval_time: 0.0564\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.9256)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for n=26 circle packing in the unit square.\n\nStrategy:\n - Try to build a hexagonal grid of equal circles by binary-searching the largest\n   equal radius r that yields >=26 grid centers inside [r,1-r], for two\n   horizontal offset choices (0 or r) and several vertical shifts.\n - From the best grid (largest r), select 26 centers (center-first ordering),\n   then run a Gauss-Seidel-style monotone growth routine that maximizes each\n   r_i := min(dist_to_wall, min_j (d_ij - r_j)). This yields varied radii\n   and typically a large total radius sum.\n - Fallback to a deterministic row layout if no grid is found.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n_needed = 26\n    sqrt3 = np.sqrt(3.0)\n\n    def gen_grid(r, horiz_offset, vert_shift):\n        pts = []\n        # start at y = r + vert_shift, step sqrt3 * r\n        y = r + vert_shift\n        row = 0\n        while y <= 1.0 - r + 1e-12:\n            offset = (row % 2) * r + horiz_offset\n            x = r + offset\n            while x <= 1.0 - r + 1e-12:\n                pts.append((x, y))\n                x += 2.0 * r\n            y += sqrt3 * r\n            row += 1\n        if not pts:\n            return np.empty((0,2))\n        pts = np.array(pts)\n        return pts\n\n    def find_best_for_offset(h_offset_flag):\n        # h_offset_flag: None means horiz_offset = r, otherwise numeric offset\n        lo, hi = 1e-8, 0.5\n        best_r = 0.0\n        best_pts = None\n        for _ in range(40):\n            mid = 0.5 * (lo + hi)\n            found = False\n            for frac in np.linspace(0.0, 0.5, 5):\n                vert_shift = frac * sqrt3 * mid\n                ho = (mid if h_offset_flag is None else h_offset_flag)\n                pts = gen_grid(mid, ho, vert_shift)\n                if pts.shape[0] >= n_needed:\n                    found = True\n                    break\n            if found:\n                best_r = mid\n                lo = mid\n            else:\n                hi = mid\n        if best_r > 0:\n            # pick a vertical shift that gives a stable centered set\n            best_pts = None\n            for frac in np.linspace(0.0, 0.5, 9):\n                vert_shift = frac * sqrt3 * best_r\n                ho = (best_r if h_offset_flag is None else h_offset_flag)\n                pts = gen_grid(best_r, ho, vert_shift)\n                if pts.shape[0] >= n_needed:\n                    # sort by y descending, then x ascending to pick center-first\n                    pts_sorted = pts[np.lexsort((pts[:,0], -pts[:,1]))]\n                    if best_pts is None or pts_sorted.shape[0] > best_pts.shape[0]:\n                        best_pts = pts_sorted\n            if best_pts is None:\n                best_pts = gen_grid(best_r, (best_r if h_offset_flag is None else h_offset_flag), 0.0)\n        return best_r, best_pts\n\n    best_global = {\"r\": 0.0, \"pts\": None}\n    # try two horizontal choices: offset 0 or offset=r (encoded as None)\n    for h_flag in (0.0, None):\n        r_val, pts = find_best_for_offset(h_flag)\n        if pts is not None and r_val > best_global[\"r\"]:\n            best_global[\"r\"] = r_val\n            best_global[\"pts\"] = pts.copy()\n\n    if best_global[\"pts\"] is None:\n        # fallback deterministic row layout (guarantees feasibility)\n        rows = [6,5,6,5,4]\n        max_k = max(rows)\n        r_h = 1.0 / (2.0 * max_k)\n        r_v = 1.0 / (2.0 + (len(rows) - 1) * sqrt3)\n        base_r = min(r_h, r_v) * 0.999\n        vert_span = 2.0 * base_r + (len(rows) - 1) * base_r * sqrt3\n        top = (1.0 - vert_span) / 2.0 + base_r\n        dy = base_r * sqrt3\n        centers = []\n        for row_idx, k in enumerate(rows):\n            row_span = 2.0 * base_r * k\n            left = (1.0 - row_span) / 2.0 + base_r\n            xs = left + np.arange(k) * (2.0 * base_r)\n            y = top + row_idx * dy\n            for x in xs:\n                centers.append((x, y))\n        centers = np.array(centers[:n_needed], dtype=float)\n    else:\n        pts = best_global[\"pts\"]\n        # select first n_needed in stable center-first order\n        centers = pts[:n_needed].copy()\n        centers = np.clip(centers, 1e-12, 1.0 - 1e-12)\n\n    radii = maximize_radii_gs(centers)\n    return centers, radii, float(np.sum(radii))\n\n\ndef maximize_radii_gs(centers, tol=1e-10, max_iters=5000):\n    \"\"\"\n    Gauss-Seidel-style monotone growth:\n      r_i <- min( wall_i, min_j (d_ij - r_j) )\n    This finds the maximal feasible radii vector (pointwise) for fixed centers\n    under r_i >= 0, r_i <= wall_i, r_i + r_j <= d_ij.\n    \"\"\"\n    n = centers.shape[0]\n    wall = np.minimum.reduce([centers[:,0], centers[:,1], 1.0-centers[:,0], 1.0-centers[:,1]])\n    wall = np.maximum(wall, 0.0)\n    diff = centers[:, None, :] - centers[None, :, :]\n    dists = np.sqrt(np.sum(diff*diff, axis=2))\n    np.fill_diagonal(dists, np.inf)\n\n    r = np.zeros(n, dtype=float)\n    for it in range(max_iters):\n        max_change = 0.0\n        for i in range(n):\n            limits = dists[i] - r  # d_ij - r_j\n            allowed_by_neighbors = float(np.min(limits))\n            new_ri = allowed_by_neighbors\n            if new_ri > wall[i]:\n                new_ri = wall[i]\n            if new_ri < 0.0:\n                new_ri = 0.0\n            change = abs(new_ri - r[i])\n            if change > 0:\n                r[i] = new_ri\n                if change > max_change:\n                    max_change = change\n        if max_change < tol:\n            break\n\n    # final safety: remove any tiny violations by proportional scaling on violating pairs\n    for _ in range(4):\n        changed = False\n        for i in range(n):\n            for j in range(i+1, n):\n                dij = dists[i,j]\n                if r[i] + r[j] > dij:\n                    tot = r[i] + r[j]\n                    if tot <= 0:\n                        r[i] = r[j] = 0.0\n                    else:\n                        scale = dij / tot\n                        new_i = r[i] * scale\n                        new_j = r[j] * scale\n                        if new_i != r[i] or new_j != r[j]:\n                            r[i], r[j] = new_i, new_j\n                            changed = True\n        if not changed:\n            break\n    r = np.minimum(r, wall)\n    r = np.maximum(r, 0.0)\n    return r\n\n# EVOLVE-BLOCK-END\n\n# Fixed runner and visualization (unchanged interface)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(8,8))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect(\"equal\")\n    ax.add_patch(plt.Rectangle((0,0),1,1,fill=False,edgecolor='k',linewidth=1))\n    for i,(c,r) in enumerate(zip(centers, radii)):\n        ax.add_patch(Circle(c, r, alpha=0.6, edgecolor='k'))\n        ax.text(c[0], c[1], str(i), ha='center', va='center', fontsize=8)\n    plt.title(f\"n={len(centers)}, sum radii={sum(radii):.6f}\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, total = run_packing()\n    print(f\"Sum of radii: {total:.6f}\")\n    # visualize(centers, radii)\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.4390), Performs well on target_ratio (0.9256), Performs well on combined_score (0.9256), Performs well on eval_time (0.0564)\n\n### Program 2 (Score: 0.8249)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for 26 circles in the unit square.\n\nStrategy:\n- Use the empirically good row pattern [6,5,6,5,4].\n- Sample a small family of hex-like layouts (horizontal spacing, slight vertical scale,\n  two offset modes and small global x-shifts) and pick the layout that yields the\n  largest sum of feasible radii.\n- Radii solver alternates deterministic reduction passes (repair violating pairs by\n  shrinking the larger) and growth passes (increase each radius to the largest\n  allowed by current neighbors and the wall). Finish with proportional fixes.\nThis is a compact, deterministic constructor designed to boost total radius sum.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    rows = [6, 5, 6, 5, 4]\n    m = len(rows)\n    kmax = max(rows)\n\n    # horizontal spacing feasible range (widest row must fit)\n    a_max = 1.0 / (kmax - 1)        # (kmax-1)*a <= 1 => a <= 1/(kmax-1)\n    a_min = 0.035\n    if a_min >= a_max:\n        a_min = a_max * 0.6\n\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    sqrt3 = np.sqrt(3.0)\n\n    # Sample a near the upper bound more densely\n    for a in np.linspace(a_max * 0.99, a_min, 28):\n        # nominal vertical step for hex packing, allow slight scaling\n        v0 = a * sqrt3 / 2.0\n        for v_scale in (0.96, 0.98, 1.0, 1.02):\n            v = v0 * v_scale\n            total_h = (m - 1) * v\n            if total_h > 1.0:\n                continue\n            # two offset modes for shorter rows: centered or half-step\n            for short_half in (False, True):\n                # small global horizontal shifts\n                for dx in np.linspace(-0.02, 0.02, 5):\n                    centers = np.zeros((n, 2), dtype=float)\n                    idx = 0\n                    y0 = (1.0 - total_h) / 2.0\n                    valid = True\n                    for r_idx, cnt in enumerate(rows):\n                        y = y0 + r_idx * v\n                        if cnt < kmax and short_half:\n                            offset = a / 2.0\n                        else:\n                            offset = 0.0\n                        row_width = (cnt - 1) * a\n                        x0 = (1.0 - row_width) / 2.0 + offset + dx\n                        for c in range(cnt):\n                            if idx >= n:\n                                break\n                            x = x0 + c * a\n                            # small clamp if numerical drift pushes out\n                            if x <= 0.0 + 1e-12 or x >= 1.0 - 1e-12 or y <= 0.0 + 1e-12 or y >= 1.0 - 1e-12:\n                                x = np.clip(x, 1e-12, 1.0 - 1e-12)\n                                y = np.clip(y, 1e-12, 1.0 - 1e-12)\n                            centers[idx, 0] = x\n                            centers[idx, 1] = y\n                            idx += 1\n                    if idx < n:\n                        valid = False\n                    if not valid:\n                        continue\n                    radii = maximize_radii_alt(centers)\n                    s = float(np.sum(radii))\n                    if s > best_sum:\n                        best_sum = s\n                        best_centers = centers.copy()\n                        best_radii = radii.copy()\n\n    # Fallback simple centered hex-like layout\n    if best_centers is None:\n        a = a_max * 0.98\n        v = a * sqrt3 / 2.0\n        centers_list = []\n        y0 = (1.0 - (m - 1) * v) / 2.0\n        for r_idx, cnt in enumerate(rows):\n            y = y0 + r_idx * v\n            offset = (a / 2.0) if (cnt < kmax) else 0.0\n            row_width = (cnt - 1) * a\n            x0 = (1.0 - row_width) / 2.0 + offset\n            for c in range(cnt):\n                x = np.clip(x0 + c * a, 1e-12, 1.0 - 1e-12)\n                centers_list.append((x, y))\n        best_centers = np.array(centers_list[:n], dtype=float)\n        best_radii = maximize_radii_alt(best_centers)\n        best_sum = float(np.sum(best_radii))\n\n    return best_centers, best_radii, float(best_sum)\n\n\ndef maximize_radii_alt(centers, tol=1e-9, max_cycles=1000):\n    \"\"\"Alternate reduction/growth solver for maximizing sum of radii under pair and wall constraints.\"\"\"\n    n = centers.shape[0]\n    if n == 0:\n        return np.array([])\n\n    wall = np.minimum.reduce([centers[:, 0], centers[:, 1],\n                              1.0 - centers[:, 0], 1.0 - centers[:, 1]])\n    wall = np.maximum(wall, 0.0)\n\n    diff = centers[:, None, :] - centers[None, :, :]\n    dists = np.sqrt(np.sum(diff * diff, axis=2))\n    np.fill_diagonal(dists, np.inf)\n\n    # start from wall-limited radii (greedy)\n    r = wall.copy()\n\n    for cycle in range(max_cycles):\n        max_change = 0.0\n        # Reduction sweep: repair violating pairs by shrinking the larger circle\n        for i in range(n):\n            ri = r[i]\n            # compute violation indices where ri + rj > d_ij\n            sum_with_i = ri + r\n            viol = np.where(sum_with_i > dists[i] + 1e-14)[0]\n            for j in viol:\n                if j == i:\n                    continue\n                dij = dists[i, j]\n                # shrink the larger of r[i], r[j] to satisfy r_i + r_j <= dij\n                if r[i] >= r[j]:\n                    new_ri = max(0.0, dij - r[j])\n                    ch = ri - new_ri\n                    if ch > 0:\n                        r[i] = new_ri\n                        ri = new_ri\n                        if ch > max_change:\n                            max_change = ch\n                else:\n                    new_rj = max(0.0, dij - r[i])\n                    ch = r[j] - new_rj\n                    if ch > 0:\n                        r[j] = new_rj\n                        if ch > max_change:\n                            max_change = ch\n\n        # enforce wall limits (prevent overshoot)\n        over = r > wall\n        if np.any(over):\n            diffv = np.max(r[over] - wall[over])\n            r[over] = wall[over]\n            if diffv > max_change:\n                max_change = diffv\n\n        # Growth sweep: try to grow each circle to allowed max given neighbors\n        for i in range(n):\n            allowed = wall[i]\n            limits = dists[i, :] - r\n            # min over neighbors gives the tightest bound (self diag is inf)\n            minlim = np.min(limits)\n            target = min(allowed, minlim)\n            if target < 0.0:\n                target = 0.0\n            ch = target - r[i]\n            if ch > 0:\n                r[i] = target\n                if ch > max_change:\n                    max_change = ch\n\n        if max_change < tol:\n            break\n\n    # Final proportional fixes for any remaining tiny violations\n    for _ in range(4):\n        changed = False\n        for i in range(n):\n            for j in range(i + 1, n):\n                dij = dists[i, j]\n                if r[i] + r[j] > dij:\n                    tot = r[i] + r[j]\n                    if tot <= 0:\n                        if r[i] != 0.0 or r[j] != 0.0:\n                            r[i] = r[j] = 0.0\n                            changed = True\n                    else:\n                        scale = dij / tot\n                        new_i = r[i] * scale\n                        new_j = r[j] * scale\n                        if new_i != r[i] or new_j != r[j]:\n                            r[i], r[j] = new_i, new_j\n                            changed = True\n        if not changed:\n            break\n\n    r = np.clip(r, 0.0, wall)\n    return r\n\n\n# Fixed API\ndef run_packing():\n    centers, radii, s = construct_packing()\n    return centers, radii, s\n\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(7, 7))\n    ax.set_xlim(0, 1); ax.set_ylim(0, 1); ax.set_aspect(\"equal\")\n    ax.add_patch(plt.Rectangle((0, 0), 1, 1, fill=False, linewidth=1))\n    for c, r in zip(centers, radii):\n        ax.add_patch(Circle(c, r, alpha=0.6, edgecolor='k'))\n    plt.title(f\"n={len(centers)}, sum={sum(radii):.6f}\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, total = run_packing()\n    print(f\"Sum of radii: {total:.6f}\")\n    # visualize(centers, radii)\n# EVOLVE-BLOCK-END\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.1737), Performs well on target_ratio (0.8249), Performs well on combined_score (0.8249), Performs well on eval_time (0.4660)\n\n### Program 3 (Score: 0.8231)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Improved deterministic constructor for 26 circles in the unit square.\n\nKey ideas:\n- Use the hex-like row pattern [6,5,6,5,4].\n- Sample a small grid of horizontal spacings, small global x-shifts and two offset modes\n  (shorter rows either centered or half-step shifted) and slight vertical compression/stretch.\n- For each candidate placement compute radii with an enhanced constraint solver:\n    * start from border limits\n    * alternate reduction sweeps (repair violating pairs by shrinking the larger)\n      and growth sweeps (increase each radius to the largest allowed by current neighbors)\n    * finalize with proportional pairwise fixes\n  This alternation lets radii both relax down when needed and regrow to better fill free space,\n  producing larger total radii than greedy-only reductions.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    rows = [6, 5, 6, 5, 4]\n    m = len(rows)\n    kmax = max(rows)\n    # allowable horizontal spacing: ensure widest row fits\n    a_max = 1.0 / (kmax - 1)  # (kmax-1)*a <= 1\n    a_min = 0.035\n    if a_min >= a_max:\n        a_min = a_max * 0.6\n\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    sqrt3 = np.sqrt(3.0)\n\n    # sample horizontal spacings more densely near a_max\n    for a in np.linspace(a_max * 0.98, a_min, 20):\n        # nominal hex vertical step (can scale slightly)\n        v0 = a * sqrt3 / 2.0\n        for v_scale in (0.96, 0.98, 1.0, 1.02):\n            v = v0 * v_scale\n            total_h = (m - 1) * v\n            if total_h > 1.0:\n                continue\n            # two offset modes: 0 or half-step for shorter rows\n            for short_offset_half in (False, True):\n                # small global horizontal shifts to allow better edge packing\n                for dx in np.linspace(-0.02, 0.02, 5):\n                    centers = np.zeros((n, 2))\n                    idx = 0\n                    y0 = (1.0 - total_h) / 2.0\n                    ok = True\n                    for r_idx, cnt in enumerate(rows):\n                        y = y0 + r_idx * v\n                        # compute offset for this row\n                        if cnt < kmax and short_offset_half:\n                            offset = a / 2.0\n                        else:\n                            offset = 0.0\n                        row_width = (cnt - 1) * a\n                        x0 = (1.0 - row_width) / 2.0 + offset + dx\n                        # place points and ensure inside [0,1] (clamp edges)\n                        for c in range(cnt):\n                            if idx >= n:\n                                break\n                            x = x0 + c * a\n                            if x <= 0.0 + 1e-9 or x >= 1.0 - 1e-9 or y <= 0.0 + 1e-9 or y >= 1.0 - 1e-9:\n                                # allow small clamping but if center would be too far out, mark not ok\n                                x = np.clip(x, 1e-9, 1.0 - 1e-9)\n                                y = np.clip(y, 1e-9, 1.0 - 1e-9)\n                            centers[idx, 0] = x\n                            centers[idx, 1] = y\n                            idx += 1\n                    if idx < n:\n                        ok = False\n                    if not ok:\n                        continue\n                    radii = maximize_radii_refined(centers)\n                    s = float(np.sum(radii))\n                    if s > best_sum:\n                        best_sum = s\n                        best_centers = centers.copy()\n                        best_radii = radii.copy()\n\n    # Fallback deterministic center if sampling failed\n    if best_centers is None:\n        # simple centered hex-like placement\n        a = a_max * 0.98\n        v = a * sqrt3 / 2.0\n        centers = []\n        y0 = (1.0 - (m - 1) * v) / 2.0\n        for r_idx, cnt in enumerate(rows):\n            y = y0 + r_idx * v\n            offset = (a / 2.0) if (cnt < kmax) else 0.0\n            row_width = (cnt - 1) * a\n            x0 = (1.0 - row_width) / 2.0 + offset\n            for c in range(cnt):\n                centers.append((np.clip(x0 + c * a, 1e-9, 1.0 - 1e-9), y))\n        best_centers = np.array(centers[:n], dtype=float)\n        best_radii = maximize_radii_refined(best_centers)\n        best_sum = float(np.sum(best_radii))\n\n    return best_centers, best_radii, float(best_sum)\n\n\ndef maximize_radii_refined(centers, tol=1e-9, max_cycles=1000):\n    \"\"\"\n    Alternating repair/increase solver for the LP:\n      maximize sum r_i\n      s.t. 0 <= r_i <= wall_i, r_i + r_j <= d_ij.\n\n    Algorithm:\n      - initialize r = wall (border-limited)\n      - repeat cycles:\n          1) reduction sweep: for every violating pair shrink the larger to d_ij - other\n          2) growth sweep: for each i increase r_i to min(wall_i, min_j(d_ij - r_j))\n      - finish with proportional pairwise fixes\n    This tends to reach a large feasible sum and is deterministic.\n    \"\"\"\n    n = centers.shape[0]\n    if n == 0:\n        return np.array([])\n\n    wall = np.minimum.reduce([centers[:, 0], centers[:, 1],\n                              1.0 - centers[:, 0], 1.0 - centers[:, 1]])\n    wall = np.maximum(wall, 0.0)\n\n    diff = centers[:, None, :] - centers[None, :, :]\n    dists = np.sqrt(np.sum(diff * diff, axis=2))\n    np.fill_diagonal(dists, np.inf)\n\n    # start from border-limited radii\n    r = wall.copy()\n\n    for cycle in range(max_cycles):\n        max_change = 0.0\n        # 1) Reduction sweep (repair violations by shrinking larger)\n        for i in range(n):\n            ri = r[i]\n            # vectorized candidate check\n            s = ri + r\n            viol = np.where(s > dists[i] + 1e-12)[0]\n            for j in viol:\n                if j == i:\n                    continue\n                dij = dists[i, j]\n                # shrink the larger radius\n                if r[i] >= r[j]:\n                    new_ri = max(0.0, dij - r[j])\n                    ch = ri - new_ri\n                    if ch > 0:\n                        r[i] = new_ri\n                        ri = new_ri\n                        if ch > max_change:\n                            max_change = ch\n                else:\n                    new_rj = max(0.0, dij - r[i])\n                    ch = r[j] - new_rj\n                    if ch > 0:\n                        r[j] = new_rj\n                        if ch > max_change:\n                            max_change = ch\n\n        # enforce wall limits (no increases here)\n        over = r > wall\n        if np.any(over):\n            diffv = np.max(r[over] - wall[over])\n            r[over] = wall[over]\n            if diffv > max_change:\n                max_change = diffv\n\n        # 2) Growth sweep: try to grow each circle to the largest allowed by current neighbors\n        for i in range(n):\n            allowed = wall[i]\n            # compute min_j (d_ij - r_j)\n            lims = dists[i, :] - r\n            # ignore self (inf)\n            minlim = np.min(lims)\n            target = min(allowed, minlim)\n            if target < 0.0:\n                target = 0.0\n            ch = target - r[i]\n            if ch > 0:\n                r[i] = target\n                if ch > max_change:\n                    max_change = ch\n\n        if max_change < tol:\n            break\n\n    # Final proportional fixes for any remaining tiny violations\n    for _ in range(4):\n        changed = False\n        for i in range(n):\n            for j in range(i + 1, n):\n                dij = dists[i, j]\n                if r[i] + r[j] > dij:\n                    tot = r[i] + r[j]\n                    if tot <= 0:\n                        if r[i] != 0.0 or r[j] != 0.0:\n                            r[i] = r[j] = 0.0\n                            changed = True\n                    else:\n                        scale = dij / tot\n                        new_i = r[i] * scale\n                        new_j = r[j] * scale\n                        if new_i != r[i] or new_j != r[j]:\n                            r[i], r[j] = new_i, new_j\n                            changed = True\n        if not changed:\n            break\n\n    r = np.clip(r, 0.0, wall)\n    return r\n\n\n# Fixed API\ndef run_packing():\n    centers, radii, s = construct_packing()\n    return centers, radii, s\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(6,6))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect(\"equal\")\n    ax.add_patch(plt.Rectangle((0,0),1,1,fill=False,linewidth=1))\n    for c,r in zip(centers, radii):\n        ax.add_patch(Circle(c, r, alpha=0.6, edgecolor='k'))\n    plt.title(f\"n={len(centers)}, sum={sum(radii):.6f}\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, total = run_packing()\n    print(f\"Sum of radii: {total:.6f}\")\n    # visualize(centers, radii)\n# EVOLVE-BLOCK-END\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.1689), Performs well on target_ratio (0.8231), Performs well on combined_score (0.8231), Performs well on eval_time (0.3517)\n\n\n\n## Diverse Programs\n\n### Program D1 (Score: 0.8223)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (hex-like rows)\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    \"\"\"\n    Construct a deterministic arrangement of 26 circles in the unit square\n    using a hexagonal-row layout: rows = [6,5,6,5,4]. Then compute maximal\n    radii by enforcing boundary and pairwise non-overlap constraints\n    via iterative pairwise adjustments.\n    Returns (centers, radii, sum_of_radii).\n    \"\"\"\n    # Layout: five rows summing to 26 circles\n    rows = [6, 5, 6, 5, 4]\n    m = len(rows)\n    max_k = max(rows)\n\n    # geometric spacings for ideal hex packing with equal radii r\n    sqrt3 = np.sqrt(3.0)\n    # limits from horizontal and vertical spans:\n    r_h = 1.0 / (2.0 * max_k)                        # 2r*k <= 1 -> r <= 1/(2k)\n    r_v = 1.0 / (2.0 + (m - 1) * sqrt3)              # 2r + (m-1)*r*sqrt3 <= 1\n    base_r = min(r_h, r_v)\n\n    # compute vertical and horizontal offsets to center the pattern\n    vert_span = 2.0 * base_r + (m - 1) * base_r * sqrt3\n    top = (1.0 - vert_span) / 2.0 + base_r\n    dy = base_r * sqrt3\n\n    centers = []\n    for row_idx, k in enumerate(rows):\n        row_span = 2.0 * base_r * k\n        left = (1.0 - row_span) / 2.0 + base_r\n        xs = left + np.arange(k) * (2.0 * base_r)\n        y = top + row_idx * dy\n        for x in xs:\n            centers.append((x, y))\n\n    centers = np.array(centers)\n    # ensure we have 26\n    assert centers.shape[0] == 26\n\n    radii = compute_max_radii(centers)\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers, tol=1e-10, max_iters=2000):\n    \"\"\"\n    Given fixed centers inside [0,1]^2, compute radii that maximize sum of\n    radii subject to: 0 <= r_i <= dist_to_border_i and r_i + r_j <= dist(i,j).\n    We use a simple iterative constraint-propagation method:\n    - initialize r_i = dist_to_border_i\n    - repeatedly scan pairs; if r_i + r_j > d_ij reduce the larger of (r_i,r_j)\n      to d_ij - the_other (this keeps the pair sum at most d_ij while not\n      shrinking both unnecessarily).\n    This converges in practice for well-separated hex-like layouts.\n    \"\"\"\n    n = centers.shape[0]\n    # start with boundary-limited radii\n    radii = np.minimum(centers[:, 0], centers[:, 1])\n    radii = np.minimum(radii, 1.0 - centers[:, 0])\n    radii = np.minimum(radii, 1.0 - centers[:, 1])\n\n    # Precompute pairwise distances\n    diff = centers[:, None, :] - centers[None, :, :]\n    dists = np.sqrt(np.sum(diff * diff, axis=2))\n    # Avoid self-dist zero issues\n    np.fill_diagonal(dists, np.inf)\n\n    for it in range(max_iters):\n        max_change = 0.0\n        # scan pairs (i<j)\n        for i in range(n):\n            for j in range(i + 1, n):\n                d = dists[i, j]\n                if d <= 0.0:\n                    continue\n                ri, rj = radii[i], radii[j]\n                if ri + rj > d:\n                    # reduce the larger radius to satisfy ri + rj <= d\n                    if ri > rj:\n                        new_ri = max(0.0, d - rj)\n                        change = abs(new_ri - ri)\n                        if change > 0:\n                            radii[i] = new_ri\n                            max_change = max(max_change, change)\n                    else:\n                        new_rj = max(0.0, d - ri)\n                        change = abs(new_rj - rj)\n                        if change > 0:\n                            radii[j] = new_rj\n                            max_change = max(max_change, change)\n        if max_change < tol:\n            break\n\n    return radii\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nKey features: Alternative approach to validity, Alternative approach to sum_radii\n\n### Program D2 (Score: 0.8220)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for 26 circles in the unit square.\n\nKey ideas:\n- Use the successful hex-like row pattern [6,5,6,5,4].\n- Sweep a small set of horizontal spacings, slight vertical scaling,\n  two offset modes and small x-shifts to find a good layout.\n- For each center set compute radii with an alternating shrink/grow solver\n  that starts from wall-limited radii, repairs pairwise violations by\n  shrinking the larger circle, and then grows circles to local maxima.\nThis yields a robust, deterministic packing with a large sum of radii.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    rows = [6,5,6,5,4]\n    n = sum(rows)\n    assert n == 26\n    kmax = max(rows)\n    sqrt3 = np.sqrt(3.0)\n\n    # allowable horizontal spacing: (kmax-1)*a <= 1\n    a_max = 1.0 / (kmax - 1)\n    a_min = max(0.032, a_max * 0.42)\n\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    # sample horizontal spacings more densely near a_max\n    for a in np.linspace(a_max * 0.99, a_min, 20):\n        v0 = a * sqrt3 / 2.0\n        for v_scale in (0.97, 0.99, 1.0, 1.01):\n            v = v0 * v_scale\n            total_h = (len(rows) - 1) * v\n            if total_h > 1.0:\n                continue\n            for short_offset_half in (False, True):\n                for dx in np.linspace(-0.018, 0.018, 5):\n                    centers = np.zeros((n,2), dtype=float)\n                    idx = 0\n                    y0 = (1.0 - total_h) / 2.0\n                    ok = True\n                    for r_idx, cnt in enumerate(rows):\n                        y = y0 + r_idx * v\n                        offset = (a/2.0) if (cnt < kmax and short_offset_half) else 0.0\n                        row_width = (cnt - 1) * a\n                        x0 = (1.0 - row_width) / 2.0 + offset + dx\n                        for c in range(cnt):\n                            if idx >= n:\n                                break\n                            x = x0 + c * a\n                            # if center would be outside, mark layout invalid\n                            if x <= 0.0 + 1e-9 or x >= 1.0 - 1e-9 or y <= 0.0 + 1e-9 or y >= 1.0 - 1e-9:\n                                ok = False\n                                break\n                            centers[idx,0] = x\n                            centers[idx,1] = y\n                            idx += 1\n                        if not ok:\n                            break\n                    if not ok or idx < n:\n                        continue\n                    radii = maximize_radii(centers)\n                    s = float(np.sum(radii))\n                    if s > best_sum:\n                        best_sum = s\n                        best_centers = centers.copy()\n                        best_radii = radii.copy()\n\n    # fallback deterministic layout if none selected\n    if best_centers is None:\n        a = a_max * 0.98\n        v = a * sqrt3 / 2.0\n        centers = []\n        y0 = (1.0 - (len(rows) - 1) * v) / 2.0\n        for r_idx, cnt in enumerate(rows):\n            y = y0 + r_idx * v\n            offset = (a/2.0) if (cnt < kmax) else 0.0\n            row_width = (cnt - 1) * a\n            x0 = (1.0 - row_width) / 2.0 + offset\n            for c in range(cnt):\n                centers.append((x0 + c * a, y))\n        best_centers = np.array(centers[:n], dtype=float)\n        best_radii = maximize_radii(best_centers)\n        best_sum = float(np.sum(best_radii))\n\n    return best_centers, best_radii, float(best_sum)\n\n\ndef maximize_radii(centers, tol=1e-10, max_cycles=800):\n    \"\"\"\n    Alternating shrink/grow solver for:\n      max sum r_i s.t. 0<=r_i<=wall_i, r_i + r_j <= d_ij.\n    Deterministic, starts from wall-limited radii.\n    \"\"\"\n    n = centers.shape[0]\n    if n == 0:\n        return np.array([])\n\n    wall = np.minimum.reduce([centers[:,0], centers[:,1], 1.0 - centers[:,0], 1.0 - centers[:,1]])\n    wall = np.maximum(wall, 0.0)\n\n    diff = centers[:,None,:] - centers[None,:,:]\n    dists = np.sqrt(np.clip((diff**2).sum(axis=2), 0.0, np.inf))\n    np.fill_diagonal(dists, np.inf)\n\n    r = wall.copy()\n\n    for cycle in range(max_cycles):\n        max_change = 0.0\n        # shrink: for each violating pair reduce the larger to satisfy the pair\n        for i in range(n):\n            ri = r[i]\n            s = ri + r\n            viol = np.where(s > dists[i] + 1e-12)[0]\n            for j in viol:\n                if j == i: continue\n                dij = dists[i,j]\n                if r[i] >= r[j]:\n                    new_ri = max(0.0, dij - r[j])\n                    ch = ri - new_ri\n                    if ch > 0:\n                        r[i] = new_ri\n                        ri = new_ri\n                        if ch > max_change: max_change = ch\n                else:\n                    new_rj = max(0.0, dij - r[i])\n                    ch = r[j] - new_rj\n                    if ch > 0:\n                        r[j] = new_rj\n                        if ch > max_change: max_change = ch\n\n        # enforce wall\n        over = r > wall\n        if np.any(over):\n            diffv = np.max(r[over] - wall[over])\n            r[over] = wall[over]\n            if diffv > max_change: max_change = diffv\n\n        # grow: attempt to increase each radius to min(wall, min_j(d_ij - r_j))\n        for i in range(n):\n            allowed = wall[i]\n            lims = dists[i,:] - r\n            mn = np.min(lims)\n            target = min(allowed, mn)\n            if target < 0.0: target = 0.0\n            ch = target - r[i]\n            if ch > 0:\n                r[i] = target\n                if ch > max_change: max_change = ch\n\n        if max_change < tol:\n            break\n\n    # final proportional fixes for any tiny remaining violations\n    for _ in range(6):\n        changed = False\n        for i in range(n):\n            for j in range(i+1, n):\n                dij = dists[i,j]\n                if r[i] + r[j] > dij:\n                    tot = r[i] + r[j]\n                    if tot <= 0:\n                        if r[i] != 0.0 or r[j] != 0.0:\n                            r[i] = r[j] = 0.0\n                            changed = True\n                    else:\n                        scale = dij / tot\n                        new_i = r[i] * scale\n                        new_j = r[j] * scale\n                        if new_i != r[i] or new_j != r[j]:\n                            r[i], r[j] = new_i, new_j\n                            changed = True\n        if not changed:\n            break\n\n    r = np.clip(r, 0.0, wall)\n    return r\n\n\n# Fixed API\ndef run_packing():\n    centers, radii, s = construct_packing()\n    return centers, radii, s\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(6,6))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect(\"equal\")\n    ax.add_patch(plt.Rectangle((0,0),1,1,fill=False,linewidth=1))\n    for c,r in zip(centers, radii):\n        ax.add_patch(Circle(c, r, alpha=0.6, edgecolor='k'))\n    plt.title(f\"n={len(centers)}, sum={sum(radii):.6f}\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, total = run_packing()\n    print(f\"Sum of radii: {total:.6f}\")\n    # visualize(centers, radii)\n# EVOLVE-BLOCK-END\n```\nKey features: Alternative approach to validity, Alternative approach to sum_radii\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 0.9256, Type: High-Performer)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Improved constructor-based circle packing for n=26 using a maximal hex-grid constructor\nwith small alignment search, then per-center maximal-radius propagation.\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    \"\"\"\n    Build a near-optimal deterministic arrangement of 26 circles in the unit square.\n    Strategy:\n     - Try several hex-grid alignments (horizontal offset 0 or r, small vertical shifts)\n     - For each alignment, binary-search the largest equal radius r that yields >=26\n       grid centers inside [r,1-r].\n     - Select the best alignment (largest r), take the first 26 grid centers,\n       then run a constraint-propagation routine to maximize individual radii\n       (respecting border and pairwise non-overlap). This allows radii to vary\n       and increases the total sum beyond 26*r.\n    Returns (centers, radii, sum_of_radii).\n    \"\"\"\n    n_needed = 26\n    sqrt3 = np.sqrt(3.0)\n\n    def gen_grid(r, horiz_offset, vert_shift):\n        pts = []\n        y = r + vert_shift\n        row = 0\n        while y <= 1.0 - r + 1e-12:\n            offset = (row % 2) * r + horiz_offset\n            # place x from r+offset to 1-r\n            x = r + offset\n            while x <= 1.0 - r + 1e-12:\n                if r - 1e-12 <= x <= 1.0 - r + 1e-12:\n                    pts.append((x, y))\n                x += 2.0 * r\n            y += sqrt3 * r\n            row += 1\n        return np.array(pts)\n\n    def max_r_for_alignment(horiz_offset, vert_frac_steps=3):\n        # binary search for largest r such that count >= n_needed for some vert_shift\n        lo, hi = 1e-8, 0.5\n        best_r = 0.0\n        best_pts = None\n        for _ in range(40):\n            mid = 0.5 * (lo + hi)\n            found = False\n            for frac in np.linspace(0.0, 0.5, vert_frac_steps):\n                vert_shift = frac * sqrt3 * mid\n                pts = gen_grid(mid, horiz_offset, vert_shift)\n                if pts.shape[0] >= n_needed:\n                    found = True\n                    break\n            if found:\n                best_r = mid\n                lo = mid\n            else:\n                hi = mid\n        # Reconstruct best point set with best_r and choose lexicographically first n_needed\n        if best_r > 0:\n            # pick best vertical shift that gives most symmetric placement (centered)\n            best_pts = None\n            for frac in np.linspace(0.0, 0.5, vert_frac_steps):\n                vert_shift = frac * sqrt3 * best_r\n                pts = gen_grid(best_r, horiz_offset, vert_shift)\n                if pts.shape[0] >= n_needed:\n                    # sort by y descending (center-first) then x ascending for stability\n                    pts_sorted = pts[np.lexsort((pts[:, 0], -pts[:, 1]))]\n                    if best_pts is None or pts_sorted.shape[0] > best_pts.shape[0]:\n                        best_pts = pts_sorted\n            if best_pts is None:\n                best_pts = gen_grid(best_r, horiz_offset, 0.0)\n        return best_r, best_pts\n\n    # Try two horizontal offsets (0 or r) to allow denser packings\n    best_global = {\"r\": 0.0, \"pts\": None}\n    for horiz_choice in (0.0, 1.0):  # 1.0 stands for 'r' later scaled inside function\n        # horiz_offset is either 0.0 or r -> we encode r by passing 0.0 or 'r' marker\n        # To keep gen_grid signature simple, convert marker to actual offset inside max_r_for_alignment\n        if horiz_choice == 0.0:\n            horiz_offset = 0.0\n        else:\n            # use a lambda-like trick: pass offset as 'r' marker, handle by passing mid in gen call\n            # We'll handle by trying horiz_offset = mid inside max_r_for_alignment by calling twice:\n            horiz_offset = None  # indicator\n        # Custom version to handle horiz_offset == None (meaning offset=r)\n        def max_r_custom(h_offset_flag):\n            lo, hi = 1e-8, 0.5\n            best_r = 0.0\n            best_pts = None\n            for _ in range(40):\n                mid = 0.5 * (lo + hi)\n                found = False\n                for frac in np.linspace(0.0, 0.5, 4):\n                    vert_shift = frac * sqrt3 * mid\n                    ho = (mid if h_offset_flag is None else h_offset_flag)\n                    pts = gen_grid(mid, ho, vert_shift)\n                    if pts.shape[0] >= n_needed:\n                        found = True\n                        break\n                if found:\n                    best_r = mid\n                    lo = mid\n                else:\n                    hi = mid\n            if best_r > 0:\n                for frac in np.linspace(0.0, 0.5, 6):\n                    vert_shift = frac * sqrt3 * best_r\n                    ho = (best_r if h_offset_flag is None else h_offset_flag)\n                    pts = gen_grid(best_r, ho, vert_shift)\n                    if pts.shape[0] >= n_needed:\n                        pts_sorted = pts[np.lexsort((pts[:, 0], -pts[:, 1]))]\n                        if best_pts is None or pts_sorted.shape[0] > best_pts.shape[0]:\n                            best_pts = pts_sorted\n                if best_pts is None:\n                    best_pts = gen_grid(best_r, (best_r if h_offset_flag is None else h_offset_flag), 0.0)\n            return best_r, best_pts\n\n        r_val, pts = max_r_custom(horiz_offset)\n        if r_val > best_global[\"r\"] and pts is not None:\n            best_global[\"r\"] = r_val\n            best_global[\"pts\"] = pts.copy()\n\n    if best_global[\"pts\"] is None:\n        # Fallback to previous deterministic row layout (guarantees feasibility)\n        rows = [6,5,6,5,4]\n        sqrt3 = np.sqrt(3.0)\n        max_k = max(rows)\n        r_h = 1.0 / (2.0 * max_k)\n        r_v = 1.0 / (2.0 + (len(rows) - 1) * sqrt3)\n        base_r = min(r_h, r_v)\n        vert_span = 2.0 * base_r + (len(rows) - 1) * base_r * sqrt3\n        top = (1.0 - vert_span) / 2.0 + base_r\n        dy = base_r * sqrt3\n        centers = []\n        for row_idx, k in enumerate(rows):\n            row_span = 2.0 * base_r * k\n            left = (1.0 - row_span) / 2.0 + base_r\n            xs = left + np.arange(k) * (2.0 * base_r)\n            y = top + row_idx * dy\n            for x in xs:\n                centers.append((x, y))\n        centers = np.array(centers)\n        centers = centers[:n_needed]\n    else:\n        # pick the first n_needed sorted points\n        pts = best_global[\"pts\"]\n        centers = pts[:n_needed].copy()\n        # ensure all centers strictly within [0,1]\n        centers = np.clip(centers, 1e-12, 1.0 - 1e-12)\n\n    # Now compute maximal radii for these fixed centers\n    radii = compute_max_radii(centers)\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\ndef compute_max_radii(centers, tol=1e-12, max_iters=2000):\n    \"\"\"\n    Constraint-propagation to maximize sum of radii given fixed centers.\n    Start from border-limited radii and greedily resolve pairwise overlaps by\n    reducing the larger radius (this tends to minimize decrease in total sum).\n    \"\"\"\n    n = centers.shape[0]\n    # start with boundary-limited radii\n    radii = np.minimum.reduce((\n        centers[:, 0],\n        centers[:, 1],\n        1.0 - centers[:, 0],\n        1.0 - centers[:, 1]\n    )).astype(float)\n\n    # Precompute pairwise distances\n    diff = centers[:, None, :] - centers[None, :, :]\n    dists = np.sqrt(np.sum(diff * diff, axis=2))\n    np.fill_diagonal(dists, np.inf)\n\n    # Repeatedly enforce r_i + r_j <= d_ij by reducing the larger radius when needed.\n    for it in range(max_iters):\n        max_change = 0.0\n        for i in range(n):\n            ri = radii[i]\n            # vectorized check against j>i\n            # but keep simple loops for determinism\n            for j in range(i + 1, n):\n                d = dists[i, j]\n                if d <= 0.0:\n                    continue\n                rj = radii[j]\n                if ri + rj > d:\n                    # reduce the larger radius only\n                    if ri > rj:\n                        new_ri = max(0.0, d - rj)\n                        change = ri - new_ri\n                        if change > 0:\n                            radii[i] = new_ri\n                            ri = new_ri\n                            max_change = max(max_change, change)\n                    else:\n                        new_rj = max(0.0, d - ri)\n                        change = rj - new_rj\n                        if change > 0:\n                            radii[j] = new_rj\n                            max_change = max(max_change, change)\n        if max_change < tol:\n            break\n    return radii\n\n# EVOLVE-BLOCK-END\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nUnique approach: Excellent validity (1.000), Excellent sum_radii (2.439), Excellent target_ratio (0.926)\n\n# Current Program\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Improved deterministic constructor for 26 circles in the unit square.\n\nKey ideas:\n- Use the hex-like row pattern [6,5,6,5,4].\n- Sample a small grid of horizontal spacings, small global x-shifts and two offset modes\n  (shorter rows either centered or half-step shifted) and slight vertical compression/stretch.\n- For each candidate placement compute radii with an enhanced constraint solver:\n    * start from border limits\n    * alternate reduction sweeps (repair violating pairs by shrinking the larger)\n      and growth sweeps (increase each radius to the largest allowed by current neighbors)\n    * finalize with proportional pairwise fixes\n  This alternation lets radii both relax down when needed and regrow to better fill free space,\n  producing larger total radii than greedy-only reductions.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    rows = [6, 5, 6, 5, 4]\n    m = len(rows)\n    kmax = max(rows)\n    # allowable horizontal spacing: ensure widest row fits\n    a_max = 1.0 / (kmax - 1)  # (kmax-1)*a <= 1\n    a_min = 0.035\n    if a_min >= a_max:\n        a_min = a_max * 0.6\n\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    sqrt3 = np.sqrt(3.0)\n\n    # sample horizontal spacings more densely near a_max\n    for a in np.linspace(a_max * 0.98, a_min, 20):\n        # nominal hex vertical step (can scale slightly)\n        v0 = a * sqrt3 / 2.0\n        for v_scale in (0.96, 0.98, 1.0, 1.02):\n            v = v0 * v_scale\n            total_h = (m - 1) * v\n            if total_h > 1.0:\n                continue\n            # two offset modes: 0 or half-step for shorter rows\n            for short_offset_half in (False, True):\n                # small global horizontal shifts to allow better edge packing\n                for dx in np.linspace(-0.02, 0.02, 5):\n                    centers = np.zeros((n, 2))\n                    idx = 0\n                    y0 = (1.0 - total_h) / 2.0\n                    ok = True\n                    for r_idx, cnt in enumerate(rows):\n                        y = y0 + r_idx * v\n                        # compute offset for this row\n                        if cnt < kmax and short_offset_half:\n                            offset = a / 2.0\n                        else:\n                            offset = 0.0\n                        row_width = (cnt - 1) * a\n                        x0 = (1.0 - row_width) / 2.0 + offset + dx\n                        # place points and ensure inside [0,1] (clamp edges)\n                        for c in range(cnt):\n                            if idx >= n:\n                                break\n                            x = x0 + c * a\n                            if x <= 0.0 + 1e-9 or x >= 1.0 - 1e-9 or y <= 0.0 + 1e-9 or y >= 1.0 - 1e-9:\n                                # allow small clamping but if center would be too far out, mark not ok\n                                x = np.clip(x, 1e-9, 1.0 - 1e-9)\n                                y = np.clip(y, 1e-9, 1.0 - 1e-9)\n                            centers[idx, 0] = x\n                            centers[idx, 1] = y\n                            idx += 1\n                    if idx < n:\n                        ok = False\n                    if not ok:\n                        continue\n                    radii = maximize_radii_refined(centers)\n                    s = float(np.sum(radii))\n                    if s > best_sum:\n                        best_sum = s\n                        best_centers = centers.copy()\n                        best_radii = radii.copy()\n\n    # Fallback deterministic center if sampling failed\n    if best_centers is None:\n        # simple centered hex-like placement\n        a = a_max * 0.98\n        v = a * sqrt3 / 2.0\n        centers = []\n        y0 = (1.0 - (m - 1) * v) / 2.0\n        for r_idx, cnt in enumerate(rows):\n            y = y0 + r_idx * v\n            offset = (a / 2.0) if (cnt < kmax) else 0.0\n            row_width = (cnt - 1) * a\n            x0 = (1.0 - row_width) / 2.0 + offset\n            for c in range(cnt):\n                centers.append((np.clip(x0 + c * a, 1e-9, 1.0 - 1e-9), y))\n        best_centers = np.array(centers[:n], dtype=float)\n        best_radii = maximize_radii_refined(best_centers)\n        best_sum = float(np.sum(best_radii))\n\n    return best_centers, best_radii, float(best_sum)\n\n\ndef maximize_radii_refined(centers, tol=1e-9, max_cycles=1000):\n    \"\"\"\n    Alternating repair/increase solver for the LP:\n      maximize sum r_i\n      s.t. 0 <= r_i <= wall_i, r_i + r_j <= d_ij.\n\n    Algorithm:\n      - initialize r = wall (border-limited)\n      - repeat cycles:\n          1) reduction sweep: for every violating pair shrink the larger to d_ij - other\n          2) growth sweep: for each i increase r_i to min(wall_i, min_j(d_ij - r_j))\n      - finish with proportional pairwise fixes\n    This tends to reach a large feasible sum and is deterministic.\n    \"\"\"\n    n = centers.shape[0]\n    if n == 0:\n        return np.array([])\n\n    wall = np.minimum.reduce([centers[:, 0], centers[:, 1],\n                              1.0 - centers[:, 0], 1.0 - centers[:, 1]])\n    wall = np.maximum(wall, 0.0)\n\n    diff = centers[:, None, :] - centers[None, :, :]\n    dists = np.sqrt(np.sum(diff * diff, axis=2))\n    np.fill_diagonal(dists, np.inf)\n\n    # start from border-limited radii\n    r = wall.copy()\n\n    for cycle in range(max_cycles):\n        max_change = 0.0\n        # 1) Reduction sweep (repair violations by shrinking larger)\n        for i in range(n):\n            ri = r[i]\n            # vectorized candidate check\n            s = ri + r\n            viol = np.where(s > dists[i] + 1e-12)[0]\n            for j in viol:\n                if j == i:\n                    continue\n                dij = dists[i, j]\n                # shrink the larger radius\n                if r[i] >= r[j]:\n                    new_ri = max(0.0, dij - r[j])\n                    ch = ri - new_ri\n                    if ch > 0:\n                        r[i] = new_ri\n                        ri = new_ri\n                        if ch > max_change:\n                            max_change = ch\n                else:\n                    new_rj = max(0.0, dij - r[i])\n                    ch = r[j] - new_rj\n                    if ch > 0:\n                        r[j] = new_rj\n                        if ch > max_change:\n                            max_change = ch\n\n        # enforce wall limits (no increases here)\n        over = r > wall\n        if np.any(over):\n            diffv = np.max(r[over] - wall[over])\n            r[over] = wall[over]\n            if diffv > max_change:\n                max_change = diffv\n\n        # 2) Growth sweep: try to grow each circle to the largest allowed by current neighbors\n        for i in range(n):\n            allowed = wall[i]\n            # compute min_j (d_ij - r_j)\n            lims = dists[i, :] - r\n            # ignore self (inf)\n            minlim = np.min(lims)\n            target = min(allowed, minlim)\n            if target < 0.0:\n                target = 0.0\n            ch = target - r[i]\n            if ch > 0:\n                r[i] = target\n                if ch > max_change:\n                    max_change = ch\n\n        if max_change < tol:\n            break\n\n    # Final proportional fixes for any remaining tiny violations\n    for _ in range(4):\n        changed = False\n        for i in range(n):\n            for j in range(i + 1, n):\n                dij = dists[i, j]\n                if r[i] + r[j] > dij:\n                    tot = r[i] + r[j]\n                    if tot <= 0:\n                        if r[i] != 0.0 or r[j] != 0.0:\n                            r[i] = r[j] = 0.0\n                            changed = True\n                    else:\n                        scale = dij / tot\n                        new_i = r[i] * scale\n                        new_j = r[j] * scale\n                        if new_i != r[i] or new_j != r[j]:\n                            r[i], r[j] = new_i, new_j\n                            changed = True\n        if not changed:\n            break\n\n    r = np.clip(r, 0.0, wall)\n    return r\n\n\n# Fixed API\ndef run_packing():\n    centers, radii, s = construct_packing()\n    return centers, radii, s\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(6,6))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect(\"equal\")\n    ax.add_patch(plt.Rectangle((0,0),1,1,fill=False,linewidth=1))\n    for c,r in zip(centers, radii):\n        ax.add_patch(Circle(c, r, alpha=0.6, edgecolor='k'))\n    plt.title(f\"n={len(centers)}, sum={sum(radii):.6f}\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, total = run_packing()\n    print(f\"Sum of radii: {total:.6f}\")\n    # visualize(centers, radii)\n# EVOLVE-BLOCK-END\n```\n\n# Task\nRewrite the program to improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: complexity, diversity\nDifferent solutions with similar fitness but different features are valuable.\nProvide the complete new program code.\n\nIMPORTANT: Make sure your rewritten program maintains the same inputs and outputs\nas the original program, but with improved internal implementation.\n\n```python\n# Your rewritten program here\n```", "responses": ["```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for n=26 circle packing in the unit square.\n\nStrategy (compact):\n - Build hexagonal grids for two horizontal alignment choices (offset=0 and offset=r).\n - For each choice binary-search the largest equal radius r that yields >=26 grid centers\n   inside [r,1-r] for some small vertical shifts.\n - From best grid take 26 centers chosen center-first, then run a Gauss-Seidel\n   monotone growth to maximize each radius under wall and pairwise constraints.\nThis yields varied radii and typically a large total radius sum.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n_needed = 26\n    sqrt3 = np.sqrt(3.0)\n\n    def gen_grid(r, horiz_offset, vert_shift):\n        pts = []\n        y = r + vert_shift\n        row = 0\n        while y <= 1.0 - r + 1e-12:\n            offset = ((row % 2) * r) + horiz_offset\n            x = r + offset\n            while x <= 1.0 - r + 1e-12:\n                pts.append((x, y))\n                x += 2.0 * r\n            y += sqrt3 * r\n            row += 1\n        return np.array(pts) if pts else np.empty((0,2))\n\n    def search_alignment(hflag):\n        lo, hi = 1e-8, 0.5\n        best_r = 0.0\n        for _ in range(40):\n            mid = 0.5*(lo+hi)\n            found = False\n            for frac in np.linspace(0.0, 0.5, 5):\n                vert_shift = frac * sqrt3 * mid\n                ho = (mid if hflag is None else hflag)\n                pts = gen_grid(mid, ho, vert_shift)\n                if pts.shape[0] >= n_needed:\n                    found = True\n                    break\n            if found:\n                best_r = mid\n                lo = mid\n            else:\n                hi = mid\n        best_pts = None\n        if best_r > 0:\n            for frac in np.linspace(0.0, 0.5, 9):\n                vert_shift = frac * sqrt3 * best_r\n                ho = (best_r if hflag is None else hflag)\n                pts = gen_grid(best_r, ho, vert_shift)\n                if pts.shape[0] >= n_needed:\n                    # prefer centers nearer the square center\n                    center = np.array([0.5,0.5])\n                    d = np.sum((pts - center)**2, axis=1)\n                    order = np.argsort(d)  # smallest distance first\n                    pts_sorted = pts[order]\n                    if best_pts is None or pts_sorted.shape[0] > best_pts.shape[0]:\n                        best_pts = pts_sorted\n            if best_pts is None:\n                best_pts = gen_grid(best_r, (best_r if hflag is None else hflag), 0.0)\n        return best_r, best_pts\n\n    best_global = {\"r\":0.0, \"pts\":None}\n    for h in (0.0, None):  # None means horiz_offset = r (staggered)\n        r_val, pts = search_alignment(h)\n        if pts is not None and r_val > best_global[\"r\"]:\n            best_global[\"r\"], best_global[\"pts\"] = r_val, pts.copy()\n\n    if best_global[\"pts\"] is None:\n        # fallback deterministic centered row layout [6,5,6,5,4]\n        rows = [6,5,6,5,4]\n        kmax = max(rows)\n        r_h = 1.0 / (2.0 * kmax)\n        r_v = 1.0 / (2.0 + (len(rows)-1)*sqrt3)\n        base_r = min(r_h, r_v)*0.999\n        vert_span = 2.0*base_r + (len(rows)-1)*base_r*sqrt3\n        top = (1.0 - vert_span)/2.0 + base_r\n        dy = base_r * sqrt3\n        centers = []\n        for ri, cnt in enumerate(rows):\n            y = top + ri*dy\n            row_span = 2.0*base_r*cnt\n            left = (1.0 - row_span)/2.0 + base_r\n            xs = left + np.arange(cnt)*(2.0*base_r)\n            for x in xs:\n                centers.append((x,y))\n        centers = np.array(centers[:n_needed], dtype=float)\n    else:\n        pts = best_global[\"pts\"]\n        centers = pts[:n_needed].copy()\n        centers = np.clip(centers, 1e-12, 1.0-1e-12)\n\n    radii = maximize_radii_gs(centers)\n    return centers, radii, float(np.sum(radii))\n\n\ndef maximize_radii_gs(centers, tol=1e-10, max_iters=5000):\n    n = centers.shape[0]\n    if n == 0:\n        return np.array([])\n    wall = np.minimum.reduce([centers[:,0], centers[:,1], 1.0-centers[:,0], 1.0-centers[:,1]])\n    wall = np.maximum(wall, 0.0)\n    diff = centers[:,None,:] - centers[None,:,:]\n    dists = np.sqrt(np.sum(diff*diff, axis=2))\n    np.fill_diagonal(dists, np.inf)\n    r = np.zeros(n, dtype=float)\n    for it in range(max_iters):\n        max_change = 0.0\n        for i in range(n):\n            limits = dists[i] - r\n            allowed = float(np.min(limits))\n            newri = min(allowed, wall[i])\n            if newri < 0.0:\n                newri = 0.0\n            change = abs(newri - r[i])\n            if change:\n                r[i] = newri\n                if change > max_change:\n                    max_change = change\n        if max_change < tol:\n            break\n    # proportional repairs\n    for _ in range(4):\n        changed = False\n        for i in range(n):\n            for j in range(i+1,n):\n                dij = dists[i,j]\n                if r[i] + r[j] > dij:\n                    tot = r[i] + r[j]\n                    if tot <= 0:\n                        if r[i]!=0 or r[j]!=0:\n                            r[i]=r[j]=0.0; changed=True\n                    else:\n                        scale = dij / tot\n                        ni, nj = r[i]*scale, r[j]*scale\n                        if ni!=r[i] or nj!=r[j]:\n                            r[i], r[j] = ni, nj; changed=True\n        if not changed:\n            break\n    r = np.minimum(r, wall)\n    r = np.maximum(r, 0.0)\n    return r\n\n# Fixed API\ndef run_packing():\n    centers, radii, s = construct_packing()\n    return centers, radii, s\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(6,6))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect(\"equal\")\n    ax.add_patch(plt.Rectangle((0,0),1,1,fill=False,linewidth=1))\n    for c,r in zip(centers, radii):\n        ax.add_patch(Circle(c, r, alpha=0.6, edgecolor='k'))\n    plt.title(f\"n={len(centers)}, sum={sum(radii):.6f}\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, total = run_packing()\n    print(f\"Sum of radii: {total:.6f}\")\n# EVOLVE-BLOCK-END\n```"]}}, "artifacts_json": null, "artifact_dir": null}