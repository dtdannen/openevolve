{
  "failure_patterns": {
    "performance_regression:sum_radii decreased by n.n% (n.n \u2192 n.n)": {
      "pattern_type": "performance_regression",
      "description": "sum_radii decreased by 53.1% (1.806 \u2192 0.847)",
      "count": 1,
      "first_seen": 9,
      "last_seen": 9,
      "example_error": null,
      "parent_code": "# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (improved)\"\"\"\nimport numpy as np\nfrom math import sqrt\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square.\n    Returns: (centers, radii, sum_of_radii)\n    \"\"\"\n    n_target = 26\n\n    # Generate a hexagonal lattice of candidate centers filling the unit square\n    s = 0.12                      # lattice spacing (tunable)\n    vy = sqrt(3) / 2 * s\n    candidates = []\n    nrows = int(np.ceil(1.0 / vy)) + 1\n    ncols = int(np.ceil(1.0 / s)) + 1\n    for j in range(nrows):\n        y = j * vy\n        if y <= 0 or y >= 1: \n            continue\n        offset = (j % 2) * (s / 2.0)\n        for i in range(ncols):\n            x = i * s + offset\n            if x <= 0 or x >= 1:\n                continue\n            candidates.append((x, y))\n    candidates = np.array(candidates)\n\n    # Add four near-corner anchors to encourage large corner circles\n    corner_margin = 0.06\n    corner_points = np.array([\n        (corner_margin, corner_margin),\n        (1 - corner_margin, corner_margin),\n        (corner_margin, 1 - corner_margin),\n        (1 - corner_margin, 1 - corner_margin),\n    ])\n    # Merge and uniquify candidates\n    all_pts = np.vstack((candidates, corner_points))\n    # Remove duplicates (within tiny tol)\n    uniq = []\n    for p in all_pts:\n        if not any(np.linalg.norm(p - q) < 1e-6 for q in uniq):\n            uniq.append(p)\n    candidates = np.array(uniq)\n\n    # Greedy selection: pick n_target centers from candidates to maximize sum of radii\n    selected_idx = []\n    remaining = list(range(len(candidates)))\n\n    def compute_max_radii(centers):\n        m = centers.shape[0]\n        # Start with distance to walls\n        radii = np.minimum(np.minimum(centers[:,0], 1 - centers[:,0]),\n                           np.minimum(centers[:,1], 1 - centers[:,1]))\n        radii = np.clip(radii, 0.0, 1.0)\n\n        if m == 0:\n            return radii\n\n        # Iterative Gauss-Seidel relaxation to enforce r_i + r_j <= d_ij\n        coords = centers.copy()\n        tol = 1e-7\n        max_iter = 1000\n        for _ in range(max_iter):\n            max_change = 0.0\n            for i in range(m):\n                ri_old = radii[i]\n                # limit by walls already included in initial value\n                ri = ri_old\n                for j in range(m):\n                    if i == j:\n                        continue\n                    d = np.linalg.norm(coords[i] - coords[j])\n                    # If centers coincide, force tiny distance to avoid div by zero\n                    if d < 1e-8:\n                        d = 1e-8\n                    # r_i <= d - r_j\n                    bound = d - radii[j]\n                    if bound < ri:\n                        ri = max(0.0, bound)\n                # update\n                change = abs(ri - ri_old)\n                if change > max_change:\n                    max_change = change\n                radii[i] = ri\n            if max_change < tol:\n                break\n        return radii\n\n    # Greedy picking loop\n    while len(selected_idx) < n_target and remaining:\n        best_idx = None\n        best_sum = -1.0\n        # evaluate each candidate addition\n        for idx in remaining:\n            trial_indices = selected_idx + [idx]\n            centers_trial = candidates[trial_indices]\n            r_trial = compute_max_radii(centers_trial)\n            ssum = float(np.sum(r_trial))\n            if ssum > best_sum:\n                best_sum = ssum\n                best_idx = idx\n        if best_idx is None:\n            break\n        selected_idx.append(best_idx)\n        remaining.remove(best_idx)\n\n    # If greedy failed to pick enough (shouldn't), pad with nearest-to-center remaining\n    if len(selected_idx) < n_target:\n        remaining_sorted = sorted(remaining, key=lambda i: np.linalg.norm(candidates[i] - np.array([0.5,0.5])))\n        for idx in remaining_sorted:\n            if len(selected_idx) >= n_target:\n                break\n            selected_idx.append(idx)\n\n    centers = candidates[selected_idx][:n_target]\n    # Final radii computed and refined\n    radii = compute_max_radii(centers)\n\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n# EVOLVE-BLOCK-END\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, linewidth=0.5, edgecolor='k')\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)",
      "child_code": "# EVOLVE-BLOCK-START\n\"\"\"Explicit constructor for packing 26 circles in the unit square.\nDesigned to place centers in a mixed hexagonal + edge + corner pattern\nand compute maximal feasible radii by Gauss-Seidel relaxation.\n\"\"\"\nimport numpy as np\nfrom math import cos, sin, pi\n\ndef construct_packing():\n    n_target = 26\n\n    # 1) Centered hex-like cluster: 1 center + 6 inner + 7 outer = 14\n    cx, cy = 0.5, 0.5\n    centers = [(cx, cy)]\n    # inner ring (6)\n    r_inner = 0.12\n    for k in range(6):\n        a = k * 2 * pi / 6\n        centers.append((cx + r_inner * cos(a), cy + r_inner * sin(a)))\n    # outer 7 (a heptagonal-ish ring) to fill gaps toward edges\n    r_outer = 0.24\n    for k in range(7):\n        a = k * 2 * pi / 7 + 0.1  # slight rotation\n        centers.append((cx + r_outer * cos(a), cy + r_outer * sin(a)))\n\n    # 2) Edge mid positions: 2 per side (8)\n    edge_off = 0.06\n    edge_x = [0.33, 0.66]\n    edge_y = [0.33, 0.66]\n    # bottom and top\n    for x in edge_x:\n        centers.append((x, edge_off))\n        centers.append((x, 1.0 - edge_off))\n    # left and right\n    for y in edge_y:\n        centers.append((edge_off, y))\n        centers.append((1.0 - edge_off, y))\n\n    # 3) Four near-corner anchors (4)\n    cm = 0.06\n    centers.extend([\n        (cm, cm),\n        (1.0 - cm, cm),\n        (cm, 1.0 - cm),\n        (1.0 - cm, 1.0 - cm),\n    ])\n\n    # Ensure we have exactly n_target centers\n    centers = np.array(centers[:n_target])\n    # Clip tiny numerical excursions\n    centers = np.clip(centers, 1e-6, 1.0 - 1e-6)\n\n    # Compute maximal radii for fixed centers using Gauss-Seidel relaxation:\n    def compute_max_radii(pts):\n        m = pts.shape[0]\n        # initial radii limited by distance to walls\n        r = np.minimum.reduce((pts[:,0], 1 - pts[:,0], pts[:,1], 1 - pts[:,1])).copy()\n        r = np.maximum(r, 0.0)\n\n        if m == 0:\n            return r\n\n        # Precompute center-to-center distances (static)\n        D = np.linalg.norm(pts[None,:,:] - pts[:,None,:], axis=2)\n        # Guard diagonal\n        for i in range(m):\n            D[i,i] = 1e-12\n\n        tol = 1e-8\n        max_iter = 2000\n        for it in range(max_iter):\n            max_change = 0.0\n            # iterate Gauss-Seidel style\n            for i in range(m):\n                ri_old = r[i]\n                # start with wall limit (already set in r)\n                ri = min(ri_old, np.minimum.reduce((pts[i,0], 1 - pts[i,0], pts[i,1], 1 - pts[i,1])))\n                # enforce pairwise constraints r_i <= d_ij - r_j\n                # vectorized bound calculation\n                bounds = D[i] - r\n                bounds[i] = ri  # ignore self\n                # ri must be <= min_j bounds_j\n                ri_new = max(0.0, np.min(bounds))\n                # Relax update (take smaller change to improve stability)\n                # But use full update to reach maximal feasible radii\n                change = abs(ri_new - ri_old)\n                if change > max_change:\n                    max_change = change\n                r[i] = ri_new\n            if max_change < tol:\n                break\n        # final safeguard: clip non-negative\n        r = np.maximum(r, 0.0)\n        return r\n\n    radii = compute_max_radii(centers)\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n# EVOLVE-BLOCK-END\n\n# fixed runner\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n# optional visualization helper (not required)\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(6,6))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect('equal')\n    ax.add_patch(plt.Rectangle((0,0),1,1,fill=False,linewidth=1))\n    for i,(c,rr) in enumerate(zip(centers, radii)):\n        ax.add_patch(Circle(c, rr, alpha=0.6, edgecolor='k'))\n        ax.text(c[0], c[1], str(i), fontsize=8, ha='center', va='center')\n    plt.title(f\"n={len(centers)}, sum radii={sum(radii):.6f}\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, s = run_packing()\n    print(f\"Sum of radii: {s:.6f}\")\n    # visualize(centers, radii)",
      "code_diff": "Full rewrite",
      "parent_metrics": {
        "validity": 1.0,
        "sum_radii": 1.8063385766498383,
        "target_ratio": 0.6855174863946256,
        "combined_score": 0.6855174863946256,
        "eval_time": 1.0880210399627686
      },
      "child_metrics": {
        "validity": 1.0,
        "sum_radii": 0.8472200605633073,
        "target_ratio": 0.3215256396824696,
        "combined_score": 0.3215256396824696
      }
    },
    "performance_regression:target_ratio decreased by n.n% (n.n \u2192 n.n)": {
      "pattern_type": "performance_regression",
      "description": "target_ratio decreased by 53.1% (0.686 \u2192 0.322)",
      "count": 1,
      "first_seen": 9,
      "last_seen": 9,
      "example_error": null,
      "parent_code": "# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (improved)\"\"\"\nimport numpy as np\nfrom math import sqrt\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square.\n    Returns: (centers, radii, sum_of_radii)\n    \"\"\"\n    n_target = 26\n\n    # Generate a hexagonal lattice of candidate centers filling the unit square\n    s = 0.12                      # lattice spacing (tunable)\n    vy = sqrt(3) / 2 * s\n    candidates = []\n    nrows = int(np.ceil(1.0 / vy)) + 1\n    ncols = int(np.ceil(1.0 / s)) + 1\n    for j in range(nrows):\n        y = j * vy\n        if y <= 0 or y >= 1: \n            continue\n        offset = (j % 2) * (s / 2.0)\n        for i in range(ncols):\n            x = i * s + offset\n            if x <= 0 or x >= 1:\n                continue\n            candidates.append((x, y))\n    candidates = np.array(candidates)\n\n    # Add four near-corner anchors to encourage large corner circles\n    corner_margin = 0.06\n    corner_points = np.array([\n        (corner_margin, corner_margin),\n        (1 - corner_margin, corner_margin),\n        (corner_margin, 1 - corner_margin),\n        (1 - corner_margin, 1 - corner_margin),\n    ])\n    # Merge and uniquify candidates\n    all_pts = np.vstack((candidates, corner_points))\n    # Remove duplicates (within tiny tol)\n    uniq = []\n    for p in all_pts:\n        if not any(np.linalg.norm(p - q) < 1e-6 for q in uniq):\n            uniq.append(p)\n    candidates = np.array(uniq)\n\n    # Greedy selection: pick n_target centers from candidates to maximize sum of radii\n    selected_idx = []\n    remaining = list(range(len(candidates)))\n\n    def compute_max_radii(centers):\n        m = centers.shape[0]\n        # Start with distance to walls\n        radii = np.minimum(np.minimum(centers[:,0], 1 - centers[:,0]),\n                           np.minimum(centers[:,1], 1 - centers[:,1]))\n        radii = np.clip(radii, 0.0, 1.0)\n\n        if m == 0:\n            return radii\n\n        # Iterative Gauss-Seidel relaxation to enforce r_i + r_j <= d_ij\n        coords = centers.copy()\n        tol = 1e-7\n        max_iter = 1000\n        for _ in range(max_iter):\n            max_change = 0.0\n            for i in range(m):\n                ri_old = radii[i]\n                # limit by walls already included in initial value\n                ri = ri_old\n                for j in range(m):\n                    if i == j:\n                        continue\n                    d = np.linalg.norm(coords[i] - coords[j])\n                    # If centers coincide, force tiny distance to avoid div by zero\n                    if d < 1e-8:\n                        d = 1e-8\n                    # r_i <= d - r_j\n                    bound = d - radii[j]\n                    if bound < ri:\n                        ri = max(0.0, bound)\n                # update\n                change = abs(ri - ri_old)\n                if change > max_change:\n                    max_change = change\n                radii[i] = ri\n            if max_change < tol:\n                break\n        return radii\n\n    # Greedy picking loop\n    while len(selected_idx) < n_target and remaining:\n        best_idx = None\n        best_sum = -1.0\n        # evaluate each candidate addition\n        for idx in remaining:\n            trial_indices = selected_idx + [idx]\n            centers_trial = candidates[trial_indices]\n            r_trial = compute_max_radii(centers_trial)\n            ssum = float(np.sum(r_trial))\n            if ssum > best_sum:\n                best_sum = ssum\n                best_idx = idx\n        if best_idx is None:\n            break\n        selected_idx.append(best_idx)\n        remaining.remove(best_idx)\n\n    # If greedy failed to pick enough (shouldn't), pad with nearest-to-center remaining\n    if len(selected_idx) < n_target:\n        remaining_sorted = sorted(remaining, key=lambda i: np.linalg.norm(candidates[i] - np.array([0.5,0.5])))\n        for idx in remaining_sorted:\n            if len(selected_idx) >= n_target:\n                break\n            selected_idx.append(idx)\n\n    centers = candidates[selected_idx][:n_target]\n    # Final radii computed and refined\n    radii = compute_max_radii(centers)\n\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n# EVOLVE-BLOCK-END\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, linewidth=0.5, edgecolor='k')\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)",
      "child_code": "# EVOLVE-BLOCK-START\n\"\"\"Explicit constructor for packing 26 circles in the unit square.\nDesigned to place centers in a mixed hexagonal + edge + corner pattern\nand compute maximal feasible radii by Gauss-Seidel relaxation.\n\"\"\"\nimport numpy as np\nfrom math import cos, sin, pi\n\ndef construct_packing():\n    n_target = 26\n\n    # 1) Centered hex-like cluster: 1 center + 6 inner + 7 outer = 14\n    cx, cy = 0.5, 0.5\n    centers = [(cx, cy)]\n    # inner ring (6)\n    r_inner = 0.12\n    for k in range(6):\n        a = k * 2 * pi / 6\n        centers.append((cx + r_inner * cos(a), cy + r_inner * sin(a)))\n    # outer 7 (a heptagonal-ish ring) to fill gaps toward edges\n    r_outer = 0.24\n    for k in range(7):\n        a = k * 2 * pi / 7 + 0.1  # slight rotation\n        centers.append((cx + r_outer * cos(a), cy + r_outer * sin(a)))\n\n    # 2) Edge mid positions: 2 per side (8)\n    edge_off = 0.06\n    edge_x = [0.33, 0.66]\n    edge_y = [0.33, 0.66]\n    # bottom and top\n    for x in edge_x:\n        centers.append((x, edge_off))\n        centers.append((x, 1.0 - edge_off))\n    # left and right\n    for y in edge_y:\n        centers.append((edge_off, y))\n        centers.append((1.0 - edge_off, y))\n\n    # 3) Four near-corner anchors (4)\n    cm = 0.06\n    centers.extend([\n        (cm, cm),\n        (1.0 - cm, cm),\n        (cm, 1.0 - cm),\n        (1.0 - cm, 1.0 - cm),\n    ])\n\n    # Ensure we have exactly n_target centers\n    centers = np.array(centers[:n_target])\n    # Clip tiny numerical excursions\n    centers = np.clip(centers, 1e-6, 1.0 - 1e-6)\n\n    # Compute maximal radii for fixed centers using Gauss-Seidel relaxation:\n    def compute_max_radii(pts):\n        m = pts.shape[0]\n        # initial radii limited by distance to walls\n        r = np.minimum.reduce((pts[:,0], 1 - pts[:,0], pts[:,1], 1 - pts[:,1])).copy()\n        r = np.maximum(r, 0.0)\n\n        if m == 0:\n            return r\n\n        # Precompute center-to-center distances (static)\n        D = np.linalg.norm(pts[None,:,:] - pts[:,None,:], axis=2)\n        # Guard diagonal\n        for i in range(m):\n            D[i,i] = 1e-12\n\n        tol = 1e-8\n        max_iter = 2000\n        for it in range(max_iter):\n            max_change = 0.0\n            # iterate Gauss-Seidel style\n            for i in range(m):\n                ri_old = r[i]\n                # start with wall limit (already set in r)\n                ri = min(ri_old, np.minimum.reduce((pts[i,0], 1 - pts[i,0], pts[i,1], 1 - pts[i,1])))\n                # enforce pairwise constraints r_i <= d_ij - r_j\n                # vectorized bound calculation\n                bounds = D[i] - r\n                bounds[i] = ri  # ignore self\n                # ri must be <= min_j bounds_j\n                ri_new = max(0.0, np.min(bounds))\n                # Relax update (take smaller change to improve stability)\n                # But use full update to reach maximal feasible radii\n                change = abs(ri_new - ri_old)\n                if change > max_change:\n                    max_change = change\n                r[i] = ri_new\n            if max_change < tol:\n                break\n        # final safeguard: clip non-negative\n        r = np.maximum(r, 0.0)\n        return r\n\n    radii = compute_max_radii(centers)\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n# EVOLVE-BLOCK-END\n\n# fixed runner\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n# optional visualization helper (not required)\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(6,6))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect('equal')\n    ax.add_patch(plt.Rectangle((0,0),1,1,fill=False,linewidth=1))\n    for i,(c,rr) in enumerate(zip(centers, radii)):\n        ax.add_patch(Circle(c, rr, alpha=0.6, edgecolor='k'))\n        ax.text(c[0], c[1], str(i), fontsize=8, ha='center', va='center')\n    plt.title(f\"n={len(centers)}, sum radii={sum(radii):.6f}\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, s = run_packing()\n    print(f\"Sum of radii: {s:.6f}\")\n    # visualize(centers, radii)",
      "code_diff": "Full rewrite",
      "parent_metrics": {
        "validity": 1.0,
        "sum_radii": 1.8063385766498383,
        "target_ratio": 0.6855174863946256,
        "combined_score": 0.6855174863946256,
        "eval_time": 1.0880210399627686
      },
      "child_metrics": {
        "validity": 1.0,
        "sum_radii": 0.8472200605633073,
        "target_ratio": 0.3215256396824696,
        "combined_score": 0.3215256396824696
      }
    },
    "performance_regression:combined_score decreased by n.n% (n.n \u2192 n.n)": {
      "pattern_type": "performance_regression",
      "description": "combined_score decreased by 53.1% (0.686 \u2192 0.322)",
      "count": 1,
      "first_seen": 9,
      "last_seen": 9,
      "example_error": null,
      "parent_code": "# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (improved)\"\"\"\nimport numpy as np\nfrom math import sqrt\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square.\n    Returns: (centers, radii, sum_of_radii)\n    \"\"\"\n    n_target = 26\n\n    # Generate a hexagonal lattice of candidate centers filling the unit square\n    s = 0.12                      # lattice spacing (tunable)\n    vy = sqrt(3) / 2 * s\n    candidates = []\n    nrows = int(np.ceil(1.0 / vy)) + 1\n    ncols = int(np.ceil(1.0 / s)) + 1\n    for j in range(nrows):\n        y = j * vy\n        if y <= 0 or y >= 1: \n            continue\n        offset = (j % 2) * (s / 2.0)\n        for i in range(ncols):\n            x = i * s + offset\n            if x <= 0 or x >= 1:\n                continue\n            candidates.append((x, y))\n    candidates = np.array(candidates)\n\n    # Add four near-corner anchors to encourage large corner circles\n    corner_margin = 0.06\n    corner_points = np.array([\n        (corner_margin, corner_margin),\n        (1 - corner_margin, corner_margin),\n        (corner_margin, 1 - corner_margin),\n        (1 - corner_margin, 1 - corner_margin),\n    ])\n    # Merge and uniquify candidates\n    all_pts = np.vstack((candidates, corner_points))\n    # Remove duplicates (within tiny tol)\n    uniq = []\n    for p in all_pts:\n        if not any(np.linalg.norm(p - q) < 1e-6 for q in uniq):\n            uniq.append(p)\n    candidates = np.array(uniq)\n\n    # Greedy selection: pick n_target centers from candidates to maximize sum of radii\n    selected_idx = []\n    remaining = list(range(len(candidates)))\n\n    def compute_max_radii(centers):\n        m = centers.shape[0]\n        # Start with distance to walls\n        radii = np.minimum(np.minimum(centers[:,0], 1 - centers[:,0]),\n                           np.minimum(centers[:,1], 1 - centers[:,1]))\n        radii = np.clip(radii, 0.0, 1.0)\n\n        if m == 0:\n            return radii\n\n        # Iterative Gauss-Seidel relaxation to enforce r_i + r_j <= d_ij\n        coords = centers.copy()\n        tol = 1e-7\n        max_iter = 1000\n        for _ in range(max_iter):\n            max_change = 0.0\n            for i in range(m):\n                ri_old = radii[i]\n                # limit by walls already included in initial value\n                ri = ri_old\n                for j in range(m):\n                    if i == j:\n                        continue\n                    d = np.linalg.norm(coords[i] - coords[j])\n                    # If centers coincide, force tiny distance to avoid div by zero\n                    if d < 1e-8:\n                        d = 1e-8\n                    # r_i <= d - r_j\n                    bound = d - radii[j]\n                    if bound < ri:\n                        ri = max(0.0, bound)\n                # update\n                change = abs(ri - ri_old)\n                if change > max_change:\n                    max_change = change\n                radii[i] = ri\n            if max_change < tol:\n                break\n        return radii\n\n    # Greedy picking loop\n    while len(selected_idx) < n_target and remaining:\n        best_idx = None\n        best_sum = -1.0\n        # evaluate each candidate addition\n        for idx in remaining:\n            trial_indices = selected_idx + [idx]\n            centers_trial = candidates[trial_indices]\n            r_trial = compute_max_radii(centers_trial)\n            ssum = float(np.sum(r_trial))\n            if ssum > best_sum:\n                best_sum = ssum\n                best_idx = idx\n        if best_idx is None:\n            break\n        selected_idx.append(best_idx)\n        remaining.remove(best_idx)\n\n    # If greedy failed to pick enough (shouldn't), pad with nearest-to-center remaining\n    if len(selected_idx) < n_target:\n        remaining_sorted = sorted(remaining, key=lambda i: np.linalg.norm(candidates[i] - np.array([0.5,0.5])))\n        for idx in remaining_sorted:\n            if len(selected_idx) >= n_target:\n                break\n            selected_idx.append(idx)\n\n    centers = candidates[selected_idx][:n_target]\n    # Final radii computed and refined\n    radii = compute_max_radii(centers)\n\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n# EVOLVE-BLOCK-END\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, linewidth=0.5, edgecolor='k')\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)",
      "child_code": "# EVOLVE-BLOCK-START\n\"\"\"Explicit constructor for packing 26 circles in the unit square.\nDesigned to place centers in a mixed hexagonal + edge + corner pattern\nand compute maximal feasible radii by Gauss-Seidel relaxation.\n\"\"\"\nimport numpy as np\nfrom math import cos, sin, pi\n\ndef construct_packing():\n    n_target = 26\n\n    # 1) Centered hex-like cluster: 1 center + 6 inner + 7 outer = 14\n    cx, cy = 0.5, 0.5\n    centers = [(cx, cy)]\n    # inner ring (6)\n    r_inner = 0.12\n    for k in range(6):\n        a = k * 2 * pi / 6\n        centers.append((cx + r_inner * cos(a), cy + r_inner * sin(a)))\n    # outer 7 (a heptagonal-ish ring) to fill gaps toward edges\n    r_outer = 0.24\n    for k in range(7):\n        a = k * 2 * pi / 7 + 0.1  # slight rotation\n        centers.append((cx + r_outer * cos(a), cy + r_outer * sin(a)))\n\n    # 2) Edge mid positions: 2 per side (8)\n    edge_off = 0.06\n    edge_x = [0.33, 0.66]\n    edge_y = [0.33, 0.66]\n    # bottom and top\n    for x in edge_x:\n        centers.append((x, edge_off))\n        centers.append((x, 1.0 - edge_off))\n    # left and right\n    for y in edge_y:\n        centers.append((edge_off, y))\n        centers.append((1.0 - edge_off, y))\n\n    # 3) Four near-corner anchors (4)\n    cm = 0.06\n    centers.extend([\n        (cm, cm),\n        (1.0 - cm, cm),\n        (cm, 1.0 - cm),\n        (1.0 - cm, 1.0 - cm),\n    ])\n\n    # Ensure we have exactly n_target centers\n    centers = np.array(centers[:n_target])\n    # Clip tiny numerical excursions\n    centers = np.clip(centers, 1e-6, 1.0 - 1e-6)\n\n    # Compute maximal radii for fixed centers using Gauss-Seidel relaxation:\n    def compute_max_radii(pts):\n        m = pts.shape[0]\n        # initial radii limited by distance to walls\n        r = np.minimum.reduce((pts[:,0], 1 - pts[:,0], pts[:,1], 1 - pts[:,1])).copy()\n        r = np.maximum(r, 0.0)\n\n        if m == 0:\n            return r\n\n        # Precompute center-to-center distances (static)\n        D = np.linalg.norm(pts[None,:,:] - pts[:,None,:], axis=2)\n        # Guard diagonal\n        for i in range(m):\n            D[i,i] = 1e-12\n\n        tol = 1e-8\n        max_iter = 2000\n        for it in range(max_iter):\n            max_change = 0.0\n            # iterate Gauss-Seidel style\n            for i in range(m):\n                ri_old = r[i]\n                # start with wall limit (already set in r)\n                ri = min(ri_old, np.minimum.reduce((pts[i,0], 1 - pts[i,0], pts[i,1], 1 - pts[i,1])))\n                # enforce pairwise constraints r_i <= d_ij - r_j\n                # vectorized bound calculation\n                bounds = D[i] - r\n                bounds[i] = ri  # ignore self\n                # ri must be <= min_j bounds_j\n                ri_new = max(0.0, np.min(bounds))\n                # Relax update (take smaller change to improve stability)\n                # But use full update to reach maximal feasible radii\n                change = abs(ri_new - ri_old)\n                if change > max_change:\n                    max_change = change\n                r[i] = ri_new\n            if max_change < tol:\n                break\n        # final safeguard: clip non-negative\n        r = np.maximum(r, 0.0)\n        return r\n\n    radii = compute_max_radii(centers)\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n# EVOLVE-BLOCK-END\n\n# fixed runner\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n# optional visualization helper (not required)\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(6,6))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect('equal')\n    ax.add_patch(plt.Rectangle((0,0),1,1,fill=False,linewidth=1))\n    for i,(c,rr) in enumerate(zip(centers, radii)):\n        ax.add_patch(Circle(c, rr, alpha=0.6, edgecolor='k'))\n        ax.text(c[0], c[1], str(i), fontsize=8, ha='center', va='center')\n    plt.title(f\"n={len(centers)}, sum radii={sum(radii):.6f}\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, s = run_packing()\n    print(f\"Sum of radii: {s:.6f}\")\n    # visualize(centers, radii)",
      "code_diff": "Full rewrite",
      "parent_metrics": {
        "validity": 1.0,
        "sum_radii": 1.8063385766498383,
        "target_ratio": 0.6855174863946256,
        "combined_score": 0.6855174863946256,
        "eval_time": 1.0880210399627686
      },
      "child_metrics": {
        "validity": 1.0,
        "sum_radii": 0.8472200605633073,
        "target_ratio": 0.3215256396824696,
        "combined_score": 0.3215256396824696
      }
    },
    "performance_regression:eval_time decreased by n.n% (n.n \u2192 n.n)": {
      "pattern_type": "performance_regression",
      "description": "eval_time decreased by 55.0% (0.150 \u2192 0.068)",
      "count": 5,
      "first_seen": 13,
      "last_seen": 39,
      "example_error": null,
      "parent_code": "# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles using an explicit hex-lattice constructor\nand a small 1D search over lattice spacing. For each candidate spacing we take the 26\nhex-lattice points inside the unit square closest to the center, then compute maximal\nnon-overlapping radii (respecting the square boundary) by an iterative conflict resolution.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    center = np.array([0.5, 0.5])\n    best = None\n    best_sum = -1.0\n\n    # Candidate spacings (center-to-center). Wider range to explore edge vs dense packing.\n    s_vals = np.linspace(0.07, 0.28, 60)\n\n    for s in s_vals:\n        # generate hexagonal lattice (point-to-point distance = s)\n        dy = s * np.sqrt(3) / 2.0\n        # ranges large enough to cover unit square after centering\n        rng = range(-10, 11)\n        pts = []\n        for j in rng:\n            x_off = 0.5 if (j % 2 == 0) else 0.5 + s / 2.0\n            y_base = 0.5 + j * dy\n            for i in rng:\n                x = x_off + (i * s - 0.5)  # shift to create many points around center\n                y = y_base\n                # shift back by adding 0.5 to center, then we will filter\n                pts.append([x + 0.5, y])\n        pts = np.array(pts)\n\n        # select points inside the unit square\n        mask = (pts[:, 0] >= 0.0) & (pts[:, 0] <= 1.0) & (pts[:, 1] >= 0.0) & (pts[:, 1] <= 1.0)\n        pts_in = pts[mask]\n        if pts_in.shape[0] < n:\n            continue\n\n        # choose the n points closest to square center\n        dists = np.sum((pts_in - center) ** 2, axis=1)\n        order = np.argsort(dists)[:n]\n        centers = pts_in[order]\n\n        radii = compute_max_radii(centers)\n\n        ssum = radii.sum()\n        if ssum > best_sum:\n            best_sum = ssum\n            best = (centers.copy(), radii.copy(), ssum)\n\n    # Fallback: if none produced >0 (shouldn't), place a simple conservative pattern\n    if best is None:\n        # grid fallback 5x6 truncated to 26\n        xs = np.linspace(0.1, 0.9, 6)\n        ys = np.linspace(0.1, 0.9, 5)\n        grid = np.array([[x, y] for y in ys for x in xs])[:26]\n        radii = compute_max_radii(grid)\n        return grid, radii, radii.sum()\n\n    return best\n\ndef compute_max_radii(centers):\n    \"\"\"\n    Given centers (m x 2), compute a feasible set of radii maximizing each until conflicts\n    are found. Algorithm:\n      - initialize each radius to its distance to the nearest boundary\n      - iterate pairwise: if r_i + r_j > d_ij, reduce the larger of the two to d_ij - smaller\n      - repeat until no violation or max iterations reached\n    This greedy conflict resolution tends to preserve many moderate circles and yields\n    a high total radius sum for lattice-like arrangements.\n    \"\"\"\n    centers = np.asarray(centers)\n    m = centers.shape[0]\n    # initial radii limited by borders\n    radii = np.minimum.reduce([centers[:, 0], centers[:, 1], 1.0 - centers[:, 0], 1.0 - centers[:, 1]]).astype(float)\n    if m == 0:\n        return radii\n\n    # Protect extremely small negatives due to floating issues\n    radii = np.maximum(radii, 0.0)\n\n    max_iter = 500\n    eps = 1e-10\n    for _ in range(max_iter):\n        changed = False\n        # randomize pair order a bit to avoid deterministic bias\n        idxs = np.arange(m)\n        np.random.shuffle(idxs)\n        for ii in idxs:\n            for jj in range(ii + 1, m):\n                i = ii\n                j = jj\n                d = np.hypot(*(centers[i] - centers[j]))\n                if d <= 0:\n                    # coincident centers (shouldn't happen) -> collapse one\n                    if radii[j] > radii[i]:\n                        radii[j] = 0.0\n                        changed = True\n                    else:\n                        radii[i] = 0.0\n                        changed = True\n                    continue\n                if radii[i] + radii[j] > d + eps:\n                    # reduce the larger radius to fit exactly\n                    if radii[i] > radii[j]:\n                        newri = max(0.0, d - radii[j])\n                        if newri + eps < radii[i]:\n                            radii[i] = newri\n                            changed = True\n                    else:\n                        newrj = max(0.0, d - radii[i])\n                        if newrj + eps < radii[j]:\n                            radii[j] = newrj\n                            changed = True\n        if not changed:\n            break\n\n    # Final safety clamp to borders and pairwise\n    # enforce border\n    radii = np.minimum(radii, np.minimum.reduce([centers[:, 0], centers[:, 1], 1.0 - centers[:, 0], 1.0 - centers[:, 1]]))\n    # enforce pairwise once more symmetrically by halving overshoot\n    for i in range(m):\n        for j in range(i + 1, m):\n            d = np.hypot(*(centers[i] - centers[j]))\n            if radii[i] + radii[j] > d:\n                excess = radii[i] + radii[j] - d\n                # reduce both proportionally to preserve relative sizes\n                total = radii[i] + radii[j]\n                if total > 0:\n                    radii[i] -= excess * (radii[i] / total)\n                    radii[j] -= excess * (radii[j] / total)\n                else:\n                    radii[i] = radii[j] = 0.0\n    radii = np.maximum(radii, 0.0)\n    return radii\n\n# EVOLVE-BLOCK-END\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor='k', linewidth=0.3)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)",
      "child_code": "# EVOLVE-BLOCK-START\n\"\"\"Explicit constructor for n=26 circle packing in the unit square.\nBuilds a hex-like row pattern (rows = [5,6,5,5,5]) and searches a small range\nof horizontal spacings to maximize the sum of radii. Radii are computed by a\nrobust Gauss-Seidel projection that enforces r_i <= border and r_i + r_j <= d_ij.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    rows = [5, 6, 5, 5, 5]  # total 26\n    max_cols = max(rows)\n    best = None\n    best_sum = -1.0\n\n    # allowable horizontal spacing range so widest row can fit\n    margin = 1e-6\n    s_max = (1.0 - 2 * margin) / (max_cols - 1)\n    s_min = 0.10\n    s_vals = np.linspace(max(s_min, 0.08), min(s_max, 0.26), 60)\n\n    for s in s_vals:\n        h = np.sqrt(3.0) / 2.0 * s\n        # vertical centering\n        R = len(rows)\n        block_height = (R - 1) * h\n        y0 = 0.5 - block_height / 2.0\n        centers = []\n        for r_idx, k in enumerate(rows):\n            y = y0 + r_idx * h\n            row_width = (k - 1) * s\n            x_start = 0.5 - row_width / 2.0\n            # offset alternate rows for hex adjacency\n            if r_idx % 2 == 1:\n                x_start += s / 2.0\n            for j in range(k):\n                x = x_start + j * s\n                centers.append([x, y])\n        centers = np.array(centers[:n], dtype=float)\n        # discard spacings that place centers outside the square by a visible margin\n        if np.any(centers < -1e-8) or np.any(centers > 1 + 1e-8):\n            continue\n        centers = np.clip(centers, margin, 1.0 - margin)\n        radii = compute_max_radii(centers)\n        ssum = radii.sum()\n        if ssum > best_sum:\n            best_sum = ssum\n            best = (centers.copy(), radii.copy(), float(ssum))\n\n    # fallback (should not happen) - simple centered pattern\n    if best is None:\n        s = min(s_max, 0.14)\n        h = np.sqrt(3.0) / 2.0 * s\n        R = len(rows)\n        block_height = (R - 1) * h\n        y0 = 0.5 - block_height / 2.0\n        centers = []\n        for r_idx, k in enumerate(rows):\n            y = y0 + r_idx * h\n            row_width = (k - 1) * s\n            x_start = 0.5 - row_width / 2.0\n            if r_idx % 2 == 1:\n                x_start += s / 2.0\n            for j in range(k):\n                x = x_start + j * s\n                centers.append([x, y])\n        centers = np.clip(np.array(centers[:n]), margin, 1 - margin)\n        radii = compute_max_radii(centers)\n        best = (centers, radii, float(radii.sum()))\n\n    return best\n\ndef compute_max_radii(centers, tol=1e-9, max_iter=5000):\n    \"\"\"\n    Gauss-Seidel projection to the feasible polytope:\n      r_i <= border_i\n      r_i + r_j <= d_ij\n    The update sets r_i = min(border_i, min_j d_ij - r_j) in-place until convergence.\n    \"\"\"\n    centers = np.asarray(centers)\n    m = centers.shape[0]\n    if m == 0:\n        return np.array([], dtype=float)\n\n    # distance to borders\n    border = np.minimum.reduce([centers[:, 0], centers[:, 1], 1.0 - centers[:, 0], 1.0 - centers[:, 1]])\n    border = np.maximum(border, 0.0)\n\n    # initial radii: half of border (conservative start)\n    radii = 0.5 * border.copy()\n    radii = np.maximum(radii, 1e-8)\n\n    # pairwise distances\n    diff = centers[:, None, :] - centers[None, :, :]\n    dists = np.sqrt(np.sum(diff ** 2, axis=2))\n    np.fill_diagonal(dists, np.inf)\n\n    for it in range(max_iter):\n        max_change = 0.0\n        # Gauss-Seidel: update sequentially, using newest values\n        for i in range(m):\n            # r_i limited by border\n            allowed = border[i]\n            # neighbor constraints: r_i <= d_ij - r_j\n            neigh_allowed = dists[i, :] - radii\n            # take the minimum over neighbors\n            min_neigh = np.min(neigh_allowed)\n            if min_neigh < allowed:\n                allowed = min_neigh\n            if allowed < 0.0:\n                allowed = 0.0\n            change = abs(radii[i] - allowed)\n            if change > max_change:\n                max_change = change\n            radii[i] = allowed\n        if max_change < tol:\n            break\n\n    # small final pass to symmetrize any remaining tiny violations\n    for i in range(m):\n        for j in range(i + 1, m):\n            d = dists[i, j]\n            if not np.isfinite(d):\n                continue\n            if radii[i] + radii[j] > d:\n                excess = radii[i] + radii[j] - d\n                # reduce both proportionally (preserve relative sizes)\n                total = radii[i] + radii[j]\n                if total > 0:\n                    radii[i] -= excess * (radii[i] / total)\n                    radii[j] -= excess * (radii[j] / total)\n                else:\n                    radii[i] = radii[j] = 0.0\n    radii = np.maximum(radii, 0.0)\n    return radii\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor='k', linewidth=0.3)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)",
      "code_diff": "Full rewrite",
      "parent_metrics": {
        "validity": 1.0,
        "sum_radii": 1.6675410668487298,
        "target_ratio": 0.6328429096200113,
        "combined_score": 0.6328429096200113,
        "eval_time": 0.15033912658691406
      },
      "child_metrics": {
        "validity": 1.0,
        "sum_radii": 1.7297305505174605,
        "target_ratio": 0.656444231695431,
        "combined_score": 0.656444231695431,
        "eval_time": 0.06766986846923828
      }
    }
  },
  "success_patterns": {},
  "iteration_history": [
    1,
    2,
    4,
    3,
    6,
    5,
    8,
    9,
    7,
    10,
    11,
    13,
    12,
    14,
    17,
    15,
    16,
    18,
    20,
    19,
    21,
    22,
    24,
    23,
    27,
    26,
    28,
    29,
    30,
    31,
    34,
    33,
    35,
    32,
    36,
    38,
    39,
    40
  ],
  "last_update_iteration": 40
}