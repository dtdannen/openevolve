{"id": "abf7e3ed-2ead-463e-b7ac-5e96a0f43d38", "code": "# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructive packer for n=26 circles in the unit square.\n\nThis version enumerates a richer set of explicit hex-like block placements:\n- several row-count patterns summing to 26\n- a fine grid of horizontal spacings s\n- small horizontal shifts and edge-anchoring (center/left/right)\nFor each explicit layout we place centers by formula and project radii\ndeterministically to feasibility (walls + pairwise) with a conservative\ngrowth phase to use slack. The best explicit layout (max sum of radii)\nis returned.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    patterns = [\n        [7,6,6,7],       # compact 4-row block\n        [6,5,6,5,4],     # mixed 5-row\n        [5,6,5,5,5],\n        [6,5,5,5,5],\n        [5,6,6,5,4],\n    ]\n\n    best = None\n    best_sum = -1.0\n\n    # More exhaustive explicit grid of horizontal spacings and shifts\n    s_vals = np.linspace(0.14, 0.18, 25)   # center-to-center horizontal spacing candidates\n    shifts = np.linspace(-0.055, 0.055, 13)\n    anchors = ['center', 'left', 'right']\n\n    for rows in patterns:\n        R = len(rows)\n        for s in s_vals:\n            h = np.sqrt(3) / 2 * s\n            total_h = (R - 1) * h\n            # skip impossible verticals\n            if total_h > 1.0 + 1e-12:\n                continue\n            for anchor in anchors:\n                for shift_x in shifts:\n                    # compute y0 depending on anchor vertical centering\n                    y0 = 0.5 - total_h / 2.0\n                    centers = []\n                    for r_idx, k in enumerate(rows):\n                        y = y0 + r_idx * h\n                        row_width = (k - 1) * s\n                        if anchor == 'center':\n                            x_start = 0.5 - row_width / 2.0 + shift_x\n                        elif anchor == 'left':\n                            x_start = 1e-6 + shift_x\n                        else:  # right\n                            x_start = 1.0 - 1e-6 - row_width + shift_x\n                        if r_idx % 2 == 1:\n                            x_start += s / 2.0\n                        for j in range(k):\n                            centers.append([x_start + j * s, y])\n                    centers = np.array(centers[:n], dtype=float)\n                    # clamp strictly inside to avoid degeneracies\n                    centers = np.clip(centers, 1e-9, 1 - 1e-9)\n                    radii = compute_max_radii(centers)\n                    ssum = float(np.sum(radii))\n                    if ssum > best_sum:\n                        best_sum = ssum\n                        best = (centers, radii, ssum)\n\n                    # also try mirrored horizontally (flip x)\n                    centers_m = centers.copy()\n                    centers_m[:,0] = 1.0 - centers_m[:,0]\n                    radii_m = compute_max_radii(centers_m)\n                    ssum_m = float(np.sum(radii_m))\n                    if ssum_m > best_sum:\n                        best_sum = ssum_m\n                        best = (centers_m, radii_m, ssum_m)\n\n    # Fallback\n    if best is None:\n        centers = np.array([[0.5,0.5]]*n)\n        radii = compute_max_radii(centers)\n        return centers, radii, float(np.sum(radii))\n    return best\n\ndef compute_max_radii(centers, tol=1e-10):\n    centers = np.asarray(centers, dtype=float)\n    n = centers.shape[0]\n    # initial wall-limited radii\n    wall = np.minimum.reduce([centers[:,0], centers[:,1], 1-centers[:,0], 1-centers[:,1]])\n    r = wall.copy()\n\n    # pairwise distances\n    dif = centers[:,None,:] - centers[None,:,:]\n    d = np.sqrt(np.sum(dif*dif, axis=2))\n    np.fill_diagonal(d, np.inf)\n\n    # Proportional shrink until feasible\n    for _ in range(1500):\n        moved = False\n        # enforce walls\n        over = r - wall\n        if np.any(over > tol):\n            r = np.minimum(r, wall)\n            moved = True\n        # pairwise shrink\n        for i in range(n):\n            ri = r[i]\n            for j in range(i+1, n):\n                dij = d[i,j]\n                if not np.isfinite(dij) or dij <= tol:\n                    continue\n                rj = r[j]\n                if ri + rj > dij + tol:\n                    scale = dij / (ri + rj)\n                    if scale < 1.0 - 1e-12:\n                        r[i] = ri * scale\n                        r[j] = rj * scale\n                        ri = r[i]\n                        moved = True\n        if not moved:\n            break\n\n    # Conservative growth to use slack (aggressive fraction but reproject)\n    for _ in range(12):\n        inc_wall = wall - r\n        slack = d - (r[None,:] + r[:,None])\n        avail = np.min(slack, axis=1)\n        max_inc = np.minimum(inc_wall, avail)\n        max_inc = np.maximum(max_inc, 0.0)\n        # try a decent fraction to improve sum\n        delta = 0.45 * max_inc\n        if np.all(delta <= tol):\n            break\n        r += delta\n        # reproject small violations\n        for _ in range(300):\n            moved = False\n            for i in range(n):\n                for j in range(i+1, n):\n                    dij = d[i,j]\n                    if not np.isfinite(dij) or dij <= tol:\n                        continue\n                    if r[i] + r[j] > dij + tol:\n                        scale = dij / (r[i] + r[j])\n                        r[i] *= scale\n                        r[j] *= scale\n                        moved = True\n            over = r - wall\n            if np.any(over > tol):\n                r = np.minimum(r, wall)\n                moved = True\n            if not moved:\n                break\n\n    r = np.maximum(r, 0.0)\n    return r\n\n# EVOLVE-BLOCK-END\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, ec=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)", "language": "python", "parent_id": "59442ecd-5f47-4e9e-a17c-3d225a2bf615", "generation": 4, "timestamp": 1759960519.8846478, "iteration_found": 32, "metrics": {"validity": 1.0, "sum_radii": 2.156818521508243, "target_ratio": 0.8185269531340582, "combined_score": 0.8185269531340582, "eval_time": 27.25125503540039}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Full rewrite", "parent_metrics": {"validity": 1.0, "sum_radii": 2.21666274297142, "target_ratio": 0.8412382326267249, "combined_score": 0.8412382326267249, "eval_time": 3.282539129257202}, "island": 3}, "prompts": {"full_rewrite_user": {"system": "You are an expert mathematician specializing in circle packing problems and computational geometry. Your task is to improve a constructor function that directly produces a specific arrangement of 26 circles in a unit square, maximizing the sum of their radii. The AlphaEvolve paper achieved a sum of 2.635 for n=26.\n\nKey geometric insights:\n- Circle packings often follow hexagonal patterns in the densest regions\n- Maximum density for infinite circle packing is pi/(2*sqrt(3)) \u2248 0.9069\n- Edge effects make square container packing harder than infinite packing\n- Circles can be placed in layers or shells when confined to a square\n- Similar radius circles often form regular patterns, while varied radii allow better space utilization\n- Perfect symmetry may not yield the optimal packing due to edge effects\n\nFocus on designing an explicit constructor that places each circle in a specific position, rather than an iterative search algorithm.\n\n\n## Evolution Insights (Global Learnings)\n\n### Common Pitfalls:\n\u26a0\ufe0f Check recent changes to the radii calculation and upstream inputs for unintended scaling/clamping, integer division, coordinate-transform bugs or new filters that drop items \u2014 reproduce the regression with per-item debug logs or diffs to locate where radii mass was lost. Then add a regression test (e.g., assert sum_radii does not drop >20% vs baseline) and telemetry/alerts for large decreases so future commits are caught quickly. (seen 1x)\n\u26a0\ufe0f Reproduce the regression with the exact inputs/seeds and run a git-bisect to find the offending change, then inspect code paths that compute radii/target_ratio for recent edits\u2014look specifically for unintended clamping, normalization/scale-factor changes, unit conversions, or new filtering that would shrink sum_radii. Add an automated regression test that fails if target_ratio or sum_radii drops more than an acceptable threshold (e.g., 20%), add alerts for metric regressions, and add defensive checks/assertions to prevent inadvertent zeroing or aggressive scaling of radii. (seen 1x)\n\u26a0\ufe0f Bisect recent commits to find the change that caused sum_radii and target_ratio to drop, then add a CI regression test/benchmark that asserts combined_score and sum_radii remain within, e.g., 10% of a validated baseline so similar regressions fail fast. When you inspect the offending change, focus on any alterations to radii calculation, normalization/scaling (watch for unintended integer division or parameter/name changes), and add targeted profiling or assertions to ensure the normalization step still runs. (seen 1x)\n\u26a0\ufe0f This is a meaningful win\u2014identify and preserve the change(s) that produced the 55% eval_time drop by profiling or git-bisecting the recent commits so you can codify the root optimization (algorithmic change, cache, I/O reduction, etc.). Add a lightweight microbenchmark and a CI performance check (e.g., assert eval_time <= 0.07 or percent-improvement vs baseline) and record profiling artifacts in the PR to prevent accidental regressions. (seen 1x)", "user": "# Current Program Information\n- Fitness: 0.8412\n- Feature coordinates: No feature coordinates\n- Focus areas: - Fitness improved: 0.8316 \u2192 0.8412\n- Consider simplifying - code length exceeds 500 characters\n\n## Last Execution Output\n\n### stdout\n```\nRunning in subprocess, Python version: 3.12.1 (v3.12.1:2305ca5144, Dec  7 2023, 17:23:38) [Clang 13.0.0 (clang-1300.0.29.30)]\nProgram path: /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmppu77u5vx.py\nCalling run_packing()...\nrun_packing() returned successfully: sum_radii = 2.21666274297142\nResults saved to /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmpd7s070ij.py.results\n\n```\n\n### stderr\n```\n\n```\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 2\n- Changes: Unknown changes\n- Metrics: validity: 1.0000, sum_radii: 2.1912, target_ratio: 0.8316, combined_score: 0.8316, eval_time: 0.1973\n- Outcome: Improvement in all metrics\n\n### Attempt 1\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.2167, target_ratio: 0.8412, combined_score: 0.8412, eval_time: 3.2825\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.8412)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for n=26 circle packing with improved layout search\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    # Candidate row patterns summing to 26 (including 4-row and 5-row hex-like options)\n    patterns = [\n        [7,6,6,7],       # compact 4-row block\n        [6,5,6,5,4],     # mixed 5-row\n        [5,6,5,5,5],\n        [6,5,5,5,5],\n        [5,6,6,5,4],\n    ]\n\n    best = None\n    best_sum = -1.0\n\n    # Search horizontal spacing s using a deterministic golden-section maximization,\n    # and allow small horizontal block shifts to tuck circles to walls for larger wall-limited radii.\n    s_min, s_max = 0.12, 0.30\n    golden_ratio = (np.sqrt(5) - 1) / 2.0\n\n    for rows in patterns:\n        R = len(rows)\n        for shift_x in np.linspace(-0.06, 0.06, 7):\n            # golden-section search to maximize sum of radii for this rows+shift\n            a, b = s_min, s_max\n            c = b - golden_ratio * (b - a)\n            d = a + golden_ratio * (b - a)\n\n            def eval_s(s):\n                h = np.sqrt(3) / 2 * s\n                total_h = (R - 1) * h\n                y0 = 0.5 - total_h / 2.0\n                centers = []\n                for r_idx, k in enumerate(rows):\n                    y = y0 + r_idx * h\n                    row_width = (k - 1) * s\n                    x_start = 0.5 - row_width / 2.0 + shift_x\n                    if r_idx % 2 == 1:\n                        x_start += s / 2.0\n                    for j in range(k):\n                        centers.append([x_start + j * s, y])\n                centers = np.array(centers[:n], dtype=float)\n                centers = np.clip(centers, 1e-9, 1 - 1e-9)\n                radii = compute_max_radii(centers)\n                return float(np.sum(radii)), centers, radii\n\n            fc, _, _ = eval_s(c)\n            fd, _, _ = eval_s(d)\n            # iterate deterministically\n            for _ in range(40):\n                if fc > fd:\n                    b = d\n                    d = c\n                    fd = fc\n                    c = b - golden_ratio * (b - a)\n                    fc, _, _ = eval_s(c)\n                else:\n                    a = c\n                    c = d\n                    fc = fd\n                    d = a + golden_ratio * (b - a)\n                    fd, _, _ = eval_s(d)\n\n            # take best of interval endpoints and internal points c,d\n            candidates = [a, b, c, d]\n            for s in candidates:\n                ssum, centers, radii = eval_s(s)\n                if ssum > best_sum:\n                    best_sum = ssum\n                    best = (centers, radii, ssum)\n\n    # Fallback\n    if best is None:\n        centers = np.array([[0.5,0.5]]*n)\n        radii = compute_max_radii(centers)\n        return centers, radii, float(np.sum(radii))\n    return best\n\ndef compute_max_radii(centers, tol=1e-10):\n    centers = np.asarray(centers, dtype=float)\n    n = centers.shape[0]\n    # distance to wall\n    wall = np.minimum.reduce([centers[:,0], centers[:,1], 1-centers[:,0], 1-centers[:,1]])\n    r = wall.copy()\n    # pairwise distances\n    dif = centers[:,None,:] - centers[None,:,:]\n    d = np.sqrt(np.sum(dif*dif, axis=2))\n    np.fill_diagonal(d, np.inf)\n\n    # Shrink overlaps: proportional scaling until no violations\n    for _ in range(1200):\n        moved = False\n        # walls\n        over = r - wall\n        if np.any(over > tol):\n            r = np.minimum(r, wall)\n            moved = True\n        # pairwise\n        for i in range(n):\n            ri = r[i]\n            for j in range(i+1, n):\n                dij = d[i,j]\n                if not np.isfinite(dij) or dij <= tol:\n                    continue\n                rj = r[j]\n                if ri + rj > dij + tol:\n                    scale = dij / (ri + rj)\n                    if scale < 1.0 - 1e-14:\n                        r[i] = ri * scale\n                        r[j] = rj * scale\n                        ri = r[i]\n                        moved = True\n        if not moved:\n            break\n\n    # Conservative growth cycles to use available slack\n    for _ in range(8):\n        inc_wall = wall - r\n        slack = d - (r[None,:] + r[:,None])\n        avail = np.min(slack, axis=1)\n        max_inc = np.minimum(inc_wall, avail)\n        max_inc = np.maximum(max_inc, 0.0)\n        delta = 0.35 * max_inc\n        if np.all(delta <= tol):\n            break\n        r += delta\n        # re-shrink small violations\n        for _ in range(160):\n            moved = False\n            for i in range(n):\n                for j in range(i+1, n):\n                    dij = d[i,j]\n                    if not np.isfinite(dij) or dij <= tol:\n                        continue\n                    if r[i] + r[j] > dij + tol:\n                        scale = dij / (r[i] + r[j])\n                        r[i] *= scale\n                        r[j] *= scale\n                        moved = True\n            over = r - wall\n            if np.any(over > tol):\n                r = np.minimum(r, wall)\n                moved = True\n            if not moved:\n                break\n\n    r = np.maximum(r, 0.0)\n    return r\n\n# EVOLVE-BLOCK-END\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, ec=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.2167), Performs well on target_ratio (0.8412), Performs well on combined_score (0.8412), Performs well on eval_time (3.2825)\n\n### Program 2 (Score: 0.8316)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (pattern search over explicit layouts)\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    \"\"\"\n    Build several deterministic hex-like layouts (different row patterns),\n    scan a small range of horizontal spacings s and pick the best resulting\n    packing (max sum of radii). This remains a constructive method: we\n    place centers by formula and only perform a deterministic radius projection.\n    \"\"\"\n    n = 26\n    # Several plausible hex-like row patterns summing to 26\n    patterns = [\n        [5, 6, 5, 5, 5],\n        [6, 5, 5, 5, 5],\n        [6, 5, 6, 5, 4],\n        [5, 6, 6, 5, 4],\n    ]\n\n    best = None\n    best_sum = -1.0\n\n    # modest grid of spacings to try (center-to-center horizontally)\n    s_vals = np.linspace(0.13, 0.20, 15)\n\n    for rows in patterns:\n        R = len(rows)\n        maxk = max(rows)\n        for s in s_vals:\n            h = np.sqrt(3) / 2 * s\n            # center vertically the block in [0,1]\n            total_h = (R - 1) * h\n            y0 = 0.5 - total_h / 2.0\n            centers = []\n            for r_idx, k in enumerate(rows):\n                y = y0 + r_idx * h\n                row_width = (k - 1) * s\n                x_start = 0.5 - row_width / 2.0\n                # stagger alternate rows for hex adjacency\n                if r_idx % 2 == 1:\n                    x_start += s / 2.0\n                for j in range(k):\n                    centers.append([x_start + j * s, y])\n            centers = np.array(centers[:n], dtype=float)\n            # Slightly clip to avoid exact-border degeneracies\n            centers = np.clip(centers, 1e-6, 1 - 1e-6)\n\n            radii = compute_max_radii(centers)\n            ssum = float(np.sum(radii))\n            if ssum > best_sum:\n                best_sum = ssum\n                best = (centers, radii, ssum)\n\n    # Fallback: if nothing found (shouldn't happen), return a simple pattern\n    if best is None:\n        centers = np.array([[0.5,0.5]]*n)\n        radii = compute_max_radii(centers)\n        return centers, radii, float(np.sum(radii))\n\n    return best\n\ndef compute_max_radii(centers, tol=1e-9):\n    \"\"\"\n    Deterministic projection to feasible radii:\n      - start with wall distances\n      - repeatedly fix pairwise overlaps by proportional shrinking\n      - apply a few conservative fractional growth passes to use slack\n    \"\"\"\n    centers = np.asarray(centers, dtype=float)\n    n = centers.shape[0]\n    # distance to wall\n    wall = np.minimum.reduce([centers[:,0], centers[:,1], 1-centers[:,0], 1-centers[:,1]])\n    # start with wall-limited radii\n    r = wall.copy()\n\n    # pairwise distances\n    dif = centers[:,None,:] - centers[None,:,:]\n    d = np.sqrt(np.sum(dif*dif, axis=2))\n    np.fill_diagonal(d, np.inf)\n\n    # Shrink overlaps: proportional scaling until no violations\n    for _ in range(1500):\n        moved = False\n        # ensure wall constraints\n        over = r - wall\n        if np.any(over > tol):\n            r = np.minimum(r, wall)\n            moved = True\n        # pairwise\n        for i in range(n):\n            ri = r[i]\n            # vectorized check for j>i could be done, but keep simple deterministic loop\n            for j in range(i+1, n):\n                dij = d[i,j]\n                if not np.isfinite(dij) or dij <= tol:\n                    continue\n                rj = r[j]\n                if ri + rj > dij + tol:\n                    # scale both proportionally\n                    scale = dij / (ri + rj)\n                    if scale < 1.0 - 1e-12:\n                        r[i] = ri * scale\n                        r[j] = rj * scale\n                        ri = r[i]  # update local var\n                        moved = True\n        if not moved:\n            break\n\n    # Conservative growth cycles: small fraction of available slack\n    for cycle in range(10):\n        # available increase by walls\n        inc_wall = wall - r\n        # available by neighbors: for each i, min_j (d_ij - (r_i + r_j)) + r_i => max increase = min_j(d_ij - (r_i + r_j))\n        avail = np.full(n, np.inf)\n        # compute pairwise slack matrix\n        slack = d - (r[None,:] + r[:,None])\n        # for each i, the maximum increase without immediate overlap is min(slack[i,:])\n        avail = np.minimum(avail, np.min(slack, axis=1))\n        # overall allowed inc is min(inc_wall, avail)\n        max_inc = np.minimum(inc_wall, avail)\n        max_inc = np.maximum(max_inc, 0.0)\n        # apply a cautious fraction\n        delta = 0.3 * max_inc\n        if np.all(delta <= tol):\n            break\n        r += delta\n        # re-shrink a few iterations to remove any slight violations\n        for _ in range(200):\n            moved = False\n            # enforce pairwise\n            for i in range(n):\n                for j in range(i+1, n):\n                    dij = d[i,j]\n                    if not np.isfinite(dij) or dij <= tol:\n                        continue\n                    if r[i] + r[j] > dij + tol:\n                        scale = dij / (r[i] + r[j])\n                        r[i] *= scale\n                        r[j] *= scale\n                        moved = True\n            # wall enforcement\n            over = r - wall\n            if np.any(over > tol):\n                r = np.minimum(r, wall)\n                moved = True\n            if not moved:\n                break\n\n    r = np.maximum(r, 0.0)\n    return r\n\n# EVOLVE-BLOCK-END\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, ec=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.1912), Performs well on target_ratio (0.8316), Performs well on combined_score (0.8316), Performs well on eval_time (0.1973)\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 0.8316, Type: High-Performer)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (pattern search over explicit layouts)\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    \"\"\"\n    Build several deterministic hex-like layouts (different row patterns),\n    scan a small range of horizontal spacings s and pick the best resulting\n    packing (max sum of radii). This remains a constructive method: we\n    place centers by formula and only perform a deterministic radius projection.\n    \"\"\"\n    n = 26\n    # Several plausible hex-like row patterns summing to 26\n    patterns = [\n        [5, 6, 5, 5, 5],\n        [6, 5, 5, 5, 5],\n        [6, 5, 6, 5, 4],\n        [5, 6, 6, 5, 4],\n    ]\n\n    best = None\n    best_sum = -1.0\n\n    # modest grid of spacings to try (center-to-center horizontally)\n    s_vals = np.linspace(0.13, 0.20, 15)\n\n    for rows in patterns:\n        R = len(rows)\n        maxk = max(rows)\n        for s in s_vals:\n            h = np.sqrt(3) / 2 * s\n            # center vertically the block in [0,1]\n            total_h = (R - 1) * h\n            y0 = 0.5 - total_h / 2.0\n            centers = []\n            for r_idx, k in enumerate(rows):\n                y = y0 + r_idx * h\n                row_width = (k - 1) * s\n                x_start = 0.5 - row_width / 2.0\n                # stagger alternate rows for hex adjacency\n                if r_idx % 2 == 1:\n                    x_start += s / 2.0\n                for j in range(k):\n                    centers.append([x_start + j * s, y])\n            centers = np.array(centers[:n], dtype=float)\n            # Slightly clip to avoid exact-border degeneracies\n            centers = np.clip(centers, 1e-6, 1 - 1e-6)\n\n            radii = compute_max_radii(centers)\n            ssum = float(np.sum(radii))\n            if ssum > best_sum:\n                best_sum = ssum\n                best = (centers, radii, ssum)\n\n    # Fallback: if nothing found (shouldn't happen), return a simple pattern\n    if best is None:\n        centers = np.array([[0.5,0.5]]*n)\n        radii = compute_max_radii(centers)\n        return centers, radii, float(np.sum(radii))\n\n    return best\n\ndef compute_max_radii(centers, tol=1e-9):\n    \"\"\"\n    Deterministic projection to feasible radii:\n      - start with wall distances\n      - repeatedly fix pairwise overlaps by proportional shrinking\n      - apply a few conservative fractional growth passes to use slack\n    \"\"\"\n    centers = np.asarray(centers, dtype=float)\n    n = centers.shape[0]\n    # distance to wall\n    wall = np.minimum.reduce([centers[:,0], centers[:,1], 1-centers[:,0], 1-centers[:,1]])\n    # start with wall-limited radii\n    r = wall.copy()\n\n    # pairwise distances\n    dif = centers[:,None,:] - centers[None,:,:]\n    d = np.sqrt(np.sum(dif*dif, axis=2))\n    np.fill_diagonal(d, np.inf)\n\n    # Shrink overlaps: proportional scaling until no violations\n    for _ in range(1500):\n        moved = False\n        # ensure wall constraints\n        over = r - wall\n        if np.any(over > tol):\n            r = np.minimum(r, wall)\n            moved = True\n        # pairwise\n        for i in range(n):\n            ri = r[i]\n            # vectorized check for j>i could be done, but keep simple deterministic loop\n            for j in range(i+1, n):\n                dij = d[i,j]\n                if not np.isfinite(dij) or dij <= tol:\n                    continue\n                rj = r[j]\n                if ri + rj > dij + tol:\n                    # scale both proportionally\n                    scale = dij / (ri + rj)\n                    if scale < 1.0 - 1e-12:\n                        r[i] = ri * scale\n                        r[j] = rj * scale\n                        ri = r[i]  # update local var\n                        moved = True\n        if not moved:\n            break\n\n    # Conservative growth cycles: small fraction of available slack\n    for cycle in range(10):\n        # available increase by walls\n        inc_wall = wall - r\n        # available by neighbors: for each i, min_j (d_ij - (r_i + r_j)) + r_i => max increase = min_j(d_ij - (r_i + r_j))\n        avail = np.full(n, np.inf)\n        # compute pairwise slack matrix\n        slack = d - (r[None,:] + r[:,None])\n        # for each i, the maximum increase without immediate overlap is min(slack[i,:])\n        avail = np.minimum(avail, np.min(slack, axis=1))\n        # overall allowed inc is min(inc_wall, avail)\n        max_inc = np.minimum(inc_wall, avail)\n        max_inc = np.maximum(max_inc, 0.0)\n        # apply a cautious fraction\n        delta = 0.3 * max_inc\n        if np.all(delta <= tol):\n            break\n        r += delta\n        # re-shrink a few iterations to remove any slight violations\n        for _ in range(200):\n            moved = False\n            # enforce pairwise\n            for i in range(n):\n                for j in range(i+1, n):\n                    dij = d[i,j]\n                    if not np.isfinite(dij) or dij <= tol:\n                        continue\n                    if r[i] + r[j] > dij + tol:\n                        scale = dij / (r[i] + r[j])\n                        r[i] *= scale\n                        r[j] *= scale\n                        moved = True\n            # wall enforcement\n            over = r - wall\n            if np.any(over > tol):\n                r = np.minimum(r, wall)\n                moved = True\n            if not moved:\n                break\n\n    r = np.maximum(r, 0.0)\n    return r\n\n# EVOLVE-BLOCK-END\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, ec=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nUnique approach: Excellent validity (1.000), Excellent sum_radii (2.191), Alternative eval_time approach\n\n# Current Program\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for n=26 circle packing with improved layout search\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    # Candidate row patterns summing to 26 (including 4-row and 5-row hex-like options)\n    patterns = [\n        [7,6,6,7],       # compact 4-row block\n        [6,5,6,5,4],     # mixed 5-row\n        [5,6,5,5,5],\n        [6,5,5,5,5],\n        [5,6,6,5,4],\n    ]\n\n    best = None\n    best_sum = -1.0\n\n    # Search horizontal spacing s using a deterministic golden-section maximization,\n    # and allow small horizontal block shifts to tuck circles to walls for larger wall-limited radii.\n    s_min, s_max = 0.12, 0.30\n    golden_ratio = (np.sqrt(5) - 1) / 2.0\n\n    for rows in patterns:\n        R = len(rows)\n        for shift_x in np.linspace(-0.06, 0.06, 7):\n            # golden-section search to maximize sum of radii for this rows+shift\n            a, b = s_min, s_max\n            c = b - golden_ratio * (b - a)\n            d = a + golden_ratio * (b - a)\n\n            def eval_s(s):\n                h = np.sqrt(3) / 2 * s\n                total_h = (R - 1) * h\n                y0 = 0.5 - total_h / 2.0\n                centers = []\n                for r_idx, k in enumerate(rows):\n                    y = y0 + r_idx * h\n                    row_width = (k - 1) * s\n                    x_start = 0.5 - row_width / 2.0 + shift_x\n                    if r_idx % 2 == 1:\n                        x_start += s / 2.0\n                    for j in range(k):\n                        centers.append([x_start + j * s, y])\n                centers = np.array(centers[:n], dtype=float)\n                centers = np.clip(centers, 1e-9, 1 - 1e-9)\n                radii = compute_max_radii(centers)\n                return float(np.sum(radii)), centers, radii\n\n            fc, _, _ = eval_s(c)\n            fd, _, _ = eval_s(d)\n            # iterate deterministically\n            for _ in range(40):\n                if fc > fd:\n                    b = d\n                    d = c\n                    fd = fc\n                    c = b - golden_ratio * (b - a)\n                    fc, _, _ = eval_s(c)\n                else:\n                    a = c\n                    c = d\n                    fc = fd\n                    d = a + golden_ratio * (b - a)\n                    fd, _, _ = eval_s(d)\n\n            # take best of interval endpoints and internal points c,d\n            candidates = [a, b, c, d]\n            for s in candidates:\n                ssum, centers, radii = eval_s(s)\n                if ssum > best_sum:\n                    best_sum = ssum\n                    best = (centers, radii, ssum)\n\n    # Fallback\n    if best is None:\n        centers = np.array([[0.5,0.5]]*n)\n        radii = compute_max_radii(centers)\n        return centers, radii, float(np.sum(radii))\n    return best\n\ndef compute_max_radii(centers, tol=1e-10):\n    centers = np.asarray(centers, dtype=float)\n    n = centers.shape[0]\n    # distance to wall\n    wall = np.minimum.reduce([centers[:,0], centers[:,1], 1-centers[:,0], 1-centers[:,1]])\n    r = wall.copy()\n    # pairwise distances\n    dif = centers[:,None,:] - centers[None,:,:]\n    d = np.sqrt(np.sum(dif*dif, axis=2))\n    np.fill_diagonal(d, np.inf)\n\n    # Shrink overlaps: proportional scaling until no violations\n    for _ in range(1200):\n        moved = False\n        # walls\n        over = r - wall\n        if np.any(over > tol):\n            r = np.minimum(r, wall)\n            moved = True\n        # pairwise\n        for i in range(n):\n            ri = r[i]\n            for j in range(i+1, n):\n                dij = d[i,j]\n                if not np.isfinite(dij) or dij <= tol:\n                    continue\n                rj = r[j]\n                if ri + rj > dij + tol:\n                    scale = dij / (ri + rj)\n                    if scale < 1.0 - 1e-14:\n                        r[i] = ri * scale\n                        r[j] = rj * scale\n                        ri = r[i]\n                        moved = True\n        if not moved:\n            break\n\n    # Conservative growth cycles to use available slack\n    for _ in range(8):\n        inc_wall = wall - r\n        slack = d - (r[None,:] + r[:,None])\n        avail = np.min(slack, axis=1)\n        max_inc = np.minimum(inc_wall, avail)\n        max_inc = np.maximum(max_inc, 0.0)\n        delta = 0.35 * max_inc\n        if np.all(delta <= tol):\n            break\n        r += delta\n        # re-shrink small violations\n        for _ in range(160):\n            moved = False\n            for i in range(n):\n                for j in range(i+1, n):\n                    dij = d[i,j]\n                    if not np.isfinite(dij) or dij <= tol:\n                        continue\n                    if r[i] + r[j] > dij + tol:\n                        scale = dij / (r[i] + r[j])\n                        r[i] *= scale\n                        r[j] *= scale\n                        moved = True\n            over = r - wall\n            if np.any(over > tol):\n                r = np.minimum(r, wall)\n                moved = True\n            if not moved:\n                break\n\n    r = np.maximum(r, 0.0)\n    return r\n\n# EVOLVE-BLOCK-END\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, ec=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\n\n# Task\nRewrite the program to improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: complexity, diversity\nDifferent solutions with similar fitness but different features are valuable.\nProvide the complete new program code.\n\nIMPORTANT: Make sure your rewritten program maintains the same inputs and outputs\nas the original program, but with improved internal implementation.\n\n```python\n# Your rewritten program here\n```", "responses": ["```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructive packer for n=26 circles in the unit square.\n\nThis version enumerates a richer set of explicit hex-like block placements:\n- several row-count patterns summing to 26\n- a fine grid of horizontal spacings s\n- small horizontal shifts and edge-anchoring (center/left/right)\nFor each explicit layout we place centers by formula and project radii\ndeterministically to feasibility (walls + pairwise) with a conservative\ngrowth phase to use slack. The best explicit layout (max sum of radii)\nis returned.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    patterns = [\n        [7,6,6,7],       # compact 4-row block\n        [6,5,6,5,4],     # mixed 5-row\n        [5,6,5,5,5],\n        [6,5,5,5,5],\n        [5,6,6,5,4],\n    ]\n\n    best = None\n    best_sum = -1.0\n\n    # More exhaustive explicit grid of horizontal spacings and shifts\n    s_vals = np.linspace(0.14, 0.18, 25)   # center-to-center horizontal spacing candidates\n    shifts = np.linspace(-0.055, 0.055, 13)\n    anchors = ['center', 'left', 'right']\n\n    for rows in patterns:\n        R = len(rows)\n        for s in s_vals:\n            h = np.sqrt(3) / 2 * s\n            total_h = (R - 1) * h\n            # skip impossible verticals\n            if total_h > 1.0 + 1e-12:\n                continue\n            for anchor in anchors:\n                for shift_x in shifts:\n                    # compute y0 depending on anchor vertical centering\n                    y0 = 0.5 - total_h / 2.0\n                    centers = []\n                    for r_idx, k in enumerate(rows):\n                        y = y0 + r_idx * h\n                        row_width = (k - 1) * s\n                        if anchor == 'center':\n                            x_start = 0.5 - row_width / 2.0 + shift_x\n                        elif anchor == 'left':\n                            x_start = 1e-6 + shift_x\n                        else:  # right\n                            x_start = 1.0 - 1e-6 - row_width + shift_x\n                        if r_idx % 2 == 1:\n                            x_start += s / 2.0\n                        for j in range(k):\n                            centers.append([x_start + j * s, y])\n                    centers = np.array(centers[:n], dtype=float)\n                    # clamp strictly inside to avoid degeneracies\n                    centers = np.clip(centers, 1e-9, 1 - 1e-9)\n                    radii = compute_max_radii(centers)\n                    ssum = float(np.sum(radii))\n                    if ssum > best_sum:\n                        best_sum = ssum\n                        best = (centers, radii, ssum)\n\n                    # also try mirrored horizontally (flip x)\n                    centers_m = centers.copy()\n                    centers_m[:,0] = 1.0 - centers_m[:,0]\n                    radii_m = compute_max_radii(centers_m)\n                    ssum_m = float(np.sum(radii_m))\n                    if ssum_m > best_sum:\n                        best_sum = ssum_m\n                        best = (centers_m, radii_m, ssum_m)\n\n    # Fallback\n    if best is None:\n        centers = np.array([[0.5,0.5]]*n)\n        radii = compute_max_radii(centers)\n        return centers, radii, float(np.sum(radii))\n    return best\n\ndef compute_max_radii(centers, tol=1e-10):\n    centers = np.asarray(centers, dtype=float)\n    n = centers.shape[0]\n    # initial wall-limited radii\n    wall = np.minimum.reduce([centers[:,0], centers[:,1], 1-centers[:,0], 1-centers[:,1]])\n    r = wall.copy()\n\n    # pairwise distances\n    dif = centers[:,None,:] - centers[None,:,:]\n    d = np.sqrt(np.sum(dif*dif, axis=2))\n    np.fill_diagonal(d, np.inf)\n\n    # Proportional shrink until feasible\n    for _ in range(1500):\n        moved = False\n        # enforce walls\n        over = r - wall\n        if np.any(over > tol):\n            r = np.minimum(r, wall)\n            moved = True\n        # pairwise shrink\n        for i in range(n):\n            ri = r[i]\n            for j in range(i+1, n):\n                dij = d[i,j]\n                if not np.isfinite(dij) or dij <= tol:\n                    continue\n                rj = r[j]\n                if ri + rj > dij + tol:\n                    scale = dij / (ri + rj)\n                    if scale < 1.0 - 1e-12:\n                        r[i] = ri * scale\n                        r[j] = rj * scale\n                        ri = r[i]\n                        moved = True\n        if not moved:\n            break\n\n    # Conservative growth to use slack (aggressive fraction but reproject)\n    for _ in range(12):\n        inc_wall = wall - r\n        slack = d - (r[None,:] + r[:,None])\n        avail = np.min(slack, axis=1)\n        max_inc = np.minimum(inc_wall, avail)\n        max_inc = np.maximum(max_inc, 0.0)\n        # try a decent fraction to improve sum\n        delta = 0.45 * max_inc\n        if np.all(delta <= tol):\n            break\n        r += delta\n        # reproject small violations\n        for _ in range(300):\n            moved = False\n            for i in range(n):\n                for j in range(i+1, n):\n                    dij = d[i,j]\n                    if not np.isfinite(dij) or dij <= tol:\n                        continue\n                    if r[i] + r[j] > dij + tol:\n                        scale = dij / (r[i] + r[j])\n                        r[i] *= scale\n                        r[j] *= scale\n                        moved = True\n            over = r - wall\n            if np.any(over > tol):\n                r = np.minimum(r, wall)\n                moved = True\n            if not moved:\n                break\n\n    r = np.maximum(r, 0.0)\n    return r\n\n# EVOLVE-BLOCK-END\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, ec=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```"]}}, "artifacts_json": "{\"stdout\": \"Running in subprocess, Python version: 3.12.1 (v3.12.1:2305ca5144, Dec  7 2023, 17:23:38) [Clang 13.0.0 (clang-1300.0.29.30)]\\nProgram path: /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmpuws7nf0q.py\\nCalling run_packing()...\\nrun_packing() returned successfully: sum_radii = 2.156818521508243\\nResults saved to /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmp90whtmtm.py.results\\n\", \"stderr\": \"\"}", "artifact_dir": null}