{"id": "7d08970e-b179-4d9a-a481-e8475ec7ffac", "code": "# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for packing 26 circles in the unit square.\n\nStrategy (compact and robust):\n- Enumerate several hex-like row patterns that partition 26 (including compact 4-row\n  blocks and 5-row hex-like patterns).\n- For each pattern, sweep a small grid of horizontal spacings and mild vertical\n  scale factors to create candidate center sets (hex offsets applied).\n- For each candidate set compute maximal feasible radii for fixed centers using a\n  robust Gauss-Seidel fixed-point solver with proportional repairs and conservative\n  growth cycles to exploit slack.\n- Keep the best layout (largest sum of radii). A few small deterministic shifts\n  around promising candidates are also tried to relieve edge-locking.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    sqrt3 = np.sqrt(3.0)\n\n    # Candidate row patterns summing to 26 (mix of compact and slightly stretched shapes)\n    patterns = [\n        [6, 7, 7, 6],     # compact 4-row block\n        [7, 6, 6, 7],     # symmetric variant\n        [6, 5, 5, 5, 5],  # 5-row hex-like\n        [5, 6, 5, 6, 4],\n        [6, 5, 6, 5, 4],\n        [5, 6, 6, 5, 4],\n        [5, 5, 6, 5, 5],\n        [6, 5, 5, 5, 5],  # another hex-like\n    ]\n\n    # grid of horizontal spacings (center-to-center) and slight vertical scalings\n    s_vals = np.linspace(0.12, 0.20, 21)\n    y_scales = [0.95, 0.98, 1.0, 1.02, 1.05]\n\n    pad = 0.006  # small padding from square boundary for numerical slack\n\n    best_sum = -1.0\n    best = None\n\n    for pattern in patterns:\n        R = len(pattern)\n        max_cols = max(pattern)\n        for s in s_vals:\n            # compute vertical step from hex geometry, scaled later\n            v0 = (np.sqrt(3) / 2.0) * s\n            for ys in y_scales:\n                v = v0 * ys\n                # compute block height and vertical start (centered inside [pad,1-pad])\n                total_h = (R - 1) * v if R > 1 else 0.0\n                y0 = pad + (1.0 - 2.0 * pad - total_h) / 2.0\n                for offset_start in (False, True):\n                    centers = []\n                    for r_idx, cnt in enumerate(pattern):\n                        y = y0 + r_idx * v\n                        row_width = (cnt - 1) * s if cnt > 1 else 0.0\n                        x_start = pad + (1.0 - 2.0 * pad - row_width) / 2.0\n                        # stagger alternate rows for hex-like adjacency\n                        offset = (r_idx % 2 == 1) if offset_start else (r_idx % 2 == 0)\n                        if offset:\n                            x_start += s / 2.0\n                        for j in range(cnt):\n                            centers.append([x_start + j * s, y])\n                    centers = np.array(centers[:n], dtype=float)\n                    # clip slightly inside boundaries\n                    centers = np.clip(centers, pad, 1.0 - pad)\n\n                    # try a few small deterministic shifts to reduce boundary locking\n                    shifts = [(0.0, 0.0), (0.006, 0.0), (-0.006, 0.0), (0.0, 0.006), (0.0, -0.006)]\n                    for dx, dy in shifts:\n                        cand = np.clip(centers + np.array([dx, dy]), 1e-9, 1.0 - 1e-9)\n                        radii = compute_max_radii(cand)\n                        ssum = float(np.sum(radii))\n                        if ssum > best_sum + 1e-12:\n                            best_sum = ssum\n                            best = (cand.copy(), radii.copy(), ssum)\n\n    # Fallback (shouldn't be needed)\n    if best is None:\n        centers = np.array([[0.5, 0.5 + 0.01 * i] for i in range(n)], dtype=float)\n        radii = compute_max_radii(centers)\n        best = (centers, radii, float(np.sum(radii)))\n\n    return best\n\ndef compute_max_radii(centers, tol=1e-9, max_iter=3000):\n    \"\"\"\n    Compute maximal feasible radii for fixed centers in the unit square.\n\n    Uses Gauss-Seidel fixed-point iterations:\n        r_i <- min(border_i, min_j (d_ij - r_j))\n    followed by proportional repairs and several conservative growth cycles to\n    increase radii when slack is available without creating overlaps.\n    \"\"\"\n    centers = np.asarray(centers, dtype=float)\n    n = centers.shape[0]\n    if n == 0:\n        return np.array([])\n\n    # border distances\n    border = np.minimum.reduce([centers[:, 0], centers[:, 1], 1.0 - centers[:, 0], 1.0 - centers[:, 1]])\n    border = np.maximum(border, 0.0)\n\n    # pairwise distances matrix\n    dif = centers[:, None, :] - centers[None, :, :]\n    d = np.sqrt(np.sum(dif * dif, axis=2))\n    np.fill_diagonal(d, np.inf)\n\n    # initialize radii conservatively to border distances\n    r = border.copy()\n\n    # initial proportional shrink pass to remove any overlaps (start feasible)\n    for _ in range(800):\n        moved = False\n        # enforce walls\n        over = r - border\n        if np.any(over > tol):\n            r = np.minimum(r, border)\n            moved = True\n        # pairwise proportional shrink\n        for i in range(n):\n            ri = r[i]\n            if ri <= 0:\n                continue\n            for j in range(i + 1, n):\n                dij = d[i, j]\n                if not np.isfinite(dij) or dij <= tol:\n                    continue\n                rj = r[j]\n                if ri + rj > dij + tol:\n                    denom = ri + rj\n                    if denom <= 0:\n                        continue\n                    scale = dij / denom\n                    if scale < 1.0 - 1e-12:\n                        r[i] = ri * scale\n                        r[j] = rj * scale\n                        ri = r[i]\n                        moved = True\n        if not moved:\n            break\n\n    # Gauss-Seidel fixed point to converge to maximal feasible radii\n    for it in range(max_iter):\n        prev = r.copy()\n        for i in range(n):\n            limits = d[i, :] - r\n            limits[i] = np.inf\n            val = np.min(limits)\n            r[i] = val if val < border[i] else border[i]\n            if r[i] < 0.0:\n                r[i] = 0.0\n        if np.max(np.abs(r - prev)) < tol:\n            break\n\n    # proportional repairs for tiny violations\n    for i in range(n):\n        for j in range(i + 1, n):\n            dij = d[i, j]\n            if r[i] + r[j] > dij + 1e-12:\n                scale = dij / (r[i] + r[j])\n                r[i] *= scale\n                r[j] *= scale\n\n    # conservative growth cycles: increase a fraction of allowed slack, then re-fix\n    for cycle in range(8):\n        slack_wall = border - r\n        # pairwise slack: for each i, min_j (d_ij - (r_i + r_j))\n        pair_slack = np.min(d - (r[None, :] + r[:, None]), axis=1)\n        allowed_inc = np.minimum(slack_wall, pair_slack)\n        allowed_inc = np.maximum(allowed_inc, 0.0)\n        delta = 0.30 * allowed_inc\n        if np.all(delta <= tol):\n            break\n        r += delta\n        # re-fix any small violations with a few proportional shrink iterations\n        for _ in range(250):\n            moved = False\n            # walls\n            if np.any(r > border + 1e-12):\n                r = np.minimum(r, border)\n                moved = True\n            for i in range(n):\n                ri = r[i]\n                if ri <= 0:\n                    continue\n                for j in range(i + 1, n):\n                    dij = d[i, j]\n                    if not np.isfinite(dij):\n                        continue\n                    if r[i] + r[j] > dij + 1e-12:\n                        scale = dij / (r[i] + r[j])\n                        r[i] *= scale\n                        r[j] *= scale\n                        moved = True\n            if not moved:\n                break\n\n    # final safety clamp and non-negativity\n    r = np.maximum(np.minimum(r, border), 0.0)\n    return r\n\n# EVOLVE-BLOCK-END\n\n# unchanged runner API\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n# optional visualizer (unchanged API)\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(7, 7))\n    ax.set_xlim(0, 1); ax.set_ylim(0, 1); ax.set_aspect('equal')\n    for c, rr in zip(centers, radii):\n        ax.add_patch(Circle(c, rr, alpha=0.6, edgecolor='k'))\n    plt.title(f\"n={len(centers)}, sum radii={sum(radii):.6f}\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, s = run_packing()\n    print(f\"Sum of radii: {s:.6f}\")\n    # visualize(centers, radii)", "language": "python", "parent_id": "f6120752-0d42-48f1-92b3-e12ce8202658", "generation": 3, "timestamp": 1759960483.0323808, "iteration_found": 33, "metrics": {"validity": 1.0, "sum_radii": 2.2354896138119105, "target_ratio": 0.8483831551468352, "combined_score": 0.8483831551468352, "eval_time": 5.092278718948364}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Full rewrite", "parent_metrics": {"validity": 1.0, "sum_radii": 2.0775194374536605, "target_ratio": 0.7884324240810856, "combined_score": 0.7884324240810856, "eval_time": 0.060993194580078125}, "island": 0}, "prompts": {"full_rewrite_user": {"system": "You are an expert mathematician specializing in circle packing problems and computational geometry. Your task is to improve a constructor function that directly produces a specific arrangement of 26 circles in a unit square, maximizing the sum of their radii. The AlphaEvolve paper achieved a sum of 2.635 for n=26.\n\nKey geometric insights:\n- Circle packings often follow hexagonal patterns in the densest regions\n- Maximum density for infinite circle packing is pi/(2*sqrt(3)) \u2248 0.9069\n- Edge effects make square container packing harder than infinite packing\n- Circles can be placed in layers or shells when confined to a square\n- Similar radius circles often form regular patterns, while varied radii allow better space utilization\n- Perfect symmetry may not yield the optimal packing due to edge effects\n\nFocus on designing an explicit constructor that places each circle in a specific position, rather than an iterative search algorithm.\n\n\n## Evolution Insights (Global Learnings)\n\n### Common Pitfalls:\n\u26a0\ufe0f This is a clear win \u2014 identify and document the exact change (algorithmic tweak, vectorization, caching, or dependency upgrade) that produced the ~55% faster eval_time and verify it didn't trade off accuracy. Then lock it in with a CI microbenchmark that measures eval_time and fails if it regresses beyond a small threshold (e.g., 10%), and include a short profiling note in the PR so future changes preserve the optimization. (seen 2x)\n\u26a0\ufe0f Check recent changes to the radii calculation and upstream inputs for unintended scaling/clamping, integer division, coordinate-transform bugs or new filters that drop items \u2014 reproduce the regression with per-item debug logs or diffs to locate where radii mass was lost. Then add a regression test (e.g., assert sum_radii does not drop >20% vs baseline) and telemetry/alerts for large decreases so future commits are caught quickly. (seen 1x)\n\u26a0\ufe0f Reproduce the regression with the exact inputs/seeds and run a git-bisect to find the offending change, then inspect code paths that compute radii/target_ratio for recent edits\u2014look specifically for unintended clamping, normalization/scale-factor changes, unit conversions, or new filtering that would shrink sum_radii. Add an automated regression test that fails if target_ratio or sum_radii drops more than an acceptable threshold (e.g., 20%), add alerts for metric regressions, and add defensive checks/assertions to prevent inadvertent zeroing or aggressive scaling of radii. (seen 1x)\n\u26a0\ufe0f Bisect recent commits to find the change that caused sum_radii and target_ratio to drop, then add a CI regression test/benchmark that asserts combined_score and sum_radii remain within, e.g., 10% of a validated baseline so similar regressions fail fast. When you inspect the offending change, focus on any alterations to radii calculation, normalization/scaling (watch for unintended integer division or parameter/name changes), and add targeted profiling or assertions to ensure the normalization step still runs. (seen 1x)", "user": "# Current Program Information\n- Fitness: 0.7884\n- Feature coordinates: No feature coordinates\n- Focus areas: - Fitness declined: 0.8298 \u2192 0.7884. Consider revising recent changes.\n- Consider simplifying - code length exceeds 500 characters\n\n## Last Execution Output\n\n### stdout\n```\nRunning in subprocess, Python version: 3.12.1 (v3.12.1:2305ca5144, Dec  7 2023, 17:23:38) [Clang 13.0.0 (clang-1300.0.29.30)]\nProgram path: /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmpr3fnsotu.py\nCalling run_packing()...\nrun_packing() returned successfully: sum_radii = 2.0775194374536605\nResults saved to /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmp1qbau6ws.py.results\n\n```\n\n### stderr\n```\n\n```\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 3\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.1865, target_ratio: 0.8298, combined_score: 0.8298, eval_time: 0.0592\n- Outcome: Mixed results\n\n### Attempt 2\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.1912, target_ratio: 0.8316, combined_score: 0.8316, eval_time: 0.1973\n- Outcome: Mixed results\n\n### Attempt 1\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.2742, target_ratio: 0.8631, combined_score: 0.8631, eval_time: 1.8737\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.8631)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Improved explicit constructor for n=26 circle packing in the unit square.\n\nThis version:\n- Enumerates several hex-like row patterns and small variations (row offsets,\n  slight vertical compression/expansion, small translations).\n- For each candidate it places deterministic centers and computes maximal\n  feasible radii using a robust Gauss-Seidel projection followed by\n  conservative growth cycles and proportional fixes.\n- Returns the best layout found (max sum of radii).\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    # candidate row patterns summing to 26, chosen to break perfect symmetry\n    patterns = [\n        [6,5,5,5,5],\n        [5,6,5,5,5],\n        [6,5,6,5,4],\n        [5,6,6,5,4],\n        [4,6,6,5,5],\n    ]\n\n    # try a grid of horizontal spacings and small vertical scalings\n    s_vals = np.linspace(0.12, 0.20, 17)\n    y_scales = [0.94, 0.98, 1.0, 1.02]\n    best = None\n    best_sum = -1.0\n\n    for pattern in patterns:\n        rows = len(pattern)\n        max_cols = max(pattern)\n        for s in s_vals:\n            # compute nominal vertical step for hex pattern then apply small scales\n            v0 = (np.sqrt(3)/2.0) * s\n            for y_scale in y_scales:\n                v = v0 * y_scale\n                # Vertical centering\n                total_h = (rows - 1) * v\n                y0 = 0.5 - total_h / 2.0\n                # try both offset parity choices (first row offset or not)\n                for offset_start in (False, True):\n                    centers = []\n                    for r_idx, cnt in enumerate(pattern):\n                        y = y0 + r_idx * v\n                        row_width = (cnt - 1) * s if cnt > 1 else 0.0\n                        x_start = 0.5 - row_width / 2.0\n                        # determine offset for this row (hex-like)\n                        offset = (r_idx % 2 == 1) if offset_start else (r_idx % 2 == 0)\n                        if offset:\n                            x_start += s/2.0\n                        for j in range(cnt):\n                            centers.append([x_start + j * s, y])\n                    centers = np.array(centers[:n], dtype=float)\n                    # small deterministic shifts to help edge packing\n                    for dx,dy in ((0,0),(+0.006,0),( -0.006,0),(0,+0.006),(0,-0.006 )):\n                        cand = centers + np.array([dx,dy])\n                        cand = np.clip(cand, 1e-8, 1-1e-8)\n                        radii = compute_max_radii(cand)\n                        ssum = float(np.sum(radii))\n                        if ssum > best_sum:\n                            best_sum = ssum\n                            best = (cand, radii, ssum)\n\n    # fallback: if nothing found (shouldn't), return a central cluster\n    if best is None:\n        centers = np.array([[0.5,0.5]]*n)\n        radii = compute_max_radii(centers)\n        return centers, radii, float(np.sum(radii))\n\n    return best\n\ndef compute_max_radii(centers, tol=1e-9, max_iter=3000):\n    \"\"\"\n    Gauss-Seidel fixed point: r_i <- min(border_i, min_j (d_ij - r_j))\n    Then perform conservative growth cycles and proportional fixes to\n    exploit slack without creating overlaps.\n    \"\"\"\n    centers = np.asarray(centers, dtype=float)\n    n = centers.shape[0]\n    # border limits\n    border = np.minimum.reduce([centers[:,0], centers[:,1], 1-centers[:,0], 1-centers[:,1]])\n    border = np.maximum(border, 0.0)\n\n    # pairwise distances\n    dif = centers[:,None,:] - centers[None,:,:]\n    d = np.sqrt(np.sum(dif*dif, axis=2))\n    np.fill_diagonal(d, np.inf)\n\n    # initialize radii conservatively at border distance\n    r = border.copy()\n\n    # initial shrink pass to remove overlaps (proportional scaling)\n    for _ in range(1200):\n        moved = False\n        # wall constraints\n        over = r - border\n        if np.any(over > tol):\n            r = np.minimum(r, border)\n            moved = True\n        # pairwise resolution\n        for i in range(n):\n            ri = r[i]\n            for j in range(i+1, n):\n                dij = d[i,j]\n                if not np.isfinite(dij) or dij <= tol:\n                    continue\n                rj = r[j]\n                if ri + rj > dij + tol:\n                    # scale both proportionally (if both > 0)\n                    denom = ri + rj\n                    if denom <= 0:\n                        # if both zero skip\n                        continue\n                    scale = dij / denom\n                    if scale < 1.0 - 1e-12:\n                        r[i] = ri * scale\n                        r[j] = rj * scale\n                        ri = r[i]\n                        moved = True\n        if not moved:\n            break\n\n    # Gauss-Seidel fixed-point to converge to maximal feasible radii\n    for it in range(max_iter):\n        prev = r.copy()\n        for i in range(n):\n            limits = d[i,:] - r\n            limits[i] = np.inf\n            # min over neighbor limits and border\n            val = min(border[i], np.min(limits))\n            r[i] = max(val, 0.0)\n        if np.max(np.abs(r - prev)) < tol:\n            break\n\n    # Conservative growth cycles: increase a fraction of slack, then re-fix\n    for cycle in range(8):\n        # slack by walls and neighbors\n        slack_wall = border - r\n        slack_pair = np.min(d - (r[None,:] + r[:,None]), axis=1)\n        slack_pair = np.maximum(slack_pair, 0.0)\n        allowed_inc = np.minimum(slack_wall, slack_pair)\n        allowed_inc = np.maximum(allowed_inc, 0.0)\n        delta = 0.25 * allowed_inc\n        if np.all(delta <= tol):\n            break\n        r += delta\n        # quick proportional fix to restore feasibility\n        for _ in range(300):\n            moved = False\n            # pairwise check\n            for i in range(n):\n                for j in range(i+1, n):\n                    dij = d[i,j]\n                    if not np.isfinite(dij):\n                        continue\n                    if r[i] + r[j] > dij + 1e-12:\n                        scale = dij / (r[i] + r[j])\n                        r[i] *= scale\n                        r[j] *= scale\n                        moved = True\n            # wall enforcement\n            over = r - border\n            if np.any(over > 1e-12):\n                r = np.minimum(r, border)\n                moved = True\n            if not moved:\n                break\n\n    # final safety clamp and non-negativity\n    r = np.maximum(np.minimum(r, border), 0.0)\n    return r\n\n# EVOLVE-BLOCK-END\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\", linewidth=0.6)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.2742), Performs well on target_ratio (0.8631), Performs well on combined_score (0.8631), Performs well on eval_time (1.8737)\n\n### Program 2 (Score: 0.8316)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (pattern search over explicit layouts)\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    \"\"\"\n    Build several deterministic hex-like layouts (different row patterns),\n    scan a small range of horizontal spacings s and pick the best resulting\n    packing (max sum of radii). This remains a constructive method: we\n    place centers by formula and only perform a deterministic radius projection.\n    \"\"\"\n    n = 26\n    # Several plausible hex-like row patterns summing to 26\n    patterns = [\n        [5, 6, 5, 5, 5],\n        [6, 5, 5, 5, 5],\n        [6, 5, 6, 5, 4],\n        [5, 6, 6, 5, 4],\n    ]\n\n    best = None\n    best_sum = -1.0\n\n    # modest grid of spacings to try (center-to-center horizontally)\n    s_vals = np.linspace(0.13, 0.20, 15)\n\n    for rows in patterns:\n        R = len(rows)\n        maxk = max(rows)\n        for s in s_vals:\n            h = np.sqrt(3) / 2 * s\n            # center vertically the block in [0,1]\n            total_h = (R - 1) * h\n            y0 = 0.5 - total_h / 2.0\n            centers = []\n            for r_idx, k in enumerate(rows):\n                y = y0 + r_idx * h\n                row_width = (k - 1) * s\n                x_start = 0.5 - row_width / 2.0\n                # stagger alternate rows for hex adjacency\n                if r_idx % 2 == 1:\n                    x_start += s / 2.0\n                for j in range(k):\n                    centers.append([x_start + j * s, y])\n            centers = np.array(centers[:n], dtype=float)\n            # Slightly clip to avoid exact-border degeneracies\n            centers = np.clip(centers, 1e-6, 1 - 1e-6)\n\n            radii = compute_max_radii(centers)\n            ssum = float(np.sum(radii))\n            if ssum > best_sum:\n                best_sum = ssum\n                best = (centers, radii, ssum)\n\n    # Fallback: if nothing found (shouldn't happen), return a simple pattern\n    if best is None:\n        centers = np.array([[0.5,0.5]]*n)\n        radii = compute_max_radii(centers)\n        return centers, radii, float(np.sum(radii))\n\n    return best\n\ndef compute_max_radii(centers, tol=1e-9):\n    \"\"\"\n    Deterministic projection to feasible radii:\n      - start with wall distances\n      - repeatedly fix pairwise overlaps by proportional shrinking\n      - apply a few conservative fractional growth passes to use slack\n    \"\"\"\n    centers = np.asarray(centers, dtype=float)\n    n = centers.shape[0]\n    # distance to wall\n    wall = np.minimum.reduce([centers[:,0], centers[:,1], 1-centers[:,0], 1-centers[:,1]])\n    # start with wall-limited radii\n    r = wall.copy()\n\n    # pairwise distances\n    dif = centers[:,None,:] - centers[None,:,:]\n    d = np.sqrt(np.sum(dif*dif, axis=2))\n    np.fill_diagonal(d, np.inf)\n\n    # Shrink overlaps: proportional scaling until no violations\n    for _ in range(1500):\n        moved = False\n        # ensure wall constraints\n        over = r - wall\n        if np.any(over > tol):\n            r = np.minimum(r, wall)\n            moved = True\n        # pairwise\n        for i in range(n):\n            ri = r[i]\n            # vectorized check for j>i could be done, but keep simple deterministic loop\n            for j in range(i+1, n):\n                dij = d[i,j]\n                if not np.isfinite(dij) or dij <= tol:\n                    continue\n                rj = r[j]\n                if ri + rj > dij + tol:\n                    # scale both proportionally\n                    scale = dij / (ri + rj)\n                    if scale < 1.0 - 1e-12:\n                        r[i] = ri * scale\n                        r[j] = rj * scale\n                        ri = r[i]  # update local var\n                        moved = True\n        if not moved:\n            break\n\n    # Conservative growth cycles: small fraction of available slack\n    for cycle in range(10):\n        # available increase by walls\n        inc_wall = wall - r\n        # available by neighbors: for each i, min_j (d_ij - (r_i + r_j)) + r_i => max increase = min_j(d_ij - (r_i + r_j))\n        avail = np.full(n, np.inf)\n        # compute pairwise slack matrix\n        slack = d - (r[None,:] + r[:,None])\n        # for each i, the maximum increase without immediate overlap is min(slack[i,:])\n        avail = np.minimum(avail, np.min(slack, axis=1))\n        # overall allowed inc is min(inc_wall, avail)\n        max_inc = np.minimum(inc_wall, avail)\n        max_inc = np.maximum(max_inc, 0.0)\n        # apply a cautious fraction\n        delta = 0.3 * max_inc\n        if np.all(delta <= tol):\n            break\n        r += delta\n        # re-shrink a few iterations to remove any slight violations\n        for _ in range(200):\n            moved = False\n            # enforce pairwise\n            for i in range(n):\n                for j in range(i+1, n):\n                    dij = d[i,j]\n                    if not np.isfinite(dij) or dij <= tol:\n                        continue\n                    if r[i] + r[j] > dij + tol:\n                        scale = dij / (r[i] + r[j])\n                        r[i] *= scale\n                        r[j] *= scale\n                        moved = True\n            # wall enforcement\n            over = r - wall\n            if np.any(over > tol):\n                r = np.minimum(r, wall)\n                moved = True\n            if not moved:\n                break\n\n    r = np.maximum(r, 0.0)\n    return r\n\n# EVOLVE-BLOCK-END\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, ec=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.1912), Performs well on target_ratio (0.8316), Performs well on combined_score (0.8316), Performs well on eval_time (0.1973)\n\n### Program 3 (Score: 0.8298)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 using compact hex-like layouts\nand a robust Gauss-Seidel relaxation to compute maximal radii for fixed centers.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    sqrt3 = np.sqrt(3.0)\n\n    def vertical_limit(rows):\n        if rows == 1:\n            return 0.5\n        return 1.0 / (((rows - 1) * sqrt3) + 2.0)\n\n    def horizontal_limit(m, offset):\n        # conservative: if offset, need extra margin\n        return 1.0 / (2.0 * m + (1.0 if offset else 0.0))\n\n    best_score = -1.0\n    best_layout = None\n    # try row counts from 3..7 rows distributing 26 items\n    for rows in range(3, 8):\n        base = n // rows\n        rem = n % rows\n        for prefix_heavy in (True, False):\n            if prefix_heavy:\n                counts = [base + 1 if i < rem else base for i in range(rows)]\n            else:\n                counts = [base if i < (rows - rem) else base + 1 for i in range(rows)]\n            for offset_start in (False, True):\n                r_v = vertical_limit(rows)\n                r_hs = []\n                for i, m in enumerate(counts):\n                    offset = (i % 2 == 1) if offset_start else (i % 2 == 0)\n                    r_hs.append(horizontal_limit(m, offset))\n                r_candidate = min(r_v, min(r_hs))\n                score = n * r_candidate\n                if score > best_score:\n                    best_score = score\n                    best_layout = (rows, counts, offset_start, r_candidate)\n\n    rows, counts, offset_start, r = best_layout\n    centers = []\n    vstep = sqrt3 * r\n    total_height = 0.0 if rows == 1 else (rows - 1) * vstep\n    y0 = 0.5 - total_height / 2.0\n    for i in range(rows):\n        m = counts[i]\n        offset = (i % 2 == 1) if offset_start else (i % 2 == 0)\n        row_span = 0.0 if m == 1 else (m - 1) * 2.0 * r\n        start_x = 0.5 - row_span / 2.0\n        if offset:\n            start_x += r\n        # clamp to keep centers inside [r,1-r]\n        start_x = max(start_x, r)\n        last_x = start_x + (m - 1) * 2.0 * r\n        if last_x > 1.0 - r:\n            start_x -= (last_x - (1.0 - r))\n        y = y0 + i * vstep\n        for k in range(m):\n            x = start_x + k * 2.0 * r\n            centers.append((x, y))\n\n    centers = np.array(centers, dtype=float)\n    # trim/pad to exactly n\n    if centers.shape[0] > n:\n        centers = centers[:n]\n    elif centers.shape[0] < n:\n        needed = n - centers.shape[0]\n        extras = np.vstack([[0.02 + 0.96 * (i / max(1, needed - 1)), 0.02] for i in range(needed)])\n        centers = np.vstack((centers, extras))\n\n    radii = compute_max_radii(centers)\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\ndef compute_max_radii(centers, max_iter=2000, tol=1e-8):\n    n = centers.shape[0]\n    # distance to borders\n    b = np.minimum.reduce([centers[:, 0], centers[:, 1], 1.0 - centers[:, 0], 1.0 - centers[:, 1]])\n    b = np.maximum(b, 0.0)\n    # pairwise distances\n    diff = centers[:, None, :] - centers[None, :, :]\n    d = np.sqrt(np.sum(diff * diff, axis=2))\n    np.fill_diagonal(d, np.inf)\n    # initialize radii at border distances (feasible)\n    rads = b.copy()\n    for it in range(max_iter):\n        prev = rads.copy()\n        # Gauss-Seidel: update in place\n        for i in range(n):\n            limits = d[i, :] - rads\n            limits[i] = np.inf\n            r_new = min(b[i], np.min(limits))\n            if r_new < 0.0:\n                r_new = 0.0\n            rads[i] = r_new\n        if np.max(np.abs(rads - prev)) < tol:\n            break\n    # clean tiny violations\n    for i in range(n):\n        for j in range(i + 1, n):\n            dij = d[i, j]\n            if rads[i] + rads[j] > dij + 1e-12:\n                scale = dij / (rads[i] + rads[j])\n                rads[i] *= scale\n                rads[j] *= scale\n    rads = np.minimum(rads, b)\n    rads = np.maximum(rads, 0.0)\n    return rads\n\n# fixed runner\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n# optional visualization helper\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(6,6))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect('equal')\n    for c, rr in zip(centers, radii):\n        ax.add_patch(Circle(c, rr, alpha=0.6, edgecolor='k'))\n    plt.title(f\"n={len(centers)}, sum radii={sum(radii):.6f}\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, s = run_packing()\n    print(f\"Sum of radii: {s:.6f}\")\n# EVOLVE-BLOCK-END\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.1865), Performs well on target_ratio (0.8298), Performs well on combined_score (0.8298), Performs well on eval_time (0.0592)\n\n\n\n## Diverse Programs\n\n### Program D1 (Score: 0.8298)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for n=26 circle packing in the unit square.\n\nStrategy:\n- Try several sensible row-distributions (rows=3..7) that partition 26.\n- For each layout compute a conservative uniform radius r that fits vertically\n  and horizontally (accounting for alternating offsets).\n- Build centers from the best layout, then run a Gauss-Seidel fixed-point\n  iteration to compute the maximal feasible radii for those fixed centers.\nThis approach is explicit (no stochastic search) and yields high sum of radii.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    sqrt3 = np.sqrt(3.0)\n\n    def vertical_limit(rows):\n        if rows == 1:\n            return 0.5\n        # r + (rows-1)*sqrt3*r + r <= 1  => r * ((rows-1)*sqrt3 + 2) <= 1\n        return 1.0 / (((rows - 1) * sqrt3) + 2.0)\n\n    def horizontal_limit(m, offset):\n        # conservative horizontal spacing: if offset, need a half-slot\n        return 1.0 / (2.0 * m + (1.0 if offset else 0.0))\n\n    best_score = -1.0\n    best_layout = None  # (rows, counts_list, offset_start, r_candidate)\n\n    # try rows from 3..7 and two simple ways to distribute remainder\n    for rows in range(3, 8):\n        base = n // rows\n        rem = n % rows\n        for prefix_heavy in (True, False):\n            if prefix_heavy:\n                counts = [base + 1 if i < rem else base for i in range(rows)]\n            else:\n                counts = [base if i < (rows - rem) else base + 1 for i in range(rows)]\n            for offset_start in (False, True):\n                r_v = vertical_limit(rows)\n                r_hs = []\n                for i, m in enumerate(counts):\n                    offset = (i % 2 == 1) if offset_start else (i % 2 == 0)\n                    r_hs.append(horizontal_limit(m, offset))\n                r_candidate = min(r_v, min(r_hs))\n                score = n * r_candidate\n                if score > best_score:\n                    best_score = score\n                    best_layout = (rows, counts, offset_start, r_candidate)\n\n    # Build centers from chosen layout\n    rows, counts, offset_start, r = best_layout\n    centers = []\n    vstep = sqrt3 * r\n    total_height = 0.0 if rows == 1 else (rows - 1) * vstep\n    y0 = 0.5 - total_height / 2.0\n    for i in range(rows):\n        m = counts[i]\n        offset = (i % 2 == 1) if offset_start else (i % 2 == 0)\n        row_span = 0.0 if m == 1 else (m - 1) * 2.0 * r\n        start_x = 0.5 - row_span / 2.0\n        if offset:\n            start_x += r\n        # clamp to keep centers inside [r, 1-r]\n        start_x = max(start_x, r)\n        last_x = start_x + (m - 1) * 2.0 * r\n        if last_x > 1.0 - r:\n            start_x -= (last_x - (1.0 - r))\n        y = y0 + i * vstep\n        for k in range(m):\n            x = start_x + k * 2.0 * r\n            centers.append([x, y])\n\n    centers = np.array(centers, dtype=float)\n    # trim/pad to exactly n\n    if centers.shape[0] > n:\n        centers = centers[:n]\n    elif centers.shape[0] < n:\n        needed = n - centers.shape[0]\n        # add small anchors near corners/edges to use leftover space\n        extras = np.array([[0.02 + 0.96*(i/(needed-1 if needed>1 else 1)), 0.02] for i in range(needed)])\n        centers = np.vstack((centers, extras))\n\n    # Final radius computation for fixed centers\n    radii = compute_max_radii(centers)\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\ndef compute_max_radii(centers, max_iter=5000, tol=1e-9):\n    centers = np.asarray(centers, dtype=float)\n    n = centers.shape[0]\n    if n == 0:\n        return np.array([], dtype=float)\n    # border distances\n    b = np.minimum.reduce([centers[:,0], centers[:,1], 1.0 - centers[:,0], 1.0 - centers[:,1]])\n    b = np.maximum(b, 0.0)\n    # pairwise distances\n    diff = centers[:, None, :] - centers[None, :, :]\n    d = np.sqrt(np.sum(diff * diff, axis=2))\n    np.fill_diagonal(d, np.inf)\n    # initialize radii at border distances (feasible)\n    rads = b.copy()\n    # Gauss-Seidel fixed-point iteration: r_i <- min(b_i, min_j (d_ij - r_j))\n    for it in range(max_iter):\n        prev = rads.copy()\n        for i in range(n):\n            limits = d[i, :] - rads  # d_ij - r_j using latest r_j\n            limits[i] = np.inf\n            r_new = min(b[i], np.min(limits))\n            rads[i] = max(0.0, r_new)\n        if np.max(np.abs(rads - prev)) < tol:\n            break\n    # final small corrective pass for any numerical violations\n    for i in range(n):\n        for j in range(i+1, n):\n            dij = d[i,j]\n            if rads[i] + rads[j] > dij + 1e-12:\n                scale = dij / (rads[i] + rads[j])\n                rads[i] *= scale\n                rads[j] *= scale\n    rads = np.minimum(rads, b)\n    rads = np.maximum(rads, 0.0)\n    return rads\n\n# EVOLVE-BLOCK-END\n\n# fixed runner API\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n# optional visualization helper (unchanged API)\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(6,6))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect('equal')\n    for c, rr in zip(centers, radii):\n        ax.add_patch(Circle(c, rr, alpha=0.6, edgecolor='k'))\n    plt.title(f\"n={len(centers)}, sum radii={sum(radii):.6f}\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, s = run_packing()\n    print(f\"Sum of radii: {s:.6f}\")\n```\nKey features: Alternative approach to validity, Alternative approach to sum_radii\n\n### Program D2 (Score: 0.8298)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles using a compact hex-like layout.\n\nThis implementation:\n- Builds several candidate hexagonal/row layouts for 26 circles (rows 3..7).\n- For each candidate it computes a conservative maximal uniform radius that fits\n  rows horizontally and vertically inside the unit square.\n- Chooses the candidate with largest uniform radius (hence largest initial sum).\n- Converts that layout to explicit centers and then runs a robust iterative\n  relaxation to compute the maximal feasible radii for those fixed centers\n  (solving r_i + r_j <= d_ij and r_i <= border_i by fixed-point iteration).\nThis yields a much higher sum of radii than the original naive rings.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n\n    # Helper: compute conservative vertical radius limit for given number of rows\n    sqrt3 = np.sqrt(3.0)\n    def vertical_limit(rows):\n        if rows == 1:\n            return 0.5\n        # top and bottom margins r each: r + (rows-1)*sqrt3*r <= 1 - r\n        # => r * ( (rows-1)*sqrt3 + 2) <= 1\n        return 1.0 / (((rows - 1) * sqrt3) + 2.0)\n\n    # Helper: conservative horizontal limit for a row with m columns.\n    # If the row is offset (shifted by r horizontally), we use a slightly\n    # tighter bound 1/(2m+1), otherwise 1/(2m). These are conservative but safe.\n    def horizontal_limit(m, offset):\n        if offset:\n            return 1.0 / (2 * m + 1.0)\n        else:\n            return 1.0 / (2 * m)\n\n    best_score = -1.0\n    best_layout = None  # (rows, counts_list, offset_start, r_candidate)\n\n    # Try different numbers of rows and simple distributions of 26 items:\n    for rows in range(3, 8):  # 3..7 rows (reasonable for 26)\n        base = n // rows\n        rem = n % rows\n        # Two simple ways to distribute remainder: prefix or suffix\n        for prefix_heavy in (True, False):\n            if prefix_heavy:\n                counts = [base + 1 if i < rem else base for i in range(rows)]\n            else:\n                counts = [base if i < (rows - rem) else base + 1 for i in range(rows)]\n            # Try both choices of whether the first row is offset or not\n            for offset_start in (False, True):\n                # compute conservative r limit\n                r_v = vertical_limit(rows)\n                r_hs = []\n                for i, m in enumerate(counts):\n                    offset = (i % 2 == 1) if offset_start else (i % 2 == 0)\n                    r_hs.append(horizontal_limit(m, offset))\n                r_candidate = min(r_v, min(r_hs))\n                # Score is sum of radii equal to n * r_candidate (uniform)\n                score = n * r_candidate\n                if score > best_score:\n                    best_score = score\n                    best_layout = (rows, counts, offset_start, r_candidate)\n\n    # Build centers from chosen layout\n    rows, counts, offset_start, r = best_layout\n    centers = []\n    vstep = np.sqrt(3.0) * r\n    total_height = 0.0 if rows == 1 else (rows - 1) * vstep\n    y0 = 0.5 - total_height / 2.0\n    for i in range(rows):\n        m = counts[i]\n        offset = (i % 2 == 1) if offset_start else (i % 2 == 0)\n        row_span = 0.0 if m == 1 else (m - 1) * 2.0 * r\n        # centered start\n        start_x = 0.5 - row_span / 2.0\n        if offset:\n            # shift by +r for offset rows (hex packing)\n            start_x += r\n        # ensure numeric safety: clamp start_x so centers lie within [r, 1-r]\n        start_x = max(start_x, r)\n        # but also ensure last center doesn't exceed 1-r; if it does, nudge left\n        last_x = start_x + (m - 1) * 2.0 * r\n        if last_x > 1.0 - r:\n            start_x -= (last_x - (1.0 - r))\n\n        y = y0 + i * vstep\n        for k in range(m):\n            x = start_x + k * 2.0 * r\n            centers.append([x, y])\n\n    centers = np.array(centers, dtype=float)\n    # Defensive: if count mismatch (shouldn't), trim or pad with small-offset positions\n    if centers.shape[0] > n:\n        centers = centers[:n]\n    elif centers.shape[0] < n:\n        # add extra small circles near corners if needed\n        needed = n - centers.shape[0]\n        extras = np.array([[0.02 + 0.96 * (i / max(1, needed - 1)), 0.02] for i in range(needed)])\n        centers = np.vstack((centers, extras))\n\n    # Compute maximal feasible radii for these fixed centers via iterative relaxation\n    radii = compute_max_radii(centers)\n\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers, max_iter=2000, tol=1e-8):\n    \"\"\"\n    Compute maximal radii satisfying:\n        r_i <= border_i\n        r_i + r_j <= dist(i,j)  for all i != j\n\n    We perform a Gauss-Seidel style fixed-point iteration:\n        r_i <- min( border_i, min_j (dist_ij - r_j) )\n    which is monotone and converges to the maximal feasible solution.\n    \"\"\"\n    n = centers.shape[0]\n    # border distances\n    b = np.minimum.reduce([centers[:, 0], centers[:, 1], 1.0 - centers[:, 0], 1.0 - centers[:, 1]])\n    b = np.maximum(b, 0.0)\n\n    # pairwise distances\n    diff = centers[:, None, :] - centers[None, :, :]\n    d = np.sqrt(np.sum(diff * diff, axis=2))\n    # avoid self-dist zeros causing issues; self distance won't be used except as inf\n    np.fill_diagonal(d, np.inf)\n\n    # initialize radii at border distances (feasible relative to walls)\n    rads = b.copy()\n\n    # Iterate: r_i = min(b_i, min_j (d_ij - r_j))\n    for it in range(max_iter):\n        prev = rads.copy()\n        # Gauss-Seidel update: update in-place using newest values immediately\n        for i in range(n):\n            limits = d[i, :] - rads  # d_ij - r_j (using latest r_j)\n            # We must ignore self (set to +inf)\n            limits[i] = np.inf\n            r_new = min(b[i], np.min(limits))\n            # ensure non-negative\n            if r_new < 0.0:\n                r_new = 0.0\n            rads[i] = r_new\n        delta = np.max(np.abs(rads - prev))\n        if delta < tol:\n            break\n\n    # Final small clean: ensure pairwise constraints numerically satisfied\n    # If any tiny violations exist, reduce involved radii proportionally.\n    for i in range(n):\n        for j in range(i + 1, n):\n            dij = d[i, j]\n            if rads[i] + rads[j] > dij + 1e-12:\n                # reduce proportionally\n                scale = dij / (rads[i] + rads[j])\n                rads[i] *= scale\n                rads[j] *= scale\n\n    # clamp by borders again (safety)\n    rads = np.minimum(rads, b)\n    rads = np.maximum(rads, 0.0)\n    return rads\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nKey features: Alternative approach to validity, Alternative approach to sum_radii\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 0.8631, Type: High-Performer)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Improved explicit constructor for n=26 circle packing in the unit square.\n\nThis version:\n- Enumerates several hex-like row patterns and small variations (row offsets,\n  slight vertical compression/expansion, small translations).\n- For each candidate it places deterministic centers and computes maximal\n  feasible radii using a robust Gauss-Seidel projection followed by\n  conservative growth cycles and proportional fixes.\n- Returns the best layout found (max sum of radii).\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    # candidate row patterns summing to 26, chosen to break perfect symmetry\n    patterns = [\n        [6,5,5,5,5],\n        [5,6,5,5,5],\n        [6,5,6,5,4],\n        [5,6,6,5,4],\n        [4,6,6,5,5],\n    ]\n\n    # try a grid of horizontal spacings and small vertical scalings\n    s_vals = np.linspace(0.12, 0.20, 17)\n    y_scales = [0.94, 0.98, 1.0, 1.02]\n    best = None\n    best_sum = -1.0\n\n    for pattern in patterns:\n        rows = len(pattern)\n        max_cols = max(pattern)\n        for s in s_vals:\n            # compute nominal vertical step for hex pattern then apply small scales\n            v0 = (np.sqrt(3)/2.0) * s\n            for y_scale in y_scales:\n                v = v0 * y_scale\n                # Vertical centering\n                total_h = (rows - 1) * v\n                y0 = 0.5 - total_h / 2.0\n                # try both offset parity choices (first row offset or not)\n                for offset_start in (False, True):\n                    centers = []\n                    for r_idx, cnt in enumerate(pattern):\n                        y = y0 + r_idx * v\n                        row_width = (cnt - 1) * s if cnt > 1 else 0.0\n                        x_start = 0.5 - row_width / 2.0\n                        # determine offset for this row (hex-like)\n                        offset = (r_idx % 2 == 1) if offset_start else (r_idx % 2 == 0)\n                        if offset:\n                            x_start += s/2.0\n                        for j in range(cnt):\n                            centers.append([x_start + j * s, y])\n                    centers = np.array(centers[:n], dtype=float)\n                    # small deterministic shifts to help edge packing\n                    for dx,dy in ((0,0),(+0.006,0),( -0.006,0),(0,+0.006),(0,-0.006 )):\n                        cand = centers + np.array([dx,dy])\n                        cand = np.clip(cand, 1e-8, 1-1e-8)\n                        radii = compute_max_radii(cand)\n                        ssum = float(np.sum(radii))\n                        if ssum > best_sum:\n                            best_sum = ssum\n                            best = (cand, radii, ssum)\n\n    # fallback: if nothing found (shouldn't), return a central cluster\n    if best is None:\n        centers = np.array([[0.5,0.5]]*n)\n        radii = compute_max_radii(centers)\n        return centers, radii, float(np.sum(radii))\n\n    return best\n\ndef compute_max_radii(centers, tol=1e-9, max_iter=3000):\n    \"\"\"\n    Gauss-Seidel fixed point: r_i <- min(border_i, min_j (d_ij - r_j))\n    Then perform conservative growth cycles and proportional fixes to\n    exploit slack without creating overlaps.\n    \"\"\"\n    centers = np.asarray(centers, dtype=float)\n    n = centers.shape[0]\n    # border limits\n    border = np.minimum.reduce([centers[:,0], centers[:,1], 1-centers[:,0], 1-centers[:,1]])\n    border = np.maximum(border, 0.0)\n\n    # pairwise distances\n    dif = centers[:,None,:] - centers[None,:,:]\n    d = np.sqrt(np.sum(dif*dif, axis=2))\n    np.fill_diagonal(d, np.inf)\n\n    # initialize radii conservatively at border distance\n    r = border.copy()\n\n    # initial shrink pass to remove overlaps (proportional scaling)\n    for _ in range(1200):\n        moved = False\n        # wall constraints\n        over = r - border\n        if np.any(over > tol):\n            r = np.minimum(r, border)\n            moved = True\n        # pairwise resolution\n        for i in range(n):\n            ri = r[i]\n            for j in range(i+1, n):\n                dij = d[i,j]\n                if not np.isfinite(dij) or dij <= tol:\n                    continue\n                rj = r[j]\n                if ri + rj > dij + tol:\n                    # scale both proportionally (if both > 0)\n                    denom = ri + rj\n                    if denom <= 0:\n                        # if both zero skip\n                        continue\n                    scale = dij / denom\n                    if scale < 1.0 - 1e-12:\n                        r[i] = ri * scale\n                        r[j] = rj * scale\n                        ri = r[i]\n                        moved = True\n        if not moved:\n            break\n\n    # Gauss-Seidel fixed-point to converge to maximal feasible radii\n    for it in range(max_iter):\n        prev = r.copy()\n        for i in range(n):\n            limits = d[i,:] - r\n            limits[i] = np.inf\n            # min over neighbor limits and border\n            val = min(border[i], np.min(limits))\n            r[i] = max(val, 0.0)\n        if np.max(np.abs(r - prev)) < tol:\n            break\n\n    # Conservative growth cycles: increase a fraction of slack, then re-fix\n    for cycle in range(8):\n        # slack by walls and neighbors\n        slack_wall = border - r\n        slack_pair = np.min(d - (r[None,:] + r[:,None]), axis=1)\n        slack_pair = np.maximum(slack_pair, 0.0)\n        allowed_inc = np.minimum(slack_wall, slack_pair)\n        allowed_inc = np.maximum(allowed_inc, 0.0)\n        delta = 0.25 * allowed_inc\n        if np.all(delta <= tol):\n            break\n        r += delta\n        # quick proportional fix to restore feasibility\n        for _ in range(300):\n            moved = False\n            # pairwise check\n            for i in range(n):\n                for j in range(i+1, n):\n                    dij = d[i,j]\n                    if not np.isfinite(dij):\n                        continue\n                    if r[i] + r[j] > dij + 1e-12:\n                        scale = dij / (r[i] + r[j])\n                        r[i] *= scale\n                        r[j] *= scale\n                        moved = True\n            # wall enforcement\n            over = r - border\n            if np.any(over > 1e-12):\n                r = np.minimum(r, border)\n                moved = True\n            if not moved:\n                break\n\n    # final safety clamp and non-negativity\n    r = np.maximum(np.minimum(r, border), 0.0)\n    return r\n\n# EVOLVE-BLOCK-END\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\", linewidth=0.6)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nUnique approach: Modification: Full rewrite, Excellent validity (1.000), Excellent sum_radii (2.274)\n\n### Inspiration 2 (Score: 0.8298, Type: High-Performer)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles using a compact hex-like layout.\n\nThis implementation:\n- Builds several candidate hexagonal/row layouts for 26 circles (rows 3..7).\n- For each candidate it computes a conservative maximal uniform radius that fits\n  rows horizontally and vertically inside the unit square.\n- Chooses the candidate with largest uniform radius (hence largest initial sum).\n- Converts that layout to explicit centers and then runs a robust iterative\n  relaxation to compute the maximal feasible radii for those fixed centers\n  (solving r_i + r_j <= d_ij and r_i <= border_i by fixed-point iteration).\nThis yields a much higher sum of radii than the original naive rings.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n\n    # Helper: compute conservative vertical radius limit for given number of rows\n    sqrt3 = np.sqrt(3.0)\n    def vertical_limit(rows):\n        if rows == 1:\n            return 0.5\n        # top and bottom margins r each: r + (rows-1)*sqrt3*r <= 1 - r\n        # => r * ( (rows-1)*sqrt3 + 2) <= 1\n        return 1.0 / (((rows - 1) * sqrt3) + 2.0)\n\n    # Helper: conservative horizontal limit for a row with m columns.\n    # If the row is offset (shifted by r horizontally), we use a slightly\n    # tighter bound 1/(2m+1), otherwise 1/(2m). These are conservative but safe.\n    def horizontal_limit(m, offset):\n        if offset:\n            return 1.0 / (2 * m + 1.0)\n        else:\n            return 1.0 / (2 * m)\n\n    best_score = -1.0\n    best_layout = None  # (rows, counts_list, offset_start, r_candidate)\n\n    # Try different numbers of rows and simple distributions of 26 items:\n    for rows in range(3, 8):  # 3..7 rows (reasonable for 26)\n        base = n // rows\n        rem = n % rows\n        # Two simple ways to distribute remainder: prefix or suffix\n        for prefix_heavy in (True, False):\n            if prefix_heavy:\n                counts = [base + 1 if i < rem else base for i in range(rows)]\n            else:\n                counts = [base if i < (rows - rem) else base + 1 for i in range(rows)]\n            # Try both choices of whether the first row is offset or not\n            for offset_start in (False, True):\n                # compute conservative r limit\n                r_v = vertical_limit(rows)\n                r_hs = []\n                for i, m in enumerate(counts):\n                    offset = (i % 2 == 1) if offset_start else (i % 2 == 0)\n                    r_hs.append(horizontal_limit(m, offset))\n                r_candidate = min(r_v, min(r_hs))\n                # Score is sum of radii equal to n * r_candidate (uniform)\n                score = n * r_candidate\n                if score > best_score:\n                    best_score = score\n                    best_layout = (rows, counts, offset_start, r_candidate)\n\n    # Build centers from chosen layout\n    rows, counts, offset_start, r = best_layout\n    centers = []\n    vstep = np.sqrt(3.0) * r\n    total_height = 0.0 if rows == 1 else (rows - 1) * vstep\n    y0 = 0.5 - total_height / 2.0\n    for i in range(rows):\n        m = counts[i]\n        offset = (i % 2 == 1) if offset_start else (i % 2 == 0)\n        row_span = 0.0 if m == 1 else (m - 1) * 2.0 * r\n        # centered start\n        start_x = 0.5 - row_span / 2.0\n        if offset:\n            # shift by +r for offset rows (hex packing)\n            start_x += r\n        # ensure numeric safety: clamp start_x so centers lie within [r, 1-r]\n        start_x = max(start_x, r)\n        # but also ensure last center doesn't exceed 1-r; if it does, nudge left\n        last_x = start_x + (m - 1) * 2.0 * r\n        if last_x > 1.0 - r:\n            start_x -= (last_x - (1.0 - r))\n\n        y = y0 + i * vstep\n        for k in range(m):\n            x = start_x + k * 2.0 * r\n            centers.append([x, y])\n\n    centers = np.array(centers, dtype=float)\n    # Defensive: if count mismatch (shouldn't), trim or pad with small-offset positions\n    if centers.shape[0] > n:\n        centers = centers[:n]\n    elif centers.shape[0] < n:\n        # add extra small circles near corners if needed\n        needed = n - centers.shape[0]\n        extras = np.array([[0.02 + 0.96 * (i / max(1, needed - 1)), 0.02] for i in range(needed)])\n        centers = np.vstack((centers, extras))\n\n    # Compute maximal feasible radii for these fixed centers via iterative relaxation\n    radii = compute_max_radii(centers)\n\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers, max_iter=2000, tol=1e-8):\n    \"\"\"\n    Compute maximal radii satisfying:\n        r_i <= border_i\n        r_i + r_j <= dist(i,j)  for all i != j\n\n    We perform a Gauss-Seidel style fixed-point iteration:\n        r_i <- min( border_i, min_j (dist_ij - r_j) )\n    which is monotone and converges to the maximal feasible solution.\n    \"\"\"\n    n = centers.shape[0]\n    # border distances\n    b = np.minimum.reduce([centers[:, 0], centers[:, 1], 1.0 - centers[:, 0], 1.0 - centers[:, 1]])\n    b = np.maximum(b, 0.0)\n\n    # pairwise distances\n    diff = centers[:, None, :] - centers[None, :, :]\n    d = np.sqrt(np.sum(diff * diff, axis=2))\n    # avoid self-dist zeros causing issues; self distance won't be used except as inf\n    np.fill_diagonal(d, np.inf)\n\n    # initialize radii at border distances (feasible relative to walls)\n    rads = b.copy()\n\n    # Iterate: r_i = min(b_i, min_j (d_ij - r_j))\n    for it in range(max_iter):\n        prev = rads.copy()\n        # Gauss-Seidel update: update in-place using newest values immediately\n        for i in range(n):\n            limits = d[i, :] - rads  # d_ij - r_j (using latest r_j)\n            # We must ignore self (set to +inf)\n            limits[i] = np.inf\n            r_new = min(b[i], np.min(limits))\n            # ensure non-negative\n            if r_new < 0.0:\n                r_new = 0.0\n            rads[i] = r_new\n        delta = np.max(np.abs(rads - prev))\n        if delta < tol:\n            break\n\n    # Final small clean: ensure pairwise constraints numerically satisfied\n    # If any tiny violations exist, reduce involved radii proportionally.\n    for i in range(n):\n        for j in range(i + 1, n):\n            dij = d[i, j]\n            if rads[i] + rads[j] > dij + 1e-12:\n                # reduce proportionally\n                scale = dij / (rads[i] + rads[j])\n                rads[i] *= scale\n                rads[j] *= scale\n\n    # clamp by borders again (safety)\n    rads = np.minimum(rads, b)\n    rads = np.maximum(rads, 0.0)\n    return rads\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nUnique approach: Modification: Full rewrite, Excellent validity (1.000), Excellent sum_radii (2.186)\n\n### Inspiration 3 (Score: 0.7293, Type: Alternative)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (improved)\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square.\n    Returns:\n        centers: np.array (26,2)\n        radii: np.array (26,)\n        sum_of_radii: float\n    \"\"\"\n    n = 26\n\n    # Hex-like row pattern that fits 26 circles: row lengths sum to 26\n    rows = [5, 6, 5, 5, 5]  # total 26\n    maxk = max(rows)\n\n    # Choose a reasonable spacing s for hex packing so centers are well-distributed.\n    # This s is a design choice; it affects how large radii can grow after constraints.\n    s = 0.17  # horizontal center spacing\n    h = np.sqrt(3) / 2 * s  # vertical spacing for hex pattern\n\n    centers = []\n    y0 = 0.5 - (len(rows) - 1) * h / 2.0\n    for r_idx, k in enumerate(rows):\n        y = y0 + r_idx * h\n        # center the row horizontally; alternate offset for hex pattern\n        row_width = (k - 1) * s\n        x_start = 0.5 - row_width / 2.0\n        # offset every other row by s/2 for hex packing\n        if r_idx % 2 == 1:\n            x_start += s / 2.0\n        for j in range(k):\n            x = x_start + j * s\n            centers.append([x, y])\n\n    centers = np.array(centers[:n], dtype=float)\n    # Clip to avoid numerical issues at exact border (will be handled by radii)\n    centers = np.clip(centers, 1e-6, 1 - 1e-6)\n\n    radii = compute_max_radii(centers)\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers, tol=1e-8):\n    \"\"\"\n    Iteratively compute maximal non-overlapping radii for given centers in unit square.\n    Starts from wall-limited radii and resolves overlaps by proportional shrinking.\n    Then attempts a few conservative growth steps to increase sum where possible.\n    \"\"\"\n    n = centers.shape[0]\n    # initial radii limited by wall distance\n    wall_dist = np.minimum.reduce([centers[:, 0], centers[:, 1],\n                                   1 - centers[:, 0], 1 - centers[:, 1]])\n    radii = wall_dist.copy()\n\n    # Precompute pairwise distances\n    diffs = centers[:, None, :] - centers[None, :, :]\n    dists = np.sqrt(np.sum(diffs * diffs, axis=2))\n    np.fill_diagonal(dists, np.inf)\n\n    # Shrink overlaps repeatedly until stable\n    for outer in range(2000):\n        changed = False\n        # Enforce wall constraints\n        over_wall = radii - wall_dist\n        if np.any(over_wall > tol):\n            radii = np.minimum(radii, wall_dist)\n            changed = True\n\n        # Pairwise resolution: proportional shrink to remove overlaps\n        for i in range(n):\n            for j in range(i + 1, n):\n                dij = dists[i, j]\n                if not np.isfinite(dij) or dij <= tol:\n                    continue\n                ri, rj = radii[i], radii[j]\n                if ri + rj > dij + tol:\n                    # scale both so ri + rj == dij (proportional)\n                    scale = dij / (ri + rj)\n                    # Avoid numerical issues when one radius is zero\n                    if scale < 1.0 - 1e-12:\n                        radii[i] = ri * scale\n                        radii[j] = rj * scale\n                        changed = True\n        if not changed:\n            break\n\n    # Conservative growth cycles: try small fractional increases, then re-shrink\n    # This helps utilize small slack without violating global constraints.\n    for growth_cycle in range(8):\n        # compute for each circle maximum possible increase without immediate overlap\n        max_inc = np.minimum.reduce([\n            wall_dist - radii,\n            np.min(dists - (radii[None, :] + radii[:, None]), axis=1)\n        ])\n        # clip negatives\n        max_inc = np.maximum(max_inc, 0.0)\n\n        # apply a fraction of allowed increase to avoid flipping many constraints at once\n        delta = 0.25 * max_inc\n        # tiny adjustments only\n        if np.all(delta <= tol):\n            break\n        radii += delta\n\n        # re-run a limited shrink pass to resolve any new overlaps (should be small)\n        for _ in range(200):\n            moved = False\n            for i in range(n):\n                for j in range(i + 1, n):\n                    dij = dists[i, j]\n                    if not np.isfinite(dij) or dij <= tol:\n                        continue\n                    if radii[i] + radii[j] > dij + tol:\n                        scale = dij / (radii[i] + radii[j])\n                        radii[i] *= scale\n                        radii[j] *= scale\n                        moved = True\n            # wall enforcement\n            over = radii - wall_dist\n            if np.any(over > tol):\n                radii = np.minimum(radii, wall_dist)\n                moved = True\n            if not moved:\n                break\n\n    # final safety clamp\n    radii = np.maximum(radii, 0.0)\n    return radii\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, ec=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nUnique approach: Modification: Full rewrite, Excellent validity (1.000), Excellent sum_radii (1.922)\n\n# Current Program\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Improved constructor for packing 26 circles in the unit square.\n\nStrategy:\n- Try several compact, hex-like row patterns (including dense 4-row 6-7-7-6 block).\n- For each pattern compute a best horizontal spacing s that fits inside the square\n  with a small padding. Build hex-offset centers deterministically.\n- For each center-set compute maximal feasible radii for fixed centers using a\n  robust Gauss-Seidel fixed-point solver:\n      r_i <- min( border_i, min_j (d_ij - r_j) )\n  with proportional repairs and a few conservative growth passes.\n- Keep the best layout (largest sum of radii) and attempt a few small deterministic\n  shifts to relieve edge effects.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    sqrt3 = np.sqrt(3.0)\n\n    # Candidate row patterns summing to 26 (varied shapes to mitigate edge effects)\n    patterns = [\n        [6, 7, 7, 6],       # very compact block (likely best)\n        [7, 6, 6, 7],       # symmetric variant\n        [6, 5, 5, 5, 5],    # 5-row hex-like\n        [5, 6, 5, 6, 4],    # alternating\n        [5, 6, 6, 5, 4],    # another balanced layout\n    ]\n\n    best_total = -1.0\n    best_centers = None\n    best_radii = None\n\n    # small padding from the square boundary to give numerical slack\n    pad = 0.006\n\n    for rows in patterns:\n        R = len(rows)\n        max_cols = max(rows)\n        # horizontal spacing bound so widest row fits\n        s_by_width = (1.0 - 2.0 * pad) / (max_cols - 1) if max_cols > 1 else 1.0\n        # vertical spacing bound so rows fit (via hex relation)\n        if R > 1:\n            h_step = (1.0 - 2.0 * pad) / (R - 1)\n            s_by_height = (2.0 / sqrt3) * h_step\n        else:\n            s_by_height = s_by_width\n        s = min(s_by_width, s_by_height)\n        # clamp s to reasonable bounds\n        s = max(min(s, 0.20), 0.10)\n\n        h = sqrt3 / 2.0 * s\n        total_h = (R - 1) * h if R > 1 else 0.0\n        y0 = pad + (1.0 - 2.0 * pad - total_h) / 2.0\n\n        centers = []\n        for i, cnt in enumerate(rows):\n            y = y0 + i * h\n            row_width = (cnt - 1) * s if cnt > 1 else 0.0\n            x_start = pad + (1.0 - 2.0 * pad - row_width) / 2.0\n            # offset alternate rows for hex adjacency\n            if (i % 2) == 1:\n                x_start += s / 2.0\n            for k in range(cnt):\n                x = x_start + k * s\n                centers.append([x, y])\n        centers = np.array(centers[:n], dtype=float)\n        # ensure strictly inside with small margin\n        centers = np.clip(centers, pad, 1.0 - pad)\n\n        radii = compute_max_radii(centers)\n        total = float(np.sum(radii))\n        if total > best_total:\n            best_total = total\n            best_centers = centers.copy()\n            best_radii = radii.copy()\n\n    # Try small deterministic shifts around the best layout to mitigate boundary locking\n    if best_centers is None:\n        # fallback simple pattern (should not happen)\n        best_centers = np.array([[0.5, 0.5 + 0.01 * i] for i in range(n)])\n        best_radii = compute_max_radii(best_centers)\n        best_total = float(np.sum(best_radii))\n    else:\n        shifts = [(0.0, 0.0), (0.006, 0.0), (-0.006, 0.0), (0.0, 0.006), (0.0, -0.006),\n                  (0.009, 0.009), (-0.009, -0.009)]\n        for dx, dy in shifts:\n            cand = np.clip(best_centers + np.array([dx, dy]), 1e-8, 1.0 - 1e-8)\n            r_cand = compute_max_radii(cand)\n            tot = float(np.sum(r_cand))\n            if tot > best_total + 1e-12:\n                best_total = tot\n                best_centers = cand.copy()\n                best_radii = r_cand.copy()\n\n    # Final safety: ensure exactly n centers\n    if best_centers.shape[0] > n:\n        best_centers = best_centers[:n]\n        best_radii = best_radii[:n]\n    elif best_centers.shape[0] < n:\n        need = n - best_centers.shape[0]\n        extra = np.array([[0.5, 0.02 + 0.96 * (i / max(1, need - 1))] for i in range(need)])\n        best_centers = np.vstack((best_centers, extra))\n        best_radii = compute_max_radii(best_centers)\n        best_total = float(np.sum(best_radii))\n\n    return best_centers, best_radii, float(np.sum(best_radii))\n\n\ndef compute_max_radii(centers, max_iter=4000, tol=1e-9):\n    \"\"\"\n    Gauss-Seidel fixed-point solver for maximal radii with fixed centers inside unit square.\n    r_i <- min(border_i, min_j (d_ij - r_j))\n    Monotone in-place updates converge to the maximal feasible radii.\n    After convergence apply proportional repairs for numerical safety and a couple of\n    conservative growth cycles to capture slack.\n    \"\"\"\n    n = centers.shape[0]\n    if n == 0:\n        return np.array([])\n\n    # distance to borders\n    b = np.minimum.reduce([centers[:, 0], centers[:, 1], 1.0 - centers[:, 0], 1.0 - centers[:, 1]])\n    b = np.maximum(b, 0.0)\n\n    # pairwise distances\n    diff = centers[:, None, :] - centers[None, :, :]\n    d = np.sqrt(np.sum(diff * diff, axis=2))\n    np.fill_diagonal(d, np.inf)\n\n    # initialize radii at border distances (feasible)\n    rads = b.copy()\n\n    # Gauss-Seidel fixed-point iterations\n    for it in range(max_iter):\n        prev = rads.copy()\n        # update in-place for monotone convergence\n        for i in range(n):\n            # bounds by neighbors (d_ij - r_j)\n            limits = d[i, :] - rads\n            limits[i] = np.inf\n            r_new = min(b[i], np.min(limits))\n            rads[i] = r_new if r_new > 0.0 else 0.0\n        if np.max(np.abs(rads - prev)) < tol:\n            break\n\n    # Proportional repair for tiny violations\n    for i in range(n):\n        for j in range(i + 1, n):\n            dij = d[i, j]\n            if rads[i] + rads[j] > dij + 1e-12:\n                scale = dij / (rads[i] + rads[j])\n                rads[i] *= scale\n                rads[j] *= scale\n\n    # A few conservative growth cycles to exploit slack (apply fractions)\n    for cycle in range(6):\n        neighbor_bound = np.min(d - rads[None, :], axis=1)\n        allowed = np.minimum(b, neighbor_bound)\n        inc = np.maximum(allowed - rads, 0.0)\n        if np.all(inc <= 1e-12):\n            break\n        # apply small fraction to avoid global conflicts\n        rads += 0.33 * inc\n        # re-shrink small number of times to maintain feasibility\n        for _ in range(200):\n            moved = False\n            # enforce walls\n            if np.any(rads > b + 1e-12):\n                rads = np.minimum(rads, b)\n                moved = True\n            for i in range(n):\n                for j in range(i + 1, n):\n                    dij = d[i, j]\n                    if rads[i] + rads[j] > dij + 1e-12:\n                        scale = dij / (rads[i] + rads[j])\n                        rads[i] *= scale\n                        rads[j] *= scale\n                        moved = True\n            if not moved:\n                break\n\n    rads = np.maximum(rads, 0.0)\n    return rads\n\n# EVOLVE-BLOCK-END\n\n\n# unchanged runner and visualizer API\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # visualize(centers, radii)\n```\n\n# Task\nRewrite the program to improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: complexity, diversity\nDifferent solutions with similar fitness but different features are valuable.\nProvide the complete new program code.\n\nIMPORTANT: Make sure your rewritten program maintains the same inputs and outputs\nas the original program, but with improved internal implementation.\n\n```python\n# Your rewritten program here\n```", "responses": ["```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for packing 26 circles in the unit square.\n\nStrategy (compact and robust):\n- Enumerate several hex-like row patterns that partition 26 (including compact 4-row\n  blocks and 5-row hex-like patterns).\n- For each pattern, sweep a small grid of horizontal spacings and mild vertical\n  scale factors to create candidate center sets (hex offsets applied).\n- For each candidate set compute maximal feasible radii for fixed centers using a\n  robust Gauss-Seidel fixed-point solver with proportional repairs and conservative\n  growth cycles to exploit slack.\n- Keep the best layout (largest sum of radii). A few small deterministic shifts\n  around promising candidates are also tried to relieve edge-locking.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    sqrt3 = np.sqrt(3.0)\n\n    # Candidate row patterns summing to 26 (mix of compact and slightly stretched shapes)\n    patterns = [\n        [6, 7, 7, 6],     # compact 4-row block\n        [7, 6, 6, 7],     # symmetric variant\n        [6, 5, 5, 5, 5],  # 5-row hex-like\n        [5, 6, 5, 6, 4],\n        [6, 5, 6, 5, 4],\n        [5, 6, 6, 5, 4],\n        [5, 5, 6, 5, 5],\n        [6, 5, 5, 5, 5],  # another hex-like\n    ]\n\n    # grid of horizontal spacings (center-to-center) and slight vertical scalings\n    s_vals = np.linspace(0.12, 0.20, 21)\n    y_scales = [0.95, 0.98, 1.0, 1.02, 1.05]\n\n    pad = 0.006  # small padding from square boundary for numerical slack\n\n    best_sum = -1.0\n    best = None\n\n    for pattern in patterns:\n        R = len(pattern)\n        max_cols = max(pattern)\n        for s in s_vals:\n            # compute vertical step from hex geometry, scaled later\n            v0 = (np.sqrt(3) / 2.0) * s\n            for ys in y_scales:\n                v = v0 * ys\n                # compute block height and vertical start (centered inside [pad,1-pad])\n                total_h = (R - 1) * v if R > 1 else 0.0\n                y0 = pad + (1.0 - 2.0 * pad - total_h) / 2.0\n                for offset_start in (False, True):\n                    centers = []\n                    for r_idx, cnt in enumerate(pattern):\n                        y = y0 + r_idx * v\n                        row_width = (cnt - 1) * s if cnt > 1 else 0.0\n                        x_start = pad + (1.0 - 2.0 * pad - row_width) / 2.0\n                        # stagger alternate rows for hex-like adjacency\n                        offset = (r_idx % 2 == 1) if offset_start else (r_idx % 2 == 0)\n                        if offset:\n                            x_start += s / 2.0\n                        for j in range(cnt):\n                            centers.append([x_start + j * s, y])\n                    centers = np.array(centers[:n], dtype=float)\n                    # clip slightly inside boundaries\n                    centers = np.clip(centers, pad, 1.0 - pad)\n\n                    # try a few small deterministic shifts to reduce boundary locking\n                    shifts = [(0.0, 0.0), (0.006, 0.0), (-0.006, 0.0), (0.0, 0.006), (0.0, -0.006)]\n                    for dx, dy in shifts:\n                        cand = np.clip(centers + np.array([dx, dy]), 1e-9, 1.0 - 1e-9)\n                        radii = compute_max_radii(cand)\n                        ssum = float(np.sum(radii))\n                        if ssum > best_sum + 1e-12:\n                            best_sum = ssum\n                            best = (cand.copy(), radii.copy(), ssum)\n\n    # Fallback (shouldn't be needed)\n    if best is None:\n        centers = np.array([[0.5, 0.5 + 0.01 * i] for i in range(n)], dtype=float)\n        radii = compute_max_radii(centers)\n        best = (centers, radii, float(np.sum(radii)))\n\n    return best\n\ndef compute_max_radii(centers, tol=1e-9, max_iter=3000):\n    \"\"\"\n    Compute maximal feasible radii for fixed centers in the unit square.\n\n    Uses Gauss-Seidel fixed-point iterations:\n        r_i <- min(border_i, min_j (d_ij - r_j))\n    followed by proportional repairs and several conservative growth cycles to\n    increase radii when slack is available without creating overlaps.\n    \"\"\"\n    centers = np.asarray(centers, dtype=float)\n    n = centers.shape[0]\n    if n == 0:\n        return np.array([])\n\n    # border distances\n    border = np.minimum.reduce([centers[:, 0], centers[:, 1], 1.0 - centers[:, 0], 1.0 - centers[:, 1]])\n    border = np.maximum(border, 0.0)\n\n    # pairwise distances matrix\n    dif = centers[:, None, :] - centers[None, :, :]\n    d = np.sqrt(np.sum(dif * dif, axis=2))\n    np.fill_diagonal(d, np.inf)\n\n    # initialize radii conservatively to border distances\n    r = border.copy()\n\n    # initial proportional shrink pass to remove any overlaps (start feasible)\n    for _ in range(800):\n        moved = False\n        # enforce walls\n        over = r - border\n        if np.any(over > tol):\n            r = np.minimum(r, border)\n            moved = True\n        # pairwise proportional shrink\n        for i in range(n):\n            ri = r[i]\n            if ri <= 0:\n                continue\n            for j in range(i + 1, n):\n                dij = d[i, j]\n                if not np.isfinite(dij) or dij <= tol:\n                    continue\n                rj = r[j]\n                if ri + rj > dij + tol:\n                    denom = ri + rj\n                    if denom <= 0:\n                        continue\n                    scale = dij / denom\n                    if scale < 1.0 - 1e-12:\n                        r[i] = ri * scale\n                        r[j] = rj * scale\n                        ri = r[i]\n                        moved = True\n        if not moved:\n            break\n\n    # Gauss-Seidel fixed point to converge to maximal feasible radii\n    for it in range(max_iter):\n        prev = r.copy()\n        for i in range(n):\n            limits = d[i, :] - r\n            limits[i] = np.inf\n            val = np.min(limits)\n            r[i] = val if val < border[i] else border[i]\n            if r[i] < 0.0:\n                r[i] = 0.0\n        if np.max(np.abs(r - prev)) < tol:\n            break\n\n    # proportional repairs for tiny violations\n    for i in range(n):\n        for j in range(i + 1, n):\n            dij = d[i, j]\n            if r[i] + r[j] > dij + 1e-12:\n                scale = dij / (r[i] + r[j])\n                r[i] *= scale\n                r[j] *= scale\n\n    # conservative growth cycles: increase a fraction of allowed slack, then re-fix\n    for cycle in range(8):\n        slack_wall = border - r\n        # pairwise slack: for each i, min_j (d_ij - (r_i + r_j))\n        pair_slack = np.min(d - (r[None, :] + r[:, None]), axis=1)\n        allowed_inc = np.minimum(slack_wall, pair_slack)\n        allowed_inc = np.maximum(allowed_inc, 0.0)\n        delta = 0.30 * allowed_inc\n        if np.all(delta <= tol):\n            break\n        r += delta\n        # re-fix any small violations with a few proportional shrink iterations\n        for _ in range(250):\n            moved = False\n            # walls\n            if np.any(r > border + 1e-12):\n                r = np.minimum(r, border)\n                moved = True\n            for i in range(n):\n                ri = r[i]\n                if ri <= 0:\n                    continue\n                for j in range(i + 1, n):\n                    dij = d[i, j]\n                    if not np.isfinite(dij):\n                        continue\n                    if r[i] + r[j] > dij + 1e-12:\n                        scale = dij / (r[i] + r[j])\n                        r[i] *= scale\n                        r[j] *= scale\n                        moved = True\n            if not moved:\n                break\n\n    # final safety clamp and non-negativity\n    r = np.maximum(np.minimum(r, border), 0.0)\n    return r\n\n# EVOLVE-BLOCK-END\n\n# unchanged runner API\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n# optional visualizer (unchanged API)\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(7, 7))\n    ax.set_xlim(0, 1); ax.set_ylim(0, 1); ax.set_aspect('equal')\n    for c, rr in zip(centers, radii):\n        ax.add_patch(Circle(c, rr, alpha=0.6, edgecolor='k'))\n    plt.title(f\"n={len(centers)}, sum radii={sum(radii):.6f}\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, s = run_packing()\n    print(f\"Sum of radii: {s:.6f}\")\n    # visualize(centers, radii)\n```"]}}, "artifacts_json": "{\"stdout\": \"Running in subprocess, Python version: 3.12.1 (v3.12.1:2305ca5144, Dec  7 2023, 17:23:38) [Clang 13.0.0 (clang-1300.0.29.30)]\\nProgram path: /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmp42kw1p3l.py\\nCalling run_packing()...\\nrun_packing() returned successfully: sum_radii = 2.2354896138119105\\nResults saved to /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmp6ghsr3ek.py.results\\n\", \"stderr\": \"\"}", "artifact_dir": null}