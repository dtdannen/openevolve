{"id": "1f2acb89-65c5-482f-8b9a-9740395dd1f3", "code": "# EVOLVE-BLOCK-START\n\"\"\"Deterministic explicit constructor for packing n=26 circles in the unit square.\n\nThis version improves on prior attempts by:\n- Trying a compact family of asymmetric hex-like row patterns that sum to 26.\n- Scanning a fine grid of horizontal spacings and slight vertical scalings.\n- Trying both stagger parities and small global shifts to exploit edge slack.\n- Computing maximal feasible radii for fixed centers using a robust procedure:\n    * initialize at wall-distances\n    * proportional shrink passes to remove overlaps\n    * Gauss-Seidel fixed-point to converge to a feasible maximal set\n    * cautious growth cycles with proportional repairs to capture slack\nAll steps are deterministic and constructor-only (no random search).\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    # Diverse plausible row patterns summing to 26 (break symmetry to help edges)\n    patterns = [\n        [5, 6, 5, 5, 5],\n        [6, 5, 5, 5, 5],\n        [6, 5, 6, 5, 4],\n        [5, 6, 6, 5, 4],\n        [4, 6, 6, 5, 5],\n    ]\n\n    # search parameters tuned for good coverage but modest eval time\n    s_vals = np.linspace(0.125, 0.195, 30)   # horizontal center-to-center spacing\n    y_scales = [0.94, 0.98, 1.00, 1.02]      # slight vertical compression/expansion\n    shifts = [(0.0, 0.0), (0.006, 0.0), (-0.006, 0.0), (0.0, 0.006), (0.0, -0.006)]\n\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    for pattern in patterns:\n        rows = len(pattern)\n        max_cols = max(pattern)\n        for s in s_vals:\n            # ideal hex vertical step for spacing s\n            v0 = (np.sqrt(3.0) / 2.0) * s\n            for y_scale in y_scales:\n                v = v0 * y_scale\n                # compute total vertical span and center block vertically\n                total_h = (rows - 1) * v if rows > 1 else 0.0\n                if total_h > 1.0 + 1e-12:\n                    continue\n                y0 = 0.5 - total_h / 2.0\n                for offset_start in (False, True):\n                    centers = []\n                    for r_idx, cnt in enumerate(pattern):\n                        y = y0 + r_idx * v\n                        row_span = (cnt - 1) * s if cnt > 1 else 0.0\n                        x_start = 0.5 - row_span / 2.0\n                        offset = (r_idx % 2 == 1) if offset_start else (r_idx % 2 == 0)\n                        if offset:\n                            x_start += s / 2.0\n                        # ensure row stays inside [0,1] by tiny shift if necessary\n                        last_x = x_start + (cnt - 1) * s\n                        if x_start < 0.0:\n                            x_start = 0.0\n                        if last_x > 1.0:\n                            x_start -= (last_x - 1.0)\n                        for j in range(cnt):\n                            centers.append([x_start + j * s, y])\n                    # trim/pad to exactly n deterministically\n                    centers = np.array(centers, dtype=float)\n                    if centers.shape[0] < n:\n                        # pad with small positions along bottom-left corner\n                        needed = n - centers.shape[0]\n                        extra_xs = np.linspace(0.02, 0.18, needed)\n                        extras = np.vstack([extra_xs, np.full(needed, 0.02)]).T\n                        centers = np.vstack((centers, extras))\n                    elif centers.shape[0] > n:\n                        centers = centers[:n]\n\n                    # try small global shifts to exploit slack near walls\n                    for dx, dy in shifts:\n                        cand = centers + np.array([dx, dy])\n                        cand = np.clip(cand, 1e-9, 1.0 - 1e-9)\n                        radii = compute_max_radii(cand)\n                        ssum = float(np.sum(radii))\n                        if ssum > best_sum + 1e-12:\n                            best_sum = ssum\n                            best_centers = cand.copy()\n                            best_radii = radii.copy()\n\n    # fallback single layout (should not be needed)\n    if best_centers is None:\n        rows = [5, 6, 5, 5, 5]\n        s = 0.16\n        v = (np.sqrt(3.0) / 2.0) * s\n        y0 = 0.5 - (len(rows) - 1) * v / 2.0\n        centers = []\n        for r_idx, cnt in enumerate(rows):\n            y = y0 + r_idx * v\n            row_span = (cnt - 1) * s if cnt > 1 else 0.0\n            x_start = 0.5 - row_span / 2.0\n            if r_idx % 2 == 1:\n                x_start += s / 2.0\n            for j in range(cnt):\n                centers.append([x_start + j * s, y])\n        centers = np.clip(np.array(centers[:n], dtype=float), 1e-9, 1.0 - 1e-9)\n        best_centers = centers\n        best_radii = compute_max_radii(centers)\n        best_sum = float(np.sum(best_radii))\n\n    return best_centers, best_radii, float(best_sum)\n\n\ndef compute_max_radii(centers, tol=1e-10, max_iter=3000):\n    \"\"\"\n    Robust deterministic procedure to compute near-maximal radii for fixed centers:\n      - initialize at border distances\n      - proportional shrink passes to remove overlaps\n      - Gauss-Seidel fixed-point to converge\n      - cautious growth cycles with proportional repairs to capture slack\n    \"\"\"\n    centers = np.asarray(centers, dtype=float)\n    n = centers.shape[0]\n    if n == 0:\n        return np.array([], dtype=float)\n\n    # distances to walls\n    border = np.minimum.reduce([centers[:, 0], centers[:, 1], 1.0 - centers[:, 0], 1.0 - centers[:, 1]])\n    border = np.maximum(border, 0.0)\n\n    # pairwise distances\n    dif = centers[:, None, :] - centers[None, :, :]\n    d = np.sqrt(np.sum(dif * dif, axis=2))\n    np.fill_diagonal(d, np.inf)\n\n    # initialize radii at border distances (feasible)\n    r = border.copy()\n\n    # Initial proportional shrink to remove any overlaps caused by numerical issues\n    for _ in range(1000):\n        moved = False\n        # enforce walls\n        over = r - border\n        if np.any(over > 0):\n            r = np.minimum(r, border)\n            moved = True\n        # pairwise proportional fixes\n        for i in range(n):\n            ri = r[i]\n            for j in range(i + 1, n):\n                dij = d[i, j]\n                if not np.isfinite(dij) or dij <= 0.0:\n                    continue\n                rj = r[j]\n                if ri + rj > dij + 1e-12:\n                    total = ri + rj\n                    if total <= 0.0:\n                        r[i] = r[j] = 0.0\n                        moved = True\n                        continue\n                    scale = dij / total\n                    # apply scale only if it reduces\n                    if scale < 1.0 - 1e-14:\n                        r[i] = ri * scale\n                        r[j] = rj * scale\n                        ri = r[i]\n                        moved = True\n        if not moved:\n            break\n\n    # Gauss-Seidel fixed-point to approach maximal feasible radii\n    for it in range(max_iter):\n        prev = r.copy()\n        for i in range(n):\n            limits = d[i, :] - r\n            limits[i] = np.inf\n            val = np.min(limits)\n            # enforce wall limit\n            if val > border[i]:\n                val = border[i]\n            if val < 0.0:\n                val = 0.0\n            r[i] = val\n        if np.max(np.abs(r - prev)) < tol:\n            break\n\n    # Conservative growth cycles: use small fraction of slack, then repair\n    for cycle in range(10):\n        # slack to walls\n        slack_wall = border - r\n        # slack to neighbors: for each i, min_j (d_ij - (r_i + r_j))\n        slack_pair = np.min(d - (r[None, :] + r[:, None]), axis=1)\n        slack_pair = np.maximum(slack_pair, 0.0)\n        allowed_inc = np.minimum(slack_wall, slack_pair)\n        allowed_inc = np.maximum(allowed_inc, 0.0)\n        delta = 0.22 * allowed_inc  # cautious fraction\n        if np.all(delta <= 1e-12):\n            break\n        r += delta\n\n        # proportional repair loops to restore feasibility\n        for _ in range(300):\n            moved = False\n            # pairwise check\n            for i in range(n):\n                for j in range(i + 1, n):\n                    dij = d[i, j]\n                    if not np.isfinite(dij):\n                        continue\n                    if r[i] + r[j] > dij + 1e-12:\n                        total = r[i] + r[j]\n                        if total <= 0.0:\n                            r[i] = r[j] = 0.0\n                            moved = True\n                            continue\n                        scale = dij / total\n                        r[i] *= scale\n                        r[j] *= scale\n                        moved = True\n            # walls\n            over = r - border\n            if np.any(over > 1e-12):\n                r = np.minimum(r, border)\n                moved = True\n            if not moved:\n                break\n\n    # final safety clamps\n    r = np.maximum(np.minimum(r, border), 0.0)\n    return r\n\n# EVOLVE-BLOCK-END\n\n\n# fixed runner API\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(7, 7))\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    for c, rr in zip(centers, radii):\n        ax.add_patch(Circle(c, rr, alpha=0.6, edgecolor=\"k\"))\n    plt.title(f\"n={len(centers)}, sum radii={sum(radii):.6f}\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, s = run_packing()\n    print(f\"Sum of radii: {s:.6f}\")\n    # To visualize, uncomment:\n    # visualize(centers, radii)", "language": "python", "parent_id": "258ab555-38b8-48b5-8bcc-21f937d27824", "generation": 3, "timestamp": 1759960528.183887, "iteration_found": 36, "metrics": {"validity": 1.0, "sum_radii": 2.2769021808649645, "target_ratio": 0.8640994993794933, "combined_score": 0.8640994993794933, "eval_time": 3.0955970287323}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Full rewrite", "parent_metrics": {"validity": 1.0, "sum_radii": 2.0172599393008572, "target_ratio": 0.7655635443267012, "combined_score": 0.7655635443267012, "eval_time": 0.07656502723693848}, "island": 0}, "prompts": {"full_rewrite_user": {"system": "You are an expert mathematician specializing in circle packing problems and computational geometry. Your task is to improve a constructor function that directly produces a specific arrangement of 26 circles in a unit square, maximizing the sum of their radii. The AlphaEvolve paper achieved a sum of 2.635 for n=26.\n\nKey geometric insights:\n- Circle packings often follow hexagonal patterns in the densest regions\n- Maximum density for infinite circle packing is pi/(2*sqrt(3)) \u2248 0.9069\n- Edge effects make square container packing harder than infinite packing\n- Circles can be placed in layers or shells when confined to a square\n- Similar radius circles often form regular patterns, while varied radii allow better space utilization\n- Perfect symmetry may not yield the optimal packing due to edge effects\n\nFocus on designing an explicit constructor that places each circle in a specific position, rather than an iterative search algorithm.\n\n\n## Evolution Insights (Global Learnings)\n\n### Common Pitfalls:\n\u26a0\ufe0f This is a real win\u2014eval_time dropped 55%. To preserve it, add a CI microbenchmark that records eval_time and target_ratio and fails on regressions (e.g., >10% slower or target_ratio below the current baseline), and commit the profiling artifacts or notes describing the specific optimization (caching/vectorization/IO change) so future reviewers can avoid undoing it. (seen 3x)\n\u26a0\ufe0f Check recent changes to the radii calculation and upstream inputs for unintended scaling/clamping, integer division, coordinate-transform bugs or new filters that drop items \u2014 reproduce the regression with per-item debug logs or diffs to locate where radii mass was lost. Then add a regression test (e.g., assert sum_radii does not drop >20% vs baseline) and telemetry/alerts for large decreases so future commits are caught quickly. (seen 1x)\n\u26a0\ufe0f Reproduce the regression with the exact inputs/seeds and run a git-bisect to find the offending change, then inspect code paths that compute radii/target_ratio for recent edits\u2014look specifically for unintended clamping, normalization/scale-factor changes, unit conversions, or new filtering that would shrink sum_radii. Add an automated regression test that fails if target_ratio or sum_radii drops more than an acceptable threshold (e.g., 20%), add alerts for metric regressions, and add defensive checks/assertions to prevent inadvertent zeroing or aggressive scaling of radii. (seen 1x)\n\u26a0\ufe0f Bisect recent commits to find the change that caused sum_radii and target_ratio to drop, then add a CI regression test/benchmark that asserts combined_score and sum_radii remain within, e.g., 10% of a validated baseline so similar regressions fail fast. When you inspect the offending change, focus on any alterations to radii calculation, normalization/scaling (watch for unintended integer division or parameter/name changes), and add targeted profiling or assertions to ensure the normalization step still runs. (seen 1x)", "user": "# Current Program Information\n- Fitness: 0.7656\n- Feature coordinates: No feature coordinates\n- Focus areas: - Fitness declined: 0.8501 \u2192 0.7656. Consider revising recent changes.\n- Consider simplifying - code length exceeds 500 characters\n\n## Last Execution Output\n\n### stdout\n```\nRunning in subprocess, Python version: 3.12.1 (v3.12.1:2305ca5144, Dec  7 2023, 17:23:38) [Clang 13.0.0 (clang-1300.0.29.30)]\nProgram path: /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmp218c0xq8.py\nCalling run_packing()...\nrun_packing() returned successfully: sum_radii = 2.0172599393008572\nResults saved to /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmputkecgds.py.results\n\n```\n\n### stderr\n```\n\n```\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 3\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.2400, target_ratio: 0.8501, combined_score: 0.8501, eval_time: 2.1468\n- Outcome: Mixed results\n\n### Attempt 2\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.2660, target_ratio: 0.8600, combined_score: 0.8600, eval_time: 0.1256\n- Outcome: Mixed results\n\n### Attempt 1\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.2742, target_ratio: 0.8631, combined_score: 0.8631, eval_time: 1.8737\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.8631)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Improved explicit constructor for n=26 circle packing in the unit square.\n\nThis version:\n- Enumerates several hex-like row patterns and small variations (row offsets,\n  slight vertical compression/expansion, small translations).\n- For each candidate it places deterministic centers and computes maximal\n  feasible radii using a robust Gauss-Seidel projection followed by\n  conservative growth cycles and proportional fixes.\n- Returns the best layout found (max sum of radii).\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    # candidate row patterns summing to 26, chosen to break perfect symmetry\n    patterns = [\n        [6,5,5,5,5],\n        [5,6,5,5,5],\n        [6,5,6,5,4],\n        [5,6,6,5,4],\n        [4,6,6,5,5],\n    ]\n\n    # try a grid of horizontal spacings and small vertical scalings\n    s_vals = np.linspace(0.12, 0.20, 17)\n    y_scales = [0.94, 0.98, 1.0, 1.02]\n    best = None\n    best_sum = -1.0\n\n    for pattern in patterns:\n        rows = len(pattern)\n        max_cols = max(pattern)\n        for s in s_vals:\n            # compute nominal vertical step for hex pattern then apply small scales\n            v0 = (np.sqrt(3)/2.0) * s\n            for y_scale in y_scales:\n                v = v0 * y_scale\n                # Vertical centering\n                total_h = (rows - 1) * v\n                y0 = 0.5 - total_h / 2.0\n                # try both offset parity choices (first row offset or not)\n                for offset_start in (False, True):\n                    centers = []\n                    for r_idx, cnt in enumerate(pattern):\n                        y = y0 + r_idx * v\n                        row_width = (cnt - 1) * s if cnt > 1 else 0.0\n                        x_start = 0.5 - row_width / 2.0\n                        # determine offset for this row (hex-like)\n                        offset = (r_idx % 2 == 1) if offset_start else (r_idx % 2 == 0)\n                        if offset:\n                            x_start += s/2.0\n                        for j in range(cnt):\n                            centers.append([x_start + j * s, y])\n                    centers = np.array(centers[:n], dtype=float)\n                    # small deterministic shifts to help edge packing\n                    for dx,dy in ((0,0),(+0.006,0),( -0.006,0),(0,+0.006),(0,-0.006 )):\n                        cand = centers + np.array([dx,dy])\n                        cand = np.clip(cand, 1e-8, 1-1e-8)\n                        radii = compute_max_radii(cand)\n                        ssum = float(np.sum(radii))\n                        if ssum > best_sum:\n                            best_sum = ssum\n                            best = (cand, radii, ssum)\n\n    # fallback: if nothing found (shouldn't), return a central cluster\n    if best is None:\n        centers = np.array([[0.5,0.5]]*n)\n        radii = compute_max_radii(centers)\n        return centers, radii, float(np.sum(radii))\n\n    return best\n\ndef compute_max_radii(centers, tol=1e-9, max_iter=3000):\n    \"\"\"\n    Gauss-Seidel fixed point: r_i <- min(border_i, min_j (d_ij - r_j))\n    Then perform conservative growth cycles and proportional fixes to\n    exploit slack without creating overlaps.\n    \"\"\"\n    centers = np.asarray(centers, dtype=float)\n    n = centers.shape[0]\n    # border limits\n    border = np.minimum.reduce([centers[:,0], centers[:,1], 1-centers[:,0], 1-centers[:,1]])\n    border = np.maximum(border, 0.0)\n\n    # pairwise distances\n    dif = centers[:,None,:] - centers[None,:,:]\n    d = np.sqrt(np.sum(dif*dif, axis=2))\n    np.fill_diagonal(d, np.inf)\n\n    # initialize radii conservatively at border distance\n    r = border.copy()\n\n    # initial shrink pass to remove overlaps (proportional scaling)\n    for _ in range(1200):\n        moved = False\n        # wall constraints\n        over = r - border\n        if np.any(over > tol):\n            r = np.minimum(r, border)\n            moved = True\n        # pairwise resolution\n        for i in range(n):\n            ri = r[i]\n            for j in range(i+1, n):\n                dij = d[i,j]\n                if not np.isfinite(dij) or dij <= tol:\n                    continue\n                rj = r[j]\n                if ri + rj > dij + tol:\n                    # scale both proportionally (if both > 0)\n                    denom = ri + rj\n                    if denom <= 0:\n                        # if both zero skip\n                        continue\n                    scale = dij / denom\n                    if scale < 1.0 - 1e-12:\n                        r[i] = ri * scale\n                        r[j] = rj * scale\n                        ri = r[i]\n                        moved = True\n        if not moved:\n            break\n\n    # Gauss-Seidel fixed-point to converge to maximal feasible radii\n    for it in range(max_iter):\n        prev = r.copy()\n        for i in range(n):\n            limits = d[i,:] - r\n            limits[i] = np.inf\n            # min over neighbor limits and border\n            val = min(border[i], np.min(limits))\n            r[i] = max(val, 0.0)\n        if np.max(np.abs(r - prev)) < tol:\n            break\n\n    # Conservative growth cycles: increase a fraction of slack, then re-fix\n    for cycle in range(8):\n        # slack by walls and neighbors\n        slack_wall = border - r\n        slack_pair = np.min(d - (r[None,:] + r[:,None]), axis=1)\n        slack_pair = np.maximum(slack_pair, 0.0)\n        allowed_inc = np.minimum(slack_wall, slack_pair)\n        allowed_inc = np.maximum(allowed_inc, 0.0)\n        delta = 0.25 * allowed_inc\n        if np.all(delta <= tol):\n            break\n        r += delta\n        # quick proportional fix to restore feasibility\n        for _ in range(300):\n            moved = False\n            # pairwise check\n            for i in range(n):\n                for j in range(i+1, n):\n                    dij = d[i,j]\n                    if not np.isfinite(dij):\n                        continue\n                    if r[i] + r[j] > dij + 1e-12:\n                        scale = dij / (r[i] + r[j])\n                        r[i] *= scale\n                        r[j] *= scale\n                        moved = True\n            # wall enforcement\n            over = r - border\n            if np.any(over > 1e-12):\n                r = np.minimum(r, border)\n                moved = True\n            if not moved:\n                break\n\n    # final safety clamp and non-negativity\n    r = np.maximum(np.minimum(r, border), 0.0)\n    return r\n\n# EVOLVE-BLOCK-END\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\", linewidth=0.6)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.2742), Performs well on target_ratio (0.8631), Performs well on combined_score (0.8631), Performs well on eval_time (1.8737)\n\n### Program 2 (Score: 0.8600)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for packing 26 circles in the unit square.\n\nStrategy:\n- Try a small family of hex-like row patterns that sum to 26.\n- For each pattern scan a modest grid of horizontal spacings s (center-to-center).\n- Build centers (staggered rows for hex packing), clamp inside (tiny margin).\n- For each candidate center set compute maximal feasible radii using a\n  Gauss-Seidel fixed-point iteration:\n     r_i <- min( wall_i, min_j (d_ij - r_j) )\n  which converges to the maximal radii for fixed centers.\n- Return the best (centers, radii) that maximize sum(radii).\n\"\"\"\nimport numpy as np\nfrom math import sqrt\n\ndef construct_packing():\n    n = 26\n    # plausible hex-like row patterns summing to 26\n    patterns = [\n        [5,6,5,5,5],\n        [6,5,5,5,5],\n        [6,5,6,5,4],\n        [5,6,6,5,4],\n        [4,6,6,5,5],\n    ]\n\n    best = None\n    best_sum = -1.0\n\n    # search horizontal center-to-center spacing s\n    s_vals = np.linspace(0.12, 0.20, 36)\n\n    for rows in patterns:\n        R = len(rows)\n        for s in s_vals:\n            h = sqrt(3.0)/2.0 * s  # vertical step for hex arrangement when horizontal step = s\n            total_h = (R - 1) * h\n            y0 = 0.5 - total_h/2.0\n            centers = []\n            for r_idx, k in enumerate(rows):\n                y = y0 + r_idx * h\n                row_width = (k - 1) * s if k > 1 else 0.0\n                x_start = 0.5 - row_width/2.0\n                # stagger alternate rows\n                if r_idx % 2 == 1:\n                    x_start += s/2.0\n                for j in range(k):\n                    centers.append([x_start + j*s, y])\n            centers = np.array(centers[:n], dtype=float)\n            # ensure strictly inside unit square to avoid zero wall distances\n            centers = np.clip(centers, 1e-8, 1.0 - 1e-8)\n\n            radii = compute_max_radii(centers)\n            ssum = float(np.sum(radii))\n            if ssum > best_sum:\n                best_sum = ssum\n                best = (centers.copy(), radii.copy(), ssum)\n\n    # fallback: if nothing found (very unlikely), return a simple centered arrangement\n    if best is None:\n        centers = np.array([[0.5,0.5]]*n)\n        radii = compute_max_radii(centers)\n        return centers, radii, float(np.sum(radii))\n\n    return best\n\ndef compute_max_radii(centers, max_iter=5000, tol=1e-9):\n    \"\"\"\n    Gauss-Seidel fixed-point iteration to compute maximal radii for fixed centers:\n      r_i <= wall_i\n      r_i + r_j <= d_ij   for all i != j\n\n    Update rule (in-place):\n      r_i <- min( wall_i, min_j (d_ij - r_j) )\n    \"\"\"\n    centers = np.asarray(centers, dtype=float)\n    m = centers.shape[0]\n    if m == 0:\n        return np.array([], dtype=float)\n\n    # wall distances\n    wall = np.minimum.reduce([centers[:,0], centers[:,1], 1.0-centers[:,0], 1.0-centers[:,1]])\n    wall = np.maximum(wall, 0.0)\n\n    # pairwise distances\n    diff = centers[:, None, :] - centers[None, :, :]\n    d = np.sqrt(np.sum(diff*diff, axis=2))\n    np.fill_diagonal(d, np.inf)\n\n    # initialize radii at wall distances (feasible)\n    r = wall.copy()\n\n    for it in range(max_iter):\n        prev = r.copy()\n        # Gauss-Seidel: update sequentially using latest values\n        for i in range(m):\n            # neighbor limits: d[i,:] - r (using current r_j)\n            limits = d[i, :] - r\n            limits[i] = np.inf\n            r_new = np.min(limits)\n            if r_new > wall[i]:\n                r_new = wall[i]\n            if r_new < 0.0:\n                r_new = 0.0\n            r[i] = r_new\n        if np.max(np.abs(r - prev)) < tol:\n            break\n\n    # final safety pass: fix tiny pairwise violations proportionally\n    for i in range(m):\n        for j in range(i+1, m):\n            dij = d[i,j]\n            if not np.isfinite(dij):\n                continue\n            if r[i] + r[j] > dij + 1e-12:\n                scale = dij / (r[i] + r[j])\n                r[i] *= scale\n                r[j] *= scale\n\n    r = np.minimum(r, wall)\n    r = np.maximum(r, 0.0)\n    return r\n\n# fixed runner API\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(6,6))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect('equal')\n    for c, rr in zip(centers, radii):\n        ax.add_patch(Circle(c, rr, alpha=0.6, edgecolor='k'))\n    plt.title(f\"n={len(centers)}, sum radii={sum(radii):.6f}\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, s = run_packing()\n    print(f\"Sum of radii: {s:.6f}\")\n# EVOLVE-BLOCK-END\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.2660), Performs well on target_ratio (0.8600), Performs well on combined_score (0.8600), Performs well on eval_time (0.1256)\n\n### Program 3 (Score: 0.8501)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for n=26 circle packing in the unit square.\n\nStrategy:\n- Try a small ensemble of plausible hex-like row patterns that sum to 26.\n- For each pattern scan a modest 2D parameter grid:\n    - horizontal center spacing s\n    - vertical compression factor vf (multiplies sqrt(3)/2 * s)\n  and two stagger choices, plus tiny global shifts.\n- For each candidate compute maximal feasible radii for the fixed centers\n  by a fast Gauss-Seidel fixed-point projection (r_i <- min(border_i, min_j d_ij - r_j)).\n- Return the best candidate (largest sum of radii). Entirely deterministic.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    # row patterns summing to 26 (varied shapes to reduce edge effects)\n    patterns = [\n        [6,5,5,5,5],   # common compact layout\n        [6,5,6,5,4],\n        [5,6,6,5,4],\n        [5,6,5,6,4],\n        [4,6,6,6,4],\n    ]\n\n    best = None\n    best_sum = -1.0\n\n    # horizontal spacings to try (center-to-center); smaller s -> denser block\n    s_vals = np.linspace(0.12, 0.22, 25)  # tradeoff: resolution vs speed\n    # vertical factor multiplies the ideal hex vertical step (sqrt(3)/2*s)\n    vf_vals = np.linspace(0.88, 1.06, 7)   # allow slight compression/expansion\n    shifts = [(0.0,0.0), (0.015,0.0), (-0.015,0.0), (0.0,0.015), (0.0,-0.015)]\n\n    for rows in patterns:\n        R = len(rows)\n        max_cols = max(rows)\n        for s in s_vals:\n            # base vertical step\n            base_v = np.sqrt(3.0) / 2.0 * s\n            for vf in vf_vals:\n                vstep = base_v * vf\n                # compute block height and early prune if impossible\n                total_h = 0.0 if R == 1 else (R - 1) * vstep\n                if total_h + 1e-12 > 1.0:  # won't fit vertically\n                    continue\n                for offset_start in (False, True):\n                    # build nominal centers centered in square\n                    y0 = 0.5 - total_h / 2.0\n                    centers = []\n                    for i in range(R):\n                        m = rows[i]\n                        offset = (i % 2 == 1) if offset_start else (i % 2 == 0)\n                        row_span = 0.0 if m == 1 else (m - 1) * s\n                        start_x = 0.5 - row_span / 2.0\n                        if offset:\n                            start_x += s / 2.0\n                        # clamp start so centers remain inside [0,1]\n                        start_x = max(start_x, 0.0)\n                        last_x = start_x + (m - 1) * s\n                        if last_x > 1.0:\n                            start_x -= (last_x - 1.0)\n                        y = y0 + i * vstep\n                        for k in range(m):\n                            x = start_x + k * s\n                            centers.append([x, y])\n                    centers = np.array(centers, dtype=float)\n                    if centers.shape[0] < n:\n                        # pad with small extras near a corner\n                        needed = n - centers.shape[0]\n                        extra_xs = np.linspace(0.02, 0.18, needed)\n                        extras = np.vstack([extra_xs, np.full(needed, 0.02)]).T\n                        centers = np.vstack((centers, extras))\n                    elif centers.shape[0] > n:\n                        centers = centers[:n]\n\n                    # try tiny global shifts to exploit edge slack\n                    for dx, dy in shifts:\n                        cand = centers + np.array([dx, dy])\n                        cand = np.clip(cand, 1e-9, 1.0 - 1e-9)\n                        radii = compute_max_radii(cand)\n                        ssum = float(np.sum(radii))\n                        if ssum > best_sum + 1e-12:\n                            best_sum = ssum\n                            best = (cand, radii, ssum)\n\n    # fallback: if nothing found (shouldn't happen), return a simple pattern\n    if best is None:\n        centers = np.tile([0.5,0.5], (n,1))\n        radii = compute_max_radii(centers)\n        return centers, radii, float(np.sum(radii))\n\n    return best\n\ndef compute_max_radii(centers, max_iter=1200, tol=1e-7):\n    \"\"\"\n    Gauss-Seidel fixed-point projection to maximal radii for fixed centers:\n      r_i <= border_i\n      r_i + r_j <= d_ij\n    Update rule (in-place): r_i <- min(border_i, min_j (d_ij - r_j))\n    Uses vectorized pairwise distances (n small so acceptable).\n    \"\"\"\n    centers = np.asarray(centers, dtype=float)\n    n = centers.shape[0]\n    # distances to walls\n    b = np.minimum.reduce([centers[:,0], centers[:,1], 1.0-centers[:,0], 1.0-centers[:,1]])\n    b = np.maximum(b, 0.0)\n    # pairwise distances\n    dif = centers[:,None,:] - centers[None,:,:]\n    d = np.sqrt(np.sum(dif*dif, axis=2))\n    np.fill_diagonal(d, np.inf)\n    # initialize radii at border distances (feasible) but slightly reduced to let growth converge\n    r = b.copy()\n    # Gauss-Seidel iterations\n    for it in range(max_iter):\n        prev = r.copy()\n        for i in range(n):\n            # compute limits d_ij - r_j using current r (some updated this iter)\n            limits = d[i,:] - r\n            # ignore self\n            limits[i] = np.inf\n            r_new = np.minimum(b[i], np.min(limits))\n            if r_new < 0.0:\n                r_new = 0.0\n            r[i] = r_new\n        if np.max(np.abs(r - prev)) < tol:\n            break\n\n    # small proportional fixes for any remaining tiny overlaps\n    slack = d - (r[:,None] + r[None,:])\n    # where slack < 0, scale the two radii proportionally\n    for i in range(n):\n        for j in range(i+1, n):\n            if slack[i,j] < -1e-12:\n                # reduce both proportionally to remove overlap\n                total = r[i] + r[j]\n                if total <= 0.0:\n                    r[i] = r[j] = 0.0\n                else:\n                    scale = d[i,j] / total\n                    r[i] *= scale\n                    r[j] *= scale\n\n    # final clamp\n    r = np.minimum(r, b)\n    r = np.maximum(r, 0.0)\n    return r\n\n# EVOLVE-BLOCK-END\n\n# fixed runner (unchanged API)\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\nif __name__ == \"__main__\":\n    centers, radii, s = run_packing()\n    print(f\"Sum of radii: {s:.6f}\")\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.2400), Performs well on target_ratio (0.8501), Performs well on combined_score (0.8501), Performs well on eval_time (2.1468)\n\n\n\n## Diverse Programs\n\n### Program D1 (Score: 0.8316)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (pattern search over explicit layouts)\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    \"\"\"\n    Build several deterministic hex-like layouts (different row patterns),\n    scan a small range of horizontal spacings s and pick the best resulting\n    packing (max sum of radii). This remains a constructive method: we\n    place centers by formula and only perform a deterministic radius projection.\n    \"\"\"\n    n = 26\n    # Several plausible hex-like row patterns summing to 26\n    patterns = [\n        [5, 6, 5, 5, 5],\n        [6, 5, 5, 5, 5],\n        [6, 5, 6, 5, 4],\n        [5, 6, 6, 5, 4],\n    ]\n\n    best = None\n    best_sum = -1.0\n\n    # modest grid of spacings to try (center-to-center horizontally)\n    s_vals = np.linspace(0.13, 0.20, 15)\n\n    for rows in patterns:\n        R = len(rows)\n        maxk = max(rows)\n        for s in s_vals:\n            h = np.sqrt(3) / 2 * s\n            # center vertically the block in [0,1]\n            total_h = (R - 1) * h\n            y0 = 0.5 - total_h / 2.0\n            centers = []\n            for r_idx, k in enumerate(rows):\n                y = y0 + r_idx * h\n                row_width = (k - 1) * s\n                x_start = 0.5 - row_width / 2.0\n                # stagger alternate rows for hex adjacency\n                if r_idx % 2 == 1:\n                    x_start += s / 2.0\n                for j in range(k):\n                    centers.append([x_start + j * s, y])\n            centers = np.array(centers[:n], dtype=float)\n            # Slightly clip to avoid exact-border degeneracies\n            centers = np.clip(centers, 1e-6, 1 - 1e-6)\n\n            radii = compute_max_radii(centers)\n            ssum = float(np.sum(radii))\n            if ssum > best_sum:\n                best_sum = ssum\n                best = (centers, radii, ssum)\n\n    # Fallback: if nothing found (shouldn't happen), return a simple pattern\n    if best is None:\n        centers = np.array([[0.5,0.5]]*n)\n        radii = compute_max_radii(centers)\n        return centers, radii, float(np.sum(radii))\n\n    return best\n\ndef compute_max_radii(centers, tol=1e-9):\n    \"\"\"\n    Deterministic projection to feasible radii:\n      - start with wall distances\n      - repeatedly fix pairwise overlaps by proportional shrinking\n      - apply a few conservative fractional growth passes to use slack\n    \"\"\"\n    centers = np.asarray(centers, dtype=float)\n    n = centers.shape[0]\n    # distance to wall\n    wall = np.minimum.reduce([centers[:,0], centers[:,1], 1-centers[:,0], 1-centers[:,1]])\n    # start with wall-limited radii\n    r = wall.copy()\n\n    # pairwise distances\n    dif = centers[:,None,:] - centers[None,:,:]\n    d = np.sqrt(np.sum(dif*dif, axis=2))\n    np.fill_diagonal(d, np.inf)\n\n    # Shrink overlaps: proportional scaling until no violations\n    for _ in range(1500):\n        moved = False\n        # ensure wall constraints\n        over = r - wall\n        if np.any(over > tol):\n            r = np.minimum(r, wall)\n            moved = True\n        # pairwise\n        for i in range(n):\n            ri = r[i]\n            # vectorized check for j>i could be done, but keep simple deterministic loop\n            for j in range(i+1, n):\n                dij = d[i,j]\n                if not np.isfinite(dij) or dij <= tol:\n                    continue\n                rj = r[j]\n                if ri + rj > dij + tol:\n                    # scale both proportionally\n                    scale = dij / (ri + rj)\n                    if scale < 1.0 - 1e-12:\n                        r[i] = ri * scale\n                        r[j] = rj * scale\n                        ri = r[i]  # update local var\n                        moved = True\n        if not moved:\n            break\n\n    # Conservative growth cycles: small fraction of available slack\n    for cycle in range(10):\n        # available increase by walls\n        inc_wall = wall - r\n        # available by neighbors: for each i, min_j (d_ij - (r_i + r_j)) + r_i => max increase = min_j(d_ij - (r_i + r_j))\n        avail = np.full(n, np.inf)\n        # compute pairwise slack matrix\n        slack = d - (r[None,:] + r[:,None])\n        # for each i, the maximum increase without immediate overlap is min(slack[i,:])\n        avail = np.minimum(avail, np.min(slack, axis=1))\n        # overall allowed inc is min(inc_wall, avail)\n        max_inc = np.minimum(inc_wall, avail)\n        max_inc = np.maximum(max_inc, 0.0)\n        # apply a cautious fraction\n        delta = 0.3 * max_inc\n        if np.all(delta <= tol):\n            break\n        r += delta\n        # re-shrink a few iterations to remove any slight violations\n        for _ in range(200):\n            moved = False\n            # enforce pairwise\n            for i in range(n):\n                for j in range(i+1, n):\n                    dij = d[i,j]\n                    if not np.isfinite(dij) or dij <= tol:\n                        continue\n                    if r[i] + r[j] > dij + tol:\n                        scale = dij / (r[i] + r[j])\n                        r[i] *= scale\n                        r[j] *= scale\n                        moved = True\n            # wall enforcement\n            over = r - wall\n            if np.any(over > tol):\n                r = np.minimum(r, wall)\n                moved = True\n            if not moved:\n                break\n\n    r = np.maximum(r, 0.0)\n    return r\n\n# EVOLVE-BLOCK-END\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, ec=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nKey features: Alternative approach to validity, Alternative approach to sum_radii\n\n### Program D2 (Score: 0.8298)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 using compact hex-like layouts\nand a robust Gauss-Seidel relaxation to compute maximal radii for fixed centers.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    sqrt3 = np.sqrt(3.0)\n\n    def vertical_limit(rows):\n        if rows == 1:\n            return 0.5\n        return 1.0 / (((rows - 1) * sqrt3) + 2.0)\n\n    def horizontal_limit(m, offset):\n        # conservative: if offset, need extra margin\n        return 1.0 / (2.0 * m + (1.0 if offset else 0.0))\n\n    best_score = -1.0\n    best_layout = None\n    # try row counts from 3..7 rows distributing 26 items\n    for rows in range(3, 8):\n        base = n // rows\n        rem = n % rows\n        for prefix_heavy in (True, False):\n            if prefix_heavy:\n                counts = [base + 1 if i < rem else base for i in range(rows)]\n            else:\n                counts = [base if i < (rows - rem) else base + 1 for i in range(rows)]\n            for offset_start in (False, True):\n                r_v = vertical_limit(rows)\n                r_hs = []\n                for i, m in enumerate(counts):\n                    offset = (i % 2 == 1) if offset_start else (i % 2 == 0)\n                    r_hs.append(horizontal_limit(m, offset))\n                r_candidate = min(r_v, min(r_hs))\n                score = n * r_candidate\n                if score > best_score:\n                    best_score = score\n                    best_layout = (rows, counts, offset_start, r_candidate)\n\n    rows, counts, offset_start, r = best_layout\n    centers = []\n    vstep = sqrt3 * r\n    total_height = 0.0 if rows == 1 else (rows - 1) * vstep\n    y0 = 0.5 - total_height / 2.0\n    for i in range(rows):\n        m = counts[i]\n        offset = (i % 2 == 1) if offset_start else (i % 2 == 0)\n        row_span = 0.0 if m == 1 else (m - 1) * 2.0 * r\n        start_x = 0.5 - row_span / 2.0\n        if offset:\n            start_x += r\n        # clamp to keep centers inside [r,1-r]\n        start_x = max(start_x, r)\n        last_x = start_x + (m - 1) * 2.0 * r\n        if last_x > 1.0 - r:\n            start_x -= (last_x - (1.0 - r))\n        y = y0 + i * vstep\n        for k in range(m):\n            x = start_x + k * 2.0 * r\n            centers.append((x, y))\n\n    centers = np.array(centers, dtype=float)\n    # trim/pad to exactly n\n    if centers.shape[0] > n:\n        centers = centers[:n]\n    elif centers.shape[0] < n:\n        needed = n - centers.shape[0]\n        extras = np.vstack([[0.02 + 0.96 * (i / max(1, needed - 1)), 0.02] for i in range(needed)])\n        centers = np.vstack((centers, extras))\n\n    radii = compute_max_radii(centers)\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\ndef compute_max_radii(centers, max_iter=2000, tol=1e-8):\n    n = centers.shape[0]\n    # distance to borders\n    b = np.minimum.reduce([centers[:, 0], centers[:, 1], 1.0 - centers[:, 0], 1.0 - centers[:, 1]])\n    b = np.maximum(b, 0.0)\n    # pairwise distances\n    diff = centers[:, None, :] - centers[None, :, :]\n    d = np.sqrt(np.sum(diff * diff, axis=2))\n    np.fill_diagonal(d, np.inf)\n    # initialize radii at border distances (feasible)\n    rads = b.copy()\n    for it in range(max_iter):\n        prev = rads.copy()\n        # Gauss-Seidel: update in place\n        for i in range(n):\n            limits = d[i, :] - rads\n            limits[i] = np.inf\n            r_new = min(b[i], np.min(limits))\n            if r_new < 0.0:\n                r_new = 0.0\n            rads[i] = r_new\n        if np.max(np.abs(rads - prev)) < tol:\n            break\n    # clean tiny violations\n    for i in range(n):\n        for j in range(i + 1, n):\n            dij = d[i, j]\n            if rads[i] + rads[j] > dij + 1e-12:\n                scale = dij / (rads[i] + rads[j])\n                rads[i] *= scale\n                rads[j] *= scale\n    rads = np.minimum(rads, b)\n    rads = np.maximum(rads, 0.0)\n    return rads\n\n# fixed runner\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n# optional visualization helper\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(6,6))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect('equal')\n    for c, rr in zip(centers, radii):\n        ax.add_patch(Circle(c, rr, alpha=0.6, edgecolor='k'))\n    plt.title(f\"n={len(centers)}, sum radii={sum(radii):.6f}\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, s = run_packing()\n    print(f\"Sum of radii: {s:.6f}\")\n# EVOLVE-BLOCK-END\n```\nKey features: Alternative approach to validity, Alternative approach to sum_radii\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 0.7293, Type: Alternative)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (improved)\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square.\n    Returns:\n        centers: np.array (26,2)\n        radii: np.array (26,)\n        sum_of_radii: float\n    \"\"\"\n    n = 26\n\n    # Hex-like row pattern that fits 26 circles: row lengths sum to 26\n    rows = [5, 6, 5, 5, 5]  # total 26\n    maxk = max(rows)\n\n    # Choose a reasonable spacing s for hex packing so centers are well-distributed.\n    # This s is a design choice; it affects how large radii can grow after constraints.\n    s = 0.17  # horizontal center spacing\n    h = np.sqrt(3) / 2 * s  # vertical spacing for hex pattern\n\n    centers = []\n    y0 = 0.5 - (len(rows) - 1) * h / 2.0\n    for r_idx, k in enumerate(rows):\n        y = y0 + r_idx * h\n        # center the row horizontally; alternate offset for hex pattern\n        row_width = (k - 1) * s\n        x_start = 0.5 - row_width / 2.0\n        # offset every other row by s/2 for hex packing\n        if r_idx % 2 == 1:\n            x_start += s / 2.0\n        for j in range(k):\n            x = x_start + j * s\n            centers.append([x, y])\n\n    centers = np.array(centers[:n], dtype=float)\n    # Clip to avoid numerical issues at exact border (will be handled by radii)\n    centers = np.clip(centers, 1e-6, 1 - 1e-6)\n\n    radii = compute_max_radii(centers)\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers, tol=1e-8):\n    \"\"\"\n    Iteratively compute maximal non-overlapping radii for given centers in unit square.\n    Starts from wall-limited radii and resolves overlaps by proportional shrinking.\n    Then attempts a few conservative growth steps to increase sum where possible.\n    \"\"\"\n    n = centers.shape[0]\n    # initial radii limited by wall distance\n    wall_dist = np.minimum.reduce([centers[:, 0], centers[:, 1],\n                                   1 - centers[:, 0], 1 - centers[:, 1]])\n    radii = wall_dist.copy()\n\n    # Precompute pairwise distances\n    diffs = centers[:, None, :] - centers[None, :, :]\n    dists = np.sqrt(np.sum(diffs * diffs, axis=2))\n    np.fill_diagonal(dists, np.inf)\n\n    # Shrink overlaps repeatedly until stable\n    for outer in range(2000):\n        changed = False\n        # Enforce wall constraints\n        over_wall = radii - wall_dist\n        if np.any(over_wall > tol):\n            radii = np.minimum(radii, wall_dist)\n            changed = True\n\n        # Pairwise resolution: proportional shrink to remove overlaps\n        for i in range(n):\n            for j in range(i + 1, n):\n                dij = dists[i, j]\n                if not np.isfinite(dij) or dij <= tol:\n                    continue\n                ri, rj = radii[i], radii[j]\n                if ri + rj > dij + tol:\n                    # scale both so ri + rj == dij (proportional)\n                    scale = dij / (ri + rj)\n                    # Avoid numerical issues when one radius is zero\n                    if scale < 1.0 - 1e-12:\n                        radii[i] = ri * scale\n                        radii[j] = rj * scale\n                        changed = True\n        if not changed:\n            break\n\n    # Conservative growth cycles: try small fractional increases, then re-shrink\n    # This helps utilize small slack without violating global constraints.\n    for growth_cycle in range(8):\n        # compute for each circle maximum possible increase without immediate overlap\n        max_inc = np.minimum.reduce([\n            wall_dist - radii,\n            np.min(dists - (radii[None, :] + radii[:, None]), axis=1)\n        ])\n        # clip negatives\n        max_inc = np.maximum(max_inc, 0.0)\n\n        # apply a fraction of allowed increase to avoid flipping many constraints at once\n        delta = 0.25 * max_inc\n        # tiny adjustments only\n        if np.all(delta <= tol):\n            break\n        radii += delta\n\n        # re-run a limited shrink pass to resolve any new overlaps (should be small)\n        for _ in range(200):\n            moved = False\n            for i in range(n):\n                for j in range(i + 1, n):\n                    dij = dists[i, j]\n                    if not np.isfinite(dij) or dij <= tol:\n                        continue\n                    if radii[i] + radii[j] > dij + tol:\n                        scale = dij / (radii[i] + radii[j])\n                        radii[i] *= scale\n                        radii[j] *= scale\n                        moved = True\n            # wall enforcement\n            over = radii - wall_dist\n            if np.any(over > tol):\n                radii = np.minimum(radii, wall_dist)\n                moved = True\n            if not moved:\n                break\n\n    # final safety clamp\n    radii = np.maximum(radii, 0.0)\n    return radii\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, ec=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nUnique approach: Modification: Full rewrite, Excellent validity (1.000), Excellent sum_radii (1.922)\n\n### Inspiration 2 (Score: 0.8600, Type: High-Performer)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for packing 26 circles in the unit square.\n\nStrategy:\n- Try a small family of hex-like row patterns that sum to 26.\n- For each pattern scan a modest grid of horizontal spacings s (center-to-center).\n- Build centers (staggered rows for hex packing), clamp inside (tiny margin).\n- For each candidate center set compute maximal feasible radii using a\n  Gauss-Seidel fixed-point iteration:\n     r_i <- min( wall_i, min_j (d_ij - r_j) )\n  which converges to the maximal radii for fixed centers.\n- Return the best (centers, radii) that maximize sum(radii).\n\"\"\"\nimport numpy as np\nfrom math import sqrt\n\ndef construct_packing():\n    n = 26\n    # plausible hex-like row patterns summing to 26\n    patterns = [\n        [5,6,5,5,5],\n        [6,5,5,5,5],\n        [6,5,6,5,4],\n        [5,6,6,5,4],\n        [4,6,6,5,5],\n    ]\n\n    best = None\n    best_sum = -1.0\n\n    # search horizontal center-to-center spacing s\n    s_vals = np.linspace(0.12, 0.20, 36)\n\n    for rows in patterns:\n        R = len(rows)\n        for s in s_vals:\n            h = sqrt(3.0)/2.0 * s  # vertical step for hex arrangement when horizontal step = s\n            total_h = (R - 1) * h\n            y0 = 0.5 - total_h/2.0\n            centers = []\n            for r_idx, k in enumerate(rows):\n                y = y0 + r_idx * h\n                row_width = (k - 1) * s if k > 1 else 0.0\n                x_start = 0.5 - row_width/2.0\n                # stagger alternate rows\n                if r_idx % 2 == 1:\n                    x_start += s/2.0\n                for j in range(k):\n                    centers.append([x_start + j*s, y])\n            centers = np.array(centers[:n], dtype=float)\n            # ensure strictly inside unit square to avoid zero wall distances\n            centers = np.clip(centers, 1e-8, 1.0 - 1e-8)\n\n            radii = compute_max_radii(centers)\n            ssum = float(np.sum(radii))\n            if ssum > best_sum:\n                best_sum = ssum\n                best = (centers.copy(), radii.copy(), ssum)\n\n    # fallback: if nothing found (very unlikely), return a simple centered arrangement\n    if best is None:\n        centers = np.array([[0.5,0.5]]*n)\n        radii = compute_max_radii(centers)\n        return centers, radii, float(np.sum(radii))\n\n    return best\n\ndef compute_max_radii(centers, max_iter=5000, tol=1e-9):\n    \"\"\"\n    Gauss-Seidel fixed-point iteration to compute maximal radii for fixed centers:\n      r_i <= wall_i\n      r_i + r_j <= d_ij   for all i != j\n\n    Update rule (in-place):\n      r_i <- min( wall_i, min_j (d_ij - r_j) )\n    \"\"\"\n    centers = np.asarray(centers, dtype=float)\n    m = centers.shape[0]\n    if m == 0:\n        return np.array([], dtype=float)\n\n    # wall distances\n    wall = np.minimum.reduce([centers[:,0], centers[:,1], 1.0-centers[:,0], 1.0-centers[:,1]])\n    wall = np.maximum(wall, 0.0)\n\n    # pairwise distances\n    diff = centers[:, None, :] - centers[None, :, :]\n    d = np.sqrt(np.sum(diff*diff, axis=2))\n    np.fill_diagonal(d, np.inf)\n\n    # initialize radii at wall distances (feasible)\n    r = wall.copy()\n\n    for it in range(max_iter):\n        prev = r.copy()\n        # Gauss-Seidel: update sequentially using latest values\n        for i in range(m):\n            # neighbor limits: d[i,:] - r (using current r_j)\n            limits = d[i, :] - r\n            limits[i] = np.inf\n            r_new = np.min(limits)\n            if r_new > wall[i]:\n                r_new = wall[i]\n            if r_new < 0.0:\n                r_new = 0.0\n            r[i] = r_new\n        if np.max(np.abs(r - prev)) < tol:\n            break\n\n    # final safety pass: fix tiny pairwise violations proportionally\n    for i in range(m):\n        for j in range(i+1, m):\n            dij = d[i,j]\n            if not np.isfinite(dij):\n                continue\n            if r[i] + r[j] > dij + 1e-12:\n                scale = dij / (r[i] + r[j])\n                r[i] *= scale\n                r[j] *= scale\n\n    r = np.minimum(r, wall)\n    r = np.maximum(r, 0.0)\n    return r\n\n# fixed runner API\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(6,6))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect('equal')\n    for c, rr in zip(centers, radii):\n        ax.add_patch(Circle(c, rr, alpha=0.6, edgecolor='k'))\n    plt.title(f\"n={len(centers)}, sum radii={sum(radii):.6f}\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, s = run_packing()\n    print(f\"Sum of radii: {s:.6f}\")\n# EVOLVE-BLOCK-END\n```\nUnique approach: Modification: Full rewrite, Excellent validity (1.000), Excellent sum_radii (2.266)\n\n### Inspiration 3 (Score: 0.6326, Type: Alternative)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (improved)\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    \"\"\"\n    Deterministic constructor for 26 circles in the unit square.\n    Strategy:\n      - 4 corner anchors (to exploit corner space)\n      - 8 edge anchors (two per side)\n      - 14-circle central cluster (1 center + 6 inner ring + 7 outer ring)\n    After fixing centers, compute maximal non-overlapping radii with a\n    Gauss-Seidel solver (r_i <= dist_to_border and r_i <= d_ij - r_j).\n    \"\"\"\n    n = 26\n    centers = []\n\n    # Corner anchors (allow bigger corner circles)\n    cm = 0.08\n    centers += [(cm, cm), (1 - cm, cm), (cm, 1 - cm), (1 - cm, 1 - cm)]\n\n    # Edge anchors: two per side near quarter positions\n    centers += [\n        (0.25, cm), (0.75, cm),  # bottom\n        (0.25, 1 - cm), (0.75, 1 - cm),  # top\n        (cm, 0.25), (cm, 0.75),  # left\n        (1 - cm, 0.25), (1 - cm, 0.75)  # right\n    ]\n\n    # Central cluster: one center, 6 close neighbors, 7 further neighbors\n    cx, cy = 0.5, 0.5\n    centers.append((cx, cy))\n    r1 = 0.12  # inner ring radius (center-to-center)\n    for k in range(6):\n        ang = 2 * np.pi * k / 6\n        centers.append((cx + r1 * np.cos(ang), cy + r1 * np.sin(ang)))\n    r2 = 0.22  # outer ring radius (center-to-center)\n    # offset outer ring so it doesn't coincide with inner ring directions\n    for k in range(7):\n        ang = 2 * np.pi * k / 7 + 0.5 * (2 * np.pi / 7)\n        centers.append((cx + r2 * np.cos(ang), cy + r2 * np.sin(ang)))\n\n    # Collect exactly n unique centers (keep order, remove near-duplicates)\n    pts = []\n    tol = 1e-9\n    for p in centers:\n        p = (float(p[0]), float(p[1]))\n        if p[0] < 0.0: p = (0.0, p[1])\n        if p[1] < 0.0: p = (p[0], 0.0)\n        if p[0] > 1.0: p = (1.0, p[1])\n        if p[1] > 1.0: p = (p[0], 1.0)\n        if not any((abs(p[0]-q[0]) < tol and abs(p[1]-q[1]) < tol) for q in pts):\n            pts.append(p)\n        if len(pts) >= n:\n            break\n    centers = np.array(pts[:n])\n\n    # Slight nudge of edge/corner anchors outward to free center space (deterministic)\n    # Move corners slightly towards their corner, edges slightly toward border\n    for i, (x, y) in enumerate(centers):\n        # identify near-corner\n        if x < 0.15 and y < 0.15:\n            centers[i] = (max(0.0, x - 0.01), max(0.0, y - 0.01))\n        elif x > 0.85 and y < 0.15:\n            centers[i] = (min(1.0, x + 0.01), max(0.0, y - 0.01))\n        elif x < 0.15 and y > 0.85:\n            centers[i] = (max(0.0, x - 0.01), min(1.0, y + 0.01))\n        elif x > 0.85 and y > 0.85:\n            centers[i] = (min(1.0, x + 0.01), min(1.0, y + 0.01))\n        # edges\n        elif y < 0.2:\n            centers[i] = (x, max(0.0, y - 0.005))\n        elif y > 0.8:\n            centers[i] = (x, min(1.0, y + 0.005))\n        elif x < 0.2:\n            centers[i] = (max(0.0, x - 0.005), y)\n        elif x > 0.8:\n            centers[i] = (min(1.0, x + 0.005), y)\n\n    # Ensure strictly inside bounds (small margin)\n    centers = np.clip(centers, 1e-6, 1 - 1e-6)\n\n    radii = compute_max_radii(centers)\n    sum_r = float(np.sum(radii))\n    return centers, radii, sum_r\n\ndef compute_max_radii(centers, tol=1e-8, max_iter=3000):\n    \"\"\"\n    Gauss-Seidel iterative solver for maximal radii given fixed centers.\n    r_i <= dist_to_border(i)\n    r_i + r_j <= dist(ci, cj)\n    \"\"\"\n    n = centers.shape[0]\n    # distance to borders\n    border = np.minimum.reduce([centers[:,0], centers[:,1], 1 - centers[:,0], 1 - centers[:,1]])\n    # initial guess: half of border (conservative)\n    radii = np.maximum(1e-6, 0.5 * border)\n\n    # pairwise distances\n    diff = centers[:, None, :] - centers[None, :, :]\n    dists = np.sqrt((diff ** 2).sum(axis=2))\n    np.fill_diagonal(dists, np.inf)\n\n    for it in range(max_iter):\n        max_change = 0.0\n        # update each radius in-place (Gauss-Seidel)\n        for i in range(n):\n            # allowed by border\n            allowed = border[i]\n            # allowed by neighbors using current r_j\n            neigh_allowed = dists[i, :] - radii\n            # take minimum; neigh_allowed includes inf on diagonal\n            mn = neigh_allowed.min()\n            if mn < allowed:\n                allowed = mn\n            if allowed < 0.0:\n                allowed = 0.0\n            change = abs(allowed - radii[i])\n            if change > max_change:\n                max_change = change\n            radii[i] = allowed\n        if max_change < tol:\n            break\n\n    # safety clamp\n    radii = np.clip(radii, 0.0, None)\n    return radii\n\n# EVOLVE-BLOCK-END\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(7,7))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect(\"equal\")\n    for c, r in zip(centers, radii):\n        ax.add_patch(Circle(c, r, alpha=0.6, edgecolor=\"k\"))\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, s = run_packing()\n    print(f\"Sum of radii: {s:.6f}\")\n    # visualize(centers, radii)\n```\nUnique approach: Modification: Full rewrite, Excellent validity (1.000), Excellent sum_radii (1.667)\n\n# Current Program\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Improved constructor-based circle packing for n=26 using explicit layouts.\n\nStrategy:\n- Try a small set of plausible hex-like row patterns that sum to 26.\n- For each pattern scan a grid of horizontal spacings (center-to-center).\n- For each candidate build centered centers (with alternating offsets for hex)\n  then compute maximal feasible radii for those fixed centers via a robust\n  Gauss-Seidel solver with a few conservative growth passes.\n- Return the candidate with largest sum of radii.\n\nDeterministic, constructor-only (no randomized search).\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    # candidate row patterns summing to 26 (various asymmetries to reduce edge effects)\n    patterns = [\n        [6, 5, 6, 5, 4],\n        [5, 6, 5, 6, 4],\n        [5, 6, 5, 5, 5],\n        [6, 5, 5, 5, 5],\n        [4, 6, 6, 5, 5],\n    ]\n\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    # horizontal center spacing candidates (reasonable hex-like range)\n    s_vals = np.linspace(0.12, 0.20, 25)\n\n    for rows in patterns:\n        R = len(rows)\n        max_cols = max(rows)\n        for s in s_vals:\n            h = np.sqrt(3.0) / 2.0 * s  # vertical spacing\n            # compute vertical block span and center vertically\n            total_h = (R - 1) * h if R > 1 else 0.0\n            y0 = 0.5 - total_h / 2.0\n            centers = []\n            for r_idx, cols in enumerate(rows):\n                y = y0 + r_idx * h\n                row_width = (cols - 1) * s if cols > 1 else 0.0\n                x_start = 0.5 - row_width / 2.0\n                # stagger every other row (classic hex pattern)\n                if r_idx % 2 == 1:\n                    x_start += s / 2.0\n                for j in range(cols):\n                    centers.append([x_start + j * s, y])\n            centers = np.array(centers[:n], dtype=float)\n            # skip candidates that obviously place centers outside [0,1]\n            if centers.size == 0:\n                continue\n            if np.any(centers < -1e-8) or np.any(centers > 1 + 1e-8):\n                continue\n            # clamp tiny numerical excursions\n            centers = np.clip(centers, 1e-8, 1 - 1e-8)\n\n            radii = compute_max_radii(centers)\n            ssum = float(np.sum(radii))\n            if ssum > best_sum:\n                best_sum = ssum\n                best_centers = centers.copy()\n                best_radii = radii.copy()\n\n    # fallback: if nothing selected (shouldn't happen), build a known pattern\n    if best_centers is None:\n        rows = [5,6,5,5,5]\n        s = 0.15\n        h = np.sqrt(3.0)/2*s\n        total_h = (len(rows)-1)*h\n        y0 = 0.5 - total_h/2.0\n        centers = []\n        for r_idx, cols in enumerate(rows):\n            y = y0 + r_idx*h\n            row_width = (cols-1)*s if cols>1 else 0.0\n            x_start = 0.5 - row_width/2.0\n            if r_idx % 2 == 1:\n                x_start += s/2.0\n            for j in range(cols):\n                centers.append([x_start + j*s, y])\n        best_centers = np.clip(np.array(centers[:n]), 1e-8, 1-1e-8)\n        best_radii = compute_max_radii(best_centers)\n        best_sum = float(np.sum(best_radii))\n\n    return best_centers, best_radii, float(best_sum)\n\n\ndef compute_max_radii(centers, max_iter=2000, tol=1e-9):\n    \"\"\"\n    Compute maximal radii for fixed centers satisfying:\n      r_i <= border_i\n      r_i + r_j <= d_ij  (i != j)\n    Uses Gauss-Seidel fixed-point iteration (monotone) and a few conservative\n    growth passes to use slack safely.\n    \"\"\"\n    centers = np.asarray(centers, dtype=float)\n    n = centers.shape[0]\n    if n == 0:\n        return np.array([], dtype=float)\n\n    # border distances\n    border = np.minimum.reduce([centers[:,0], centers[:,1], 1.0 - centers[:,0], 1.0 - centers[:,1]])\n    border = np.maximum(border, 0.0)\n\n    # pairwise distances\n    diff = centers[:, None, :] - centers[None, :, :]\n    d = np.sqrt(np.sum(diff * diff, axis=2))\n    np.fill_diagonal(d, np.inf)\n\n    # initialize radii to border distances (feasible wrt walls)\n    r = border.copy()\n\n    # Gauss-Seidel iterations: r_i = min(border_i, min_j (d_ij - r_j))\n    for it in range(max_iter):\n        prev = r.copy()\n        for i in range(n):\n            limits = d[i, :] - r\n            limits[i] = np.inf\n            candidate = np.min(limits)\n            new = candidate if candidate < border[i] else border[i]\n            if new < 0.0:\n                new = 0.0\n            r[i] = new\n        if np.max(np.abs(r - prev)) < tol:\n            break\n\n    # Conservative growth cycles: increase a fraction of available slack, then re-shrink\n    for cycle in range(8):\n        # compute slack matrix: d - (r_i + r_j)\n        slack = d - (r[None,:] + r[:,None])\n        # for each i, maximum extra without immediate overlap is min(slack[i,:])\n        max_inc = np.minimum(border - r, np.min(slack, axis=1))\n        max_inc = np.maximum(max_inc, 0.0)\n        delta = 0.25 * max_inc\n        if np.all(delta <= tol):\n            break\n        r += delta\n        # small re-shrink (fix any slight violations)\n        for _ in range(200):\n            moved = False\n            # pairwise proportional fix\n            for i in range(n):\n                for j in range(i+1, n):\n                    dij = d[i,j]\n                    if not np.isfinite(dij):\n                        continue\n                    if r[i] + r[j] > dij + 1e-12:\n                        scale = dij / (r[i] + r[j])\n                        r[i] *= scale\n                        r[j] *= scale\n                        moved = True\n            # enforce borders\n            over = r - border\n            if np.any(over > 0):\n                r = np.minimum(r, border)\n                moved = True\n            if not moved:\n                break\n\n    r = np.maximum(r, 0.0)\n    return r\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, ec=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # To inspect visually, uncomment:\n    # visualize(centers, radii)\n```\n\n# Task\nRewrite the program to improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: complexity, diversity\nDifferent solutions with similar fitness but different features are valuable.\nProvide the complete new program code.\n\nIMPORTANT: Make sure your rewritten program maintains the same inputs and outputs\nas the original program, but with improved internal implementation.\n\n```python\n# Your rewritten program here\n```", "responses": ["```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic explicit constructor for packing n=26 circles in the unit square.\n\nThis version improves on prior attempts by:\n- Trying a compact family of asymmetric hex-like row patterns that sum to 26.\n- Scanning a fine grid of horizontal spacings and slight vertical scalings.\n- Trying both stagger parities and small global shifts to exploit edge slack.\n- Computing maximal feasible radii for fixed centers using a robust procedure:\n    * initialize at wall-distances\n    * proportional shrink passes to remove overlaps\n    * Gauss-Seidel fixed-point to converge to a feasible maximal set\n    * cautious growth cycles with proportional repairs to capture slack\nAll steps are deterministic and constructor-only (no random search).\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    # Diverse plausible row patterns summing to 26 (break symmetry to help edges)\n    patterns = [\n        [5, 6, 5, 5, 5],\n        [6, 5, 5, 5, 5],\n        [6, 5, 6, 5, 4],\n        [5, 6, 6, 5, 4],\n        [4, 6, 6, 5, 5],\n    ]\n\n    # search parameters tuned for good coverage but modest eval time\n    s_vals = np.linspace(0.125, 0.195, 30)   # horizontal center-to-center spacing\n    y_scales = [0.94, 0.98, 1.00, 1.02]      # slight vertical compression/expansion\n    shifts = [(0.0, 0.0), (0.006, 0.0), (-0.006, 0.0), (0.0, 0.006), (0.0, -0.006)]\n\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    for pattern in patterns:\n        rows = len(pattern)\n        max_cols = max(pattern)\n        for s in s_vals:\n            # ideal hex vertical step for spacing s\n            v0 = (np.sqrt(3.0) / 2.0) * s\n            for y_scale in y_scales:\n                v = v0 * y_scale\n                # compute total vertical span and center block vertically\n                total_h = (rows - 1) * v if rows > 1 else 0.0\n                if total_h > 1.0 + 1e-12:\n                    continue\n                y0 = 0.5 - total_h / 2.0\n                for offset_start in (False, True):\n                    centers = []\n                    for r_idx, cnt in enumerate(pattern):\n                        y = y0 + r_idx * v\n                        row_span = (cnt - 1) * s if cnt > 1 else 0.0\n                        x_start = 0.5 - row_span / 2.0\n                        offset = (r_idx % 2 == 1) if offset_start else (r_idx % 2 == 0)\n                        if offset:\n                            x_start += s / 2.0\n                        # ensure row stays inside [0,1] by tiny shift if necessary\n                        last_x = x_start + (cnt - 1) * s\n                        if x_start < 0.0:\n                            x_start = 0.0\n                        if last_x > 1.0:\n                            x_start -= (last_x - 1.0)\n                        for j in range(cnt):\n                            centers.append([x_start + j * s, y])\n                    # trim/pad to exactly n deterministically\n                    centers = np.array(centers, dtype=float)\n                    if centers.shape[0] < n:\n                        # pad with small positions along bottom-left corner\n                        needed = n - centers.shape[0]\n                        extra_xs = np.linspace(0.02, 0.18, needed)\n                        extras = np.vstack([extra_xs, np.full(needed, 0.02)]).T\n                        centers = np.vstack((centers, extras))\n                    elif centers.shape[0] > n:\n                        centers = centers[:n]\n\n                    # try small global shifts to exploit slack near walls\n                    for dx, dy in shifts:\n                        cand = centers + np.array([dx, dy])\n                        cand = np.clip(cand, 1e-9, 1.0 - 1e-9)\n                        radii = compute_max_radii(cand)\n                        ssum = float(np.sum(radii))\n                        if ssum > best_sum + 1e-12:\n                            best_sum = ssum\n                            best_centers = cand.copy()\n                            best_radii = radii.copy()\n\n    # fallback single layout (should not be needed)\n    if best_centers is None:\n        rows = [5, 6, 5, 5, 5]\n        s = 0.16\n        v = (np.sqrt(3.0) / 2.0) * s\n        y0 = 0.5 - (len(rows) - 1) * v / 2.0\n        centers = []\n        for r_idx, cnt in enumerate(rows):\n            y = y0 + r_idx * v\n            row_span = (cnt - 1) * s if cnt > 1 else 0.0\n            x_start = 0.5 - row_span / 2.0\n            if r_idx % 2 == 1:\n                x_start += s / 2.0\n            for j in range(cnt):\n                centers.append([x_start + j * s, y])\n        centers = np.clip(np.array(centers[:n], dtype=float), 1e-9, 1.0 - 1e-9)\n        best_centers = centers\n        best_radii = compute_max_radii(centers)\n        best_sum = float(np.sum(best_radii))\n\n    return best_centers, best_radii, float(best_sum)\n\n\ndef compute_max_radii(centers, tol=1e-10, max_iter=3000):\n    \"\"\"\n    Robust deterministic procedure to compute near-maximal radii for fixed centers:\n      - initialize at border distances\n      - proportional shrink passes to remove overlaps\n      - Gauss-Seidel fixed-point to converge\n      - cautious growth cycles with proportional repairs to capture slack\n    \"\"\"\n    centers = np.asarray(centers, dtype=float)\n    n = centers.shape[0]\n    if n == 0:\n        return np.array([], dtype=float)\n\n    # distances to walls\n    border = np.minimum.reduce([centers[:, 0], centers[:, 1], 1.0 - centers[:, 0], 1.0 - centers[:, 1]])\n    border = np.maximum(border, 0.0)\n\n    # pairwise distances\n    dif = centers[:, None, :] - centers[None, :, :]\n    d = np.sqrt(np.sum(dif * dif, axis=2))\n    np.fill_diagonal(d, np.inf)\n\n    # initialize radii at border distances (feasible)\n    r = border.copy()\n\n    # Initial proportional shrink to remove any overlaps caused by numerical issues\n    for _ in range(1000):\n        moved = False\n        # enforce walls\n        over = r - border\n        if np.any(over > 0):\n            r = np.minimum(r, border)\n            moved = True\n        # pairwise proportional fixes\n        for i in range(n):\n            ri = r[i]\n            for j in range(i + 1, n):\n                dij = d[i, j]\n                if not np.isfinite(dij) or dij <= 0.0:\n                    continue\n                rj = r[j]\n                if ri + rj > dij + 1e-12:\n                    total = ri + rj\n                    if total <= 0.0:\n                        r[i] = r[j] = 0.0\n                        moved = True\n                        continue\n                    scale = dij / total\n                    # apply scale only if it reduces\n                    if scale < 1.0 - 1e-14:\n                        r[i] = ri * scale\n                        r[j] = rj * scale\n                        ri = r[i]\n                        moved = True\n        if not moved:\n            break\n\n    # Gauss-Seidel fixed-point to approach maximal feasible radii\n    for it in range(max_iter):\n        prev = r.copy()\n        for i in range(n):\n            limits = d[i, :] - r\n            limits[i] = np.inf\n            val = np.min(limits)\n            # enforce wall limit\n            if val > border[i]:\n                val = border[i]\n            if val < 0.0:\n                val = 0.0\n            r[i] = val\n        if np.max(np.abs(r - prev)) < tol:\n            break\n\n    # Conservative growth cycles: use small fraction of slack, then repair\n    for cycle in range(10):\n        # slack to walls\n        slack_wall = border - r\n        # slack to neighbors: for each i, min_j (d_ij - (r_i + r_j))\n        slack_pair = np.min(d - (r[None, :] + r[:, None]), axis=1)\n        slack_pair = np.maximum(slack_pair, 0.0)\n        allowed_inc = np.minimum(slack_wall, slack_pair)\n        allowed_inc = np.maximum(allowed_inc, 0.0)\n        delta = 0.22 * allowed_inc  # cautious fraction\n        if np.all(delta <= 1e-12):\n            break\n        r += delta\n\n        # proportional repair loops to restore feasibility\n        for _ in range(300):\n            moved = False\n            # pairwise check\n            for i in range(n):\n                for j in range(i + 1, n):\n                    dij = d[i, j]\n                    if not np.isfinite(dij):\n                        continue\n                    if r[i] + r[j] > dij + 1e-12:\n                        total = r[i] + r[j]\n                        if total <= 0.0:\n                            r[i] = r[j] = 0.0\n                            moved = True\n                            continue\n                        scale = dij / total\n                        r[i] *= scale\n                        r[j] *= scale\n                        moved = True\n            # walls\n            over = r - border\n            if np.any(over > 1e-12):\n                r = np.minimum(r, border)\n                moved = True\n            if not moved:\n                break\n\n    # final safety clamps\n    r = np.maximum(np.minimum(r, border), 0.0)\n    return r\n\n# EVOLVE-BLOCK-END\n\n\n# fixed runner API\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(7, 7))\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    for c, rr in zip(centers, radii):\n        ax.add_patch(Circle(c, rr, alpha=0.6, edgecolor=\"k\"))\n    plt.title(f\"n={len(centers)}, sum radii={sum(radii):.6f}\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, s = run_packing()\n    print(f\"Sum of radii: {s:.6f}\")\n    # To visualize, uncomment:\n    # visualize(centers, radii)\n```"]}}, "artifacts_json": "{\"stdout\": \"Running in subprocess, Python version: 3.12.1 (v3.12.1:2305ca5144, Dec  7 2023, 17:23:38) [Clang 13.0.0 (clang-1300.0.29.30)]\\nProgram path: /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmpirgahff6.py\\nCalling run_packing()...\\nrun_packing() returned successfully: sum_radii = 2.2769021808649645\\nResults saved to /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmp5oshxqac.py.results\\n\", \"stderr\": \"\"}", "artifact_dir": null}