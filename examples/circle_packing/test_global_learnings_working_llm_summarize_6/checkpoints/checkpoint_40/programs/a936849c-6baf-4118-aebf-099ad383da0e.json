{"id": "a936849c-6baf-4118-aebf-099ad383da0e", "code": "# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for n=26 circle packing in the unit square.\n\nStrategy:\n- Try a small ensemble of plausible hex-like row patterns that sum to 26.\n- For each pattern scan horizontal spacings and slight vertical scales.\n- For each candidate place deterministic centers and compute near-maximal\n  radii by a Gauss\u2013Seidel projection + a few conservative growth cycles.\nReturn the best layout found (largest sum of radii).\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    # candidate compact row patterns (sum to 26)\n    patterns = [\n        [6,5,5,5,5],\n        [5,6,5,5,5],\n        [6,5,6,5,4],\n        [5,6,6,5,4],\n        [4,6,6,5,5],\n    ]\n\n    s_vals = np.linspace(0.12, 0.20, 17)\n    y_scales = [0.94, 0.98, 1.0, 1.02]\n    shifts = [(0,0),(+0.006,0),(-0.006,0),(0,+0.006),(0,-0.006)]\n    best = None\n    best_sum = -1.0\n\n    for pattern in patterns:\n        rows = len(pattern)\n        for s in s_vals:\n            v0 = (np.sqrt(3)/2.0) * s\n            for y_scale in y_scales:\n                v = v0 * y_scale\n                total_h = (rows - 1) * v\n                if total_h > 1.0 + 1e-12:  # can't fit vertically\n                    continue\n                y0 = 0.5 - total_h/2.0\n                for offset_start in (False, True):\n                    centers = []\n                    for r_idx, cnt in enumerate(pattern):\n                        y = y0 + r_idx * v\n                        row_width = (cnt - 1) * s if cnt > 1 else 0.0\n                        x_start = 0.5 - row_width / 2.0\n                        offset = (r_idx % 2 == 1) if offset_start else (r_idx % 2 == 0)\n                        if offset:\n                            x_start += s/2.0\n                        for j in range(cnt):\n                            centers.append([x_start + j * s, y])\n                    centers = np.array(centers[:n], dtype=float)\n                    # try small deterministic global shifts\n                    for dx,dy in shifts:\n                        cand = np.clip(centers + np.array([dx,dy]), 1e-9, 1-1e-9)\n                        radii = compute_max_radii(cand)\n                        ssum = float(np.sum(radii))\n                        if ssum > best_sum + 1e-12:\n                            best_sum = ssum\n                            best = (cand, radii, ssum)\n\n    if best is None:\n        centers = np.tile([0.5,0.5], (n,1))\n        radii = compute_max_radii(centers)\n        return centers, radii, float(np.sum(radii))\n    return best\n\ndef compute_max_radii(centers, tol=1e-9, max_iter=3000):\n    centers = np.asarray(centers, dtype=float)\n    n = centers.shape[0]\n    if n == 0:\n        return np.array([], dtype=float)\n    border = np.minimum.reduce([centers[:,0], centers[:,1], 1-centers[:,0], 1-centers[:,1]])\n    border = np.maximum(border, 0.0)\n    dif = centers[:,None,:] - centers[None,:,:]\n    d = np.sqrt(np.sum(dif*dif, axis=2))\n    np.fill_diagonal(d, np.inf)\n    # init\n    r = border.copy()\n\n    # initial proportional shrink to remove gross overlaps\n    for _ in range(800):\n        moved = False\n        over = r - border\n        if np.any(over > tol):\n            r = np.minimum(r, border); moved = True\n        for i in range(n):\n            ri = r[i]\n            for j in range(i+1, n):\n                dij = d[i,j]\n                if not np.isfinite(dij) or dij <= tol:\n                    continue\n                rj = r[j]\n                if ri + rj > dij + tol:\n                    denom = ri + rj\n                    if denom > 0:\n                        scale = dij / denom\n                        if scale < 1.0 - 1e-12:\n                            r[i] = ri * scale\n                            r[j] = rj * scale\n                            ri = r[i]\n                            moved = True\n        if not moved:\n            break\n\n    # Gauss-Seidel fixed point to reach feasible maximal radii\n    for it in range(max_iter):\n        prev = r.copy()\n        for i in range(n):\n            limits = d[i,:] - r\n            limits[i] = np.inf\n            val = min(border[i], np.min(limits))\n            r[i] = max(val, 0.0)\n        if np.max(np.abs(r - prev)) < tol:\n            break\n\n    # conservative growth cycles to exploit slack\n    for cycle in range(8):\n        slack_wall = border - r\n        slack_pair = np.min(d - (r[None,:] + r[:,None]), axis=1)\n        slack_pair = np.maximum(slack_pair, 0.0)\n        allowed = np.minimum(slack_wall, slack_pair)\n        delta = 0.25 * allowed\n        if np.all(delta <= tol):\n            break\n        r += delta\n        # quick proportional fixes\n        for _ in range(300):\n            moved = False\n            for i in range(n):\n                for j in range(i+1, n):\n                    dij = d[i,j]\n                    if not np.isfinite(dij):\n                        continue\n                    if r[i] + r[j] > dij + 1e-12:\n                        scale = dij / (r[i] + r[j])\n                        r[i] *= scale; r[j] *= scale\n                        moved = True\n            over = r - border\n            if np.any(over > 1e-12):\n                r = np.minimum(r, border); moved = True\n            if not moved:\n                break\n\n    r = np.maximum(np.minimum(r, border), 0.0)\n    return r\n\n# EVOLVE-BLOCK-END\n\n# Fixed runner\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\nif __name__ == \"__main__\":\n    centers, radii, s = run_packing()\n    print(f\"Sum of radii: {s:.6f}\")", "language": "python", "parent_id": "ce780558-7beb-4c6b-8917-4071af9884ec", "generation": 2, "timestamp": 1759960473.796633, "iteration_found": 34, "metrics": {"validity": 1.0, "sum_radii": 2.2742049336217494, "target_ratio": 0.8630758761372863, "combined_score": 0.8630758761372863, "eval_time": 1.916308879852295}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Full rewrite", "parent_metrics": {"validity": 1.0, "sum_radii": 1.2747241896735293, "target_ratio": 0.4837662958912825, "combined_score": 0.4837662958912825}, "island": 0}, "prompts": {"full_rewrite_user": {"system": "You are an expert mathematician specializing in circle packing problems and computational geometry. Your task is to improve a constructor function that directly produces a specific arrangement of 26 circles in a unit square, maximizing the sum of their radii. The AlphaEvolve paper achieved a sum of 2.635 for n=26.\n\nKey geometric insights:\n- Circle packings often follow hexagonal patterns in the densest regions\n- Maximum density for infinite circle packing is pi/(2*sqrt(3)) \u2248 0.9069\n- Edge effects make square container packing harder than infinite packing\n- Circles can be placed in layers or shells when confined to a square\n- Similar radius circles often form regular patterns, while varied radii allow better space utilization\n- Perfect symmetry may not yield the optimal packing due to edge effects\n\nFocus on designing an explicit constructor that places each circle in a specific position, rather than an iterative search algorithm.\n\n\n## Evolution Insights (Global Learnings)\n\n### Common Pitfalls:\n\u26a0\ufe0f This is a clear win \u2014 identify and document the exact change (algorithmic tweak, vectorization, caching, or dependency upgrade) that produced the ~55% faster eval_time and verify it didn't trade off accuracy. Then lock it in with a CI microbenchmark that measures eval_time and fails if it regresses beyond a small threshold (e.g., 10%), and include a short profiling note in the PR so future changes preserve the optimization. (seen 2x)\n\u26a0\ufe0f Check recent changes to the radii calculation and upstream inputs for unintended scaling/clamping, integer division, coordinate-transform bugs or new filters that drop items \u2014 reproduce the regression with per-item debug logs or diffs to locate where radii mass was lost. Then add a regression test (e.g., assert sum_radii does not drop >20% vs baseline) and telemetry/alerts for large decreases so future commits are caught quickly. (seen 1x)\n\u26a0\ufe0f Reproduce the regression with the exact inputs/seeds and run a git-bisect to find the offending change, then inspect code paths that compute radii/target_ratio for recent edits\u2014look specifically for unintended clamping, normalization/scale-factor changes, unit conversions, or new filtering that would shrink sum_radii. Add an automated regression test that fails if target_ratio or sum_radii drops more than an acceptable threshold (e.g., 20%), add alerts for metric regressions, and add defensive checks/assertions to prevent inadvertent zeroing or aggressive scaling of radii. (seen 1x)\n\u26a0\ufe0f Bisect recent commits to find the change that caused sum_radii and target_ratio to drop, then add a CI regression test/benchmark that asserts combined_score and sum_radii remain within, e.g., 10% of a validated baseline so similar regressions fail fast. When you inspect the offending change, focus on any alterations to radii calculation, normalization/scaling (watch for unintended integer division or parameter/name changes), and add targeted profiling or assertions to ensure the normalization step still runs. (seen 1x)", "user": "# Current Program Information\n- Fitness: 0.4838\n- Feature coordinates: No feature coordinates\n- Focus areas: - Fitness declined: 0.8316 \u2192 0.4838. Consider revising recent changes.\n- Consider simplifying - code length exceeds 500 characters\n\n## Last Execution Output\n\n### stdout\n```\nRunning in subprocess, Python version: 3.12.1 (v3.12.1:2305ca5144, Dec  7 2023, 17:23:38) [Clang 13.0.0 (clang-1300.0.29.30)]\nProgram path: /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmpy0moli2k.py\nCalling run_packing()...\nrun_packing() returned successfully: sum_radii = 1.2747241896735293\nResults saved to /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmpmsgs4m5f.py.results\n\n```\n\n### stderr\n```\n\n```\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 3\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.1912, target_ratio: 0.8316, combined_score: 0.8316, eval_time: 0.1973\n- Outcome: Mixed results\n\n### Attempt 2\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.2400, target_ratio: 0.8501, combined_score: 0.8501, eval_time: 2.1468\n- Outcome: Mixed results\n\n### Attempt 1\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.2742, target_ratio: 0.8631, combined_score: 0.8631, eval_time: 1.8737\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.8631)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Improved explicit constructor for n=26 circle packing in the unit square.\n\nThis version:\n- Enumerates several hex-like row patterns and small variations (row offsets,\n  slight vertical compression/expansion, small translations).\n- For each candidate it places deterministic centers and computes maximal\n  feasible radii using a robust Gauss-Seidel projection followed by\n  conservative growth cycles and proportional fixes.\n- Returns the best layout found (max sum of radii).\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    # candidate row patterns summing to 26, chosen to break perfect symmetry\n    patterns = [\n        [6,5,5,5,5],\n        [5,6,5,5,5],\n        [6,5,6,5,4],\n        [5,6,6,5,4],\n        [4,6,6,5,5],\n    ]\n\n    # try a grid of horizontal spacings and small vertical scalings\n    s_vals = np.linspace(0.12, 0.20, 17)\n    y_scales = [0.94, 0.98, 1.0, 1.02]\n    best = None\n    best_sum = -1.0\n\n    for pattern in patterns:\n        rows = len(pattern)\n        max_cols = max(pattern)\n        for s in s_vals:\n            # compute nominal vertical step for hex pattern then apply small scales\n            v0 = (np.sqrt(3)/2.0) * s\n            for y_scale in y_scales:\n                v = v0 * y_scale\n                # Vertical centering\n                total_h = (rows - 1) * v\n                y0 = 0.5 - total_h / 2.0\n                # try both offset parity choices (first row offset or not)\n                for offset_start in (False, True):\n                    centers = []\n                    for r_idx, cnt in enumerate(pattern):\n                        y = y0 + r_idx * v\n                        row_width = (cnt - 1) * s if cnt > 1 else 0.0\n                        x_start = 0.5 - row_width / 2.0\n                        # determine offset for this row (hex-like)\n                        offset = (r_idx % 2 == 1) if offset_start else (r_idx % 2 == 0)\n                        if offset:\n                            x_start += s/2.0\n                        for j in range(cnt):\n                            centers.append([x_start + j * s, y])\n                    centers = np.array(centers[:n], dtype=float)\n                    # small deterministic shifts to help edge packing\n                    for dx,dy in ((0,0),(+0.006,0),( -0.006,0),(0,+0.006),(0,-0.006 )):\n                        cand = centers + np.array([dx,dy])\n                        cand = np.clip(cand, 1e-8, 1-1e-8)\n                        radii = compute_max_radii(cand)\n                        ssum = float(np.sum(radii))\n                        if ssum > best_sum:\n                            best_sum = ssum\n                            best = (cand, radii, ssum)\n\n    # fallback: if nothing found (shouldn't), return a central cluster\n    if best is None:\n        centers = np.array([[0.5,0.5]]*n)\n        radii = compute_max_radii(centers)\n        return centers, radii, float(np.sum(radii))\n\n    return best\n\ndef compute_max_radii(centers, tol=1e-9, max_iter=3000):\n    \"\"\"\n    Gauss-Seidel fixed point: r_i <- min(border_i, min_j (d_ij - r_j))\n    Then perform conservative growth cycles and proportional fixes to\n    exploit slack without creating overlaps.\n    \"\"\"\n    centers = np.asarray(centers, dtype=float)\n    n = centers.shape[0]\n    # border limits\n    border = np.minimum.reduce([centers[:,0], centers[:,1], 1-centers[:,0], 1-centers[:,1]])\n    border = np.maximum(border, 0.0)\n\n    # pairwise distances\n    dif = centers[:,None,:] - centers[None,:,:]\n    d = np.sqrt(np.sum(dif*dif, axis=2))\n    np.fill_diagonal(d, np.inf)\n\n    # initialize radii conservatively at border distance\n    r = border.copy()\n\n    # initial shrink pass to remove overlaps (proportional scaling)\n    for _ in range(1200):\n        moved = False\n        # wall constraints\n        over = r - border\n        if np.any(over > tol):\n            r = np.minimum(r, border)\n            moved = True\n        # pairwise resolution\n        for i in range(n):\n            ri = r[i]\n            for j in range(i+1, n):\n                dij = d[i,j]\n                if not np.isfinite(dij) or dij <= tol:\n                    continue\n                rj = r[j]\n                if ri + rj > dij + tol:\n                    # scale both proportionally (if both > 0)\n                    denom = ri + rj\n                    if denom <= 0:\n                        # if both zero skip\n                        continue\n                    scale = dij / denom\n                    if scale < 1.0 - 1e-12:\n                        r[i] = ri * scale\n                        r[j] = rj * scale\n                        ri = r[i]\n                        moved = True\n        if not moved:\n            break\n\n    # Gauss-Seidel fixed-point to converge to maximal feasible radii\n    for it in range(max_iter):\n        prev = r.copy()\n        for i in range(n):\n            limits = d[i,:] - r\n            limits[i] = np.inf\n            # min over neighbor limits and border\n            val = min(border[i], np.min(limits))\n            r[i] = max(val, 0.0)\n        if np.max(np.abs(r - prev)) < tol:\n            break\n\n    # Conservative growth cycles: increase a fraction of slack, then re-fix\n    for cycle in range(8):\n        # slack by walls and neighbors\n        slack_wall = border - r\n        slack_pair = np.min(d - (r[None,:] + r[:,None]), axis=1)\n        slack_pair = np.maximum(slack_pair, 0.0)\n        allowed_inc = np.minimum(slack_wall, slack_pair)\n        allowed_inc = np.maximum(allowed_inc, 0.0)\n        delta = 0.25 * allowed_inc\n        if np.all(delta <= tol):\n            break\n        r += delta\n        # quick proportional fix to restore feasibility\n        for _ in range(300):\n            moved = False\n            # pairwise check\n            for i in range(n):\n                for j in range(i+1, n):\n                    dij = d[i,j]\n                    if not np.isfinite(dij):\n                        continue\n                    if r[i] + r[j] > dij + 1e-12:\n                        scale = dij / (r[i] + r[j])\n                        r[i] *= scale\n                        r[j] *= scale\n                        moved = True\n            # wall enforcement\n            over = r - border\n            if np.any(over > 1e-12):\n                r = np.minimum(r, border)\n                moved = True\n            if not moved:\n                break\n\n    # final safety clamp and non-negativity\n    r = np.maximum(np.minimum(r, border), 0.0)\n    return r\n\n# EVOLVE-BLOCK-END\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\", linewidth=0.6)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.2742), Performs well on target_ratio (0.8631), Performs well on combined_score (0.8631), Performs well on eval_time (1.8737)\n\n### Program 2 (Score: 0.8501)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for n=26 circle packing in the unit square.\n\nStrategy:\n- Try a small ensemble of plausible hex-like row patterns that sum to 26.\n- For each pattern scan a modest 2D parameter grid:\n    - horizontal center spacing s\n    - vertical compression factor vf (multiplies sqrt(3)/2 * s)\n  and two stagger choices, plus tiny global shifts.\n- For each candidate compute maximal feasible radii for the fixed centers\n  by a fast Gauss-Seidel fixed-point projection (r_i <- min(border_i, min_j d_ij - r_j)).\n- Return the best candidate (largest sum of radii). Entirely deterministic.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    # row patterns summing to 26 (varied shapes to reduce edge effects)\n    patterns = [\n        [6,5,5,5,5],   # common compact layout\n        [6,5,6,5,4],\n        [5,6,6,5,4],\n        [5,6,5,6,4],\n        [4,6,6,6,4],\n    ]\n\n    best = None\n    best_sum = -1.0\n\n    # horizontal spacings to try (center-to-center); smaller s -> denser block\n    s_vals = np.linspace(0.12, 0.22, 25)  # tradeoff: resolution vs speed\n    # vertical factor multiplies the ideal hex vertical step (sqrt(3)/2*s)\n    vf_vals = np.linspace(0.88, 1.06, 7)   # allow slight compression/expansion\n    shifts = [(0.0,0.0), (0.015,0.0), (-0.015,0.0), (0.0,0.015), (0.0,-0.015)]\n\n    for rows in patterns:\n        R = len(rows)\n        max_cols = max(rows)\n        for s in s_vals:\n            # base vertical step\n            base_v = np.sqrt(3.0) / 2.0 * s\n            for vf in vf_vals:\n                vstep = base_v * vf\n                # compute block height and early prune if impossible\n                total_h = 0.0 if R == 1 else (R - 1) * vstep\n                if total_h + 1e-12 > 1.0:  # won't fit vertically\n                    continue\n                for offset_start in (False, True):\n                    # build nominal centers centered in square\n                    y0 = 0.5 - total_h / 2.0\n                    centers = []\n                    for i in range(R):\n                        m = rows[i]\n                        offset = (i % 2 == 1) if offset_start else (i % 2 == 0)\n                        row_span = 0.0 if m == 1 else (m - 1) * s\n                        start_x = 0.5 - row_span / 2.0\n                        if offset:\n                            start_x += s / 2.0\n                        # clamp start so centers remain inside [0,1]\n                        start_x = max(start_x, 0.0)\n                        last_x = start_x + (m - 1) * s\n                        if last_x > 1.0:\n                            start_x -= (last_x - 1.0)\n                        y = y0 + i * vstep\n                        for k in range(m):\n                            x = start_x + k * s\n                            centers.append([x, y])\n                    centers = np.array(centers, dtype=float)\n                    if centers.shape[0] < n:\n                        # pad with small extras near a corner\n                        needed = n - centers.shape[0]\n                        extra_xs = np.linspace(0.02, 0.18, needed)\n                        extras = np.vstack([extra_xs, np.full(needed, 0.02)]).T\n                        centers = np.vstack((centers, extras))\n                    elif centers.shape[0] > n:\n                        centers = centers[:n]\n\n                    # try tiny global shifts to exploit edge slack\n                    for dx, dy in shifts:\n                        cand = centers + np.array([dx, dy])\n                        cand = np.clip(cand, 1e-9, 1.0 - 1e-9)\n                        radii = compute_max_radii(cand)\n                        ssum = float(np.sum(radii))\n                        if ssum > best_sum + 1e-12:\n                            best_sum = ssum\n                            best = (cand, radii, ssum)\n\n    # fallback: if nothing found (shouldn't happen), return a simple pattern\n    if best is None:\n        centers = np.tile([0.5,0.5], (n,1))\n        radii = compute_max_radii(centers)\n        return centers, radii, float(np.sum(radii))\n\n    return best\n\ndef compute_max_radii(centers, max_iter=1200, tol=1e-7):\n    \"\"\"\n    Gauss-Seidel fixed-point projection to maximal radii for fixed centers:\n      r_i <= border_i\n      r_i + r_j <= d_ij\n    Update rule (in-place): r_i <- min(border_i, min_j (d_ij - r_j))\n    Uses vectorized pairwise distances (n small so acceptable).\n    \"\"\"\n    centers = np.asarray(centers, dtype=float)\n    n = centers.shape[0]\n    # distances to walls\n    b = np.minimum.reduce([centers[:,0], centers[:,1], 1.0-centers[:,0], 1.0-centers[:,1]])\n    b = np.maximum(b, 0.0)\n    # pairwise distances\n    dif = centers[:,None,:] - centers[None,:,:]\n    d = np.sqrt(np.sum(dif*dif, axis=2))\n    np.fill_diagonal(d, np.inf)\n    # initialize radii at border distances (feasible) but slightly reduced to let growth converge\n    r = b.copy()\n    # Gauss-Seidel iterations\n    for it in range(max_iter):\n        prev = r.copy()\n        for i in range(n):\n            # compute limits d_ij - r_j using current r (some updated this iter)\n            limits = d[i,:] - r\n            # ignore self\n            limits[i] = np.inf\n            r_new = np.minimum(b[i], np.min(limits))\n            if r_new < 0.0:\n                r_new = 0.0\n            r[i] = r_new\n        if np.max(np.abs(r - prev)) < tol:\n            break\n\n    # small proportional fixes for any remaining tiny overlaps\n    slack = d - (r[:,None] + r[None,:])\n    # where slack < 0, scale the two radii proportionally\n    for i in range(n):\n        for j in range(i+1, n):\n            if slack[i,j] < -1e-12:\n                # reduce both proportionally to remove overlap\n                total = r[i] + r[j]\n                if total <= 0.0:\n                    r[i] = r[j] = 0.0\n                else:\n                    scale = d[i,j] / total\n                    r[i] *= scale\n                    r[j] *= scale\n\n    # final clamp\n    r = np.minimum(r, b)\n    r = np.maximum(r, 0.0)\n    return r\n\n# EVOLVE-BLOCK-END\n\n# fixed runner (unchanged API)\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\nif __name__ == \"__main__\":\n    centers, radii, s = run_packing()\n    print(f\"Sum of radii: {s:.6f}\")\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.2400), Performs well on target_ratio (0.8501), Performs well on combined_score (0.8501), Performs well on eval_time (2.1468)\n\n### Program 3 (Score: 0.8316)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (pattern search over explicit layouts)\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    \"\"\"\n    Build several deterministic hex-like layouts (different row patterns),\n    scan a small range of horizontal spacings s and pick the best resulting\n    packing (max sum of radii). This remains a constructive method: we\n    place centers by formula and only perform a deterministic radius projection.\n    \"\"\"\n    n = 26\n    # Several plausible hex-like row patterns summing to 26\n    patterns = [\n        [5, 6, 5, 5, 5],\n        [6, 5, 5, 5, 5],\n        [6, 5, 6, 5, 4],\n        [5, 6, 6, 5, 4],\n    ]\n\n    best = None\n    best_sum = -1.0\n\n    # modest grid of spacings to try (center-to-center horizontally)\n    s_vals = np.linspace(0.13, 0.20, 15)\n\n    for rows in patterns:\n        R = len(rows)\n        maxk = max(rows)\n        for s in s_vals:\n            h = np.sqrt(3) / 2 * s\n            # center vertically the block in [0,1]\n            total_h = (R - 1) * h\n            y0 = 0.5 - total_h / 2.0\n            centers = []\n            for r_idx, k in enumerate(rows):\n                y = y0 + r_idx * h\n                row_width = (k - 1) * s\n                x_start = 0.5 - row_width / 2.0\n                # stagger alternate rows for hex adjacency\n                if r_idx % 2 == 1:\n                    x_start += s / 2.0\n                for j in range(k):\n                    centers.append([x_start + j * s, y])\n            centers = np.array(centers[:n], dtype=float)\n            # Slightly clip to avoid exact-border degeneracies\n            centers = np.clip(centers, 1e-6, 1 - 1e-6)\n\n            radii = compute_max_radii(centers)\n            ssum = float(np.sum(radii))\n            if ssum > best_sum:\n                best_sum = ssum\n                best = (centers, radii, ssum)\n\n    # Fallback: if nothing found (shouldn't happen), return a simple pattern\n    if best is None:\n        centers = np.array([[0.5,0.5]]*n)\n        radii = compute_max_radii(centers)\n        return centers, radii, float(np.sum(radii))\n\n    return best\n\ndef compute_max_radii(centers, tol=1e-9):\n    \"\"\"\n    Deterministic projection to feasible radii:\n      - start with wall distances\n      - repeatedly fix pairwise overlaps by proportional shrinking\n      - apply a few conservative fractional growth passes to use slack\n    \"\"\"\n    centers = np.asarray(centers, dtype=float)\n    n = centers.shape[0]\n    # distance to wall\n    wall = np.minimum.reduce([centers[:,0], centers[:,1], 1-centers[:,0], 1-centers[:,1]])\n    # start with wall-limited radii\n    r = wall.copy()\n\n    # pairwise distances\n    dif = centers[:,None,:] - centers[None,:,:]\n    d = np.sqrt(np.sum(dif*dif, axis=2))\n    np.fill_diagonal(d, np.inf)\n\n    # Shrink overlaps: proportional scaling until no violations\n    for _ in range(1500):\n        moved = False\n        # ensure wall constraints\n        over = r - wall\n        if np.any(over > tol):\n            r = np.minimum(r, wall)\n            moved = True\n        # pairwise\n        for i in range(n):\n            ri = r[i]\n            # vectorized check for j>i could be done, but keep simple deterministic loop\n            for j in range(i+1, n):\n                dij = d[i,j]\n                if not np.isfinite(dij) or dij <= tol:\n                    continue\n                rj = r[j]\n                if ri + rj > dij + tol:\n                    # scale both proportionally\n                    scale = dij / (ri + rj)\n                    if scale < 1.0 - 1e-12:\n                        r[i] = ri * scale\n                        r[j] = rj * scale\n                        ri = r[i]  # update local var\n                        moved = True\n        if not moved:\n            break\n\n    # Conservative growth cycles: small fraction of available slack\n    for cycle in range(10):\n        # available increase by walls\n        inc_wall = wall - r\n        # available by neighbors: for each i, min_j (d_ij - (r_i + r_j)) + r_i => max increase = min_j(d_ij - (r_i + r_j))\n        avail = np.full(n, np.inf)\n        # compute pairwise slack matrix\n        slack = d - (r[None,:] + r[:,None])\n        # for each i, the maximum increase without immediate overlap is min(slack[i,:])\n        avail = np.minimum(avail, np.min(slack, axis=1))\n        # overall allowed inc is min(inc_wall, avail)\n        max_inc = np.minimum(inc_wall, avail)\n        max_inc = np.maximum(max_inc, 0.0)\n        # apply a cautious fraction\n        delta = 0.3 * max_inc\n        if np.all(delta <= tol):\n            break\n        r += delta\n        # re-shrink a few iterations to remove any slight violations\n        for _ in range(200):\n            moved = False\n            # enforce pairwise\n            for i in range(n):\n                for j in range(i+1, n):\n                    dij = d[i,j]\n                    if not np.isfinite(dij) or dij <= tol:\n                        continue\n                    if r[i] + r[j] > dij + tol:\n                        scale = dij / (r[i] + r[j])\n                        r[i] *= scale\n                        r[j] *= scale\n                        moved = True\n            # wall enforcement\n            over = r - wall\n            if np.any(over > tol):\n                r = np.minimum(r, wall)\n                moved = True\n            if not moved:\n                break\n\n    r = np.maximum(r, 0.0)\n    return r\n\n# EVOLVE-BLOCK-END\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, ec=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.1912), Performs well on target_ratio (0.8316), Performs well on combined_score (0.8316), Performs well on eval_time (0.1973)\n\n\n\n## Diverse Programs\n\n### Program D1 (Score: 0.8298)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for n=26 circle packing in the unit square.\n\nStrategy:\n- Try several sensible row-distributions (rows=3..7) that partition 26.\n- For each layout compute a conservative uniform radius r that fits vertically\n  and horizontally (accounting for alternating offsets).\n- Build centers from the best layout, then run a Gauss-Seidel fixed-point\n  iteration to compute the maximal feasible radii for those fixed centers.\nThis approach is explicit (no stochastic search) and yields high sum of radii.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    sqrt3 = np.sqrt(3.0)\n\n    def vertical_limit(rows):\n        if rows == 1:\n            return 0.5\n        # r + (rows-1)*sqrt3*r + r <= 1  => r * ((rows-1)*sqrt3 + 2) <= 1\n        return 1.0 / (((rows - 1) * sqrt3) + 2.0)\n\n    def horizontal_limit(m, offset):\n        # conservative horizontal spacing: if offset, need a half-slot\n        return 1.0 / (2.0 * m + (1.0 if offset else 0.0))\n\n    best_score = -1.0\n    best_layout = None  # (rows, counts_list, offset_start, r_candidate)\n\n    # try rows from 3..7 and two simple ways to distribute remainder\n    for rows in range(3, 8):\n        base = n // rows\n        rem = n % rows\n        for prefix_heavy in (True, False):\n            if prefix_heavy:\n                counts = [base + 1 if i < rem else base for i in range(rows)]\n            else:\n                counts = [base if i < (rows - rem) else base + 1 for i in range(rows)]\n            for offset_start in (False, True):\n                r_v = vertical_limit(rows)\n                r_hs = []\n                for i, m in enumerate(counts):\n                    offset = (i % 2 == 1) if offset_start else (i % 2 == 0)\n                    r_hs.append(horizontal_limit(m, offset))\n                r_candidate = min(r_v, min(r_hs))\n                score = n * r_candidate\n                if score > best_score:\n                    best_score = score\n                    best_layout = (rows, counts, offset_start, r_candidate)\n\n    # Build centers from chosen layout\n    rows, counts, offset_start, r = best_layout\n    centers = []\n    vstep = sqrt3 * r\n    total_height = 0.0 if rows == 1 else (rows - 1) * vstep\n    y0 = 0.5 - total_height / 2.0\n    for i in range(rows):\n        m = counts[i]\n        offset = (i % 2 == 1) if offset_start else (i % 2 == 0)\n        row_span = 0.0 if m == 1 else (m - 1) * 2.0 * r\n        start_x = 0.5 - row_span / 2.0\n        if offset:\n            start_x += r\n        # clamp to keep centers inside [r, 1-r]\n        start_x = max(start_x, r)\n        last_x = start_x + (m - 1) * 2.0 * r\n        if last_x > 1.0 - r:\n            start_x -= (last_x - (1.0 - r))\n        y = y0 + i * vstep\n        for k in range(m):\n            x = start_x + k * 2.0 * r\n            centers.append([x, y])\n\n    centers = np.array(centers, dtype=float)\n    # trim/pad to exactly n\n    if centers.shape[0] > n:\n        centers = centers[:n]\n    elif centers.shape[0] < n:\n        needed = n - centers.shape[0]\n        # add small anchors near corners/edges to use leftover space\n        extras = np.array([[0.02 + 0.96*(i/(needed-1 if needed>1 else 1)), 0.02] for i in range(needed)])\n        centers = np.vstack((centers, extras))\n\n    # Final radius computation for fixed centers\n    radii = compute_max_radii(centers)\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\ndef compute_max_radii(centers, max_iter=5000, tol=1e-9):\n    centers = np.asarray(centers, dtype=float)\n    n = centers.shape[0]\n    if n == 0:\n        return np.array([], dtype=float)\n    # border distances\n    b = np.minimum.reduce([centers[:,0], centers[:,1], 1.0 - centers[:,0], 1.0 - centers[:,1]])\n    b = np.maximum(b, 0.0)\n    # pairwise distances\n    diff = centers[:, None, :] - centers[None, :, :]\n    d = np.sqrt(np.sum(diff * diff, axis=2))\n    np.fill_diagonal(d, np.inf)\n    # initialize radii at border distances (feasible)\n    rads = b.copy()\n    # Gauss-Seidel fixed-point iteration: r_i <- min(b_i, min_j (d_ij - r_j))\n    for it in range(max_iter):\n        prev = rads.copy()\n        for i in range(n):\n            limits = d[i, :] - rads  # d_ij - r_j using latest r_j\n            limits[i] = np.inf\n            r_new = min(b[i], np.min(limits))\n            rads[i] = max(0.0, r_new)\n        if np.max(np.abs(rads - prev)) < tol:\n            break\n    # final small corrective pass for any numerical violations\n    for i in range(n):\n        for j in range(i+1, n):\n            dij = d[i,j]\n            if rads[i] + rads[j] > dij + 1e-12:\n                scale = dij / (rads[i] + rads[j])\n                rads[i] *= scale\n                rads[j] *= scale\n    rads = np.minimum(rads, b)\n    rads = np.maximum(rads, 0.0)\n    return rads\n\n# EVOLVE-BLOCK-END\n\n# fixed runner API\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n# optional visualization helper (unchanged API)\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(6,6))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect('equal')\n    for c, rr in zip(centers, radii):\n        ax.add_patch(Circle(c, rr, alpha=0.6, edgecolor='k'))\n    plt.title(f\"n={len(centers)}, sum radii={sum(radii):.6f}\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, s = run_packing()\n    print(f\"Sum of radii: {s:.6f}\")\n```\nKey features: Alternative approach to validity, Alternative approach to sum_radii\n\n### Program D2 (Score: 0.8298)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 using compact hex-like layouts\nand a robust Gauss-Seidel relaxation to compute maximal radii for fixed centers.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    sqrt3 = np.sqrt(3.0)\n\n    def vertical_limit(rows):\n        if rows == 1:\n            return 0.5\n        return 1.0 / (((rows - 1) * sqrt3) + 2.0)\n\n    def horizontal_limit(m, offset):\n        # conservative: if offset, need extra margin\n        return 1.0 / (2.0 * m + (1.0 if offset else 0.0))\n\n    best_score = -1.0\n    best_layout = None\n    # try row counts from 3..7 rows distributing 26 items\n    for rows in range(3, 8):\n        base = n // rows\n        rem = n % rows\n        for prefix_heavy in (True, False):\n            if prefix_heavy:\n                counts = [base + 1 if i < rem else base for i in range(rows)]\n            else:\n                counts = [base if i < (rows - rem) else base + 1 for i in range(rows)]\n            for offset_start in (False, True):\n                r_v = vertical_limit(rows)\n                r_hs = []\n                for i, m in enumerate(counts):\n                    offset = (i % 2 == 1) if offset_start else (i % 2 == 0)\n                    r_hs.append(horizontal_limit(m, offset))\n                r_candidate = min(r_v, min(r_hs))\n                score = n * r_candidate\n                if score > best_score:\n                    best_score = score\n                    best_layout = (rows, counts, offset_start, r_candidate)\n\n    rows, counts, offset_start, r = best_layout\n    centers = []\n    vstep = sqrt3 * r\n    total_height = 0.0 if rows == 1 else (rows - 1) * vstep\n    y0 = 0.5 - total_height / 2.0\n    for i in range(rows):\n        m = counts[i]\n        offset = (i % 2 == 1) if offset_start else (i % 2 == 0)\n        row_span = 0.0 if m == 1 else (m - 1) * 2.0 * r\n        start_x = 0.5 - row_span / 2.0\n        if offset:\n            start_x += r\n        # clamp to keep centers inside [r,1-r]\n        start_x = max(start_x, r)\n        last_x = start_x + (m - 1) * 2.0 * r\n        if last_x > 1.0 - r:\n            start_x -= (last_x - (1.0 - r))\n        y = y0 + i * vstep\n        for k in range(m):\n            x = start_x + k * 2.0 * r\n            centers.append((x, y))\n\n    centers = np.array(centers, dtype=float)\n    # trim/pad to exactly n\n    if centers.shape[0] > n:\n        centers = centers[:n]\n    elif centers.shape[0] < n:\n        needed = n - centers.shape[0]\n        extras = np.vstack([[0.02 + 0.96 * (i / max(1, needed - 1)), 0.02] for i in range(needed)])\n        centers = np.vstack((centers, extras))\n\n    radii = compute_max_radii(centers)\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\ndef compute_max_radii(centers, max_iter=2000, tol=1e-8):\n    n = centers.shape[0]\n    # distance to borders\n    b = np.minimum.reduce([centers[:, 0], centers[:, 1], 1.0 - centers[:, 0], 1.0 - centers[:, 1]])\n    b = np.maximum(b, 0.0)\n    # pairwise distances\n    diff = centers[:, None, :] - centers[None, :, :]\n    d = np.sqrt(np.sum(diff * diff, axis=2))\n    np.fill_diagonal(d, np.inf)\n    # initialize radii at border distances (feasible)\n    rads = b.copy()\n    for it in range(max_iter):\n        prev = rads.copy()\n        # Gauss-Seidel: update in place\n        for i in range(n):\n            limits = d[i, :] - rads\n            limits[i] = np.inf\n            r_new = min(b[i], np.min(limits))\n            if r_new < 0.0:\n                r_new = 0.0\n            rads[i] = r_new\n        if np.max(np.abs(rads - prev)) < tol:\n            break\n    # clean tiny violations\n    for i in range(n):\n        for j in range(i + 1, n):\n            dij = d[i, j]\n            if rads[i] + rads[j] > dij + 1e-12:\n                scale = dij / (rads[i] + rads[j])\n                rads[i] *= scale\n                rads[j] *= scale\n    rads = np.minimum(rads, b)\n    rads = np.maximum(rads, 0.0)\n    return rads\n\n# fixed runner\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n# optional visualization helper\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(6,6))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect('equal')\n    for c, rr in zip(centers, radii):\n        ax.add_patch(Circle(c, rr, alpha=0.6, edgecolor='k'))\n    plt.title(f\"n={len(centers)}, sum radii={sum(radii):.6f}\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, s = run_packing()\n    print(f\"Sum of radii: {s:.6f}\")\n# EVOLVE-BLOCK-END\n```\nKey features: Alternative approach to validity, Alternative approach to sum_radii\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 0.7201, Type: Alternative)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for n=26 circle packing in the unit square.\n\nStrategy:\n- Try a small ensemble of compact hex-like row patterns that sum to 26.\n- For each pattern compute a conservative uniform spacing (candidate radius)\n  so the pattern fits horizontally and vertically.\n- Build centers from the best candidate and compute maximal radii for those\n  fixed centers via a monotone Gauss-Seidel fixed-point:\n    r_i <- min(border_i, min_j (d_ij - r_j))\n  which converges to the maximal feasible radii.\nThis combines the strong ideas from earlier high-performing attempts.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    # A selection of plausible row patterns summing to 26 (vary symmetry)\n    patterns = [\n        [6,5,5,5,5],   # common compact\n        [5,6,5,6,4],\n        [6,6,5,5,4],\n        [5,5,6,5,5],\n        [6,5,6,5,4],\n    ]\n\n    def vertical_limit(rows, r):\n        # ensure vertical stack of centers separated by sqrt(3)*r fits in [r,1-r]\n        if rows == 1:\n            return 0.5\n        return 1.0 / (((rows - 1) * np.sqrt(3.0)) + 2.0)\n\n    def horizontal_limit(m, offset):\n        # conservative: if offset row, need extra half-step at edges\n        return 1.0 / (2.0 * m + (1.0 if offset else 0.0))\n\n    best = None  # (sum_radii, centers, radii)\n    for pat in patterns:\n        rows = len(pat)\n        # try both choices of which row type is offset\n        for offset_start in (False, True):\n            # compute candidate uniform radius r_c that would be safe\n            r_v = vertical_limit(rows, None)\n            r_hs = []\n            for i, m in enumerate(pat):\n                offset = (i % 2 == 1) if offset_start else (i % 2 == 0)\n                r_hs.append(horizontal_limit(m, offset))\n            r_candidate = min(r_v, min(r_hs))\n            # create centers using hex spacing based on r_candidate\n            r = r_candidate\n            vstep = np.sqrt(3.0) * r\n            total_height = 0.0 if rows == 1 else (rows - 1) * vstep\n            y0 = 0.5 - total_height / 2.0\n            centers = []\n            for i, m in enumerate(pat):\n                offset = (i % 2 == 1) if offset_start else (i % 2 == 0)\n                row_span = 0.0 if m == 1 else (m - 1) * 2.0 * r\n                start_x = 0.5 - row_span / 2.0\n                if offset:\n                    start_x += r\n                # clamp to [r,1-r] and nudge if necessary\n                start_x = max(start_x, r)\n                last_x = start_x + (m - 1) * 2.0 * r\n                if last_x > 1.0 - r:\n                    start_x -= (last_x - (1.0 - r))\n                y = y0 + i * vstep\n                for k in range(m):\n                    centers.append([start_x + k * 2.0 * r, y])\n            centers = np.array(centers, dtype=float)\n            # trim/pad to exactly n\n            if centers.shape[0] > n:\n                centers = centers[:n]\n            elif centers.shape[0] < n:\n                needed = n - centers.shape[0]\n                # place extras near small inward offsets from corners\n                extras = np.array([[0.02 + 0.96*(i/(max(1,needed-1))), 0.02] for i in range(needed)])\n                centers = np.vstack((centers, extras))\n            centers = np.clip(centers, 1e-9, 1.0 - 1e-9)\n            radii = compute_max_radii(centers)\n            s = float(np.sum(radii))\n            if best is None or s > best[0]:\n                best = (s, centers, radii)\n\n    # small deterministic local shifts: try nudges to escape edge alignment issues\n    best_s, best_centers, best_radii = best\n    shifts = [(0.0,0.0),(+0.005,0.0),(-0.005,0.0),(0.0,+0.005),(0.0,-0.005)]\n    for dx,dy in shifts:\n        pts = np.clip(best_centers + np.array([dx,dy]), 1e-9, 1.0-1e-9)\n        r2 = compute_max_radii(pts)\n        s2 = float(np.sum(r2))\n        if s2 > best_s + 1e-12:\n            best_s, best_centers, best_radii = s2, pts, r2\n\n    return best_centers, best_radii, float(best_s)\n\ndef compute_max_radii(centers, max_iter=5000, tol=1e-10):\n    n = centers.shape[0]\n    # distances to walls\n    b = np.minimum.reduce([centers[:,0], centers[:,1], 1.0-centers[:,0], 1.0-centers[:,1]])\n    b = np.maximum(b, 0.0)\n    # pairwise distances\n    diff = centers[:,None,:] - centers[None,:,:]\n    d = np.sqrt(np.sum(diff*diff, axis=2))\n    np.fill_diagonal(d, np.inf)\n    # initialize radii to border distances (feasible)\n    rads = b.copy()\n    # Gauss-Seidel fixed-point: r_i = min(b_i, min_j (d_ij - r_j))\n    for it in range(max_iter):\n        prev = rads.copy()\n        for i in range(n):\n            limits = d[i,:] - rads\n            limits[i] = np.inf\n            r_new = min(b[i], np.min(limits))\n            rads[i] = max(0.0, r_new)\n        if np.max(np.abs(rads - prev)) < tol:\n            break\n    # final small cleanup to remove any numerical violations\n    for i in range(n):\n        for j in range(i+1,n):\n            dij = d[i,j]\n            if rads[i] + rads[j] > dij + 1e-12:\n                scale = dij / (rads[i] + rads[j])\n                rads[i] *= scale\n                rads[j] *= scale\n    rads = np.minimum(rads, b)\n    rads = np.maximum(rads, 0.0)\n    return rads\n# EVOLVE-BLOCK-END\n\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(6,6))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect(\"equal\")\n    for i,(c,r) in enumerate(zip(centers,radii)):\n        ax.add_patch(Circle(c,r,alpha=0.6,edgecolor='k'))\n        ax.text(c[0],c[1],str(i),ha='center',va='center',fontsize=8)\n    plt.title(f\"n={len(centers)} sum={sum(radii):.6f}\"); plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # visualize(centers, radii)\n```\nUnique approach: Modification: Full rewrite, Excellent validity (1.000), Excellent sum_radii (1.897)\n\n### Inspiration 2 (Score: 0.8298, Type: High-Performer)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for n=26 circle packing in the unit square.\n\nStrategy:\n- Try several sensible row-distributions (rows=3..7) that partition 26.\n- For each layout compute a conservative uniform radius r that fits vertically\n  and horizontally (accounting for alternating offsets).\n- Build centers from the best layout, then run a Gauss-Seidel fixed-point\n  iteration to compute the maximal feasible radii for those fixed centers.\nThis approach is explicit (no stochastic search) and yields high sum of radii.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    sqrt3 = np.sqrt(3.0)\n\n    def vertical_limit(rows):\n        if rows == 1:\n            return 0.5\n        # r + (rows-1)*sqrt3*r + r <= 1  => r * ((rows-1)*sqrt3 + 2) <= 1\n        return 1.0 / (((rows - 1) * sqrt3) + 2.0)\n\n    def horizontal_limit(m, offset):\n        # conservative horizontal spacing: if offset, need a half-slot\n        return 1.0 / (2.0 * m + (1.0 if offset else 0.0))\n\n    best_score = -1.0\n    best_layout = None  # (rows, counts_list, offset_start, r_candidate)\n\n    # try rows from 3..7 and two simple ways to distribute remainder\n    for rows in range(3, 8):\n        base = n // rows\n        rem = n % rows\n        for prefix_heavy in (True, False):\n            if prefix_heavy:\n                counts = [base + 1 if i < rem else base for i in range(rows)]\n            else:\n                counts = [base if i < (rows - rem) else base + 1 for i in range(rows)]\n            for offset_start in (False, True):\n                r_v = vertical_limit(rows)\n                r_hs = []\n                for i, m in enumerate(counts):\n                    offset = (i % 2 == 1) if offset_start else (i % 2 == 0)\n                    r_hs.append(horizontal_limit(m, offset))\n                r_candidate = min(r_v, min(r_hs))\n                score = n * r_candidate\n                if score > best_score:\n                    best_score = score\n                    best_layout = (rows, counts, offset_start, r_candidate)\n\n    # Build centers from chosen layout\n    rows, counts, offset_start, r = best_layout\n    centers = []\n    vstep = sqrt3 * r\n    total_height = 0.0 if rows == 1 else (rows - 1) * vstep\n    y0 = 0.5 - total_height / 2.0\n    for i in range(rows):\n        m = counts[i]\n        offset = (i % 2 == 1) if offset_start else (i % 2 == 0)\n        row_span = 0.0 if m == 1 else (m - 1) * 2.0 * r\n        start_x = 0.5 - row_span / 2.0\n        if offset:\n            start_x += r\n        # clamp to keep centers inside [r, 1-r]\n        start_x = max(start_x, r)\n        last_x = start_x + (m - 1) * 2.0 * r\n        if last_x > 1.0 - r:\n            start_x -= (last_x - (1.0 - r))\n        y = y0 + i * vstep\n        for k in range(m):\n            x = start_x + k * 2.0 * r\n            centers.append([x, y])\n\n    centers = np.array(centers, dtype=float)\n    # trim/pad to exactly n\n    if centers.shape[0] > n:\n        centers = centers[:n]\n    elif centers.shape[0] < n:\n        needed = n - centers.shape[0]\n        # add small anchors near corners/edges to use leftover space\n        extras = np.array([[0.02 + 0.96*(i/(needed-1 if needed>1 else 1)), 0.02] for i in range(needed)])\n        centers = np.vstack((centers, extras))\n\n    # Final radius computation for fixed centers\n    radii = compute_max_radii(centers)\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\ndef compute_max_radii(centers, max_iter=5000, tol=1e-9):\n    centers = np.asarray(centers, dtype=float)\n    n = centers.shape[0]\n    if n == 0:\n        return np.array([], dtype=float)\n    # border distances\n    b = np.minimum.reduce([centers[:,0], centers[:,1], 1.0 - centers[:,0], 1.0 - centers[:,1]])\n    b = np.maximum(b, 0.0)\n    # pairwise distances\n    diff = centers[:, None, :] - centers[None, :, :]\n    d = np.sqrt(np.sum(diff * diff, axis=2))\n    np.fill_diagonal(d, np.inf)\n    # initialize radii at border distances (feasible)\n    rads = b.copy()\n    # Gauss-Seidel fixed-point iteration: r_i <- min(b_i, min_j (d_ij - r_j))\n    for it in range(max_iter):\n        prev = rads.copy()\n        for i in range(n):\n            limits = d[i, :] - rads  # d_ij - r_j using latest r_j\n            limits[i] = np.inf\n            r_new = min(b[i], np.min(limits))\n            rads[i] = max(0.0, r_new)\n        if np.max(np.abs(rads - prev)) < tol:\n            break\n    # final small corrective pass for any numerical violations\n    for i in range(n):\n        for j in range(i+1, n):\n            dij = d[i,j]\n            if rads[i] + rads[j] > dij + 1e-12:\n                scale = dij / (rads[i] + rads[j])\n                rads[i] *= scale\n                rads[j] *= scale\n    rads = np.minimum(rads, b)\n    rads = np.maximum(rads, 0.0)\n    return rads\n\n# EVOLVE-BLOCK-END\n\n# fixed runner API\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n# optional visualization helper (unchanged API)\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(6,6))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect('equal')\n    for c, rr in zip(centers, radii):\n        ax.add_patch(Circle(c, rr, alpha=0.6, edgecolor='k'))\n    plt.title(f\"n={len(centers)}, sum radii={sum(radii):.6f}\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, s = run_packing()\n    print(f\"Sum of radii: {s:.6f}\")\n```\nUnique approach: Modification: Full rewrite, Excellent validity (1.000), Excellent sum_radii (2.186)\n\n### Inspiration 3 (Score: 0.6993, Type: Alternative)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 (hex-lattice spacing search + GS radius solver)\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    center = np.array([0.5, 0.5])\n    best = None\n    best_sum = -1.0\n\n    # search over candidate hex lattice spacings (center-to-center)\n    s_vals = np.linspace(0.075, 0.22, 60)\n    for s in s_vals:\n        dy = s * np.sqrt(3) / 2.0\n        pts = []\n        # generate rows covering [0,1]\n        jmax = int(np.ceil(1.0 / dy)) + 2\n        imax = int(np.ceil(1.0 / s)) + 2\n        for j in range(-1, jmax):\n            y = j * dy\n            offset = (j % 2) * (s / 2.0)\n            for i in range(-1, imax):\n                x = i * s + offset\n                if 0.0 <= x <= 1.0 and 0.0 <= y <= 1.0:\n                    pts.append((x, y))\n        if len(pts) < n:\n            continue\n        pts = np.array(pts)\n        # choose n points nearest the square center to form a compact block\n        d2 = np.sum((pts - center) ** 2, axis=1)\n        order = np.argsort(d2)[:n]\n        centers = pts[order]\n        radii = compute_max_radii(centers)\n        ssum = float(np.sum(radii))\n        if ssum > best_sum + 1e-12:\n            best_sum = ssum\n            best = (centers.copy(), radii.copy(), ssum)\n\n    # fallback: compact explicit pattern if search failed\n    if best is None:\n        # use a 6-5-5-5-5 hex-like block centered\n        rows = [6,5,5,5,5]\n        s = 0.16\n        dy = s * np.sqrt(3) / 2.0\n        total_h = (len(rows)-1)*dy\n        y0 = 0.5 - total_h/2.0\n        centers = []\n        for r, cnt in enumerate(rows):\n            y = y0 + r*dy\n            off = (s/2.0) if (r%2==1) else 0.0\n            total_w = (cnt-1)*s\n            x0 = 0.5 - total_w/2.0 + off\n            for k in range(cnt):\n                centers.append((x0 + k*s, y))\n        centers = np.array(centers[:n])\n        radii = compute_max_radii(centers)\n        best = (centers, radii, float(np.sum(radii)))\n\n    return best\n\ndef compute_max_radii(centers, max_iters=2000, tol=1e-9):\n    \"\"\"\n    Gauss-Seidel style exact feasible radius computation:\n      r_i <- min(dist_to_border_i, min_j (dist(i,j) - r_j))\n    This converges to maximal feasible radii for fixed centers.\n    \"\"\"\n    centers = np.asarray(centers, dtype=float)\n    n = centers.shape[0]\n    if n == 0:\n        return np.array([])\n    # distance to border\n    dist_border = np.minimum.reduce([centers[:,0], 1-centers[:,0], centers[:,1], 1-centers[:,1]])\n    # initial radii: conservative (border-limited)\n    r = dist_border.copy()\n    # precompute pairwise distances\n    dif = centers[:, None, :] - centers[None, :, :]\n    D = np.sqrt((dif**2).sum(axis=2))\n    np.fill_diagonal(D, np.inf)\n\n    for it in range(max_iters):\n        max_change = 0.0\n        for i in range(n):\n            # neighbor bounds: d_ij - r_j\n            neigh_bounds = D[i] - r\n            # ignore self (D[i,i]=inf)\n            newr = min(dist_border[i], np.min(neigh_bounds))\n            if newr < 0:\n                newr = 0.0\n            change = abs(r[i] - newr)\n            if change > 0:\n                r[i] = newr\n            if change > max_change:\n                max_change = change\n        if max_change < tol:\n            break\n    # safety clamp\n    r = np.maximum(r, 0.0)\n    return r\n\n# EVOLVE-BLOCK-END\n\n# Fixed runner (unchanged interface)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(7,7))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect('equal')\n    ax.add_patch(plt.Rectangle((0,0),1,1,fill=False,linewidth=1))\n    for i,(c,rr) in enumerate(zip(centers, radii)):\n        ax.add_patch(Circle(c, rr, alpha=0.6, edgecolor='k'))\n        ax.text(c[0], c[1], str(i), fontsize=8, ha='center', va='center')\n    plt.title(f\"n={len(centers)}, sum radii={sum(radii):.6f}\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, s = run_packing()\n    print(f\"Sum of radii: {s:.6f}\")\n    # visualize(centers, radii)\n```\nUnique approach: Modification: Full rewrite, Excellent validity (1.000), Excellent sum_radii (1.843)\n\n# Current Program\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (hex-like explicit layout +\niterative radius maximization)\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a deterministic arrangement of 26 circle centers in [0,1]^2\n    using a compact hexagonal-like layout and compute near-maximal radii\n    by iterative enforcement of border and non-overlap constraints.\n\n    Returns:\n        centers: (26,2) array of center coordinates\n        radii: (26,) array of radii\n        sum_of_radii: float sum(radii)\n    \"\"\"\n    n = 26\n    # Hex-like row counts summing to 26\n    row_counts = [5, 6, 6, 6, 3]  # 5+6+6+6+3 = 26\n    rows = len(row_counts)\n\n    # nominal horizontal spacing (controls circle sizes)\n    s = 0.12  # adjust to pack tightly but leave room to borders\n    dy = s * np.sqrt(3) / 2.0\n\n    # compute vertical placement centered in unit square\n    total_height = (rows - 1) * dy\n    y0 = 0.5 - total_height / 2.0\n\n    centers = []\n    for r, count in enumerate(row_counts):\n        y = y0 + r * dy\n        # stagger alternate rows for hex pattern\n        offset = (s / 2.0) if (r % 2 == 1) else 0.0\n        total_width = (count - 1) * s\n        x0 = 0.5 - total_width / 2.0 + offset\n        for k in range(count):\n            x = x0 + k * s\n            centers.append([x, y])\n\n    centers = np.array(centers[:n], dtype=float)\n\n    # Clip slightly inward to avoid exact-border degeneracies\n    centers = np.clip(centers, 0.01, 0.99)\n\n    # Compute maximal radii for fixed centers using iterative Gauss-Seidel style updates\n    radii = compute_max_radii(centers)\n\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers, max_iters=500, tol=1e-9):\n    \"\"\"\n    Given fixed centers, compute (approx.) maximal non-overlapping radii\n    by repeatedly enforcing:\n      r_i <= distance_to_border(i)\n      r_i <= min_j ( dist(i,j) - r_j )\n\n    A Gauss-Seidel sequential update converges quickly for this small n.\n    \"\"\"\n    n = centers.shape[0]\n    # initial radii: limited by border distances\n    radii = np.minimum.reduce([\n        centers[:, 0],\n        centers[:, 1],\n        1.0 - centers[:, 0],\n        1.0 - centers[:, 1],\n    ]).copy()\n\n    if n == 0:\n        return radii\n\n    # iterative enforcement\n    for it in range(max_iters):\n        max_change = 0.0\n        for i in range(n):\n            # limit by border\n            limit = min(centers[i, 0], centers[i, 1], 1.0 - centers[i, 0], 1.0 - centers[i, 1])\n            # limit by neighbors using their current radii\n            for j in range(n):\n                if i == j:\n                    continue\n                d = np.hypot(*(centers[i] - centers[j]))\n                # if centers coincide or are extremely close, set small limit\n                if d <= 1e-12:\n                    neighbor_limit = 0.0\n                else:\n                    neighbor_limit = max(0.0, d - radii[j])\n                if neighbor_limit < limit:\n                    limit = neighbor_limit\n            # ensure non-negative\n            if limit < 0.0:\n                limit = 0.0\n            change = abs(radii[i] - limit)\n            if change > 0:\n                radii[i] = limit\n            if change > max_change:\n                max_change = change\n        if max_change < tol:\n            break\n\n    return radii\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor='k')\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\n\n# Task\nRewrite the program to improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: complexity, diversity\nDifferent solutions with similar fitness but different features are valuable.\nProvide the complete new program code.\n\nIMPORTANT: Make sure your rewritten program maintains the same inputs and outputs\nas the original program, but with improved internal implementation.\n\n```python\n# Your rewritten program here\n```", "responses": ["```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for n=26 circle packing in the unit square.\n\nStrategy:\n- Try a small ensemble of plausible hex-like row patterns that sum to 26.\n- For each pattern scan horizontal spacings and slight vertical scales.\n- For each candidate place deterministic centers and compute near-maximal\n  radii by a Gauss\u2013Seidel projection + a few conservative growth cycles.\nReturn the best layout found (largest sum of radii).\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    # candidate compact row patterns (sum to 26)\n    patterns = [\n        [6,5,5,5,5],\n        [5,6,5,5,5],\n        [6,5,6,5,4],\n        [5,6,6,5,4],\n        [4,6,6,5,5],\n    ]\n\n    s_vals = np.linspace(0.12, 0.20, 17)\n    y_scales = [0.94, 0.98, 1.0, 1.02]\n    shifts = [(0,0),(+0.006,0),(-0.006,0),(0,+0.006),(0,-0.006)]\n    best = None\n    best_sum = -1.0\n\n    for pattern in patterns:\n        rows = len(pattern)\n        for s in s_vals:\n            v0 = (np.sqrt(3)/2.0) * s\n            for y_scale in y_scales:\n                v = v0 * y_scale\n                total_h = (rows - 1) * v\n                if total_h > 1.0 + 1e-12:  # can't fit vertically\n                    continue\n                y0 = 0.5 - total_h/2.0\n                for offset_start in (False, True):\n                    centers = []\n                    for r_idx, cnt in enumerate(pattern):\n                        y = y0 + r_idx * v\n                        row_width = (cnt - 1) * s if cnt > 1 else 0.0\n                        x_start = 0.5 - row_width / 2.0\n                        offset = (r_idx % 2 == 1) if offset_start else (r_idx % 2 == 0)\n                        if offset:\n                            x_start += s/2.0\n                        for j in range(cnt):\n                            centers.append([x_start + j * s, y])\n                    centers = np.array(centers[:n], dtype=float)\n                    # try small deterministic global shifts\n                    for dx,dy in shifts:\n                        cand = np.clip(centers + np.array([dx,dy]), 1e-9, 1-1e-9)\n                        radii = compute_max_radii(cand)\n                        ssum = float(np.sum(radii))\n                        if ssum > best_sum + 1e-12:\n                            best_sum = ssum\n                            best = (cand, radii, ssum)\n\n    if best is None:\n        centers = np.tile([0.5,0.5], (n,1))\n        radii = compute_max_radii(centers)\n        return centers, radii, float(np.sum(radii))\n    return best\n\ndef compute_max_radii(centers, tol=1e-9, max_iter=3000):\n    centers = np.asarray(centers, dtype=float)\n    n = centers.shape[0]\n    if n == 0:\n        return np.array([], dtype=float)\n    border = np.minimum.reduce([centers[:,0], centers[:,1], 1-centers[:,0], 1-centers[:,1]])\n    border = np.maximum(border, 0.0)\n    dif = centers[:,None,:] - centers[None,:,:]\n    d = np.sqrt(np.sum(dif*dif, axis=2))\n    np.fill_diagonal(d, np.inf)\n    # init\n    r = border.copy()\n\n    # initial proportional shrink to remove gross overlaps\n    for _ in range(800):\n        moved = False\n        over = r - border\n        if np.any(over > tol):\n            r = np.minimum(r, border); moved = True\n        for i in range(n):\n            ri = r[i]\n            for j in range(i+1, n):\n                dij = d[i,j]\n                if not np.isfinite(dij) or dij <= tol:\n                    continue\n                rj = r[j]\n                if ri + rj > dij + tol:\n                    denom = ri + rj\n                    if denom > 0:\n                        scale = dij / denom\n                        if scale < 1.0 - 1e-12:\n                            r[i] = ri * scale\n                            r[j] = rj * scale\n                            ri = r[i]\n                            moved = True\n        if not moved:\n            break\n\n    # Gauss-Seidel fixed point to reach feasible maximal radii\n    for it in range(max_iter):\n        prev = r.copy()\n        for i in range(n):\n            limits = d[i,:] - r\n            limits[i] = np.inf\n            val = min(border[i], np.min(limits))\n            r[i] = max(val, 0.0)\n        if np.max(np.abs(r - prev)) < tol:\n            break\n\n    # conservative growth cycles to exploit slack\n    for cycle in range(8):\n        slack_wall = border - r\n        slack_pair = np.min(d - (r[None,:] + r[:,None]), axis=1)\n        slack_pair = np.maximum(slack_pair, 0.0)\n        allowed = np.minimum(slack_wall, slack_pair)\n        delta = 0.25 * allowed\n        if np.all(delta <= tol):\n            break\n        r += delta\n        # quick proportional fixes\n        for _ in range(300):\n            moved = False\n            for i in range(n):\n                for j in range(i+1, n):\n                    dij = d[i,j]\n                    if not np.isfinite(dij):\n                        continue\n                    if r[i] + r[j] > dij + 1e-12:\n                        scale = dij / (r[i] + r[j])\n                        r[i] *= scale; r[j] *= scale\n                        moved = True\n            over = r - border\n            if np.any(over > 1e-12):\n                r = np.minimum(r, border); moved = True\n            if not moved:\n                break\n\n    r = np.maximum(np.minimum(r, border), 0.0)\n    return r\n\n# EVOLVE-BLOCK-END\n\n# Fixed runner\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\nif __name__ == \"__main__\":\n    centers, radii, s = run_packing()\n    print(f\"Sum of radii: {s:.6f}\")\n```"]}}, "artifacts_json": "{\"stdout\": \"Running in subprocess, Python version: 3.12.1 (v3.12.1:2305ca5144, Dec  7 2023, 17:23:38) [Clang 13.0.0 (clang-1300.0.29.30)]\\nProgram path: /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmp_fjni_ds.py\\nCalling run_packing()...\\nrun_packing() returned successfully: sum_radii = 2.2742049336217494\\nResults saved to /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmpo2r2h9c0.py.results\\n\", \"stderr\": \"\"}", "artifact_dir": null}