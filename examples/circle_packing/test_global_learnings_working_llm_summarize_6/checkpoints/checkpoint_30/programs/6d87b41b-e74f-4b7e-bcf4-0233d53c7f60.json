{"id": "6d87b41b-e74f-4b7e-bcf4-0233d53c7f60", "code": "# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (improved)\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    \"\"\"\n    Deterministic constructor for 26 circles in the unit square.\n    Strategy:\n      - 4 corner anchors (to exploit corner space)\n      - 8 edge anchors (two per side)\n      - 14-circle central cluster (1 center + 6 inner ring + 7 outer ring)\n    After fixing centers, compute maximal non-overlapping radii with a\n    Gauss-Seidel solver (r_i <= dist_to_border and r_i <= d_ij - r_j).\n    \"\"\"\n    n = 26\n    centers = []\n\n    # Corner anchors (allow bigger corner circles)\n    cm = 0.08\n    centers += [(cm, cm), (1 - cm, cm), (cm, 1 - cm), (1 - cm, 1 - cm)]\n\n    # Edge anchors: two per side near quarter positions\n    centers += [\n        (0.25, cm), (0.75, cm),  # bottom\n        (0.25, 1 - cm), (0.75, 1 - cm),  # top\n        (cm, 0.25), (cm, 0.75),  # left\n        (1 - cm, 0.25), (1 - cm, 0.75)  # right\n    ]\n\n    # Central cluster: one center, 6 close neighbors, 7 further neighbors\n    cx, cy = 0.5, 0.5\n    centers.append((cx, cy))\n    r1 = 0.12  # inner ring radius (center-to-center)\n    for k in range(6):\n        ang = 2 * np.pi * k / 6\n        centers.append((cx + r1 * np.cos(ang), cy + r1 * np.sin(ang)))\n    r2 = 0.22  # outer ring radius (center-to-center)\n    # offset outer ring so it doesn't coincide with inner ring directions\n    for k in range(7):\n        ang = 2 * np.pi * k / 7 + 0.5 * (2 * np.pi / 7)\n        centers.append((cx + r2 * np.cos(ang), cy + r2 * np.sin(ang)))\n\n    # Collect exactly n unique centers (keep order, remove near-duplicates)\n    pts = []\n    tol = 1e-9\n    for p in centers:\n        p = (float(p[0]), float(p[1]))\n        if p[0] < 0.0: p = (0.0, p[1])\n        if p[1] < 0.0: p = (p[0], 0.0)\n        if p[0] > 1.0: p = (1.0, p[1])\n        if p[1] > 1.0: p = (p[0], 1.0)\n        if not any((abs(p[0]-q[0]) < tol and abs(p[1]-q[1]) < tol) for q in pts):\n            pts.append(p)\n        if len(pts) >= n:\n            break\n    centers = np.array(pts[:n])\n\n    # Slight nudge of edge/corner anchors outward to free center space (deterministic)\n    # Move corners slightly towards their corner, edges slightly toward border\n    for i, (x, y) in enumerate(centers):\n        # identify near-corner\n        if x < 0.15 and y < 0.15:\n            centers[i] = (max(0.0, x - 0.01), max(0.0, y - 0.01))\n        elif x > 0.85 and y < 0.15:\n            centers[i] = (min(1.0, x + 0.01), max(0.0, y - 0.01))\n        elif x < 0.15 and y > 0.85:\n            centers[i] = (max(0.0, x - 0.01), min(1.0, y + 0.01))\n        elif x > 0.85 and y > 0.85:\n            centers[i] = (min(1.0, x + 0.01), min(1.0, y + 0.01))\n        # edges\n        elif y < 0.2:\n            centers[i] = (x, max(0.0, y - 0.005))\n        elif y > 0.8:\n            centers[i] = (x, min(1.0, y + 0.005))\n        elif x < 0.2:\n            centers[i] = (max(0.0, x - 0.005), y)\n        elif x > 0.8:\n            centers[i] = (min(1.0, x + 0.005), y)\n\n    # Ensure strictly inside bounds (small margin)\n    centers = np.clip(centers, 1e-6, 1 - 1e-6)\n\n    radii = compute_max_radii(centers)\n    sum_r = float(np.sum(radii))\n    return centers, radii, sum_r\n\ndef compute_max_radii(centers, tol=1e-8, max_iter=3000):\n    \"\"\"\n    Gauss-Seidel iterative solver for maximal radii given fixed centers.\n    r_i <= dist_to_border(i)\n    r_i + r_j <= dist(ci, cj)\n    \"\"\"\n    n = centers.shape[0]\n    # distance to borders\n    border = np.minimum.reduce([centers[:,0], centers[:,1], 1 - centers[:,0], 1 - centers[:,1]])\n    # initial guess: half of border (conservative)\n    radii = np.maximum(1e-6, 0.5 * border)\n\n    # pairwise distances\n    diff = centers[:, None, :] - centers[None, :, :]\n    dists = np.sqrt((diff ** 2).sum(axis=2))\n    np.fill_diagonal(dists, np.inf)\n\n    for it in range(max_iter):\n        max_change = 0.0\n        # update each radius in-place (Gauss-Seidel)\n        for i in range(n):\n            # allowed by border\n            allowed = border[i]\n            # allowed by neighbors using current r_j\n            neigh_allowed = dists[i, :] - radii\n            # take minimum; neigh_allowed includes inf on diagonal\n            mn = neigh_allowed.min()\n            if mn < allowed:\n                allowed = mn\n            if allowed < 0.0:\n                allowed = 0.0\n            change = abs(allowed - radii[i])\n            if change > max_change:\n                max_change = change\n            radii[i] = allowed\n        if max_change < tol:\n            break\n\n    # safety clamp\n    radii = np.clip(radii, 0.0, None)\n    return radii\n\n# EVOLVE-BLOCK-END\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(7,7))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect(\"equal\")\n    for c, r in zip(centers, radii):\n        ax.add_patch(Circle(c, r, alpha=0.6, edgecolor=\"k\"))\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, s = run_packing()\n    print(f\"Sum of radii: {s:.6f}\")\n    # visualize(centers, radii)", "language": "python", "parent_id": "5fcd83cb-d585-49b6-bfb7-086236553a2d", "generation": 2, "timestamp": 1759960184.637135, "iteration_found": 10, "metrics": {"validity": 1.0, "sum_radii": 1.6669987468984175, "target_ratio": 0.6326370955971224, "combined_score": 0.6326370955971224, "eval_time": 0.05368781089782715}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Full rewrite", "parent_metrics": {"validity": 1.0, "sum_radii": 1.7377983227048748, "target_ratio": 0.6595060048215844, "combined_score": 0.6595060048215844, "eval_time": 0.0540618896484375}, "island": 0}, "prompts": {"full_rewrite_user": {"system": "You are an expert mathematician specializing in circle packing problems and computational geometry. Your task is to improve a constructor function that directly produces a specific arrangement of 26 circles in a unit square, maximizing the sum of their radii. The AlphaEvolve paper achieved a sum of 2.635 for n=26.\n\nKey geometric insights:\n- Circle packings often follow hexagonal patterns in the densest regions\n- Maximum density for infinite circle packing is pi/(2*sqrt(3)) \u2248 0.9069\n- Edge effects make square container packing harder than infinite packing\n- Circles can be placed in layers or shells when confined to a square\n- Similar radius circles often form regular patterns, while varied radii allow better space utilization\n- Perfect symmetry may not yield the optimal packing due to edge effects\n\nFocus on designing an explicit constructor that places each circle in a specific position, rather than an iterative search algorithm.\n", "user": "# Current Program Information\n- Fitness: 0.6595\n- Feature coordinates: No feature coordinates\n- Focus areas: - Fitness improved: 0.3642 \u2192 0.6595\n- Consider simplifying - code length exceeds 500 characters\n\n## Last Execution Output\n\n### stdout\n```\nRunning in subprocess, Python version: 3.12.1 (v3.12.1:2305ca5144, Dec  7 2023, 17:23:38) [Clang 13.0.0 (clang-1300.0.29.30)]\nProgram path: /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmpcbit071c.py\nCalling run_packing()...\nrun_packing() returned successfully: sum_radii = 1.7377983227048748\nResults saved to /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmpr73s0yzd.py.results\n\n```\n\n### stderr\n```\n\n```\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 3\n- Changes: Unknown changes\n- Metrics: validity: 1.0000, sum_radii: 0.9598, target_ratio: 0.3642, combined_score: 0.3642\n- Outcome: Improvement in all metrics\n\n### Attempt 2\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 1.7378, target_ratio: 0.6595, combined_score: 0.6595, eval_time: 0.0541\n- Outcome: Mixed results\n\n### Attempt 1\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 1.8063, target_ratio: 0.6855, combined_score: 0.6855, eval_time: 1.0880\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.6855)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (improved)\"\"\"\nimport numpy as np\nfrom math import sqrt\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square.\n    Returns: (centers, radii, sum_of_radii)\n    \"\"\"\n    n_target = 26\n\n    # Generate a hexagonal lattice of candidate centers filling the unit square\n    s = 0.12                      # lattice spacing (tunable)\n    vy = sqrt(3) / 2 * s\n    candidates = []\n    nrows = int(np.ceil(1.0 / vy)) + 1\n    ncols = int(np.ceil(1.0 / s)) + 1\n    for j in range(nrows):\n        y = j * vy\n        if y <= 0 or y >= 1: \n            continue\n        offset = (j % 2) * (s / 2.0)\n        for i in range(ncols):\n            x = i * s + offset\n            if x <= 0 or x >= 1:\n                continue\n            candidates.append((x, y))\n    candidates = np.array(candidates)\n\n    # Add four near-corner anchors to encourage large corner circles\n    corner_margin = 0.06\n    corner_points = np.array([\n        (corner_margin, corner_margin),\n        (1 - corner_margin, corner_margin),\n        (corner_margin, 1 - corner_margin),\n        (1 - corner_margin, 1 - corner_margin),\n    ])\n    # Merge and uniquify candidates\n    all_pts = np.vstack((candidates, corner_points))\n    # Remove duplicates (within tiny tol)\n    uniq = []\n    for p in all_pts:\n        if not any(np.linalg.norm(p - q) < 1e-6 for q in uniq):\n            uniq.append(p)\n    candidates = np.array(uniq)\n\n    # Greedy selection: pick n_target centers from candidates to maximize sum of radii\n    selected_idx = []\n    remaining = list(range(len(candidates)))\n\n    def compute_max_radii(centers):\n        m = centers.shape[0]\n        # Start with distance to walls\n        radii = np.minimum(np.minimum(centers[:,0], 1 - centers[:,0]),\n                           np.minimum(centers[:,1], 1 - centers[:,1]))\n        radii = np.clip(radii, 0.0, 1.0)\n\n        if m == 0:\n            return radii\n\n        # Iterative Gauss-Seidel relaxation to enforce r_i + r_j <= d_ij\n        coords = centers.copy()\n        tol = 1e-7\n        max_iter = 1000\n        for _ in range(max_iter):\n            max_change = 0.0\n            for i in range(m):\n                ri_old = radii[i]\n                # limit by walls already included in initial value\n                ri = ri_old\n                for j in range(m):\n                    if i == j:\n                        continue\n                    d = np.linalg.norm(coords[i] - coords[j])\n                    # If centers coincide, force tiny distance to avoid div by zero\n                    if d < 1e-8:\n                        d = 1e-8\n                    # r_i <= d - r_j\n                    bound = d - radii[j]\n                    if bound < ri:\n                        ri = max(0.0, bound)\n                # update\n                change = abs(ri - ri_old)\n                if change > max_change:\n                    max_change = change\n                radii[i] = ri\n            if max_change < tol:\n                break\n        return radii\n\n    # Greedy picking loop\n    while len(selected_idx) < n_target and remaining:\n        best_idx = None\n        best_sum = -1.0\n        # evaluate each candidate addition\n        for idx in remaining:\n            trial_indices = selected_idx + [idx]\n            centers_trial = candidates[trial_indices]\n            r_trial = compute_max_radii(centers_trial)\n            ssum = float(np.sum(r_trial))\n            if ssum > best_sum:\n                best_sum = ssum\n                best_idx = idx\n        if best_idx is None:\n            break\n        selected_idx.append(best_idx)\n        remaining.remove(best_idx)\n\n    # If greedy failed to pick enough (shouldn't), pad with nearest-to-center remaining\n    if len(selected_idx) < n_target:\n        remaining_sorted = sorted(remaining, key=lambda i: np.linalg.norm(candidates[i] - np.array([0.5,0.5])))\n        for idx in remaining_sorted:\n            if len(selected_idx) >= n_target:\n                break\n            selected_idx.append(idx)\n\n    centers = candidates[selected_idx][:n_target]\n    # Final radii computed and refined\n    radii = compute_max_radii(centers)\n\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n# EVOLVE-BLOCK-END\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, linewidth=0.5, edgecolor='k')\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (1.8063), Performs well on target_ratio (0.6855), Performs well on combined_score (0.6855), Performs well on eval_time (1.0880)\n\n### Program 2 (Score: 0.6595)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (improved)\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square\n    that attempts to maximize the sum of their radii using an\n    explicit hex-like lattice of centers and a robust radius solver.\n\n    Returns:\n        Tuple of (centers, radii, sum_of_radii)\n    \"\"\"\n    n = 26\n\n    # Layout: 5 rows with counts [6,5,5,5,5] -> total 26\n    rows = [6, 5, 5, 5, 5]\n    R = len(rows)\n\n    # margin so circles can grow to border\n    margin = 0.01\n\n    # choose a horizontal spacing so the widest row fits\n    max_cols = max(rows)\n    a = (1.0 - 2 * margin) / (max_cols - 1)  # horizontal spacing between centers\n    v = a * np.sqrt(3) / 2  # vertical spacing for hex pattern\n\n    # vertical offset to center rows in the unit square\n    total_height = (R - 1) * v\n    y0 = margin + (1.0 - 2 * margin - total_height) / 2.0\n\n    centers = []\n    for r_idx, cols in enumerate(rows):\n        y = y0 + r_idx * v\n        # alternate rows are offset by a/2 to create hex packing\n        offset = 0.0 if cols == max_cols else a / 2.0\n        # center horizontally\n        row_width = (cols - 1) * a\n        x_start = margin + (1.0 - 2 * margin - row_width) / 2.0 + offset\n        for c in range(cols):\n            x = x_start + c * a\n            centers.append([x, y])\n    centers = np.array(centers[:n])\n    centers = np.clip(centers, margin, 1.0 - margin)\n\n    # Compute maximal radii with iterative Gauss-Seidel style updates:\n    radii = compute_max_radii(centers)\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers, tol=1e-8, max_iter=2000):\n    \"\"\"\n    Given fixed centers, compute maximal radii so that:\n      - each circle stays inside unit square\n      - no two circles overlap: r_i + r_j <= dist(ci, cj)\n\n    Method: iterative Gauss-Seidel updates:\n      r_i <- min(dist_to_border(i), min_j dist(i,j) - r_j)\n    Repeat until convergence.\n    \"\"\"\n    n = centers.shape[0]\n    # distance to border\n    border = np.minimum.reduce([centers[:, 0], centers[:, 1], 1 - centers[:, 0], 1 - centers[:, 1]])\n\n    # initial radii: modest guess based on lattice spacing if available\n    # use small positive initial values to allow growth\n    radii = np.clip(0.5 * border, 1e-6, None)\n\n    # Precompute pairwise distances\n    diff = centers[:, None, :] - centers[None, :, :]\n    dists = np.sqrt(np.sum(diff * diff, axis=2))\n    # avoid zero on diagonal\n    np.fill_diagonal(dists, np.inf)\n\n    for it in range(max_iter):\n        max_change = 0.0\n        # Gauss-Seidel style: update in place which accelerates convergence\n        for i in range(n):\n            # allowed by border\n            allowed = border[i]\n            # allowed by neighbors: for each j, r_i <= d_ij - r_j\n            # using current r_j (some already updated this iteration)\n            neigh_allowed = dists[i, :] - radii\n            # exclude self (inf - radii -> large)\n            allowed = min(allowed, np.min(neigh_allowed))\n            if allowed < 0:\n                allowed = 0.0\n            # clamp small negative numerical artifacts\n            newr = allowed\n            change = abs(newr - radii[i])\n            if change > max_change:\n                max_change = change\n            radii[i] = newr\n        if max_change < tol:\n            break\n\n    # final safety clamp\n    radii = np.maximum(radii, 0.0)\n    return radii\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\", linewidth=0.4)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (1.7378), Performs well on target_ratio (0.6595), Performs well on combined_score (0.6595), Performs well on eval_time (0.0541)\n\n### Program 3 (Score: 0.3642)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square\n    that attempts to maximize the sum of their radii.\n\n    Returns:\n        Tuple of (centers, radii, sum_of_radii)\n        centers: np.array of shape (26, 2) with (x, y) coordinates\n        radii: np.array of shape (26) with radius of each circle\n        sum_of_radii: Sum of all radii\n    \"\"\"\n    # Initialize arrays for 26 circles\n    n = 26\n    centers = np.zeros((n, 2))\n\n    # Place circles in a structured pattern\n    # This is a simple pattern - evolution will improve this\n\n    # First, place a large circle in the center\n    centers[0] = [0.5, 0.5]\n\n    # Place 8 circles around it in a ring\n    for i in range(8):\n        angle = 2 * np.pi * i / 8\n        centers[i + 1] = [0.5 + 0.3 * np.cos(angle), 0.5 + 0.3 * np.sin(angle)]\n\n    # Place 16 more circles in an outer ring\n    for i in range(16):\n        angle = 2 * np.pi * i / 16\n        centers[i + 9] = [0.5 + 0.7 * np.cos(angle), 0.5 + 0.7 * np.sin(angle)]\n\n    # Additional positioning adjustment to make sure all circles\n    # are inside the square and don't overlap\n    # Clip to ensure everything is inside the unit square\n    centers = np.clip(centers, 0.01, 0.99)\n\n    # Compute maximum valid radii for this configuration\n    radii = compute_max_radii(centers)\n\n    # Calculate the sum of radii\n    sum_radii = np.sum(radii)\n\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers):\n    \"\"\"\n    Compute the maximum possible radii for each circle position\n    such that they don't overlap and stay within the unit square.\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n\n    Returns:\n        np.array of shape (n) with radius of each circle\n    \"\"\"\n    n = centers.shape[0]\n    radii = np.ones(n)\n\n    # First, limit by distance to square borders\n    for i in range(n):\n        x, y = centers[i]\n        # Distance to borders\n        radii[i] = min(x, y, 1 - x, 1 - y)\n\n    # Then, limit by distance to other circles\n    # Each pair of circles with centers at distance d can have\n    # sum of radii at most d to avoid overlap\n    for i in range(n):\n        for j in range(i + 1, n):\n            dist = np.sqrt(np.sum((centers[i] - centers[j]) ** 2))\n\n            # If current radii would cause overlap\n            if radii[i] + radii[j] > dist:\n                # Scale both radii proportionally\n                scale = dist / (radii[i] + radii[j])\n                radii[i] *= scale\n                radii[j] *= scale\n\n    return radii\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.5)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\")\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii}\")\n    # AlphaEvolve improved this to 2.635\n\n    # Uncomment to visualize:\n    visualize(centers, radii)\n\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (0.9598), Performs well on target_ratio (0.3642), Performs well on combined_score (0.3642)\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 0.3642, Type: Exploratory)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square\n    that attempts to maximize the sum of their radii.\n\n    Returns:\n        Tuple of (centers, radii, sum_of_radii)\n        centers: np.array of shape (26, 2) with (x, y) coordinates\n        radii: np.array of shape (26) with radius of each circle\n        sum_of_radii: Sum of all radii\n    \"\"\"\n    # Initialize arrays for 26 circles\n    n = 26\n    centers = np.zeros((n, 2))\n\n    # Place circles in a structured pattern\n    # This is a simple pattern - evolution will improve this\n\n    # First, place a large circle in the center\n    centers[0] = [0.5, 0.5]\n\n    # Place 8 circles around it in a ring\n    for i in range(8):\n        angle = 2 * np.pi * i / 8\n        centers[i + 1] = [0.5 + 0.3 * np.cos(angle), 0.5 + 0.3 * np.sin(angle)]\n\n    # Place 16 more circles in an outer ring\n    for i in range(16):\n        angle = 2 * np.pi * i / 16\n        centers[i + 9] = [0.5 + 0.7 * np.cos(angle), 0.5 + 0.7 * np.sin(angle)]\n\n    # Additional positioning adjustment to make sure all circles\n    # are inside the square and don't overlap\n    # Clip to ensure everything is inside the unit square\n    centers = np.clip(centers, 0.01, 0.99)\n\n    # Compute maximum valid radii for this configuration\n    radii = compute_max_radii(centers)\n\n    # Calculate the sum of radii\n    sum_radii = np.sum(radii)\n\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers):\n    \"\"\"\n    Compute the maximum possible radii for each circle position\n    such that they don't overlap and stay within the unit square.\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n\n    Returns:\n        np.array of shape (n) with radius of each circle\n    \"\"\"\n    n = centers.shape[0]\n    radii = np.ones(n)\n\n    # First, limit by distance to square borders\n    for i in range(n):\n        x, y = centers[i]\n        # Distance to borders\n        radii[i] = min(x, y, 1 - x, 1 - y)\n\n    # Then, limit by distance to other circles\n    # Each pair of circles with centers at distance d can have\n    # sum of radii at most d to avoid overlap\n    for i in range(n):\n        for j in range(i + 1, n):\n            dist = np.sqrt(np.sum((centers[i] - centers[j]) ** 2))\n\n            # If current radii would cause overlap\n            if radii[i] + radii[j] > dist:\n                # Scale both radii proportionally\n                scale = dist / (radii[i] + radii[j])\n                radii[i] *= scale\n                radii[j] *= scale\n\n    return radii\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.5)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\")\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii}\")\n    # AlphaEvolve improved this to 2.635\n\n    # Uncomment to visualize:\n    visualize(centers, radii)\n\n```\nUnique approach: Excellent validity (1.000), Excellent sum_radii (0.960), NumPy-based implementation\n\n### Inspiration 2 (Score: 0.6855, Type: Alternative)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (improved)\"\"\"\nimport numpy as np\nfrom math import sqrt\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square.\n    Returns: (centers, radii, sum_of_radii)\n    \"\"\"\n    n_target = 26\n\n    # Generate a hexagonal lattice of candidate centers filling the unit square\n    s = 0.12                      # lattice spacing (tunable)\n    vy = sqrt(3) / 2 * s\n    candidates = []\n    nrows = int(np.ceil(1.0 / vy)) + 1\n    ncols = int(np.ceil(1.0 / s)) + 1\n    for j in range(nrows):\n        y = j * vy\n        if y <= 0 or y >= 1: \n            continue\n        offset = (j % 2) * (s / 2.0)\n        for i in range(ncols):\n            x = i * s + offset\n            if x <= 0 or x >= 1:\n                continue\n            candidates.append((x, y))\n    candidates = np.array(candidates)\n\n    # Add four near-corner anchors to encourage large corner circles\n    corner_margin = 0.06\n    corner_points = np.array([\n        (corner_margin, corner_margin),\n        (1 - corner_margin, corner_margin),\n        (corner_margin, 1 - corner_margin),\n        (1 - corner_margin, 1 - corner_margin),\n    ])\n    # Merge and uniquify candidates\n    all_pts = np.vstack((candidates, corner_points))\n    # Remove duplicates (within tiny tol)\n    uniq = []\n    for p in all_pts:\n        if not any(np.linalg.norm(p - q) < 1e-6 for q in uniq):\n            uniq.append(p)\n    candidates = np.array(uniq)\n\n    # Greedy selection: pick n_target centers from candidates to maximize sum of radii\n    selected_idx = []\n    remaining = list(range(len(candidates)))\n\n    def compute_max_radii(centers):\n        m = centers.shape[0]\n        # Start with distance to walls\n        radii = np.minimum(np.minimum(centers[:,0], 1 - centers[:,0]),\n                           np.minimum(centers[:,1], 1 - centers[:,1]))\n        radii = np.clip(radii, 0.0, 1.0)\n\n        if m == 0:\n            return radii\n\n        # Iterative Gauss-Seidel relaxation to enforce r_i + r_j <= d_ij\n        coords = centers.copy()\n        tol = 1e-7\n        max_iter = 1000\n        for _ in range(max_iter):\n            max_change = 0.0\n            for i in range(m):\n                ri_old = radii[i]\n                # limit by walls already included in initial value\n                ri = ri_old\n                for j in range(m):\n                    if i == j:\n                        continue\n                    d = np.linalg.norm(coords[i] - coords[j])\n                    # If centers coincide, force tiny distance to avoid div by zero\n                    if d < 1e-8:\n                        d = 1e-8\n                    # r_i <= d - r_j\n                    bound = d - radii[j]\n                    if bound < ri:\n                        ri = max(0.0, bound)\n                # update\n                change = abs(ri - ri_old)\n                if change > max_change:\n                    max_change = change\n                radii[i] = ri\n            if max_change < tol:\n                break\n        return radii\n\n    # Greedy picking loop\n    while len(selected_idx) < n_target and remaining:\n        best_idx = None\n        best_sum = -1.0\n        # evaluate each candidate addition\n        for idx in remaining:\n            trial_indices = selected_idx + [idx]\n            centers_trial = candidates[trial_indices]\n            r_trial = compute_max_radii(centers_trial)\n            ssum = float(np.sum(r_trial))\n            if ssum > best_sum:\n                best_sum = ssum\n                best_idx = idx\n        if best_idx is None:\n            break\n        selected_idx.append(best_idx)\n        remaining.remove(best_idx)\n\n    # If greedy failed to pick enough (shouldn't), pad with nearest-to-center remaining\n    if len(selected_idx) < n_target:\n        remaining_sorted = sorted(remaining, key=lambda i: np.linalg.norm(candidates[i] - np.array([0.5,0.5])))\n        for idx in remaining_sorted:\n            if len(selected_idx) >= n_target:\n                break\n            selected_idx.append(idx)\n\n    centers = candidates[selected_idx][:n_target]\n    # Final radii computed and refined\n    radii = compute_max_radii(centers)\n\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n# EVOLVE-BLOCK-END\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, linewidth=0.5, edgecolor='k')\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nUnique approach: Modification: Full rewrite, Excellent validity (1.000), Excellent sum_radii (1.806)\n\n# Current Program\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (improved)\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square\n    that attempts to maximize the sum of their radii using an\n    explicit hex-like lattice of centers and a robust radius solver.\n\n    Returns:\n        Tuple of (centers, radii, sum_of_radii)\n    \"\"\"\n    n = 26\n\n    # Layout: 5 rows with counts [6,5,5,5,5] -> total 26\n    rows = [6, 5, 5, 5, 5]\n    R = len(rows)\n\n    # margin so circles can grow to border\n    margin = 0.01\n\n    # choose a horizontal spacing so the widest row fits\n    max_cols = max(rows)\n    a = (1.0 - 2 * margin) / (max_cols - 1)  # horizontal spacing between centers\n    v = a * np.sqrt(3) / 2  # vertical spacing for hex pattern\n\n    # vertical offset to center rows in the unit square\n    total_height = (R - 1) * v\n    y0 = margin + (1.0 - 2 * margin - total_height) / 2.0\n\n    centers = []\n    for r_idx, cols in enumerate(rows):\n        y = y0 + r_idx * v\n        # alternate rows are offset by a/2 to create hex packing\n        offset = 0.0 if cols == max_cols else a / 2.0\n        # center horizontally\n        row_width = (cols - 1) * a\n        x_start = margin + (1.0 - 2 * margin - row_width) / 2.0 + offset\n        for c in range(cols):\n            x = x_start + c * a\n            centers.append([x, y])\n    centers = np.array(centers[:n])\n    centers = np.clip(centers, margin, 1.0 - margin)\n\n    # Compute maximal radii with iterative Gauss-Seidel style updates:\n    radii = compute_max_radii(centers)\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers, tol=1e-8, max_iter=2000):\n    \"\"\"\n    Given fixed centers, compute maximal radii so that:\n      - each circle stays inside unit square\n      - no two circles overlap: r_i + r_j <= dist(ci, cj)\n\n    Method: iterative Gauss-Seidel updates:\n      r_i <- min(dist_to_border(i), min_j dist(i,j) - r_j)\n    Repeat until convergence.\n    \"\"\"\n    n = centers.shape[0]\n    # distance to border\n    border = np.minimum.reduce([centers[:, 0], centers[:, 1], 1 - centers[:, 0], 1 - centers[:, 1]])\n\n    # initial radii: modest guess based on lattice spacing if available\n    # use small positive initial values to allow growth\n    radii = np.clip(0.5 * border, 1e-6, None)\n\n    # Precompute pairwise distances\n    diff = centers[:, None, :] - centers[None, :, :]\n    dists = np.sqrt(np.sum(diff * diff, axis=2))\n    # avoid zero on diagonal\n    np.fill_diagonal(dists, np.inf)\n\n    for it in range(max_iter):\n        max_change = 0.0\n        # Gauss-Seidel style: update in place which accelerates convergence\n        for i in range(n):\n            # allowed by border\n            allowed = border[i]\n            # allowed by neighbors: for each j, r_i <= d_ij - r_j\n            # using current r_j (some already updated this iteration)\n            neigh_allowed = dists[i, :] - radii\n            # exclude self (inf - radii -> large)\n            allowed = min(allowed, np.min(neigh_allowed))\n            if allowed < 0:\n                allowed = 0.0\n            # clamp small negative numerical artifacts\n            newr = allowed\n            change = abs(newr - radii[i])\n            if change > max_change:\n                max_change = change\n            radii[i] = newr\n        if max_change < tol:\n            break\n\n    # final safety clamp\n    radii = np.maximum(radii, 0.0)\n    return radii\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\", linewidth=0.4)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\n\n# Task\nRewrite the program to improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: complexity, diversity\nDifferent solutions with similar fitness but different features are valuable.\nProvide the complete new program code.\n\nIMPORTANT: Make sure your rewritten program maintains the same inputs and outputs\nas the original program, but with improved internal implementation.\n\n```python\n# Your rewritten program here\n```", "responses": ["```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (improved)\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    \"\"\"\n    Deterministic constructor for 26 circles in the unit square.\n    Strategy:\n      - 4 corner anchors (to exploit corner space)\n      - 8 edge anchors (two per side)\n      - 14-circle central cluster (1 center + 6 inner ring + 7 outer ring)\n    After fixing centers, compute maximal non-overlapping radii with a\n    Gauss-Seidel solver (r_i <= dist_to_border and r_i <= d_ij - r_j).\n    \"\"\"\n    n = 26\n    centers = []\n\n    # Corner anchors (allow bigger corner circles)\n    cm = 0.08\n    centers += [(cm, cm), (1 - cm, cm), (cm, 1 - cm), (1 - cm, 1 - cm)]\n\n    # Edge anchors: two per side near quarter positions\n    centers += [\n        (0.25, cm), (0.75, cm),  # bottom\n        (0.25, 1 - cm), (0.75, 1 - cm),  # top\n        (cm, 0.25), (cm, 0.75),  # left\n        (1 - cm, 0.25), (1 - cm, 0.75)  # right\n    ]\n\n    # Central cluster: one center, 6 close neighbors, 7 further neighbors\n    cx, cy = 0.5, 0.5\n    centers.append((cx, cy))\n    r1 = 0.12  # inner ring radius (center-to-center)\n    for k in range(6):\n        ang = 2 * np.pi * k / 6\n        centers.append((cx + r1 * np.cos(ang), cy + r1 * np.sin(ang)))\n    r2 = 0.22  # outer ring radius (center-to-center)\n    # offset outer ring so it doesn't coincide with inner ring directions\n    for k in range(7):\n        ang = 2 * np.pi * k / 7 + 0.5 * (2 * np.pi / 7)\n        centers.append((cx + r2 * np.cos(ang), cy + r2 * np.sin(ang)))\n\n    # Collect exactly n unique centers (keep order, remove near-duplicates)\n    pts = []\n    tol = 1e-9\n    for p in centers:\n        p = (float(p[0]), float(p[1]))\n        if p[0] < 0.0: p = (0.0, p[1])\n        if p[1] < 0.0: p = (p[0], 0.0)\n        if p[0] > 1.0: p = (1.0, p[1])\n        if p[1] > 1.0: p = (p[0], 1.0)\n        if not any((abs(p[0]-q[0]) < tol and abs(p[1]-q[1]) < tol) for q in pts):\n            pts.append(p)\n        if len(pts) >= n:\n            break\n    centers = np.array(pts[:n])\n\n    # Slight nudge of edge/corner anchors outward to free center space (deterministic)\n    # Move corners slightly towards their corner, edges slightly toward border\n    for i, (x, y) in enumerate(centers):\n        # identify near-corner\n        if x < 0.15 and y < 0.15:\n            centers[i] = (max(0.0, x - 0.01), max(0.0, y - 0.01))\n        elif x > 0.85 and y < 0.15:\n            centers[i] = (min(1.0, x + 0.01), max(0.0, y - 0.01))\n        elif x < 0.15 and y > 0.85:\n            centers[i] = (max(0.0, x - 0.01), min(1.0, y + 0.01))\n        elif x > 0.85 and y > 0.85:\n            centers[i] = (min(1.0, x + 0.01), min(1.0, y + 0.01))\n        # edges\n        elif y < 0.2:\n            centers[i] = (x, max(0.0, y - 0.005))\n        elif y > 0.8:\n            centers[i] = (x, min(1.0, y + 0.005))\n        elif x < 0.2:\n            centers[i] = (max(0.0, x - 0.005), y)\n        elif x > 0.8:\n            centers[i] = (min(1.0, x + 0.005), y)\n\n    # Ensure strictly inside bounds (small margin)\n    centers = np.clip(centers, 1e-6, 1 - 1e-6)\n\n    radii = compute_max_radii(centers)\n    sum_r = float(np.sum(radii))\n    return centers, radii, sum_r\n\ndef compute_max_radii(centers, tol=1e-8, max_iter=3000):\n    \"\"\"\n    Gauss-Seidel iterative solver for maximal radii given fixed centers.\n    r_i <= dist_to_border(i)\n    r_i + r_j <= dist(ci, cj)\n    \"\"\"\n    n = centers.shape[0]\n    # distance to borders\n    border = np.minimum.reduce([centers[:,0], centers[:,1], 1 - centers[:,0], 1 - centers[:,1]])\n    # initial guess: half of border (conservative)\n    radii = np.maximum(1e-6, 0.5 * border)\n\n    # pairwise distances\n    diff = centers[:, None, :] - centers[None, :, :]\n    dists = np.sqrt((diff ** 2).sum(axis=2))\n    np.fill_diagonal(dists, np.inf)\n\n    for it in range(max_iter):\n        max_change = 0.0\n        # update each radius in-place (Gauss-Seidel)\n        for i in range(n):\n            # allowed by border\n            allowed = border[i]\n            # allowed by neighbors using current r_j\n            neigh_allowed = dists[i, :] - radii\n            # take minimum; neigh_allowed includes inf on diagonal\n            mn = neigh_allowed.min()\n            if mn < allowed:\n                allowed = mn\n            if allowed < 0.0:\n                allowed = 0.0\n            change = abs(allowed - radii[i])\n            if change > max_change:\n                max_change = change\n            radii[i] = allowed\n        if max_change < tol:\n            break\n\n    # safety clamp\n    radii = np.clip(radii, 0.0, None)\n    return radii\n\n# EVOLVE-BLOCK-END\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(7,7))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect(\"equal\")\n    for c, r in zip(centers, radii):\n        ax.add_patch(Circle(c, r, alpha=0.6, edgecolor=\"k\"))\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, s = run_packing()\n    print(f\"Sum of radii: {s:.6f}\")\n    # visualize(centers, radii)\n```"]}}, "artifacts_json": "{\"stdout\": \"Running in subprocess, Python version: 3.12.1 (v3.12.1:2305ca5144, Dec  7 2023, 17:23:38) [Clang 13.0.0 (clang-1300.0.29.30)]\\nProgram path: /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmpi6rbf4vy.py\\nCalling run_packing()...\\nrun_packing() returned successfully: sum_radii = 1.6669987468984175\\nResults saved to /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmp5ceae1ni.py.results\\n\", \"stderr\": \"\"}", "artifact_dir": null}