{"id": "b169e66a-f4c7-4ed9-8655-7f35c91c5ddc", "code": "# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for packing 26 circles in the unit square.\n\nStrategy:\n- Try several compact hex-like row layouts known to work well for n=26.\n- For each layout, sweep a small range of horizontal spacings (s) around the\n  largest spacing that fits the pattern inside the unit square.\n- For each candidate center set compute maximal feasible radii by a\n  Gauss-Seidel style projection (coordinate-wise maximal radii for fixed centers).\n- Return the best configuration found (centers, radii, sum_radii).\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    # Diverse compact row layouts (all sum to 26)\n    row_patterns = [\n        [6, 5, 6, 5, 4],      # high-performing pattern from experiments\n        [5, 6, 5, 5, 5],      # alternative balanced\n        [5, 5, 4, 4, 4, 4],   # elongated\n        [6, 5, 5, 5, 5],      # wider middle\n    ]\n\n    sin60 = np.sqrt(3.0) / 2.0\n    margin = 0.006  # small interior margin to avoid exact touches\n\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    # For each pattern, try a small sweep of spacings near the maximal allowable\n    for rows in row_patterns:\n        num_rows = len(rows)\n        max_cols = max(rows)\n        # compute maximal s to fit width and height with margins\n        if max_cols > 1:\n            s_max_w = (1.0 - 2.0 * margin) / (max_cols - 1)\n        else:\n            s_max_w = 1.0 - 2.0 * margin\n        if num_rows > 1:\n            s_max_h = (1.0 - 2.0 * margin) / ((num_rows - 1) * sin60)\n        else:\n            s_max_h = 1.0 - 2.0 * margin\n        s_base = min(s_max_w, s_max_h)\n        # try a few reduced spacings to let radii relax and avoid exact touching\n        for factor in np.linspace(0.86, 0.995, 10):\n            s = s_base * factor\n            dy = s * sin60\n            # center vertically\n            total_h = (num_rows - 1) * dy if num_rows > 1 else 0.0\n            y0 = 0.5 - total_h / 2.0\n            centers_list = []\n            for ri, cnt in enumerate(rows):\n                # hex-like staggering: alternate rows shifted by half spacing\n                stagger = (ri % 2) * 0.5 * s\n                if cnt > 1:\n                    row_width = (cnt - 1) * s\n                else:\n                    row_width = 0.0\n                x_start = 0.5 - row_width / 2.0 + stagger\n                y = y0 + ri * dy\n                for j in range(cnt):\n                    x = x_start + j * s\n                    centers_list.append([x, y])\n            centers = np.array(centers_list, dtype=float)\n            if centers.shape[0] != n:\n                # skip if pattern unexpectedly didn't produce 26\n                continue\n            # clip slight numerical excursions\n            centers = np.clip(centers, 1e-9, 1.0 - 1e-9)\n            radii = compute_max_radii_relax(centers, max_iter=2500, tol=1e-9)\n            ssum = float(np.sum(radii))\n            if ssum > best_sum:\n                best_sum = ssum\n                best_centers = centers.copy()\n                best_radii = radii.copy()\n\n    # Fallback deterministic pattern if none found (shouldn't happen)\n    if best_centers is None:\n        rows = [6,5,6,5,4]\n        sin60 = np.sqrt(3.0)/2.0\n        s = 0.14\n        dy = s * sin60\n        total_h = (len(rows)-1)*dy\n        y0 = 0.5 - total_h/2.0\n        centers = []\n        for ri,cnt in enumerate(rows):\n            stagger = (ri%2)*0.5*s\n            row_width = (cnt-1)*s if cnt>1 else 0.0\n            x_start = 0.5 - row_width/2.0 + stagger\n            y = y0 + ri*dy\n            for j in range(cnt):\n                centers.append([x_start + j*s, y])\n        best_centers = np.clip(np.array(centers[:n],dtype=float),1e-9,1-1e-9)\n        best_radii = compute_max_radii_relax(best_centers)\n\n    sum_radii = float(np.sum(best_radii))\n    return best_centers, best_radii, sum_radii\n\ndef compute_max_radii_relax(centers, max_iter=2000, tol=1e-8):\n    \"\"\"\n    Gauss-Seidel projection to compute maximal radii for fixed centers.\n    Update rule:\n      r_i := min(dist_to_wall_i, min_{j!=i} max(0, d_ij - r_j))\n    This yields a feasible (and typically maximal) radii vector.\n    \"\"\"\n    n = centers.shape[0]\n    xs = centers[:,0]; ys = centers[:,1]\n    wall = np.minimum.reduce([xs, ys, 1.0 - xs, 1.0 - ys])\n    wall = np.maximum(wall, 0.0)\n\n    # pairwise distances\n    diff = centers[:, None, :] - centers[None, :, :]\n    D = np.sqrt(np.maximum(np.sum(diff*diff, axis=2), 1e-14))\n    np.fill_diagonal(D, np.inf)\n\n    # initialize at wall limits\n    r = wall.copy()\n\n    for it in range(max_iter):\n        max_change = 0.0\n        for i in range(n):\n            # candidate bounds from other circles: D[i] - r\n            cand = D[i] - r\n            # ensure non-negative candidate values before min\n            cand = np.maximum(cand, 0.0)\n            # pairbound is min over j of cand_j (j != i)\n            pairbound = float(np.min(cand))\n            newri = min(wall[i], pairbound)\n            # clamp small negatives\n            newri = max(newri, 0.0)\n            change = abs(newri - r[i])\n            if change > 0:\n                r[i] = newri\n                if change > max_change:\n                    max_change = change\n        if max_change < tol:\n            break\n\n    return np.maximum(r, 0.0)\n\n# EVOLVE-BLOCK-END\n\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n# Optional: small visualizer if run standalone\nif __name__ == \"__main__\":\n    c, r, s = run_packing()\n    print(f\"Sum of radii: {s:.6f}\")", "language": "python", "parent_id": "56f7c4b3-d906-4ee9-aa0c-47a149529f8a", "generation": 2, "timestamp": 1759958327.8117492, "iteration_found": 25, "metrics": {"validity": 1.0, "sum_radii": 2.2191689344443306, "target_ratio": 0.8421893489352299, "combined_score": 0.8421893489352299, "eval_time": 0.06849002838134766}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Full rewrite", "parent_metrics": {"validity": 1.0, "sum_radii": 1.6605617046555405, "target_ratio": 0.6301941953151957, "combined_score": 0.6301941953151957, "eval_time": 0.05823016166687012}, "island": 0}, "prompts": {"full_rewrite_user": {"system": "You are an expert mathematician specializing in circle packing problems and computational geometry. Your task is to improve a constructor function that directly produces a specific arrangement of 26 circles in a unit square, maximizing the sum of their radii. The AlphaEvolve paper achieved a sum of 2.635 for n=26.\n\nKey geometric insights:\n- Circle packings often follow hexagonal patterns in the densest regions\n- Maximum density for infinite circle packing is pi/(2*sqrt(3)) \u2248 0.9069\n- Edge effects make square container packing harder than infinite packing\n- Circles can be placed in layers or shells when confined to a square\n- Similar radius circles often form regular patterns, while varied radii allow better space utilization\n- Perfect symmetry may not yield the optimal packing due to edge effects\n\nFocus on designing an explicit constructor that places each circle in a specific position, rather than an iterative search algorithm.\n", "user": "# Current Program Information\n- Fitness: 0.6302\n- Feature coordinates: No feature coordinates\n- Focus areas: - Fitness declined: 0.8214 \u2192 0.6302. Consider revising recent changes.\n- Consider simplifying - code length exceeds 500 characters\n\n## Last Execution Output\n\n### stdout\n```\nRunning in subprocess, Python version: 3.12.1 (v3.12.1:2305ca5144, Dec  7 2023, 17:23:38) [Clang 13.0.0 (clang-1300.0.29.30)]\nProgram path: /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmp9d_otarj.py\nCalling run_packing()...\nrun_packing() returned successfully: sum_radii = 1.6605617046555405\nResults saved to /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmpu_bpes1w.py.results\n\n```\n\n### stderr\n```\n\n```\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 3\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.1645, target_ratio: 0.8214, combined_score: 0.8214, eval_time: 0.0564\n- Outcome: Mixed results\n\n### Attempt 2\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.1667, target_ratio: 0.8223, combined_score: 0.8223, eval_time: 0.0541\n- Outcome: Mixed results\n\n### Attempt 1\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.2660, target_ratio: 0.8600, combined_score: 0.8600, eval_time: 0.1070\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.8600)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (deterministic multi-candidate constructor)\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    center = np.array([0.5, 0.5])\n\n    # candidate generators: hex lattice (vary spacing, rotation, compression)\n    s_vals = np.linspace(0.105, 0.235, 18)\n    thetas = [0.0, np.pi/18, np.pi/12]  # small rotations\n    alphas = [0.80, 0.88, 0.94, 1.0]    # compress toward center to fight edge effects\n\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    # prepare integer grid\n    M = 9\n    ii = np.arange(-M, M + 1)\n    jj = np.arange(-M, M + 1)\n    I, J = np.meshgrid(ii, jj)\n    IJ = np.column_stack((I.ravel(), J.ravel()))\n\n    for s in s_vals:\n        # hex basis before rotation\n        v1 = np.array([s, 0.0])\n        v2 = np.array([s * 0.5, s * (np.sqrt(3) / 2.0)])\n        base_pts = IJ.dot(np.vstack((v1, v2)).T)  # large grid\n        for theta in thetas:\n            c = np.cos(theta); si = np.sin(theta)\n            R = np.array([[c, -si], [si, c]])\n            pts_rot = base_pts.dot(R.T) + center  # rotate and center\n            # keep only points roughly inside square (with small margin)\n            mask = np.all((pts_rot >= -1e-9) & (pts_rot <= 1 + 1e-9), axis=1)\n            valid = pts_rot[mask]\n            if valid.shape[0] < n:\n                continue\n            # sort by distance to center and take a local cloud\n            d2 = np.sum((valid - center) ** 2, axis=1)\n            order = np.argsort(d2)\n            cloud = valid[order[: max(n + 12, 2 * n)]]\n\n            for alpha in alphas:\n                # compress slightly toward center\n                offsets = cloud - center\n                comp = center + offsets * alpha\n                comp = np.clip(comp, 1e-9, 1 - 1e-9)\n                # pick closest n to center\n                d2c = np.sum((comp - center) ** 2, axis=1)\n                pick = np.argsort(d2c)[:n]\n                centers = comp[pick]\n                radii = compute_max_radii_relax(centers, max_iter=1200, tol=1e-8)\n                ssum = float(np.sum(radii))\n                if ssum > best_sum:\n                    best_sum = ssum\n                    best_centers = centers.copy()\n                    best_radii = radii.copy()\n\n    # also try a few structured row patterns (hand-tuned)\n    row_patterns = [\n        [5,5,4,4,4,4],  # compact\n        [6,5,5,5,5],    # wider middle\n        [5,6,5,5,5],    # alternative\n    ]\n    for rows in row_patterns:\n        max_cols = max(rows)\n        # try few spacings\n        for s in np.linspace(0.12, 0.20, 9):\n            sin60 = np.sqrt(3)/2\n            dy = s * sin60\n            # compute vertical offset to center\n            num_rows = len(rows)\n            height = (num_rows - 1) * dy\n            y0 = 0.5 - height / 2.0\n            centers_list = []\n            for ri, k in enumerate(rows):\n                offset = (s/2.0) if (ri % 2 == 1) else 0.0\n                row_width = (k - 1) * s\n                x0 = 0.5 - row_width / 2.0 + offset\n                y = y0 + ri * dy\n                for j in range(k):\n                    x = x0 + j * s\n                    centers_list.append([x, y])\n            centers_arr = np.clip(np.array(centers_list), 1e-9, 1-1e-9)\n            if centers_arr.shape[0] != n:\n                continue\n            radii = compute_max_radii_relax(centers_arr, max_iter=1200, tol=1e-8)\n            ssum = float(np.sum(radii))\n            if ssum > best_sum:\n                best_sum = ssum\n                best_centers = centers_arr.copy()\n                best_radii = radii.copy()\n\n    # final fallback: a symmetric concentric pattern\n    if best_centers is None:\n        best_centers = np.zeros((n,2))\n        best_centers[0] = center\n        for i in range(1,9):\n            ang = 2*np.pi*(i-1)/8\n            best_centers[i] = center + 0.18*np.array([np.cos(ang), np.sin(ang)])\n        for i in range(16):\n            ang = 2*np.pi*i/16\n            best_centers[i+9] = center + 0.34*np.array([np.cos(ang), np.sin(ang)])\n        best_centers = np.clip(best_centers, 1e-9, 1-1e-9)\n        best_radii = compute_max_radii_relax(best_centers)\n\n    sum_radii = float(np.sum(best_radii))\n    return best_centers, best_radii, sum_radii\n\ndef compute_max_radii_relax(centers, max_iter=800, tol=1e-7):\n    \"\"\"\n    Gauss-Seidel projection: for fixed centers compute maximal feasible radii.\n    r_i := min(dist_to_wall_i, min_j (dist_ij - r_j))\n    \"\"\"\n    n = centers.shape[0]\n    border = np.minimum.reduce([centers[:,0], centers[:,1], 1-centers[:,0], 1-centers[:,1]])\n    r = border.copy()\n    # precompute distances\n    diff = centers[:,None,:] - centers[None,:,:]\n    D = np.sqrt(np.maximum(np.sum(diff*diff,axis=2), 1e-12))\n    for it in range(max_iter):\n        maxc = 0.0\n        for i in range(n):\n            # compute candidate bounds dist_ij - r_j\n            cand = D[i] - r\n            cand[i] = border[i]  # ignore self\n            # only non-negative allowed\n            allowed = np.minimum(border[i], np.min(np.maximum(cand, 0.0)))\n            change = abs(r[i] - allowed)\n            if change > 0:\n                r[i] = allowed\n                if change > maxc: maxc = change\n        if maxc < tol:\n            break\n    return np.maximum(0.0, r)\n\n# EVOLVE-BLOCK-END\n\n# fixed runner\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(6,6))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect('equal')\n    for c,r in zip(centers, radii):\n        ax.add_patch(Circle(c, r, alpha=0.5, edgecolor='k'))\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, s = run_packing()\n    print(f\"Sum of radii: {s:.6f}\")\n    # visualize(centers, radii)\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.2660), Performs well on target_ratio (0.8600), Performs well on combined_score (0.8600), Performs well on eval_time (0.1070)\n\n### Program 2 (Score: 0.8223)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Improved constructor-based circle packing for n=26 circles.\n\nThis uses a hexagonal-like layered layout (rows with alternating offsets)\nto pack 26 circles in a unit square with equal radii chosen to be as large\nas possible given the row with the maximum count. This yields a much larger\nsum of radii than the original simple concentric-ring layout.\n\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square.\n\n    Returns:\n        Tuple of (centers, radii, sum_of_radii)\n        centers: np.array of shape (26, 2) with (x, y) coordinates\n        radii: np.array of shape (26) with radius of each circle\n        sum_of_radii: Sum of all radii\n    \"\"\"\n    # Row layout chosen to sum to 26: 6 + 5 + 6 + 5 + 4 = 26\n    rows = [6, 5, 6, 5, 4]\n    n = sum(rows)\n    assert n == 26\n\n    # Spacing factors for hexagonal-style packing\n    sqrt3 = np.sqrt(3.0)\n    max_in_row = max(rows)\n\n    # Horizontal constraint: 2 * r * max_in_row <= 1  -> r <= 1/(2*max_in_row)\n    r_horiz = 1.0 / (2.0 * max_in_row)\n\n    # Vertical constraint for rows: last_center_y <= 1 - r\n    # if y_k = r + k * (sqrt3 * r) for k=0..(R-1), require r + (R-1)*sqrt3*r <= 1 - r\n    # -> r * ((R-1)*sqrt3 + 2) <= 1\n    R = len(rows)\n    r_vert = 1.0 / (((R - 1) * sqrt3) + 2.0)\n\n    # Choose the limiting radius (horizontal typically limits for our layout)\n    r = min(r_horiz, r_vert)\n\n    centers = []\n    # Build rows from bottom (k=0) to top (k=R-1)\n    for k, count in enumerate(rows):\n        # Alternate offset every other row (hex packing)\n        offset = (k % 2) == 1\n        if not offset:\n            x_start = r  # centers: r, r+2r, r+4r, ...\n        else:\n            x_start = 2.0 * r  # shifted by r: 2r, 4r, ...\n        # horizontal spacing 2r\n        xs = [x_start + j * 2.0 * r for j in range(count)]\n        y = r + k * (sqrt3 * r)\n        for x in xs:\n            centers.append([x, y])\n\n    centers = np.array(centers, dtype=float)\n    # Safety: ensure count and containment\n    assert centers.shape == (n, 2)\n\n    # Use equal radii (designed to be non-overlapping inside the unit square)\n    radii = np.full(n, r, dtype=float)\n\n    # Validate and (if numeric tiny issues) correct radii so no overlaps and inside bounds\n    # Compute minimal distance to borders and to neighbors, then clamp to those maxima.\n    # This is a conservative correction; for a correctly computed r it leaves values unchanged.\n    # Distance to border\n    border_dists = np.minimum.reduce([centers[:, 0], centers[:, 1], 1.0 - centers[:, 0], 1.0 - centers[:, 1]])\n    max_by_border = border_dists.copy()\n\n    # Minimal half pairwise distances\n    pairwise = np.sqrt(((centers[:, None, :] - centers[None, :, :]) ** 2).sum(axis=2))\n    # set diagonal to large so min ignores zero\n    np.fill_diagonal(pairwise, np.inf)\n    min_half_dist = 0.5 * pairwise.min(axis=1)\n\n    # Final allowed radius per circle\n    allowed = np.minimum(max_by_border, min_half_dist)\n    # Numerical tolerance: do not enlarge beyond our design r, only shrink if necessary\n    radii = np.minimum(radii, allowed)\n    # If any radius became extremely small (shouldn't happen), fall back to allowed values\n    radii = np.maximum(radii, 1e-12)\n\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Hex-style Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.1667), Performs well on target_ratio (0.8223), Performs well on combined_score (0.8223), Performs well on eval_time (0.0541)\n\n### Program 3 (Score: 0.8214)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Explicit constructor for packing 26 circles in the unit square.\n\nUses a hexagonal-style layered layout with rows [6,5,6,5,4].\nSelects a uniform design radius from width/height constraints and\nthen clamps each circle by its border and neighbor distances to ensure\nfeasibility (no overlaps). This yields a compact, high-sum arrangement.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    # Row pattern summing to 26\n    rows = [6, 5, 6, 5, 4]\n    n = sum(rows)\n    assert n == 26\n\n    sqrt3 = np.sqrt(3.0)\n    R = len(rows)\n    max_in_row = max(rows)\n\n    # Horizontal constraint (centers spaced 2r): 2*r*max_in_row <= 1\n    r_horiz = 1.0 / (2.0 * max_in_row)\n    # Vertical constraint: r + (R-1)*sqrt3*r <= 1 - r  => r * (((R-1)*sqrt3)+2) <= 1\n    r_vert = 1.0 / (((R - 1) * sqrt3) + 2.0)\n\n    # choose safe design radius (slightly reduced to avoid exact boundary touches)\n    r = min(r_horiz, r_vert) * 0.999\n\n    centers = []\n    for k, count in enumerate(rows):\n        # alternate offset for hex packing\n        offset = (k % 2) == 1\n        if not offset:\n            x_start = r\n        else:\n            x_start = 2.0 * r\n        xs = [x_start + j * 2.0 * r for j in range(count)]\n        y = r + k * (sqrt3 * r)\n        for x in xs:\n            centers.append([x, y])\n\n    centers = np.array(centers, dtype=float)\n    # Enforce in-unit-square numerically\n    centers = np.clip(centers, 1e-12, 1.0 - 1e-12)\n\n    # Per-circle maximum allowed by borders\n    border_dists = np.minimum.reduce([centers[:, 0], centers[:, 1], 1.0 - centers[:, 0], 1.0 - centers[:, 1]])\n\n    # Pairwise distances (set diagonal to inf)\n    pd = np.sqrt(((centers[:, None, :] - centers[None, :, :]) ** 2).sum(axis=2))\n    np.fill_diagonal(pd, np.inf)\n    # For each circle, half the minimal pairwise distance is the max radius if neighbors had zero radius\n    min_half = 0.5 * pd.min(axis=1)\n\n    allowed = np.minimum(border_dists, min_half)\n    # Do not enlarge beyond design r (we rely on the hex design), only shrink if needed\n    radii = np.minimum(r * np.ones_like(allowed), allowed)\n    radii = np.maximum(radii, 0.0)\n\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n# EVOLVE-BLOCK-END\n\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    return construct_packing()\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(6,6))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect(\"equal\")\n    for i,(c,r) in enumerate(zip(centers, radii)):\n        ax.add_patch(Circle(c, r, alpha=0.6, edgecolor='k'))\n        ax.text(c[0], c[1], str(i), ha='center', va='center', fontsize=8)\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # visualize(centers, radii)\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.1645), Performs well on target_ratio (0.8214), Performs well on combined_score (0.8214), Performs well on eval_time (0.0564)\n\n\n\n## Diverse Programs\n\n### Program D1 (Score: 0.7976)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (explicit constructor)\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    \"\"\"\n    Construct 26 circle centers in a compact hex-like layered layout optimized\n    for large total radii, then compute near-maximal radii by coordinate-wise\n    tightening + a final proportional correction pass.\n    Returns (centers, radii, sum_of_radii).\n    \"\"\"\n    n = 26\n    # Row layout chosen for compactness (sums to 26)\n    rows = [5, 5, 4, 4, 4, 4]\n    num_rows = len(rows)\n    max_cols = max(rows)\n\n    sin60 = np.sqrt(3) / 2.0\n\n    # compute spacing s to fit width and height, with tiny shrink to avoid exact-touch\n    s_max_width = 1.0 / max_cols\n    s_max_height = 1.0 / ((num_rows - 1) * sin60 + 1.0)\n    s = 0.995 * min(s_max_width, s_max_height)\n    dy = sin60 * s\n    r0 = s / 2.0\n\n    centers = []\n    # Place rows bottom-to-top, horizontally centered relative to max_cols\n    for ri in range(num_rows):\n        k = rows[ri]\n        left = r0 + ((max_cols - k) * 0.5) * s\n        y = r0 + ri * dy\n        xs = left + np.arange(k) * s\n        for x in xs:\n            centers.append([x, y])\n\n    centers = np.array(centers[:n], dtype=float)\n    # clip tiny margin for numerical safety\n    centers = np.clip(centers, 1e-9, 1 - 1e-9)\n\n    radii = compute_max_radii(centers)\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\ndef compute_max_radii(centers, tol=1e-9, max_iter=2000):\n    \"\"\"\n    Coordinate-wise maximal radii solver:\n    - Start with border limits\n    - Repeatedly for each circle i set r_i = min(border_i, min_j (d_ij - r_j))\n      (clamped >=0). Iterate until convergence. This yields a feasible, often\n      near-maximal configuration. Finish with a proportional safety pass to\n      eliminate tiny overlaps.\n    \"\"\"\n    n = centers.shape[0]\n    # border limits\n    border = np.minimum.reduce([centers[:,0], centers[:,1], 1-centers[:,0], 1-centers[:,1]])\n    radii = border.copy()  # start maximal by border\n\n    # pairwise distances\n    diff = centers[:, None, :] - centers[None, :, :]\n    dists = np.sqrt(np.sum(diff * diff, axis=2))\n    np.fill_diagonal(dists, np.inf)\n\n    for it in range(max_iter):\n        max_change = 0.0\n        # Gauss-Seidel style: update each radius to the largest allowed given current others\n        for i in range(n):\n            limit = border[i]\n            # for j != i: r_i <= d_ij - r_j\n            # vectorized compute\n            oth = dists[i]\n            limits = oth - radii\n            # only consider finite distances\n            if limits.size:\n                limit = min(limit, float(np.min(limits)))\n            new_ri = max(0.0, limit)\n            if new_ri != radii[i]:\n                max_change = max(max_change, abs(radii[i] - new_ri))\n                radii[i] = new_ri\n        if max_change < tol:\n            break\n\n    # Final proportional relaxation to remove any tiny numerical overlaps\n    eps = 1e-12\n    for _ in range(200):\n        changed = False\n        for i in range(n):\n            for j in range(i+1, n):\n                dij = dists[i, j]\n                if not np.isfinite(dij):\n                    continue\n                if radii[i] + radii[j] > dij - eps:\n                    total = radii[i] + radii[j]\n                    if total > 0:\n                        factor = max(0.0, (dij - eps) / total)\n                        # scale both proportionally (preserve relative sizes)\n                        new_ri = radii[i] * factor\n                        new_rj = radii[j] * factor\n                    else:\n                        new_ri, new_rj = 0.0, 0.0\n                    if new_ri != radii[i] or new_rj != radii[j]:\n                        radii[i], radii[j] = new_ri, new_rj\n                        changed = True\n        # reapply border\n        clipped = np.minimum(radii, border)\n        if not changed and np.allclose(radii, clipped, atol=1e-12):\n            radii = clipped\n            break\n        radii = clipped\n    radii = np.maximum(radii, 0.0)\n    return radii\n\n# EVOLVE-BLOCK-END\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor='k')\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nKey features: Alternative approach to validity, Alternative approach to sum_radii\n\n### Program D2 (Score: 0.7880)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (explicit hex-like layout + Gauss-Seidel radii)\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    \"\"\"\n    Build 26 explicit centers arranged in a compact hex-like strip and compute\n    maximal non-overlapping radii by Gauss-Seidel style projection (no global search).\n    Returns: centers (26,2), radii (26,), sum_of_radii\n    \"\"\"\n    n = 26\n    # Row layout (works well in practice)\n    rows = [5, 5, 4, 4, 4, 4]  # sum = 26\n    num_rows = len(rows)\n    max_cols = max(rows)\n\n    # geometric constants\n    sin60 = np.sqrt(3) / 2.0\n    margin = 0.006  # small margin so centers aren't exactly on border\n\n    # choose spacing to respect both width and height\n    s_max_width = (1.0 - 2.0 * margin) / max_cols\n    s_max_height = (1.0 - 2.0 * margin) / ((num_rows - 1) * sin60 + 1.0)\n    s = 0.995 * min(s_max_width, s_max_height)\n    dy = sin60 * s\n    r_guess = s / 2.0\n\n    centers = []\n    # place rows from bottom to top, center each row horizontally\n    for ri in range(num_rows):\n        k = rows[ri]\n        # leftmost x such that row is centered within available width\n        row_width = (k - 1) * s\n        x_start = 0.5 - row_width / 2.0\n        # shift shorter rows by half-spacing to create hex-like staggering\n        if k < max_cols:\n            x_start += 0.5 * s\n        y = margin + r_guess + ri * dy\n        for j in range(k):\n            x = x_start + j * s\n            # clip tiny epsilon to stay inside strict interior\n            x = float(np.clip(x, margin + 1e-9, 1.0 - margin - 1e-9))\n            yy = float(np.clip(y, margin + 1e-9, 1.0 - margin - 1e-9))\n            centers.append([x, yy])\n\n    centers = np.array(centers[:n], dtype=float)\n\n    radii = compute_max_radii_gs(centers)\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\ndef compute_max_radii_gs(centers, tol=1e-9, max_iter=5000):\n    \"\"\"\n    Gauss-Seidel projection to find maximal feasible radii for fixed centers:\n      r_i <= dist_to_wall_i\n      r_i + r_j <= dist(i,j)\n    Update in-place: r_i <- min(dist_to_wall_i, min_j (d_ij - r_j))\n    \"\"\"\n    n = centers.shape[0]\n    xs = centers[:,0]; ys = centers[:,1]\n    wall = np.minimum.reduce([xs, ys, 1 - xs, 1 - ys])\n    wall = np.maximum(wall, 0.0)\n\n    # pairwise distances\n    diff = centers[:, None, :] - centers[None, :, :]\n    d = np.sqrt(np.maximum(np.sum(diff**2, axis=2), 1e-12))\n    np.fill_diagonal(d, np.inf)  # ignore self in min computations\n\n    # initialize radii at wall distances (upper bounds)\n    r = wall.copy()\n\n    for it in range(max_iter):\n        max_change = 0.0\n        # Gauss-Seidel: update each radius using latest values\n        for i in range(n):\n            # compute candidate bounds from other circles: d_ij - r_j\n            candidates = d[i] - r  # vector length n (includes self -> inf - r_i)\n            # enforce non-negativity on candidates\n            candidates = np.maximum(candidates, 0.0)\n            # r_i must be <= min(border, min_j candidates_j)\n            new_r = min(wall[i], float(np.min(candidates)))\n            # rarely can numerical noise make tiny negative; clamp\n            new_r = max(new_r, 0.0)\n            change = abs(r[i] - new_r)\n            if change > 0:\n                r[i] = new_r\n                if change > max_change:\n                    max_change = change\n        if max_change < tol:\n            break\n\n    # final safeguard\n    r = np.maximum(r, 0.0)\n    return r\n\n# EVOLVE-BLOCK-END\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nKey features: Alternative approach to validity, Alternative approach to sum_radii\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 0.7193, Type: Alternative)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (improved)\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    \"\"\"\n    Construct an explicit arrangement of 26 circle centers in the unit square\n    using a compact hex-like layered layout, then compute maximal non-overlapping\n    radii for those fixed centers by an iterative constraint propagation.\n    Returns (centers, radii, sum_of_radii).\n    \"\"\"\n    n = 26\n    # Row counts chosen to give 26 points in a compact hex-like layout\n    row_counts = [6, 5, 5, 5, 5]  # sum = 26\n    rows = len(row_counts)\n\n    # Hex lattice spacing guess (fits inside unit square with small margins)\n    margin = 0.01\n    # Choose horizontal spacing s constrained by the longest row width\n    max_row = max(row_counts)\n    s = min((1.0 - 2 * margin) / (max_row - 1), 0.19)  # keep a safe spacing\n    h = s * np.sqrt(3) / 2.0  # vertical spacing for hex pattern\n\n    # Vertical centering\n    total_height = (rows - 1) * h\n    y0 = (1.0 - total_height) / 2.0\n\n    centers = np.zeros((n, 2))\n    idx = 0\n    for r in range(rows):\n        k = row_counts[r]\n        # Alternate offset to create hex-like staggering\n        offset = (s / 2.0) if (r % 2 == 1) else 0.0\n        row_width = (k - 1) * s\n        x_start = 0.5 - row_width / 2.0 + offset\n        y = y0 + r * h\n        for j in range(k):\n            x = x_start + j * s\n            # Clip into interior slightly to avoid exact border\n            centers[idx, 0] = np.clip(x, margin, 1.0 - margin)\n            centers[idx, 1] = np.clip(y, margin, 1.0 - margin)\n            idx += 1\n\n    # Safety: ensure exactly 26\n    centers = centers[:n]\n\n    radii = compute_max_radii(centers)\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\ndef compute_max_radii(centers, tol=1e-7, max_iter=5000):\n    \"\"\"\n    Given fixed centers, compute maximal radii so circles stay inside [0,1]^2\n    and do not overlap. Procedure: start with border-limited radii and iteratively\n    enforce pairwise non-overlap by shrinking the larger circle when needed.\n    This is a monotone decreasing scheme that typically converges quickly.\n    \"\"\"\n    n = centers.shape[0]\n    # initial radii limited by distance to borders\n    radii = np.minimum.reduce([centers[:,0], centers[:,1], 1-centers[:,0], 1-centers[:,1]])\n    radii = np.maximum(radii, 0.0)\n\n    # Precompute pairwise center distances\n    diff = centers[:, None, :] - centers[None, :, :]\n    dists = np.sqrt(np.sum(diff**2, axis=2))\n    # Avoid self distances = 0 on diagonal\n    np.fill_diagonal(dists, np.inf)\n\n    for it in range(max_iter):\n        max_change = 0.0\n        # enforce pairwise constraints\n        for i in range(n):\n            for j in range(i+1, n):\n                dij = dists[i, j]\n                if dij == 0.0 or np.isinf(dij):\n                    continue\n                ri = radii[i]\n                rj = radii[j]\n                total = ri + rj\n                if total > dij:\n                    # amount to reduce\n                    overlap = total - dij\n                    # reduce the larger radius by the overlap (never increase)\n                    if ri >= rj:\n                        new_ri = max(0.0, ri - overlap)\n                        change = ri - new_ri\n                        if change > 0:\n                            radii[i] = new_ri\n                            max_change = max(max_change, change)\n                    else:\n                        new_rj = max(0.0, rj - overlap)\n                        change = rj - new_rj\n                        if change > 0:\n                            radii[j] = new_rj\n                            max_change = max(max_change, change)\n        # re-enforce border limits (they may still be the binding constraint)\n        border_limits = np.minimum.reduce([centers[:,0], centers[:,1], 1-centers[:,0], 1-centers[:,1]])\n        # ensure we don't increase any radius above border limits\n        clipped = np.minimum(radii, border_limits)\n        max_change = max(max_change, float(np.max(np.abs(radii - clipped))))\n        radii = clipped\n\n        if max_change < tol:\n            break\n\n    return radii\n\n# EVOLVE-BLOCK-END\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor='k')\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nUnique approach: Modification: Full rewrite, Excellent validity (1.000), Excellent sum_radii (1.895)\n\n### Inspiration 2 (Score: 0.7545, Type: Alternative)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (hex-like layers)\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    \"\"\"\n    Construct 26 circle centers in a compact hex-like layered layout inside [0,1]^2,\n    then compute maximal non-overlapping radii for those fixed centers.\n    Returns (centers, radii, sum_of_radii).\n    \"\"\"\n    n = 26\n    # Row counts chosen to create a compact hex-like packing (sum = 26)\n    row_counts = [6, 5, 5, 5, 5]\n    rows = len(row_counts)\n\n    margin = 0.005\n    max_row = max(row_counts)\n    # horizontal spacing chosen to fit the widest row comfortably\n    s = min((1.0 - 2 * margin) / (max_row - 1), 0.18)\n    h = s * np.sqrt(3) / 2.0  # vertical spacing for hex pattern\n\n    total_height = (rows - 1) * h\n    y0 = (1.0 - total_height) / 2.0\n\n    centers = np.zeros((n, 2))\n    idx = 0\n    for r in range(rows):\n        k = row_counts[r]\n        offset = (s / 2.0) if (r % 2 == 1) else 0.0\n        row_width = (k - 1) * s\n        x_start = 0.5 - row_width / 2.0 + offset\n        y = y0 + r * h\n        for j in range(k):\n            x = x_start + j * s\n            centers[idx, 0] = np.clip(x, margin, 1.0 - margin)\n            centers[idx, 1] = np.clip(y, margin, 1.0 - margin)\n            idx += 1\n    centers = centers[:n]\n\n    radii = compute_max_radii(centers)\n    return centers, radii, float(np.sum(radii))\n\ndef compute_max_radii(centers, tol=1e-8, max_iter=5000):\n    \"\"\"\n    Iteratively compute maximal radii for fixed centers so circles lie in [0,1]^2\n    and are pairwise non-overlapping. At each violation we shrink the larger of the\n    two circles involved. Monotone decreasing process converges quickly.\n    \"\"\"\n    n = centers.shape[0]\n    # start with border-limited radii\n    border = np.minimum.reduce([centers[:,0], centers[:,1], 1-centers[:,0], 1-centers[:,1]])\n    radii = border.copy()\n\n    # pairwise center distances\n    diff = centers[:, None, :] - centers[None, :, :]\n    dists = np.sqrt(np.sum(diff**2, axis=2))\n    np.fill_diagonal(dists, np.inf)\n\n    for _ in range(max_iter):\n        max_change = 0.0\n        # check all pairs\n        for i in range(n):\n            # vectorized over j would be fine but keep explicit to control shrink policy\n            for j in range(i+1, n):\n                dij = dists[i, j]\n                if not np.isfinite(dij):\n                    continue\n                ri, rj = radii[i], radii[j]\n                if ri + rj > dij:\n                    overlap = ri + rj - dij\n                    # shrink the larger radius by the overlap (never increase)\n                    if ri >= rj:\n                        newri = max(0.0, ri - overlap)\n                        change = ri - newri\n                        if change > 0:\n                            radii[i] = newri\n                            max_change = max(max_change, change)\n                    else:\n                        newrj = max(0.0, rj - overlap)\n                        change = rj - newrj\n                        if change > 0:\n                            radii[j] = newrj\n                            max_change = max(max_change, change)\n        # ensure border constraints still hold\n        clipped = np.minimum(radii, border)\n        max_change = max(max_change, float(np.max(np.abs(radii - clipped))))\n        radii = clipped\n        if max_change < tol:\n            break\n    return radii\n\n# EVOLVE-BLOCK-END\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor='k')\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nUnique approach: Modification: Full rewrite, Excellent validity (1.000), Excellent sum_radii (1.988)\n\n### Inspiration 3 (Score: 0.6928, Type: Alternative)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (hex-like rows + coordinate-wise LP relaxation)\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square.\n    Returns: (centers, radii, sum_of_radii)\n    \"\"\"\n    # Layout: 5 rows with counts [6,5,5,5,5] (total 26), arranged like a hexagonal strip\n    counts = [6, 5, 5, 5, 5]\n    n = sum(counts)\n    assert n == 26\n\n    margin = 0.02  # keep centers away from exact border so wall distance is positive\n    max_count = max(counts)\n    avail_w = 1.0 - 2.0 * margin\n\n    # horizontal spacing chosen so max_count fits comfortably\n    spacing_x = avail_w / max_count\n    # hex vertical spacing for closest-packing-like rows\n    spacing_y = spacing_x * (np.sqrt(3) / 2.0)\n\n    # compute y positions centered vertically\n    nrows = len(counts)\n    total_h = spacing_y * (nrows - 1)\n    y0 = 0.5 - total_h / 2.0\n\n    centers = []\n    for row_idx, cnt in enumerate(counts):\n        y = y0 + row_idx * spacing_y\n        # center row horizontally\n        row_width = spacing_x * (cnt - 1) if cnt > 1 else 0.0\n        x_start = 0.5 - row_width / 2.0\n        # offset alternate rows for hex-like packing (shift by half spacing when row has fewer centers)\n        # choose offset so rows with cnt < max_count get half-shift to interlock\n        if cnt < max_count:\n            offset = spacing_x / 2.0\n        else:\n            offset = 0.0\n        for k in range(cnt):\n            x = x_start + offset + k * spacing_x\n            # clamp to ensure strictly inside unit square interior\n            x = float(np.clip(x, margin + 1e-6, 1.0 - margin - 1e-6))\n            y = float(np.clip(y, margin + 1e-6, 1.0 - margin - 1e-6))\n            centers.append([x, y])\n\n    centers = np.array(centers, dtype=float)\n    # Safety: if we accidentally produced different count, pad or trim (shouldn't happen)\n    if centers.shape[0] > 26:\n        centers = centers[:26].copy()\n    elif centers.shape[0] < 26:\n        # add a small cluster near a corner if needed (unlikely)\n        extra = 26 - centers.shape[0]\n        for i in range(extra):\n            centers = np.vstack([centers, [margin + 0.02*(i+1), margin + 0.02*(i+1)]])\n\n    radii = compute_max_radii(centers)\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\ndef compute_max_radii(centers, tol=1e-7, max_iter=2000):\n    \"\"\"\n    Compute near-optimal radii by coordinate-wise relaxation to satisfy:\n      r_i >= 0, r_i <= dist_to_wall_i, r_i + r_j <= dist_ij for all pairs.\n    The update r_i := min(dist_to_wall_i, min_j (max(0, dist_ij - r_j))) is a Gauss-Seidel\n    style projection that converges to a maximal feasible point for these constraints.\n    \"\"\"\n    n = centers.shape[0]\n    # distance to walls\n    xs = centers[:, 0]\n    ys = centers[:, 1]\n    wall = np.minimum.reduce([xs, ys, 1.0 - xs, 1.0 - ys])\n    wall = np.maximum(0.0, wall)\n\n    # precompute pairwise distances\n    dif = centers[:, None, :] - centers[None, :, :]  # (n,n,2)\n    dists = np.sqrt(np.sum(dif * dif, axis=2))\n    # avoid zero distances on diagonal\n    # dists[i,i] = 0\n\n    # initialize radii to wall distances (upper bounds)\n    r = wall.copy()\n\n    # iterative coordinate update\n    for it in range(max_iter):\n        r_prev = r.copy()\n        for i in range(n):\n            # for each j != i, r_i <= max(0, d_ij - r_j)\n            # take min over j to enforce all pair constraints, and also wall bound\n            # skip j==i where d_ii==0\n            bounds = wall[i]\n            # vectorized computation for speed\n            # dists[i] - r gives candidate upper bounds; ignore self by setting to large positive\n            candidates = dists[i] - r\n            candidates[i] = bounds  # won't be chosen smaller\n            # ensure non-negative\n            candidates = np.maximum(0.0, candidates)\n            # r_i should be <= min(candidates)\n            bounds = min(bounds, np.min(candidates))\n            r[i] = bounds\n        # convergence check\n        if np.max(np.abs(r - r_prev)) < tol:\n            break\n\n    # final safeguard: clip non-negatives\n    r = np.maximum(0.0, r)\n    return r\n\n# EVOLVE-BLOCK-END\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nUnique approach: Modification: Full rewrite, Excellent validity (1.000), Excellent sum_radii (1.826)\n\n# Current Program\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (explicit hex-like constructor)\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct an explicit arrangement of 26 circle centers in the unit square\n    using a compact hexagonal-ish layered layout, then compute maximal\n    non-overlapping radii for those fixed centers.\n    Returns: centers (26,2), radii (26,), sum_of_radii (float)\n    \"\"\"\n    n = 26\n    # Choose a balanced row layout summing to 26 (compact, with slight asymmetry)\n    rows = [5, 5, 4, 4, 4, 4]  # sum = 26\n    num_rows = len(rows)\n    max_cols = max(rows)\n\n    # Keep a small margin so centers are not placed exactly on the border\n    margin = 0.008\n\n    # Hex geometry\n    sin60 = np.sqrt(3.0) / 2.0\n\n    # Determine maximal horizontal spacing s so the widest row fits between margins\n    # and maximal vertical spacing so the stacked rows fit between margins.\n    if max_cols > 1:\n        s_max_width = (1.0 - 2.0 * margin) / (max_cols - 1)\n    else:\n        s_max_width = 1.0 - 2.0 * margin\n    if num_rows > 1:\n        s_max_height = (1.0 - 2.0 * margin) / ((num_rows - 1) * sin60)\n    else:\n        s_max_height = 1.0 - 2.0 * margin\n\n    # Use a slightly reduced spacing to avoid numerical touches\n    s = 0.985 * min(s_max_width, s_max_height)\n    dy = s * sin60\n\n    centers = []\n    # center vertically so pattern is symmetric in y\n    total_h = (num_rows - 1) * dy\n    y0 = 0.5 - total_h / 2.0\n\n    # build rows from bottom to top\n    for ri, cnt in enumerate(rows):\n        # For hex-like staggering, alternate rows get half-shift\n        stagger = (ri % 2) * 0.5 * s\n        # compute row width and leftmost x so the row is horizontally centered\n        if cnt > 1:\n            row_width = (cnt - 1) * s\n        else:\n            row_width = 0.0\n        x_start = 0.5 - row_width / 2.0 + stagger\n        y = y0 + ri * dy\n        for j in range(cnt):\n            x = x_start + j * s\n            # ensure inside [margin, 1-margin]\n            x = float(np.clip(x, margin, 1.0 - margin))\n            yv = float(np.clip(y, margin, 1.0 - margin))\n            centers.append([x, yv])\n\n    centers = np.array(centers, dtype=float)\n    # safety: ensure exactly n centers\n    if centers.shape[0] > n:\n        centers = centers[:n].copy()\n    elif centers.shape[0] < n:\n        # pad near center if needed (shouldn't happen)\n        extra = n - centers.shape[0]\n        for k in range(extra):\n            centers = np.vstack([centers, [0.5 + 0.01 * (k + 1), 0.5 + 0.01 * (k + 1)]])\n\n    radii = compute_max_radii(centers)\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers, tol=1e-9, max_iter=3000):\n    \"\"\"\n    Given fixed centers inside [0,1]^2, compute the maximal feasible radii vector\n    satisfying r_i >= 0, r_i <= dist_to_wall_i, and r_i + r_j <= dist_ij.\n    Use Gauss-Seidel coordinate-wise projection:\n        r_i := min(dist_to_wall_i, min_{j!=i} max(0, dist_ij - r_j))\n    This converges to the largest feasible radii for the fixed centers.\n    \"\"\"\n    n = centers.shape[0]\n    # distance to walls (upper bounds)\n    xs = centers[:, 0]\n    ys = centers[:, 1]\n    wall = np.minimum.reduce([xs, ys, 1.0 - xs, 1.0 - ys])\n    wall = np.maximum(wall, 0.0)\n\n    # pairwise distances\n    diff = centers[:, None, :] - centers[None, :, :]\n    dists = np.sqrt(np.maximum(np.sum(diff * diff, axis=2), 1e-14))\n    # ensure diagonal is large so it doesn't affect mins\n    np.fill_diagonal(dists, np.inf)\n\n    # initialize radii at wall limits (a valid upper bound)\n    r = wall.copy()\n\n    for it in range(max_iter):\n        max_change = 0.0\n        # Gauss-Seidel updates: use newest radii as they become available\n        for i in range(n):\n            # candidate upper bounds from other circles: dist_ij - r_j\n            candidates = dists[i] - r\n            # ensure we don't take the diagonal (set to large positive)\n            candidates[i] = np.inf\n            # feasible based on pairwise constraints: r_i <= min_j max(0, dist_ij - r_j)\n            pair_bound = np.min(np.maximum(0.0, candidates))\n            new_ri = min(wall[i], pair_bound)\n            # never increase above current (but algorithm allows increase if wall allows; that's fine)\n            change = abs(new_ri - r[i])\n            if change > 0:\n                r[i] = new_ri\n                if change > max_change:\n                    max_change = change\n        if max_change < tol:\n            break\n\n    # final clamp\n    r = np.maximum(r, 0.0)\n    return r\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\n\n# Task\nRewrite the program to improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: complexity, diversity\nDifferent solutions with similar fitness but different features are valuable.\nProvide the complete new program code.\n\nIMPORTANT: Make sure your rewritten program maintains the same inputs and outputs\nas the original program, but with improved internal implementation.\n\n```python\n# Your rewritten program here\n```", "responses": ["```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for packing 26 circles in the unit square.\n\nStrategy:\n- Try several compact hex-like row layouts known to work well for n=26.\n- For each layout, sweep a small range of horizontal spacings (s) around the\n  largest spacing that fits the pattern inside the unit square.\n- For each candidate center set compute maximal feasible radii by a\n  Gauss-Seidel style projection (coordinate-wise maximal radii for fixed centers).\n- Return the best configuration found (centers, radii, sum_radii).\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    # Diverse compact row layouts (all sum to 26)\n    row_patterns = [\n        [6, 5, 6, 5, 4],      # high-performing pattern from experiments\n        [5, 6, 5, 5, 5],      # alternative balanced\n        [5, 5, 4, 4, 4, 4],   # elongated\n        [6, 5, 5, 5, 5],      # wider middle\n    ]\n\n    sin60 = np.sqrt(3.0) / 2.0\n    margin = 0.006  # small interior margin to avoid exact touches\n\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    # For each pattern, try a small sweep of spacings near the maximal allowable\n    for rows in row_patterns:\n        num_rows = len(rows)\n        max_cols = max(rows)\n        # compute maximal s to fit width and height with margins\n        if max_cols > 1:\n            s_max_w = (1.0 - 2.0 * margin) / (max_cols - 1)\n        else:\n            s_max_w = 1.0 - 2.0 * margin\n        if num_rows > 1:\n            s_max_h = (1.0 - 2.0 * margin) / ((num_rows - 1) * sin60)\n        else:\n            s_max_h = 1.0 - 2.0 * margin\n        s_base = min(s_max_w, s_max_h)\n        # try a few reduced spacings to let radii relax and avoid exact touching\n        for factor in np.linspace(0.86, 0.995, 10):\n            s = s_base * factor\n            dy = s * sin60\n            # center vertically\n            total_h = (num_rows - 1) * dy if num_rows > 1 else 0.0\n            y0 = 0.5 - total_h / 2.0\n            centers_list = []\n            for ri, cnt in enumerate(rows):\n                # hex-like staggering: alternate rows shifted by half spacing\n                stagger = (ri % 2) * 0.5 * s\n                if cnt > 1:\n                    row_width = (cnt - 1) * s\n                else:\n                    row_width = 0.0\n                x_start = 0.5 - row_width / 2.0 + stagger\n                y = y0 + ri * dy\n                for j in range(cnt):\n                    x = x_start + j * s\n                    centers_list.append([x, y])\n            centers = np.array(centers_list, dtype=float)\n            if centers.shape[0] != n:\n                # skip if pattern unexpectedly didn't produce 26\n                continue\n            # clip slight numerical excursions\n            centers = np.clip(centers, 1e-9, 1.0 - 1e-9)\n            radii = compute_max_radii_relax(centers, max_iter=2500, tol=1e-9)\n            ssum = float(np.sum(radii))\n            if ssum > best_sum:\n                best_sum = ssum\n                best_centers = centers.copy()\n                best_radii = radii.copy()\n\n    # Fallback deterministic pattern if none found (shouldn't happen)\n    if best_centers is None:\n        rows = [6,5,6,5,4]\n        sin60 = np.sqrt(3.0)/2.0\n        s = 0.14\n        dy = s * sin60\n        total_h = (len(rows)-1)*dy\n        y0 = 0.5 - total_h/2.0\n        centers = []\n        for ri,cnt in enumerate(rows):\n            stagger = (ri%2)*0.5*s\n            row_width = (cnt-1)*s if cnt>1 else 0.0\n            x_start = 0.5 - row_width/2.0 + stagger\n            y = y0 + ri*dy\n            for j in range(cnt):\n                centers.append([x_start + j*s, y])\n        best_centers = np.clip(np.array(centers[:n],dtype=float),1e-9,1-1e-9)\n        best_radii = compute_max_radii_relax(best_centers)\n\n    sum_radii = float(np.sum(best_radii))\n    return best_centers, best_radii, sum_radii\n\ndef compute_max_radii_relax(centers, max_iter=2000, tol=1e-8):\n    \"\"\"\n    Gauss-Seidel projection to compute maximal radii for fixed centers.\n    Update rule:\n      r_i := min(dist_to_wall_i, min_{j!=i} max(0, d_ij - r_j))\n    This yields a feasible (and typically maximal) radii vector.\n    \"\"\"\n    n = centers.shape[0]\n    xs = centers[:,0]; ys = centers[:,1]\n    wall = np.minimum.reduce([xs, ys, 1.0 - xs, 1.0 - ys])\n    wall = np.maximum(wall, 0.0)\n\n    # pairwise distances\n    diff = centers[:, None, :] - centers[None, :, :]\n    D = np.sqrt(np.maximum(np.sum(diff*diff, axis=2), 1e-14))\n    np.fill_diagonal(D, np.inf)\n\n    # initialize at wall limits\n    r = wall.copy()\n\n    for it in range(max_iter):\n        max_change = 0.0\n        for i in range(n):\n            # candidate bounds from other circles: D[i] - r\n            cand = D[i] - r\n            # ensure non-negative candidate values before min\n            cand = np.maximum(cand, 0.0)\n            # pairbound is min over j of cand_j (j != i)\n            pairbound = float(np.min(cand))\n            newri = min(wall[i], pairbound)\n            # clamp small negatives\n            newri = max(newri, 0.0)\n            change = abs(newri - r[i])\n            if change > 0:\n                r[i] = newri\n                if change > max_change:\n                    max_change = change\n        if max_change < tol:\n            break\n\n    return np.maximum(r, 0.0)\n\n# EVOLVE-BLOCK-END\n\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n# Optional: small visualizer if run standalone\nif __name__ == \"__main__\":\n    c, r, s = run_packing()\n    print(f\"Sum of radii: {s:.6f}\")\n```"]}}, "artifacts_json": "{\"stdout\": \"Running in subprocess, Python version: 3.12.1 (v3.12.1:2305ca5144, Dec  7 2023, 17:23:38) [Clang 13.0.0 (clang-1300.0.29.30)]\\nProgram path: /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmp0elplcco.py\\nCalling run_packing()...\\nrun_packing() returned successfully: sum_radii = 2.2191689344443306\\nResults saved to /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmpjzg_rq7r.py.results\\n\", \"stderr\": \"\"}", "artifact_dir": null}