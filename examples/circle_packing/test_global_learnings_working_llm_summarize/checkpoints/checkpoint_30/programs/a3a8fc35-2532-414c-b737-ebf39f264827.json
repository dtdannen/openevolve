{"id": "a3a8fc35-2532-414c-b737-ebf39f264827", "code": "# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for 26 circles in the unit square.\nBuilds compact hex-like rows (pattern [6,5,6,5,4]) and searches a small\nfamily of spacings, small rotations and mild radial compressions to fight\nedge effects. For fixed centers we compute maximal feasible radii by a\nGauss-Seidel style projection (fast, convergent).\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    rows = [6, 5, 6, 5, 4]  # compact hex-like strip summing to 26\n    sqrt3 = np.sqrt(3.0)\n    sin60 = sqrt3 / 2.0\n\n    # candidate parameters (small deterministic grid)\n    s_vals = np.linspace(0.11, 0.185, 13)   # horizontal spacing between centers\n    thetas = [0.0, np.pi/36, -np.pi/36]     # tiny rotations\n    alphas = [1.00, 0.96, 0.92]             # compress toward center to reduce edge loss\n\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n    center = np.array([0.5, 0.5])\n\n    for s in s_vals:\n        dy = sin60 * s\n        # compute vertical span and center y0 so rows sit roughly centered\n        R = len(rows)\n        total_h = (R - 1) * dy\n        y0 = 0.5 - total_h / 2.0\n        for theta in thetas:\n            c = np.cos(theta); si = np.sin(theta)\n            Rmat = np.array([[c, -si], [si, c]])\n            for alpha in alphas:\n                centers = []\n                for ri, cnt in enumerate(rows):\n                    # compute x positions centered\n                    row_width = (cnt - 1) * s\n                    x0 = 0.5 - row_width / 2.0\n                    # alternate offset to mimic hex staggering\n                    offset = (s/2.0) if (ri % 2 == 1) else 0.0\n                    xs = x0 + offset + np.arange(cnt) * s\n                    y = y0 + ri * dy\n                    for x in xs:\n                        centers.append([x, y])\n                C = np.array(centers)\n                # compress slightly toward square center, then rotate about center\n                C = center + (C - center) * alpha\n                C = (C - center).dot(Rmat.T) + center\n                # clip tiny numerical drift\n                C = np.clip(C, 1e-9, 1.0 - 1e-9)\n                if C.shape[0] != n:\n                    continue\n                radii = compute_max_radii_relax(C, max_iter=1200, tol=1e-8)\n                ssum = float(np.sum(radii))\n                if ssum > best_sum:\n                    best_sum = ssum\n                    best_centers = C.copy()\n                    best_radii = radii.copy()\n\n    # fallback pattern if search failed\n    if best_centers is None:\n        best_centers = np.array([[0.5,0.5]])\n        best_radii = np.array([0.1])\n\n    return best_centers, best_radii, float(np.sum(best_radii))\n\ndef compute_max_radii_relax(centers, max_iter=800, tol=1e-8):\n    \"\"\"\n    For fixed centers compute maximal feasible radii solving:\n      r_i <= dist_to_wall_i, r_i + r_j <= dist_ij\n    via Gauss-Seidel coordinate projections:\n      r_i := min(dist_wall_i, min_j max(0, dist_ij - r_j))\n    \"\"\"\n    n = centers.shape[0]\n    xs = centers[:,0]; ys = centers[:,1]\n    border = np.minimum.reduce([xs, ys, 1.0 - xs, 1.0 - ys])\n    # pairwise distances\n    diff = centers[:, None, :] - centers[None, :, :]\n    D = np.sqrt(np.maximum((diff**2).sum(axis=2), 1e-16))\n    np.fill_diagonal(D, np.inf)\n    # initialize radii by border distances (safe upper bound)\n    r = border.copy()\n    for it in range(max_iter):\n        max_change = 0.0\n        for i in range(n):\n            # candidate bounds from neighbors: D[i] - r (vector), but cannot be negative\n            cand = D[i] - r\n            cand[i] = border[i]  # ignore self\n            # allowed radius is min(border[i], min_j max(0, D_ij - r_j))\n            allowed = min(border[i], float(np.min(np.maximum(cand, 0.0))))\n            change = abs(r[i] - allowed)\n            if change > 0:\n                r[i] = allowed\n                if change > max_change:\n                    max_change = change\n        if max_change < tol:\n            break\n    return np.maximum(0.0, r)\n\n# EVOLVE-BLOCK-END\n\n# fixed runner\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(6,6))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect('equal')\n    for c,r in zip(centers, radii):\n        ax.add_patch(Circle(c, r, alpha=0.5, edgecolor='k'))\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, s = run_packing()\n    print(f\"Sum of radii: {s:.6f}\")\n    # visualize(centers, radii)", "language": "python", "parent_id": "200bdf2b-15bd-48dd-b450-37f13a2c7986", "generation": 2, "timestamp": 1759958356.4151611, "iteration_found": 28, "metrics": {"validity": 1.0, "sum_radii": 1.9105148139475039, "target_ratio": 0.7250530603216334, "combined_score": 0.7250530603216334, "eval_time": 0.08677029609680176}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Full rewrite", "parent_metrics": {"validity": 1.0, "sum_radii": 2.0550740989663336, "target_ratio": 0.7799142690574322, "combined_score": 0.7799142690574322, "eval_time": 0.05728578567504883}, "island": 0}, "prompts": {"full_rewrite_user": {"system": "You are an expert mathematician specializing in circle packing problems and computational geometry. Your task is to improve a constructor function that directly produces a specific arrangement of 26 circles in a unit square, maximizing the sum of their radii. The AlphaEvolve paper achieved a sum of 2.635 for n=26.\n\nKey geometric insights:\n- Circle packings often follow hexagonal patterns in the densest regions\n- Maximum density for infinite circle packing is pi/(2*sqrt(3)) \u2248 0.9069\n- Edge effects make square container packing harder than infinite packing\n- Circles can be placed in layers or shells when confined to a square\n- Similar radius circles often form regular patterns, while varied radii allow better space utilization\n- Perfect symmetry may not yield the optimal packing due to edge effects\n\nFocus on designing an explicit constructor that places each circle in a specific position, rather than an iterative search algorithm.\n", "user": "# Current Program Information\n- Fitness: 0.7799\n- Feature coordinates: No feature coordinates\n- Focus areas: - Fitness declined: 0.8223 \u2192 0.7799. Consider revising recent changes.\n- Consider simplifying - code length exceeds 500 characters\n\n## Last Execution Output\n\n### stdout\n```\nRunning in subprocess, Python version: 3.12.1 (v3.12.1:2305ca5144, Dec  7 2023, 17:23:38) [Clang 13.0.0 (clang-1300.0.29.30)]\nProgram path: /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmpo66uvxs0.py\nCalling run_packing()...\nrun_packing() returned successfully: sum_radii = 2.0550740989663336\nResults saved to /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmpks_o_u1_.py.results\n\n```\n\n### stderr\n```\n\n```\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 3\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.1667, target_ratio: 0.8223, combined_score: 0.8223, eval_time: 0.0561\n- Outcome: Mixed results\n\n### Attempt 2\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.1667, target_ratio: 0.8223, combined_score: 0.8223, eval_time: 0.0541\n- Outcome: Mixed results\n\n### Attempt 1\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.2660, target_ratio: 0.8600, combined_score: 0.8600, eval_time: 0.1070\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.8600)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (deterministic multi-candidate constructor)\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    center = np.array([0.5, 0.5])\n\n    # candidate generators: hex lattice (vary spacing, rotation, compression)\n    s_vals = np.linspace(0.105, 0.235, 18)\n    thetas = [0.0, np.pi/18, np.pi/12]  # small rotations\n    alphas = [0.80, 0.88, 0.94, 1.0]    # compress toward center to fight edge effects\n\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    # prepare integer grid\n    M = 9\n    ii = np.arange(-M, M + 1)\n    jj = np.arange(-M, M + 1)\n    I, J = np.meshgrid(ii, jj)\n    IJ = np.column_stack((I.ravel(), J.ravel()))\n\n    for s in s_vals:\n        # hex basis before rotation\n        v1 = np.array([s, 0.0])\n        v2 = np.array([s * 0.5, s * (np.sqrt(3) / 2.0)])\n        base_pts = IJ.dot(np.vstack((v1, v2)).T)  # large grid\n        for theta in thetas:\n            c = np.cos(theta); si = np.sin(theta)\n            R = np.array([[c, -si], [si, c]])\n            pts_rot = base_pts.dot(R.T) + center  # rotate and center\n            # keep only points roughly inside square (with small margin)\n            mask = np.all((pts_rot >= -1e-9) & (pts_rot <= 1 + 1e-9), axis=1)\n            valid = pts_rot[mask]\n            if valid.shape[0] < n:\n                continue\n            # sort by distance to center and take a local cloud\n            d2 = np.sum((valid - center) ** 2, axis=1)\n            order = np.argsort(d2)\n            cloud = valid[order[: max(n + 12, 2 * n)]]\n\n            for alpha in alphas:\n                # compress slightly toward center\n                offsets = cloud - center\n                comp = center + offsets * alpha\n                comp = np.clip(comp, 1e-9, 1 - 1e-9)\n                # pick closest n to center\n                d2c = np.sum((comp - center) ** 2, axis=1)\n                pick = np.argsort(d2c)[:n]\n                centers = comp[pick]\n                radii = compute_max_radii_relax(centers, max_iter=1200, tol=1e-8)\n                ssum = float(np.sum(radii))\n                if ssum > best_sum:\n                    best_sum = ssum\n                    best_centers = centers.copy()\n                    best_radii = radii.copy()\n\n    # also try a few structured row patterns (hand-tuned)\n    row_patterns = [\n        [5,5,4,4,4,4],  # compact\n        [6,5,5,5,5],    # wider middle\n        [5,6,5,5,5],    # alternative\n    ]\n    for rows in row_patterns:\n        max_cols = max(rows)\n        # try few spacings\n        for s in np.linspace(0.12, 0.20, 9):\n            sin60 = np.sqrt(3)/2\n            dy = s * sin60\n            # compute vertical offset to center\n            num_rows = len(rows)\n            height = (num_rows - 1) * dy\n            y0 = 0.5 - height / 2.0\n            centers_list = []\n            for ri, k in enumerate(rows):\n                offset = (s/2.0) if (ri % 2 == 1) else 0.0\n                row_width = (k - 1) * s\n                x0 = 0.5 - row_width / 2.0 + offset\n                y = y0 + ri * dy\n                for j in range(k):\n                    x = x0 + j * s\n                    centers_list.append([x, y])\n            centers_arr = np.clip(np.array(centers_list), 1e-9, 1-1e-9)\n            if centers_arr.shape[0] != n:\n                continue\n            radii = compute_max_radii_relax(centers_arr, max_iter=1200, tol=1e-8)\n            ssum = float(np.sum(radii))\n            if ssum > best_sum:\n                best_sum = ssum\n                best_centers = centers_arr.copy()\n                best_radii = radii.copy()\n\n    # final fallback: a symmetric concentric pattern\n    if best_centers is None:\n        best_centers = np.zeros((n,2))\n        best_centers[0] = center\n        for i in range(1,9):\n            ang = 2*np.pi*(i-1)/8\n            best_centers[i] = center + 0.18*np.array([np.cos(ang), np.sin(ang)])\n        for i in range(16):\n            ang = 2*np.pi*i/16\n            best_centers[i+9] = center + 0.34*np.array([np.cos(ang), np.sin(ang)])\n        best_centers = np.clip(best_centers, 1e-9, 1-1e-9)\n        best_radii = compute_max_radii_relax(best_centers)\n\n    sum_radii = float(np.sum(best_radii))\n    return best_centers, best_radii, sum_radii\n\ndef compute_max_radii_relax(centers, max_iter=800, tol=1e-7):\n    \"\"\"\n    Gauss-Seidel projection: for fixed centers compute maximal feasible radii.\n    r_i := min(dist_to_wall_i, min_j (dist_ij - r_j))\n    \"\"\"\n    n = centers.shape[0]\n    border = np.minimum.reduce([centers[:,0], centers[:,1], 1-centers[:,0], 1-centers[:,1]])\n    r = border.copy()\n    # precompute distances\n    diff = centers[:,None,:] - centers[None,:,:]\n    D = np.sqrt(np.maximum(np.sum(diff*diff,axis=2), 1e-12))\n    for it in range(max_iter):\n        maxc = 0.0\n        for i in range(n):\n            # compute candidate bounds dist_ij - r_j\n            cand = D[i] - r\n            cand[i] = border[i]  # ignore self\n            # only non-negative allowed\n            allowed = np.minimum(border[i], np.min(np.maximum(cand, 0.0)))\n            change = abs(r[i] - allowed)\n            if change > 0:\n                r[i] = allowed\n                if change > maxc: maxc = change\n        if maxc < tol:\n            break\n    return np.maximum(0.0, r)\n\n# EVOLVE-BLOCK-END\n\n# fixed runner\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(6,6))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect('equal')\n    for c,r in zip(centers, radii):\n        ax.add_patch(Circle(c, r, alpha=0.5, edgecolor='k'))\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, s = run_packing()\n    print(f\"Sum of radii: {s:.6f}\")\n    # visualize(centers, radii)\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.2660), Performs well on target_ratio (0.8600), Performs well on combined_score (0.8600), Performs well on eval_time (0.1070)\n\n### Program 2 (Score: 0.8223)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Improved constructor-based circle packing for n=26 circles.\n\nThis uses a hexagonal-like layered layout (rows with alternating offsets)\nto pack 26 circles in a unit square with equal radii chosen to be as large\nas possible given the row with the maximum count. This yields a much larger\nsum of radii than the original simple concentric-ring layout.\n\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square.\n\n    Returns:\n        Tuple of (centers, radii, sum_of_radii)\n        centers: np.array of shape (26, 2) with (x, y) coordinates\n        radii: np.array of shape (26) with radius of each circle\n        sum_of_radii: Sum of all radii\n    \"\"\"\n    # Row layout chosen to sum to 26: 6 + 5 + 6 + 5 + 4 = 26\n    rows = [6, 5, 6, 5, 4]\n    n = sum(rows)\n    assert n == 26\n\n    # Spacing factors for hexagonal-style packing\n    sqrt3 = np.sqrt(3.0)\n    max_in_row = max(rows)\n\n    # Horizontal constraint: 2 * r * max_in_row <= 1  -> r <= 1/(2*max_in_row)\n    r_horiz = 1.0 / (2.0 * max_in_row)\n\n    # Vertical constraint for rows: last_center_y <= 1 - r\n    # if y_k = r + k * (sqrt3 * r) for k=0..(R-1), require r + (R-1)*sqrt3*r <= 1 - r\n    # -> r * ((R-1)*sqrt3 + 2) <= 1\n    R = len(rows)\n    r_vert = 1.0 / (((R - 1) * sqrt3) + 2.0)\n\n    # Choose the limiting radius (horizontal typically limits for our layout)\n    r = min(r_horiz, r_vert)\n\n    centers = []\n    # Build rows from bottom (k=0) to top (k=R-1)\n    for k, count in enumerate(rows):\n        # Alternate offset every other row (hex packing)\n        offset = (k % 2) == 1\n        if not offset:\n            x_start = r  # centers: r, r+2r, r+4r, ...\n        else:\n            x_start = 2.0 * r  # shifted by r: 2r, 4r, ...\n        # horizontal spacing 2r\n        xs = [x_start + j * 2.0 * r for j in range(count)]\n        y = r + k * (sqrt3 * r)\n        for x in xs:\n            centers.append([x, y])\n\n    centers = np.array(centers, dtype=float)\n    # Safety: ensure count and containment\n    assert centers.shape == (n, 2)\n\n    # Use equal radii (designed to be non-overlapping inside the unit square)\n    radii = np.full(n, r, dtype=float)\n\n    # Validate and (if numeric tiny issues) correct radii so no overlaps and inside bounds\n    # Compute minimal distance to borders and to neighbors, then clamp to those maxima.\n    # This is a conservative correction; for a correctly computed r it leaves values unchanged.\n    # Distance to border\n    border_dists = np.minimum.reduce([centers[:, 0], centers[:, 1], 1.0 - centers[:, 0], 1.0 - centers[:, 1]])\n    max_by_border = border_dists.copy()\n\n    # Minimal half pairwise distances\n    pairwise = np.sqrt(((centers[:, None, :] - centers[None, :, :]) ** 2).sum(axis=2))\n    # set diagonal to large so min ignores zero\n    np.fill_diagonal(pairwise, np.inf)\n    min_half_dist = 0.5 * pairwise.min(axis=1)\n\n    # Final allowed radius per circle\n    allowed = np.minimum(max_by_border, min_half_dist)\n    # Numerical tolerance: do not enlarge beyond our design r, only shrink if necessary\n    radii = np.minimum(radii, allowed)\n    # If any radius became extremely small (shouldn't happen), fall back to allowed values\n    radii = np.maximum(radii, 1e-12)\n\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Hex-style Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.1667), Performs well on target_ratio (0.8223), Performs well on combined_score (0.8223), Performs well on eval_time (0.0541)\n\n### Program 3 (Score: 0.8223)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for 26 circles in the unit square using a compact\nhexagonal-like layered layout with analytically chosen spacing and a final\nper-circle clamping to the true local maxima (border and half pairwise distance).\nThis explicit constructor aims to maximize the sum of radii for n=26.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    # row layout sums to 26\n    rows = [6, 5, 6, 5, 4]\n    n = sum(rows)\n    assert n == 26\n\n    sqrt3 = np.sqrt(3.0)\n    max_in_row = max(rows)\n\n    # horizontal constraint: 2*r*max_in_row <= 1  => r <= 1/(2*max_in_row)\n    r_horiz = 1.0 / (2.0 * max_in_row)\n    # vertical constraint: r + (R-1)*sqrt3*r <= 1 - r  => r * ((R-1)*sqrt3 + 2) <= 1\n    R = len(rows)\n    r_vert = 1.0 / (((R - 1) * sqrt3) + 2.0)\n\n    # Choose limiting radius (horizontal usually tighter here)\n    r_design = min(r_horiz, r_vert)\n\n    centers = []\n    # Build rows bottom-to-top with alternating offsets (hex-like)\n    for k, count in enumerate(rows):\n        offset = (k % 2) == 1\n        if not offset:\n            x_start = r_design\n        else:\n            x_start = 2.0 * r_design\n        xs = [x_start + j * 2.0 * r_design for j in range(count)]\n        y = r_design + k * (sqrt3 * r_design)\n        for x in xs:\n            # clip tiny numerical issues; actual allowed radii will be clamped later\n            centers.append([min(max(x, 0.0), 1.0), min(max(y, 0.0), 1.0)])\n\n    centers = np.array(centers, dtype=float)\n    assert centers.shape == (n, 2)\n\n    # Distance to borders\n    border = np.minimum.reduce([centers[:, 0], centers[:, 1], 1.0 - centers[:, 0], 1.0 - centers[:, 1]])\n    # Pairwise distances and half-minimums\n    d2 = ((centers[:, None, :] - centers[None, :, :]) ** 2).sum(axis=2)\n    pairwise = np.sqrt(d2)\n    np.fill_diagonal(pairwise, np.inf)\n    min_half = 0.5 * pairwise.min(axis=1)\n\n    allowed = np.minimum(border, min_half)\n    # Ensure radii do not exceed design r and are non-negative\n    radii = np.clip(np.minimum(allowed, r_design), 0.0, None)\n\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n# EVOLVE-BLOCK-END\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(6, 6))\n    ax.set_xlim(0, 1); ax.set_ylim(0, 1); ax.set_aspect(\"equal\")\n    for i, (c, r) in enumerate(zip(centers, radii)):\n        ax.add_patch(Circle(c, r, alpha=0.6, edgecolor=\"k\"))\n        ax.text(c[0], c[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # visualize(centers, radii)\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.1667), Performs well on target_ratio (0.8223), Performs well on combined_score (0.8223), Performs well on eval_time (0.0561)\n\n\n\n## Diverse Programs\n\n### Program D1 (Score: 0.8214)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Explicit constructor for packing 26 circles in the unit square.\n\nUses a hexagonal-style layered layout with rows [6,5,6,5,4].\nSelects a uniform design radius from width/height constraints and\nthen clamps each circle by its border and neighbor distances to ensure\nfeasibility (no overlaps). This yields a compact, high-sum arrangement.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    # Row pattern summing to 26\n    rows = [6, 5, 6, 5, 4]\n    n = sum(rows)\n    assert n == 26\n\n    sqrt3 = np.sqrt(3.0)\n    R = len(rows)\n    max_in_row = max(rows)\n\n    # Horizontal constraint (centers spaced 2r): 2*r*max_in_row <= 1\n    r_horiz = 1.0 / (2.0 * max_in_row)\n    # Vertical constraint: r + (R-1)*sqrt3*r <= 1 - r  => r * (((R-1)*sqrt3)+2) <= 1\n    r_vert = 1.0 / (((R - 1) * sqrt3) + 2.0)\n\n    # choose safe design radius (slightly reduced to avoid exact boundary touches)\n    r = min(r_horiz, r_vert) * 0.999\n\n    centers = []\n    for k, count in enumerate(rows):\n        # alternate offset for hex packing\n        offset = (k % 2) == 1\n        if not offset:\n            x_start = r\n        else:\n            x_start = 2.0 * r\n        xs = [x_start + j * 2.0 * r for j in range(count)]\n        y = r + k * (sqrt3 * r)\n        for x in xs:\n            centers.append([x, y])\n\n    centers = np.array(centers, dtype=float)\n    # Enforce in-unit-square numerically\n    centers = np.clip(centers, 1e-12, 1.0 - 1e-12)\n\n    # Per-circle maximum allowed by borders\n    border_dists = np.minimum.reduce([centers[:, 0], centers[:, 1], 1.0 - centers[:, 0], 1.0 - centers[:, 1]])\n\n    # Pairwise distances (set diagonal to inf)\n    pd = np.sqrt(((centers[:, None, :] - centers[None, :, :]) ** 2).sum(axis=2))\n    np.fill_diagonal(pd, np.inf)\n    # For each circle, half the minimal pairwise distance is the max radius if neighbors had zero radius\n    min_half = 0.5 * pd.min(axis=1)\n\n    allowed = np.minimum(border_dists, min_half)\n    # Do not enlarge beyond design r (we rely on the hex design), only shrink if needed\n    radii = np.minimum(r * np.ones_like(allowed), allowed)\n    radii = np.maximum(radii, 0.0)\n\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n# EVOLVE-BLOCK-END\n\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    return construct_packing()\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(6,6))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect(\"equal\")\n    for i,(c,r) in enumerate(zip(centers, radii)):\n        ax.add_patch(Circle(c, r, alpha=0.6, edgecolor='k'))\n        ax.text(c[0], c[1], str(i), ha='center', va='center', fontsize=8)\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # visualize(centers, radii)\n```\nKey features: Alternative approach to validity, Alternative approach to sum_radii\n\n### Program D2 (Score: 0.8223)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Explicit constructor for 26-circle packing in the unit square.\n\nUses a compact hex-like layered layout (rows with alternating offsets).\nRadii are set to the maximal feasible per-center given border and pairwise\nseparation: r_i = min(distance to border, 0.5 * min_j dist(center_i, center_j)).\nThis yields a simple, deterministic, high-quality feasible packing.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    # row pattern chosen to be compact: sums to 26\n    rows = [6, 5, 6, 5, 4]\n    assert sum(rows) == n\n\n    sqrt3 = np.sqrt(3.0)\n    R = len(rows)\n\n    # Compute a candidate base radius r limited by horizontal and vertical packing constraints\n    max_in_row = max(rows)\n    r_horiz = 1.0 / (2.0 * max_in_row)  # 2r * max_in_row <= 1 -> r <= 1/(2*max)\n    r_vert = 1.0 / (((R - 1) * sqrt3) + 2.0)  # r*((R-1)*sqrt3 + 2) <= 1\n    r = min(r_horiz, r_vert)\n\n    # Build centered hex-like rows\n    centers = []\n    # vertical centering: total vertical span between first and last row\n    total_v = (R - 1) * sqrt3 * r\n    y0 = 0.5 - 0.5 * total_v\n    for k, count in enumerate(rows):\n        offset = (k % 2) == 1\n        if not offset:\n            x_start = 0.5 - (count - 1) * r\n            # positions: x_start + j * 2r, j=0..count-1\n            xs = x_start + np.arange(count) * (2.0 * r)\n        else:\n            # offset rows are shifted by r, and to center such row use start = 0.5 - count*r\n            x_start = 0.5 - count * r\n            xs = x_start + r + np.arange(count) * (2.0 * r)\n        y = y0 + k * (sqrt3 * r)\n        for x in xs:\n            centers.append((float(x), float(y)))\n\n    centers = np.array(centers, dtype=float)\n    # Safety: clip tiny numerical excursions\n    centers = np.clip(centers, 1e-12, 1.0 - 1e-12)\n    assert centers.shape == (n, 2)\n\n    # Compute maximal feasible per-circle radii (conservative but feasible)\n    # border limits\n    border = np.minimum.reduce([centers[:,0], centers[:,1], 1.0 - centers[:,0], 1.0 - centers[:,1]])\n    # pairwise distances\n    dif = centers[:, None, :] - centers[None, :, :]\n    D = np.sqrt(np.maximum(1e-20, np.sum(dif**2, axis=2)))\n    np.fill_diagonal(D, np.inf)\n    min_half = 0.5 * D.min(axis=1)\n    allowed = np.minimum(border, min_half)\n    # do not exceed our base r (layout assumed r is safe); but allow slightly larger where possible\n    # however to keep packing stable, cap by max(allowed, tiny)\n    radii = np.maximum(allowed, 0.0)\n\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n# EVOLVE-BLOCK-END\n\n# fixed interface\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\nif __name__ == \"__main__\":\n    centers, radii, s = run_packing()\n    print(f\"Sum of radii: {s:.6f}\")\n```\nKey features: Alternative approach to validity, Alternative approach to sum_radii\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 0.4777, Type: Experimental)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (improved)\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct an explicit arrangement of 26 circles in a unit square.\n    Uses a centered hexagonal-like layering with row counts [6,5,4,5,6].\n    We search a small range of horizontal spacings to maximize the sum of radii.\n    Returns:\n        centers: np.array (26,2)\n        radii: np.array (26,)\n        sum_of_radii: float\n    \"\"\"\n    n = 26\n    row_counts = [6, 5, 4, 5, 6]  # symmetric pattern totaling 26\n\n    def build_centers(s):\n        # horizontal spacing s, vertical spacing for hex packing\n        h = s * np.sqrt(3) / 2.0\n        rows = len(row_counts)\n        total_h = (rows - 1) * h\n        y0 = 0.5 - total_h / 2.0\n        centers = []\n        for r, cnt in enumerate(row_counts):\n            y = y0 + r * h\n            width = (cnt - 1) * s\n            x0 = 0.5 - width / 2.0\n            # alternate offset to emulate hex packing\n            offset = (s / 2.0) if (r % 2 == 1) else 0.0\n            for k in range(cnt):\n                x = x0 + offset + k * s\n                centers.append([x, y])\n        centers = np.array(centers)\n        return centers\n\n    def compute_max_radii(centers, tol=1e-6, max_iters=2000):\n        m = centers.shape[0]\n        # start with distance to borders\n        radii = np.minimum.reduce([centers[:, 0], centers[:, 1],\n                                   1.0 - centers[:, 0], 1.0 - centers[:, 1]])\n        # small positive to avoid zeros\n        radii = np.maximum(radii, 1e-12)\n        # Iteratively enforce pairwise non-overlap by proportional scaling\n        for _ in range(max_iters):\n            max_change = 0.0\n            for i in range(m):\n                for j in range(i + 1, m):\n                    dx = centers[i, 0] - centers[j, 0]\n                    dy = centers[i, 1] - centers[j, 1]\n                    dist = np.hypot(dx, dy)\n                    if dist <= 0:\n                        # identical centers; collapse both very small\n                        desired = 0.5 * min(radii[i], radii[j])\n                        change = abs(radii[i] - desired) + abs(radii[j] - desired)\n                        radii[i] = radii[j] = desired\n                        max_change = max(max_change, change)\n                        continue\n                    sumr = radii[i] + radii[j]\n                    if sumr > dist:\n                        # scale both proportionally to preserve relative sizes\n                        scale = dist / sumr\n                        new_ri = radii[i] * scale\n                        new_rj = radii[j] * scale\n                        max_change = max(max_change, abs(new_ri - radii[i]), abs(new_rj - radii[j]))\n                        radii[i], radii[j] = new_ri, new_rj\n            # enforce border limits (in case numeric drift)\n            border_limits = np.minimum.reduce([centers[:, 0], centers[:, 1],\n                                              1.0 - centers[:, 0], 1.0 - centers[:, 1]])\n            # If any radius exceeds border, clamp it and continue iterating\n            over = radii - border_limits\n            if np.any(over > 0):\n                max_change = max(max_change, float(np.max(over)))\n                radii = np.minimum(radii, border_limits)\n            if max_change < tol:\n                break\n        # ensure non-negative\n        radii = np.clip(radii, 0.0, None)\n        return radii\n\n    # Search a small range of horizontal spacings s to maximize sum of radii\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n    # reasonable s range so pattern fits in the unit square: try from 0.08 to 0.20\n    for s in np.linspace(0.08, 0.20, 25):\n        centers = build_centers(s)\n        # discard if centers go outside [0,1] by too much\n        if (centers.min() < -1e-6) or (centers.max() > 1.0 + 1e-6):\n            continue\n        radii = compute_max_radii(centers)\n        total = float(np.sum(radii))\n        if total > best_sum:\n            best_sum = total\n            best_centers = centers.copy()\n            best_radii = radii.copy()\n\n    # Fallback: if search failed, use a default spacing\n    if best_centers is None:\n        best_centers = build_centers(0.12)\n        best_radii = compute_max_radii(best_centers)\n\n    # Final slight nudge: small local optimize scale of all centers toward center\n    # to try to increase sum (edge-effect adjustment)\n    cx, cy = 0.5, 0.5\n    for alpha in [0.0, 0.02, -0.02]:\n        moved = best_centers * (1.0 - abs(alpha)) + alpha * np.array([cx, cy])\n        # keep inside\n        moved = np.clip(moved, 0.0, 1.0)\n        radii = compute_max_radii(moved)\n        total = float(np.sum(radii))\n        if total > best_sum:\n            best_sum = total\n            best_centers = moved.copy()\n            best_radii = radii.copy()\n\n    sum_radii = float(np.sum(best_radii))\n    return best_centers, best_radii, sum_radii\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nUnique approach: Modification: Full rewrite, Excellent validity (1.000), Excellent sum_radii (1.259)\n\n### Inspiration 2 (Score: 0.7193, Type: Alternative)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (improved)\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    \"\"\"\n    Construct an explicit arrangement of 26 circle centers in the unit square\n    using a compact hex-like layered layout, then compute maximal non-overlapping\n    radii for those fixed centers by an iterative constraint propagation.\n    Returns (centers, radii, sum_of_radii).\n    \"\"\"\n    n = 26\n    # Row counts chosen to give 26 points in a compact hex-like layout\n    row_counts = [6, 5, 5, 5, 5]  # sum = 26\n    rows = len(row_counts)\n\n    # Hex lattice spacing guess (fits inside unit square with small margins)\n    margin = 0.01\n    # Choose horizontal spacing s constrained by the longest row width\n    max_row = max(row_counts)\n    s = min((1.0 - 2 * margin) / (max_row - 1), 0.19)  # keep a safe spacing\n    h = s * np.sqrt(3) / 2.0  # vertical spacing for hex pattern\n\n    # Vertical centering\n    total_height = (rows - 1) * h\n    y0 = (1.0 - total_height) / 2.0\n\n    centers = np.zeros((n, 2))\n    idx = 0\n    for r in range(rows):\n        k = row_counts[r]\n        # Alternate offset to create hex-like staggering\n        offset = (s / 2.0) if (r % 2 == 1) else 0.0\n        row_width = (k - 1) * s\n        x_start = 0.5 - row_width / 2.0 + offset\n        y = y0 + r * h\n        for j in range(k):\n            x = x_start + j * s\n            # Clip into interior slightly to avoid exact border\n            centers[idx, 0] = np.clip(x, margin, 1.0 - margin)\n            centers[idx, 1] = np.clip(y, margin, 1.0 - margin)\n            idx += 1\n\n    # Safety: ensure exactly 26\n    centers = centers[:n]\n\n    radii = compute_max_radii(centers)\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\ndef compute_max_radii(centers, tol=1e-7, max_iter=5000):\n    \"\"\"\n    Given fixed centers, compute maximal radii so circles stay inside [0,1]^2\n    and do not overlap. Procedure: start with border-limited radii and iteratively\n    enforce pairwise non-overlap by shrinking the larger circle when needed.\n    This is a monotone decreasing scheme that typically converges quickly.\n    \"\"\"\n    n = centers.shape[0]\n    # initial radii limited by distance to borders\n    radii = np.minimum.reduce([centers[:,0], centers[:,1], 1-centers[:,0], 1-centers[:,1]])\n    radii = np.maximum(radii, 0.0)\n\n    # Precompute pairwise center distances\n    diff = centers[:, None, :] - centers[None, :, :]\n    dists = np.sqrt(np.sum(diff**2, axis=2))\n    # Avoid self distances = 0 on diagonal\n    np.fill_diagonal(dists, np.inf)\n\n    for it in range(max_iter):\n        max_change = 0.0\n        # enforce pairwise constraints\n        for i in range(n):\n            for j in range(i+1, n):\n                dij = dists[i, j]\n                if dij == 0.0 or np.isinf(dij):\n                    continue\n                ri = radii[i]\n                rj = radii[j]\n                total = ri + rj\n                if total > dij:\n                    # amount to reduce\n                    overlap = total - dij\n                    # reduce the larger radius by the overlap (never increase)\n                    if ri >= rj:\n                        new_ri = max(0.0, ri - overlap)\n                        change = ri - new_ri\n                        if change > 0:\n                            radii[i] = new_ri\n                            max_change = max(max_change, change)\n                    else:\n                        new_rj = max(0.0, rj - overlap)\n                        change = rj - new_rj\n                        if change > 0:\n                            radii[j] = new_rj\n                            max_change = max(max_change, change)\n        # re-enforce border limits (they may still be the binding constraint)\n        border_limits = np.minimum.reduce([centers[:,0], centers[:,1], 1-centers[:,0], 1-centers[:,1]])\n        # ensure we don't increase any radius above border limits\n        clipped = np.minimum(radii, border_limits)\n        max_change = max(max_change, float(np.max(np.abs(radii - clipped))))\n        radii = clipped\n\n        if max_change < tol:\n            break\n\n    return radii\n\n# EVOLVE-BLOCK-END\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor='k')\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nUnique approach: Modification: Full rewrite, Excellent validity (1.000), Excellent sum_radii (1.895)\n\n### Inspiration 3 (Score: 0.6928, Type: Alternative)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (hex-like rows + coordinate-wise LP relaxation)\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square.\n    Returns: (centers, radii, sum_of_radii)\n    \"\"\"\n    # Layout: 5 rows with counts [6,5,5,5,5] (total 26), arranged like a hexagonal strip\n    counts = [6, 5, 5, 5, 5]\n    n = sum(counts)\n    assert n == 26\n\n    margin = 0.02  # keep centers away from exact border so wall distance is positive\n    max_count = max(counts)\n    avail_w = 1.0 - 2.0 * margin\n\n    # horizontal spacing chosen so max_count fits comfortably\n    spacing_x = avail_w / max_count\n    # hex vertical spacing for closest-packing-like rows\n    spacing_y = spacing_x * (np.sqrt(3) / 2.0)\n\n    # compute y positions centered vertically\n    nrows = len(counts)\n    total_h = spacing_y * (nrows - 1)\n    y0 = 0.5 - total_h / 2.0\n\n    centers = []\n    for row_idx, cnt in enumerate(counts):\n        y = y0 + row_idx * spacing_y\n        # center row horizontally\n        row_width = spacing_x * (cnt - 1) if cnt > 1 else 0.0\n        x_start = 0.5 - row_width / 2.0\n        # offset alternate rows for hex-like packing (shift by half spacing when row has fewer centers)\n        # choose offset so rows with cnt < max_count get half-shift to interlock\n        if cnt < max_count:\n            offset = spacing_x / 2.0\n        else:\n            offset = 0.0\n        for k in range(cnt):\n            x = x_start + offset + k * spacing_x\n            # clamp to ensure strictly inside unit square interior\n            x = float(np.clip(x, margin + 1e-6, 1.0 - margin - 1e-6))\n            y = float(np.clip(y, margin + 1e-6, 1.0 - margin - 1e-6))\n            centers.append([x, y])\n\n    centers = np.array(centers, dtype=float)\n    # Safety: if we accidentally produced different count, pad or trim (shouldn't happen)\n    if centers.shape[0] > 26:\n        centers = centers[:26].copy()\n    elif centers.shape[0] < 26:\n        # add a small cluster near a corner if needed (unlikely)\n        extra = 26 - centers.shape[0]\n        for i in range(extra):\n            centers = np.vstack([centers, [margin + 0.02*(i+1), margin + 0.02*(i+1)]])\n\n    radii = compute_max_radii(centers)\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\ndef compute_max_radii(centers, tol=1e-7, max_iter=2000):\n    \"\"\"\n    Compute near-optimal radii by coordinate-wise relaxation to satisfy:\n      r_i >= 0, r_i <= dist_to_wall_i, r_i + r_j <= dist_ij for all pairs.\n    The update r_i := min(dist_to_wall_i, min_j (max(0, dist_ij - r_j))) is a Gauss-Seidel\n    style projection that converges to a maximal feasible point for these constraints.\n    \"\"\"\n    n = centers.shape[0]\n    # distance to walls\n    xs = centers[:, 0]\n    ys = centers[:, 1]\n    wall = np.minimum.reduce([xs, ys, 1.0 - xs, 1.0 - ys])\n    wall = np.maximum(0.0, wall)\n\n    # precompute pairwise distances\n    dif = centers[:, None, :] - centers[None, :, :]  # (n,n,2)\n    dists = np.sqrt(np.sum(dif * dif, axis=2))\n    # avoid zero distances on diagonal\n    # dists[i,i] = 0\n\n    # initialize radii to wall distances (upper bounds)\n    r = wall.copy()\n\n    # iterative coordinate update\n    for it in range(max_iter):\n        r_prev = r.copy()\n        for i in range(n):\n            # for each j != i, r_i <= max(0, d_ij - r_j)\n            # take min over j to enforce all pair constraints, and also wall bound\n            # skip j==i where d_ii==0\n            bounds = wall[i]\n            # vectorized computation for speed\n            # dists[i] - r gives candidate upper bounds; ignore self by setting to large positive\n            candidates = dists[i] - r\n            candidates[i] = bounds  # won't be chosen smaller\n            # ensure non-negative\n            candidates = np.maximum(0.0, candidates)\n            # r_i should be <= min(candidates)\n            bounds = min(bounds, np.min(candidates))\n            r[i] = bounds\n        # convergence check\n        if np.max(np.abs(r - r_prev)) < tol:\n            break\n\n    # final safeguard: clip non-negatives\n    r = np.maximum(0.0, r)\n    return r\n\n# EVOLVE-BLOCK-END\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nUnique approach: Modification: Full rewrite, Excellent validity (1.000), Excellent sum_radii (1.826)\n\n# Current Program\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (improved explicit constructor)\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square\n    using a compact hexagonal-ish layered layout (no iterative search).\n    Returns:\n        centers: np.array (26,2)\n        radii: np.array (26,)\n        sum_of_radii: float\n    \"\"\"\n    n = 26\n\n    # Row layout chosen to balance width/height: six rows with at most 5 columns\n    rows = [5, 5, 4, 4, 4, 4]  # sums to 26\n    num_rows = len(rows)\n    max_cols = max(rows)\n\n    # hexagonal geometry constants\n    sin60 = np.sqrt(3) / 2.0\n\n    # compute a spacing s that fits both width and height constraints\n    s_max_width = 1.0 / max_cols  # conservative: max cols * s <= 1\n    s_max_height = 1.0 / ((num_rows - 1) * sin60 + 1.0)  # (rows-1)*dy + 2*r where r = s/2 -> derived\n    s = 0.99 * min(s_max_width, s_max_height)  # slight shrink to avoid boundary touch\n    dy = sin60 * s\n    r_guess = s / 2.0\n\n    centers = []\n    # place rows from bottom to top\n    for ri in range(num_rows):\n        k = rows[ri]\n        # center rows horizontally with respect to max_cols grid; left offset shifts shorter rows to center\n        left_offset = r_guess + ((max_cols - k) * 0.5) * s\n        y = r_guess + ri * dy\n        xs = left_offset + np.arange(k) * s\n        for x in xs:\n            centers.append([x, y])\n\n    centers = np.array(centers, dtype=float)\n    # ensure inside (numerical safety)\n    centers = np.clip(centers, 1e-6, 1 - 1e-6)\n\n    radii = compute_max_radii(centers)\n    sum_radii = float(np.sum(radii))\n\n    # If something went odd, pad/truncate (defensive)\n    if centers.shape[0] != n:\n        centers = centers[:n]\n        radii = radii[:n]\n        sum_radii = float(np.sum(radii))\n\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers, max_iters=500, tol=1e-8):\n    \"\"\"\n    Iterative relaxation to find feasible (nearly maximal) radii:\n    - start with border-limited radii\n    - repeatedly enforce pairwise non-overlap by proportional shrinking\n    This converges to a feasible vector; it's not a global optimizer but effective here.\n    \"\"\"\n    n = centers.shape[0]\n    # initial radii: limited by border\n    radii = np.zeros(n, dtype=float)\n    for i in range(n):\n        x, y = centers[i]\n        radii[i] = min(x, y, 1 - x, 1 - y)\n\n    # iterative pairwise relaxation\n    for it in range(max_iters):\n        max_change = 0.0\n\n        # enforce pairwise non-overlap\n        for i in range(n):\n            xi = centers[i]\n            ri = radii[i]\n            for j in range(i + 1, n):\n                xj = centers[j]\n                rj = radii[j]\n                d = np.hypot(*(xi - xj))\n                if d <= 0:\n                    # coincide centers (shouldn't happen) -> shrink both\n                    target = 0.0\n                    if ri + rj > 0:\n                        scale = 0.5\n                        new_ri = ri * scale\n                        new_rj = rj * scale\n                    else:\n                        new_ri, new_rj = ri, rj\n                else:\n                    # small epsilon to avoid exact-touch numerical issues\n                    eps = 1e-10\n                    overlap = (ri + rj) - (d - eps)\n                    if overlap > 0:\n                        # reduce proportionally to current sizes (preserve relative sizes)\n                        if ri + rj > 0:\n                            scale = max(0.0, (d - eps) / (ri + rj))\n                            new_ri = ri * scale\n                            new_rj = rj * scale\n                        else:\n                            new_ri, new_rj = ri, rj\n                    else:\n                        new_ri, new_rj = ri, rj\n\n                # apply updates and track maximum change\n                if new_ri != ri:\n                    max_change = max(max_change, abs(new_ri - ri))\n                    radii[i] = new_ri\n                if new_rj != rj:\n                    max_change = max(max_change, abs(new_rj - rj))\n                    radii[j] = new_rj\n\n        # re-apply border constraints (in case shrinks allowed border increase; we must ensure border is still enforced)\n        for i in range(n):\n            x, y = centers[i]\n            border_limit = min(x, y, 1 - x, 1 - y)\n            if radii[i] > border_limit:\n                max_change = max(max_change, abs(radii[i] - border_limit))\n                radii[i] = border_limit\n\n        if max_change < tol:\n            break\n\n    return radii\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.5)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\n\n# Task\nRewrite the program to improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: complexity, diversity\nDifferent solutions with similar fitness but different features are valuable.\nProvide the complete new program code.\n\nIMPORTANT: Make sure your rewritten program maintains the same inputs and outputs\nas the original program, but with improved internal implementation.\n\n```python\n# Your rewritten program here\n```", "responses": ["```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for 26 circles in the unit square.\nBuilds compact hex-like rows (pattern [6,5,6,5,4]) and searches a small\nfamily of spacings, small rotations and mild radial compressions to fight\nedge effects. For fixed centers we compute maximal feasible radii by a\nGauss-Seidel style projection (fast, convergent).\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    rows = [6, 5, 6, 5, 4]  # compact hex-like strip summing to 26\n    sqrt3 = np.sqrt(3.0)\n    sin60 = sqrt3 / 2.0\n\n    # candidate parameters (small deterministic grid)\n    s_vals = np.linspace(0.11, 0.185, 13)   # horizontal spacing between centers\n    thetas = [0.0, np.pi/36, -np.pi/36]     # tiny rotations\n    alphas = [1.00, 0.96, 0.92]             # compress toward center to reduce edge loss\n\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n    center = np.array([0.5, 0.5])\n\n    for s in s_vals:\n        dy = sin60 * s\n        # compute vertical span and center y0 so rows sit roughly centered\n        R = len(rows)\n        total_h = (R - 1) * dy\n        y0 = 0.5 - total_h / 2.0\n        for theta in thetas:\n            c = np.cos(theta); si = np.sin(theta)\n            Rmat = np.array([[c, -si], [si, c]])\n            for alpha in alphas:\n                centers = []\n                for ri, cnt in enumerate(rows):\n                    # compute x positions centered\n                    row_width = (cnt - 1) * s\n                    x0 = 0.5 - row_width / 2.0\n                    # alternate offset to mimic hex staggering\n                    offset = (s/2.0) if (ri % 2 == 1) else 0.0\n                    xs = x0 + offset + np.arange(cnt) * s\n                    y = y0 + ri * dy\n                    for x in xs:\n                        centers.append([x, y])\n                C = np.array(centers)\n                # compress slightly toward square center, then rotate about center\n                C = center + (C - center) * alpha\n                C = (C - center).dot(Rmat.T) + center\n                # clip tiny numerical drift\n                C = np.clip(C, 1e-9, 1.0 - 1e-9)\n                if C.shape[0] != n:\n                    continue\n                radii = compute_max_radii_relax(C, max_iter=1200, tol=1e-8)\n                ssum = float(np.sum(radii))\n                if ssum > best_sum:\n                    best_sum = ssum\n                    best_centers = C.copy()\n                    best_radii = radii.copy()\n\n    # fallback pattern if search failed\n    if best_centers is None:\n        best_centers = np.array([[0.5,0.5]])\n        best_radii = np.array([0.1])\n\n    return best_centers, best_radii, float(np.sum(best_radii))\n\ndef compute_max_radii_relax(centers, max_iter=800, tol=1e-8):\n    \"\"\"\n    For fixed centers compute maximal feasible radii solving:\n      r_i <= dist_to_wall_i, r_i + r_j <= dist_ij\n    via Gauss-Seidel coordinate projections:\n      r_i := min(dist_wall_i, min_j max(0, dist_ij - r_j))\n    \"\"\"\n    n = centers.shape[0]\n    xs = centers[:,0]; ys = centers[:,1]\n    border = np.minimum.reduce([xs, ys, 1.0 - xs, 1.0 - ys])\n    # pairwise distances\n    diff = centers[:, None, :] - centers[None, :, :]\n    D = np.sqrt(np.maximum((diff**2).sum(axis=2), 1e-16))\n    np.fill_diagonal(D, np.inf)\n    # initialize radii by border distances (safe upper bound)\n    r = border.copy()\n    for it in range(max_iter):\n        max_change = 0.0\n        for i in range(n):\n            # candidate bounds from neighbors: D[i] - r (vector), but cannot be negative\n            cand = D[i] - r\n            cand[i] = border[i]  # ignore self\n            # allowed radius is min(border[i], min_j max(0, D_ij - r_j))\n            allowed = min(border[i], float(np.min(np.maximum(cand, 0.0))))\n            change = abs(r[i] - allowed)\n            if change > 0:\n                r[i] = allowed\n                if change > max_change:\n                    max_change = change\n        if max_change < tol:\n            break\n    return np.maximum(0.0, r)\n\n# EVOLVE-BLOCK-END\n\n# fixed runner\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(6,6))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect('equal')\n    for c,r in zip(centers, radii):\n        ax.add_patch(Circle(c, r, alpha=0.5, edgecolor='k'))\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, s = run_packing()\n    print(f\"Sum of radii: {s:.6f}\")\n    # visualize(centers, radii)\n```"]}}, "artifacts_json": "{\"stdout\": \"Running in subprocess, Python version: 3.12.1 (v3.12.1:2305ca5144, Dec  7 2023, 17:23:38) [Clang 13.0.0 (clang-1300.0.29.30)]\\nProgram path: /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmpc4c6abx1.py\\nCalling run_packing()...\\nrun_packing() returned successfully: sum_radii = 1.9105148139475039\\nResults saved to /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmpf1yi2p1p.py.results\\n\", \"stderr\": \"\"}", "artifact_dir": null}