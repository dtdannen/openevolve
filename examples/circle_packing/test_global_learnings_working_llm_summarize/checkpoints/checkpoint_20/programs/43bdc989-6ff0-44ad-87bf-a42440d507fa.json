{"id": "43bdc989-6ff0-44ad-87bf-a42440d507fa", "code": "# EVOLVE-BLOCK-START\n\"\"\"Explicit constructor for 26-circle packing in the unit square.\n\nUses a compact hex-like layered layout (rows with alternating offsets).\nRadii are set to the maximal feasible per-center given border and pairwise\nseparation: r_i = min(distance to border, 0.5 * min_j dist(center_i, center_j)).\nThis yields a simple, deterministic, high-quality feasible packing.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    # row pattern chosen to be compact: sums to 26\n    rows = [6, 5, 6, 5, 4]\n    assert sum(rows) == n\n\n    sqrt3 = np.sqrt(3.0)\n    R = len(rows)\n\n    # Compute a candidate base radius r limited by horizontal and vertical packing constraints\n    max_in_row = max(rows)\n    r_horiz = 1.0 / (2.0 * max_in_row)  # 2r * max_in_row <= 1 -> r <= 1/(2*max)\n    r_vert = 1.0 / (((R - 1) * sqrt3) + 2.0)  # r*((R-1)*sqrt3 + 2) <= 1\n    r = min(r_horiz, r_vert)\n\n    # Build centered hex-like rows\n    centers = []\n    # vertical centering: total vertical span between first and last row\n    total_v = (R - 1) * sqrt3 * r\n    y0 = 0.5 - 0.5 * total_v\n    for k, count in enumerate(rows):\n        offset = (k % 2) == 1\n        if not offset:\n            x_start = 0.5 - (count - 1) * r\n            # positions: x_start + j * 2r, j=0..count-1\n            xs = x_start + np.arange(count) * (2.0 * r)\n        else:\n            # offset rows are shifted by r, and to center such row use start = 0.5 - count*r\n            x_start = 0.5 - count * r\n            xs = x_start + r + np.arange(count) * (2.0 * r)\n        y = y0 + k * (sqrt3 * r)\n        for x in xs:\n            centers.append((float(x), float(y)))\n\n    centers = np.array(centers, dtype=float)\n    # Safety: clip tiny numerical excursions\n    centers = np.clip(centers, 1e-12, 1.0 - 1e-12)\n    assert centers.shape == (n, 2)\n\n    # Compute maximal feasible per-circle radii (conservative but feasible)\n    # border limits\n    border = np.minimum.reduce([centers[:,0], centers[:,1], 1.0 - centers[:,0], 1.0 - centers[:,1]])\n    # pairwise distances\n    dif = centers[:, None, :] - centers[None, :, :]\n    D = np.sqrt(np.maximum(1e-20, np.sum(dif**2, axis=2)))\n    np.fill_diagonal(D, np.inf)\n    min_half = 0.5 * D.min(axis=1)\n    allowed = np.minimum(border, min_half)\n    # do not exceed our base r (layout assumed r is safe); but allow slightly larger where possible\n    # however to keep packing stable, cap by max(allowed, tiny)\n    radii = np.maximum(allowed, 0.0)\n\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n# EVOLVE-BLOCK-END\n\n# fixed interface\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\nif __name__ == \"__main__\":\n    centers, radii, s = run_packing()\n    print(f\"Sum of radii: {s:.6f}\")", "language": "python", "parent_id": "80c5a4ed-6f28-42e3-ab9c-8a747fcdb285", "generation": 2, "timestamp": 1759958250.873486, "iteration_found": 18, "metrics": {"validity": 1.0, "sum_radii": 2.166666666666666, "target_ratio": 0.8222643896268184, "combined_score": 0.8222643896268184, "eval_time": 0.06087493896484375}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Full rewrite", "parent_metrics": {"validity": 1.0, "sum_radii": 1.2550000000000001, "target_ratio": 0.4762808349146111, "combined_score": 0.4762808349146111}, "island": 0}, "prompts": {"full_rewrite_user": {"system": "You are an expert mathematician specializing in circle packing problems and computational geometry. Your task is to improve a constructor function that directly produces a specific arrangement of 26 circles in a unit square, maximizing the sum of their radii. The AlphaEvolve paper achieved a sum of 2.635 for n=26.\n\nKey geometric insights:\n- Circle packings often follow hexagonal patterns in the densest regions\n- Maximum density for infinite circle packing is pi/(2*sqrt(3)) \u2248 0.9069\n- Edge effects make square container packing harder than infinite packing\n- Circles can be placed in layers or shells when confined to a square\n- Similar radius circles often form regular patterns, while varied radii allow better space utilization\n- Perfect symmetry may not yield the optimal packing due to edge effects\n\nFocus on designing an explicit constructor that places each circle in a specific position, rather than an iterative search algorithm.\n", "user": "# Current Program Information\n- Fitness: 0.4763\n- Feature coordinates: No feature coordinates\n- Focus areas: - Fitness declined: 0.7683 \u2192 0.4763. Consider revising recent changes.\n- Consider simplifying - code length exceeds 500 characters\n\n## Last Execution Output\n\n### stdout\n```\nRunning in subprocess, Python version: 3.12.1 (v3.12.1:2305ca5144, Dec  7 2023, 17:23:38) [Clang 13.0.0 (clang-1300.0.29.30)]\nProgram path: /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmpwijz1hj9.py\nCalling run_packing()...\nrun_packing() returned successfully: sum_radii = 1.2550000000000001\nResults saved to /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmpjbdy6f60.py.results\n\n```\n\n### stderr\n```\n\n```\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 3\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.0246, target_ratio: 0.7683, combined_score: 0.7683, eval_time: 0.0675\n- Outcome: Mixed results\n\n### Attempt 2\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.0551, target_ratio: 0.7799, combined_score: 0.7799, eval_time: 0.0573\n- Outcome: Mixed results\n\n### Attempt 1\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.1667, target_ratio: 0.8223, combined_score: 0.8223, eval_time: 0.0541\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.8223)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Improved constructor-based circle packing for n=26 circles.\n\nThis uses a hexagonal-like layered layout (rows with alternating offsets)\nto pack 26 circles in a unit square with equal radii chosen to be as large\nas possible given the row with the maximum count. This yields a much larger\nsum of radii than the original simple concentric-ring layout.\n\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square.\n\n    Returns:\n        Tuple of (centers, radii, sum_of_radii)\n        centers: np.array of shape (26, 2) with (x, y) coordinates\n        radii: np.array of shape (26) with radius of each circle\n        sum_of_radii: Sum of all radii\n    \"\"\"\n    # Row layout chosen to sum to 26: 6 + 5 + 6 + 5 + 4 = 26\n    rows = [6, 5, 6, 5, 4]\n    n = sum(rows)\n    assert n == 26\n\n    # Spacing factors for hexagonal-style packing\n    sqrt3 = np.sqrt(3.0)\n    max_in_row = max(rows)\n\n    # Horizontal constraint: 2 * r * max_in_row <= 1  -> r <= 1/(2*max_in_row)\n    r_horiz = 1.0 / (2.0 * max_in_row)\n\n    # Vertical constraint for rows: last_center_y <= 1 - r\n    # if y_k = r + k * (sqrt3 * r) for k=0..(R-1), require r + (R-1)*sqrt3*r <= 1 - r\n    # -> r * ((R-1)*sqrt3 + 2) <= 1\n    R = len(rows)\n    r_vert = 1.0 / (((R - 1) * sqrt3) + 2.0)\n\n    # Choose the limiting radius (horizontal typically limits for our layout)\n    r = min(r_horiz, r_vert)\n\n    centers = []\n    # Build rows from bottom (k=0) to top (k=R-1)\n    for k, count in enumerate(rows):\n        # Alternate offset every other row (hex packing)\n        offset = (k % 2) == 1\n        if not offset:\n            x_start = r  # centers: r, r+2r, r+4r, ...\n        else:\n            x_start = 2.0 * r  # shifted by r: 2r, 4r, ...\n        # horizontal spacing 2r\n        xs = [x_start + j * 2.0 * r for j in range(count)]\n        y = r + k * (sqrt3 * r)\n        for x in xs:\n            centers.append([x, y])\n\n    centers = np.array(centers, dtype=float)\n    # Safety: ensure count and containment\n    assert centers.shape == (n, 2)\n\n    # Use equal radii (designed to be non-overlapping inside the unit square)\n    radii = np.full(n, r, dtype=float)\n\n    # Validate and (if numeric tiny issues) correct radii so no overlaps and inside bounds\n    # Compute minimal distance to borders and to neighbors, then clamp to those maxima.\n    # This is a conservative correction; for a correctly computed r it leaves values unchanged.\n    # Distance to border\n    border_dists = np.minimum.reduce([centers[:, 0], centers[:, 1], 1.0 - centers[:, 0], 1.0 - centers[:, 1]])\n    max_by_border = border_dists.copy()\n\n    # Minimal half pairwise distances\n    pairwise = np.sqrt(((centers[:, None, :] - centers[None, :, :]) ** 2).sum(axis=2))\n    # set diagonal to large so min ignores zero\n    np.fill_diagonal(pairwise, np.inf)\n    min_half_dist = 0.5 * pairwise.min(axis=1)\n\n    # Final allowed radius per circle\n    allowed = np.minimum(max_by_border, min_half_dist)\n    # Numerical tolerance: do not enlarge beyond our design r, only shrink if necessary\n    radii = np.minimum(radii, allowed)\n    # If any radius became extremely small (shouldn't happen), fall back to allowed values\n    radii = np.maximum(radii, 1e-12)\n\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Hex-style Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.1667), Performs well on target_ratio (0.8223), Performs well on combined_score (0.8223), Performs well on eval_time (0.0541)\n\n### Program 2 (Score: 0.7799)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (improved explicit constructor)\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square\n    using a compact hexagonal-ish layered layout (no iterative search).\n    Returns:\n        centers: np.array (26,2)\n        radii: np.array (26,)\n        sum_of_radii: float\n    \"\"\"\n    n = 26\n\n    # Row layout chosen to balance width/height: six rows with at most 5 columns\n    rows = [5, 5, 4, 4, 4, 4]  # sums to 26\n    num_rows = len(rows)\n    max_cols = max(rows)\n\n    # hexagonal geometry constants\n    sin60 = np.sqrt(3) / 2.0\n\n    # compute a spacing s that fits both width and height constraints\n    s_max_width = 1.0 / max_cols  # conservative: max cols * s <= 1\n    s_max_height = 1.0 / ((num_rows - 1) * sin60 + 1.0)  # (rows-1)*dy + 2*r where r = s/2 -> derived\n    s = 0.99 * min(s_max_width, s_max_height)  # slight shrink to avoid boundary touch\n    dy = sin60 * s\n    r_guess = s / 2.0\n\n    centers = []\n    # place rows from bottom to top\n    for ri in range(num_rows):\n        k = rows[ri]\n        # center rows horizontally with respect to max_cols grid; left offset shifts shorter rows to center\n        left_offset = r_guess + ((max_cols - k) * 0.5) * s\n        y = r_guess + ri * dy\n        xs = left_offset + np.arange(k) * s\n        for x in xs:\n            centers.append([x, y])\n\n    centers = np.array(centers, dtype=float)\n    # ensure inside (numerical safety)\n    centers = np.clip(centers, 1e-6, 1 - 1e-6)\n\n    radii = compute_max_radii(centers)\n    sum_radii = float(np.sum(radii))\n\n    # If something went odd, pad/truncate (defensive)\n    if centers.shape[0] != n:\n        centers = centers[:n]\n        radii = radii[:n]\n        sum_radii = float(np.sum(radii))\n\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers, max_iters=500, tol=1e-8):\n    \"\"\"\n    Iterative relaxation to find feasible (nearly maximal) radii:\n    - start with border-limited radii\n    - repeatedly enforce pairwise non-overlap by proportional shrinking\n    This converges to a feasible vector; it's not a global optimizer but effective here.\n    \"\"\"\n    n = centers.shape[0]\n    # initial radii: limited by border\n    radii = np.zeros(n, dtype=float)\n    for i in range(n):\n        x, y = centers[i]\n        radii[i] = min(x, y, 1 - x, 1 - y)\n\n    # iterative pairwise relaxation\n    for it in range(max_iters):\n        max_change = 0.0\n\n        # enforce pairwise non-overlap\n        for i in range(n):\n            xi = centers[i]\n            ri = radii[i]\n            for j in range(i + 1, n):\n                xj = centers[j]\n                rj = radii[j]\n                d = np.hypot(*(xi - xj))\n                if d <= 0:\n                    # coincide centers (shouldn't happen) -> shrink both\n                    target = 0.0\n                    if ri + rj > 0:\n                        scale = 0.5\n                        new_ri = ri * scale\n                        new_rj = rj * scale\n                    else:\n                        new_ri, new_rj = ri, rj\n                else:\n                    # small epsilon to avoid exact-touch numerical issues\n                    eps = 1e-10\n                    overlap = (ri + rj) - (d - eps)\n                    if overlap > 0:\n                        # reduce proportionally to current sizes (preserve relative sizes)\n                        if ri + rj > 0:\n                            scale = max(0.0, (d - eps) / (ri + rj))\n                            new_ri = ri * scale\n                            new_rj = rj * scale\n                        else:\n                            new_ri, new_rj = ri, rj\n                    else:\n                        new_ri, new_rj = ri, rj\n\n                # apply updates and track maximum change\n                if new_ri != ri:\n                    max_change = max(max_change, abs(new_ri - ri))\n                    radii[i] = new_ri\n                if new_rj != rj:\n                    max_change = max(max_change, abs(new_rj - rj))\n                    radii[j] = new_rj\n\n        # re-apply border constraints (in case shrinks allowed border increase; we must ensure border is still enforced)\n        for i in range(n):\n            x, y = centers[i]\n            border_limit = min(x, y, 1 - x, 1 - y)\n            if radii[i] > border_limit:\n                max_change = max(max_change, abs(radii[i] - border_limit))\n                radii[i] = border_limit\n\n        if max_change < tol:\n            break\n\n    return radii\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.5)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.0551), Performs well on target_ratio (0.7799), Performs well on combined_score (0.7799), Performs well on eval_time (0.0573)\n\n### Program 3 (Score: 0.7683)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (improved)\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square.\n    Strategy:\n    - Generate candidate hexagonal-lattice clusters (varying lattice spacing s and\n      a small inward compression alpha).\n    - For each candidate, pick the 26 lattice points closest to the square center,\n      compute maximum non-overlapping radii (iterative Gauss-Seidel relaxation),\n      and keep the candidate with the largest sum of radii.\n    Returns:\n        centers: np.array (26,2), radii: np.array (26,), sum_radii: float\n    \"\"\"\n    n = 26\n    center_sq = np.array([0.5, 0.5])\n\n    # Search parameters (small, deterministic grid)\n    s_values = np.linspace(0.11, 0.24, 14)  # hex spacing candidates\n    alpha_values = [0.85, 0.9, 0.95, 1.0]  # small inward compression to fight edge effects\n\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    # precompute integer grid range large enough to produce many candidates\n    m = 8\n    ii = np.arange(-m, m + 1)\n    jj = np.arange(-m, m + 1)\n    I, J = np.meshgrid(ii, jj)\n    IJ = np.column_stack((I.ravel(), J.ravel()))\n\n    for s in s_values:\n        # hex lattice basis\n        v1 = np.array([s, 0.0])\n        v2 = np.array([s * 0.5, s * (np.sqrt(3) / 2.0)])\n        pts = IJ.dot(np.vstack((v1, v2)).T)  # shape (Npts, 2)\n        # shift to square center\n        pts = pts + center_sq\n\n        # Keep only points inside the unit square (small tolerance)\n        inside_mask = np.all((pts >= -1e-9) & (pts <= 1 + 1e-9), axis=1)\n        valid_pts = pts[inside_mask]\n        if valid_pts.shape[0] < n:\n            # still might work after compression but skip if insufficient\n            continue\n\n        # sort by distance to center and take a cluster of 26 closest\n        dists = np.sum((valid_pts - center_sq) ** 2, axis=1)\n        order = np.argsort(dists)\n        base_cluster = valid_pts[order[: max(n + 10, n * 2)]]  # take extra for alpha perturb\n\n        for alpha in alpha_values:\n            # compress radial offsets slightly toward center to reduce edge effects\n            offsets = base_cluster - center_sq\n            comp = center_sq + offsets * alpha\n            # ensure points lie in [0,1]\n            comp = np.clip(comp, 1e-6, 1 - 1e-6)\n\n            # re-sort and pick exactly n closest\n            d2 = np.sum((comp - center_sq) ** 2, axis=1)\n            pick_idx = np.argsort(d2)[:n]\n            centers = comp[pick_idx]\n\n            # compute maximum radii for these fixed centers\n            radii = compute_max_radii_relax(centers, max_iter=1000, tol=1e-8)\n\n            sum_r = float(np.sum(radii))\n            if sum_r > best_sum:\n                best_sum = sum_r\n                best_centers = centers.copy()\n                best_radii = radii.copy()\n\n    # Fallback: if nothing found (shouldn't happen), use small regular pattern\n    if best_centers is None:\n        best_centers = np.zeros((n, 2))\n        best_centers[0] = center_sq\n        for i in range(1, 9):\n            angle = 2 * np.pi * (i - 1) / 8\n            best_centers[i] = center_sq + 0.18 * np.array([np.cos(angle), np.sin(angle)])\n        for i in range(16):\n            angle = 2 * np.pi * i / 16\n            best_centers[i + 9] = center_sq + 0.34 * np.array([np.cos(angle), np.sin(angle)])\n        best_centers = np.clip(best_centers, 1e-6, 1 - 1e-6)\n        best_radii = compute_max_radii_relax(best_centers)\n\n    sum_radii = float(np.sum(best_radii))\n    return best_centers, best_radii, sum_radii\n\n\ndef compute_max_radii_relax(centers, max_iter=500, tol=1e-7):\n    \"\"\"\n    Given fixed centers inside unit square, compute the maximum possible non-overlapping\n    radii using a simple iterative Gauss-Seidel relaxation:\n      r_i = min(distance to border, min_j (dist(i,j) - r_j))\n    This converges to the largest feasible radii vector for given centers.\n    \"\"\"\n    n = centers.shape[0]\n    # initial radii: limited by borders\n    border_d = np.minimum.reduce([centers[:, 0], centers[:, 1], 1 - centers[:, 0], 1 - centers[:, 1]])\n    radii = np.maximum(border_d.copy(), 0.0)\n\n    # pairwise distance matrix\n    diff = centers[:, None, :] - centers[None, :, :]\n    dist = np.sqrt(np.maximum(np.sum(diff ** 2, axis=2), 1e-12))\n\n    for it in range(max_iter):\n        max_change = 0.0\n        # Gauss-Seidel: update each radius using latest values\n        for i in range(n):\n            # limit by border\n            r_new = border_d[i]\n            # limit by other circles: r_i <= dist_ij - r_j for all j != i\n            # compute vector, ignore j==i\n            others = dist[i]\n            constraints = others - radii\n            constraints[i] = r_new  # won't be used\n            # the maximum feasible r_i is the minimum of constraints (and border)\n            r_allowed = np.min(constraints[np.arange(n) != i])\n            if r_allowed < r_new:\n                r_new = max(r_allowed, 0.0)\n            # update\n            change = abs(r_new - radii[i])\n            if change > 0:\n                radii[i] = r_new\n                if change > max_change:\n                    max_change = change\n        if max_change < tol:\n            break\n\n    # numerical safety\n    radii = np.maximum(radii, 0.0)\n    return radii\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.5)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.0246), Performs well on target_ratio (0.7683), Performs well on combined_score (0.7683), Performs well on eval_time (0.0675)\n\n\n\n## Diverse Programs\n\n### Program D1 (Score: 0.7545)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (hex-like layers)\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    \"\"\"\n    Construct 26 circle centers in a compact hex-like layered layout inside [0,1]^2,\n    then compute maximal non-overlapping radii for those fixed centers.\n    Returns (centers, radii, sum_of_radii).\n    \"\"\"\n    n = 26\n    # Row counts chosen to create a compact hex-like packing (sum = 26)\n    row_counts = [6, 5, 5, 5, 5]\n    rows = len(row_counts)\n\n    margin = 0.005\n    max_row = max(row_counts)\n    # horizontal spacing chosen to fit the widest row comfortably\n    s = min((1.0 - 2 * margin) / (max_row - 1), 0.18)\n    h = s * np.sqrt(3) / 2.0  # vertical spacing for hex pattern\n\n    total_height = (rows - 1) * h\n    y0 = (1.0 - total_height) / 2.0\n\n    centers = np.zeros((n, 2))\n    idx = 0\n    for r in range(rows):\n        k = row_counts[r]\n        offset = (s / 2.0) if (r % 2 == 1) else 0.0\n        row_width = (k - 1) * s\n        x_start = 0.5 - row_width / 2.0 + offset\n        y = y0 + r * h\n        for j in range(k):\n            x = x_start + j * s\n            centers[idx, 0] = np.clip(x, margin, 1.0 - margin)\n            centers[idx, 1] = np.clip(y, margin, 1.0 - margin)\n            idx += 1\n    centers = centers[:n]\n\n    radii = compute_max_radii(centers)\n    return centers, radii, float(np.sum(radii))\n\ndef compute_max_radii(centers, tol=1e-8, max_iter=5000):\n    \"\"\"\n    Iteratively compute maximal radii for fixed centers so circles lie in [0,1]^2\n    and are pairwise non-overlapping. At each violation we shrink the larger of the\n    two circles involved. Monotone decreasing process converges quickly.\n    \"\"\"\n    n = centers.shape[0]\n    # start with border-limited radii\n    border = np.minimum.reduce([centers[:,0], centers[:,1], 1-centers[:,0], 1-centers[:,1]])\n    radii = border.copy()\n\n    # pairwise center distances\n    diff = centers[:, None, :] - centers[None, :, :]\n    dists = np.sqrt(np.sum(diff**2, axis=2))\n    np.fill_diagonal(dists, np.inf)\n\n    for _ in range(max_iter):\n        max_change = 0.0\n        # check all pairs\n        for i in range(n):\n            # vectorized over j would be fine but keep explicit to control shrink policy\n            for j in range(i+1, n):\n                dij = dists[i, j]\n                if not np.isfinite(dij):\n                    continue\n                ri, rj = radii[i], radii[j]\n                if ri + rj > dij:\n                    overlap = ri + rj - dij\n                    # shrink the larger radius by the overlap (never increase)\n                    if ri >= rj:\n                        newri = max(0.0, ri - overlap)\n                        change = ri - newri\n                        if change > 0:\n                            radii[i] = newri\n                            max_change = max(max_change, change)\n                    else:\n                        newrj = max(0.0, rj - overlap)\n                        change = rj - newrj\n                        if change > 0:\n                            radii[j] = newrj\n                            max_change = max(max_change, change)\n        # ensure border constraints still hold\n        clipped = np.minimum(radii, border)\n        max_change = max(max_change, float(np.max(np.abs(radii - clipped))))\n        radii = clipped\n        if max_change < tol:\n            break\n    return radii\n\n# EVOLVE-BLOCK-END\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor='k')\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nKey features: Alternative approach to validity, Alternative approach to sum_radii\n\n### Program D2 (Score: 0.7193)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (improved)\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    \"\"\"\n    Construct an explicit arrangement of 26 circle centers in the unit square\n    using a compact hex-like layered layout, then compute maximal non-overlapping\n    radii for those fixed centers by an iterative constraint propagation.\n    Returns (centers, radii, sum_of_radii).\n    \"\"\"\n    n = 26\n    # Row counts chosen to give 26 points in a compact hex-like layout\n    row_counts = [6, 5, 5, 5, 5]  # sum = 26\n    rows = len(row_counts)\n\n    # Hex lattice spacing guess (fits inside unit square with small margins)\n    margin = 0.01\n    # Choose horizontal spacing s constrained by the longest row width\n    max_row = max(row_counts)\n    s = min((1.0 - 2 * margin) / (max_row - 1), 0.19)  # keep a safe spacing\n    h = s * np.sqrt(3) / 2.0  # vertical spacing for hex pattern\n\n    # Vertical centering\n    total_height = (rows - 1) * h\n    y0 = (1.0 - total_height) / 2.0\n\n    centers = np.zeros((n, 2))\n    idx = 0\n    for r in range(rows):\n        k = row_counts[r]\n        # Alternate offset to create hex-like staggering\n        offset = (s / 2.0) if (r % 2 == 1) else 0.0\n        row_width = (k - 1) * s\n        x_start = 0.5 - row_width / 2.0 + offset\n        y = y0 + r * h\n        for j in range(k):\n            x = x_start + j * s\n            # Clip into interior slightly to avoid exact border\n            centers[idx, 0] = np.clip(x, margin, 1.0 - margin)\n            centers[idx, 1] = np.clip(y, margin, 1.0 - margin)\n            idx += 1\n\n    # Safety: ensure exactly 26\n    centers = centers[:n]\n\n    radii = compute_max_radii(centers)\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\ndef compute_max_radii(centers, tol=1e-7, max_iter=5000):\n    \"\"\"\n    Given fixed centers, compute maximal radii so circles stay inside [0,1]^2\n    and do not overlap. Procedure: start with border-limited radii and iteratively\n    enforce pairwise non-overlap by shrinking the larger circle when needed.\n    This is a monotone decreasing scheme that typically converges quickly.\n    \"\"\"\n    n = centers.shape[0]\n    # initial radii limited by distance to borders\n    radii = np.minimum.reduce([centers[:,0], centers[:,1], 1-centers[:,0], 1-centers[:,1]])\n    radii = np.maximum(radii, 0.0)\n\n    # Precompute pairwise center distances\n    diff = centers[:, None, :] - centers[None, :, :]\n    dists = np.sqrt(np.sum(diff**2, axis=2))\n    # Avoid self distances = 0 on diagonal\n    np.fill_diagonal(dists, np.inf)\n\n    for it in range(max_iter):\n        max_change = 0.0\n        # enforce pairwise constraints\n        for i in range(n):\n            for j in range(i+1, n):\n                dij = dists[i, j]\n                if dij == 0.0 or np.isinf(dij):\n                    continue\n                ri = radii[i]\n                rj = radii[j]\n                total = ri + rj\n                if total > dij:\n                    # amount to reduce\n                    overlap = total - dij\n                    # reduce the larger radius by the overlap (never increase)\n                    if ri >= rj:\n                        new_ri = max(0.0, ri - overlap)\n                        change = ri - new_ri\n                        if change > 0:\n                            radii[i] = new_ri\n                            max_change = max(max_change, change)\n                    else:\n                        new_rj = max(0.0, rj - overlap)\n                        change = rj - new_rj\n                        if change > 0:\n                            radii[j] = new_rj\n                            max_change = max(max_change, change)\n        # re-enforce border limits (they may still be the binding constraint)\n        border_limits = np.minimum.reduce([centers[:,0], centers[:,1], 1-centers[:,0], 1-centers[:,1]])\n        # ensure we don't increase any radius above border limits\n        clipped = np.minimum(radii, border_limits)\n        max_change = max(max_change, float(np.max(np.abs(radii - clipped))))\n        radii = clipped\n\n        if max_change < tol:\n            break\n\n    return radii\n\n# EVOLVE-BLOCK-END\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor='k')\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nKey features: Alternative approach to validity, Alternative approach to sum_radii\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 0.8223, Type: High-Performer)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Improved constructor-based circle packing for n=26 circles.\n\nThis uses a hexagonal-like layered layout (rows with alternating offsets)\nto pack 26 circles in a unit square with equal radii chosen to be as large\nas possible given the row with the maximum count. This yields a much larger\nsum of radii than the original simple concentric-ring layout.\n\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square.\n\n    Returns:\n        Tuple of (centers, radii, sum_of_radii)\n        centers: np.array of shape (26, 2) with (x, y) coordinates\n        radii: np.array of shape (26) with radius of each circle\n        sum_of_radii: Sum of all radii\n    \"\"\"\n    # Row layout chosen to sum to 26: 6 + 5 + 6 + 5 + 4 = 26\n    rows = [6, 5, 6, 5, 4]\n    n = sum(rows)\n    assert n == 26\n\n    # Spacing factors for hexagonal-style packing\n    sqrt3 = np.sqrt(3.0)\n    max_in_row = max(rows)\n\n    # Horizontal constraint: 2 * r * max_in_row <= 1  -> r <= 1/(2*max_in_row)\n    r_horiz = 1.0 / (2.0 * max_in_row)\n\n    # Vertical constraint for rows: last_center_y <= 1 - r\n    # if y_k = r + k * (sqrt3 * r) for k=0..(R-1), require r + (R-1)*sqrt3*r <= 1 - r\n    # -> r * ((R-1)*sqrt3 + 2) <= 1\n    R = len(rows)\n    r_vert = 1.0 / (((R - 1) * sqrt3) + 2.0)\n\n    # Choose the limiting radius (horizontal typically limits for our layout)\n    r = min(r_horiz, r_vert)\n\n    centers = []\n    # Build rows from bottom (k=0) to top (k=R-1)\n    for k, count in enumerate(rows):\n        # Alternate offset every other row (hex packing)\n        offset = (k % 2) == 1\n        if not offset:\n            x_start = r  # centers: r, r+2r, r+4r, ...\n        else:\n            x_start = 2.0 * r  # shifted by r: 2r, 4r, ...\n        # horizontal spacing 2r\n        xs = [x_start + j * 2.0 * r for j in range(count)]\n        y = r + k * (sqrt3 * r)\n        for x in xs:\n            centers.append([x, y])\n\n    centers = np.array(centers, dtype=float)\n    # Safety: ensure count and containment\n    assert centers.shape == (n, 2)\n\n    # Use equal radii (designed to be non-overlapping inside the unit square)\n    radii = np.full(n, r, dtype=float)\n\n    # Validate and (if numeric tiny issues) correct radii so no overlaps and inside bounds\n    # Compute minimal distance to borders and to neighbors, then clamp to those maxima.\n    # This is a conservative correction; for a correctly computed r it leaves values unchanged.\n    # Distance to border\n    border_dists = np.minimum.reduce([centers[:, 0], centers[:, 1], 1.0 - centers[:, 0], 1.0 - centers[:, 1]])\n    max_by_border = border_dists.copy()\n\n    # Minimal half pairwise distances\n    pairwise = np.sqrt(((centers[:, None, :] - centers[None, :, :]) ** 2).sum(axis=2))\n    # set diagonal to large so min ignores zero\n    np.fill_diagonal(pairwise, np.inf)\n    min_half_dist = 0.5 * pairwise.min(axis=1)\n\n    # Final allowed radius per circle\n    allowed = np.minimum(max_by_border, min_half_dist)\n    # Numerical tolerance: do not enlarge beyond our design r, only shrink if necessary\n    radii = np.minimum(radii, allowed)\n    # If any radius became extremely small (shouldn't happen), fall back to allowed values\n    radii = np.maximum(radii, 1e-12)\n\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Hex-style Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nUnique approach: Modification: Full rewrite, Excellent validity (1.000), Excellent sum_radii (2.167)\n\n### Inspiration 2 (Score: 0.7683, Type: Alternative)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (improved)\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square.\n    Strategy:\n    - Generate candidate hexagonal-lattice clusters (varying lattice spacing s and\n      a small inward compression alpha).\n    - For each candidate, pick the 26 lattice points closest to the square center,\n      compute maximum non-overlapping radii (iterative Gauss-Seidel relaxation),\n      and keep the candidate with the largest sum of radii.\n    Returns:\n        centers: np.array (26,2), radii: np.array (26,), sum_radii: float\n    \"\"\"\n    n = 26\n    center_sq = np.array([0.5, 0.5])\n\n    # Search parameters (small, deterministic grid)\n    s_values = np.linspace(0.11, 0.24, 14)  # hex spacing candidates\n    alpha_values = [0.85, 0.9, 0.95, 1.0]  # small inward compression to fight edge effects\n\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    # precompute integer grid range large enough to produce many candidates\n    m = 8\n    ii = np.arange(-m, m + 1)\n    jj = np.arange(-m, m + 1)\n    I, J = np.meshgrid(ii, jj)\n    IJ = np.column_stack((I.ravel(), J.ravel()))\n\n    for s in s_values:\n        # hex lattice basis\n        v1 = np.array([s, 0.0])\n        v2 = np.array([s * 0.5, s * (np.sqrt(3) / 2.0)])\n        pts = IJ.dot(np.vstack((v1, v2)).T)  # shape (Npts, 2)\n        # shift to square center\n        pts = pts + center_sq\n\n        # Keep only points inside the unit square (small tolerance)\n        inside_mask = np.all((pts >= -1e-9) & (pts <= 1 + 1e-9), axis=1)\n        valid_pts = pts[inside_mask]\n        if valid_pts.shape[0] < n:\n            # still might work after compression but skip if insufficient\n            continue\n\n        # sort by distance to center and take a cluster of 26 closest\n        dists = np.sum((valid_pts - center_sq) ** 2, axis=1)\n        order = np.argsort(dists)\n        base_cluster = valid_pts[order[: max(n + 10, n * 2)]]  # take extra for alpha perturb\n\n        for alpha in alpha_values:\n            # compress radial offsets slightly toward center to reduce edge effects\n            offsets = base_cluster - center_sq\n            comp = center_sq + offsets * alpha\n            # ensure points lie in [0,1]\n            comp = np.clip(comp, 1e-6, 1 - 1e-6)\n\n            # re-sort and pick exactly n closest\n            d2 = np.sum((comp - center_sq) ** 2, axis=1)\n            pick_idx = np.argsort(d2)[:n]\n            centers = comp[pick_idx]\n\n            # compute maximum radii for these fixed centers\n            radii = compute_max_radii_relax(centers, max_iter=1000, tol=1e-8)\n\n            sum_r = float(np.sum(radii))\n            if sum_r > best_sum:\n                best_sum = sum_r\n                best_centers = centers.copy()\n                best_radii = radii.copy()\n\n    # Fallback: if nothing found (shouldn't happen), use small regular pattern\n    if best_centers is None:\n        best_centers = np.zeros((n, 2))\n        best_centers[0] = center_sq\n        for i in range(1, 9):\n            angle = 2 * np.pi * (i - 1) / 8\n            best_centers[i] = center_sq + 0.18 * np.array([np.cos(angle), np.sin(angle)])\n        for i in range(16):\n            angle = 2 * np.pi * i / 16\n            best_centers[i + 9] = center_sq + 0.34 * np.array([np.cos(angle), np.sin(angle)])\n        best_centers = np.clip(best_centers, 1e-6, 1 - 1e-6)\n        best_radii = compute_max_radii_relax(best_centers)\n\n    sum_radii = float(np.sum(best_radii))\n    return best_centers, best_radii, sum_radii\n\n\ndef compute_max_radii_relax(centers, max_iter=500, tol=1e-7):\n    \"\"\"\n    Given fixed centers inside unit square, compute the maximum possible non-overlapping\n    radii using a simple iterative Gauss-Seidel relaxation:\n      r_i = min(distance to border, min_j (dist(i,j) - r_j))\n    This converges to the largest feasible radii vector for given centers.\n    \"\"\"\n    n = centers.shape[0]\n    # initial radii: limited by borders\n    border_d = np.minimum.reduce([centers[:, 0], centers[:, 1], 1 - centers[:, 0], 1 - centers[:, 1]])\n    radii = np.maximum(border_d.copy(), 0.0)\n\n    # pairwise distance matrix\n    diff = centers[:, None, :] - centers[None, :, :]\n    dist = np.sqrt(np.maximum(np.sum(diff ** 2, axis=2), 1e-12))\n\n    for it in range(max_iter):\n        max_change = 0.0\n        # Gauss-Seidel: update each radius using latest values\n        for i in range(n):\n            # limit by border\n            r_new = border_d[i]\n            # limit by other circles: r_i <= dist_ij - r_j for all j != i\n            # compute vector, ignore j==i\n            others = dist[i]\n            constraints = others - radii\n            constraints[i] = r_new  # won't be used\n            # the maximum feasible r_i is the minimum of constraints (and border)\n            r_allowed = np.min(constraints[np.arange(n) != i])\n            if r_allowed < r_new:\n                r_new = max(r_allowed, 0.0)\n            # update\n            change = abs(r_new - radii[i])\n            if change > 0:\n                radii[i] = r_new\n                if change > max_change:\n                    max_change = change\n        if max_change < tol:\n            break\n\n    # numerical safety\n    radii = np.maximum(radii, 0.0)\n    return radii\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.5)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nUnique approach: Modification: Full rewrite, Excellent validity (1.000), Excellent sum_radii (2.025)\n\n### Inspiration 3 (Score: 0.6928, Type: Alternative)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (hex-like lattice)\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    \"\"\"\n    Build 26 explicit centers in a hexagonal-like layout and compute\n    the maximal non-overlapping radii (within the unit square) by\n    iteratively enforcing wall and pairwise constraints.\n    Returns: centers (26,2), radii (26,), sum_of_radii\n    \"\"\"\n    n = 26\n\n    # Hex-like layout: 5 rows with counts summing to 26: [6,5,5,5,5]\n    counts = [6, 5, 5, 5, 5]\n    s = 0.16                        # horizontal spacing\n    h = s * np.sqrt(3) / 2         # vertical spacing for hex packing\n    center_x, center_y = 0.5, 0.5\n\n    centers = []\n    row_offsets = [ -2, -1, 0, 1, 2 ]  # vertical row indices\n    for r_idx, k in enumerate(counts):\n        y = center_y + row_offsets[r_idx] * h\n        # shift alternate (shorter) rows by s/2 to approximate hex packing\n        shift = (s / 2.0) if k < max(counts) else 0.0\n        xs = center_x + (np.arange(k) - (k - 1) / 2.0) * s + shift\n        for x in xs:\n            centers.append([x, y])\n\n    centers = np.array(centers[:n])  # ensure exactly 26\n\n    radii = compute_max_radii(centers)\n\n    return centers, radii, float(np.sum(radii))\n\n\ndef compute_max_radii(centers, max_iter=2000, tol=1e-9):\n    \"\"\"\n    Given fixed centers, compute maximal radii so circles stay inside [0,1]^2\n    and non-overlapping. Uses a Gauss-Seidel style iterative clipping:\n      r_i <- min(dist_to_wall_i, min_j (dist_ij - r_j))\n    until convergence.\n    \"\"\"\n    n = centers.shape[0]\n    # initial radii: limited by distance to walls\n    radii = np.minimum.reduce([centers[:,0], centers[:,1], 1-centers[:,0], 1-centers[:,1]]).astype(float)\n\n    # iterative tightening (Gauss-Seidel)\n    for it in range(max_iter):\n        max_change = 0.0\n        for i in range(n):\n            # wall limit\n            wall_lim = min(centers[i,0], centers[i,1], 1-centers[i,0], 1-centers[i,1])\n            r_new = wall_lim\n            # pairwise limits\n            for j in range(n):\n                if i == j:\n                    continue\n                d = np.hypot(*(centers[i] - centers[j]))\n                if d <= 1e-12:\n                    # coincident centers -> force zero radius\n                    r_new = 0.0\n                    break\n                # maximum allowed r_i given r_j to avoid overlap: d - r_j\n                # only meaningful if d - r_j >= 0\n                candidate = d - radii[j]\n                if candidate < r_new:\n                    r_new = candidate\n            # clamp and non-negative\n            if r_new < 0.0:\n                r_new = 0.0\n            change = abs(radii[i] - r_new)\n            if change > 0:\n                radii[i] = r_new\n                if change > max_change:\n                    max_change = change\n        if max_change < tol:\n            break\n\n    return radii\n\n# EVOLVE-BLOCK-END\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.5)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nUnique approach: Modification: Full rewrite, Excellent validity (1.000), Excellent sum_radii (1.826)\n\n# Current Program\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (improved)\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square.\n    Returns (centers, radii, sum_of_radii).\n    Strategy:\n      - Build several hexagonal-lattice candidates (vary spacing)\n      - For each, select the 26 lattice points nearest the square center\n      - Compute maximal radii by iterative constraint projection (border + pairwise)\n      - Return the candidate with largest sum of radii\n    \"\"\"\n    n = 26\n    center_pt = np.array([0.5, 0.5])\n    best = None\n    best_sum = -1.0\n\n    # Try a handful of spacings to get a dense central hex region of ~26 points\n    for s in np.linspace(0.10, 0.17, 8):\n        pts = []\n        dy = s * np.sqrt(3) / 2.0\n        # create hex grid covering the unit square with a small margin\n        margin = s * 0.6\n        y = margin\n        row = 0\n        while y <= 1.0 - margin + 1e-12:\n            xoff = (s / 2.0) if (row % 2 == 1) else 0.0\n            x = margin + xoff\n            while x <= 1.0 - margin + 1e-12:\n                pts.append((x, y))\n                x += s\n            row += 1\n            y += dy\n        pts = np.array(pts)\n        if pts.shape[0] < n:\n            continue\n        # choose the n points nearest the square center to avoid edges\n        dists = np.sum((pts - center_pt) ** 2, axis=1)\n        idx = np.argsort(dists)[:n]\n        centers = pts[idx]\n\n        radii = compute_max_radii(centers)\n        ssum = np.sum(radii)\n        if ssum > best_sum:\n            best_sum = ssum\n            best = (centers.copy(), radii.copy(), ssum)\n\n    # Fallback: if none produced enough points, make a small manual pattern\n    if best is None:\n        centers = manual_pattern()\n        radii = compute_max_radii(centers)\n        best = (centers, radii, np.sum(radii))\n\n    return best\n\ndef compute_max_radii(centers):\n    \"\"\"\n    Given centers (m x 2), compute maximal non-overlapping radii inside unit square.\n    Uses iterative Gauss-Seidel projection:\n      - initialize by distance to border\n      - repeatedly enforce pairwise non-overlap r_i + r_j <= dist(i,j)\n      - converge until changes are tiny\n    \"\"\"\n    m = centers.shape[0]\n    # initial radii: limited by border\n    radii = np.minimum.reduce([centers[:,0], centers[:,1], 1-centers[:,0], 1-centers[:,1]])\n    radii = np.maximum(radii, 1e-12)\n\n    # precompute inter-center distances\n    D = np.sqrt(np.maximum(1e-12, np.sum((centers[:,None,:] - centers[None,:,:])**2, axis=2)))\n    tol = 1e-6\n    for _ in range(500):\n        max_change = 0.0\n        # enforce pairwise constraints in Gauss-Seidel fashion\n        for i in range(m):\n            for j in range(m):\n                if i == j: \n                    continue\n                dij = D[i,j]\n                # required r_i <= dij - r_j\n                bound = dij - radii[j]\n                if bound < radii[i]:\n                    change = radii[i] - max(bound, 0.0)\n                    radii[i] = max(bound, 0.0)\n                    if change > max_change:\n                        max_change = change\n            # also reapply border constraint\n            bord = min(centers[i,0], centers[i,1], 1-centers[i,0], 1-centers[i,1])\n            if radii[i] > bord:\n                change = radii[i] - bord\n                radii[i] = bord\n                if change > max_change:\n                    max_change = change\n        if max_change < tol:\n            break\n    return radii\n\ndef manual_pattern():\n    # fallback explicit pattern: concentric-ish arrangement biased toward center\n    pts = []\n    pts.append((0.5, 0.5))\n    # two inner hex rings (8 + 6)\n    for k, r in enumerate((0.14, 0.26)):\n        count = 6 if k==0 else 8\n        for i in range(count):\n            ang = 2*np.pi*i/count\n            pts.append((0.5 + r*np.cos(ang), 0.5 + r*np.sin(ang)))\n    # outer ring of remaining points spaced around\n    angles = np.linspace(0, 2*np.pi, 26, endpoint=False)\n    i = 0\n    r = 0.42\n    while len(pts) < 26:\n        pts.append((0.5 + r*np.cos(angles[i]), 0.5 + r*np.sin(angles[i])))\n        i += 1\n    return np.array(pts[:26])\n\n# EVOLVE-BLOCK-END\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.5)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\n\n# Task\nRewrite the program to improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: complexity, diversity\nDifferent solutions with similar fitness but different features are valuable.\nProvide the complete new program code.\n\nIMPORTANT: Make sure your rewritten program maintains the same inputs and outputs\nas the original program, but with improved internal implementation.\n\n```python\n# Your rewritten program here\n```", "responses": ["```python\n# EVOLVE-BLOCK-START\n\"\"\"Explicit constructor for 26-circle packing in the unit square.\n\nUses a compact hex-like layered layout (rows with alternating offsets).\nRadii are set to the maximal feasible per-center given border and pairwise\nseparation: r_i = min(distance to border, 0.5 * min_j dist(center_i, center_j)).\nThis yields a simple, deterministic, high-quality feasible packing.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    # row pattern chosen to be compact: sums to 26\n    rows = [6, 5, 6, 5, 4]\n    assert sum(rows) == n\n\n    sqrt3 = np.sqrt(3.0)\n    R = len(rows)\n\n    # Compute a candidate base radius r limited by horizontal and vertical packing constraints\n    max_in_row = max(rows)\n    r_horiz = 1.0 / (2.0 * max_in_row)  # 2r * max_in_row <= 1 -> r <= 1/(2*max)\n    r_vert = 1.0 / (((R - 1) * sqrt3) + 2.0)  # r*((R-1)*sqrt3 + 2) <= 1\n    r = min(r_horiz, r_vert)\n\n    # Build centered hex-like rows\n    centers = []\n    # vertical centering: total vertical span between first and last row\n    total_v = (R - 1) * sqrt3 * r\n    y0 = 0.5 - 0.5 * total_v\n    for k, count in enumerate(rows):\n        offset = (k % 2) == 1\n        if not offset:\n            x_start = 0.5 - (count - 1) * r\n            # positions: x_start + j * 2r, j=0..count-1\n            xs = x_start + np.arange(count) * (2.0 * r)\n        else:\n            # offset rows are shifted by r, and to center such row use start = 0.5 - count*r\n            x_start = 0.5 - count * r\n            xs = x_start + r + np.arange(count) * (2.0 * r)\n        y = y0 + k * (sqrt3 * r)\n        for x in xs:\n            centers.append((float(x), float(y)))\n\n    centers = np.array(centers, dtype=float)\n    # Safety: clip tiny numerical excursions\n    centers = np.clip(centers, 1e-12, 1.0 - 1e-12)\n    assert centers.shape == (n, 2)\n\n    # Compute maximal feasible per-circle radii (conservative but feasible)\n    # border limits\n    border = np.minimum.reduce([centers[:,0], centers[:,1], 1.0 - centers[:,0], 1.0 - centers[:,1]])\n    # pairwise distances\n    dif = centers[:, None, :] - centers[None, :, :]\n    D = np.sqrt(np.maximum(1e-20, np.sum(dif**2, axis=2)))\n    np.fill_diagonal(D, np.inf)\n    min_half = 0.5 * D.min(axis=1)\n    allowed = np.minimum(border, min_half)\n    # do not exceed our base r (layout assumed r is safe); but allow slightly larger where possible\n    # however to keep packing stable, cap by max(allowed, tiny)\n    radii = np.maximum(allowed, 0.0)\n\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n# EVOLVE-BLOCK-END\n\n# fixed interface\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\nif __name__ == \"__main__\":\n    centers, radii, s = run_packing()\n    print(f\"Sum of radii: {s:.6f}\")\n```"]}}, "artifacts_json": "{\"stdout\": \"Running in subprocess, Python version: 3.12.1 (v3.12.1:2305ca5144, Dec  7 2023, 17:23:38) [Clang 13.0.0 (clang-1300.0.29.30)]\\nProgram path: /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmpoah0k3ca.py\\nCalling run_packing()...\\nrun_packing() returned successfully: sum_radii = 2.166666666666666\\nResults saved to /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmpv5ko3yml.py.results\\n\", \"stderr\": \"\"}", "artifact_dir": null}