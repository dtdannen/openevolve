{"id": "123af9bc-5feb-4e3a-8404-a312a8e9bd46", "code": "# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for packing n=26 circles in the unit square.\nExplicit hex-like layer layout tuned for 26 circles with a Gauss-Seidel\nstyle solver that finds (approximately) maximal feasible radii.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    # Compact hex-like row counts summing to 26 (tuned)\n    rows = [6, 5, 6, 5, 4]  # total 26\n    R = len(rows)\n\n    # Use horizontal spacing suitable for 6 columns\n    s = 1.0 / 6.0\n    h = s * np.sqrt(3.0) / 2.0\n\n    # vertical center\n    y0 = 0.5 - ((R - 1) * h) / 2.0\n\n    centers = []\n    for i, k in enumerate(rows):\n        y = y0 + i * h\n        left = 0.5 - ((k - 1) * s) / 2.0\n        xs = left + np.arange(k) * s\n        for x in xs:\n            centers.append((float(x), float(y)))\n    centers = np.array(centers[:n])\n    assert centers.shape[0] == n\n\n    radii = compute_max_radii_gs(centers)\n    sum_radii = float(radii.sum())\n    return centers, radii, sum_radii\n\ndef compute_max_radii_gs(centers, max_iter=2000, tol=1e-10):\n    \"\"\"Gauss-Seidel style update:\n       r_i <- min(border_i, max(0, min_j (d_ij - r_j)))\n       Uses most recent r_j values for updates (sequential).\n    \"\"\"\n    n = centers.shape[0]\n    # border limits\n    border = np.minimum.reduce([centers[:,0], centers[:,1], 1.0-centers[:,0], 1.0-centers[:,1]])\n    border = np.maximum(border, 0.0)\n\n    # pairwise distances\n    dif = centers[:, None, :] - centers[None, :, :]\n    dists = np.hypot(dif[...,0], dif[...,1])\n    np.fill_diagonal(dists, np.inf)\n\n    # initialize with border distances (conservative)\n    r = border.copy()\n\n    for it in range(max_iter):\n        max_change = 0.0\n        # sequential Gauss-Seidel updates\n        for i in range(n):\n            # neighbor constraints: r_i <= d_ij - r_j for all j\n            limits = dists[i, :] - r\n            limits[i] = np.inf  # ignore self\n            new = np.minimum(border[i], np.min(limits))\n            if new < 0.0:\n                new = 0.0\n            # clamp numeric noise\n            if not np.isfinite(new):\n                new = 0.0\n            change = abs(r[i] - new)\n            if change > 0:\n                r[i] = new\n                if change > max_change:\n                    max_change = change\n        if max_change < tol:\n            break\n\n    # final safety clamps\n    r = np.clip(r, 0.0, border)\n    return r\n\n# EVOLVE-BLOCK-END\n\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(6,6))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect(\"equal\")\n    ax.add_patch(plt.Rectangle((0,0),1,1,fill=False,edgecolor='k'))\n    for c, r in zip(centers, radii):\n        ax.add_patch(Circle(c, r, alpha=0.6, edgecolor='k'))\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # visualize(centers, radii)", "language": "python", "parent_id": "3be73501-67cf-45a9-b3ed-3a406abbbb70", "generation": 3, "timestamp": 1759960961.1436071, "iteration_found": 19, "metrics": {"validity": 1.0, "sum_radii": 1.9999999999999991, "target_ratio": 0.7590132827324475, "combined_score": 0.7590132827324475, "eval_time": 0.05563783645629883}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Full rewrite", "parent_metrics": {"validity": 1.0, "sum_radii": 1.2770469613566842, "target_ratio": 0.4846478031714172, "combined_score": 0.4846478031714172}, "island": 0}, "prompts": {"full_rewrite_user": {"system": "You are an expert mathematician specializing in circle packing problems and computational geometry. Your task is to improve a constructor function that directly produces a specific arrangement of 26 circles in a unit square, maximizing the sum of their radii. The AlphaEvolve paper achieved a sum of 2.635 for n=26.\n\nKey geometric insights:\n- Circle packings often follow hexagonal patterns in the densest regions\n- Maximum density for infinite circle packing is pi/(2*sqrt(3)) \u2248 0.9069\n- Edge effects make square container packing harder than infinite packing\n- Circles can be placed in layers or shells when confined to a square\n- Similar radius circles often form regular patterns, while varied radii allow better space utilization\n- Perfect symmetry may not yield the optimal packing due to edge effects\n\nFocus on designing an explicit constructor that places each circle in a specific position, rather than an iterative search algorithm.\n\n\n## Evolution Insights (Global Learnings)\n\n### Common Pitfalls:\n\u26a0\ufe0f (seen 2x) A recent change to the radius/scale calculation (likely altering normalization/clipping or removing a padding/scale factor) reduced the aggregated sum_radii by ~12% (0.960 \u2192 0.845), which directly lowered target_ratio and combined_score. When changing geometry/normalization logic, avoid unintentionally shrinking radii by checking unit conversions and scale/padding behavior, and add regression/unit tests that assert sum_radii and downstream metrics remain within acceptable bounds.\n\u26a0\ufe0f (seen 2x) A change that reduced the number and/or size of produced candidate regions \u2014 likely from more aggressive filtering, tighter thresholds, or reduced sampling \u2014 caused the sum_radii to drop from 0.9598 to 0.8446 and directly lowered target_ratio/combined_score by ~12% (0.364\u21920.321), a regression observed in 2 runs across iterations 1\u20139. When making similar optimizations, avoid aggressive pruning or threshold changes without targeted regression tests: instrument and guard changes with automated checks for target_ratio, combined_score and sum_radii (and a rollback path) so coverage/quality regressions are caught before release.\n\u26a0\ufe0f (seen 2x) The regression was caused by a change that reduced the effective coverage/search radii (sum_radii dropped from 0.9598 to 0.8446), which directly lowered the fraction of targets hit (target_ratio 0.3642\u21920.3205) and thus cut the combined_score by ~12%. When changing spatial/coverage parameters or adding aggressive pruning, avoid shrinking radii/thresholds without verifying impact \u2014 run targeted benchmarks and regression tests that assert minimum target_ratio/combined_score and apply such changes incrementally while monitoring these metrics.\n\u26a0\ufe0f (seen 1x) A change short-circuited or replaced the radii/target-ratio computation (or the object-initialization path) with a no-op/default-zero path so sum_radii and target_ratio collapsed to 0.0 and the overall validity check flipped from valid to invalid (1.000 \u2192 0.000).  \n\nWhen modifying initialization or computation code, avoid introducing default-zero returns or gating that skips construction; add unit/regression tests and assertions that core invariants (e.g., sum_radii > 0, validity == true) remain true and include CI checks to catch regressions early.", "user": "# Current Program Information\n- Fitness: 0.4846\n- Feature coordinates: No feature coordinates\n- Focus areas: - Fitness declined: 0.6773 \u2192 0.4846. Consider revising recent changes.\n- Consider simplifying - code length exceeds 500 characters\n\n## Last Execution Output\n\n### stdout\n```\nRunning in subprocess, Python version: 3.12.1 (v3.12.1:2305ca5144, Dec  7 2023, 17:23:38) [Clang 13.0.0 (clang-1300.0.29.30)]\nProgram path: /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmpsiwvrlnk.py\nCalling run_packing()...\nrun_packing() returned successfully: sum_radii = 1.2770469613566842\nResults saved to /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmpji4r_jcy.py.results\n\n```\n\n### stderr\n```\n\n```\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 3\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 1.7847, target_ratio: 0.6773, combined_score: 0.6773, eval_time: 0.0539\n- Outcome: Mixed results\n\n### Attempt 2\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 1.9580, target_ratio: 0.7431, combined_score: 0.7431, eval_time: 0.0555\n- Outcome: Mixed results\n\n### Attempt 1\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.1667, target_ratio: 0.8223, combined_score: 0.8223, eval_time: 0.0605\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.8223)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (explicit hex-like constructor)\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square\n    that attempts to maximize the sum of their radii using a\n    hexagonal-like lattice with row counts tuned to 26.\n    Returns:\n        Tuple of (centers, radii, sum_of_radii)\n    \"\"\"\n    n = 26\n    # Hex-like design parameters: choose spacing s as large as possible for 6 columns\n    s = 1.0 / 6.0  # horizontal spacing: max for 6 columns\n    h = s * np.sqrt(3) / 2.0  # vertical spacing for hex lattice\n\n    # Row structure summing to 26\n    rows = [6, 5, 6, 5, 4]  # total 26\n    R = len(rows)\n\n    # vertical placement centered in [0,1]\n    y0 = 0.5 - ( (R - 1) * h ) / 2.0\n\n    centers = []\n    for i, k in enumerate(rows):\n        y = y0 + i * h\n        # center each row horizontally\n        left = 0.5 - ((k - 1) * s) / 2.0\n        xs = left + np.arange(k) * s\n        for x in xs:\n            centers.append([x, y])\n\n    centers = np.array(centers[:n])  # ensure length 26\n\n    # Compute conservative maximal radii:\n    # For each circle, radius <= distance to border, and <= 0.5 * min_pairwise_distance\n    radii = np.empty(n)\n    for i in range(n):\n        x, y = centers[i]\n        border_dist = min(x, y, 1 - x, 1 - y)\n        # compute min distance to any other center\n        if n == 1:\n            min_pair = 2.0  # irrelevant\n        else:\n            dists = np.sqrt(((centers - centers[i]) ** 2).sum(axis=1))\n            dists[i] = np.inf\n            min_pair = dists.min()\n        # take half the nearest neighbor distance (guarantees non-overlap) and border limit\n        radii[i] = min(border_dist, 0.5 * min_pair)\n\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, ec=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.1667), Performs well on target_ratio (0.8223), Performs well on combined_score (0.8223), Performs well on eval_time (0.0605)\n\n### Program 2 (Score: 0.7431)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for packing 26 circles in a unit square.\n\nDesign: a hex-like layered layout tuned for the square (rows: 4,6,6,6,4),\nsmall margin from walls, followed by an iterative relaxation that first\nremoves overlaps and then performs greedy local expansions to increase\nthe sum of radii while preserving non-overlap.\n\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    n = 26\n    # Row structure chosen to tile the square compactly (total 26)\n    rows = [4, 6, 6, 6, 4]\n    num_rows = len(rows)\n    max_k = max(rows)\n\n    edge = 0.008  # margin from walls\n    # horizontal spacing so the widest row fits, include small offset allowance\n    s_h = (1.0 - 2 * edge) / (max_k - 1 + 0.5)\n    s_v = s_h * np.sqrt(3) / 2.0  # hex vertical spacing\n\n    # If vertical span doesn't fit, recompute based on vertical constraint\n    span_y = (num_rows - 1) * s_v\n    if span_y > (1.0 - 2 * edge):\n        s_v = (1.0 - 2 * edge) / (num_rows - 1)\n        s_h = s_v * 2.0 / np.sqrt(3)\n        span_y = (num_rows - 1) * s_v\n\n    y0 = 0.5 - span_y / 2.0\n    centers = []\n    for i, k in enumerate(rows):\n        y = y0 + i * s_v\n        x0 = 0.5 - ((k - 1) * s_h) / 2.0\n        # stagger every other row slightly (hex offset) for tighter packing\n        if i % 2 == 1:\n            x0 += s_h * 0.25\n        for j in range(k):\n            x = x0 + j * s_h\n            # clamp to stay inside margin\n            x = np.clip(x, edge, 1.0 - edge)\n            yc = np.clip(y, edge, 1.0 - edge)\n            centers.append((x, yc))\n\n    centers = np.asarray(centers)\n    assert centers.shape[0] == n\n\n    radii = compute_max_radii(centers)\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers, max_iters=400):\n    \"\"\"Compute radii by alternating overlap-removal and greedy expansion.\"\"\"\n    n = centers.shape[0]\n    # distances matrix (symmetric), with diagonal inf to ignore self\n    diffs = centers[:, None, :] - centers[None, :, :]\n    dists = np.hypot(diffs[..., 0], diffs[..., 1])\n    np.fill_diagonal(dists, np.inf)\n\n    # limit by walls initially\n    wall_limits = np.minimum.reduce([centers[:, 0], centers[:, 1],\n                                    1.0 - centers[:, 0], 1.0 - centers[:, 1]])\n    radii = wall_limits.copy()\n\n    eps = 1e-10\n    # Phase A: remove overlaps by proportional shrinking until consistent\n    for _ in range(200):\n        changed = False\n        # compute pairwise sums and find violations\n        sum_r = radii[:, None] + radii[None, :]\n        # where sum_r > dists -> violation (upper triangle)\n        viol = (sum_r > dists + 1e-9)\n        # iterate violations (sparse)\n        viol_idx = np.transpose(np.nonzero(np.triu(viol, k=1)))\n        if viol_idx.size == 0:\n            break\n        for i, j in viol_idx:\n            d = dists[i, j]\n            if d <= eps:\n                # coincide -> zero both\n                if radii[i] > 0:\n                    radii[i] = 0.0\n                    changed = True\n                if radii[j] > 0:\n                    radii[j] = 0.0\n                    changed = True\n                continue\n            s = radii[i] + radii[j]\n            if s <= d + 1e-12:\n                continue\n            factor = (d - 1e-9) / s\n            factor = max(factor, 1e-9)\n            # scale both down\n            newi = radii[i] * factor\n            newj = radii[j] * factor\n            if newi < radii[i] - 1e-12 or newj < radii[j] - 1e-12:\n                radii[i], radii[j] = newi, newj\n                changed = True\n        # re-apply wall limits\n        over = radii > wall_limits\n        if np.any(over):\n            radii[over] = wall_limits[over]\n            changed = True\n        if not changed:\n            break\n\n    # Phase B: greedy local expansion (attempt to increase each radius to its maximum\n    # given other radii fixed), then re-balance overlaps. Repeat a few cycles.\n    for cycle in range(60):\n        grew = False\n        # compute for each i the minimal slack to others: min_j (d_ij - r_j)\n        slack = np.min(dists - radii[None, :], axis=1)\n        allowed = np.minimum(wall_limits, slack)\n        # ensure allowed >= 0\n        allowed = np.maximum(allowed, 0.0)\n        # Try to increase where possible\n        # Only increase a little to maintain numerical stability: set r = max(r, allowed)\n        inc = allowed - radii\n        if np.any(inc > 1e-12):\n            grew = True\n            radii = np.maximum(radii, allowed)\n        # After expansion, fix any overlaps that may have arisen by a short proportional shrink\n        changed = False\n        sum_r = radii[:, None] + radii[None, :]\n        viol = (sum_r > dists + 1e-9)\n        viol_idx = np.transpose(np.nonzero(np.triu(viol, k=1)))\n        if viol_idx.size:\n            changed = True\n            # process violations again\n            for i, j in viol_idx:\n                d = dists[i, j]\n                if d <= eps:\n                    radii[i] = radii[j] = 0.0\n                    continue\n                s = radii[i] + radii[j]\n                if s <= d + 1e-12:\n                    continue\n                factor = (d - 1e-9) / s\n                factor = max(factor, 1e-9)\n                radii[i] *= factor\n                radii[j] *= factor\n        # apply wall limits again\n        over = radii > wall_limits\n        if np.any(over):\n            radii[over] = wall_limits[over]\n            changed = True\n        if not grew and not changed:\n            break\n\n    # final safety clamp\n    radii = np.maximum(radii, 0.0)\n    return radii\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (1.9580), Performs well on target_ratio (0.7431), Performs well on combined_score (0.7431), Performs well on eval_time (0.0555)\n\n### Program 3 (Score: 0.6773)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (improved deterministic hex-like constructor)\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    \"\"\"\n    Construct an explicit arrangement of 26 circles in the unit square.\n    Returns centers (26,2), radii (26,), sum_of_radii.\n    Approach:\n      - Build a compact hexagonal/triangular-like lattice with row counts summing to 26.\n      - Scale and center the lattice to fill the unit square with a small margin.\n      - Apply a tiny deterministic perturbation to break perfect symmetry.\n      - Compute maximum feasible radii by iteratively enforcing border and pairwise constraints.\n    \"\"\"\n    n = 26\n    # row pattern summing to 26 (compact hex-like)\n    row_counts = [5, 6, 5, 5, 5]  # total 26\n    sqrt3 = np.sqrt(3.0)\n\n    # create centered integer lattice coordinates (unit horizontal spacing)\n    points = []\n    for r_idx, cnt in enumerate(row_counts):\n        # x positions centered about 0\n        xs = [(i - (cnt - 1) / 2.0) for i in range(cnt)]\n        y = r_idx * (sqrt3 / 2.0)\n        for x in xs:\n            points.append((x, y))\n    pts = np.array(points)  # shape (26,2)\n\n    # bounding box of raw pattern\n    min_xy = pts.min(axis=0)\n    max_xy = pts.max(axis=0)\n    W, H = (max_xy - min_xy)\n\n    # leave a small margin within unit square\n    margin = 0.01\n    # scale to fit inside unit square with margin\n    if W <= 0 or H <= 0:\n        scale = 1.0\n    else:\n        scale = min((1 - 2 * margin) / W, (1 - 2 * margin) / H)\n\n    # apply scale and translate to center in unit square\n    pts = (pts - min_xy) * scale + margin\n    # center residual space\n    residual = 1.0 - (pts.max(axis=0) - pts.min(axis=0))\n    pts += residual / 2.0 - pts.min(axis=0)\n\n    # deterministic tiny perturbation (breaks perfect symmetry, helps border packing)\n    # uses sines to produce reproducible small offsets\n    jitter_scale = 0.006 * min(scale, 1.0)\n    idxs = np.arange(len(pts))\n    pts[:, 0] += jitter_scale * np.sin(3.3 * idxs + 0.7)\n    pts[:, 1] += jitter_scale * np.cos(2.7 * idxs + 1.1)\n\n    # clip to avoid numerical leaving unit square\n    pts = np.clip(pts, 0.0001, 0.9999)\n\n    # compute maximal feasible radii with an iterative proportional correction (deterministic)\n    radii = compute_max_radii_iterative(pts)\n\n    sum_radii = float(np.sum(radii))\n    return pts, radii, sum_radii\n\ndef compute_max_radii_iterative(centers, max_iter=200, tol=1e-8):\n    \"\"\"\n    Iteratively compute feasible radii maximizing sum under:\n      r_i >= 0\n      r_i + r_j <= dist(center_i, center_j)\n      r_i <= dist_to_border(center_i)\n    The simple iterative proportional scaling converges reliably for these structured layouts.\n    \"\"\"\n    n = centers.shape[0]\n    # initial radii limited by border\n    border = np.minimum.reduce([centers[:,0], centers[:,1], 1-centers[:,0], 1-centers[:,1]])\n    radii = border.copy()\n    # precompute pairwise distances\n    dx = centers[:, 0:1] - centers[:, 0:1].T\n    dy = centers[:, 1:2] - centers[:, 1:2].T\n    dists = np.sqrt(np.maximum(1e-12, dx*dx + dy*dy))\n\n    for it in range(max_iter):\n        changed = False\n        # enforce border constraint explicitly each iteration\n        radii = np.minimum(radii, border)\n        # loop through pairs, scale down violating pairs proportionally\n        for i in range(n):\n            ri = radii[i]\n            # vectorized across j>i\n            js = np.arange(i+1, n)\n            if js.size == 0:\n                continue\n            rj = radii[js]\n            dij = dists[i, js]\n            # indices where violation occurs\n            overlap = ri + rj > dij + 1e-12\n            if not np.any(overlap):\n                continue\n            # compute scales for violating pairs\n            scales = np.empty_like(rj)\n            scales[:] = 1.0\n            scales[overlap] = (dij[overlap] / (ri + rj[overlap]))\n            # apply proportional shrink: multiply both by min scale across their interactions\n            # For stability apply the smallest scale for the pair (i and each j)\n            # accumulate minimal scale for each index\n            min_scale_i = scales[overlap].min() if np.any(overlap) else 1.0\n            if min_scale_i < 1.0 - 1e-12:\n                radii[i] *= min_scale_i\n                changed = True\n            # for the j's apply their pairwise scales\n            for k, j in enumerate(js):\n                sc = scales[k]\n                if sc < 1.0 - 1e-12:\n                    radii[j] *= sc\n                    changed = True\n        # re-apply border clamp\n        radii = np.minimum(radii, border)\n        if not changed:\n            break\n    # ensure non-negative\n    radii = np.clip(radii, 0.0, None)\n    return radii\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (1.7847), Performs well on target_ratio (0.6773), Performs well on combined_score (0.6773), Performs well on eval_time (0.0539)\n\n\n\n## Diverse Programs\n\n### Program D1 (Score: 0.5775)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (improved)\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square\n    that attempts to maximize the sum of their radii by placing\n    centers on a hexagonal lattice, selecting the 26 points closest\n    to the square center, and then maximizing radii with an iterative\n    relaxation that enforces border and non-overlap constraints.\n    Returns:\n        centers: (26,2) np.array\n        radii: (26,) np.array\n        sum_of_radii: float\n    \"\"\"\n    n = 26\n    center = np.array([0.5, 0.5])\n\n    # Build a hexagonal lattice and pick 26 centers nearest the square center.\n    # Choose spacing s so that lattice yields at least 26 points inside.\n    s = 0.12\n    found = False\n    for _ in range(30):\n        pts = []\n        dy = s * np.sqrt(3) / 2\n        # rows enough to cover the unit square\n        nrows = int(np.ceil(1.0 / dy)) + 4\n        for r in range(-nrows, nrows + 1):\n            y = 0.5 + r * dy\n            shift = (s / 2) if (r % 2) else 0.0\n            # columns wide enough\n            ncols = int(np.ceil(1.0 / s)) + 6\n            for c in range(-ncols, ncols + 1):\n                x = 0.5 + c * s + shift\n                if 0.0 <= x <= 1.0 and 0.0 <= y <= 1.0:\n                    pts.append((x, y))\n        pts = np.array(pts)\n        if pts.shape[0] >= n:\n            found = True\n            break\n        s *= 0.9  # reduce spacing to get more points\n    if not found:\n        # fallback uniform grid\n        gx = gy = int(np.ceil(np.sqrt(n)))\n        xs = np.linspace(1.0/(2*gx), 1-1.0/(2*gx), gx)\n        ys = np.linspace(1.0/(2*gy), 1-1.0/(2*gy), gy)\n        pts = np.array([(x,y) for y in ys for x in xs])\n\n    # pick the n points closest to the square center\n    dists = np.sum((pts - center)**2, axis=1)\n    idx = np.argsort(dists)[:n]\n    centers = pts[idx].copy()\n\n    # Compute maximal radii by iterative relaxation:\n    radii = np.minimum(centers[:,0], centers[:,1])\n    radii = np.minimum(radii, 1 - centers[:,0])\n    radii = np.minimum(radii, 1 - centers[:,1])\n    # Start with border-limited radii (conservative)\n    # Now iteratively remove overlaps and try to grow circles where possible.\n    coords = centers\n    pair_d = np.sqrt(((coords[:,None,:] - coords[None,:,:])**2).sum(axis=2))\n    np.fill_diagonal(pair_d, np.inf)\n\n    for it in range(500):\n        old = radii.copy()\n        # 1) enforce pairwise non-overlap by proportional scaling when needed\n        # iterate over all pairs; using vectorization: find violating pairs\n        ri = radii[:,None]\n        rj = radii[None,:]\n        sumr = ri + rj\n        viol = sumr > pair_d\n        if np.any(viol):\n            # For every violating pair (i,j) scale both by factor d/(r_i+r_j)\n            # but accumulate the most restrictive scale per circle\n            scales = np.ones_like(sumr)\n            scales[viol] = pair_d[viol] / sumr[viol]\n            # For each circle take min scale across its pairs (most limiting)\n            min_scale_i = scales.min(axis=1)\n            radii *= min_scale_i\n            # recompute matrices after scaling\n            ri = radii[:,None]; rj = radii[None,:]; sumr = ri + rj\n        # 2) enforce border limits\n        border = np.minimum(np.minimum(centers[:,0], centers[:,1]),\n                            np.minimum(1-centers[:,0], 1-centers[:,1]))\n        radii = np.minimum(radii, border)\n        # 3) attempt to grow each circle up to the slack allowed by neighbors & border\n        # compute for each i: allowed = min(border_i, min_j (d_ij - r_j))\n        allowed = np.full(n, np.inf)\n        # d_ij - r_j\n        slack = pair_d - radii[None,:]\n        # avoid self\n        slack[np.isinf(pair_d)] = np.inf\n        min_slack = slack.min(axis=1)\n        allowed = np.minimum(border, min_slack)\n        # prevent negative allowed\n        allowed = np.maximum(allowed, 0.0)\n        # relaxed growth toward allowed (avoid oscillations)\n        grow_mask = allowed > radii\n        radii[grow_mask] = (radii[grow_mask] + allowed[grow_mask]) / 2.0\n\n        if np.max(np.abs(radii - old)) < 1e-7:\n            break\n\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n\n# EVOLVE-BLOCK-END\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.5)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nKey features: Alternative approach to validity, Alternative approach to sum_radii\n\n### Program D2 (Score: 0.5699)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (explicit hex-like constructor)\nProduces deterministic centers for 26 circles and computes maximal feasible radii\nvia a Gauss-Seidel style iterative solver for the pairwise non-overlap constraints.\n\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square.\n\n    Returns:\n        centers: np.array of shape (26, 2)\n        radii: np.array of shape (26,)\n        sum_radii: float\n    \"\"\"\n    n = 26\n\n    # Row pattern chosen to approximate a truncated hexagonal packing:\n    # counts per row sum to 26 (5 + 6 + 5 + 6 + 4 = 26)\n    row_counts = [5, 6, 5, 6, 4]\n    s = 0.12  # nominal horizontal spacing for the lattice; will be scaled to fit\n\n    coords = []\n    for r, count in enumerate(row_counts):\n        # vertical spacing in hex grid is sqrt(3)/2 * s\n        y = r * (np.sqrt(3) / 2.0) * s\n        # alternate rows are offset by s/2\n        x0 = 0.5 * s if (r % 2) else 0.0\n        for k in range(count):\n            coords.append([x0 + k * s, y])\n\n    centers = np.array(coords)\n    assert centers.shape[0] == n\n\n    # Normalize bounding box to fit nicely inside [pad, 1 - pad]^2\n    pad = 0.02\n    mins = centers.min(axis=0)\n    maxs = centers.max(axis=0)\n    span = maxs - mins\n    # handle degenerate span (shouldn't happen here)\n    span[span == 0] = 1.0\n    centers = (centers - mins) / span  # now in [0,1] box\n    centers = centers * (1 - 2 * pad) + pad\n\n    # Move packing slightly toward center to reduce boundary crowding (small translation)\n    # This preserves relative arrangement but can improve achievable radii.\n    shift = np.array([0.0, 0.0])  # kept zero for determinism; tweak if desired\n    centers = np.clip(centers + shift, pad, 1 - pad)\n\n    radii = compute_max_radii(centers)\n\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers, max_iter=5000, tol=1e-8):\n    \"\"\"\n    Compute (approximately) maximal radii for given fixed centers so that:\n      - each circle stays inside the unit square\n      - circles do not overlap: r_i + r_j <= dist(c_i, c_j)\n\n    Uses an iterative Gauss-Seidel style update:\n      r_i <- min(distance_to_border_i, min_j max(0, dist_ij - r_j))\n\n    This finds the largest feasible radii vector consistent with the constraints.\n    \"\"\"\n    n = centers.shape[0]\n    # initial upper bounds from border distances\n    border = np.minimum.reduce([centers[:, 0], centers[:, 1], 1 - centers[:, 0], 1 - centers[:, 1]])\n    radii = border.copy()\n\n    # Precompute pairwise distances\n    dif = centers[:, None, :] - centers[None, :, :]  # shape (n,n,2)\n    dists = np.sqrt(np.sum(dif * dif, axis=2))\n    # Avoid self-distances of zero causing issues\n    np.fill_diagonal(dists, np.inf)\n\n    for it in range(int(max_iter)):\n        max_change = 0.0\n        # Gauss-Seidel: update each radius using the most recent values of others\n        for i in range(n):\n            # start with border constraint\n            limit = border[i]\n            # neighbor constraints: r_i <= dist_ij - r_j for all j\n            # compute dist_ij - r_j and take the minimum (but not negative)\n            # Use vectorized operations for speed inside loop\n            limits_from_neighbors = dists[i, :] - radii\n            # ignore self (set to +inf)\n            limits_from_neighbors[i] = np.inf\n            # the effective limit is min(border, min_j limits_from_neighbors)\n            neighbor_min = limits_from_neighbors.min()\n            if neighbor_min < limit:\n                limit = max(0.0, neighbor_min)\n            change = abs(radii[i] - limit)\n            if change:\n                radii[i] = limit\n                if change > max_change:\n                    max_change = change\n        if max_change < tol:\n            break\n\n    # Final safeguard: clip radii nonnegative and to border\n    radii = np.clip(radii, 0.0, border)\n    return radii\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nKey features: Alternative approach to validity, Alternative approach to sum_radii\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 0.3642, Type: Exploratory)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square\n    that attempts to maximize the sum of their radii.\n\n    Returns:\n        Tuple of (centers, radii, sum_of_radii)\n        centers: np.array of shape (26, 2) with (x, y) coordinates\n        radii: np.array of shape (26) with radius of each circle\n        sum_of_radii: Sum of all radii\n    \"\"\"\n    # Initialize arrays for 26 circles\n    n = 26\n    centers = np.zeros((n, 2))\n\n    # Place circles in a structured pattern\n    # This is a simple pattern - evolution will improve this\n\n    # First, place a large circle in the center\n    centers[0] = [0.5, 0.5]\n\n    # Place 8 circles around it in a ring\n    for i in range(8):\n        angle = 2 * np.pi * i / 8\n        centers[i + 1] = [0.5 + 0.3 * np.cos(angle), 0.5 + 0.3 * np.sin(angle)]\n\n    # Place 16 more circles in an outer ring\n    for i in range(16):\n        angle = 2 * np.pi * i / 16\n        centers[i + 9] = [0.5 + 0.7 * np.cos(angle), 0.5 + 0.7 * np.sin(angle)]\n\n    # Additional positioning adjustment to make sure all circles\n    # are inside the square and don't overlap\n    # Clip to ensure everything is inside the unit square\n    centers = np.clip(centers, 0.01, 0.99)\n\n    # Compute maximum valid radii for this configuration\n    radii = compute_max_radii(centers)\n\n    # Calculate the sum of radii\n    sum_radii = np.sum(radii)\n\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers):\n    \"\"\"\n    Compute the maximum possible radii for each circle position\n    such that they don't overlap and stay within the unit square.\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n\n    Returns:\n        np.array of shape (n) with radius of each circle\n    \"\"\"\n    n = centers.shape[0]\n    radii = np.ones(n)\n\n    # First, limit by distance to square borders\n    for i in range(n):\n        x, y = centers[i]\n        # Distance to borders\n        radii[i] = min(x, y, 1 - x, 1 - y)\n\n    # Then, limit by distance to other circles\n    # Each pair of circles with centers at distance d can have\n    # sum of radii at most d to avoid overlap\n    for i in range(n):\n        for j in range(i + 1, n):\n            dist = np.sqrt(np.sum((centers[i] - centers[j]) ** 2))\n\n            # If current radii would cause overlap\n            if radii[i] + radii[j] > dist:\n                # Scale both radii proportionally\n                scale = dist / (radii[i] + radii[j])\n                radii[i] *= scale\n                radii[j] *= scale\n\n    return radii\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.5)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\")\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii}\")\n    # AlphaEvolve improved this to 2.635\n\n    # Uncomment to visualize:\n    visualize(centers, radii)\n\n```\nUnique approach: Excellent validity (1.000), Excellent sum_radii (0.960), NumPy-based implementation\n\n### Inspiration 2 (Score: 0.6773, Type: Alternative)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (improved deterministic hex-like constructor)\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    \"\"\"\n    Construct an explicit arrangement of 26 circles in the unit square.\n    Returns centers (26,2), radii (26,), sum_of_radii.\n    Approach:\n      - Build a compact hexagonal/triangular-like lattice with row counts summing to 26.\n      - Scale and center the lattice to fill the unit square with a small margin.\n      - Apply a tiny deterministic perturbation to break perfect symmetry.\n      - Compute maximum feasible radii by iteratively enforcing border and pairwise constraints.\n    \"\"\"\n    n = 26\n    # row pattern summing to 26 (compact hex-like)\n    row_counts = [5, 6, 5, 5, 5]  # total 26\n    sqrt3 = np.sqrt(3.0)\n\n    # create centered integer lattice coordinates (unit horizontal spacing)\n    points = []\n    for r_idx, cnt in enumerate(row_counts):\n        # x positions centered about 0\n        xs = [(i - (cnt - 1) / 2.0) for i in range(cnt)]\n        y = r_idx * (sqrt3 / 2.0)\n        for x in xs:\n            points.append((x, y))\n    pts = np.array(points)  # shape (26,2)\n\n    # bounding box of raw pattern\n    min_xy = pts.min(axis=0)\n    max_xy = pts.max(axis=0)\n    W, H = (max_xy - min_xy)\n\n    # leave a small margin within unit square\n    margin = 0.01\n    # scale to fit inside unit square with margin\n    if W <= 0 or H <= 0:\n        scale = 1.0\n    else:\n        scale = min((1 - 2 * margin) / W, (1 - 2 * margin) / H)\n\n    # apply scale and translate to center in unit square\n    pts = (pts - min_xy) * scale + margin\n    # center residual space\n    residual = 1.0 - (pts.max(axis=0) - pts.min(axis=0))\n    pts += residual / 2.0 - pts.min(axis=0)\n\n    # deterministic tiny perturbation (breaks perfect symmetry, helps border packing)\n    # uses sines to produce reproducible small offsets\n    jitter_scale = 0.006 * min(scale, 1.0)\n    idxs = np.arange(len(pts))\n    pts[:, 0] += jitter_scale * np.sin(3.3 * idxs + 0.7)\n    pts[:, 1] += jitter_scale * np.cos(2.7 * idxs + 1.1)\n\n    # clip to avoid numerical leaving unit square\n    pts = np.clip(pts, 0.0001, 0.9999)\n\n    # compute maximal feasible radii with an iterative proportional correction (deterministic)\n    radii = compute_max_radii_iterative(pts)\n\n    sum_radii = float(np.sum(radii))\n    return pts, radii, sum_radii\n\ndef compute_max_radii_iterative(centers, max_iter=200, tol=1e-8):\n    \"\"\"\n    Iteratively compute feasible radii maximizing sum under:\n      r_i >= 0\n      r_i + r_j <= dist(center_i, center_j)\n      r_i <= dist_to_border(center_i)\n    The simple iterative proportional scaling converges reliably for these structured layouts.\n    \"\"\"\n    n = centers.shape[0]\n    # initial radii limited by border\n    border = np.minimum.reduce([centers[:,0], centers[:,1], 1-centers[:,0], 1-centers[:,1]])\n    radii = border.copy()\n    # precompute pairwise distances\n    dx = centers[:, 0:1] - centers[:, 0:1].T\n    dy = centers[:, 1:2] - centers[:, 1:2].T\n    dists = np.sqrt(np.maximum(1e-12, dx*dx + dy*dy))\n\n    for it in range(max_iter):\n        changed = False\n        # enforce border constraint explicitly each iteration\n        radii = np.minimum(radii, border)\n        # loop through pairs, scale down violating pairs proportionally\n        for i in range(n):\n            ri = radii[i]\n            # vectorized across j>i\n            js = np.arange(i+1, n)\n            if js.size == 0:\n                continue\n            rj = radii[js]\n            dij = dists[i, js]\n            # indices where violation occurs\n            overlap = ri + rj > dij + 1e-12\n            if not np.any(overlap):\n                continue\n            # compute scales for violating pairs\n            scales = np.empty_like(rj)\n            scales[:] = 1.0\n            scales[overlap] = (dij[overlap] / (ri + rj[overlap]))\n            # apply proportional shrink: multiply both by min scale across their interactions\n            # For stability apply the smallest scale for the pair (i and each j)\n            # accumulate minimal scale for each index\n            min_scale_i = scales[overlap].min() if np.any(overlap) else 1.0\n            if min_scale_i < 1.0 - 1e-12:\n                radii[i] *= min_scale_i\n                changed = True\n            # for the j's apply their pairwise scales\n            for k, j in enumerate(js):\n                sc = scales[k]\n                if sc < 1.0 - 1e-12:\n                    radii[j] *= sc\n                    changed = True\n        # re-apply border clamp\n        radii = np.minimum(radii, border)\n        if not changed:\n            break\n    # ensure non-negative\n    radii = np.clip(radii, 0.0, None)\n    return radii\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nUnique approach: Modification: Full rewrite, Excellent validity (1.000), Excellent sum_radii (1.785)\n\n### Inspiration 3 (Score: 0.4846, Type: Experimental)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (improved deterministic constructor)\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square\n    that attempts to maximize the sum of their radii using a\n    deterministic hex-like layered layout and an iterative radius relaxation.\n    Returns:\n        centers: np.array (26,2)\n        radii: np.array (26,)\n        sum_of_radii: float\n    \"\"\"\n    n = 26\n\n    # Row structure (sensible hex-like layering that sums to 26)\n    rows = [5, 6, 6, 6, 3]  # total 26\n    num_rows = len(rows)\n    max_k = max(rows)\n\n    # small margin from walls\n    edge = 0.01\n\n    # choose a candidate horizontal spacing s so widest row fits comfortably\n    # include a safety 0.5*s to accommodate offset rows\n    s_h = (1.0 - 2 * edge) / (max_k - 1 + 0.5)\n\n    # vertical hex spacing\n    s_v = s_h * np.sqrt(3) / 2.0\n\n    # ensure vertical fit: adjust s_h if vertical span is too big\n    if (num_rows - 1) * s_v > (1.0 - 2 * edge):\n        s_v = (1.0 - 2 * edge) / (num_rows - 1)\n        s_h = s_v * 2.0 / np.sqrt(3)\n\n    # build centers: y positions centered, x positions centered for each row\n    span_y = (num_rows - 1) * s_v\n    y0 = 0.5 - span_y / 2.0\n    centers = []\n    for i, k in enumerate(rows):\n        y = y0 + i * s_v\n        # center the k points horizontally with spacing s_h\n        x0 = 0.5 - ((k - 1) * s_h) / 2.0\n        for j in range(k):\n            x = x0 + j * s_h\n            # clip to remain inside [edge,1-edge]\n            x = np.clip(x, edge, 1 - edge)\n            y = np.clip(y, edge, 1 - edge)\n            centers.append((x, y))\n\n    centers = np.array(centers)\n    assert centers.shape[0] == n\n\n    # Compute radii via iterative relaxation:\n    radii = compute_max_radii(centers)\n\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers):\n    \"\"\"\n    Compute maximum non-overlapping radii for given centers inside unit square.\n    Uses iterative proportional reduction for violating pairs and enforces wall limits.\n    \"\"\"\n    n = centers.shape[0]\n    eps = 1e-12\n\n    # initial radii limited by walls\n    wall_dists = np.minimum.reduce([centers[:, 0], centers[:, 1], 1 - centers[:, 0], 1 - centers[:, 1]])\n    # start with wall distances (largest possible individually)\n    radii = wall_dists.copy()\n\n    # Precompute pairwise distances\n    diffs = centers[:, None, :] - centers[None, :, :]\n    dists = np.sqrt(np.sum(diffs ** 2, axis=2)) + np.eye(n)  # avoid zeros on diagonal\n\n    # Iteratively fix overlaps: ensure r_i + r_j <= d_ij\n    # We'll loop and reduce both radii proportionally when violation occurs.\n    max_iters = 300\n    for _ in range(max_iters):\n        changed = False\n\n        # enforce pairwise constraints\n        for i in range(n):\n            # vectorize j loop for speed\n            # compute j>i to avoid duplicate work\n            for j in range(i + 1, n):\n                d = dists[i, j]\n                if d <= eps:\n                    # centers coincide (unlikely in our constructor) -> collapse radii\n                    if radii[i] > 0:\n                        radii[i] = 0.0\n                        changed = True\n                    if radii[j] > 0:\n                        radii[j] = 0.0\n                        changed = True\n                    continue\n                s = radii[i] + radii[j]\n                if s > d - 1e-9:\n                    # proportional scale to remove overlap (leave tiny slack)\n                    factor = (d - 1e-9) / s\n                    # To avoid zeroing tiny distances, if factor<0 set to very small positive\n                    factor = max(factor, 1e-9)\n                    # Scale both but prefer to preserve larger relative sizes (proportional)\n                    radii[i] *= factor\n                    radii[j] *= factor\n                    changed = True\n\n        # enforce wall constraints\n        for i in range(n):\n            if radii[i] > wall_dists[i]:\n                radii[i] = wall_dists[i]\n                changed = True\n\n        if not changed:\n            break\n\n    # Final pass: clamp tiny negatives / numerical issues\n    radii = np.maximum(radii, 0.0)\n    return radii\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor='k')\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nUnique approach: Modification: Full rewrite, Excellent validity (1.000), Excellent sum_radii (1.277)\n\n# Current Program\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for packing n=26 circles in the unit square.\nUses an explicit hex-like layered layout (rows [5,6,6,6,3]) and a stable\niterative proportional relaxation to compute maximal non-overlapping radii.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    rows = [5, 6, 6, 6, 3]  # sums to 26\n    num_rows = len(rows)\n    max_k = max(rows)\n\n    edge = 0.01  # margin from square boundary\n\n    # horizontal spacing chosen so the widest row fits, allowing half-step offsets\n    s_h = (1.0 - 2 * edge) / (max_k - 1 + 0.5)\n    s_v = s_h * np.sqrt(3) / 2.0  # hex vertical spacing\n\n    # If vertical span too large, rescale s_h and s_v to fit\n    if (num_rows - 1) * s_v > (1.0 - 2 * edge):\n        s_v = (1.0 - 2 * edge) / (num_rows - 1)\n        s_h = s_v * 2.0 / np.sqrt(3)\n\n    # center rows vertically and each row horizontally\n    span_y = (num_rows - 1) * s_v\n    y0 = 0.5 - span_y / 2.0\n    centers = []\n    for i, k in enumerate(rows):\n        y = y0 + i * s_v\n        x0 = 0.5 - ((k - 1) * s_h) / 2.0\n        for j in range(k):\n            x = x0 + j * s_h\n            # clip to margin-safe region\n            x = float(np.clip(x, edge, 1 - edge))\n            yc = float(np.clip(y, edge, 1 - edge))\n            centers.append((x, yc))\n\n    centers = np.array(centers)\n    assert centers.shape[0] == n\n\n    radii = compute_max_radii(centers)\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\ndef compute_max_radii(centers, max_iters=400):\n    \"\"\"\n    Initialize radii to distance to nearest wall, then iteratively enforce\n    pairwise constraints r_i + r_j <= d_ij by proportional reductions.\n    \"\"\"\n    n = centers.shape[0]\n    # limit by walls\n    wall_dists = np.minimum.reduce([centers[:,0], centers[:,1], 1-centers[:,0], 1-centers[:,1]])\n    radii = wall_dists.copy()\n\n    # precompute pairwise distances\n    diffs = centers[:, None, :] - centers[None, :, :]\n    dists = np.sqrt(np.sum(diffs**2, axis=2))\n    eps = 1e-12\n\n    for _ in range(max_iters):\n        changed = False\n        # pairwise constraints\n        for i in range(n):\n            ri = radii[i]\n            for j in range(i+1, n):\n                rj = radii[j]\n                d = dists[i, j]\n                if d <= eps:\n                    # coincide: collapse both gently\n                    if radii[i] > 0:\n                        radii[i] = 0.0\n                        changed = True\n                    if radii[j] > 0:\n                        radii[j] = 0.0\n                        changed = True\n                    continue\n                s = ri + rj\n                # if overlap beyond tiny slack\n                if s > d - 1e-9:\n                    factor = max((d - 1e-9) / s, 1e-9)\n                    # scale both proportionally\n                    radii[i] *= factor\n                    radii[j] *= factor\n                    changed = True\n                    # update local ri/rj to propagate within inner loop\n                    ri = radii[i]\n                    rj = radii[j]\n        # enforce walls\n        for i in range(n):\n            if radii[i] > wall_dists[i]:\n                radii[i] = wall_dists[i]\n                changed = True\n        if not changed:\n            break\n\n    radii = np.maximum(radii, 0.0)\n    return radii\n\n# EVOLVE-BLOCK-END\n\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(6,6))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect(\"equal\")\n    for c, r in zip(centers, radii):\n        ax.add_patch(Circle(c, r, alpha=0.6, edgecolor='k'))\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # visualize(centers, radii)\n```\n\n# Task\nRewrite the program to improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: complexity, diversity\nDifferent solutions with similar fitness but different features are valuable.\nProvide the complete new program code.\n\nIMPORTANT: Make sure your rewritten program maintains the same inputs and outputs\nas the original program, but with improved internal implementation.\n\n```python\n# Your rewritten program here\n```", "responses": ["```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for packing n=26 circles in the unit square.\nExplicit hex-like layer layout tuned for 26 circles with a Gauss-Seidel\nstyle solver that finds (approximately) maximal feasible radii.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    # Compact hex-like row counts summing to 26 (tuned)\n    rows = [6, 5, 6, 5, 4]  # total 26\n    R = len(rows)\n\n    # Use horizontal spacing suitable for 6 columns\n    s = 1.0 / 6.0\n    h = s * np.sqrt(3.0) / 2.0\n\n    # vertical center\n    y0 = 0.5 - ((R - 1) * h) / 2.0\n\n    centers = []\n    for i, k in enumerate(rows):\n        y = y0 + i * h\n        left = 0.5 - ((k - 1) * s) / 2.0\n        xs = left + np.arange(k) * s\n        for x in xs:\n            centers.append((float(x), float(y)))\n    centers = np.array(centers[:n])\n    assert centers.shape[0] == n\n\n    radii = compute_max_radii_gs(centers)\n    sum_radii = float(radii.sum())\n    return centers, radii, sum_radii\n\ndef compute_max_radii_gs(centers, max_iter=2000, tol=1e-10):\n    \"\"\"Gauss-Seidel style update:\n       r_i <- min(border_i, max(0, min_j (d_ij - r_j)))\n       Uses most recent r_j values for updates (sequential).\n    \"\"\"\n    n = centers.shape[0]\n    # border limits\n    border = np.minimum.reduce([centers[:,0], centers[:,1], 1.0-centers[:,0], 1.0-centers[:,1]])\n    border = np.maximum(border, 0.0)\n\n    # pairwise distances\n    dif = centers[:, None, :] - centers[None, :, :]\n    dists = np.hypot(dif[...,0], dif[...,1])\n    np.fill_diagonal(dists, np.inf)\n\n    # initialize with border distances (conservative)\n    r = border.copy()\n\n    for it in range(max_iter):\n        max_change = 0.0\n        # sequential Gauss-Seidel updates\n        for i in range(n):\n            # neighbor constraints: r_i <= d_ij - r_j for all j\n            limits = dists[i, :] - r\n            limits[i] = np.inf  # ignore self\n            new = np.minimum(border[i], np.min(limits))\n            if new < 0.0:\n                new = 0.0\n            # clamp numeric noise\n            if not np.isfinite(new):\n                new = 0.0\n            change = abs(r[i] - new)\n            if change > 0:\n                r[i] = new\n                if change > max_change:\n                    max_change = change\n        if max_change < tol:\n            break\n\n    # final safety clamps\n    r = np.clip(r, 0.0, border)\n    return r\n\n# EVOLVE-BLOCK-END\n\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(6,6))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect(\"equal\")\n    ax.add_patch(plt.Rectangle((0,0),1,1,fill=False,edgecolor='k'))\n    for c, r in zip(centers, radii):\n        ax.add_patch(Circle(c, r, alpha=0.6, edgecolor='k'))\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # visualize(centers, radii)\n```"]}}, "artifacts_json": "{\"stdout\": \"Running in subprocess, Python version: 3.12.1 (v3.12.1:2305ca5144, Dec  7 2023, 17:23:38) [Clang 13.0.0 (clang-1300.0.29.30)]\\nProgram path: /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmpx9cuco94.py\\nCalling run_packing()...\\nrun_packing() returned successfully: sum_radii = 1.9999999999999991\\nResults saved to /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmp7bgs6gbz.py.results\\n\", \"stderr\": \"\"}", "artifact_dir": null}