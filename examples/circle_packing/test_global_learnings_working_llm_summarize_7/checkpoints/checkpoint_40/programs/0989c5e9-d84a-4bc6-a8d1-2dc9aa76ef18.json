{"id": "0989c5e9-d84a-4bc6-a8d1-2dc9aa76ef18", "code": "# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for n=26 circles in the unit square.\n\nStrategy:\n- Place centers in a compact hex-like block with row counts tuned for 26:\n  rows = [6,5,6,5,4].\n- Try a small deterministic set of horizontal/vertical compressions to\n  balance interior hex packing against edge effects.\n- For each candidate center set compute near-maximal feasible radii using\n  an aggressive grow-then-robust-shrink solver that tends to reach larger\n  total radii than a purely conservative half-neighbor rule.\nReturns centers (26,2), radii (26,), sum_radii.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    rows = [6, 5, 6, 5, 4]  # total 26\n    R = len(rows)\n    max_k = max(rows)\n    margin = 1e-6\n\n    # base horizontal spacing (fit widest row)\n    s0 = (1.0 - 2 * margin) / (max_k - 1) if max_k > 1 else 0.5\n\n    # Try a few deterministic compressions to mitigate edge effects\n    s_scales = np.array([1.00, 0.98, 0.96, 0.94])\n    y_scales = np.array([1.00, 0.98, 0.96])\n\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    for ss in s_scales:\n        for ys in y_scales:\n            s = s0 * ss\n            h = s * np.sqrt(3.0) / 2.0 * ys\n\n            # If vertical span too large, rescale to fit exactly\n            span_v = (R - 1) * h\n            if span_v > 1.0 - 2 * margin:\n                h = (1.0 - 2 * margin) / (R - 1)\n                s = h * 2.0 / np.sqrt(3.0)\n\n            y0 = 0.5 - ((R - 1) * h) / 2.0\n            centers = []\n            for i, k in enumerate(rows):\n                y = y0 + i * h\n                left = 0.5 - ((k - 1) * s) / 2.0\n                xs = left + np.arange(k) * s\n                for x in xs:\n                    centers.append([float(x), float(y)])\n            centers = np.array(centers[:n], dtype=float)\n            centers = np.clip(centers, margin, 1.0 - margin)\n\n            radii = compute_max_radii_aggressive(centers)\n            total = float(np.sum(radii))\n            if total > best_sum:\n                best_sum = total\n                best_centers = centers\n                best_radii = radii\n\n    return best_centers, best_radii, float(np.sum(best_radii))\n\n\ndef compute_max_radii_aggressive(centers, max_outer=300, max_inner=80, tol=1e-11):\n    \"\"\"\n    Aggressive grow-then-shrink solver:\n    - Start with a conservative safe radius (min(border, 0.5*nearest_pair)).\n    - Outer loop: set radii equal to the available allowed (min over neighbors and border).\n    - Inner loop: robustly resolve overlaps by per-pair proportional shrinking until none remain.\n    This approach often yields a larger feasible sum than purely incremental growth.\n    \"\"\"\n    n = centers.shape[0]\n    if n == 0:\n        return np.array([])\n\n    # border limits\n    border = np.minimum.reduce([centers[:, 0], centers[:, 1],\n                                1.0 - centers[:, 0], 1.0 - centers[:, 1]])\n    # pairwise distances\n    dif = centers[:, None, :] - centers[None, :, :]\n    D = np.hypot(dif[..., 0], dif[..., 1])\n    np.fill_diagonal(D, np.inf)\n\n    # initial safe radii\n    min_pair = np.min(D, axis=1)\n    radii = np.minimum(border, 0.5 * min_pair)\n    radii = np.maximum(radii, 0.0)\n\n    for outer in range(max_outer):\n        old = radii.copy()\n\n        # compute allowed assignment: allowed_i = min(border_i, min_j (D_ij - r_j))\n        slack = D - radii[None, :]   # D_ij - r_j\n        allowed = np.minimum(border, np.min(slack, axis=1))\n        allowed = np.maximum(allowed, 0.0)\n        radii = allowed.copy()\n\n        # inner robust shrink: resolve overlaps thoroughly\n        for inner in range(max_inner):\n            ri = radii[:, None]\n            rj = radii[None, :]\n            sumr = ri + rj\n            viol = sumr > D + 1e-14\n            if not np.any(viol):\n                break\n            # per-pair scale factors (<=1)\n            factors = np.ones_like(sumr)\n            factors[viol] = D[viol] / sumr[viol]\n            # per-circle minimal scale (most restrictive across pairs)\n            min_scale = factors.min(axis=1)\n            min_scale = np.maximum(min_scale, 1e-12)\n            radii *= min_scale\n            # enforce wall bounds after shrinking\n            radii = np.minimum(radii, border)\n\n        # small safety clamp and convergence test\n        radii = np.minimum(radii, border)\n        if np.max(np.abs(radii - old)) < tol:\n            break\n\n    radii = np.maximum(radii, 0.0)\n    return radii\n\n# EVOLVE-BLOCK-END\n\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")", "language": "python", "parent_id": "0939e9c7-48fd-4c99-bf70-ed51d8f3505e", "generation": 3, "timestamp": 1759961130.023558, "iteration_found": 39, "metrics": {"validity": 1.0, "sum_radii": 2.1999995999999995, "target_ratio": 0.834914459203036, "combined_score": 0.834914459203036, "eval_time": 0.05444192886352539}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Full rewrite", "parent_metrics": {"validity": 1.0, "sum_radii": 2.166666666666666, "target_ratio": 0.8222643896268184, "combined_score": 0.8222643896268184, "eval_time": 0.060469865798950195}, "island": 0}, "prompts": {"full_rewrite_user": {"system": "You are an expert mathematician specializing in circle packing problems and computational geometry. Your task is to improve a constructor function that directly produces a specific arrangement of 26 circles in a unit square, maximizing the sum of their radii. The AlphaEvolve paper achieved a sum of 2.635 for n=26.\n\nKey geometric insights:\n- Circle packings often follow hexagonal patterns in the densest regions\n- Maximum density for infinite circle packing is pi/(2*sqrt(3)) \u2248 0.9069\n- Edge effects make square container packing harder than infinite packing\n- Circles can be placed in layers or shells when confined to a square\n- Similar radius circles often form regular patterns, while varied radii allow better space utilization\n- Perfect symmetry may not yield the optimal packing due to edge effects\n\nFocus on designing an explicit constructor that places each circle in a specific position, rather than an iterative search algorithm.\n\n\n## Evolution Insights (Global Learnings)\n\n### Common Pitfalls:\n\u26a0\ufe0f (seen 4x) A change that reduced the computed aggregate radius (sum_radii) by ~12% \u2014 e.g., introducing an unintended scaling/normalization, an extra clamp, or an off-by-one in the radius accumulation \u2014 caused target_ratio and combined_score to fall from 0.3642 to 0.3205 across iterations 1\u201328. When modifying geometry/aggregation code, avoid introducing implicit scalings or clamps without explicit tests; add unit/regression checks for sum_radii and dependent metrics, log intermediate radius values, and review precision/normalization changes to catch unintended shrinkage early.\n\u26a0\ufe0f (seen 4x) A code change that reduced the aggregate coverage/allocatable radius (sum_radii fell from 0.9598 to 0.8446)\u2014for example shrinking buffers, tightening placement radii, or simplifying the packing/placement heuristic\u2014caused a consistent ~12% drop in target_ratio and combined_score across multiple iterations. Avoid making capacity-reducing or constraint-tightening optimizations without targeted benchmarks and automated regression checks; whenever you adjust allocation radii, buffer/window sizes, or packing heuristics, compare against prior baselines to catch and revert harmful target_ratio regressions.\n\u26a0\ufe0f (seen 4x) A recent change effectively reduced the search/matching radius (sum_radii fell from 0.9598 to 0.8446), tightening matching/coverage and causing the target_ratio/combined_score to drop ~12% (0.364 \u2192 0.321). When modifying geometric parameters, normalization, or pruning logic, avoid hard\u2011coding or implicitly scaling radii\u2014always run targeted regression tests for combined_score/target_ratio and make such parameters configurable so you can quickly tune or roll back if performance regresses.\n\u26a0\ufe0f (seen 2x) A change in the evaluation path\u2014likely introducing caching, short-circuiting, or a loop refactor in the eval routine\u2014reduced per-evaluation time by ~11% (0.0605 \u2192 0.0538) across iterations 26\u201330 but coincided with small shifts in domain metrics (sum_radii and target_ratio), indicating the optimization altered ordering or stateful behavior of the evaluator. When making similar performance changes, avoid altering execution order or hidden side effects; add regression tests that assert both performance and domain-specific numeric invariants and run controlled A/B comparisons to catch unintended behavioral changes.\n\u26a0\ufe0f (seen 1x) A change short-circuited or replaced the radii/target-ratio computation (or the object-initialization path) with a no-op/default-zero path so sum_radii and target_ratio collapsed to 0.0 and the overall validity check flipped from valid to invalid (1.000 \u2192 0.000).  \n\nWhen modifying initialization or computation code, avoid introducing default-zero returns or gating that skips construction; add unit/regression tests and assertions that core invariants (e.g., sum_radii > 0, validity == true) remain true and include CI checks to catch regressions early.", "user": "# Current Program Information\n- Fitness: 0.8223\n- Feature coordinates: No feature coordinates\n- Focus areas: - Fitness declined: 0.8306 \u2192 0.8223. Consider revising recent changes.\n- Consider simplifying - code length exceeds 500 characters\n\n## Last Execution Output\n\n### stdout\n```\nRunning in subprocess, Python version: 3.12.1 (v3.12.1:2305ca5144, Dec  7 2023, 17:23:38) [Clang 13.0.0 (clang-1300.0.29.30)]\nProgram path: /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmp0dwhzwed.py\nCalling run_packing()...\nrun_packing() returned successfully: sum_radii = 2.166666666666666\nResults saved to /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmphzblm94n.py.results\n\n```\n\n### stderr\n```\n\n```\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 3\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.1886, target_ratio: 0.8306, combined_score: 0.8306, eval_time: 0.0538\n- Outcome: Mixed results\n\n### Attempt 2\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.1886, target_ratio: 0.8306, combined_score: 0.8306, eval_time: 0.0537\n- Outcome: Mixed results\n\n### Attempt 1\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.2000, target_ratio: 0.8349, combined_score: 0.8349, eval_time: 0.0548\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.8349)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for n=26 circles in the unit square.\n\nImprovements over prior versions:\n- Small deterministic grid-search over horizontal/vertical spacing scales to\n  pick the best-shaped hex-like block for 26 circles.\n- A more aggressive growth-then-robust-shrink radii solver that fully expands\n  radii toward slack then repeatedly resolves overlaps (stronger convergence\n  to a larger feasible sum of radii).\nReturns centers (26,2), radii (26,), sum_radii.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    rows = [6, 5, 6, 5, 4]  # total 26\n    R = len(rows)\n    max_k = max(rows)\n    margin = 1e-6\n    # base horizontal spacing (widest row)\n    if max_k > 1:\n        s0 = (1.0 - 2 * margin) / (max_k - 1)\n    else:\n        s0 = 0.5\n\n    # deterministic candidate scales (tries a few compressions to balance edge effects)\n    s_scales = np.array([1.00, 0.98, 0.96, 0.94, 0.92])\n    y_scales = np.array([1.00, 0.98, 0.96])\n\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    for ss in s_scales:\n        for ys in y_scales:\n            s = s0 * ss\n            h = s * np.sqrt(3.0) / 2.0 * ys\n            # if vertical span too large, rescale to fit exactly\n            span_v = (R - 1) * h\n            if span_v > 1.0 - 2 * margin:\n                h = (1.0 - 2 * margin) / (R - 1)\n                s = h * 2.0 / np.sqrt(3.0)\n            # build centered rows\n            y0 = 0.5 - ((R - 1) * h) / 2.0\n            centers = []\n            for i, k in enumerate(rows):\n                y = y0 + i * h\n                left = 0.5 - ((k - 1) * s) / 2.0\n                xs = left + np.arange(k) * s\n                for x in xs:\n                    centers.append([float(x), float(y)])\n            centers = np.array(centers[:n], dtype=float)\n            # minor safeguard: clip into unit square (numerical)\n            centers = np.clip(centers, margin, 1.0 - margin)\n\n            radii = compute_max_radii_aggressive(centers)\n            sumr = float(np.sum(radii))\n            if sumr > best_sum:\n                best_sum = sumr\n                best_centers = centers\n                best_radii = radii\n\n    return best_centers, best_radii, float(np.sum(best_radii))\n\n\ndef compute_max_radii_aggressive(centers, max_outer=300, max_inner=60, tol=1e-10):\n    \"\"\"\n    Aggressive grow-then-shrink solver:\n    - start with conservative radii (min border, 0.5*nearest)\n    - repeatedly:\n        * compute allowed = min(border, min_j (D_ij - r_j)) and set radii = allowed\n        * run an inner loop that repeatedly resolves overlaps by proportional scaling\n          (using the most restrictive scale per circle) until no overlaps remain\n    This often reaches a larger feasible sum than tiny incremental growth steps.\n    \"\"\"\n    n = centers.shape[0]\n    if n == 0:\n        return np.array([])\n\n    # border limits\n    border = np.minimum.reduce([centers[:, 0], centers[:, 1],\n                                1.0 - centers[:, 0], 1.0 - centers[:, 1]])\n    # pairwise distances\n    dif = centers[:, None, :] - centers[None, :, :]\n    D = np.hypot(dif[..., 0], dif[..., 1])\n    np.fill_diagonal(D, np.inf)\n\n    # initial safe radii\n    min_pair = np.min(D, axis=1)\n    radii = np.minimum(border, 0.5 * min_pair)\n    radii = np.maximum(radii, 0.0)\n\n    for outer in range(max_outer):\n        old = radii.copy()\n\n        # compute allowed growth (full assignment)\n        slack = D - radii[None, :]   # allowed remaining toward each other\n        allowed = np.minimum(border, np.min(slack, axis=1))\n        allowed = np.maximum(allowed, 0.0)\n        radii = allowed.copy()\n\n        # inner robust shrink: resolve overlaps thoroughly\n        for inner in range(max_inner):\n            ri = radii[:, None]\n            rj = radii[None, :]\n            sumr = ri + rj\n            viol = sumr > D + 1e-14\n            if not np.any(viol):\n                break\n            # per-pair scale factors (<=1)\n            factors = np.ones_like(sumr)\n            factors[viol] = D[viol] / sumr[viol]\n            # per-circle minimal scale\n            min_scale = factors.min(axis=1)\n            # clamp to avoid underflow\n            min_scale = np.maximum(min_scale, 1e-12)\n            radii *= min_scale\n            # ensure wall limits\n            radii = np.minimum(radii, border)\n\n        # small relaxation to avoid infinite oscillation:\n        radii = np.minimum(radii, border)\n\n        if np.max(np.abs(radii - old)) < tol:\n            break\n\n    radii = np.maximum(radii, 0.0)\n    return radii\n\n# EVOLVE-BLOCK-END\n\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.2000), Performs well on target_ratio (0.8349), Performs well on combined_score (0.8349), Performs well on eval_time (0.0548)\n\n### Program 2 (Score: 0.8306)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for packing n=26 circles in the unit square.\n\nDesign:\n- Place centers in a compact hex-like layered layout tuned for 26 circles: rows [6,5,6,5,4].\n- Compute near-maximal radii for fixed centers via a stable vectorized relaxation:\n  resolve overlaps with proportional shrinking (most restrictive per circle),\n  enforce wall constraints, then gently grow toward available slack to increase\n  the sum of radii. Deterministic and efficient.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    rows = [6, 5, 6, 5, 4]  # sum = 26\n    R = len(rows)\n    max_k = max(rows)\n\n    # small margin so centers remain strictly inside\n    margin = 1e-9\n    s = (1.0 - 2 * margin) / (max_k - 1) if max_k > 1 else 0.5\n    h = s * np.sqrt(3.0) / 2.0\n\n    # if vertical span too large, scale spacing to fit\n    span_v = (R - 1) * h\n    if span_v > 1.0 - 2 * margin:\n        h = (1.0 - 2 * margin) / (R - 1)\n        s = h * 2.0 / np.sqrt(3.0)\n\n    y0 = 0.5 - ((R - 1) * h) / 2.0\n    centers = []\n    for i, k in enumerate(rows):\n        y = y0 + i * h\n        # center each row horizontally (no alternating offset to keep a compact block)\n        left = 0.5 - ((k - 1) * s) / 2.0\n        xs = left + np.arange(k) * s\n        for x in xs:\n            centers.append([float(x), float(y)])\n    centers = np.array(centers[:n], dtype=float)\n\n    radii = compute_max_radii(centers)\n    return centers, radii, float(np.sum(radii))\n\ndef compute_max_radii(centers, max_iters=1200, tol=1e-10):\n    n = centers.shape[0]\n    if n == 0:\n        return np.array([])\n\n    # wall-limited initial radii\n    border = np.minimum.reduce([centers[:,0], centers[:,1],\n                                1.0 - centers[:,0], 1.0 - centers[:,1]])\n    radii = border.copy()\n\n    if n == 1:\n        return radii\n\n    # pairwise center distances\n    diffs = centers[:, None, :] - centers[None, :, :]\n    D = np.hypot(diffs[...,0], diffs[...,1])\n    np.fill_diagonal(D, np.inf)\n\n    for _ in range(max_iters):\n        old = radii.copy()\n\n        # Resolve overlaps by proportional shrinking:\n        ri = radii[:, None]\n        rj = radii[None, :]\n        sumr = ri + rj\n        viol = sumr > D\n        if np.any(viol):\n            factors = np.ones_like(sumr)\n            factors[viol] = D[viol] / sumr[viol]\n            # for each circle take the most restrictive scale (min across pairs)\n            min_scale = factors.min(axis=1)\n            min_scale = np.maximum(min_scale, 1e-12)\n            radii *= min_scale\n\n        # enforce walls\n        radii = np.minimum(radii, border)\n\n        # Controlled growth toward allowed maxima:\n        # allowed_i = min(border_i, min_j (D_ij - r_j))\n        slack = D - radii[None, :]  # D_ij - r_j\n        allowed = np.minimum(border, slack.min(axis=1))\n        allowed = np.maximum(allowed, 0.0)\n        grow = allowed > radii + 1e-14\n        radii[grow] = 0.5 * (radii[grow] + allowed[grow])\n\n        if np.max(np.abs(radii - old)) < tol:\n            break\n\n    return np.maximum(radii, 0.0)\n\n# EVOLVE-BLOCK-END\n\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    \"\"\"Optional visualization (requires matplotlib)\"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(6,6))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect('equal')\n    for c, r in zip(centers, radii):\n        ax.add_patch(Circle(c, r, alpha=0.6, edgecolor='k'))\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, s = run_packing()\n    print(f\"Sum of radii: {s:.6f}\")\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.1886), Performs well on target_ratio (0.8306), Performs well on combined_score (0.8306), Performs well on eval_time (0.0537)\n\n### Program 3 (Score: 0.8306)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for n=26 circles in the unit square.\n\nDesign:\n- Place centers in a compact hex-like layered layout tuned for 26 circles: rows [6,5,6,5,4].\n- Compute near-maximal non-overlapping radii for fixed centers via a stable,\n  vectorized iterative relaxation: (1) resolve overlaps by per-pair proportional\n  shrinking using the most restrictive scale per circle, (2) enforce wall limits,\n  (3) perform controlled growth toward available slack to increase the sum.\nReturns centers (26,2), radii (26,), sum_radii.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    rows = [6, 5, 6, 5, 4]  # sums to 26\n    R = len(rows)\n    max_k = max(rows)\n\n    # horizontal spacing chosen to nearly fill unit width for widest row\n    margin = 1e-6\n    s = (1.0 - 2 * margin) / (max_k - 1) if max_k > 1 else 0.5\n    h = s * np.sqrt(3.0) / 2.0\n\n    # If vertical span would exceed unit, scale down spacing\n    span_v = (R - 1) * h\n    if span_v > 1.0 - 2 * margin:\n        h = (1.0 - 2 * margin) / (R - 1)\n        s = h * 2.0 / np.sqrt(3.0)\n\n    # build centered rows deterministically\n    y0 = 0.5 - ((R - 1) * h) / 2.0\n    centers = []\n    for i, k in enumerate(rows):\n        y = y0 + i * h\n        left = 0.5 - ((k - 1) * s) / 2.0\n        xs = left + np.arange(k) * s\n        for x in xs:\n            centers.append([float(x), float(y)])\n    centers = np.array(centers[:n], dtype=float)\n\n    radii = compute_max_radii(centers)\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\ndef compute_max_radii(centers, max_iters=1200, tol=1e-11):\n    n = centers.shape[0]\n    if n == 0:\n        return np.array([])\n    # initial radii limited by walls\n    border = np.minimum.reduce([centers[:,0], centers[:,1],\n                                1.0 - centers[:,0], 1.0 - centers[:,1]])\n    radii = border.copy()\n\n    if n <= 1:\n        return radii\n\n    # pairwise distances (constant)\n    diffs = centers[:, None, :] - centers[None, :, :]\n    D = np.hypot(diffs[...,0], diffs[...,1])\n    np.fill_diagonal(D, np.inf)\n\n    for it in range(max_iters):\n        old = radii.copy()\n\n        # Resolve overlaps via per-pair proportional scaling factors\n        ri = radii[:, None]\n        rj = radii[None, :]\n        sumr = ri + rj\n        viol = sumr > D\n        if np.any(viol):\n            factors = np.ones_like(sumr)\n            factors[viol] = D[viol] / sumr[viol]\n            # For each circle take the most restrictive scale across its pairs\n            min_scale = factors.min(axis=1)\n            min_scale = np.maximum(min_scale, 1e-12)\n            radii *= min_scale\n\n        # enforce wall limits\n        radii = np.minimum(radii, border)\n\n        # Controlled growth toward available slack:\n        # allowed_i = min(border_i, min_j (D_ij - r_j))\n        slack = D - radii[None, :]  # remaining center-to-center allowance\n        allowed = np.minimum(border, slack.min(axis=1))\n        allowed = np.maximum(allowed, 0.0)\n        # grow partially toward allowed to avoid oscillation (use 0.5 blending)\n        grow_mask = allowed > radii + 1e-14\n        radii[grow_mask] = (radii[grow_mask] + allowed[grow_mask]) / 2.0\n\n        if np.max(np.abs(radii - old)) < tol:\n            break\n\n    radii = np.maximum(radii, 0.0)\n    return radii\n\n# EVOLVE-BLOCK-END\n\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n# Optional visualization helper (not used in automated evaluation)\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(6,6))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect('equal')\n    for c, r in zip(centers, radii):\n        ax.add_patch(Circle(c, r, alpha=0.6, edgecolor='k'))\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.1886), Performs well on target_ratio (0.8306), Performs well on combined_score (0.8306), Performs well on eval_time (0.0538)\n\n\n\n## Diverse Programs\n\n### Program D1 (Score: 0.8306)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for n=26 circles in the unit square.\nUses a compact hex-like layout tuned for 26 circles and a vectorized\niterative relaxation to maximize the sum of radii (fixed centers).\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    rows = [6, 5, 6, 5, 4]  # sum = 26\n    R = len(rows)\n    max_k = max(rows)\n\n    # choose horizontal spacing so the widest row spans the unit width (tiny margin)\n    margin = 1e-6\n    s = (1.0 - 2 * margin) / (max_k - 1) if max_k > 1 else 0.5\n    h = s * np.sqrt(3.0) / 2.0\n\n    # if vertical span too large, scale spacing down to fit\n    span_v = (R - 1) * h\n    if span_v > 1.0 - 2 * margin:\n        h = (1.0 - 2 * margin) / (R - 1)\n        s = h * 2.0 / np.sqrt(3.0)\n\n    # build centered rows deterministically\n    y0 = 0.5 - ((R - 1) * h) / 2.0\n    centers = []\n    for i, k in enumerate(rows):\n        y = y0 + i * h\n        left = 0.5 - ((k - 1) * s) / 2.0\n        xs = left + np.arange(k) * s\n        for x in xs:\n            centers.append([float(x), float(y)])\n    centers = np.array(centers[:n], dtype=float)\n\n    radii = compute_max_radii(centers)\n    return centers, radii, float(np.sum(radii))\n\ndef compute_max_radii(centers, max_iters=1000, tol=1e-10):\n    n = centers.shape[0]\n    if n == 0:\n        return np.array([])\n    # initial radii limited by walls\n    border = np.minimum.reduce([centers[:,0], centers[:,1],\n                                1.0 - centers[:,0], 1.0 - centers[:,1]])\n    radii = border.copy()\n    if n == 1:\n        return radii\n\n    # pairwise distance matrix\n    diffs = centers[:, None, :] - centers[None, :, :]\n    D = np.hypot(diffs[...,0], diffs[...,1])\n    np.fill_diagonal(D, np.inf)\n\n    for _ in range(max_iters):\n        old = radii.copy()\n\n        # resolve overlaps by proportional shrinking:\n        ri = radii[:, None]\n        rj = radii[None, :]\n        sumr = ri + rj\n        viol = sumr > D\n        if np.any(viol):\n            factors = np.ones_like(sumr)\n            factors[viol] = D[viol] / sumr[viol]\n            # most restrictive factor per circle\n            min_scale = factors.min(axis=1)\n            min_scale = np.maximum(min_scale, 1e-12)\n            radii *= min_scale\n\n        # enforce walls\n        radii = np.minimum(radii, border)\n\n        # attempt controlled growth toward allowed slack\n        slack = D - radii[None, :]   # remaining center-to-center allowance for each pair\n        allowed = np.minimum(border, slack.min(axis=1))\n        allowed = np.maximum(allowed, 0.0)\n        grow = allowed > radii\n        radii[grow] = 0.5 * (radii[grow] + allowed[grow])\n\n        if np.max(np.abs(radii - old)) < tol:\n            break\n\n    return np.maximum(radii, 0.0)\n\n# EVOLVE-BLOCK-END\n\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(6,6))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect('equal')\n    for c, r in zip(centers, radii):\n        ax.add_patch(Circle(c, r, alpha=0.6, edgecolor='k'))\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, s = run_packing()\n    print(f\"Sum of radii: {s:.6f}\")\n```\nKey features: Alternative approach to validity, Alternative approach to sum_radii\n\n### Program D2 (Score: 0.8306)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Improved deterministic constructor for packing n=26 circles in the unit square.\n\nDesign:\n- Use a compact hex-like layered layout tuned for 26 circles: rows [6,5,6,5,4].\n- Compute maximal feasible radii for fixed centers via a stable, vectorized\n  iterative relaxation that (a) resolves overlaps by proportional shrinking\n  using the most restrictive pairwise scale per circle, (b) enforces wall limits,\n  and (c) attempts controlled growth toward available slack to increase the sum\n  of radii.\nReturns centers (26,2), radii (26,), sum_radii.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    # compact hex-like rows summing to 26\n    rows = [6, 5, 6, 5, 4]\n    R = len(rows)\n    max_k = max(rows)\n\n    # choose horizontal spacing so widest row nearly fills the unit width\n    margin = 1e-6\n    if max_k > 1:\n        s = (1.0 - 2 * margin) / (max_k - 1)\n    else:\n        s = 0.5\n    h = s * np.sqrt(3.0) / 2.0\n\n    # if vertical span would exceed unit square, scale down spacing\n    span_v = (R - 1) * h\n    if span_v > 1.0 - 2 * margin:\n        h = (1.0 - 2 * margin) / (R - 1)\n        s = h * 2.0 / np.sqrt(3.0)\n\n    # build centered rows deterministically\n    y0 = 0.5 - ((R - 1) * h) / 2.0\n    centers = []\n    for i, k in enumerate(rows):\n        y = y0 + i * h\n        left = 0.5 - ((k - 1) * s) / 2.0\n        xs = left + np.arange(k) * s\n        for x in xs:\n            centers.append([float(x), float(y)])\n    centers = np.array(centers[:n], dtype=float)\n\n    radii = compute_max_radii(centers)\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\ndef compute_max_radii(centers, max_iters=800, tol=1e-10):\n    n = centers.shape[0]\n    # initial radii limited by walls\n    border = np.minimum.reduce([centers[:,0], centers[:,1],\n                                1.0 - centers[:,0], 1.0 - centers[:,1]])\n    radii = border.copy()\n\n    if n <= 1:\n        return radii\n\n    # precompute pairwise distances\n    diffs = centers[:, None, :] - centers[None, :, :]\n    D = np.hypot(diffs[...,0], diffs[...,1])\n    np.fill_diagonal(D, np.inf)\n\n    for it in range(max_iters):\n        old = radii.copy()\n\n        # resolve overlaps in a vectorized way:\n        ri = radii[:, None]\n        rj = radii[None, :]\n        sumr = ri + rj\n        viol = sumr > D\n        if np.any(viol):\n            # per-pair scale factors (<=1). For non-violations factor=1\n            factors = np.ones_like(sumr)\n            factors[viol] = D[viol] / sumr[viol]\n            # for each circle take the most restrictive (minimum) scale across its pairs\n            min_scale = factors.min(axis=1)\n            # avoid exact zeros\n            min_scale = np.maximum(min_scale, 1e-12)\n            radii *= min_scale\n\n        # enforce walls\n        radii = np.minimum(radii, border)\n\n        # attempt controlled growth toward available slack:\n        # allowed_i = min(border_i, min_j (D_ij - r_j))\n        slack = D - radii[None, :]  # allowed center-to-center remaining for each pair\n        allowed = np.minimum(border, slack.min(axis=1))\n        allowed = np.maximum(allowed, 0.0)\n        grow_mask = allowed > radii + 1e-14\n        radii[grow_mask] = (radii[grow_mask] + allowed[grow_mask]) / 2.0\n\n        if np.max(np.abs(radii - old)) < tol:\n            break\n\n    radii = np.maximum(radii, 0.0)\n    return radii\n\n# EVOLVE-BLOCK-END\n\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n# Optional visualization helper (not used in automated evaluation)\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(6,6))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect('equal')\n    for c, r in zip(centers, radii):\n        ax.add_patch(Circle(c, r, alpha=0.6, edgecolor='k'))\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n```\nKey features: Alternative approach to validity, Alternative approach to sum_radii\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 0.8349, Type: High-Performer)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for n=26 circles in the unit square.\n\nImprovements over prior versions:\n- Small deterministic grid-search over horizontal/vertical spacing scales to\n  pick the best-shaped hex-like block for 26 circles.\n- A more aggressive growth-then-robust-shrink radii solver that fully expands\n  radii toward slack then repeatedly resolves overlaps (stronger convergence\n  to a larger feasible sum of radii).\nReturns centers (26,2), radii (26,), sum_radii.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    rows = [6, 5, 6, 5, 4]  # total 26\n    R = len(rows)\n    max_k = max(rows)\n    margin = 1e-6\n    # base horizontal spacing (widest row)\n    if max_k > 1:\n        s0 = (1.0 - 2 * margin) / (max_k - 1)\n    else:\n        s0 = 0.5\n\n    # deterministic candidate scales (tries a few compressions to balance edge effects)\n    s_scales = np.array([1.00, 0.98, 0.96, 0.94, 0.92])\n    y_scales = np.array([1.00, 0.98, 0.96])\n\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    for ss in s_scales:\n        for ys in y_scales:\n            s = s0 * ss\n            h = s * np.sqrt(3.0) / 2.0 * ys\n            # if vertical span too large, rescale to fit exactly\n            span_v = (R - 1) * h\n            if span_v > 1.0 - 2 * margin:\n                h = (1.0 - 2 * margin) / (R - 1)\n                s = h * 2.0 / np.sqrt(3.0)\n            # build centered rows\n            y0 = 0.5 - ((R - 1) * h) / 2.0\n            centers = []\n            for i, k in enumerate(rows):\n                y = y0 + i * h\n                left = 0.5 - ((k - 1) * s) / 2.0\n                xs = left + np.arange(k) * s\n                for x in xs:\n                    centers.append([float(x), float(y)])\n            centers = np.array(centers[:n], dtype=float)\n            # minor safeguard: clip into unit square (numerical)\n            centers = np.clip(centers, margin, 1.0 - margin)\n\n            radii = compute_max_radii_aggressive(centers)\n            sumr = float(np.sum(radii))\n            if sumr > best_sum:\n                best_sum = sumr\n                best_centers = centers\n                best_radii = radii\n\n    return best_centers, best_radii, float(np.sum(best_radii))\n\n\ndef compute_max_radii_aggressive(centers, max_outer=300, max_inner=60, tol=1e-10):\n    \"\"\"\n    Aggressive grow-then-shrink solver:\n    - start with conservative radii (min border, 0.5*nearest)\n    - repeatedly:\n        * compute allowed = min(border, min_j (D_ij - r_j)) and set radii = allowed\n        * run an inner loop that repeatedly resolves overlaps by proportional scaling\n          (using the most restrictive scale per circle) until no overlaps remain\n    This often reaches a larger feasible sum than tiny incremental growth steps.\n    \"\"\"\n    n = centers.shape[0]\n    if n == 0:\n        return np.array([])\n\n    # border limits\n    border = np.minimum.reduce([centers[:, 0], centers[:, 1],\n                                1.0 - centers[:, 0], 1.0 - centers[:, 1]])\n    # pairwise distances\n    dif = centers[:, None, :] - centers[None, :, :]\n    D = np.hypot(dif[..., 0], dif[..., 1])\n    np.fill_diagonal(D, np.inf)\n\n    # initial safe radii\n    min_pair = np.min(D, axis=1)\n    radii = np.minimum(border, 0.5 * min_pair)\n    radii = np.maximum(radii, 0.0)\n\n    for outer in range(max_outer):\n        old = radii.copy()\n\n        # compute allowed growth (full assignment)\n        slack = D - radii[None, :]   # allowed remaining toward each other\n        allowed = np.minimum(border, np.min(slack, axis=1))\n        allowed = np.maximum(allowed, 0.0)\n        radii = allowed.copy()\n\n        # inner robust shrink: resolve overlaps thoroughly\n        for inner in range(max_inner):\n            ri = radii[:, None]\n            rj = radii[None, :]\n            sumr = ri + rj\n            viol = sumr > D + 1e-14\n            if not np.any(viol):\n                break\n            # per-pair scale factors (<=1)\n            factors = np.ones_like(sumr)\n            factors[viol] = D[viol] / sumr[viol]\n            # per-circle minimal scale\n            min_scale = factors.min(axis=1)\n            # clamp to avoid underflow\n            min_scale = np.maximum(min_scale, 1e-12)\n            radii *= min_scale\n            # ensure wall limits\n            radii = np.minimum(radii, border)\n\n        # small relaxation to avoid infinite oscillation:\n        radii = np.minimum(radii, border)\n\n        if np.max(np.abs(radii - old)) < tol:\n            break\n\n    radii = np.maximum(radii, 0.0)\n    return radii\n\n# EVOLVE-BLOCK-END\n\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n```\nUnique approach: Modification: Full rewrite, Excellent validity (1.000), Excellent sum_radii (2.200)\n\n### Inspiration 2 (Score: 0.8223, Type: High-Performer)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (explicit hex-like constructor)\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square\n    that attempts to maximize the sum of their radii using a\n    hexagonal-like lattice with row counts tuned to 26.\n    Returns:\n        Tuple of (centers, radii, sum_of_radii)\n    \"\"\"\n    n = 26\n    # Hex-like design parameters: choose spacing s as large as possible for 6 columns\n    s = 1.0 / 6.0  # horizontal spacing: max for 6 columns\n    h = s * np.sqrt(3) / 2.0  # vertical spacing for hex lattice\n\n    # Row structure summing to 26\n    rows = [6, 5, 6, 5, 4]  # total 26\n    R = len(rows)\n\n    # vertical placement centered in [0,1]\n    y0 = 0.5 - ( (R - 1) * h ) / 2.0\n\n    centers = []\n    for i, k in enumerate(rows):\n        y = y0 + i * h\n        # center each row horizontally\n        left = 0.5 - ((k - 1) * s) / 2.0\n        xs = left + np.arange(k) * s\n        for x in xs:\n            centers.append([x, y])\n\n    centers = np.array(centers[:n])  # ensure length 26\n\n    # Compute conservative maximal radii:\n    # For each circle, radius <= distance to border, and <= 0.5 * min_pairwise_distance\n    radii = np.empty(n)\n    for i in range(n):\n        x, y = centers[i]\n        border_dist = min(x, y, 1 - x, 1 - y)\n        # compute min distance to any other center\n        if n == 1:\n            min_pair = 2.0  # irrelevant\n        else:\n            dists = np.sqrt(((centers - centers[i]) ** 2).sum(axis=1))\n            dists[i] = np.inf\n            min_pair = dists.min()\n        # take half the nearest neighbor distance (guarantees non-overlap) and border limit\n        radii[i] = min(border_dist, 0.5 * min_pair)\n\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, ec=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nUnique approach: Modification: Full rewrite, Excellent validity (1.000), Excellent sum_radii (2.167)\n\n### Inspiration 3 (Score: 0.6665, Type: Alternative)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for packing 26 circles in a unit square.\n\nDesign: a compact hex-like layered layout tuned for 26 circles (rows: 6,5,6,5,4)\nand a robust radii relaxation that alternates proportional overlap removal with\ncontrolled growth toward available slack. The routines are vectorized with numpy.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    rows = [6, 5, 6, 5, 4]  # sums to 26, compact hex-like layering\n    R = len(rows)\n    max_k = max(rows)\n\n    margin = 0.003  # small margin to the square boundary\n    # horizontal spacing: fit widest row into available width\n    s = (1.0 - 2 * margin) / (max_k - 1) if max_k > 1 else 0.5\n    h = s * np.sqrt(3) / 2.0  # hex vertical spacing\n\n    # if vertical span doesn't fit, recompute s from vertical constraint\n    span_v = (R - 1) * h\n    if span_v > 1.0 - 2 * margin:\n        h = (1.0 - 2 * margin) / (R - 1)\n        s = h * 2.0 / np.sqrt(3)\n        span_v = (R - 1) * h\n\n    y0 = 0.5 - span_v / 2.0\n    centers = []\n    for i, k in enumerate(rows):\n        y = y0 + i * h\n        # center each row horizontally\n        left = 0.5 - ((k - 1) * s) / 2.0\n        # stagger alternate rows by half spacing for hex-like packing\n        if i % 2 == 1:\n            left += s / 2.0\n        xs = left + np.arange(k) * s\n        for x in xs:\n            # clip to remain inside margin\n            centers.append((np.clip(x, margin, 1.0 - margin),\n                            np.clip(y, margin, 1.0 - margin)))\n    centers = np.asarray(centers[:n])\n    assert centers.shape[0] == n\n\n    radii = compute_max_radii(centers)\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\ndef compute_max_radii(centers, max_iters=1200, tol=1e-10):\n    \"\"\"Compute feasible radii for fixed centers by alternating shrink/grow steps.\"\"\"\n    n = centers.shape[0]\n    # distance to walls\n    border = np.minimum.reduce([centers[:, 0], centers[:, 1],\n                                1.0 - centers[:, 0], 1.0 - centers[:, 1]])\n    # start with wall-limited radii\n    radii = border.copy()\n    if n <= 1:\n        return radii\n\n    # pairwise distances matrix (constant)\n    diffs = centers[:, None, :] - centers[None, :, :]\n    D = np.hypot(diffs[..., 0], diffs[..., 1])\n    np.fill_diagonal(D, np.inf)\n\n    for it in range(max_iters):\n        old = radii.copy()\n\n        # 1) Resolve overlaps by proportional scaling using per-circle most restrictive factor\n        ri = radii[:, None]\n        rj = radii[None, :]\n        sumr = ri + rj\n        viol = sumr > D\n        if np.any(viol):\n            factors = np.ones_like(sumr)\n            factors[viol] = D[viol] / sumr[viol]\n            # per-circle scale is the minimum factor across its pairs\n            min_scale = factors.min(axis=1)\n            min_scale = np.maximum(min_scale, 1e-12)\n            radii *= min_scale\n\n        # re-apply border constraints\n        radii = np.minimum(radii, border)\n\n        # 2) Growth step: compute for each circle the tightest limit by neighbors\n        # allowed_i = min(border_i, min_j (D_ij - r_j))\n        slack = D - radii[None, :]  # D_ij - r_j\n        allowed = np.minimum(border, slack.min(axis=1))\n        allowed = np.maximum(allowed, 0.0)\n        # move partially toward allowed to avoid oscillation\n        grow_mask = allowed > radii + 1e-14\n        # use a moderate growth factor for stability\n        radii[grow_mask] = radii[grow_mask] * 0.4 + allowed[grow_mask] * 0.6\n\n        # early exit on convergence\n        if np.max(np.abs(radii - old)) < tol:\n            break\n\n    radii = np.maximum(radii, 0.0)\n    return radii\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(7,7))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect(\"equal\")\n    for i, (c, r) in enumerate(zip(centers, radii)):\n        ax.add_patch(Circle(c, r, alpha=0.6, ec='k'))\n        ax.text(c[0], c[1], str(i), ha='center', va='center', fontsize=8)\n    plt.title(f\"n={len(centers)}, sum radii={sum(radii):.6f}\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # visualize(centers, radii)\n# EVOLVE-BLOCK-END\n```\nUnique approach: Modification: Full rewrite, Excellent validity (1.000), Excellent sum_radii (1.756)\n\n# Current Program\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for n=26 circles in the unit square.\nPlaces centers in a compact hex-like arrangement tuned for 26 circles\nand assigns each circle the conservative maximal radius:\nmin(distance to boundary, 0.5 * nearest-center-distance).\nThis simple explicit layout is effective and fast.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    # hex-like row pattern that sums to 26\n    rows = [6, 5, 6, 5, 4]\n    # choose horizontal spacing so 6 columns fit across unit square\n    s = 1.0 / 6.0\n    h = s * np.sqrt(3) / 2.0\n\n    # vertical start so the block is centered in the unit square\n    R = len(rows)\n    y0 = 0.5 - ((R - 1) * h) / 2.0\n\n    centers = []\n    for i, k in enumerate(rows):\n        y = y0 + i * h\n        left = 0.5 - ((k - 1) * s) / 2.0\n        xs = left + np.arange(k) * s\n        for x in xs:\n            centers.append((float(x), float(y)))\n    centers = np.array(centers[:n], dtype=float)\n\n    # compute pairwise distances and border distances\n    if centers.shape[0] == 0:\n        return centers, np.array([]), 0.0\n    dif = centers[:, None, :] - centers[None, :, :]\n    dmat = np.sqrt(np.sum(dif * dif, axis=2))\n    np.fill_diagonal(dmat, np.inf)\n    min_pair = dmat.min(axis=1)\n    border = np.minimum.reduce([centers[:,0], centers[:,1], 1-centers[:,0], 1-centers[:,1]])\n    # radius is limited by border and half nearest neighbor distance\n    radii = np.minimum(border, 0.5 * min_pair)\n    # numerical safety\n    radii = np.clip(radii, 0.0, None)\n    return centers, radii, float(radii.sum())\n\n# EVOLVE-BLOCK-END\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(6, 6))\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    for i, (c, r) in enumerate(zip(centers, radii)):\n        ax.add_patch(Circle(c, r, alpha=0.6, ec=\"k\"))\n        ax.text(c[0], c[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={radii.sum():.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # visualize(centers, radii)\n```\n\n# Task\nRewrite the program to improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: complexity, diversity\nDifferent solutions with similar fitness but different features are valuable.\nProvide the complete new program code.\n\nIMPORTANT: Make sure your rewritten program maintains the same inputs and outputs\nas the original program, but with improved internal implementation.\n\n```python\n# Your rewritten program here\n```", "responses": ["```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for n=26 circles in the unit square.\n\nStrategy:\n- Place centers in a compact hex-like block with row counts tuned for 26:\n  rows = [6,5,6,5,4].\n- Try a small deterministic set of horizontal/vertical compressions to\n  balance interior hex packing against edge effects.\n- For each candidate center set compute near-maximal feasible radii using\n  an aggressive grow-then-robust-shrink solver that tends to reach larger\n  total radii than a purely conservative half-neighbor rule.\nReturns centers (26,2), radii (26,), sum_radii.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    rows = [6, 5, 6, 5, 4]  # total 26\n    R = len(rows)\n    max_k = max(rows)\n    margin = 1e-6\n\n    # base horizontal spacing (fit widest row)\n    s0 = (1.0 - 2 * margin) / (max_k - 1) if max_k > 1 else 0.5\n\n    # Try a few deterministic compressions to mitigate edge effects\n    s_scales = np.array([1.00, 0.98, 0.96, 0.94])\n    y_scales = np.array([1.00, 0.98, 0.96])\n\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    for ss in s_scales:\n        for ys in y_scales:\n            s = s0 * ss\n            h = s * np.sqrt(3.0) / 2.0 * ys\n\n            # If vertical span too large, rescale to fit exactly\n            span_v = (R - 1) * h\n            if span_v > 1.0 - 2 * margin:\n                h = (1.0 - 2 * margin) / (R - 1)\n                s = h * 2.0 / np.sqrt(3.0)\n\n            y0 = 0.5 - ((R - 1) * h) / 2.0\n            centers = []\n            for i, k in enumerate(rows):\n                y = y0 + i * h\n                left = 0.5 - ((k - 1) * s) / 2.0\n                xs = left + np.arange(k) * s\n                for x in xs:\n                    centers.append([float(x), float(y)])\n            centers = np.array(centers[:n], dtype=float)\n            centers = np.clip(centers, margin, 1.0 - margin)\n\n            radii = compute_max_radii_aggressive(centers)\n            total = float(np.sum(radii))\n            if total > best_sum:\n                best_sum = total\n                best_centers = centers\n                best_radii = radii\n\n    return best_centers, best_radii, float(np.sum(best_radii))\n\n\ndef compute_max_radii_aggressive(centers, max_outer=300, max_inner=80, tol=1e-11):\n    \"\"\"\n    Aggressive grow-then-shrink solver:\n    - Start with a conservative safe radius (min(border, 0.5*nearest_pair)).\n    - Outer loop: set radii equal to the available allowed (min over neighbors and border).\n    - Inner loop: robustly resolve overlaps by per-pair proportional shrinking until none remain.\n    This approach often yields a larger feasible sum than purely incremental growth.\n    \"\"\"\n    n = centers.shape[0]\n    if n == 0:\n        return np.array([])\n\n    # border limits\n    border = np.minimum.reduce([centers[:, 0], centers[:, 1],\n                                1.0 - centers[:, 0], 1.0 - centers[:, 1]])\n    # pairwise distances\n    dif = centers[:, None, :] - centers[None, :, :]\n    D = np.hypot(dif[..., 0], dif[..., 1])\n    np.fill_diagonal(D, np.inf)\n\n    # initial safe radii\n    min_pair = np.min(D, axis=1)\n    radii = np.minimum(border, 0.5 * min_pair)\n    radii = np.maximum(radii, 0.0)\n\n    for outer in range(max_outer):\n        old = radii.copy()\n\n        # compute allowed assignment: allowed_i = min(border_i, min_j (D_ij - r_j))\n        slack = D - radii[None, :]   # D_ij - r_j\n        allowed = np.minimum(border, np.min(slack, axis=1))\n        allowed = np.maximum(allowed, 0.0)\n        radii = allowed.copy()\n\n        # inner robust shrink: resolve overlaps thoroughly\n        for inner in range(max_inner):\n            ri = radii[:, None]\n            rj = radii[None, :]\n            sumr = ri + rj\n            viol = sumr > D + 1e-14\n            if not np.any(viol):\n                break\n            # per-pair scale factors (<=1)\n            factors = np.ones_like(sumr)\n            factors[viol] = D[viol] / sumr[viol]\n            # per-circle minimal scale (most restrictive across pairs)\n            min_scale = factors.min(axis=1)\n            min_scale = np.maximum(min_scale, 1e-12)\n            radii *= min_scale\n            # enforce wall bounds after shrinking\n            radii = np.minimum(radii, border)\n\n        # small safety clamp and convergence test\n        radii = np.minimum(radii, border)\n        if np.max(np.abs(radii - old)) < tol:\n            break\n\n    radii = np.maximum(radii, 0.0)\n    return radii\n\n# EVOLVE-BLOCK-END\n\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n```"]}}, "artifacts_json": "{\"stdout\": \"Running in subprocess, Python version: 3.12.1 (v3.12.1:2305ca5144, Dec  7 2023, 17:23:38) [Clang 13.0.0 (clang-1300.0.29.30)]\\nProgram path: /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmppx6th08m.py\\nCalling run_packing()...\\nrun_packing() returned successfully: sum_radii = 2.1999995999999995\\nResults saved to /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmpr6n9_xb6.py.results\\n\", \"stderr\": \"\"}", "artifact_dir": null}