{"id": "a4ae3af2-42c5-48a2-a5a9-a8c95f4a28e5", "code": "# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for n=26 circle packing with a small\ncenter-adjustment pass to improve the hand-crafted layouts.\n\nApproach:\n - Build 3 geometry-inspired candidate center sets (hex-rows, rings, staggered).\n - Use a robust deterministic radius relaxation to get non-overlapping radii.\n - Pick the best candidate by sum of radii, then run a short deterministic\n   greedy center-adjustment pass (small discrete moves) to improve the sum.\nNo randomness; all choices are deterministic.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n\n    def layout_rows():\n        rows = [6,5,6,5,4]\n        max_k = max(rows)\n        margin = 1e-6\n        s = (1.0-2*margin)/(max_k-1) if max_k>1 else 0.5\n        h = s*np.sqrt(3)/2.0\n        R = len(rows)\n        span_v = (R-1)*h\n        if span_v > 1.0-2*margin:\n            h = (1.0-2*margin)/(R-1); s = h*2.0/np.sqrt(3)\n        y0 = 0.5 - ((R-1)*h)/2.0\n        centers=[]\n        for i,k in enumerate(rows):\n            y = y0 + i*h\n            left = 0.5 - ((k-1)*s)/2.0\n            offset = 0.018*(0.5 - abs(y-0.5))  # small inward pull\n            xs = left + np.arange(k)*s\n            xs = xs*(1-offset) + 0.5*offset\n            for x in xs: centers.append([x,y])\n        return np.array(centers[:n])\n\n    def layout_rings():\n        cx,cy = 0.5,0.5\n        centers = [[cx,cy]]\n        r1, r2, r3 = 0.095, 0.195, 0.335\n        for i in range(6):\n            a = i*2*np.pi/6\n            centers.append([cx + r1*np.cos(a), cy + r1*np.sin(a)])\n        for i in range(12):\n            a = i*2*np.pi/12 + np.pi/12\n            centers.append([cx + r2*np.cos(a), cy + r2*np.sin(a)])\n        for i in range(7):\n            a = i*2*np.pi/7 + 0.07*i\n            bias = 1.0 + 0.045*np.cos(2*a)\n            centers.append([cx + r3*bias*np.cos(a), cy + r3*bias*np.sin(a)])\n        return np.array(centers[:n])\n\n    def layout_staggered():\n        rows = [7,6,7,6]\n        max_k = max(rows)\n        margin = 1e-6\n        s = (1.0-2*margin)/max_k\n        h = s*0.95*np.sqrt(3)/2.0\n        R = len(rows)\n        span_v = (R-1)*h\n        if span_v > 1.0-2*margin:\n            h = (1.0-2*margin)/(R-1); s = h*2.0/np.sqrt(3)/0.95\n        y0 = 0.5 - ((R-1)*h)/2.0\n        centers=[]\n        for i,k in enumerate(rows):\n            y = y0 + i*h\n            off = (i%2)*(s/2.0)\n            left = 0.5 - ((k-1)*s)/2.0\n            xs = left + np.arange(k)*s + off\n            xs = np.clip(xs,0.02,0.98)\n            for x in xs: centers.append([x,y])\n        return np.array(centers[:n])\n\n    candidates = [layout_rows(), layout_rings(), layout_staggered()]\n\n    best_centers = None\n    best_radii = None\n    best_sum = -1.0\n    for c in candidates:\n        r = compute_max_radii(c)\n        s = float(np.sum(r))\n        if s > best_sum:\n            best_sum = s; best_centers = c.copy(); best_radii = r.copy()\n\n    # Short deterministic greedy center-adjustment pass:\n    # Try small moves on each center (grid of offsets) and accept only if sum improves.\n    # This is a local deterministic refinement (no random).\n    deltas = [0.0,  0.02, -0.02, 0.01, -0.01]\n    # try coarse then fine pass\n    for radius_step in [0,1]:\n        improved = False\n        for i in range(best_centers.shape[0]):\n            base = best_centers.copy()\n            current_sum = float(np.sum(best_radii))\n            best_local_sum = current_sum\n            best_local_pos = best_centers[i].copy()\n            for dx in deltas:\n                for dy in deltas:\n                    if dx==0.0 and dy==0.0 and radius_step==1:\n                        continue\n                    cand = base.copy()\n                    cand[i,0] = np.clip(cand[i,0] + dx, 0.0, 1.0)\n                    cand[i,1] = np.clip(cand[i,1] + dy, 0.0, 1.0)\n                    r_cand = compute_max_radii(cand, max_iters=400, tol=1e-7)\n                    s_cand = float(np.sum(r_cand))\n                    if s_cand > best_local_sum + 1e-9:\n                        best_local_sum = s_cand\n                        best_local_pos = cand[i].copy()\n                        best_local_radii = r_cand\n            if best_local_sum > current_sum + 1e-9:\n                best_centers[i] = best_local_pos\n                best_radii = best_local_radii\n                best_sum = best_local_sum\n                improved = True\n        if not improved:\n            break\n\n    return best_centers, best_radii, float(np.sum(best_radii))\n\ndef compute_max_radii(centers, max_iters=800, tol=1e-8):\n    \"\"\"\n    Deterministic radius relaxation:\n     - start with wall-limited radii\n     - iteratively shrink to resolve overlaps using per-pair scaling factors\n     - re-apply wall limits and grow cautiously toward allowed slack\n    \"\"\"\n    centers = np.asarray(centers, dtype=float)\n    n = centers.shape[0]\n    if n == 0:\n        return np.array([])\n    # initial wall limits\n    border = np.minimum.reduce([centers[:,0], centers[:,1], 1.0-centers[:,0], 1.0-centers[:,1]])\n    radii = np.maximum(border.copy(), 0.0)\n\n    if n==1:\n        return radii\n\n    diffs = centers[:,None,:] - centers[None,:,:]\n    D = np.hypot(diffs[...,0], diffs[...,1])\n    np.fill_diagonal(D, np.inf)\n\n    for it in range(max_iters):\n        old = radii.copy()\n\n        ri = radii[:,None]; rj = radii[None,:]\n        sumr = ri + rj\n        viol = sumr > D\n        if np.any(viol):\n            factors = np.ones_like(sumr)\n            factors[viol] = D[viol] / sumr[viol]\n            # for each circle, most restrictive factor across pairs\n            min_scale = factors.min(axis=1)\n            min_scale = np.maximum(min_scale, 1e-12)\n            radii *= min_scale\n\n        # enforce wall limits\n        radii = np.minimum(radii, border)\n\n        # compute allowed growth: allowed_i = min(border_i, min_j D_ij - r_j)\n        slack = D - radii[None,:]\n        allowed = np.minimum(border, slack.min(axis=1))\n        allowed = np.maximum(allowed, 0.0)\n        grow_mask = allowed > radii\n        # grow fractionally to avoid oscillation\n        radii[grow_mask] = radii[grow_mask] + 0.5*(allowed[grow_mask] - radii[grow_mask])\n\n        if np.max(np.abs(radii-old)) < tol:\n            break\n\n    return np.maximum(radii, 0.0)\n\n# EVOLVE-BLOCK-END\n\n# fixed runtime interface\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(8,8))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect(\"equal\")\n    for c,r in zip(centers, radii):\n        ax.add_patch(Circle(c, r, alpha=0.6, ec='k'))\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, s = run_packing()\n    print(f\"Sum of radii: {s:.6f}\")", "language": "python", "parent_id": "4a68986f-8dcd-484b-80f6-70261be35ac6", "generation": 3, "timestamp": 1759961158.868854, "iteration_found": 40, "metrics": {"validity": 1.0, "sum_radii": 2.421494782344319, "target_ratio": 0.9189733519333281, "combined_score": 0.9189733519333281, "eval_time": 0.3525211811065674}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Full rewrite", "parent_metrics": {"validity": 1.0, "sum_radii": 2.194287656922871, "target_ratio": 0.8327467388701598, "combined_score": 0.8327467388701598, "eval_time": 0.05486178398132324}, "island": 2}, "prompts": {"full_rewrite_user": {"system": "You are an expert mathematician specializing in circle packing problems and computational geometry. Your task is to improve a constructor function that directly produces a specific arrangement of 26 circles in a unit square, maximizing the sum of their radii. The AlphaEvolve paper achieved a sum of 2.635 for n=26.\n\nKey geometric insights:\n- Circle packings often follow hexagonal patterns in the densest regions\n- Maximum density for infinite circle packing is pi/(2*sqrt(3)) \u2248 0.9069\n- Edge effects make square container packing harder than infinite packing\n- Circles can be placed in layers or shells when confined to a square\n- Similar radius circles often form regular patterns, while varied radii allow better space utilization\n- Perfect symmetry may not yield the optimal packing due to edge effects\n\nFocus on designing an explicit constructor that places each circle in a specific position, rather than an iterative search algorithm.\n\n\n## Evolution Insights (Global Learnings)\n\n### Common Pitfalls:\n\u26a0\ufe0f (seen 4x) A change that reduced the computed aggregate radius (sum_radii) by ~12% \u2014 e.g., introducing an unintended scaling/normalization, an extra clamp, or an off-by-one in the radius accumulation \u2014 caused target_ratio and combined_score to fall from 0.3642 to 0.3205 across iterations 1\u201328. When modifying geometry/aggregation code, avoid introducing implicit scalings or clamps without explicit tests; add unit/regression checks for sum_radii and dependent metrics, log intermediate radius values, and review precision/normalization changes to catch unintended shrinkage early.\n\u26a0\ufe0f (seen 4x) A code change that reduced the aggregate coverage/allocatable radius (sum_radii fell from 0.9598 to 0.8446)\u2014for example shrinking buffers, tightening placement radii, or simplifying the packing/placement heuristic\u2014caused a consistent ~12% drop in target_ratio and combined_score across multiple iterations. Avoid making capacity-reducing or constraint-tightening optimizations without targeted benchmarks and automated regression checks; whenever you adjust allocation radii, buffer/window sizes, or packing heuristics, compare against prior baselines to catch and revert harmful target_ratio regressions.\n\u26a0\ufe0f (seen 4x) A recent change effectively reduced the search/matching radius (sum_radii fell from 0.9598 to 0.8446), tightening matching/coverage and causing the target_ratio/combined_score to drop ~12% (0.364 \u2192 0.321). When modifying geometric parameters, normalization, or pruning logic, avoid hard\u2011coding or implicitly scaling radii\u2014always run targeted regression tests for combined_score/target_ratio and make such parameters configurable so you can quickly tune or roll back if performance regresses.\n\u26a0\ufe0f (seen 2x) A change in the evaluation path\u2014likely introducing caching, short-circuiting, or a loop refactor in the eval routine\u2014reduced per-evaluation time by ~11% (0.0605 \u2192 0.0538) across iterations 26\u201330 but coincided with small shifts in domain metrics (sum_radii and target_ratio), indicating the optimization altered ordering or stateful behavior of the evaluator. When making similar performance changes, avoid altering execution order or hidden side effects; add regression tests that assert both performance and domain-specific numeric invariants and run controlled A/B comparisons to catch unintended behavioral changes.\n\u26a0\ufe0f (seen 1x) A change short-circuited or replaced the radii/target-ratio computation (or the object-initialization path) with a no-op/default-zero path so sum_radii and target_ratio collapsed to 0.0 and the overall validity check flipped from valid to invalid (1.000 \u2192 0.000).  \n\nWhen modifying initialization or computation code, avoid introducing default-zero returns or gating that skips construction; add unit/regression tests and assertions that core invariants (e.g., sum_radii > 0, validity == true) remain true and include CI checks to catch regressions early.", "user": "# Current Program Information\n- Fitness: 0.8327\n- Feature coordinates: No feature coordinates\n- Focus areas: - Fitness improved: 0.8306 \u2192 0.8327\n- Consider simplifying - code length exceeds 500 characters\n\n## Last Execution Output\n\n### stdout\n```\nRunning in subprocess, Python version: 3.12.1 (v3.12.1:2305ca5144, Dec  7 2023, 17:23:38) [Clang 13.0.0 (clang-1300.0.29.30)]\nProgram path: /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmptx12al_p.py\nCalling run_packing()...\nrun_packing() returned successfully: sum_radii = 2.194287656922871\nResults saved to /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmpx3qytmfc.py.results\n\n```\n\n### stderr\n```\n\n```\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 2\n- Changes: Unknown changes\n- Metrics: validity: 1.0000, sum_radii: 2.1886, target_ratio: 0.8306, combined_score: 0.8306, eval_time: 0.0540\n- Outcome: Improvement in all metrics\n\n### Attempt 1\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.1943, target_ratio: 0.8327, combined_score: 0.8327, eval_time: 0.0549\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.8327)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Improved constructor-based circle packing for n=26.\n\nThis module builds several deterministic, geometry-inspired candidate layouts\n(shifted hex-rows, concentric hex-like rings, and a staggered-grid) and keeps\nthe candidate that yields the largest sum of valid radii (computed by a fast\ndeterministic relaxation). The approach remains a constructor (no randomized\nsearch), but evaluates a few hand-crafted deterministic patterns and picks\nthe best\u2014improving robustness versus a single fixed layout.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n\n    # Candidate layout A: improved hex-like rows (a small inward shift to better\n    # exploit interior space and break perfect symmetry)\n    def layout_rows():\n        rows = [6, 5, 6, 5, 4]\n        max_k = max(rows)\n        margin = 1e-6\n        s = (1.0 - 2 * margin) / (max_k - 1) if max_k > 1 else 0.5\n        h = s * np.sqrt(3) / 2.0\n        R = len(rows)\n        span_v = (R - 1) * h\n        if span_v > 1.0 - 2 * margin:\n            h = (1.0 - 2 * margin) / (R - 1)\n            s = h * 2.0 / np.sqrt(3)\n        # shift inward slightly toward center to increase wall-limited radii\n        y0 = 0.5 - ((R - 1) * h) / 2.0\n        centers = []\n        for i, k in enumerate(rows):\n            y = y0 + i * h\n            left = 0.5 - ((k - 1) * s) / 2.0\n            # small deterministic inward offset proportional to row distance from center\n            offset = 0.02 * (0.5 - abs(y - 0.5))\n            xs = left + np.arange(k) * s\n            xs = xs * (1 - offset) + 0.5 * offset  # pull slightly toward center line\n            for x in xs:\n                centers.append([x, y])\n        return np.array(centers[:n])\n\n    # Candidate layout B: concentric center + hex ring + larger hex ring + partial outer ring\n    def layout_rings():\n        cx, cy = 0.5, 0.5\n        centers = [[cx, cy]]  # center\n        # tuned nominal radii for rings (deterministic choices)\n        r1 = 0.095  # inner ring distance\n        r2 = 0.195  # second ring distance\n        r3 = 0.335  # outer partial ring distance\n        # first ring: 6\n        for i in range(6):\n            a = i * 2 * np.pi / 6\n            centers.append([cx + r1 * np.cos(a), cy + r1 * np.sin(a)])\n        # second ring: 12 (rotated)\n        for i in range(12):\n            a = i * 2 * np.pi / 12 + np.pi / 12\n            centers.append([cx + r2 * np.cos(a), cy + r2 * np.sin(a)])\n        # third ring: only 7 to reach 26 total (place them slightly biased toward walls)\n        for i in range(7):\n            a = i * 2 * np.pi / 7 + 0.08 * i  # tiny deterministic twist\n            # bias radius by angle to push some toward edges (exploit wall space)\n            bias = 1.0 + 0.05 * np.cos(2 * a)\n            centers.append([cx + r3 * bias * np.cos(a), cy + r3 * bias * np.sin(a)])\n        return np.array(centers[:n])\n\n    # Candidate layout C: staggered block (four rows with alternating offsets)\n    def layout_staggered():\n        rows = [7, 6, 7, 6]  # 26 when truncated to 26\n        max_k = max(rows)\n        margin = 1e-6\n        s = (1.0 - 2 * margin) / (max_k)  # pack slightly tighter horizontally\n        h = s * 0.95 * np.sqrt(3) / 2.0\n        R = len(rows)\n        span_v = (R - 1) * h\n        if span_v > 1.0 - 2 * margin:\n            h = (1.0 - 2 * margin) / (R - 1)\n            s = h * 2.0 / np.sqrt(3) / 0.95\n        y0 = 0.5 - ((R - 1) * h) / 2.0\n        centers = []\n        for i, k in enumerate(rows):\n            y = y0 + i * h\n            # alternate offset to make a denser staggered packing\n            off = (i % 2) * (s / 2.0)\n            left = 0.5 - ((k - 1) * s) / 2.0\n            xs = left + np.arange(k) * s + off\n            # pull extreme row endpoints inwards a touch to avoid tiny wall clipping\n            xs = np.clip(xs, 0.02, 0.98)\n            for x in xs:\n                centers.append([x, y])\n        return np.array(centers[:n])\n\n    # Evaluate candidates and pick the best by sum of radii after deterministic relaxation\n    candidates = [layout_rows(), layout_rings(), layout_staggered()]\n    best = None\n    best_sum = -1.0\n    best_radii = None\n    for centers in candidates:\n        radii = compute_max_radii(centers)\n        s = float(np.sum(radii))\n        if s > best_sum:\n            best_sum = s\n            best = centers\n            best_radii = radii\n\n    # Safety: if something weird happens, fall back to a simple grid construction\n    if best is None:\n        best = layout_rows()\n        best_radii = compute_max_radii(best)\n        best_sum = float(np.sum(best_radii))\n\n    return best, best_radii, float(best_sum)\n\n\ndef compute_max_radii(centers, max_iters=1200, tol=1e-8):\n    \"\"\"\n    Deterministic radius relaxation:\n      - initialize radii limited by distance to walls\n      - iteratively resolve pairwise overlaps by proportional shrinking\n      - re-apply wall limits and make cautious growth toward available slack\n    This is identical in spirit to previous successful relaxations but tuned\n    with slightly more iterations and stability safeguards.\n    \"\"\"\n    n = centers.shape[0]\n    # limit radii by walls initially\n    border = np.minimum.reduce([centers[:, 0], centers[:, 1],\n                                1.0 - centers[:, 0], 1.0 - centers[:, 1]])\n    radii = np.maximum(border.copy(), 0.0)\n\n    if n <= 1:\n        return radii\n\n    diffs = centers[:, None, :] - centers[None, :, :]\n    D = np.hypot(diffs[..., 0], diffs[..., 1])\n    np.fill_diagonal(D, np.inf)\n\n    for it in range(max_iters):\n        old = radii.copy()\n\n        # pairwise overlap resolution\n        ri = radii[:, None]\n        rj = radii[None, :]\n        sumr = ri + rj\n        viol = sumr > D\n        if np.any(viol):\n            # compute per-pair scaling (<=1) to exactly resolve the overlap\n            factors = np.ones_like(sumr)\n            factors[viol] = D[viol] / sumr[viol]\n            # each circle must satisfy all its pairwise constraints -> take min scale\n            min_scale = factors.min(axis=1)\n            # avoid collapse to zero accidentally\n            min_scale = np.maximum(min_scale, 1e-12)\n            radii *= min_scale\n\n        # enforce wall limits\n        radii = np.minimum(radii, border)\n\n        # cautious growth toward available slack: compute allowed radius per circle\n        # allowed_i = min(border_i, min_j (D_ij - r_j))\n        slack = D - radii[None, :]  # remaining center-to-center allowable distance wrt j\n        allowed = np.minimum(border, slack.min(axis=1))\n        allowed = np.maximum(allowed, 0.0)\n        # grow a fraction toward allowed to avoid oscillations\n        grow_mask = allowed > radii\n        radii[grow_mask] = radii[grow_mask] + 0.5 * (allowed[grow_mask] - radii[grow_mask])\n\n        if np.max(np.abs(radii - old)) < tol:\n            break\n\n    radii = np.maximum(radii, 0.0)\n    return radii\n\n# EVOLVE-BLOCK-END\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, ec=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.1943), Performs well on target_ratio (0.8327), Performs well on combined_score (0.8327), Performs well on eval_time (0.0549)\n\n### Program 2 (Score: 0.8306)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (hex-like constructor + radius relaxation)\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct an explicit arrangement of 26 circles in a unit square using\n    a hex-like layered layout (rows tuned to 26) and compute near-maximal\n    non-overlapping radii via iterative relaxation.\n    Returns:\n        centers: (26,2) np.array\n        radii: (26,) np.array\n        sum_of_radii: float\n    \"\"\"\n    n = 26\n    # Hex-like row counts summing to 26\n    rows = [6, 5, 6, 5, 4]\n    R = len(rows)\n\n    # pick horizontal spacing s to fit widest row comfortably\n    max_k = max(rows)\n    # choose s so widest row uses full width but allow small margin\n    margin = 1e-6\n    s = (1.0 - 2 * margin) / (max_k - 1) if max_k > 1 else 0.5\n    # hex vertical spacing\n    h = s * np.sqrt(3) / 2.0\n\n    # if total vertical span too big, shrink s to fit\n    span_v = (R - 1) * h\n    if span_v > 1.0 - 2 * margin:\n        h = (1.0 - 2 * margin) / (R - 1)\n        s = h * 2.0 / np.sqrt(3)\n\n    # build centered rows (no arbitrary random jitter \u2014 deterministic)\n    y0 = 0.5 - ((R - 1) * h) / 2.0\n    centers = []\n    for i, k in enumerate(rows):\n        y = y0 + i * h\n        left = 0.5 - ((k - 1) * s) / 2.0\n        xs = left + np.arange(k) * s\n        for x in xs:\n            centers.append([x, y])\n    centers = np.array(centers[:n])\n\n    # compute radii with an iterative relaxation that enforces walls and pairwise constraints\n    radii = compute_max_radii(centers)\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers, max_iters=800, tol=1e-9):\n    \"\"\"\n    Given fixed centers inside [0,1]^2, compute radii so circles are non-overlapping\n    and lie inside the square. The algorithm:\n      - start with wall-limited radii\n      - repeatedly shrink to resolve overlaps (proportional scaling)\n      - re-apply wall limits\n      - attempt controlled growth toward available slack\n    \"\"\"\n    n = centers.shape[0]\n    # initial radii limited by walls\n    border = np.minimum.reduce([centers[:, 0], centers[:, 1],\n                                1.0 - centers[:, 0], 1.0 - centers[:, 1]])\n    radii = border.copy()\n\n    if n <= 1:\n        return radii\n\n    # pairwise distance matrix (constant)\n    diffs = centers[:, None, :] - centers[None, :, :]\n    D = np.hypot(diffs[..., 0], diffs[..., 1])\n    np.fill_diagonal(D, np.inf)\n\n    for it in range(max_iters):\n        old = radii.copy()\n\n        # resolve overlaps: if r_i + r_j > d_ij, apply pairwise proportional scaling\n        ri = radii[:, None]\n        rj = radii[None, :]\n        sumr = ri + rj\n        viol = sumr > D\n        if np.any(viol):\n            # compute per-pair scaling factors (<=1)\n            factors = np.ones_like(sumr)\n            factors[viol] = D[viol] / sumr[viol]\n            # for each circle take the most restrictive scale across its pairs\n            min_scale = factors.min(axis=1)\n            # avoid numerical zeros: clamp to tiny positive\n            min_scale = np.maximum(min_scale, 1e-12)\n            radii *= min_scale\n\n        # enforce wall limits\n        radii = np.minimum(radii, border)\n\n        # attempt to grow toward available slack:\n        # allowed_i = min(border_i, min_j (D_ij - r_j))\n        slack = D - radii[None, :]  # allowed center-to-center distance remaining for each pair\n        # since D diagonal is inf, min excludes self\n        allowed = np.minimum(border, slack.min(axis=1))\n        allowed = np.maximum(allowed, 0.0)\n        # grow partially toward allowed to avoid oscillation\n        grow_mask = allowed > radii\n        radii[grow_mask] = (radii[grow_mask] + allowed[grow_mask]) / 2.0\n\n        if np.max(np.abs(radii - old)) < tol:\n            break\n\n    radii = np.maximum(radii, 0.0)\n    return radii\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, ec=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.1886), Performs well on target_ratio (0.8306), Performs well on combined_score (0.8306), Performs well on eval_time (0.0540)\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 0.8306, Type: High-Performer)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (hex-like constructor + radius relaxation)\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct an explicit arrangement of 26 circles in a unit square using\n    a hex-like layered layout (rows tuned to 26) and compute near-maximal\n    non-overlapping radii via iterative relaxation.\n    Returns:\n        centers: (26,2) np.array\n        radii: (26,) np.array\n        sum_of_radii: float\n    \"\"\"\n    n = 26\n    # Hex-like row counts summing to 26\n    rows = [6, 5, 6, 5, 4]\n    R = len(rows)\n\n    # pick horizontal spacing s to fit widest row comfortably\n    max_k = max(rows)\n    # choose s so widest row uses full width but allow small margin\n    margin = 1e-6\n    s = (1.0 - 2 * margin) / (max_k - 1) if max_k > 1 else 0.5\n    # hex vertical spacing\n    h = s * np.sqrt(3) / 2.0\n\n    # if total vertical span too big, shrink s to fit\n    span_v = (R - 1) * h\n    if span_v > 1.0 - 2 * margin:\n        h = (1.0 - 2 * margin) / (R - 1)\n        s = h * 2.0 / np.sqrt(3)\n\n    # build centered rows (no arbitrary random jitter \u2014 deterministic)\n    y0 = 0.5 - ((R - 1) * h) / 2.0\n    centers = []\n    for i, k in enumerate(rows):\n        y = y0 + i * h\n        left = 0.5 - ((k - 1) * s) / 2.0\n        xs = left + np.arange(k) * s\n        for x in xs:\n            centers.append([x, y])\n    centers = np.array(centers[:n])\n\n    # compute radii with an iterative relaxation that enforces walls and pairwise constraints\n    radii = compute_max_radii(centers)\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers, max_iters=800, tol=1e-9):\n    \"\"\"\n    Given fixed centers inside [0,1]^2, compute radii so circles are non-overlapping\n    and lie inside the square. The algorithm:\n      - start with wall-limited radii\n      - repeatedly shrink to resolve overlaps (proportional scaling)\n      - re-apply wall limits\n      - attempt controlled growth toward available slack\n    \"\"\"\n    n = centers.shape[0]\n    # initial radii limited by walls\n    border = np.minimum.reduce([centers[:, 0], centers[:, 1],\n                                1.0 - centers[:, 0], 1.0 - centers[:, 1]])\n    radii = border.copy()\n\n    if n <= 1:\n        return radii\n\n    # pairwise distance matrix (constant)\n    diffs = centers[:, None, :] - centers[None, :, :]\n    D = np.hypot(diffs[..., 0], diffs[..., 1])\n    np.fill_diagonal(D, np.inf)\n\n    for it in range(max_iters):\n        old = radii.copy()\n\n        # resolve overlaps: if r_i + r_j > d_ij, apply pairwise proportional scaling\n        ri = radii[:, None]\n        rj = radii[None, :]\n        sumr = ri + rj\n        viol = sumr > D\n        if np.any(viol):\n            # compute per-pair scaling factors (<=1)\n            factors = np.ones_like(sumr)\n            factors[viol] = D[viol] / sumr[viol]\n            # for each circle take the most restrictive scale across its pairs\n            min_scale = factors.min(axis=1)\n            # avoid numerical zeros: clamp to tiny positive\n            min_scale = np.maximum(min_scale, 1e-12)\n            radii *= min_scale\n\n        # enforce wall limits\n        radii = np.minimum(radii, border)\n\n        # attempt to grow toward available slack:\n        # allowed_i = min(border_i, min_j (D_ij - r_j))\n        slack = D - radii[None, :]  # allowed center-to-center distance remaining for each pair\n        # since D diagonal is inf, min excludes self\n        allowed = np.minimum(border, slack.min(axis=1))\n        allowed = np.maximum(allowed, 0.0)\n        # grow partially toward allowed to avoid oscillation\n        grow_mask = allowed > radii\n        radii[grow_mask] = (radii[grow_mask] + allowed[grow_mask]) / 2.0\n\n        if np.max(np.abs(radii - old)) < tol:\n            break\n\n    radii = np.maximum(radii, 0.0)\n    return radii\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, ec=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nUnique approach: Excellent validity (1.000), Excellent sum_radii (2.189), Alternative eval_time approach\n\n# Current Program\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Improved constructor-based circle packing for n=26.\n\nThis module builds several deterministic, geometry-inspired candidate layouts\n(shifted hex-rows, concentric hex-like rings, and a staggered-grid) and keeps\nthe candidate that yields the largest sum of valid radii (computed by a fast\ndeterministic relaxation). The approach remains a constructor (no randomized\nsearch), but evaluates a few hand-crafted deterministic patterns and picks\nthe best\u2014improving robustness versus a single fixed layout.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n\n    # Candidate layout A: improved hex-like rows (a small inward shift to better\n    # exploit interior space and break perfect symmetry)\n    def layout_rows():\n        rows = [6, 5, 6, 5, 4]\n        max_k = max(rows)\n        margin = 1e-6\n        s = (1.0 - 2 * margin) / (max_k - 1) if max_k > 1 else 0.5\n        h = s * np.sqrt(3) / 2.0\n        R = len(rows)\n        span_v = (R - 1) * h\n        if span_v > 1.0 - 2 * margin:\n            h = (1.0 - 2 * margin) / (R - 1)\n            s = h * 2.0 / np.sqrt(3)\n        # shift inward slightly toward center to increase wall-limited radii\n        y0 = 0.5 - ((R - 1) * h) / 2.0\n        centers = []\n        for i, k in enumerate(rows):\n            y = y0 + i * h\n            left = 0.5 - ((k - 1) * s) / 2.0\n            # small deterministic inward offset proportional to row distance from center\n            offset = 0.02 * (0.5 - abs(y - 0.5))\n            xs = left + np.arange(k) * s\n            xs = xs * (1 - offset) + 0.5 * offset  # pull slightly toward center line\n            for x in xs:\n                centers.append([x, y])\n        return np.array(centers[:n])\n\n    # Candidate layout B: concentric center + hex ring + larger hex ring + partial outer ring\n    def layout_rings():\n        cx, cy = 0.5, 0.5\n        centers = [[cx, cy]]  # center\n        # tuned nominal radii for rings (deterministic choices)\n        r1 = 0.095  # inner ring distance\n        r2 = 0.195  # second ring distance\n        r3 = 0.335  # outer partial ring distance\n        # first ring: 6\n        for i in range(6):\n            a = i * 2 * np.pi / 6\n            centers.append([cx + r1 * np.cos(a), cy + r1 * np.sin(a)])\n        # second ring: 12 (rotated)\n        for i in range(12):\n            a = i * 2 * np.pi / 12 + np.pi / 12\n            centers.append([cx + r2 * np.cos(a), cy + r2 * np.sin(a)])\n        # third ring: only 7 to reach 26 total (place them slightly biased toward walls)\n        for i in range(7):\n            a = i * 2 * np.pi / 7 + 0.08 * i  # tiny deterministic twist\n            # bias radius by angle to push some toward edges (exploit wall space)\n            bias = 1.0 + 0.05 * np.cos(2 * a)\n            centers.append([cx + r3 * bias * np.cos(a), cy + r3 * bias * np.sin(a)])\n        return np.array(centers[:n])\n\n    # Candidate layout C: staggered block (four rows with alternating offsets)\n    def layout_staggered():\n        rows = [7, 6, 7, 6]  # 26 when truncated to 26\n        max_k = max(rows)\n        margin = 1e-6\n        s = (1.0 - 2 * margin) / (max_k)  # pack slightly tighter horizontally\n        h = s * 0.95 * np.sqrt(3) / 2.0\n        R = len(rows)\n        span_v = (R - 1) * h\n        if span_v > 1.0 - 2 * margin:\n            h = (1.0 - 2 * margin) / (R - 1)\n            s = h * 2.0 / np.sqrt(3) / 0.95\n        y0 = 0.5 - ((R - 1) * h) / 2.0\n        centers = []\n        for i, k in enumerate(rows):\n            y = y0 + i * h\n            # alternate offset to make a denser staggered packing\n            off = (i % 2) * (s / 2.0)\n            left = 0.5 - ((k - 1) * s) / 2.0\n            xs = left + np.arange(k) * s + off\n            # pull extreme row endpoints inwards a touch to avoid tiny wall clipping\n            xs = np.clip(xs, 0.02, 0.98)\n            for x in xs:\n                centers.append([x, y])\n        return np.array(centers[:n])\n\n    # Evaluate candidates and pick the best by sum of radii after deterministic relaxation\n    candidates = [layout_rows(), layout_rings(), layout_staggered()]\n    best = None\n    best_sum = -1.0\n    best_radii = None\n    for centers in candidates:\n        radii = compute_max_radii(centers)\n        s = float(np.sum(radii))\n        if s > best_sum:\n            best_sum = s\n            best = centers\n            best_radii = radii\n\n    # Safety: if something weird happens, fall back to a simple grid construction\n    if best is None:\n        best = layout_rows()\n        best_radii = compute_max_radii(best)\n        best_sum = float(np.sum(best_radii))\n\n    return best, best_radii, float(best_sum)\n\n\ndef compute_max_radii(centers, max_iters=1200, tol=1e-8):\n    \"\"\"\n    Deterministic radius relaxation:\n      - initialize radii limited by distance to walls\n      - iteratively resolve pairwise overlaps by proportional shrinking\n      - re-apply wall limits and make cautious growth toward available slack\n    This is identical in spirit to previous successful relaxations but tuned\n    with slightly more iterations and stability safeguards.\n    \"\"\"\n    n = centers.shape[0]\n    # limit radii by walls initially\n    border = np.minimum.reduce([centers[:, 0], centers[:, 1],\n                                1.0 - centers[:, 0], 1.0 - centers[:, 1]])\n    radii = np.maximum(border.copy(), 0.0)\n\n    if n <= 1:\n        return radii\n\n    diffs = centers[:, None, :] - centers[None, :, :]\n    D = np.hypot(diffs[..., 0], diffs[..., 1])\n    np.fill_diagonal(D, np.inf)\n\n    for it in range(max_iters):\n        old = radii.copy()\n\n        # pairwise overlap resolution\n        ri = radii[:, None]\n        rj = radii[None, :]\n        sumr = ri + rj\n        viol = sumr > D\n        if np.any(viol):\n            # compute per-pair scaling (<=1) to exactly resolve the overlap\n            factors = np.ones_like(sumr)\n            factors[viol] = D[viol] / sumr[viol]\n            # each circle must satisfy all its pairwise constraints -> take min scale\n            min_scale = factors.min(axis=1)\n            # avoid collapse to zero accidentally\n            min_scale = np.maximum(min_scale, 1e-12)\n            radii *= min_scale\n\n        # enforce wall limits\n        radii = np.minimum(radii, border)\n\n        # cautious growth toward available slack: compute allowed radius per circle\n        # allowed_i = min(border_i, min_j (D_ij - r_j))\n        slack = D - radii[None, :]  # remaining center-to-center allowable distance wrt j\n        allowed = np.minimum(border, slack.min(axis=1))\n        allowed = np.maximum(allowed, 0.0)\n        # grow a fraction toward allowed to avoid oscillations\n        grow_mask = allowed > radii\n        radii[grow_mask] = radii[grow_mask] + 0.5 * (allowed[grow_mask] - radii[grow_mask])\n\n        if np.max(np.abs(radii - old)) < tol:\n            break\n\n    radii = np.maximum(radii, 0.0)\n    return radii\n\n# EVOLVE-BLOCK-END\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, ec=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\n\n# Task\nRewrite the program to improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: complexity, diversity\nDifferent solutions with similar fitness but different features are valuable.\nProvide the complete new program code.\n\nIMPORTANT: Make sure your rewritten program maintains the same inputs and outputs\nas the original program, but with improved internal implementation.\n\n```python\n# Your rewritten program here\n```", "responses": ["```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for n=26 circle packing with a small\ncenter-adjustment pass to improve the hand-crafted layouts.\n\nApproach:\n - Build 3 geometry-inspired candidate center sets (hex-rows, rings, staggered).\n - Use a robust deterministic radius relaxation to get non-overlapping radii.\n - Pick the best candidate by sum of radii, then run a short deterministic\n   greedy center-adjustment pass (small discrete moves) to improve the sum.\nNo randomness; all choices are deterministic.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n\n    def layout_rows():\n        rows = [6,5,6,5,4]\n        max_k = max(rows)\n        margin = 1e-6\n        s = (1.0-2*margin)/(max_k-1) if max_k>1 else 0.5\n        h = s*np.sqrt(3)/2.0\n        R = len(rows)\n        span_v = (R-1)*h\n        if span_v > 1.0-2*margin:\n            h = (1.0-2*margin)/(R-1); s = h*2.0/np.sqrt(3)\n        y0 = 0.5 - ((R-1)*h)/2.0\n        centers=[]\n        for i,k in enumerate(rows):\n            y = y0 + i*h\n            left = 0.5 - ((k-1)*s)/2.0\n            offset = 0.018*(0.5 - abs(y-0.5))  # small inward pull\n            xs = left + np.arange(k)*s\n            xs = xs*(1-offset) + 0.5*offset\n            for x in xs: centers.append([x,y])\n        return np.array(centers[:n])\n\n    def layout_rings():\n        cx,cy = 0.5,0.5\n        centers = [[cx,cy]]\n        r1, r2, r3 = 0.095, 0.195, 0.335\n        for i in range(6):\n            a = i*2*np.pi/6\n            centers.append([cx + r1*np.cos(a), cy + r1*np.sin(a)])\n        for i in range(12):\n            a = i*2*np.pi/12 + np.pi/12\n            centers.append([cx + r2*np.cos(a), cy + r2*np.sin(a)])\n        for i in range(7):\n            a = i*2*np.pi/7 + 0.07*i\n            bias = 1.0 + 0.045*np.cos(2*a)\n            centers.append([cx + r3*bias*np.cos(a), cy + r3*bias*np.sin(a)])\n        return np.array(centers[:n])\n\n    def layout_staggered():\n        rows = [7,6,7,6]\n        max_k = max(rows)\n        margin = 1e-6\n        s = (1.0-2*margin)/max_k\n        h = s*0.95*np.sqrt(3)/2.0\n        R = len(rows)\n        span_v = (R-1)*h\n        if span_v > 1.0-2*margin:\n            h = (1.0-2*margin)/(R-1); s = h*2.0/np.sqrt(3)/0.95\n        y0 = 0.5 - ((R-1)*h)/2.0\n        centers=[]\n        for i,k in enumerate(rows):\n            y = y0 + i*h\n            off = (i%2)*(s/2.0)\n            left = 0.5 - ((k-1)*s)/2.0\n            xs = left + np.arange(k)*s + off\n            xs = np.clip(xs,0.02,0.98)\n            for x in xs: centers.append([x,y])\n        return np.array(centers[:n])\n\n    candidates = [layout_rows(), layout_rings(), layout_staggered()]\n\n    best_centers = None\n    best_radii = None\n    best_sum = -1.0\n    for c in candidates:\n        r = compute_max_radii(c)\n        s = float(np.sum(r))\n        if s > best_sum:\n            best_sum = s; best_centers = c.copy(); best_radii = r.copy()\n\n    # Short deterministic greedy center-adjustment pass:\n    # Try small moves on each center (grid of offsets) and accept only if sum improves.\n    # This is a local deterministic refinement (no random).\n    deltas = [0.0,  0.02, -0.02, 0.01, -0.01]\n    # try coarse then fine pass\n    for radius_step in [0,1]:\n        improved = False\n        for i in range(best_centers.shape[0]):\n            base = best_centers.copy()\n            current_sum = float(np.sum(best_radii))\n            best_local_sum = current_sum\n            best_local_pos = best_centers[i].copy()\n            for dx in deltas:\n                for dy in deltas:\n                    if dx==0.0 and dy==0.0 and radius_step==1:\n                        continue\n                    cand = base.copy()\n                    cand[i,0] = np.clip(cand[i,0] + dx, 0.0, 1.0)\n                    cand[i,1] = np.clip(cand[i,1] + dy, 0.0, 1.0)\n                    r_cand = compute_max_radii(cand, max_iters=400, tol=1e-7)\n                    s_cand = float(np.sum(r_cand))\n                    if s_cand > best_local_sum + 1e-9:\n                        best_local_sum = s_cand\n                        best_local_pos = cand[i].copy()\n                        best_local_radii = r_cand\n            if best_local_sum > current_sum + 1e-9:\n                best_centers[i] = best_local_pos\n                best_radii = best_local_radii\n                best_sum = best_local_sum\n                improved = True\n        if not improved:\n            break\n\n    return best_centers, best_radii, float(np.sum(best_radii))\n\ndef compute_max_radii(centers, max_iters=800, tol=1e-8):\n    \"\"\"\n    Deterministic radius relaxation:\n     - start with wall-limited radii\n     - iteratively shrink to resolve overlaps using per-pair scaling factors\n     - re-apply wall limits and grow cautiously toward allowed slack\n    \"\"\"\n    centers = np.asarray(centers, dtype=float)\n    n = centers.shape[0]\n    if n == 0:\n        return np.array([])\n    # initial wall limits\n    border = np.minimum.reduce([centers[:,0], centers[:,1], 1.0-centers[:,0], 1.0-centers[:,1]])\n    radii = np.maximum(border.copy(), 0.0)\n\n    if n==1:\n        return radii\n\n    diffs = centers[:,None,:] - centers[None,:,:]\n    D = np.hypot(diffs[...,0], diffs[...,1])\n    np.fill_diagonal(D, np.inf)\n\n    for it in range(max_iters):\n        old = radii.copy()\n\n        ri = radii[:,None]; rj = radii[None,:]\n        sumr = ri + rj\n        viol = sumr > D\n        if np.any(viol):\n            factors = np.ones_like(sumr)\n            factors[viol] = D[viol] / sumr[viol]\n            # for each circle, most restrictive factor across pairs\n            min_scale = factors.min(axis=1)\n            min_scale = np.maximum(min_scale, 1e-12)\n            radii *= min_scale\n\n        # enforce wall limits\n        radii = np.minimum(radii, border)\n\n        # compute allowed growth: allowed_i = min(border_i, min_j D_ij - r_j)\n        slack = D - radii[None,:]\n        allowed = np.minimum(border, slack.min(axis=1))\n        allowed = np.maximum(allowed, 0.0)\n        grow_mask = allowed > radii\n        # grow fractionally to avoid oscillation\n        radii[grow_mask] = radii[grow_mask] + 0.5*(allowed[grow_mask] - radii[grow_mask])\n\n        if np.max(np.abs(radii-old)) < tol:\n            break\n\n    return np.maximum(radii, 0.0)\n\n# EVOLVE-BLOCK-END\n\n# fixed runtime interface\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(8,8))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect(\"equal\")\n    for c,r in zip(centers, radii):\n        ax.add_patch(Circle(c, r, alpha=0.6, ec='k'))\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, s = run_packing()\n    print(f\"Sum of radii: {s:.6f}\")\n```"]}}, "artifacts_json": "{\"stdout\": \"Running in subprocess, Python version: 3.12.1 (v3.12.1:2305ca5144, Dec  7 2023, 17:23:38) [Clang 13.0.0 (clang-1300.0.29.30)]\\nProgram path: /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmp_4_aa8xw.py\\nCalling run_packing()...\\nrun_packing() returned successfully: sum_radii = 2.421494782344319\\nResults saved to /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmpl7lh9tih.py.results\\n\", \"stderr\": \"\"}", "artifact_dir": null}