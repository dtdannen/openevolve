{"id": "876d619c-68ac-4809-b63e-95bce432608f", "code": "# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for n=26 circles in the unit square.\n\nStrategy:\n- Place centers in a compact hex-like layered layout tuned for 26 circles:\n  rows = [6,5,6,5,4].\n- Try a small set of horizontal/vertical scale candidates to balance edge effects.\n- For each candidate, compute near-maximal radii with an aggressive\n  grow-then-robust-shrink relaxation (vectorized). Pick the best sum of radii.\nReturns: centers (26,2), radii (26,), sum_radii (float).\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    rows = [6, 5, 6, 5, 4]  # sums to 26\n    R = len(rows)\n    max_k = max(rows)\n    margin = 1e-6\n\n    # base spacing for the widest row\n    s0 = (1.0 - 2 * margin) / (max_k - 1) if max_k > 1 else 0.5\n    s_scales = np.array([1.00, 0.985, 0.97, 0.955, 0.94])\n    y_scales = np.array([1.00, 0.98, 0.96])\n\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    for ss in s_scales:\n        for ys in y_scales:\n            s = s0 * ss\n            h = s * np.sqrt(3.0) / 2.0 * ys\n            # if vertical span too large, rescale to fit exactly\n            span_v = (R - 1) * h\n            if span_v > 1.0 - 2 * margin:\n                h = (1.0 - 2 * margin) / (R - 1)\n                s = h * 2.0 / np.sqrt(3.0)\n            # build centered rows (no staggering needed here; rows lengths already vary)\n            y0 = 0.5 - ((R - 1) * h) / 2.0\n            centers = []\n            for i, k in enumerate(rows):\n                y = y0 + i * h\n                left = 0.5 - ((k - 1) * s) / 2.0\n                xs = left + np.arange(k) * s\n                for x in xs:\n                    centers.append([float(x), float(y)])\n            centers = np.array(centers[:n], dtype=float)\n            centers = np.clip(centers, margin, 1.0 - margin)\n\n            radii = compute_max_radii_aggressive(centers)\n            sumr = float(np.sum(radii))\n            if sumr > best_sum:\n                best_sum = sumr\n                best_centers = centers.copy()\n                best_radii = radii.copy()\n\n    return best_centers, best_radii, float(np.sum(best_radii))\n\n\ndef compute_max_radii_aggressive(centers, max_outer=400, max_inner=120, tol=1e-11):\n    \"\"\"\n    Aggressive grow-then-shrink solver:\n    - start with safe radii = min(distance-to-wall, 0.5*nearest-center)\n    - outer loop: compute allowed = min(border, min_j (D_ij - r_j)) and set radii = allowed\n    - inner loop: resolve overlaps thoroughly by computing per-pair scale factors and\n      applying the most restrictive per-circle scale until no violations remain\n    This often attains a larger feasible sum than tiny incremental updates.\n    \"\"\"\n    n = centers.shape[0]\n    if n == 0:\n        return np.array([])\n\n    # border limits\n    border = np.minimum.reduce([centers[:, 0], centers[:, 1],\n                                1.0 - centers[:, 0], 1.0 - centers[:, 1]])\n    # pairwise distances\n    dif = centers[:, None, :] - centers[None, :, :]\n    D = np.hypot(dif[..., 0], dif[..., 1])\n    np.fill_diagonal(D, np.inf)\n\n    # initial safe radii\n    min_pair = np.min(D, axis=1)\n    radii = np.minimum(border, 0.5 * min_pair)\n    radii = np.maximum(radii, 0.0)\n\n    for outer in range(max_outer):\n        old = radii.copy()\n\n        # compute allowed growth target for each circle:\n        # allowed_i = min(border_i, min_j (D_ij - r_j))\n        slack = D - radii[None, :]   # shape (n,n)\n        allowed = np.minimum(border, np.min(slack, axis=1))\n        allowed = np.maximum(allowed, 0.0)\n        # move fully to allowed (aggressive growth), then inner loop will fix overlaps\n        radii = allowed.copy()\n\n        # inner robust shrink: resolve overlaps thoroughly\n        for inner in range(max_inner):\n            ri = radii[:, None]\n            rj = radii[None, :]\n            sumr = ri + rj\n            viol = sumr > D + 1e-14\n            if not np.any(viol):\n                break\n            # per-pair scale factors (<=1)\n            factors = np.ones_like(sumr)\n            factors[viol] = D[viol] / sumr[viol]\n            # per-circle minimal scale (most restrictive across pairs)\n            min_scale = factors.min(axis=1)\n            # clamp to avoid numerical underflow\n            min_scale = np.maximum(min_scale, 1e-12)\n            radii *= min_scale\n            # enforce walls after shrink\n            radii = np.minimum(radii, border)\n\n        # final clamp to walls\n        radii = np.minimum(radii, border)\n\n        if np.max(np.abs(radii - old)) < tol:\n            break\n\n    radii = np.maximum(radii, 0.0)\n    return radii\n\n\n# EVOLVE-BLOCK-END\n\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")", "language": "python", "parent_id": "84699f1b-ad01-4e1f-87f2-9c1cb7a22e67", "generation": 2, "timestamp": 1759961109.716811, "iteration_found": 37, "metrics": {"validity": 1.0, "sum_radii": 2.1999995999999995, "target_ratio": 0.834914459203036, "combined_score": 0.834914459203036, "eval_time": 0.054508209228515625}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Full rewrite", "parent_metrics": {"validity": 1.0, "sum_radii": 1.2770469613566842, "target_ratio": 0.4846478031714172, "combined_score": 0.4846478031714172}, "island": 0}, "prompts": {"full_rewrite_user": {"system": "You are an expert mathematician specializing in circle packing problems and computational geometry. Your task is to improve a constructor function that directly produces a specific arrangement of 26 circles in a unit square, maximizing the sum of their radii. The AlphaEvolve paper achieved a sum of 2.635 for n=26.\n\nKey geometric insights:\n- Circle packings often follow hexagonal patterns in the densest regions\n- Maximum density for infinite circle packing is pi/(2*sqrt(3)) \u2248 0.9069\n- Edge effects make square container packing harder than infinite packing\n- Circles can be placed in layers or shells when confined to a square\n- Similar radius circles often form regular patterns, while varied radii allow better space utilization\n- Perfect symmetry may not yield the optimal packing due to edge effects\n\nFocus on designing an explicit constructor that places each circle in a specific position, rather than an iterative search algorithm.\n\n\n## Evolution Insights (Global Learnings)\n\n### Common Pitfalls:\n\u26a0\ufe0f (seen 4x) A change that reduced the computed aggregate radius (sum_radii) by ~12% \u2014 e.g., introducing an unintended scaling/normalization, an extra clamp, or an off-by-one in the radius accumulation \u2014 caused target_ratio and combined_score to fall from 0.3642 to 0.3205 across iterations 1\u201328. When modifying geometry/aggregation code, avoid introducing implicit scalings or clamps without explicit tests; add unit/regression checks for sum_radii and dependent metrics, log intermediate radius values, and review precision/normalization changes to catch unintended shrinkage early.\n\u26a0\ufe0f (seen 4x) A code change that reduced the aggregate coverage/allocatable radius (sum_radii fell from 0.9598 to 0.8446)\u2014for example shrinking buffers, tightening placement radii, or simplifying the packing/placement heuristic\u2014caused a consistent ~12% drop in target_ratio and combined_score across multiple iterations. Avoid making capacity-reducing or constraint-tightening optimizations without targeted benchmarks and automated regression checks; whenever you adjust allocation radii, buffer/window sizes, or packing heuristics, compare against prior baselines to catch and revert harmful target_ratio regressions.\n\u26a0\ufe0f (seen 4x) A recent change effectively reduced the search/matching radius (sum_radii fell from 0.9598 to 0.8446), tightening matching/coverage and causing the target_ratio/combined_score to drop ~12% (0.364 \u2192 0.321). When modifying geometric parameters, normalization, or pruning logic, avoid hard\u2011coding or implicitly scaling radii\u2014always run targeted regression tests for combined_score/target_ratio and make such parameters configurable so you can quickly tune or roll back if performance regresses.\n\u26a0\ufe0f (seen 1x) A change short-circuited or replaced the radii/target-ratio computation (or the object-initialization path) with a no-op/default-zero path so sum_radii and target_ratio collapsed to 0.0 and the overall validity check flipped from valid to invalid (1.000 \u2192 0.000).  \n\nWhen modifying initialization or computation code, avoid introducing default-zero returns or gating that skips construction; add unit/regression tests and assertions that core invariants (e.g., sum_radii > 0, validity == true) remain true and include CI checks to catch regressions early.\n\u26a0\ufe0f (seen 1x) The change introduced a faster, approximate/optimized evaluation path (e.g., an algorithmic shortcut or inlined loop) that cut eval_time by ~11% (0.060\u21920.054) but slightly increased sum_radii and target_ratio, indicating the optimization altered numeric results rather than being a pure refactor. When making similar micro-optimizations, avoid changing numerical semantics or thresholds\u2014add precise numeric regression tests, run broader benchmarks, and verify that any speed gains don\u2019t introduce unacceptable deviations in result quality.", "user": "# Current Program Information\n- Fitness: 0.4846\n- Feature coordinates: No feature coordinates\n- Focus areas: - Fitness declined: 0.8306 \u2192 0.4846. Consider revising recent changes.\n- Consider simplifying - code length exceeds 500 characters\n\n## Last Execution Output\n\n### stdout\n```\nRunning in subprocess, Python version: 3.12.1 (v3.12.1:2305ca5144, Dec  7 2023, 17:23:38) [Clang 13.0.0 (clang-1300.0.29.30)]\nProgram path: /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmpshjflrx1.py\nCalling run_packing()...\nrun_packing() returned successfully: sum_radii = 1.2770469613566842\nResults saved to /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmpzcvoldr_.py.results\n\n```\n\n### stderr\n```\n\n```\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 3\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.1886, target_ratio: 0.8306, combined_score: 0.8306, eval_time: 0.0560\n- Outcome: Mixed results\n\n### Attempt 2\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.1886, target_ratio: 0.8306, combined_score: 0.8306, eval_time: 0.0538\n- Outcome: Mixed results\n\n### Attempt 1\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.2000, target_ratio: 0.8349, combined_score: 0.8349, eval_time: 0.0548\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.8349)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for n=26 circles in the unit square.\n\nImprovements over prior versions:\n- Small deterministic grid-search over horizontal/vertical spacing scales to\n  pick the best-shaped hex-like block for 26 circles.\n- A more aggressive growth-then-robust-shrink radii solver that fully expands\n  radii toward slack then repeatedly resolves overlaps (stronger convergence\n  to a larger feasible sum of radii).\nReturns centers (26,2), radii (26,), sum_radii.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    rows = [6, 5, 6, 5, 4]  # total 26\n    R = len(rows)\n    max_k = max(rows)\n    margin = 1e-6\n    # base horizontal spacing (widest row)\n    if max_k > 1:\n        s0 = (1.0 - 2 * margin) / (max_k - 1)\n    else:\n        s0 = 0.5\n\n    # deterministic candidate scales (tries a few compressions to balance edge effects)\n    s_scales = np.array([1.00, 0.98, 0.96, 0.94, 0.92])\n    y_scales = np.array([1.00, 0.98, 0.96])\n\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    for ss in s_scales:\n        for ys in y_scales:\n            s = s0 * ss\n            h = s * np.sqrt(3.0) / 2.0 * ys\n            # if vertical span too large, rescale to fit exactly\n            span_v = (R - 1) * h\n            if span_v > 1.0 - 2 * margin:\n                h = (1.0 - 2 * margin) / (R - 1)\n                s = h * 2.0 / np.sqrt(3.0)\n            # build centered rows\n            y0 = 0.5 - ((R - 1) * h) / 2.0\n            centers = []\n            for i, k in enumerate(rows):\n                y = y0 + i * h\n                left = 0.5 - ((k - 1) * s) / 2.0\n                xs = left + np.arange(k) * s\n                for x in xs:\n                    centers.append([float(x), float(y)])\n            centers = np.array(centers[:n], dtype=float)\n            # minor safeguard: clip into unit square (numerical)\n            centers = np.clip(centers, margin, 1.0 - margin)\n\n            radii = compute_max_radii_aggressive(centers)\n            sumr = float(np.sum(radii))\n            if sumr > best_sum:\n                best_sum = sumr\n                best_centers = centers\n                best_radii = radii\n\n    return best_centers, best_radii, float(np.sum(best_radii))\n\n\ndef compute_max_radii_aggressive(centers, max_outer=300, max_inner=60, tol=1e-10):\n    \"\"\"\n    Aggressive grow-then-shrink solver:\n    - start with conservative radii (min border, 0.5*nearest)\n    - repeatedly:\n        * compute allowed = min(border, min_j (D_ij - r_j)) and set radii = allowed\n        * run an inner loop that repeatedly resolves overlaps by proportional scaling\n          (using the most restrictive scale per circle) until no overlaps remain\n    This often reaches a larger feasible sum than tiny incremental growth steps.\n    \"\"\"\n    n = centers.shape[0]\n    if n == 0:\n        return np.array([])\n\n    # border limits\n    border = np.minimum.reduce([centers[:, 0], centers[:, 1],\n                                1.0 - centers[:, 0], 1.0 - centers[:, 1]])\n    # pairwise distances\n    dif = centers[:, None, :] - centers[None, :, :]\n    D = np.hypot(dif[..., 0], dif[..., 1])\n    np.fill_diagonal(D, np.inf)\n\n    # initial safe radii\n    min_pair = np.min(D, axis=1)\n    radii = np.minimum(border, 0.5 * min_pair)\n    radii = np.maximum(radii, 0.0)\n\n    for outer in range(max_outer):\n        old = radii.copy()\n\n        # compute allowed growth (full assignment)\n        slack = D - radii[None, :]   # allowed remaining toward each other\n        allowed = np.minimum(border, np.min(slack, axis=1))\n        allowed = np.maximum(allowed, 0.0)\n        radii = allowed.copy()\n\n        # inner robust shrink: resolve overlaps thoroughly\n        for inner in range(max_inner):\n            ri = radii[:, None]\n            rj = radii[None, :]\n            sumr = ri + rj\n            viol = sumr > D + 1e-14\n            if not np.any(viol):\n                break\n            # per-pair scale factors (<=1)\n            factors = np.ones_like(sumr)\n            factors[viol] = D[viol] / sumr[viol]\n            # per-circle minimal scale\n            min_scale = factors.min(axis=1)\n            # clamp to avoid underflow\n            min_scale = np.maximum(min_scale, 1e-12)\n            radii *= min_scale\n            # ensure wall limits\n            radii = np.minimum(radii, border)\n\n        # small relaxation to avoid infinite oscillation:\n        radii = np.minimum(radii, border)\n\n        if np.max(np.abs(radii - old)) < tol:\n            break\n\n    radii = np.maximum(radii, 0.0)\n    return radii\n\n# EVOLVE-BLOCK-END\n\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.2000), Performs well on target_ratio (0.8349), Performs well on combined_score (0.8349), Performs well on eval_time (0.0548)\n\n### Program 2 (Score: 0.8306)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for n=26 circles in the unit square.\n\nDesign:\n- Place centers in a compact hex-like layered layout tuned for 26 circles: rows [6,5,6,5,4].\n- Compute near-maximal non-overlapping radii for fixed centers via a stable,\n  vectorized iterative relaxation: (1) resolve overlaps by per-pair proportional\n  shrinking using the most restrictive scale per circle, (2) enforce wall limits,\n  (3) perform controlled growth toward available slack to increase the sum.\nReturns centers (26,2), radii (26,), sum_radii.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    rows = [6, 5, 6, 5, 4]  # sums to 26\n    R = len(rows)\n    max_k = max(rows)\n\n    # horizontal spacing chosen to nearly fill unit width for widest row\n    margin = 1e-6\n    s = (1.0 - 2 * margin) / (max_k - 1) if max_k > 1 else 0.5\n    h = s * np.sqrt(3.0) / 2.0\n\n    # If vertical span would exceed unit, scale down spacing\n    span_v = (R - 1) * h\n    if span_v > 1.0 - 2 * margin:\n        h = (1.0 - 2 * margin) / (R - 1)\n        s = h * 2.0 / np.sqrt(3.0)\n\n    # build centered rows deterministically\n    y0 = 0.5 - ((R - 1) * h) / 2.0\n    centers = []\n    for i, k in enumerate(rows):\n        y = y0 + i * h\n        left = 0.5 - ((k - 1) * s) / 2.0\n        xs = left + np.arange(k) * s\n        for x in xs:\n            centers.append([float(x), float(y)])\n    centers = np.array(centers[:n], dtype=float)\n\n    radii = compute_max_radii(centers)\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\ndef compute_max_radii(centers, max_iters=1200, tol=1e-11):\n    n = centers.shape[0]\n    if n == 0:\n        return np.array([])\n    # initial radii limited by walls\n    border = np.minimum.reduce([centers[:,0], centers[:,1],\n                                1.0 - centers[:,0], 1.0 - centers[:,1]])\n    radii = border.copy()\n\n    if n <= 1:\n        return radii\n\n    # pairwise distances (constant)\n    diffs = centers[:, None, :] - centers[None, :, :]\n    D = np.hypot(diffs[...,0], diffs[...,1])\n    np.fill_diagonal(D, np.inf)\n\n    for it in range(max_iters):\n        old = radii.copy()\n\n        # Resolve overlaps via per-pair proportional scaling factors\n        ri = radii[:, None]\n        rj = radii[None, :]\n        sumr = ri + rj\n        viol = sumr > D\n        if np.any(viol):\n            factors = np.ones_like(sumr)\n            factors[viol] = D[viol] / sumr[viol]\n            # For each circle take the most restrictive scale across its pairs\n            min_scale = factors.min(axis=1)\n            min_scale = np.maximum(min_scale, 1e-12)\n            radii *= min_scale\n\n        # enforce wall limits\n        radii = np.minimum(radii, border)\n\n        # Controlled growth toward available slack:\n        # allowed_i = min(border_i, min_j (D_ij - r_j))\n        slack = D - radii[None, :]  # remaining center-to-center allowance\n        allowed = np.minimum(border, slack.min(axis=1))\n        allowed = np.maximum(allowed, 0.0)\n        # grow partially toward allowed to avoid oscillation (use 0.5 blending)\n        grow_mask = allowed > radii + 1e-14\n        radii[grow_mask] = (radii[grow_mask] + allowed[grow_mask]) / 2.0\n\n        if np.max(np.abs(radii - old)) < tol:\n            break\n\n    radii = np.maximum(radii, 0.0)\n    return radii\n\n# EVOLVE-BLOCK-END\n\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n# Optional visualization helper (not used in automated evaluation)\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(6,6))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect('equal')\n    for c, r in zip(centers, radii):\n        ax.add_patch(Circle(c, r, alpha=0.6, edgecolor='k'))\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.1886), Performs well on target_ratio (0.8306), Performs well on combined_score (0.8306), Performs well on eval_time (0.0538)\n\n### Program 3 (Score: 0.8306)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Improved deterministic constructor for packing n=26 circles in the unit square.\n\nDesign:\n- Use a compact hex-like layered layout tuned for 26 circles: rows [6,5,6,5,4].\n- Compute maximal feasible radii for fixed centers via a stable, vectorized\n  iterative relaxation that (a) resolves overlaps by proportional shrinking\n  using the most restrictive pairwise scale per circle, (b) enforces wall limits,\n  and (c) attempts controlled growth toward available slack to increase the sum\n  of radii.\nReturns centers (26,2), radii (26,), sum_radii.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    # compact hex-like rows summing to 26\n    rows = [6, 5, 6, 5, 4]\n    R = len(rows)\n    max_k = max(rows)\n\n    # choose horizontal spacing so widest row nearly fills the unit width\n    margin = 1e-6\n    if max_k > 1:\n        s = (1.0 - 2 * margin) / (max_k - 1)\n    else:\n        s = 0.5\n    h = s * np.sqrt(3.0) / 2.0\n\n    # if vertical span would exceed unit square, scale down spacing\n    span_v = (R - 1) * h\n    if span_v > 1.0 - 2 * margin:\n        h = (1.0 - 2 * margin) / (R - 1)\n        s = h * 2.0 / np.sqrt(3.0)\n\n    # build centered rows deterministically\n    y0 = 0.5 - ((R - 1) * h) / 2.0\n    centers = []\n    for i, k in enumerate(rows):\n        y = y0 + i * h\n        left = 0.5 - ((k - 1) * s) / 2.0\n        xs = left + np.arange(k) * s\n        for x in xs:\n            centers.append([float(x), float(y)])\n    centers = np.array(centers[:n], dtype=float)\n\n    radii = compute_max_radii(centers)\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\ndef compute_max_radii(centers, max_iters=800, tol=1e-10):\n    n = centers.shape[0]\n    # initial radii limited by walls\n    border = np.minimum.reduce([centers[:,0], centers[:,1],\n                                1.0 - centers[:,0], 1.0 - centers[:,1]])\n    radii = border.copy()\n\n    if n <= 1:\n        return radii\n\n    # precompute pairwise distances\n    diffs = centers[:, None, :] - centers[None, :, :]\n    D = np.hypot(diffs[...,0], diffs[...,1])\n    np.fill_diagonal(D, np.inf)\n\n    for it in range(max_iters):\n        old = radii.copy()\n\n        # resolve overlaps in a vectorized way:\n        ri = radii[:, None]\n        rj = radii[None, :]\n        sumr = ri + rj\n        viol = sumr > D\n        if np.any(viol):\n            # per-pair scale factors (<=1). For non-violations factor=1\n            factors = np.ones_like(sumr)\n            factors[viol] = D[viol] / sumr[viol]\n            # for each circle take the most restrictive (minimum) scale across its pairs\n            min_scale = factors.min(axis=1)\n            # avoid exact zeros\n            min_scale = np.maximum(min_scale, 1e-12)\n            radii *= min_scale\n\n        # enforce walls\n        radii = np.minimum(radii, border)\n\n        # attempt controlled growth toward available slack:\n        # allowed_i = min(border_i, min_j (D_ij - r_j))\n        slack = D - radii[None, :]  # allowed center-to-center remaining for each pair\n        allowed = np.minimum(border, slack.min(axis=1))\n        allowed = np.maximum(allowed, 0.0)\n        grow_mask = allowed > radii + 1e-14\n        radii[grow_mask] = (radii[grow_mask] + allowed[grow_mask]) / 2.0\n\n        if np.max(np.abs(radii - old)) < tol:\n            break\n\n    radii = np.maximum(radii, 0.0)\n    return radii\n\n# EVOLVE-BLOCK-END\n\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n# Optional visualization helper (not used in automated evaluation)\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(6,6))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect('equal')\n    for c, r in zip(centers, radii):\n        ax.add_patch(Circle(c, r, alpha=0.6, edgecolor='k'))\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.1886), Performs well on target_ratio (0.8306), Performs well on combined_score (0.8306), Performs well on eval_time (0.0560)\n\n\n\n## Diverse Programs\n\n### Program D1 (Score: 0.8306)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (hex-like constructor + radius relaxation)\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct an explicit arrangement of 26 circles in a unit square using\n    a hex-like layered layout (rows tuned to 26) and compute near-maximal\n    non-overlapping radii via iterative relaxation.\n    Returns:\n        centers: (26,2) np.array\n        radii: (26,) np.array\n        sum_of_radii: float\n    \"\"\"\n    n = 26\n    # Hex-like row counts summing to 26\n    rows = [6, 5, 6, 5, 4]\n    R = len(rows)\n\n    # pick horizontal spacing s to fit widest row comfortably\n    max_k = max(rows)\n    # choose s so widest row uses full width but allow small margin\n    margin = 1e-6\n    s = (1.0 - 2 * margin) / (max_k - 1) if max_k > 1 else 0.5\n    # hex vertical spacing\n    h = s * np.sqrt(3) / 2.0\n\n    # if total vertical span too big, shrink s to fit\n    span_v = (R - 1) * h\n    if span_v > 1.0 - 2 * margin:\n        h = (1.0 - 2 * margin) / (R - 1)\n        s = h * 2.0 / np.sqrt(3)\n\n    # build centered rows (no arbitrary random jitter \u2014 deterministic)\n    y0 = 0.5 - ((R - 1) * h) / 2.0\n    centers = []\n    for i, k in enumerate(rows):\n        y = y0 + i * h\n        left = 0.5 - ((k - 1) * s) / 2.0\n        xs = left + np.arange(k) * s\n        for x in xs:\n            centers.append([x, y])\n    centers = np.array(centers[:n])\n\n    # compute radii with an iterative relaxation that enforces walls and pairwise constraints\n    radii = compute_max_radii(centers)\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers, max_iters=800, tol=1e-9):\n    \"\"\"\n    Given fixed centers inside [0,1]^2, compute radii so circles are non-overlapping\n    and lie inside the square. The algorithm:\n      - start with wall-limited radii\n      - repeatedly shrink to resolve overlaps (proportional scaling)\n      - re-apply wall limits\n      - attempt controlled growth toward available slack\n    \"\"\"\n    n = centers.shape[0]\n    # initial radii limited by walls\n    border = np.minimum.reduce([centers[:, 0], centers[:, 1],\n                                1.0 - centers[:, 0], 1.0 - centers[:, 1]])\n    radii = border.copy()\n\n    if n <= 1:\n        return radii\n\n    # pairwise distance matrix (constant)\n    diffs = centers[:, None, :] - centers[None, :, :]\n    D = np.hypot(diffs[..., 0], diffs[..., 1])\n    np.fill_diagonal(D, np.inf)\n\n    for it in range(max_iters):\n        old = radii.copy()\n\n        # resolve overlaps: if r_i + r_j > d_ij, apply pairwise proportional scaling\n        ri = radii[:, None]\n        rj = radii[None, :]\n        sumr = ri + rj\n        viol = sumr > D\n        if np.any(viol):\n            # compute per-pair scaling factors (<=1)\n            factors = np.ones_like(sumr)\n            factors[viol] = D[viol] / sumr[viol]\n            # for each circle take the most restrictive scale across its pairs\n            min_scale = factors.min(axis=1)\n            # avoid numerical zeros: clamp to tiny positive\n            min_scale = np.maximum(min_scale, 1e-12)\n            radii *= min_scale\n\n        # enforce wall limits\n        radii = np.minimum(radii, border)\n\n        # attempt to grow toward available slack:\n        # allowed_i = min(border_i, min_j (D_ij - r_j))\n        slack = D - radii[None, :]  # allowed center-to-center distance remaining for each pair\n        # since D diagonal is inf, min excludes self\n        allowed = np.minimum(border, slack.min(axis=1))\n        allowed = np.maximum(allowed, 0.0)\n        # grow partially toward allowed to avoid oscillation\n        grow_mask = allowed > radii\n        radii[grow_mask] = (radii[grow_mask] + allowed[grow_mask]) / 2.0\n\n        if np.max(np.abs(radii - old)) < tol:\n            break\n\n    radii = np.maximum(radii, 0.0)\n    return radii\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, ec=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nKey features: Alternative approach to validity, Alternative approach to sum_radii\n\n### Program D2 (Score: 0.8306)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor-based circle packing for n=26 (hex-like rows + vectorized relaxation)\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Build centers in hex-like layered rows tuned for 26 circles and compute\n    near-maximal non-overlapping radii with a vectorized relaxation.\n    Returns: centers (26,2), radii (26,), sum_of_radii\n    \"\"\"\n    n = 26\n    rows = [6, 5, 6, 5, 4]  # sum = 26\n    R = len(rows)\n    max_k = max(rows)\n\n    # small margin to keep centers inside square and allow positive radii\n    margin = 1e-6\n    # horizontal spacing so widest row spans available width\n    sx = (1.0 - 2 * margin) / (max_k - 1) if max_k > 1 else 0.5\n    # hex vertical spacing\n    sy = sx * np.sqrt(3.0) / 2.0\n\n    # if vertical span too large, scale down spacing to fit\n    span_v = (R - 1) * sy\n    if span_v > 1.0 - 2 * margin:\n        sy = (1.0 - 2 * margin) / (R - 1)\n        sx = sy * 2.0 / np.sqrt(3.0)\n\n    y0 = 0.5 - ((R - 1) * sy) / 2.0\n    centers = []\n    for i, k in enumerate(rows):\n        y = y0 + i * sy\n        left = 0.5 - ((k - 1) * sx) / 2.0\n        xs = left + np.arange(k) * sx\n        for x in xs:\n            centers.append([x, y])\n    centers = np.array(centers[:n], dtype=float)\n\n    radii = compute_max_radii(centers)\n    return centers, radii, float(np.sum(radii))\n\n\ndef compute_max_radii(centers, max_iters=1000, tol=1e-9):\n    \"\"\"\n    Vectorized relaxation:\n      - initialize radii by distance to walls\n      - repeatedly resolve overlaps by proportional shrinking (use per-pair scales)\n      - then partially grow toward available slack to increase sum of radii\n    \"\"\"\n    n = centers.shape[0]\n    if n == 0:\n        return np.array([])\n\n    # initial radii limited by walls\n    border = np.minimum.reduce([centers[:, 0], centers[:, 1],\n                                1.0 - centers[:, 0], 1.0 - centers[:, 1]])\n    radii = border.copy()\n\n    if n == 1:\n        return radii\n\n    # pairwise distances matrix\n    diffs = centers[:, None, :] - centers[None, :, :]\n    D = np.hypot(diffs[..., 0], diffs[..., 1])\n    np.fill_diagonal(D, np.inf)\n\n    for _ in range(max_iters):\n        old = radii.copy()\n\n        # resolve overlaps: if r_i + r_j > d_ij, compute per-pair scale factors <=1\n        ri = radii[:, None]\n        rj = radii[None, :]\n        sumr = ri + rj\n        viol = sumr > D\n        if np.any(viol):\n            factors = np.ones_like(sumr)\n            factors[viol] = D[viol] / sumr[viol]\n            # take most restrictive scale for each circle\n            min_scale = factors.min(axis=1)\n            # avoid numerical zeros\n            min_scale = np.maximum(min_scale, 1e-12)\n            radii *= min_scale\n\n        # enforce wall limits\n        radii = np.minimum(radii, border)\n\n        # attempt controlled growth toward allowed = min(border, min_j (D_ij - r_j))\n        slack = D - radii[None, :]  # for each i,j remaining center-to-center slack\n        allowed = np.minimum(border, slack.min(axis=1))\n        allowed = np.maximum(allowed, 0.0)\n        grow_mask = allowed > radii\n        # grow halfway toward allowed to avoid oscillation\n        radii[grow_mask] = 0.5 * (radii[grow_mask] + allowed[grow_mask])\n\n        if np.max(np.abs(radii - old)) < tol:\n            break\n\n    radii = np.maximum(radii, 0.0)\n    return radii\n\n\n# EVOLVE-BLOCK-END\n\n\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"Optional visualization (requires matplotlib)\"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(7, 7))\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n    for i, (c, r) in enumerate(zip(centers, radii)):\n        ax.add_patch(Circle(c, r, alpha=0.6, ec=\"k\"))\n        ax.text(c[0], c[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n    plt.title(f\"n={len(centers)}, sum radii={sum(radii):.6f}\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, s = run_packing()\n    print(f\"Sum of radii: {s:.6f}\")\n    # visualize(centers, radii)\n```\nKey features: Alternative approach to validity, Alternative approach to sum_radii\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 0.6665, Type: Alternative)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for packing 26 circles in a unit square.\n\nDesign: a compact hex-like layered layout tuned for 26 circles (rows: 6,5,6,5,4)\nand a robust radii relaxation that alternates proportional overlap removal with\ncontrolled growth toward available slack. The routines are vectorized with numpy.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    rows = [6, 5, 6, 5, 4]  # sums to 26, compact hex-like layering\n    R = len(rows)\n    max_k = max(rows)\n\n    margin = 0.003  # small margin to the square boundary\n    # horizontal spacing: fit widest row into available width\n    s = (1.0 - 2 * margin) / (max_k - 1) if max_k > 1 else 0.5\n    h = s * np.sqrt(3) / 2.0  # hex vertical spacing\n\n    # if vertical span doesn't fit, recompute s from vertical constraint\n    span_v = (R - 1) * h\n    if span_v > 1.0 - 2 * margin:\n        h = (1.0 - 2 * margin) / (R - 1)\n        s = h * 2.0 / np.sqrt(3)\n        span_v = (R - 1) * h\n\n    y0 = 0.5 - span_v / 2.0\n    centers = []\n    for i, k in enumerate(rows):\n        y = y0 + i * h\n        # center each row horizontally\n        left = 0.5 - ((k - 1) * s) / 2.0\n        # stagger alternate rows by half spacing for hex-like packing\n        if i % 2 == 1:\n            left += s / 2.0\n        xs = left + np.arange(k) * s\n        for x in xs:\n            # clip to remain inside margin\n            centers.append((np.clip(x, margin, 1.0 - margin),\n                            np.clip(y, margin, 1.0 - margin)))\n    centers = np.asarray(centers[:n])\n    assert centers.shape[0] == n\n\n    radii = compute_max_radii(centers)\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\ndef compute_max_radii(centers, max_iters=1200, tol=1e-10):\n    \"\"\"Compute feasible radii for fixed centers by alternating shrink/grow steps.\"\"\"\n    n = centers.shape[0]\n    # distance to walls\n    border = np.minimum.reduce([centers[:, 0], centers[:, 1],\n                                1.0 - centers[:, 0], 1.0 - centers[:, 1]])\n    # start with wall-limited radii\n    radii = border.copy()\n    if n <= 1:\n        return radii\n\n    # pairwise distances matrix (constant)\n    diffs = centers[:, None, :] - centers[None, :, :]\n    D = np.hypot(diffs[..., 0], diffs[..., 1])\n    np.fill_diagonal(D, np.inf)\n\n    for it in range(max_iters):\n        old = radii.copy()\n\n        # 1) Resolve overlaps by proportional scaling using per-circle most restrictive factor\n        ri = radii[:, None]\n        rj = radii[None, :]\n        sumr = ri + rj\n        viol = sumr > D\n        if np.any(viol):\n            factors = np.ones_like(sumr)\n            factors[viol] = D[viol] / sumr[viol]\n            # per-circle scale is the minimum factor across its pairs\n            min_scale = factors.min(axis=1)\n            min_scale = np.maximum(min_scale, 1e-12)\n            radii *= min_scale\n\n        # re-apply border constraints\n        radii = np.minimum(radii, border)\n\n        # 2) Growth step: compute for each circle the tightest limit by neighbors\n        # allowed_i = min(border_i, min_j (D_ij - r_j))\n        slack = D - radii[None, :]  # D_ij - r_j\n        allowed = np.minimum(border, slack.min(axis=1))\n        allowed = np.maximum(allowed, 0.0)\n        # move partially toward allowed to avoid oscillation\n        grow_mask = allowed > radii + 1e-14\n        # use a moderate growth factor for stability\n        radii[grow_mask] = radii[grow_mask] * 0.4 + allowed[grow_mask] * 0.6\n\n        # early exit on convergence\n        if np.max(np.abs(radii - old)) < tol:\n            break\n\n    radii = np.maximum(radii, 0.0)\n    return radii\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(7,7))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect(\"equal\")\n    for i, (c, r) in enumerate(zip(centers, radii)):\n        ax.add_patch(Circle(c, r, alpha=0.6, ec='k'))\n        ax.text(c[0], c[1], str(i), ha='center', va='center', fontsize=8)\n    plt.title(f\"n={len(centers)}, sum radii={sum(radii):.6f}\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # visualize(centers, radii)\n# EVOLVE-BLOCK-END\n```\nUnique approach: Modification: Full rewrite, Excellent validity (1.000), Excellent sum_radii (1.756)\n\n### Inspiration 2 (Score: 0.4846, Type: Experimental)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for packing n=26 circles in the unit square.\nUses an explicit hex-like layered layout (rows [5,6,6,6,3]) and a stable\niterative proportional relaxation to compute maximal non-overlapping radii.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    rows = [5, 6, 6, 6, 3]  # sums to 26\n    num_rows = len(rows)\n    max_k = max(rows)\n\n    edge = 0.01  # margin from square boundary\n\n    # horizontal spacing chosen so the widest row fits, allowing half-step offsets\n    s_h = (1.0 - 2 * edge) / (max_k - 1 + 0.5)\n    s_v = s_h * np.sqrt(3) / 2.0  # hex vertical spacing\n\n    # If vertical span too large, rescale s_h and s_v to fit\n    if (num_rows - 1) * s_v > (1.0 - 2 * edge):\n        s_v = (1.0 - 2 * edge) / (num_rows - 1)\n        s_h = s_v * 2.0 / np.sqrt(3)\n\n    # center rows vertically and each row horizontally\n    span_y = (num_rows - 1) * s_v\n    y0 = 0.5 - span_y / 2.0\n    centers = []\n    for i, k in enumerate(rows):\n        y = y0 + i * s_v\n        x0 = 0.5 - ((k - 1) * s_h) / 2.0\n        for j in range(k):\n            x = x0 + j * s_h\n            # clip to margin-safe region\n            x = float(np.clip(x, edge, 1 - edge))\n            yc = float(np.clip(y, edge, 1 - edge))\n            centers.append((x, yc))\n\n    centers = np.array(centers)\n    assert centers.shape[0] == n\n\n    radii = compute_max_radii(centers)\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\ndef compute_max_radii(centers, max_iters=400):\n    \"\"\"\n    Initialize radii to distance to nearest wall, then iteratively enforce\n    pairwise constraints r_i + r_j <= d_ij by proportional reductions.\n    \"\"\"\n    n = centers.shape[0]\n    # limit by walls\n    wall_dists = np.minimum.reduce([centers[:,0], centers[:,1], 1-centers[:,0], 1-centers[:,1]])\n    radii = wall_dists.copy()\n\n    # precompute pairwise distances\n    diffs = centers[:, None, :] - centers[None, :, :]\n    dists = np.sqrt(np.sum(diffs**2, axis=2))\n    eps = 1e-12\n\n    for _ in range(max_iters):\n        changed = False\n        # pairwise constraints\n        for i in range(n):\n            ri = radii[i]\n            for j in range(i+1, n):\n                rj = radii[j]\n                d = dists[i, j]\n                if d <= eps:\n                    # coincide: collapse both gently\n                    if radii[i] > 0:\n                        radii[i] = 0.0\n                        changed = True\n                    if radii[j] > 0:\n                        radii[j] = 0.0\n                        changed = True\n                    continue\n                s = ri + rj\n                # if overlap beyond tiny slack\n                if s > d - 1e-9:\n                    factor = max((d - 1e-9) / s, 1e-9)\n                    # scale both proportionally\n                    radii[i] *= factor\n                    radii[j] *= factor\n                    changed = True\n                    # update local ri/rj to propagate within inner loop\n                    ri = radii[i]\n                    rj = radii[j]\n        # enforce walls\n        for i in range(n):\n            if radii[i] > wall_dists[i]:\n                radii[i] = wall_dists[i]\n                changed = True\n        if not changed:\n            break\n\n    radii = np.maximum(radii, 0.0)\n    return radii\n\n# EVOLVE-BLOCK-END\n\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(6,6))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect(\"equal\")\n    for c, r in zip(centers, radii):\n        ax.add_patch(Circle(c, r, alpha=0.6, edgecolor='k'))\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # visualize(centers, radii)\n```\nUnique approach: Modification: Full rewrite, Excellent validity (1.000), Excellent sum_radii (1.277)\n\n### Inspiration 3 (Score: 0.5699, Type: Experimental)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (explicit hex-like constructor)\nProduces deterministic centers for 26 circles and computes maximal feasible radii\nvia a Gauss-Seidel style iterative solver for the pairwise non-overlap constraints.\n\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square.\n\n    Returns:\n        centers: np.array of shape (26, 2)\n        radii: np.array of shape (26,)\n        sum_radii: float\n    \"\"\"\n    n = 26\n\n    # Row pattern chosen to approximate a truncated hexagonal packing:\n    # counts per row sum to 26 (5 + 6 + 5 + 6 + 4 = 26)\n    row_counts = [5, 6, 5, 6, 4]\n    s = 0.12  # nominal horizontal spacing for the lattice; will be scaled to fit\n\n    coords = []\n    for r, count in enumerate(row_counts):\n        # vertical spacing in hex grid is sqrt(3)/2 * s\n        y = r * (np.sqrt(3) / 2.0) * s\n        # alternate rows are offset by s/2\n        x0 = 0.5 * s if (r % 2) else 0.0\n        for k in range(count):\n            coords.append([x0 + k * s, y])\n\n    centers = np.array(coords)\n    assert centers.shape[0] == n\n\n    # Normalize bounding box to fit nicely inside [pad, 1 - pad]^2\n    pad = 0.02\n    mins = centers.min(axis=0)\n    maxs = centers.max(axis=0)\n    span = maxs - mins\n    # handle degenerate span (shouldn't happen here)\n    span[span == 0] = 1.0\n    centers = (centers - mins) / span  # now in [0,1] box\n    centers = centers * (1 - 2 * pad) + pad\n\n    # Move packing slightly toward center to reduce boundary crowding (small translation)\n    # This preserves relative arrangement but can improve achievable radii.\n    shift = np.array([0.0, 0.0])  # kept zero for determinism; tweak if desired\n    centers = np.clip(centers + shift, pad, 1 - pad)\n\n    radii = compute_max_radii(centers)\n\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers, max_iter=5000, tol=1e-8):\n    \"\"\"\n    Compute (approximately) maximal radii for given fixed centers so that:\n      - each circle stays inside the unit square\n      - circles do not overlap: r_i + r_j <= dist(c_i, c_j)\n\n    Uses an iterative Gauss-Seidel style update:\n      r_i <- min(distance_to_border_i, min_j max(0, dist_ij - r_j))\n\n    This finds the largest feasible radii vector consistent with the constraints.\n    \"\"\"\n    n = centers.shape[0]\n    # initial upper bounds from border distances\n    border = np.minimum.reduce([centers[:, 0], centers[:, 1], 1 - centers[:, 0], 1 - centers[:, 1]])\n    radii = border.copy()\n\n    # Precompute pairwise distances\n    dif = centers[:, None, :] - centers[None, :, :]  # shape (n,n,2)\n    dists = np.sqrt(np.sum(dif * dif, axis=2))\n    # Avoid self-distances of zero causing issues\n    np.fill_diagonal(dists, np.inf)\n\n    for it in range(int(max_iter)):\n        max_change = 0.0\n        # Gauss-Seidel: update each radius using the most recent values of others\n        for i in range(n):\n            # start with border constraint\n            limit = border[i]\n            # neighbor constraints: r_i <= dist_ij - r_j for all j\n            # compute dist_ij - r_j and take the minimum (but not negative)\n            # Use vectorized operations for speed inside loop\n            limits_from_neighbors = dists[i, :] - radii\n            # ignore self (set to +inf)\n            limits_from_neighbors[i] = np.inf\n            # the effective limit is min(border, min_j limits_from_neighbors)\n            neighbor_min = limits_from_neighbors.min()\n            if neighbor_min < limit:\n                limit = max(0.0, neighbor_min)\n            change = abs(radii[i] - limit)\n            if change:\n                radii[i] = limit\n                if change > max_change:\n                    max_change = change\n        if max_change < tol:\n            break\n\n    # Final safeguard: clip radii nonnegative and to border\n    radii = np.clip(radii, 0.0, border)\n    return radii\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nUnique approach: Modification: Full rewrite, Excellent validity (1.000), Excellent sum_radii (1.502)\n\n# Current Program\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (improved deterministic constructor)\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square\n    that attempts to maximize the sum of their radii using a\n    deterministic hex-like layered layout and an iterative radius relaxation.\n    Returns:\n        centers: np.array (26,2)\n        radii: np.array (26,)\n        sum_of_radii: float\n    \"\"\"\n    n = 26\n\n    # Row structure (sensible hex-like layering that sums to 26)\n    rows = [5, 6, 6, 6, 3]  # total 26\n    num_rows = len(rows)\n    max_k = max(rows)\n\n    # small margin from walls\n    edge = 0.01\n\n    # choose a candidate horizontal spacing s so widest row fits comfortably\n    # include a safety 0.5*s to accommodate offset rows\n    s_h = (1.0 - 2 * edge) / (max_k - 1 + 0.5)\n\n    # vertical hex spacing\n    s_v = s_h * np.sqrt(3) / 2.0\n\n    # ensure vertical fit: adjust s_h if vertical span is too big\n    if (num_rows - 1) * s_v > (1.0 - 2 * edge):\n        s_v = (1.0 - 2 * edge) / (num_rows - 1)\n        s_h = s_v * 2.0 / np.sqrt(3)\n\n    # build centers: y positions centered, x positions centered for each row\n    span_y = (num_rows - 1) * s_v\n    y0 = 0.5 - span_y / 2.0\n    centers = []\n    for i, k in enumerate(rows):\n        y = y0 + i * s_v\n        # center the k points horizontally with spacing s_h\n        x0 = 0.5 - ((k - 1) * s_h) / 2.0\n        for j in range(k):\n            x = x0 + j * s_h\n            # clip to remain inside [edge,1-edge]\n            x = np.clip(x, edge, 1 - edge)\n            y = np.clip(y, edge, 1 - edge)\n            centers.append((x, y))\n\n    centers = np.array(centers)\n    assert centers.shape[0] == n\n\n    # Compute radii via iterative relaxation:\n    radii = compute_max_radii(centers)\n\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers):\n    \"\"\"\n    Compute maximum non-overlapping radii for given centers inside unit square.\n    Uses iterative proportional reduction for violating pairs and enforces wall limits.\n    \"\"\"\n    n = centers.shape[0]\n    eps = 1e-12\n\n    # initial radii limited by walls\n    wall_dists = np.minimum.reduce([centers[:, 0], centers[:, 1], 1 - centers[:, 0], 1 - centers[:, 1]])\n    # start with wall distances (largest possible individually)\n    radii = wall_dists.copy()\n\n    # Precompute pairwise distances\n    diffs = centers[:, None, :] - centers[None, :, :]\n    dists = np.sqrt(np.sum(diffs ** 2, axis=2)) + np.eye(n)  # avoid zeros on diagonal\n\n    # Iteratively fix overlaps: ensure r_i + r_j <= d_ij\n    # We'll loop and reduce both radii proportionally when violation occurs.\n    max_iters = 300\n    for _ in range(max_iters):\n        changed = False\n\n        # enforce pairwise constraints\n        for i in range(n):\n            # vectorize j loop for speed\n            # compute j>i to avoid duplicate work\n            for j in range(i + 1, n):\n                d = dists[i, j]\n                if d <= eps:\n                    # centers coincide (unlikely in our constructor) -> collapse radii\n                    if radii[i] > 0:\n                        radii[i] = 0.0\n                        changed = True\n                    if radii[j] > 0:\n                        radii[j] = 0.0\n                        changed = True\n                    continue\n                s = radii[i] + radii[j]\n                if s > d - 1e-9:\n                    # proportional scale to remove overlap (leave tiny slack)\n                    factor = (d - 1e-9) / s\n                    # To avoid zeroing tiny distances, if factor<0 set to very small positive\n                    factor = max(factor, 1e-9)\n                    # Scale both but prefer to preserve larger relative sizes (proportional)\n                    radii[i] *= factor\n                    radii[j] *= factor\n                    changed = True\n\n        # enforce wall constraints\n        for i in range(n):\n            if radii[i] > wall_dists[i]:\n                radii[i] = wall_dists[i]\n                changed = True\n\n        if not changed:\n            break\n\n    # Final pass: clamp tiny negatives / numerical issues\n    radii = np.maximum(radii, 0.0)\n    return radii\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor='k')\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\n\n# Task\nRewrite the program to improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: complexity, diversity\nDifferent solutions with similar fitness but different features are valuable.\nProvide the complete new program code.\n\nIMPORTANT: Make sure your rewritten program maintains the same inputs and outputs\nas the original program, but with improved internal implementation.\n\n```python\n# Your rewritten program here\n```", "responses": ["```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for n=26 circles in the unit square.\n\nStrategy:\n- Place centers in a compact hex-like layered layout tuned for 26 circles:\n  rows = [6,5,6,5,4].\n- Try a small set of horizontal/vertical scale candidates to balance edge effects.\n- For each candidate, compute near-maximal radii with an aggressive\n  grow-then-robust-shrink relaxation (vectorized). Pick the best sum of radii.\nReturns: centers (26,2), radii (26,), sum_radii (float).\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    rows = [6, 5, 6, 5, 4]  # sums to 26\n    R = len(rows)\n    max_k = max(rows)\n    margin = 1e-6\n\n    # base spacing for the widest row\n    s0 = (1.0 - 2 * margin) / (max_k - 1) if max_k > 1 else 0.5\n    s_scales = np.array([1.00, 0.985, 0.97, 0.955, 0.94])\n    y_scales = np.array([1.00, 0.98, 0.96])\n\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    for ss in s_scales:\n        for ys in y_scales:\n            s = s0 * ss\n            h = s * np.sqrt(3.0) / 2.0 * ys\n            # if vertical span too large, rescale to fit exactly\n            span_v = (R - 1) * h\n            if span_v > 1.0 - 2 * margin:\n                h = (1.0 - 2 * margin) / (R - 1)\n                s = h * 2.0 / np.sqrt(3.0)\n            # build centered rows (no staggering needed here; rows lengths already vary)\n            y0 = 0.5 - ((R - 1) * h) / 2.0\n            centers = []\n            for i, k in enumerate(rows):\n                y = y0 + i * h\n                left = 0.5 - ((k - 1) * s) / 2.0\n                xs = left + np.arange(k) * s\n                for x in xs:\n                    centers.append([float(x), float(y)])\n            centers = np.array(centers[:n], dtype=float)\n            centers = np.clip(centers, margin, 1.0 - margin)\n\n            radii = compute_max_radii_aggressive(centers)\n            sumr = float(np.sum(radii))\n            if sumr > best_sum:\n                best_sum = sumr\n                best_centers = centers.copy()\n                best_radii = radii.copy()\n\n    return best_centers, best_radii, float(np.sum(best_radii))\n\n\ndef compute_max_radii_aggressive(centers, max_outer=400, max_inner=120, tol=1e-11):\n    \"\"\"\n    Aggressive grow-then-shrink solver:\n    - start with safe radii = min(distance-to-wall, 0.5*nearest-center)\n    - outer loop: compute allowed = min(border, min_j (D_ij - r_j)) and set radii = allowed\n    - inner loop: resolve overlaps thoroughly by computing per-pair scale factors and\n      applying the most restrictive per-circle scale until no violations remain\n    This often attains a larger feasible sum than tiny incremental updates.\n    \"\"\"\n    n = centers.shape[0]\n    if n == 0:\n        return np.array([])\n\n    # border limits\n    border = np.minimum.reduce([centers[:, 0], centers[:, 1],\n                                1.0 - centers[:, 0], 1.0 - centers[:, 1]])\n    # pairwise distances\n    dif = centers[:, None, :] - centers[None, :, :]\n    D = np.hypot(dif[..., 0], dif[..., 1])\n    np.fill_diagonal(D, np.inf)\n\n    # initial safe radii\n    min_pair = np.min(D, axis=1)\n    radii = np.minimum(border, 0.5 * min_pair)\n    radii = np.maximum(radii, 0.0)\n\n    for outer in range(max_outer):\n        old = radii.copy()\n\n        # compute allowed growth target for each circle:\n        # allowed_i = min(border_i, min_j (D_ij - r_j))\n        slack = D - radii[None, :]   # shape (n,n)\n        allowed = np.minimum(border, np.min(slack, axis=1))\n        allowed = np.maximum(allowed, 0.0)\n        # move fully to allowed (aggressive growth), then inner loop will fix overlaps\n        radii = allowed.copy()\n\n        # inner robust shrink: resolve overlaps thoroughly\n        for inner in range(max_inner):\n            ri = radii[:, None]\n            rj = radii[None, :]\n            sumr = ri + rj\n            viol = sumr > D + 1e-14\n            if not np.any(viol):\n                break\n            # per-pair scale factors (<=1)\n            factors = np.ones_like(sumr)\n            factors[viol] = D[viol] / sumr[viol]\n            # per-circle minimal scale (most restrictive across pairs)\n            min_scale = factors.min(axis=1)\n            # clamp to avoid numerical underflow\n            min_scale = np.maximum(min_scale, 1e-12)\n            radii *= min_scale\n            # enforce walls after shrink\n            radii = np.minimum(radii, border)\n\n        # final clamp to walls\n        radii = np.minimum(radii, border)\n\n        if np.max(np.abs(radii - old)) < tol:\n            break\n\n    radii = np.maximum(radii, 0.0)\n    return radii\n\n\n# EVOLVE-BLOCK-END\n\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n```"]}}, "artifacts_json": "{\"stdout\": \"Running in subprocess, Python version: 3.12.1 (v3.12.1:2305ca5144, Dec  7 2023, 17:23:38) [Clang 13.0.0 (clang-1300.0.29.30)]\\nProgram path: /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmp19xnk_ta.py\\nCalling run_packing()...\\nrun_packing() returned successfully: sum_radii = 2.1999995999999995\\nResults saved to /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmpfc5rv_rm.py.results\\n\", \"stderr\": \"\"}", "artifact_dir": null}