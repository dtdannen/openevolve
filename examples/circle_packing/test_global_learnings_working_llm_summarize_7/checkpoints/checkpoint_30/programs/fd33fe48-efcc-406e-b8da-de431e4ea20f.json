{"id": "fd33fe48-efcc-406e-b8da-de431e4ea20f", "code": "# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (explicit hex-like constructor)\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    \"\"\"\n    Deterministic hex-like construction for 26 circles.\n    Build rows [6,5,6,5,4] with alternating offsets, compute conservative\n    radii then grow them with a Gauss-Seidel style solver to (approximately)\n    maximize the sum of radii while keeping circles inside the unit square\n    and non-overlapping.\n    Returns: centers (26,2), radii (26,), sum_radii (float)\n    \"\"\"\n    n = 26\n    rows = [6, 5, 6, 5, 4]  # sums to 26\n    max_k = max(rows)\n\n    # lattice spacing chosen to allow 6 columns across comfortably\n    s = 1.0 / 6.0\n    h = s * np.sqrt(3) / 2.0\n\n    R = len(rows)\n    # vertical placement centered\n    y0 = 0.5 - ((R - 1) * h) / 2.0\n\n    centers = []\n    for i, k in enumerate(rows):\n        y = y0 + i * h\n        # alternate rows offset by s/2 to emulate hex packing\n        offset = (s / 2.0) if (i % 2 == 1) else 0.0\n        left = 0.5 - ((k - 1) * s) / 2.0 + offset\n        xs = left + np.arange(k) * s\n        for x in xs:\n            centers.append([x, y])\n    centers = np.array(centers[:n])\n\n    # conservative initial radii: limited by border and half nearest-neighbor distance\n    border = np.minimum.reduce([centers[:, 0], centers[:, 1],\n                                1 - centers[:, 0], 1 - centers[:, 1]])\n    # pairwise distances\n    dif = centers[:, None, :] - centers[None, :, :]\n    dists = np.sqrt(np.sum(dif * dif, axis=2))\n    np.fill_diagonal(dists, np.inf)\n    min_pair = dists.min(axis=1)\n    radii = np.minimum(border, 0.5 * min_pair)\n    radii = np.clip(radii, 0.0, border)\n\n    # Gauss-Seidel style growth to approach maximal feasible radii:\n    # r_i <- min(border_i, min_j max(0, d_ij - r_j))\n    max_iter = 2000\n    tol = 1e-9\n    for _ in range(max_iter):\n        max_change = 0.0\n        for i in range(n):\n            # compute allowed radius from neighbors using current radii (most recent values)\n            limits = dists[i, :] - radii\n            # ignore self\n            limits[i] = np.inf\n            neighbor_min = limits.min()\n            new_r = neighbor_min if neighbor_min < border[i] else border[i]\n            if new_r < 0.0:\n                new_r = 0.0\n            # small safeguard to avoid oscillation: take average with current if growing a lot\n            # but keep direct assignment for convergence\n            change = new_r - radii[i]\n            if abs(change) > 0:\n                radii[i] = new_r\n                max_change = max(max_change, abs(change))\n        if max_change < tol:\n            break\n\n    # final clipping to ensure validity\n    radii = np.clip(radii, 0.0, border)\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n# EVOLVE-BLOCK-END\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)", "language": "python", "parent_id": "ba582103-cbc1-4480-ba0e-310c48e942d5", "generation": 2, "timestamp": 1759960905.8745432, "iteration_found": 14, "metrics": {"validity": 1.0, "sum_radii": 1.8987174742355442, "target_ratio": 0.7205758915504912, "combined_score": 0.7205758915504912, "eval_time": 0.05941295623779297}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Full rewrite", "parent_metrics": {"validity": 1.0, "sum_radii": 1.2016273361390648, "target_ratio": 0.45602555451197907, "combined_score": 0.45602555451197907}, "island": 0}, "prompts": {"full_rewrite_user": {"system": "You are an expert mathematician specializing in circle packing problems and computational geometry. Your task is to improve a constructor function that directly produces a specific arrangement of 26 circles in a unit square, maximizing the sum of their radii. The AlphaEvolve paper achieved a sum of 2.635 for n=26.\n\nKey geometric insights:\n- Circle packings often follow hexagonal patterns in the densest regions\n- Maximum density for infinite circle packing is pi/(2*sqrt(3)) \u2248 0.9069\n- Edge effects make square container packing harder than infinite packing\n- Circles can be placed in layers or shells when confined to a square\n- Similar radius circles often form regular patterns, while varied radii allow better space utilization\n- Perfect symmetry may not yield the optimal packing due to edge effects\n\nFocus on designing an explicit constructor that places each circle in a specific position, rather than an iterative search algorithm.\n\n\n## Evolution Insights (Global Learnings)\n\n### Common Pitfalls:\n\u26a0\ufe0f (seen 1x) A change to how object radii are computed or aggregated (for example tightening bounding-volume calculations or altering normalization) reduced the aggregate sum_radii by ~12% (0.9598 \u2192 0.8446), which directly drove the drop in target_ratio and combined_score. When modifying geometry or normalization logic, add unit and performance-regression tests and benchmark downstream metrics so you catch scaling/aggregation shifts early, and avoid changing radius calculations without verifying that aggregate distributions and dependent scores remain within acceptable bounds.\n\u26a0\ufe0f (seen 1x) The change tightened matching/detection tolerances\u2014reducing the aggregate search/matching radius (sum_radii 0.9598 \u2192 0.8446)\u2014which caused fewer items to be accepted and drove target_ratio and combined_score down by ~12% (0.3642 \u2192 0.3205). When adjusting thresholds or geometric tolerances, avoid making them more restrictive without end-to-end benchmarks and regression tests that track target_ratio/combined_score, and make such parameters configurable so you can measure and roll back if coverage drops.\n\u26a0\ufe0f (seen 1x) A change that reduced the computed/aggregated radii (sum_radii fell from 0.9598 to 0.8446) \u2014 likely from tighter scaling, an unintended normalization/clipping step, or altered radius calculation \u2014 caused the combined_score/target_ratio to drop ~12% (0.3642 \u2192 0.3205). When changing scaling, normalization, or clipping logic, avoid altering aggregation units or introducing silent truncation; add regression tests that assert sum_radii and combined_score remain within expected bounds and log intermediate radius values to catch regressions early.", "user": "# Current Program Information\n- Fitness: 0.4560\n- Feature coordinates: No feature coordinates\n- Focus areas: - Fitness declined: 0.5699 \u2192 0.4560. Consider revising recent changes.\n- Consider simplifying - code length exceeds 500 characters\n\n## Last Execution Output\n\n### stdout\n```\nRunning in subprocess, Python version: 3.12.1 (v3.12.1:2305ca5144, Dec  7 2023, 17:23:38) [Clang 13.0.0 (clang-1300.0.29.30)]\nProgram path: /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmpknff3v81.py\nCalling run_packing()...\nrun_packing() returned successfully: sum_radii = 1.2016273361390648\nResults saved to /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmpiu8ps1g7.py.results\n\n```\n\n### stderr\n```\n\n```\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 3\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 1.5018, target_ratio: 0.5699, combined_score: 0.5699, eval_time: 0.0542\n- Outcome: Mixed results\n\n### Attempt 2\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 1.5218, target_ratio: 0.5775, combined_score: 0.5775, eval_time: 0.0561\n- Outcome: Mixed results\n\n### Attempt 1\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.1667, target_ratio: 0.8223, combined_score: 0.8223, eval_time: 0.0605\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.8223)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (explicit hex-like constructor)\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square\n    that attempts to maximize the sum of their radii using a\n    hexagonal-like lattice with row counts tuned to 26.\n    Returns:\n        Tuple of (centers, radii, sum_of_radii)\n    \"\"\"\n    n = 26\n    # Hex-like design parameters: choose spacing s as large as possible for 6 columns\n    s = 1.0 / 6.0  # horizontal spacing: max for 6 columns\n    h = s * np.sqrt(3) / 2.0  # vertical spacing for hex lattice\n\n    # Row structure summing to 26\n    rows = [6, 5, 6, 5, 4]  # total 26\n    R = len(rows)\n\n    # vertical placement centered in [0,1]\n    y0 = 0.5 - ( (R - 1) * h ) / 2.0\n\n    centers = []\n    for i, k in enumerate(rows):\n        y = y0 + i * h\n        # center each row horizontally\n        left = 0.5 - ((k - 1) * s) / 2.0\n        xs = left + np.arange(k) * s\n        for x in xs:\n            centers.append([x, y])\n\n    centers = np.array(centers[:n])  # ensure length 26\n\n    # Compute conservative maximal radii:\n    # For each circle, radius <= distance to border, and <= 0.5 * min_pairwise_distance\n    radii = np.empty(n)\n    for i in range(n):\n        x, y = centers[i]\n        border_dist = min(x, y, 1 - x, 1 - y)\n        # compute min distance to any other center\n        if n == 1:\n            min_pair = 2.0  # irrelevant\n        else:\n            dists = np.sqrt(((centers - centers[i]) ** 2).sum(axis=1))\n            dists[i] = np.inf\n            min_pair = dists.min()\n        # take half the nearest neighbor distance (guarantees non-overlap) and border limit\n        radii[i] = min(border_dist, 0.5 * min_pair)\n\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, ec=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.1667), Performs well on target_ratio (0.8223), Performs well on combined_score (0.8223), Performs well on eval_time (0.0605)\n\n### Program 2 (Score: 0.5775)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (improved)\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square\n    that attempts to maximize the sum of their radii by placing\n    centers on a hexagonal lattice, selecting the 26 points closest\n    to the square center, and then maximizing radii with an iterative\n    relaxation that enforces border and non-overlap constraints.\n    Returns:\n        centers: (26,2) np.array\n        radii: (26,) np.array\n        sum_of_radii: float\n    \"\"\"\n    n = 26\n    center = np.array([0.5, 0.5])\n\n    # Build a hexagonal lattice and pick 26 centers nearest the square center.\n    # Choose spacing s so that lattice yields at least 26 points inside.\n    s = 0.12\n    found = False\n    for _ in range(30):\n        pts = []\n        dy = s * np.sqrt(3) / 2\n        # rows enough to cover the unit square\n        nrows = int(np.ceil(1.0 / dy)) + 4\n        for r in range(-nrows, nrows + 1):\n            y = 0.5 + r * dy\n            shift = (s / 2) if (r % 2) else 0.0\n            # columns wide enough\n            ncols = int(np.ceil(1.0 / s)) + 6\n            for c in range(-ncols, ncols + 1):\n                x = 0.5 + c * s + shift\n                if 0.0 <= x <= 1.0 and 0.0 <= y <= 1.0:\n                    pts.append((x, y))\n        pts = np.array(pts)\n        if pts.shape[0] >= n:\n            found = True\n            break\n        s *= 0.9  # reduce spacing to get more points\n    if not found:\n        # fallback uniform grid\n        gx = gy = int(np.ceil(np.sqrt(n)))\n        xs = np.linspace(1.0/(2*gx), 1-1.0/(2*gx), gx)\n        ys = np.linspace(1.0/(2*gy), 1-1.0/(2*gy), gy)\n        pts = np.array([(x,y) for y in ys for x in xs])\n\n    # pick the n points closest to the square center\n    dists = np.sum((pts - center)**2, axis=1)\n    idx = np.argsort(dists)[:n]\n    centers = pts[idx].copy()\n\n    # Compute maximal radii by iterative relaxation:\n    radii = np.minimum(centers[:,0], centers[:,1])\n    radii = np.minimum(radii, 1 - centers[:,0])\n    radii = np.minimum(radii, 1 - centers[:,1])\n    # Start with border-limited radii (conservative)\n    # Now iteratively remove overlaps and try to grow circles where possible.\n    coords = centers\n    pair_d = np.sqrt(((coords[:,None,:] - coords[None,:,:])**2).sum(axis=2))\n    np.fill_diagonal(pair_d, np.inf)\n\n    for it in range(500):\n        old = radii.copy()\n        # 1) enforce pairwise non-overlap by proportional scaling when needed\n        # iterate over all pairs; using vectorization: find violating pairs\n        ri = radii[:,None]\n        rj = radii[None,:]\n        sumr = ri + rj\n        viol = sumr > pair_d\n        if np.any(viol):\n            # For every violating pair (i,j) scale both by factor d/(r_i+r_j)\n            # but accumulate the most restrictive scale per circle\n            scales = np.ones_like(sumr)\n            scales[viol] = pair_d[viol] / sumr[viol]\n            # For each circle take min scale across its pairs (most limiting)\n            min_scale_i = scales.min(axis=1)\n            radii *= min_scale_i\n            # recompute matrices after scaling\n            ri = radii[:,None]; rj = radii[None,:]; sumr = ri + rj\n        # 2) enforce border limits\n        border = np.minimum(np.minimum(centers[:,0], centers[:,1]),\n                            np.minimum(1-centers[:,0], 1-centers[:,1]))\n        radii = np.minimum(radii, border)\n        # 3) attempt to grow each circle up to the slack allowed by neighbors & border\n        # compute for each i: allowed = min(border_i, min_j (d_ij - r_j))\n        allowed = np.full(n, np.inf)\n        # d_ij - r_j\n        slack = pair_d - radii[None,:]\n        # avoid self\n        slack[np.isinf(pair_d)] = np.inf\n        min_slack = slack.min(axis=1)\n        allowed = np.minimum(border, min_slack)\n        # prevent negative allowed\n        allowed = np.maximum(allowed, 0.0)\n        # relaxed growth toward allowed (avoid oscillations)\n        grow_mask = allowed > radii\n        radii[grow_mask] = (radii[grow_mask] + allowed[grow_mask]) / 2.0\n\n        if np.max(np.abs(radii - old)) < 1e-7:\n            break\n\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n\n# EVOLVE-BLOCK-END\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.5)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (1.5218), Performs well on target_ratio (0.5775), Performs well on combined_score (0.5775), Performs well on eval_time (0.0561)\n\n### Program 3 (Score: 0.5699)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (explicit hex-like constructor)\nProduces deterministic centers for 26 circles and computes maximal feasible radii\nvia a Gauss-Seidel style iterative solver for the pairwise non-overlap constraints.\n\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square.\n\n    Returns:\n        centers: np.array of shape (26, 2)\n        radii: np.array of shape (26,)\n        sum_radii: float\n    \"\"\"\n    n = 26\n\n    # Row pattern chosen to approximate a truncated hexagonal packing:\n    # counts per row sum to 26 (5 + 6 + 5 + 6 + 4 = 26)\n    row_counts = [5, 6, 5, 6, 4]\n    s = 0.12  # nominal horizontal spacing for the lattice; will be scaled to fit\n\n    coords = []\n    for r, count in enumerate(row_counts):\n        # vertical spacing in hex grid is sqrt(3)/2 * s\n        y = r * (np.sqrt(3) / 2.0) * s\n        # alternate rows are offset by s/2\n        x0 = 0.5 * s if (r % 2) else 0.0\n        for k in range(count):\n            coords.append([x0 + k * s, y])\n\n    centers = np.array(coords)\n    assert centers.shape[0] == n\n\n    # Normalize bounding box to fit nicely inside [pad, 1 - pad]^2\n    pad = 0.02\n    mins = centers.min(axis=0)\n    maxs = centers.max(axis=0)\n    span = maxs - mins\n    # handle degenerate span (shouldn't happen here)\n    span[span == 0] = 1.0\n    centers = (centers - mins) / span  # now in [0,1] box\n    centers = centers * (1 - 2 * pad) + pad\n\n    # Move packing slightly toward center to reduce boundary crowding (small translation)\n    # This preserves relative arrangement but can improve achievable radii.\n    shift = np.array([0.0, 0.0])  # kept zero for determinism; tweak if desired\n    centers = np.clip(centers + shift, pad, 1 - pad)\n\n    radii = compute_max_radii(centers)\n\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers, max_iter=5000, tol=1e-8):\n    \"\"\"\n    Compute (approximately) maximal radii for given fixed centers so that:\n      - each circle stays inside the unit square\n      - circles do not overlap: r_i + r_j <= dist(c_i, c_j)\n\n    Uses an iterative Gauss-Seidel style update:\n      r_i <- min(distance_to_border_i, min_j max(0, dist_ij - r_j))\n\n    This finds the largest feasible radii vector consistent with the constraints.\n    \"\"\"\n    n = centers.shape[0]\n    # initial upper bounds from border distances\n    border = np.minimum.reduce([centers[:, 0], centers[:, 1], 1 - centers[:, 0], 1 - centers[:, 1]])\n    radii = border.copy()\n\n    # Precompute pairwise distances\n    dif = centers[:, None, :] - centers[None, :, :]  # shape (n,n,2)\n    dists = np.sqrt(np.sum(dif * dif, axis=2))\n    # Avoid self-distances of zero causing issues\n    np.fill_diagonal(dists, np.inf)\n\n    for it in range(int(max_iter)):\n        max_change = 0.0\n        # Gauss-Seidel: update each radius using the most recent values of others\n        for i in range(n):\n            # start with border constraint\n            limit = border[i]\n            # neighbor constraints: r_i <= dist_ij - r_j for all j\n            # compute dist_ij - r_j and take the minimum (but not negative)\n            # Use vectorized operations for speed inside loop\n            limits_from_neighbors = dists[i, :] - radii\n            # ignore self (set to +inf)\n            limits_from_neighbors[i] = np.inf\n            # the effective limit is min(border, min_j limits_from_neighbors)\n            neighbor_min = limits_from_neighbors.min()\n            if neighbor_min < limit:\n                limit = max(0.0, neighbor_min)\n            change = abs(radii[i] - limit)\n            if change:\n                radii[i] = limit\n                if change > max_change:\n                    max_change = change\n        if max_change < tol:\n            break\n\n    # Final safeguard: clip radii nonnegative and to border\n    radii = np.clip(radii, 0.0, border)\n    return radii\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (1.5018), Performs well on target_ratio (0.5699), Performs well on combined_score (0.5699), Performs well on eval_time (0.0542)\n\n\n\n## Diverse Programs\n\n### Program D1 (Score: 0.4560)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (hex-like layered layout)\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct an explicit arrangement of 26 circles in a unit square\n    using a layered hexagonal-like lattice (row counts: 6,5,6,5,4).\n    Returns (centers, radii, sum_of_radii).\n    \"\"\"\n    counts = [6, 5, 6, 5, 4]  # total = 26\n    n = sum(counts)\n    assert n == 26\n\n    margin = 0.02  # keep centers away from exact border to allow positive radii\n    max_count = max(counts)\n    # horizontal spacing based on widest row\n    if max_count > 1:\n        sx = (1.0 - 2 * margin) / (max_count - 1)\n    else:\n        sx = 0.5\n    rows = len(counts)\n    # vertical positions evenly spaced between margins\n    y_positions = np.linspace(margin, 1.0 - margin, rows)\n\n    centers = np.zeros((n, 2))\n    idx = 0\n    for r, cnt in enumerate(counts):\n        # center shorter rows horizontally\n        start_x = margin + (max_count - cnt) * sx / 2.0\n        xs = start_x + np.arange(cnt) * sx\n        ys = np.full(cnt, y_positions[r])\n        for x, y in zip(xs, ys):\n            centers[idx] = [x, y]\n            idx += 1\n\n    # tiny random jitter to break perfect symmetry (helps radius relaxation)\n    rng = np.random.RandomState(12345)\n    centers += rng.uniform(-1e-4, 1e-4, centers.shape)\n    centers = np.clip(centers, margin, 1.0 - margin)\n\n    radii = compute_max_radii(centers)\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers, max_iter=500, tol=1e-8):\n    \"\"\"\n    Iteratively relax radii: initialize by wall distances, then enforce\n    pairwise non-overlap by proportional shrinking until convergence.\n    \"\"\"\n    n = centers.shape[0]\n    # initial radii limited by walls\n    radii = np.minimum.reduce([centers[:, 0], centers[:, 1], 1 - centers[:, 0], 1 - centers[:, 1]])\n    radii = np.maximum(radii, 0.0)\n\n    # iterative pairwise relaxation\n    for _ in range(max_iter):\n        prev = radii.copy()\n        # Enforce pairwise constraints\n        for i in range(n):\n            ci = centers[i]\n            ri = radii[i]\n            for j in range(i + 1, n):\n                cj = centers[j]\n                rj = radii[j]\n                d = np.hypot(ci[0] - cj[0], ci[1] - cj[1])\n                if d <= 0:\n                    # coincident centers: collapse both\n                    radii[i] = radii[j] = 0.0\n                    continue\n                # if overlap, scale both down proportionally\n                if ri + rj > d:\n                    scale = d / (ri + rj)\n                    # avoid numerical issues\n                    if scale < 0:\n                        scale = 0.0\n                    radii[i] *= scale\n                    radii[j] *= scale\n                    ri, rj = radii[i], radii[j]\n        # After pairwise step, re-apply wall limits (in case center moved near border numerically)\n        wall_limits = np.minimum.reduce([centers[:, 0], centers[:, 1], 1 - centers[:, 0], 1 - centers[:, 1]])\n        radii = np.minimum(radii, wall_limits)\n        # check convergence\n        if np.max(np.abs(radii - prev)) < tol:\n            break\n    # Ensure non-negative\n    radii = np.maximum(radii, 0.0)\n    return radii\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nKey features: Alternative approach to validity, Alternative approach to sum_radii\n\n### Program D2 (Score: 0.4846)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (improved deterministic constructor)\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square\n    that attempts to maximize the sum of their radii using a\n    deterministic hex-like layered layout and an iterative radius relaxation.\n    Returns:\n        centers: np.array (26,2)\n        radii: np.array (26,)\n        sum_of_radii: float\n    \"\"\"\n    n = 26\n\n    # Row structure (sensible hex-like layering that sums to 26)\n    rows = [5, 6, 6, 6, 3]  # total 26\n    num_rows = len(rows)\n    max_k = max(rows)\n\n    # small margin from walls\n    edge = 0.01\n\n    # choose a candidate horizontal spacing s so widest row fits comfortably\n    # include a safety 0.5*s to accommodate offset rows\n    s_h = (1.0 - 2 * edge) / (max_k - 1 + 0.5)\n\n    # vertical hex spacing\n    s_v = s_h * np.sqrt(3) / 2.0\n\n    # ensure vertical fit: adjust s_h if vertical span is too big\n    if (num_rows - 1) * s_v > (1.0 - 2 * edge):\n        s_v = (1.0 - 2 * edge) / (num_rows - 1)\n        s_h = s_v * 2.0 / np.sqrt(3)\n\n    # build centers: y positions centered, x positions centered for each row\n    span_y = (num_rows - 1) * s_v\n    y0 = 0.5 - span_y / 2.0\n    centers = []\n    for i, k in enumerate(rows):\n        y = y0 + i * s_v\n        # center the k points horizontally with spacing s_h\n        x0 = 0.5 - ((k - 1) * s_h) / 2.0\n        for j in range(k):\n            x = x0 + j * s_h\n            # clip to remain inside [edge,1-edge]\n            x = np.clip(x, edge, 1 - edge)\n            y = np.clip(y, edge, 1 - edge)\n            centers.append((x, y))\n\n    centers = np.array(centers)\n    assert centers.shape[0] == n\n\n    # Compute radii via iterative relaxation:\n    radii = compute_max_radii(centers)\n\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers):\n    \"\"\"\n    Compute maximum non-overlapping radii for given centers inside unit square.\n    Uses iterative proportional reduction for violating pairs and enforces wall limits.\n    \"\"\"\n    n = centers.shape[0]\n    eps = 1e-12\n\n    # initial radii limited by walls\n    wall_dists = np.minimum.reduce([centers[:, 0], centers[:, 1], 1 - centers[:, 0], 1 - centers[:, 1]])\n    # start with wall distances (largest possible individually)\n    radii = wall_dists.copy()\n\n    # Precompute pairwise distances\n    diffs = centers[:, None, :] - centers[None, :, :]\n    dists = np.sqrt(np.sum(diffs ** 2, axis=2)) + np.eye(n)  # avoid zeros on diagonal\n\n    # Iteratively fix overlaps: ensure r_i + r_j <= d_ij\n    # We'll loop and reduce both radii proportionally when violation occurs.\n    max_iters = 300\n    for _ in range(max_iters):\n        changed = False\n\n        # enforce pairwise constraints\n        for i in range(n):\n            # vectorize j loop for speed\n            # compute j>i to avoid duplicate work\n            for j in range(i + 1, n):\n                d = dists[i, j]\n                if d <= eps:\n                    # centers coincide (unlikely in our constructor) -> collapse radii\n                    if radii[i] > 0:\n                        radii[i] = 0.0\n                        changed = True\n                    if radii[j] > 0:\n                        radii[j] = 0.0\n                        changed = True\n                    continue\n                s = radii[i] + radii[j]\n                if s > d - 1e-9:\n                    # proportional scale to remove overlap (leave tiny slack)\n                    factor = (d - 1e-9) / s\n                    # To avoid zeroing tiny distances, if factor<0 set to very small positive\n                    factor = max(factor, 1e-9)\n                    # Scale both but prefer to preserve larger relative sizes (proportional)\n                    radii[i] *= factor\n                    radii[j] *= factor\n                    changed = True\n\n        # enforce wall constraints\n        for i in range(n):\n            if radii[i] > wall_dists[i]:\n                radii[i] = wall_dists[i]\n                changed = True\n\n        if not changed:\n            break\n\n    # Final pass: clamp tiny negatives / numerical issues\n    radii = np.maximum(radii, 0.0)\n    return radii\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor='k')\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nKey features: Alternative approach to validity, Alternative approach to sum_radii\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 0.8223, Type: High-Performer)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (explicit hex-like constructor)\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square\n    that attempts to maximize the sum of their radii using a\n    hexagonal-like lattice with row counts tuned to 26.\n    Returns:\n        Tuple of (centers, radii, sum_of_radii)\n    \"\"\"\n    n = 26\n    # Hex-like design parameters: choose spacing s as large as possible for 6 columns\n    s = 1.0 / 6.0  # horizontal spacing: max for 6 columns\n    h = s * np.sqrt(3) / 2.0  # vertical spacing for hex lattice\n\n    # Row structure summing to 26\n    rows = [6, 5, 6, 5, 4]  # total 26\n    R = len(rows)\n\n    # vertical placement centered in [0,1]\n    y0 = 0.5 - ( (R - 1) * h ) / 2.0\n\n    centers = []\n    for i, k in enumerate(rows):\n        y = y0 + i * h\n        # center each row horizontally\n        left = 0.5 - ((k - 1) * s) / 2.0\n        xs = left + np.arange(k) * s\n        for x in xs:\n            centers.append([x, y])\n\n    centers = np.array(centers[:n])  # ensure length 26\n\n    # Compute conservative maximal radii:\n    # For each circle, radius <= distance to border, and <= 0.5 * min_pairwise_distance\n    radii = np.empty(n)\n    for i in range(n):\n        x, y = centers[i]\n        border_dist = min(x, y, 1 - x, 1 - y)\n        # compute min distance to any other center\n        if n == 1:\n            min_pair = 2.0  # irrelevant\n        else:\n            dists = np.sqrt(((centers - centers[i]) ** 2).sum(axis=1))\n            dists[i] = np.inf\n            min_pair = dists.min()\n        # take half the nearest neighbor distance (guarantees non-overlap) and border limit\n        radii[i] = min(border_dist, 0.5 * min_pair)\n\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, ec=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nUnique approach: Modification: Full rewrite, Excellent validity (1.000), Excellent sum_radii (2.167)\n\n### Inspiration 2 (Score: 0.5699, Type: Experimental)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (explicit hex-like constructor)\nProduces deterministic centers for 26 circles and computes maximal feasible radii\nvia a Gauss-Seidel style iterative solver for the pairwise non-overlap constraints.\n\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square.\n\n    Returns:\n        centers: np.array of shape (26, 2)\n        radii: np.array of shape (26,)\n        sum_radii: float\n    \"\"\"\n    n = 26\n\n    # Row pattern chosen to approximate a truncated hexagonal packing:\n    # counts per row sum to 26 (5 + 6 + 5 + 6 + 4 = 26)\n    row_counts = [5, 6, 5, 6, 4]\n    s = 0.12  # nominal horizontal spacing for the lattice; will be scaled to fit\n\n    coords = []\n    for r, count in enumerate(row_counts):\n        # vertical spacing in hex grid is sqrt(3)/2 * s\n        y = r * (np.sqrt(3) / 2.0) * s\n        # alternate rows are offset by s/2\n        x0 = 0.5 * s if (r % 2) else 0.0\n        for k in range(count):\n            coords.append([x0 + k * s, y])\n\n    centers = np.array(coords)\n    assert centers.shape[0] == n\n\n    # Normalize bounding box to fit nicely inside [pad, 1 - pad]^2\n    pad = 0.02\n    mins = centers.min(axis=0)\n    maxs = centers.max(axis=0)\n    span = maxs - mins\n    # handle degenerate span (shouldn't happen here)\n    span[span == 0] = 1.0\n    centers = (centers - mins) / span  # now in [0,1] box\n    centers = centers * (1 - 2 * pad) + pad\n\n    # Move packing slightly toward center to reduce boundary crowding (small translation)\n    # This preserves relative arrangement but can improve achievable radii.\n    shift = np.array([0.0, 0.0])  # kept zero for determinism; tweak if desired\n    centers = np.clip(centers + shift, pad, 1 - pad)\n\n    radii = compute_max_radii(centers)\n\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers, max_iter=5000, tol=1e-8):\n    \"\"\"\n    Compute (approximately) maximal radii for given fixed centers so that:\n      - each circle stays inside the unit square\n      - circles do not overlap: r_i + r_j <= dist(c_i, c_j)\n\n    Uses an iterative Gauss-Seidel style update:\n      r_i <- min(distance_to_border_i, min_j max(0, dist_ij - r_j))\n\n    This finds the largest feasible radii vector consistent with the constraints.\n    \"\"\"\n    n = centers.shape[0]\n    # initial upper bounds from border distances\n    border = np.minimum.reduce([centers[:, 0], centers[:, 1], 1 - centers[:, 0], 1 - centers[:, 1]])\n    radii = border.copy()\n\n    # Precompute pairwise distances\n    dif = centers[:, None, :] - centers[None, :, :]  # shape (n,n,2)\n    dists = np.sqrt(np.sum(dif * dif, axis=2))\n    # Avoid self-distances of zero causing issues\n    np.fill_diagonal(dists, np.inf)\n\n    for it in range(int(max_iter)):\n        max_change = 0.0\n        # Gauss-Seidel: update each radius using the most recent values of others\n        for i in range(n):\n            # start with border constraint\n            limit = border[i]\n            # neighbor constraints: r_i <= dist_ij - r_j for all j\n            # compute dist_ij - r_j and take the minimum (but not negative)\n            # Use vectorized operations for speed inside loop\n            limits_from_neighbors = dists[i, :] - radii\n            # ignore self (set to +inf)\n            limits_from_neighbors[i] = np.inf\n            # the effective limit is min(border, min_j limits_from_neighbors)\n            neighbor_min = limits_from_neighbors.min()\n            if neighbor_min < limit:\n                limit = max(0.0, neighbor_min)\n            change = abs(radii[i] - limit)\n            if change:\n                radii[i] = limit\n                if change > max_change:\n                    max_change = change\n        if max_change < tol:\n            break\n\n    # Final safeguard: clip radii nonnegative and to border\n    radii = np.clip(radii, 0.0, border)\n    return radii\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nUnique approach: Modification: Full rewrite, Excellent validity (1.000), Excellent sum_radii (1.502)\n\n### Inspiration 3 (Score: 0.3642, Type: Exploratory)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square\n    that attempts to maximize the sum of their radii.\n\n    Returns:\n        Tuple of (centers, radii, sum_of_radii)\n        centers: np.array of shape (26, 2) with (x, y) coordinates\n        radii: np.array of shape (26) with radius of each circle\n        sum_of_radii: Sum of all radii\n    \"\"\"\n    # Initialize arrays for 26 circles\n    n = 26\n    centers = np.zeros((n, 2))\n\n    # Place circles in a structured pattern\n    # This is a simple pattern - evolution will improve this\n\n    # First, place a large circle in the center\n    centers[0] = [0.5, 0.5]\n\n    # Place 8 circles around it in a ring\n    for i in range(8):\n        angle = 2 * np.pi * i / 8\n        centers[i + 1] = [0.5 + 0.3 * np.cos(angle), 0.5 + 0.3 * np.sin(angle)]\n\n    # Place 16 more circles in an outer ring\n    for i in range(16):\n        angle = 2 * np.pi * i / 16\n        centers[i + 9] = [0.5 + 0.7 * np.cos(angle), 0.5 + 0.7 * np.sin(angle)]\n\n    # Additional positioning adjustment to make sure all circles\n    # are inside the square and don't overlap\n    # Clip to ensure everything is inside the unit square\n    centers = np.clip(centers, 0.01, 0.99)\n\n    # Compute maximum valid radii for this configuration\n    radii = compute_max_radii(centers)\n\n    # Calculate the sum of radii\n    sum_radii = np.sum(radii)\n\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers):\n    \"\"\"\n    Compute the maximum possible radii for each circle position\n    such that they don't overlap and stay within the unit square.\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n\n    Returns:\n        np.array of shape (n) with radius of each circle\n    \"\"\"\n    n = centers.shape[0]\n    radii = np.ones(n)\n\n    # First, limit by distance to square borders\n    for i in range(n):\n        x, y = centers[i]\n        # Distance to borders\n        radii[i] = min(x, y, 1 - x, 1 - y)\n\n    # Then, limit by distance to other circles\n    # Each pair of circles with centers at distance d can have\n    # sum of radii at most d to avoid overlap\n    for i in range(n):\n        for j in range(i + 1, n):\n            dist = np.sqrt(np.sum((centers[i] - centers[j]) ** 2))\n\n            # If current radii would cause overlap\n            if radii[i] + radii[j] > dist:\n                # Scale both radii proportionally\n                scale = dist / (radii[i] + radii[j])\n                radii[i] *= scale\n                radii[j] *= scale\n\n    return radii\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.5)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\")\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii}\")\n    # AlphaEvolve improved this to 2.635\n\n    # Uncomment to visualize:\n    visualize(centers, radii)\n\n```\nUnique approach: Excellent validity (1.000), Excellent sum_radii (0.960), NumPy-based implementation\n\n# Current Program\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (hex-like layered layout)\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct an explicit arrangement of 26 circles in a unit square\n    using a layered hexagonal-like lattice (row counts: 6,5,6,5,4).\n    Returns (centers, radii, sum_of_radii).\n    \"\"\"\n    counts = [6, 5, 6, 5, 4]  # total = 26\n    n = sum(counts)\n    assert n == 26\n\n    margin = 0.02  # keep centers away from exact border to allow positive radii\n    max_count = max(counts)\n    # horizontal spacing based on widest row\n    if max_count > 1:\n        sx = (1.0 - 2 * margin) / (max_count - 1)\n    else:\n        sx = 0.5\n    rows = len(counts)\n    # vertical positions evenly spaced between margins\n    y_positions = np.linspace(margin, 1.0 - margin, rows)\n\n    centers = np.zeros((n, 2))\n    idx = 0\n    for r, cnt in enumerate(counts):\n        # center shorter rows horizontally\n        start_x = margin + (max_count - cnt) * sx / 2.0\n        xs = start_x + np.arange(cnt) * sx\n        ys = np.full(cnt, y_positions[r])\n        for x, y in zip(xs, ys):\n            centers[idx] = [x, y]\n            idx += 1\n\n    # tiny random jitter to break perfect symmetry (helps radius relaxation)\n    rng = np.random.RandomState(12345)\n    centers += rng.uniform(-1e-4, 1e-4, centers.shape)\n    centers = np.clip(centers, margin, 1.0 - margin)\n\n    radii = compute_max_radii(centers)\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers, max_iter=500, tol=1e-8):\n    \"\"\"\n    Iteratively relax radii: initialize by wall distances, then enforce\n    pairwise non-overlap by proportional shrinking until convergence.\n    \"\"\"\n    n = centers.shape[0]\n    # initial radii limited by walls\n    radii = np.minimum.reduce([centers[:, 0], centers[:, 1], 1 - centers[:, 0], 1 - centers[:, 1]])\n    radii = np.maximum(radii, 0.0)\n\n    # iterative pairwise relaxation\n    for _ in range(max_iter):\n        prev = radii.copy()\n        # Enforce pairwise constraints\n        for i in range(n):\n            ci = centers[i]\n            ri = radii[i]\n            for j in range(i + 1, n):\n                cj = centers[j]\n                rj = radii[j]\n                d = np.hypot(ci[0] - cj[0], ci[1] - cj[1])\n                if d <= 0:\n                    # coincident centers: collapse both\n                    radii[i] = radii[j] = 0.0\n                    continue\n                # if overlap, scale both down proportionally\n                if ri + rj > d:\n                    scale = d / (ri + rj)\n                    # avoid numerical issues\n                    if scale < 0:\n                        scale = 0.0\n                    radii[i] *= scale\n                    radii[j] *= scale\n                    ri, rj = radii[i], radii[j]\n        # After pairwise step, re-apply wall limits (in case center moved near border numerically)\n        wall_limits = np.minimum.reduce([centers[:, 0], centers[:, 1], 1 - centers[:, 0], 1 - centers[:, 1]])\n        radii = np.minimum(radii, wall_limits)\n        # check convergence\n        if np.max(np.abs(radii - prev)) < tol:\n            break\n    # Ensure non-negative\n    radii = np.maximum(radii, 0.0)\n    return radii\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\n\n# Task\nRewrite the program to improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: complexity, diversity\nDifferent solutions with similar fitness but different features are valuable.\nProvide the complete new program code.\n\nIMPORTANT: Make sure your rewritten program maintains the same inputs and outputs\nas the original program, but with improved internal implementation.\n\n```python\n# Your rewritten program here\n```", "responses": ["```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (explicit hex-like constructor)\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    \"\"\"\n    Deterministic hex-like construction for 26 circles.\n    Build rows [6,5,6,5,4] with alternating offsets, compute conservative\n    radii then grow them with a Gauss-Seidel style solver to (approximately)\n    maximize the sum of radii while keeping circles inside the unit square\n    and non-overlapping.\n    Returns: centers (26,2), radii (26,), sum_radii (float)\n    \"\"\"\n    n = 26\n    rows = [6, 5, 6, 5, 4]  # sums to 26\n    max_k = max(rows)\n\n    # lattice spacing chosen to allow 6 columns across comfortably\n    s = 1.0 / 6.0\n    h = s * np.sqrt(3) / 2.0\n\n    R = len(rows)\n    # vertical placement centered\n    y0 = 0.5 - ((R - 1) * h) / 2.0\n\n    centers = []\n    for i, k in enumerate(rows):\n        y = y0 + i * h\n        # alternate rows offset by s/2 to emulate hex packing\n        offset = (s / 2.0) if (i % 2 == 1) else 0.0\n        left = 0.5 - ((k - 1) * s) / 2.0 + offset\n        xs = left + np.arange(k) * s\n        for x in xs:\n            centers.append([x, y])\n    centers = np.array(centers[:n])\n\n    # conservative initial radii: limited by border and half nearest-neighbor distance\n    border = np.minimum.reduce([centers[:, 0], centers[:, 1],\n                                1 - centers[:, 0], 1 - centers[:, 1]])\n    # pairwise distances\n    dif = centers[:, None, :] - centers[None, :, :]\n    dists = np.sqrt(np.sum(dif * dif, axis=2))\n    np.fill_diagonal(dists, np.inf)\n    min_pair = dists.min(axis=1)\n    radii = np.minimum(border, 0.5 * min_pair)\n    radii = np.clip(radii, 0.0, border)\n\n    # Gauss-Seidel style growth to approach maximal feasible radii:\n    # r_i <- min(border_i, min_j max(0, d_ij - r_j))\n    max_iter = 2000\n    tol = 1e-9\n    for _ in range(max_iter):\n        max_change = 0.0\n        for i in range(n):\n            # compute allowed radius from neighbors using current radii (most recent values)\n            limits = dists[i, :] - radii\n            # ignore self\n            limits[i] = np.inf\n            neighbor_min = limits.min()\n            new_r = neighbor_min if neighbor_min < border[i] else border[i]\n            if new_r < 0.0:\n                new_r = 0.0\n            # small safeguard to avoid oscillation: take average with current if growing a lot\n            # but keep direct assignment for convergence\n            change = new_r - radii[i]\n            if abs(change) > 0:\n                radii[i] = new_r\n                max_change = max(max_change, abs(change))\n        if max_change < tol:\n            break\n\n    # final clipping to ensure validity\n    radii = np.clip(radii, 0.0, border)\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n# EVOLVE-BLOCK-END\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```"]}}, "artifacts_json": "{\"stdout\": \"Running in subprocess, Python version: 3.12.1 (v3.12.1:2305ca5144, Dec  7 2023, 17:23:38) [Clang 13.0.0 (clang-1300.0.29.30)]\\nProgram path: /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmphcbfvmmp.py\\nCalling run_packing()...\\nrun_packing() returned successfully: sum_radii = 1.8987174742355442\\nResults saved to /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmpvba3qfnd.py.results\\n\", \"stderr\": \"\"}", "artifact_dir": null}