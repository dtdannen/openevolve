{"id": "c4126805-50fc-44c4-a5a1-4e42e0f9aa12", "code": "# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for n=26 circles in the unit square.\n\nImprovements over prior versions:\n- Small deterministic grid-search over horizontal/vertical spacing scales to\n  pick the best-shaped hex-like block for 26 circles.\n- A more aggressive growth-then-robust-shrink radii solver that fully expands\n  radii toward slack then repeatedly resolves overlaps (stronger convergence\n  to a larger feasible sum of radii).\nReturns centers (26,2), radii (26,), sum_radii.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    rows = [6, 5, 6, 5, 4]  # total 26\n    R = len(rows)\n    max_k = max(rows)\n    margin = 1e-6\n    # base horizontal spacing (widest row)\n    if max_k > 1:\n        s0 = (1.0 - 2 * margin) / (max_k - 1)\n    else:\n        s0 = 0.5\n\n    # deterministic candidate scales (tries a few compressions to balance edge effects)\n    s_scales = np.array([1.00, 0.98, 0.96, 0.94, 0.92])\n    y_scales = np.array([1.00, 0.98, 0.96])\n\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    for ss in s_scales:\n        for ys in y_scales:\n            s = s0 * ss\n            h = s * np.sqrt(3.0) / 2.0 * ys\n            # if vertical span too large, rescale to fit exactly\n            span_v = (R - 1) * h\n            if span_v > 1.0 - 2 * margin:\n                h = (1.0 - 2 * margin) / (R - 1)\n                s = h * 2.0 / np.sqrt(3.0)\n            # build centered rows\n            y0 = 0.5 - ((R - 1) * h) / 2.0\n            centers = []\n            for i, k in enumerate(rows):\n                y = y0 + i * h\n                left = 0.5 - ((k - 1) * s) / 2.0\n                xs = left + np.arange(k) * s\n                for x in xs:\n                    centers.append([float(x), float(y)])\n            centers = np.array(centers[:n], dtype=float)\n            # minor safeguard: clip into unit square (numerical)\n            centers = np.clip(centers, margin, 1.0 - margin)\n\n            radii = compute_max_radii_aggressive(centers)\n            sumr = float(np.sum(radii))\n            if sumr > best_sum:\n                best_sum = sumr\n                best_centers = centers\n                best_radii = radii\n\n    return best_centers, best_radii, float(np.sum(best_radii))\n\n\ndef compute_max_radii_aggressive(centers, max_outer=300, max_inner=60, tol=1e-10):\n    \"\"\"\n    Aggressive grow-then-shrink solver:\n    - start with conservative radii (min border, 0.5*nearest)\n    - repeatedly:\n        * compute allowed = min(border, min_j (D_ij - r_j)) and set radii = allowed\n        * run an inner loop that repeatedly resolves overlaps by proportional scaling\n          (using the most restrictive scale per circle) until no overlaps remain\n    This often reaches a larger feasible sum than tiny incremental growth steps.\n    \"\"\"\n    n = centers.shape[0]\n    if n == 0:\n        return np.array([])\n\n    # border limits\n    border = np.minimum.reduce([centers[:, 0], centers[:, 1],\n                                1.0 - centers[:, 0], 1.0 - centers[:, 1]])\n    # pairwise distances\n    dif = centers[:, None, :] - centers[None, :, :]\n    D = np.hypot(dif[..., 0], dif[..., 1])\n    np.fill_diagonal(D, np.inf)\n\n    # initial safe radii\n    min_pair = np.min(D, axis=1)\n    radii = np.minimum(border, 0.5 * min_pair)\n    radii = np.maximum(radii, 0.0)\n\n    for outer in range(max_outer):\n        old = radii.copy()\n\n        # compute allowed growth (full assignment)\n        slack = D - radii[None, :]   # allowed remaining toward each other\n        allowed = np.minimum(border, np.min(slack, axis=1))\n        allowed = np.maximum(allowed, 0.0)\n        radii = allowed.copy()\n\n        # inner robust shrink: resolve overlaps thoroughly\n        for inner in range(max_inner):\n            ri = radii[:, None]\n            rj = radii[None, :]\n            sumr = ri + rj\n            viol = sumr > D + 1e-14\n            if not np.any(viol):\n                break\n            # per-pair scale factors (<=1)\n            factors = np.ones_like(sumr)\n            factors[viol] = D[viol] / sumr[viol]\n            # per-circle minimal scale\n            min_scale = factors.min(axis=1)\n            # clamp to avoid underflow\n            min_scale = np.maximum(min_scale, 1e-12)\n            radii *= min_scale\n            # ensure wall limits\n            radii = np.minimum(radii, border)\n\n        # small relaxation to avoid infinite oscillation:\n        radii = np.minimum(radii, border)\n\n        if np.max(np.abs(radii - old)) < tol:\n            break\n\n    radii = np.maximum(radii, 0.0)\n    return radii\n\n# EVOLVE-BLOCK-END\n\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")", "language": "python", "parent_id": "2d5b6042-e634-4773-8b8d-f5d0ce13e999", "generation": 4, "timestamp": 1759961056.966784, "iteration_found": 29, "metrics": {"validity": 1.0, "sum_radii": 2.1999995999999995, "target_ratio": 0.834914459203036, "combined_score": 0.834914459203036, "eval_time": 0.054757118225097656}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Full rewrite", "parent_metrics": {"validity": 1.0, "sum_radii": 2.1886080099436533, "target_ratio": 0.8305912751209311, "combined_score": 0.8305912751209311, "eval_time": 0.055953264236450195}, "island": 0}, "prompts": {"full_rewrite_user": {"system": "You are an expert mathematician specializing in circle packing problems and computational geometry. Your task is to improve a constructor function that directly produces a specific arrangement of 26 circles in a unit square, maximizing the sum of their radii. The AlphaEvolve paper achieved a sum of 2.635 for n=26.\n\nKey geometric insights:\n- Circle packings often follow hexagonal patterns in the densest regions\n- Maximum density for infinite circle packing is pi/(2*sqrt(3)) \u2248 0.9069\n- Edge effects make square container packing harder than infinite packing\n- Circles can be placed in layers or shells when confined to a square\n- Similar radius circles often form regular patterns, while varied radii allow better space utilization\n- Perfect symmetry may not yield the optimal packing due to edge effects\n\nFocus on designing an explicit constructor that places each circle in a specific position, rather than an iterative search algorithm.\n\n\n## Evolution Insights (Global Learnings)\n\n### Common Pitfalls:\n\u26a0\ufe0f (seen 3x) A recent change reduced the effective aggregation/search radius (sum_radii) by ~12% \u2014 from 0.9598 to 0.8446 \u2014 which directly lowered matching/coverage and caused target_ratio and combined_score to fall. When changing radii, thresholds, or any scale/constant computation (including refactors that alter units), avoid making unilateral reductions without end-to-end validation: add unit and performance tests, parameter sweeps, and guardrails to detect and prevent silent regressions.\n\u26a0\ufe0f (seen 3x) A code change that effectively reduced coverage/capacity \u2014 evidenced by sum_radii falling from 0.9598 to 0.8446 (for example, tightening thresholds, pruning candidate sets, or shrinking search/anchor radii) \u2014 caused a ~12% drop in target_ratio and combined_score across iterations, meaning the system now reaches fewer valid targets. When making similar changes, avoid silently shrinking coverage or capacity: add regression tests that track target_ratio/combined_score and sum_radii, run A/B comparisons, and require explicit validation before changing thresholds, pruning logic, or dimensionality reductions.\n\u26a0\ufe0f (seen 3x) A recent change effectively tightened the algorithm's geometric/selection logic (sum_radii fell from 0.9598 to 0.8446), shrinking the effective search/coverage area and causing target_ratio and combined_score to drop ~12% (0.364 \u2192 0.321). When modifying distance, radius, or selection thresholds (or applying optimizations that can be equivalent), avoid unintentional shrinking of the search space: add regression tests that track sum_radii, target_ratio, and combined_score and require performance baselines to be met before merging.\n\u26a0\ufe0f (seen 1x) A change short-circuited or replaced the radii/target-ratio computation (or the object-initialization path) with a no-op/default-zero path so sum_radii and target_ratio collapsed to 0.0 and the overall validity check flipped from valid to invalid (1.000 \u2192 0.000).  \n\nWhen modifying initialization or computation code, avoid introducing default-zero returns or gating that skips construction; add unit/regression tests and assertions that core invariants (e.g., sum_radii > 0, validity == true) remain true and include CI checks to catch regressions early.", "user": "# Current Program Information\n- Fitness: 0.8306\n- Feature coordinates: No feature coordinates\n- Focus areas: - Fitness improved: 0.8223 \u2192 0.8306\n- Consider simplifying - code length exceeds 500 characters\n\n## Last Execution Output\n\n### stdout\n```\nRunning in subprocess, Python version: 3.12.1 (v3.12.1:2305ca5144, Dec  7 2023, 17:23:38) [Clang 13.0.0 (clang-1300.0.29.30)]\nProgram path: /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmpq20kjryy.py\nCalling run_packing()...\nrun_packing() returned successfully: sum_radii = 2.1886080099436533\nResults saved to /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmplikzt4ff.py.results\n\n```\n\n### stderr\n```\n\n```\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 3\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.1667, target_ratio: 0.8223, combined_score: 0.8223, eval_time: 0.0605\n- Outcome: Mixed results\n\n### Attempt 2\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.1886, target_ratio: 0.8306, combined_score: 0.8306, eval_time: 0.0540\n- Outcome: Mixed results\n\n### Attempt 1\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.1886, target_ratio: 0.8306, combined_score: 0.8306, eval_time: 0.0560\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.8306)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Improved deterministic constructor for packing n=26 circles in the unit square.\n\nDesign:\n- Use a compact hex-like layered layout tuned for 26 circles: rows [6,5,6,5,4].\n- Compute maximal feasible radii for fixed centers via a stable, vectorized\n  iterative relaxation that (a) resolves overlaps by proportional shrinking\n  using the most restrictive pairwise scale per circle, (b) enforces wall limits,\n  and (c) attempts controlled growth toward available slack to increase the sum\n  of radii.\nReturns centers (26,2), radii (26,), sum_radii.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    # compact hex-like rows summing to 26\n    rows = [6, 5, 6, 5, 4]\n    R = len(rows)\n    max_k = max(rows)\n\n    # choose horizontal spacing so widest row nearly fills the unit width\n    margin = 1e-6\n    if max_k > 1:\n        s = (1.0 - 2 * margin) / (max_k - 1)\n    else:\n        s = 0.5\n    h = s * np.sqrt(3.0) / 2.0\n\n    # if vertical span would exceed unit square, scale down spacing\n    span_v = (R - 1) * h\n    if span_v > 1.0 - 2 * margin:\n        h = (1.0 - 2 * margin) / (R - 1)\n        s = h * 2.0 / np.sqrt(3.0)\n\n    # build centered rows deterministically\n    y0 = 0.5 - ((R - 1) * h) / 2.0\n    centers = []\n    for i, k in enumerate(rows):\n        y = y0 + i * h\n        left = 0.5 - ((k - 1) * s) / 2.0\n        xs = left + np.arange(k) * s\n        for x in xs:\n            centers.append([float(x), float(y)])\n    centers = np.array(centers[:n], dtype=float)\n\n    radii = compute_max_radii(centers)\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\ndef compute_max_radii(centers, max_iters=800, tol=1e-10):\n    n = centers.shape[0]\n    # initial radii limited by walls\n    border = np.minimum.reduce([centers[:,0], centers[:,1],\n                                1.0 - centers[:,0], 1.0 - centers[:,1]])\n    radii = border.copy()\n\n    if n <= 1:\n        return radii\n\n    # precompute pairwise distances\n    diffs = centers[:, None, :] - centers[None, :, :]\n    D = np.hypot(diffs[...,0], diffs[...,1])\n    np.fill_diagonal(D, np.inf)\n\n    for it in range(max_iters):\n        old = radii.copy()\n\n        # resolve overlaps in a vectorized way:\n        ri = radii[:, None]\n        rj = radii[None, :]\n        sumr = ri + rj\n        viol = sumr > D\n        if np.any(viol):\n            # per-pair scale factors (<=1). For non-violations factor=1\n            factors = np.ones_like(sumr)\n            factors[viol] = D[viol] / sumr[viol]\n            # for each circle take the most restrictive (minimum) scale across its pairs\n            min_scale = factors.min(axis=1)\n            # avoid exact zeros\n            min_scale = np.maximum(min_scale, 1e-12)\n            radii *= min_scale\n\n        # enforce walls\n        radii = np.minimum(radii, border)\n\n        # attempt controlled growth toward available slack:\n        # allowed_i = min(border_i, min_j (D_ij - r_j))\n        slack = D - radii[None, :]  # allowed center-to-center remaining for each pair\n        allowed = np.minimum(border, slack.min(axis=1))\n        allowed = np.maximum(allowed, 0.0)\n        grow_mask = allowed > radii + 1e-14\n        radii[grow_mask] = (radii[grow_mask] + allowed[grow_mask]) / 2.0\n\n        if np.max(np.abs(radii - old)) < tol:\n            break\n\n    radii = np.maximum(radii, 0.0)\n    return radii\n\n# EVOLVE-BLOCK-END\n\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n# Optional visualization helper (not used in automated evaluation)\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(6,6))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect('equal')\n    for c, r in zip(centers, radii):\n        ax.add_patch(Circle(c, r, alpha=0.6, edgecolor='k'))\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.1886), Performs well on target_ratio (0.8306), Performs well on combined_score (0.8306), Performs well on eval_time (0.0560)\n\n### Program 2 (Score: 0.8306)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (hex-like constructor + radius relaxation)\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct an explicit arrangement of 26 circles in a unit square using\n    a hex-like layered layout (rows tuned to 26) and compute near-maximal\n    non-overlapping radii via iterative relaxation.\n    Returns:\n        centers: (26,2) np.array\n        radii: (26,) np.array\n        sum_of_radii: float\n    \"\"\"\n    n = 26\n    # Hex-like row counts summing to 26\n    rows = [6, 5, 6, 5, 4]\n    R = len(rows)\n\n    # pick horizontal spacing s to fit widest row comfortably\n    max_k = max(rows)\n    # choose s so widest row uses full width but allow small margin\n    margin = 1e-6\n    s = (1.0 - 2 * margin) / (max_k - 1) if max_k > 1 else 0.5\n    # hex vertical spacing\n    h = s * np.sqrt(3) / 2.0\n\n    # if total vertical span too big, shrink s to fit\n    span_v = (R - 1) * h\n    if span_v > 1.0 - 2 * margin:\n        h = (1.0 - 2 * margin) / (R - 1)\n        s = h * 2.0 / np.sqrt(3)\n\n    # build centered rows (no arbitrary random jitter \u2014 deterministic)\n    y0 = 0.5 - ((R - 1) * h) / 2.0\n    centers = []\n    for i, k in enumerate(rows):\n        y = y0 + i * h\n        left = 0.5 - ((k - 1) * s) / 2.0\n        xs = left + np.arange(k) * s\n        for x in xs:\n            centers.append([x, y])\n    centers = np.array(centers[:n])\n\n    # compute radii with an iterative relaxation that enforces walls and pairwise constraints\n    radii = compute_max_radii(centers)\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers, max_iters=800, tol=1e-9):\n    \"\"\"\n    Given fixed centers inside [0,1]^2, compute radii so circles are non-overlapping\n    and lie inside the square. The algorithm:\n      - start with wall-limited radii\n      - repeatedly shrink to resolve overlaps (proportional scaling)\n      - re-apply wall limits\n      - attempt controlled growth toward available slack\n    \"\"\"\n    n = centers.shape[0]\n    # initial radii limited by walls\n    border = np.minimum.reduce([centers[:, 0], centers[:, 1],\n                                1.0 - centers[:, 0], 1.0 - centers[:, 1]])\n    radii = border.copy()\n\n    if n <= 1:\n        return radii\n\n    # pairwise distance matrix (constant)\n    diffs = centers[:, None, :] - centers[None, :, :]\n    D = np.hypot(diffs[..., 0], diffs[..., 1])\n    np.fill_diagonal(D, np.inf)\n\n    for it in range(max_iters):\n        old = radii.copy()\n\n        # resolve overlaps: if r_i + r_j > d_ij, apply pairwise proportional scaling\n        ri = radii[:, None]\n        rj = radii[None, :]\n        sumr = ri + rj\n        viol = sumr > D\n        if np.any(viol):\n            # compute per-pair scaling factors (<=1)\n            factors = np.ones_like(sumr)\n            factors[viol] = D[viol] / sumr[viol]\n            # for each circle take the most restrictive scale across its pairs\n            min_scale = factors.min(axis=1)\n            # avoid numerical zeros: clamp to tiny positive\n            min_scale = np.maximum(min_scale, 1e-12)\n            radii *= min_scale\n\n        # enforce wall limits\n        radii = np.minimum(radii, border)\n\n        # attempt to grow toward available slack:\n        # allowed_i = min(border_i, min_j (D_ij - r_j))\n        slack = D - radii[None, :]  # allowed center-to-center distance remaining for each pair\n        # since D diagonal is inf, min excludes self\n        allowed = np.minimum(border, slack.min(axis=1))\n        allowed = np.maximum(allowed, 0.0)\n        # grow partially toward allowed to avoid oscillation\n        grow_mask = allowed > radii\n        radii[grow_mask] = (radii[grow_mask] + allowed[grow_mask]) / 2.0\n\n        if np.max(np.abs(radii - old)) < tol:\n            break\n\n    radii = np.maximum(radii, 0.0)\n    return radii\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, ec=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.1886), Performs well on target_ratio (0.8306), Performs well on combined_score (0.8306), Performs well on eval_time (0.0540)\n\n### Program 3 (Score: 0.8223)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (explicit hex-like constructor)\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square\n    that attempts to maximize the sum of their radii using a\n    hexagonal-like lattice with row counts tuned to 26.\n    Returns:\n        Tuple of (centers, radii, sum_of_radii)\n    \"\"\"\n    n = 26\n    # Hex-like design parameters: choose spacing s as large as possible for 6 columns\n    s = 1.0 / 6.0  # horizontal spacing: max for 6 columns\n    h = s * np.sqrt(3) / 2.0  # vertical spacing for hex lattice\n\n    # Row structure summing to 26\n    rows = [6, 5, 6, 5, 4]  # total 26\n    R = len(rows)\n\n    # vertical placement centered in [0,1]\n    y0 = 0.5 - ( (R - 1) * h ) / 2.0\n\n    centers = []\n    for i, k in enumerate(rows):\n        y = y0 + i * h\n        # center each row horizontally\n        left = 0.5 - ((k - 1) * s) / 2.0\n        xs = left + np.arange(k) * s\n        for x in xs:\n            centers.append([x, y])\n\n    centers = np.array(centers[:n])  # ensure length 26\n\n    # Compute conservative maximal radii:\n    # For each circle, radius <= distance to border, and <= 0.5 * min_pairwise_distance\n    radii = np.empty(n)\n    for i in range(n):\n        x, y = centers[i]\n        border_dist = min(x, y, 1 - x, 1 - y)\n        # compute min distance to any other center\n        if n == 1:\n            min_pair = 2.0  # irrelevant\n        else:\n            dists = np.sqrt(((centers - centers[i]) ** 2).sum(axis=1))\n            dists[i] = np.inf\n            min_pair = dists.min()\n        # take half the nearest neighbor distance (guarantees non-overlap) and border limit\n        radii[i] = min(border_dist, 0.5 * min_pair)\n\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, ec=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.1667), Performs well on target_ratio (0.8223), Performs well on combined_score (0.8223), Performs well on eval_time (0.0605)\n\n\n\n## Diverse Programs\n\n### Program D1 (Score: 0.8223)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for n=26 circles in the unit square.\nPlaces centers in a compact hex-like arrangement tuned for 26 circles\nand assigns each circle the conservative maximal radius:\nmin(distance to boundary, 0.5 * nearest-center-distance).\nThis simple explicit layout is effective and fast.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    # hex-like row pattern that sums to 26\n    rows = [6, 5, 6, 5, 4]\n    # choose horizontal spacing so 6 columns fit across unit square\n    s = 1.0 / 6.0\n    h = s * np.sqrt(3) / 2.0\n\n    # vertical start so the block is centered in the unit square\n    R = len(rows)\n    y0 = 0.5 - ((R - 1) * h) / 2.0\n\n    centers = []\n    for i, k in enumerate(rows):\n        y = y0 + i * h\n        left = 0.5 - ((k - 1) * s) / 2.0\n        xs = left + np.arange(k) * s\n        for x in xs:\n            centers.append((float(x), float(y)))\n    centers = np.array(centers[:n], dtype=float)\n\n    # compute pairwise distances and border distances\n    if centers.shape[0] == 0:\n        return centers, np.array([]), 0.0\n    dif = centers[:, None, :] - centers[None, :, :]\n    dmat = np.sqrt(np.sum(dif * dif, axis=2))\n    np.fill_diagonal(dmat, np.inf)\n    min_pair = dmat.min(axis=1)\n    border = np.minimum.reduce([centers[:,0], centers[:,1], 1-centers[:,0], 1-centers[:,1]])\n    # radius is limited by border and half nearest neighbor distance\n    radii = np.minimum(border, 0.5 * min_pair)\n    # numerical safety\n    radii = np.clip(radii, 0.0, None)\n    return centers, radii, float(radii.sum())\n\n# EVOLVE-BLOCK-END\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(6, 6))\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    for i, (c, r) in enumerate(zip(centers, radii)):\n        ax.add_patch(Circle(c, r, alpha=0.6, ec=\"k\"))\n        ax.text(c[0], c[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={radii.sum():.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # visualize(centers, radii)\n```\nKey features: Alternative approach to validity, Alternative approach to sum_radii\n\n### Program D2 (Score: 0.8223)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for packing 26 circles in a unit square.\n\nDesign: an explicit hexagonal-like layout (rows tuned to 26) with a\ntwo-phase radius determination:\n  1) conservative initial radii: min(distance to border, 0.5*nearest-neighbor)\n  2) alternating greedy local expansion and proportional overlap removal\n     to increase the sum of radii while maintaining feasibility.\nThis reproduces and slightly improves upon high-performing explicit constructors.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    # Hex-like layout tuned for 26 circles\n    rows = [6, 5, 6, 5, 4]  # sums to 26\n    s = 1.0 / 6.0  # horizontal spacing chosen to fill width for the largest row\n    h = s * np.sqrt(3.0) / 2.0  # vertical hex spacing\n\n    # center rows vertically in unit square\n    R = len(rows)\n    y0 = 0.5 - ((R - 1) * h) / 2.0\n    centers = []\n    for i, k in enumerate(rows):\n        y = y0 + i * h\n        left = 0.5 - ((k - 1) * s) / 2.0\n        xs = left + np.arange(k) * s\n        for x in xs:\n            centers.append([x, y])\n    centers = np.array(centers[:n], dtype=float)\n\n    # compute pairwise distances (with diagonal = inf)\n    dif = centers[:, None, :] - centers[None, :, :]\n    dists = np.hypot(dif[..., 0], dif[..., 1])\n    np.fill_diagonal(dists, np.inf)\n\n    # border limits\n    border = np.minimum.reduce([centers[:, 0], centers[:, 1],\n                                1.0 - centers[:, 0], 1.0 - centers[:, 1]])\n\n    # Phase A: conservative initial radii (guaranteed non-overlap)\n    # r_i <= 0.5 * min_j d_ij  and r_i <= border_i\n    min_pair = np.min(dists, axis=1)\n    radii = np.minimum(border, 0.5 * min_pair)\n\n    # Phase B: try to greedily expand radii, then remove overlaps proportionally.\n    # Alternate a few cycles to increase total sum while preserving feasibility.\n    eps = 1e-12\n    for cycle in range(80):\n        grew = False\n        # Greedy local expansion: for each i, allowed = min(border_i, min_j (d_ij - r_j))\n        # Use vectorized computation for slack\n        slack = dists - radii[None, :]  # shape (n,n)\n        # ignore self (dists diag = inf so slack diag = inf)\n        allowed = np.minimum(border, np.min(slack, axis=1))\n        # clamp allowed >= 0\n        allowed = np.maximum(allowed, 0.0)\n        # grow where possible a bit conservatively to avoid immediate oscillations:\n        grow_mask = allowed > radii + 1e-12\n        if np.any(grow_mask):\n            # move radii toward allowed (full assignment is fine since we'll fix overlaps)\n            radii[grow_mask] = allowed[grow_mask]\n            grew = True\n\n        # Proportional overlap removal: for any pair with r_i + r_j > d_ij, scale them down\n        sum_r = radii[:, None] + radii[None, :]\n        viol = sum_r > dists + 1e-12\n        if np.any(viol):\n            # process upper triangle pairs\n            ui, uj = np.triu_indices(n, k=1)\n            for i, j in zip(ui, uj):\n                if sum_r[i, j] > dists[i, j] + 1e-12:\n                    d = dists[i, j]\n                    s = radii[i] + radii[j]\n                    if d <= eps:\n                        # coincident centers (shouldn't happen) -> collapse smaller\n                        if radii[i] >= radii[j]:\n                            radii[j] = 0.0\n                        else:\n                            radii[i] = 0.0\n                        continue\n                    factor = (d - 1e-12) / s\n                    factor = max(factor, 0.0)\n                    # scale both proportionally\n                    new_i = radii[i] * factor\n                    new_j = radii[j] * factor\n                    # apply if meaningful change\n                    if new_i < radii[i] - 1e-14 or new_j < radii[j] - 1e-14:\n                        radii[i], radii[j] = new_i, new_j\n\n        # re-apply border clamp (safety)\n        radii = np.minimum(radii, border)\n\n        if not grew:\n            # no growth possible -> converged\n            break\n\n    radii = np.clip(radii, 0.0, None)\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n# EVOLVE-BLOCK-END\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nKey features: Alternative approach to validity, Alternative approach to sum_radii\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 0.8223, Type: High-Performer)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for packing 26 circles in a unit square.\n\nDesign: an explicit hexagonal-like layout (rows tuned to 26) with a\ntwo-phase radius determination:\n  1) conservative initial radii: min(distance to border, 0.5*nearest-neighbor)\n  2) alternating greedy local expansion and proportional overlap removal\n     to increase the sum of radii while maintaining feasibility.\nThis reproduces and slightly improves upon high-performing explicit constructors.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    # Hex-like layout tuned for 26 circles\n    rows = [6, 5, 6, 5, 4]  # sums to 26\n    s = 1.0 / 6.0  # horizontal spacing chosen to fill width for the largest row\n    h = s * np.sqrt(3.0) / 2.0  # vertical hex spacing\n\n    # center rows vertically in unit square\n    R = len(rows)\n    y0 = 0.5 - ((R - 1) * h) / 2.0\n    centers = []\n    for i, k in enumerate(rows):\n        y = y0 + i * h\n        left = 0.5 - ((k - 1) * s) / 2.0\n        xs = left + np.arange(k) * s\n        for x in xs:\n            centers.append([x, y])\n    centers = np.array(centers[:n], dtype=float)\n\n    # compute pairwise distances (with diagonal = inf)\n    dif = centers[:, None, :] - centers[None, :, :]\n    dists = np.hypot(dif[..., 0], dif[..., 1])\n    np.fill_diagonal(dists, np.inf)\n\n    # border limits\n    border = np.minimum.reduce([centers[:, 0], centers[:, 1],\n                                1.0 - centers[:, 0], 1.0 - centers[:, 1]])\n\n    # Phase A: conservative initial radii (guaranteed non-overlap)\n    # r_i <= 0.5 * min_j d_ij  and r_i <= border_i\n    min_pair = np.min(dists, axis=1)\n    radii = np.minimum(border, 0.5 * min_pair)\n\n    # Phase B: try to greedily expand radii, then remove overlaps proportionally.\n    # Alternate a few cycles to increase total sum while preserving feasibility.\n    eps = 1e-12\n    for cycle in range(80):\n        grew = False\n        # Greedy local expansion: for each i, allowed = min(border_i, min_j (d_ij - r_j))\n        # Use vectorized computation for slack\n        slack = dists - radii[None, :]  # shape (n,n)\n        # ignore self (dists diag = inf so slack diag = inf)\n        allowed = np.minimum(border, np.min(slack, axis=1))\n        # clamp allowed >= 0\n        allowed = np.maximum(allowed, 0.0)\n        # grow where possible a bit conservatively to avoid immediate oscillations:\n        grow_mask = allowed > radii + 1e-12\n        if np.any(grow_mask):\n            # move radii toward allowed (full assignment is fine since we'll fix overlaps)\n            radii[grow_mask] = allowed[grow_mask]\n            grew = True\n\n        # Proportional overlap removal: for any pair with r_i + r_j > d_ij, scale them down\n        sum_r = radii[:, None] + radii[None, :]\n        viol = sum_r > dists + 1e-12\n        if np.any(viol):\n            # process upper triangle pairs\n            ui, uj = np.triu_indices(n, k=1)\n            for i, j in zip(ui, uj):\n                if sum_r[i, j] > dists[i, j] + 1e-12:\n                    d = dists[i, j]\n                    s = radii[i] + radii[j]\n                    if d <= eps:\n                        # coincident centers (shouldn't happen) -> collapse smaller\n                        if radii[i] >= radii[j]:\n                            radii[j] = 0.0\n                        else:\n                            radii[i] = 0.0\n                        continue\n                    factor = (d - 1e-12) / s\n                    factor = max(factor, 0.0)\n                    # scale both proportionally\n                    new_i = radii[i] * factor\n                    new_j = radii[j] * factor\n                    # apply if meaningful change\n                    if new_i < radii[i] - 1e-14 or new_j < radii[j] - 1e-14:\n                        radii[i], radii[j] = new_i, new_j\n\n        # re-apply border clamp (safety)\n        radii = np.minimum(radii, border)\n\n        if not grew:\n            # no growth possible -> converged\n            break\n\n    radii = np.clip(radii, 0.0, None)\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n# EVOLVE-BLOCK-END\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nUnique approach: Modification: Full rewrite, Excellent validity (1.000), Excellent sum_radii (2.167)\n\n### Inspiration 2 (Score: 0.8306, Type: High-Performer)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (hex-like constructor + radius relaxation)\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct an explicit arrangement of 26 circles in a unit square using\n    a hex-like layered layout (rows tuned to 26) and compute near-maximal\n    non-overlapping radii via iterative relaxation.\n    Returns:\n        centers: (26,2) np.array\n        radii: (26,) np.array\n        sum_of_radii: float\n    \"\"\"\n    n = 26\n    # Hex-like row counts summing to 26\n    rows = [6, 5, 6, 5, 4]\n    R = len(rows)\n\n    # pick horizontal spacing s to fit widest row comfortably\n    max_k = max(rows)\n    # choose s so widest row uses full width but allow small margin\n    margin = 1e-6\n    s = (1.0 - 2 * margin) / (max_k - 1) if max_k > 1 else 0.5\n    # hex vertical spacing\n    h = s * np.sqrt(3) / 2.0\n\n    # if total vertical span too big, shrink s to fit\n    span_v = (R - 1) * h\n    if span_v > 1.0 - 2 * margin:\n        h = (1.0 - 2 * margin) / (R - 1)\n        s = h * 2.0 / np.sqrt(3)\n\n    # build centered rows (no arbitrary random jitter \u2014 deterministic)\n    y0 = 0.5 - ((R - 1) * h) / 2.0\n    centers = []\n    for i, k in enumerate(rows):\n        y = y0 + i * h\n        left = 0.5 - ((k - 1) * s) / 2.0\n        xs = left + np.arange(k) * s\n        for x in xs:\n            centers.append([x, y])\n    centers = np.array(centers[:n])\n\n    # compute radii with an iterative relaxation that enforces walls and pairwise constraints\n    radii = compute_max_radii(centers)\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers, max_iters=800, tol=1e-9):\n    \"\"\"\n    Given fixed centers inside [0,1]^2, compute radii so circles are non-overlapping\n    and lie inside the square. The algorithm:\n      - start with wall-limited radii\n      - repeatedly shrink to resolve overlaps (proportional scaling)\n      - re-apply wall limits\n      - attempt controlled growth toward available slack\n    \"\"\"\n    n = centers.shape[0]\n    # initial radii limited by walls\n    border = np.minimum.reduce([centers[:, 0], centers[:, 1],\n                                1.0 - centers[:, 0], 1.0 - centers[:, 1]])\n    radii = border.copy()\n\n    if n <= 1:\n        return radii\n\n    # pairwise distance matrix (constant)\n    diffs = centers[:, None, :] - centers[None, :, :]\n    D = np.hypot(diffs[..., 0], diffs[..., 1])\n    np.fill_diagonal(D, np.inf)\n\n    for it in range(max_iters):\n        old = radii.copy()\n\n        # resolve overlaps: if r_i + r_j > d_ij, apply pairwise proportional scaling\n        ri = radii[:, None]\n        rj = radii[None, :]\n        sumr = ri + rj\n        viol = sumr > D\n        if np.any(viol):\n            # compute per-pair scaling factors (<=1)\n            factors = np.ones_like(sumr)\n            factors[viol] = D[viol] / sumr[viol]\n            # for each circle take the most restrictive scale across its pairs\n            min_scale = factors.min(axis=1)\n            # avoid numerical zeros: clamp to tiny positive\n            min_scale = np.maximum(min_scale, 1e-12)\n            radii *= min_scale\n\n        # enforce wall limits\n        radii = np.minimum(radii, border)\n\n        # attempt to grow toward available slack:\n        # allowed_i = min(border_i, min_j (D_ij - r_j))\n        slack = D - radii[None, :]  # allowed center-to-center distance remaining for each pair\n        # since D diagonal is inf, min excludes self\n        allowed = np.minimum(border, slack.min(axis=1))\n        allowed = np.maximum(allowed, 0.0)\n        # grow partially toward allowed to avoid oscillation\n        grow_mask = allowed > radii\n        radii[grow_mask] = (radii[grow_mask] + allowed[grow_mask]) / 2.0\n\n        if np.max(np.abs(radii - old)) < tol:\n            break\n\n    radii = np.maximum(radii, 0.0)\n    return radii\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, ec=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nUnique approach: Modification: Full rewrite, Excellent validity (1.000), Excellent sum_radii (2.189)\n\n### Inspiration 3 (Score: 0.4560, Type: Experimental)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (hex-like layered layout)\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct an explicit arrangement of 26 circles in a unit square\n    using a layered hexagonal-like lattice (row counts: 6,5,6,5,4).\n    Returns (centers, radii, sum_of_radii).\n    \"\"\"\n    counts = [6, 5, 6, 5, 4]  # total = 26\n    n = sum(counts)\n    assert n == 26\n\n    margin = 0.02  # keep centers away from exact border to allow positive radii\n    max_count = max(counts)\n    # horizontal spacing based on widest row\n    if max_count > 1:\n        sx = (1.0 - 2 * margin) / (max_count - 1)\n    else:\n        sx = 0.5\n    rows = len(counts)\n    # vertical positions evenly spaced between margins\n    y_positions = np.linspace(margin, 1.0 - margin, rows)\n\n    centers = np.zeros((n, 2))\n    idx = 0\n    for r, cnt in enumerate(counts):\n        # center shorter rows horizontally\n        start_x = margin + (max_count - cnt) * sx / 2.0\n        xs = start_x + np.arange(cnt) * sx\n        ys = np.full(cnt, y_positions[r])\n        for x, y in zip(xs, ys):\n            centers[idx] = [x, y]\n            idx += 1\n\n    # tiny random jitter to break perfect symmetry (helps radius relaxation)\n    rng = np.random.RandomState(12345)\n    centers += rng.uniform(-1e-4, 1e-4, centers.shape)\n    centers = np.clip(centers, margin, 1.0 - margin)\n\n    radii = compute_max_radii(centers)\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers, max_iter=500, tol=1e-8):\n    \"\"\"\n    Iteratively relax radii: initialize by wall distances, then enforce\n    pairwise non-overlap by proportional shrinking until convergence.\n    \"\"\"\n    n = centers.shape[0]\n    # initial radii limited by walls\n    radii = np.minimum.reduce([centers[:, 0], centers[:, 1], 1 - centers[:, 0], 1 - centers[:, 1]])\n    radii = np.maximum(radii, 0.0)\n\n    # iterative pairwise relaxation\n    for _ in range(max_iter):\n        prev = radii.copy()\n        # Enforce pairwise constraints\n        for i in range(n):\n            ci = centers[i]\n            ri = radii[i]\n            for j in range(i + 1, n):\n                cj = centers[j]\n                rj = radii[j]\n                d = np.hypot(ci[0] - cj[0], ci[1] - cj[1])\n                if d <= 0:\n                    # coincident centers: collapse both\n                    radii[i] = radii[j] = 0.0\n                    continue\n                # if overlap, scale both down proportionally\n                if ri + rj > d:\n                    scale = d / (ri + rj)\n                    # avoid numerical issues\n                    if scale < 0:\n                        scale = 0.0\n                    radii[i] *= scale\n                    radii[j] *= scale\n                    ri, rj = radii[i], radii[j]\n        # After pairwise step, re-apply wall limits (in case center moved near border numerically)\n        wall_limits = np.minimum.reduce([centers[:, 0], centers[:, 1], 1 - centers[:, 0], 1 - centers[:, 1]])\n        radii = np.minimum(radii, wall_limits)\n        # check convergence\n        if np.max(np.abs(radii - prev)) < tol:\n            break\n    # Ensure non-negative\n    radii = np.maximum(radii, 0.0)\n    return radii\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nUnique approach: Modification: Full rewrite, Excellent validity (1.000), Excellent sum_radii (1.202)\n\n# Current Program\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Improved deterministic constructor for packing n=26 circles in the unit square.\n\nDesign:\n- Use a compact hex-like layered layout tuned for 26 circles: rows [6,5,6,5,4].\n- Compute maximal feasible radii for fixed centers via a stable, vectorized\n  iterative relaxation that (a) resolves overlaps by proportional shrinking\n  using the most restrictive pairwise scale per circle, (b) enforces wall limits,\n  and (c) attempts controlled growth toward available slack to increase the sum\n  of radii.\nReturns centers (26,2), radii (26,), sum_radii.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    # compact hex-like rows summing to 26\n    rows = [6, 5, 6, 5, 4]\n    R = len(rows)\n    max_k = max(rows)\n\n    # choose horizontal spacing so widest row nearly fills the unit width\n    margin = 1e-6\n    if max_k > 1:\n        s = (1.0 - 2 * margin) / (max_k - 1)\n    else:\n        s = 0.5\n    h = s * np.sqrt(3.0) / 2.0\n\n    # if vertical span would exceed unit square, scale down spacing\n    span_v = (R - 1) * h\n    if span_v > 1.0 - 2 * margin:\n        h = (1.0 - 2 * margin) / (R - 1)\n        s = h * 2.0 / np.sqrt(3.0)\n\n    # build centered rows deterministically\n    y0 = 0.5 - ((R - 1) * h) / 2.0\n    centers = []\n    for i, k in enumerate(rows):\n        y = y0 + i * h\n        left = 0.5 - ((k - 1) * s) / 2.0\n        xs = left + np.arange(k) * s\n        for x in xs:\n            centers.append([float(x), float(y)])\n    centers = np.array(centers[:n], dtype=float)\n\n    radii = compute_max_radii(centers)\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\ndef compute_max_radii(centers, max_iters=800, tol=1e-10):\n    n = centers.shape[0]\n    # initial radii limited by walls\n    border = np.minimum.reduce([centers[:,0], centers[:,1],\n                                1.0 - centers[:,0], 1.0 - centers[:,1]])\n    radii = border.copy()\n\n    if n <= 1:\n        return radii\n\n    # precompute pairwise distances\n    diffs = centers[:, None, :] - centers[None, :, :]\n    D = np.hypot(diffs[...,0], diffs[...,1])\n    np.fill_diagonal(D, np.inf)\n\n    for it in range(max_iters):\n        old = radii.copy()\n\n        # resolve overlaps in a vectorized way:\n        ri = radii[:, None]\n        rj = radii[None, :]\n        sumr = ri + rj\n        viol = sumr > D\n        if np.any(viol):\n            # per-pair scale factors (<=1). For non-violations factor=1\n            factors = np.ones_like(sumr)\n            factors[viol] = D[viol] / sumr[viol]\n            # for each circle take the most restrictive (minimum) scale across its pairs\n            min_scale = factors.min(axis=1)\n            # avoid exact zeros\n            min_scale = np.maximum(min_scale, 1e-12)\n            radii *= min_scale\n\n        # enforce walls\n        radii = np.minimum(radii, border)\n\n        # attempt controlled growth toward available slack:\n        # allowed_i = min(border_i, min_j (D_ij - r_j))\n        slack = D - radii[None, :]  # allowed center-to-center remaining for each pair\n        allowed = np.minimum(border, slack.min(axis=1))\n        allowed = np.maximum(allowed, 0.0)\n        grow_mask = allowed > radii + 1e-14\n        radii[grow_mask] = (radii[grow_mask] + allowed[grow_mask]) / 2.0\n\n        if np.max(np.abs(radii - old)) < tol:\n            break\n\n    radii = np.maximum(radii, 0.0)\n    return radii\n\n# EVOLVE-BLOCK-END\n\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n# Optional visualization helper (not used in automated evaluation)\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(6,6))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect('equal')\n    for c, r in zip(centers, radii):\n        ax.add_patch(Circle(c, r, alpha=0.6, edgecolor='k'))\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n```\n\n# Task\nRewrite the program to improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: complexity, diversity\nDifferent solutions with similar fitness but different features are valuable.\nProvide the complete new program code.\n\nIMPORTANT: Make sure your rewritten program maintains the same inputs and outputs\nas the original program, but with improved internal implementation.\n\n```python\n# Your rewritten program here\n```", "responses": ["```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for n=26 circles in the unit square.\n\nImprovements over prior versions:\n- Small deterministic grid-search over horizontal/vertical spacing scales to\n  pick the best-shaped hex-like block for 26 circles.\n- A more aggressive growth-then-robust-shrink radii solver that fully expands\n  radii toward slack then repeatedly resolves overlaps (stronger convergence\n  to a larger feasible sum of radii).\nReturns centers (26,2), radii (26,), sum_radii.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    rows = [6, 5, 6, 5, 4]  # total 26\n    R = len(rows)\n    max_k = max(rows)\n    margin = 1e-6\n    # base horizontal spacing (widest row)\n    if max_k > 1:\n        s0 = (1.0 - 2 * margin) / (max_k - 1)\n    else:\n        s0 = 0.5\n\n    # deterministic candidate scales (tries a few compressions to balance edge effects)\n    s_scales = np.array([1.00, 0.98, 0.96, 0.94, 0.92])\n    y_scales = np.array([1.00, 0.98, 0.96])\n\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    for ss in s_scales:\n        for ys in y_scales:\n            s = s0 * ss\n            h = s * np.sqrt(3.0) / 2.0 * ys\n            # if vertical span too large, rescale to fit exactly\n            span_v = (R - 1) * h\n            if span_v > 1.0 - 2 * margin:\n                h = (1.0 - 2 * margin) / (R - 1)\n                s = h * 2.0 / np.sqrt(3.0)\n            # build centered rows\n            y0 = 0.5 - ((R - 1) * h) / 2.0\n            centers = []\n            for i, k in enumerate(rows):\n                y = y0 + i * h\n                left = 0.5 - ((k - 1) * s) / 2.0\n                xs = left + np.arange(k) * s\n                for x in xs:\n                    centers.append([float(x), float(y)])\n            centers = np.array(centers[:n], dtype=float)\n            # minor safeguard: clip into unit square (numerical)\n            centers = np.clip(centers, margin, 1.0 - margin)\n\n            radii = compute_max_radii_aggressive(centers)\n            sumr = float(np.sum(radii))\n            if sumr > best_sum:\n                best_sum = sumr\n                best_centers = centers\n                best_radii = radii\n\n    return best_centers, best_radii, float(np.sum(best_radii))\n\n\ndef compute_max_radii_aggressive(centers, max_outer=300, max_inner=60, tol=1e-10):\n    \"\"\"\n    Aggressive grow-then-shrink solver:\n    - start with conservative radii (min border, 0.5*nearest)\n    - repeatedly:\n        * compute allowed = min(border, min_j (D_ij - r_j)) and set radii = allowed\n        * run an inner loop that repeatedly resolves overlaps by proportional scaling\n          (using the most restrictive scale per circle) until no overlaps remain\n    This often reaches a larger feasible sum than tiny incremental growth steps.\n    \"\"\"\n    n = centers.shape[0]\n    if n == 0:\n        return np.array([])\n\n    # border limits\n    border = np.minimum.reduce([centers[:, 0], centers[:, 1],\n                                1.0 - centers[:, 0], 1.0 - centers[:, 1]])\n    # pairwise distances\n    dif = centers[:, None, :] - centers[None, :, :]\n    D = np.hypot(dif[..., 0], dif[..., 1])\n    np.fill_diagonal(D, np.inf)\n\n    # initial safe radii\n    min_pair = np.min(D, axis=1)\n    radii = np.minimum(border, 0.5 * min_pair)\n    radii = np.maximum(radii, 0.0)\n\n    for outer in range(max_outer):\n        old = radii.copy()\n\n        # compute allowed growth (full assignment)\n        slack = D - radii[None, :]   # allowed remaining toward each other\n        allowed = np.minimum(border, np.min(slack, axis=1))\n        allowed = np.maximum(allowed, 0.0)\n        radii = allowed.copy()\n\n        # inner robust shrink: resolve overlaps thoroughly\n        for inner in range(max_inner):\n            ri = radii[:, None]\n            rj = radii[None, :]\n            sumr = ri + rj\n            viol = sumr > D + 1e-14\n            if not np.any(viol):\n                break\n            # per-pair scale factors (<=1)\n            factors = np.ones_like(sumr)\n            factors[viol] = D[viol] / sumr[viol]\n            # per-circle minimal scale\n            min_scale = factors.min(axis=1)\n            # clamp to avoid underflow\n            min_scale = np.maximum(min_scale, 1e-12)\n            radii *= min_scale\n            # ensure wall limits\n            radii = np.minimum(radii, border)\n\n        # small relaxation to avoid infinite oscillation:\n        radii = np.minimum(radii, border)\n\n        if np.max(np.abs(radii - old)) < tol:\n            break\n\n    radii = np.maximum(radii, 0.0)\n    return radii\n\n# EVOLVE-BLOCK-END\n\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n```"]}}, "artifacts_json": "{\"stdout\": \"Running in subprocess, Python version: 3.12.1 (v3.12.1:2305ca5144, Dec  7 2023, 17:23:38) [Clang 13.0.0 (clang-1300.0.29.30)]\\nProgram path: /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmpveweg9sd.py\\nCalling run_packing()...\\nrun_packing() returned successfully: sum_radii = 2.1999995999999995\\nResults saved to /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmpa1tz16xe.py.results\\n\", \"stderr\": \"\"}", "artifact_dir": null}