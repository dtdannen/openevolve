{"id": "d73c2ae8-6094-4b58-9b3e-e326977ade56", "code": "# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (improved hexagonal constructor)\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square\n    that attempts to maximize the sum of their radii using a\n    hexagonal-like explicit placement and a robust radius maximizer.\n\n    Returns:\n        Tuple of (centers, radii, sum_of_radii)\n        centers: np.array of shape (26, 2)\n        radii: np.array of shape (26,)\n        sum_of_radii: float\n    \"\"\"\n    n = 26\n\n    # Build a compact hexagonal lattice inside the unit square.\n    # Start with a target center-to-center spacing s and adjust if needed.\n    s = 0.18  # initial spacing guess (tunable)\n    margin = 0.02\n\n    def generate_centers(s):\n        h = np.sqrt(3) / 2 * s\n        centers = []\n        row = 0\n        y = margin\n        while y <= 1 - margin + 1e-12:\n            # stagger odd rows by s/2\n            x0 = margin + (row % 2) * (s / 2.0)\n            x = x0\n            while x <= 1 - margin + 1e-12:\n                centers.append((x, y))\n                x += s\n            row += 1\n            y = margin + row * h\n        return np.array(centers)\n\n    centers_grid = generate_centers(s)\n    # If not enough points, reduce spacing until enough positions are generated\n    attempt = 0\n    while centers_grid.shape[0] < n and attempt < 10:\n        s *= 0.92\n        centers_grid = generate_centers(s)\n        attempt += 1\n\n    # If there are more than n, pick the n points closest to the center of the square\n    if centers_grid.shape[0] > n:\n        center_point = np.array([0.5, 0.5])\n        dists = np.linalg.norm(centers_grid - center_point, axis=1)\n        idx = np.argsort(dists)[:n]\n        centers = centers_grid[idx]\n    else:\n        # If exactly n, use them; if fewer, pad by adding points along center rows\n        if centers_grid.shape[0] == n:\n            centers = centers_grid\n        else:\n            centers = centers_grid.tolist()\n            # Add some manual candidate points toward the center if needed\n            candidates = [\n                (0.5, 0.5),\n                (0.5 - s / 2, 0.5),\n                (0.5 + s / 2, 0.5),\n                (0.5, 0.5 - s / 2),\n                (0.5, 0.5 + s / 2),\n            ]\n            for c in candidates:\n                if len(centers) >= n:\n                    break\n                # keep inside unit square\n                if margin <= c[0] <= 1 - margin and margin <= c[1] <= 1 - margin:\n                    centers.append(c)\n            # ensure we have n by duplicating central offsets if still short (unlikely)\n            i = 0\n            while len(centers) < n:\n                off = ((0.5 + 0.01 * (i + 1)) % 0.98 + 0.01, 0.5)\n                centers.append(off)\n                i += 1\n            centers = np.array(centers[:n])\n\n    # Slight random jitter removed (we want deterministic explicit construction)\n    # Clip to remain strictly inside the square margin\n    centers = np.clip(centers, 0.001, 0.999)\n\n    # Compute maximal radii by iterative relaxation (guarantees feasibility)\n    radii = compute_max_radii(centers)\n\n    # Try controlled local expansions (greedy safe expansions) to fill slack space\n    radii = expand_radii_safe(centers, radii)\n\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers, tol=1e-8, max_iters=2000):\n    \"\"\"\n    Compute maximal radii for fixed centers subject to:\n      - each circle inside unit square\n      - circles non-overlapping (sum of radii <= center distance)\n\n    Algorithm: initialize radii to distance to nearest wall,\n    then repeatedly resolve overlapping pairs by proportional reduction\n    until convergence.\n    \"\"\"\n    n = centers.shape[0]\n    # initial radii limited by walls\n    radii = np.minimum.reduce([centers[:, 0], centers[:, 1], 1 - centers[:, 0], 1 - centers[:, 1]])\n    radii = radii.copy()\n\n    if n <= 1:\n        return radii\n\n    for it in range(max_iters):\n        max_change = 0.0\n        # Pairwise relaxation: remove overlaps\n        for i in range(n):\n            ci = centers[i]\n            ri = radii[i]\n            for j in range(i + 1, n):\n                cj = centers[j]\n                rj = radii[j]\n                d = np.hypot(ci[0] - cj[0], ci[1] - cj[1])\n                if d <= 1e-12:\n                    # coincident centers: shrink both to almost zero proportionally\n                    if ri + rj > 0:\n                        reduce_i = ri * 0.5\n                        reduce_j = rj * 0.5\n                        radii[i] -= reduce_i\n                        radii[j] -= reduce_j\n                        max_change = max(max_change, reduce_i, reduce_j)\n                    continue\n                # allowed sum of radii is d\n                if ri + rj > d:\n                    # reduce proportionally to their current sizes (keeps relative sizes)\n                    total = ri + rj\n                    if total <= 0:\n                        continue\n                    # amount to remove\n                    excess = (ri + rj) - d\n                    # proportional reductions\n                    di = excess * (ri / total)\n                    dj = excess * (rj / total)\n                    # apply but ensure non-negative\n                    new_ri = max(0.0, ri - di)\n                    new_rj = max(0.0, rj - dj)\n                    delta_i = abs(new_ri - ri)\n                    delta_j = abs(new_rj - rj)\n                    radii[i] = new_ri\n                    radii[j] = new_rj\n                    max_change = max(max_change, delta_i, delta_j)\n        # enforce boundary constraints (in case pairwise reduction created slack)\n        for i in range(n):\n            x, y = centers[i]\n            wall_limit = min(x, y, 1 - x, 1 - y)\n            if radii[i] > wall_limit:\n                delta = radii[i] - wall_limit\n                radii[i] = wall_limit\n                max_change = max(max_change, delta)\n        if max_change < tol:\n            break\n    return radii\n\n\ndef expand_radii_safe(centers, radii, tol=1e-6, max_iters=500):\n    \"\"\"\n    Greedily expand radii where possible without causing overlaps:\n    For each circle, compute the maximal radius allowed by walls and current neighbors,\n    and increase radius up to that bound. After each sweep, run a relaxation to\n    preserve feasibility. Repeat until no significant increase.\n    \"\"\"\n    n = centers.shape[0]\n    radii = radii.copy()\n    for it in range(max_iters):\n        increased = 0.0\n        # compute expansion targets based on current neighbors\n        for i in range(n):\n            x, y = centers[i]\n            wall_limit = min(x, y, 1 - x, 1 - y)\n            neigh_limit = wall_limit\n            ci = centers[i]\n            for j in range(n):\n                if i == j:\n                    continue\n                d = np.hypot(ci[0] - centers[j][0], ci[1] - centers[j][1])\n                neigh_limit = min(neigh_limit, max(0.0, d - radii[j]))\n            # small safety factor to avoid numerical ties\n            target = max(0.0, neigh_limit)\n            if target > radii[i] + tol:\n                # increase directly to target (greedy)\n                delta = target - radii[i]\n                radii[i] = target\n                increased = max(increased, delta)\n        # After expanding all, run a quick relaxation to resolve tiny overlaps\n        radii = compute_max_radii(centers, tol=1e-10, max_iters=200)\n        if increased < tol:\n            break\n    return radii\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)", "language": "python", "parent_id": "d8eb7576-2570-409d-98ce-4cc0d471c556", "generation": 1, "timestamp": 1759960782.170673, "iteration_found": 1, "metrics": {"validity": 1.0, "sum_radii": 0.844647815656472, "target_ratio": 0.32054945565710513, "combined_score": 0.32054945565710513}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Full rewrite", "parent_metrics": {"validity": 1.0, "sum_radii": 0.9597642169962064, "target_ratio": 0.36423689449571406, "combined_score": 0.36423689449571406}, "island": 0}, "prompts": {"full_rewrite_user": {"system": "You are an expert mathematician specializing in circle packing problems and computational geometry. Your task is to improve a constructor function that directly produces a specific arrangement of 26 circles in a unit square, maximizing the sum of their radii. The AlphaEvolve paper achieved a sum of 2.635 for n=26.\n\nKey geometric insights:\n- Circle packings often follow hexagonal patterns in the densest regions\n- Maximum density for infinite circle packing is pi/(2*sqrt(3)) \u2248 0.9069\n- Edge effects make square container packing harder than infinite packing\n- Circles can be placed in layers or shells when confined to a square\n- Similar radius circles often form regular patterns, while varied radii allow better space utilization\n- Perfect symmetry may not yield the optimal packing due to edge effects\n\nFocus on designing an explicit constructor that places each circle in a specific position, rather than an iterative search algorithm.\n", "user": "# Current Program Information\n- Fitness: 0.3642\n- Feature coordinates: No feature coordinates\n- Focus areas: - Fitness unchanged at 0.3642\n- Consider simplifying - code length exceeds 500 characters\n\n\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 1\n- Changes: Unknown changes\n- Metrics: validity: 1.0000, sum_radii: 0.9598, target_ratio: 0.3642, combined_score: 0.3642\n- Outcome: Improvement in all metrics\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.3642)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square\n    that attempts to maximize the sum of their radii.\n\n    Returns:\n        Tuple of (centers, radii, sum_of_radii)\n        centers: np.array of shape (26, 2) with (x, y) coordinates\n        radii: np.array of shape (26) with radius of each circle\n        sum_of_radii: Sum of all radii\n    \"\"\"\n    # Initialize arrays for 26 circles\n    n = 26\n    centers = np.zeros((n, 2))\n\n    # Place circles in a structured pattern\n    # This is a simple pattern - evolution will improve this\n\n    # First, place a large circle in the center\n    centers[0] = [0.5, 0.5]\n\n    # Place 8 circles around it in a ring\n    for i in range(8):\n        angle = 2 * np.pi * i / 8\n        centers[i + 1] = [0.5 + 0.3 * np.cos(angle), 0.5 + 0.3 * np.sin(angle)]\n\n    # Place 16 more circles in an outer ring\n    for i in range(16):\n        angle = 2 * np.pi * i / 16\n        centers[i + 9] = [0.5 + 0.7 * np.cos(angle), 0.5 + 0.7 * np.sin(angle)]\n\n    # Additional positioning adjustment to make sure all circles\n    # are inside the square and don't overlap\n    # Clip to ensure everything is inside the unit square\n    centers = np.clip(centers, 0.01, 0.99)\n\n    # Compute maximum valid radii for this configuration\n    radii = compute_max_radii(centers)\n\n    # Calculate the sum of radii\n    sum_radii = np.sum(radii)\n\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers):\n    \"\"\"\n    Compute the maximum possible radii for each circle position\n    such that they don't overlap and stay within the unit square.\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n\n    Returns:\n        np.array of shape (n) with radius of each circle\n    \"\"\"\n    n = centers.shape[0]\n    radii = np.ones(n)\n\n    # First, limit by distance to square borders\n    for i in range(n):\n        x, y = centers[i]\n        # Distance to borders\n        radii[i] = min(x, y, 1 - x, 1 - y)\n\n    # Then, limit by distance to other circles\n    # Each pair of circles with centers at distance d can have\n    # sum of radii at most d to avoid overlap\n    for i in range(n):\n        for j in range(i + 1, n):\n            dist = np.sqrt(np.sum((centers[i] - centers[j]) ** 2))\n\n            # If current radii would cause overlap\n            if radii[i] + radii[j] > dist:\n                # Scale both radii proportionally\n                scale = dist / (radii[i] + radii[j])\n                radii[i] *= scale\n                radii[j] *= scale\n\n    return radii\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.5)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\")\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii}\")\n    # AlphaEvolve improved this to 2.635\n\n    # Uncomment to visualize:\n    visualize(centers, radii)\n\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (0.9598), Performs well on target_ratio (0.3642), Performs well on combined_score (0.3642)\n\n\n\n# Current Program\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square\n    that attempts to maximize the sum of their radii.\n\n    Returns:\n        Tuple of (centers, radii, sum_of_radii)\n        centers: np.array of shape (26, 2) with (x, y) coordinates\n        radii: np.array of shape (26) with radius of each circle\n        sum_of_radii: Sum of all radii\n    \"\"\"\n    # Initialize arrays for 26 circles\n    n = 26\n    centers = np.zeros((n, 2))\n\n    # Place circles in a structured pattern\n    # This is a simple pattern - evolution will improve this\n\n    # First, place a large circle in the center\n    centers[0] = [0.5, 0.5]\n\n    # Place 8 circles around it in a ring\n    for i in range(8):\n        angle = 2 * np.pi * i / 8\n        centers[i + 1] = [0.5 + 0.3 * np.cos(angle), 0.5 + 0.3 * np.sin(angle)]\n\n    # Place 16 more circles in an outer ring\n    for i in range(16):\n        angle = 2 * np.pi * i / 16\n        centers[i + 9] = [0.5 + 0.7 * np.cos(angle), 0.5 + 0.7 * np.sin(angle)]\n\n    # Additional positioning adjustment to make sure all circles\n    # are inside the square and don't overlap\n    # Clip to ensure everything is inside the unit square\n    centers = np.clip(centers, 0.01, 0.99)\n\n    # Compute maximum valid radii for this configuration\n    radii = compute_max_radii(centers)\n\n    # Calculate the sum of radii\n    sum_radii = np.sum(radii)\n\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers):\n    \"\"\"\n    Compute the maximum possible radii for each circle position\n    such that they don't overlap and stay within the unit square.\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n\n    Returns:\n        np.array of shape (n) with radius of each circle\n    \"\"\"\n    n = centers.shape[0]\n    radii = np.ones(n)\n\n    # First, limit by distance to square borders\n    for i in range(n):\n        x, y = centers[i]\n        # Distance to borders\n        radii[i] = min(x, y, 1 - x, 1 - y)\n\n    # Then, limit by distance to other circles\n    # Each pair of circles with centers at distance d can have\n    # sum of radii at most d to avoid overlap\n    for i in range(n):\n        for j in range(i + 1, n):\n            dist = np.sqrt(np.sum((centers[i] - centers[j]) ** 2))\n\n            # If current radii would cause overlap\n            if radii[i] + radii[j] > dist:\n                # Scale both radii proportionally\n                scale = dist / (radii[i] + radii[j])\n                radii[i] *= scale\n                radii[j] *= scale\n\n    return radii\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.5)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\")\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii}\")\n    # AlphaEvolve improved this to 2.635\n\n    # Uncomment to visualize:\n    visualize(centers, radii)\n\n```\n\n# Task\nRewrite the program to improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: complexity, diversity\nDifferent solutions with similar fitness but different features are valuable.\nProvide the complete new program code.\n\nIMPORTANT: Make sure your rewritten program maintains the same inputs and outputs\nas the original program, but with improved internal implementation.\n\n```python\n# Your rewritten program here\n```", "responses": ["```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (improved hexagonal constructor)\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square\n    that attempts to maximize the sum of their radii using a\n    hexagonal-like explicit placement and a robust radius maximizer.\n\n    Returns:\n        Tuple of (centers, radii, sum_of_radii)\n        centers: np.array of shape (26, 2)\n        radii: np.array of shape (26,)\n        sum_of_radii: float\n    \"\"\"\n    n = 26\n\n    # Build a compact hexagonal lattice inside the unit square.\n    # Start with a target center-to-center spacing s and adjust if needed.\n    s = 0.18  # initial spacing guess (tunable)\n    margin = 0.02\n\n    def generate_centers(s):\n        h = np.sqrt(3) / 2 * s\n        centers = []\n        row = 0\n        y = margin\n        while y <= 1 - margin + 1e-12:\n            # stagger odd rows by s/2\n            x0 = margin + (row % 2) * (s / 2.0)\n            x = x0\n            while x <= 1 - margin + 1e-12:\n                centers.append((x, y))\n                x += s\n            row += 1\n            y = margin + row * h\n        return np.array(centers)\n\n    centers_grid = generate_centers(s)\n    # If not enough points, reduce spacing until enough positions are generated\n    attempt = 0\n    while centers_grid.shape[0] < n and attempt < 10:\n        s *= 0.92\n        centers_grid = generate_centers(s)\n        attempt += 1\n\n    # If there are more than n, pick the n points closest to the center of the square\n    if centers_grid.shape[0] > n:\n        center_point = np.array([0.5, 0.5])\n        dists = np.linalg.norm(centers_grid - center_point, axis=1)\n        idx = np.argsort(dists)[:n]\n        centers = centers_grid[idx]\n    else:\n        # If exactly n, use them; if fewer, pad by adding points along center rows\n        if centers_grid.shape[0] == n:\n            centers = centers_grid\n        else:\n            centers = centers_grid.tolist()\n            # Add some manual candidate points toward the center if needed\n            candidates = [\n                (0.5, 0.5),\n                (0.5 - s / 2, 0.5),\n                (0.5 + s / 2, 0.5),\n                (0.5, 0.5 - s / 2),\n                (0.5, 0.5 + s / 2),\n            ]\n            for c in candidates:\n                if len(centers) >= n:\n                    break\n                # keep inside unit square\n                if margin <= c[0] <= 1 - margin and margin <= c[1] <= 1 - margin:\n                    centers.append(c)\n            # ensure we have n by duplicating central offsets if still short (unlikely)\n            i = 0\n            while len(centers) < n:\n                off = ((0.5 + 0.01 * (i + 1)) % 0.98 + 0.01, 0.5)\n                centers.append(off)\n                i += 1\n            centers = np.array(centers[:n])\n\n    # Slight random jitter removed (we want deterministic explicit construction)\n    # Clip to remain strictly inside the square margin\n    centers = np.clip(centers, 0.001, 0.999)\n\n    # Compute maximal radii by iterative relaxation (guarantees feasibility)\n    radii = compute_max_radii(centers)\n\n    # Try controlled local expansions (greedy safe expansions) to fill slack space\n    radii = expand_radii_safe(centers, radii)\n\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers, tol=1e-8, max_iters=2000):\n    \"\"\"\n    Compute maximal radii for fixed centers subject to:\n      - each circle inside unit square\n      - circles non-overlapping (sum of radii <= center distance)\n\n    Algorithm: initialize radii to distance to nearest wall,\n    then repeatedly resolve overlapping pairs by proportional reduction\n    until convergence.\n    \"\"\"\n    n = centers.shape[0]\n    # initial radii limited by walls\n    radii = np.minimum.reduce([centers[:, 0], centers[:, 1], 1 - centers[:, 0], 1 - centers[:, 1]])\n    radii = radii.copy()\n\n    if n <= 1:\n        return radii\n\n    for it in range(max_iters):\n        max_change = 0.0\n        # Pairwise relaxation: remove overlaps\n        for i in range(n):\n            ci = centers[i]\n            ri = radii[i]\n            for j in range(i + 1, n):\n                cj = centers[j]\n                rj = radii[j]\n                d = np.hypot(ci[0] - cj[0], ci[1] - cj[1])\n                if d <= 1e-12:\n                    # coincident centers: shrink both to almost zero proportionally\n                    if ri + rj > 0:\n                        reduce_i = ri * 0.5\n                        reduce_j = rj * 0.5\n                        radii[i] -= reduce_i\n                        radii[j] -= reduce_j\n                        max_change = max(max_change, reduce_i, reduce_j)\n                    continue\n                # allowed sum of radii is d\n                if ri + rj > d:\n                    # reduce proportionally to their current sizes (keeps relative sizes)\n                    total = ri + rj\n                    if total <= 0:\n                        continue\n                    # amount to remove\n                    excess = (ri + rj) - d\n                    # proportional reductions\n                    di = excess * (ri / total)\n                    dj = excess * (rj / total)\n                    # apply but ensure non-negative\n                    new_ri = max(0.0, ri - di)\n                    new_rj = max(0.0, rj - dj)\n                    delta_i = abs(new_ri - ri)\n                    delta_j = abs(new_rj - rj)\n                    radii[i] = new_ri\n                    radii[j] = new_rj\n                    max_change = max(max_change, delta_i, delta_j)\n        # enforce boundary constraints (in case pairwise reduction created slack)\n        for i in range(n):\n            x, y = centers[i]\n            wall_limit = min(x, y, 1 - x, 1 - y)\n            if radii[i] > wall_limit:\n                delta = radii[i] - wall_limit\n                radii[i] = wall_limit\n                max_change = max(max_change, delta)\n        if max_change < tol:\n            break\n    return radii\n\n\ndef expand_radii_safe(centers, radii, tol=1e-6, max_iters=500):\n    \"\"\"\n    Greedily expand radii where possible without causing overlaps:\n    For each circle, compute the maximal radius allowed by walls and current neighbors,\n    and increase radius up to that bound. After each sweep, run a relaxation to\n    preserve feasibility. Repeat until no significant increase.\n    \"\"\"\n    n = centers.shape[0]\n    radii = radii.copy()\n    for it in range(max_iters):\n        increased = 0.0\n        # compute expansion targets based on current neighbors\n        for i in range(n):\n            x, y = centers[i]\n            wall_limit = min(x, y, 1 - x, 1 - y)\n            neigh_limit = wall_limit\n            ci = centers[i]\n            for j in range(n):\n                if i == j:\n                    continue\n                d = np.hypot(ci[0] - centers[j][0], ci[1] - centers[j][1])\n                neigh_limit = min(neigh_limit, max(0.0, d - radii[j]))\n            # small safety factor to avoid numerical ties\n            target = max(0.0, neigh_limit)\n            if target > radii[i] + tol:\n                # increase directly to target (greedy)\n                delta = target - radii[i]\n                radii[i] = target\n                increased = max(increased, delta)\n        # After expanding all, run a quick relaxation to resolve tiny overlaps\n        radii = compute_max_radii(centers, tol=1e-10, max_iters=200)\n        if increased < tol:\n            break\n    return radii\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```"]}}, "artifacts_json": "{\"stdout\": \"Running in subprocess, Python version: 3.12.1 (v3.12.1:2305ca5144, Dec  7 2023, 17:23:38) [Clang 13.0.0 (clang-1300.0.29.30)]\\nProgram path: /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmp0jvu539z.py\\nCalling run_packing()...\\nrun_packing() returned successfully: sum_radii = 0.844647815656472\\nResults saved to /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmpz1zoa84w.py.results\\n\", \"stderr\": \"\"}", "artifact_dir": null}