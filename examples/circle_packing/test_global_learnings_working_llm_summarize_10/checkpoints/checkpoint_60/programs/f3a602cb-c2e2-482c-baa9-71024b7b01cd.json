{"id": "f3a602cb-c2e2-482c-baa9-71024b7b01cd", "code": "# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for 26 circles in the unit square.\nPattern: compact near-hex cluster rows [5,5,6,5,5]. Deterministic coarse\ngrid search over horizontal spacing and vertical anisotropy, per-row shifts\nand small translations. Radii set by r = min(dist_to_border, 0.5*nearest).\nA small coordinate-descent refinement tightens the best candidate.\n\"\"\"\nimport numpy as np\nfrom itertools import product\n\ndef construct_packing():\n    n = 26\n    rows = [5, 5, 6, 5, 5]\n    vstep_unit = np.sqrt(3.0) / 2.0\n\n    # base integer layout (horiz spacing = 1, vertical units = vstep_unit)\n    base = []\n    for r_idx, cnt in enumerate(rows):\n        xs = np.arange(cnt) - (cnt - 1) / 2.0\n        x_offset = 0.5 * (r_idx % 2)\n        y = r_idx * vstep_unit\n        for x in xs:\n            base.append([x + x_offset, y])\n    base = np.array(base)  # (26,2)\n\n    def compute_radii(centers, margin=0.0):\n        db = np.minimum.reduce([centers[:,0]-margin,\n                                centers[:,1]-margin,\n                                1.0-margin-centers[:,0],\n                                1.0-margin-centers[:,1]])\n        dif = centers[:, None, :] - centers[None, :, :]\n        D = np.sqrt(np.maximum(0.0, (dif**2).sum(axis=2)))\n        np.fill_diagonal(D, np.inf)\n        nearest = D.min(axis=1)\n        radii = np.minimum(db, 0.5*nearest)\n        return np.maximum(radii, 0.0)\n\n    def build_centers(s_h, v_ratio, row_shifts, dx=0.0, dy=0.0, margin=1e-12):\n        s_v = v_ratio * vstep_unit * s_h\n        coords = base.copy()\n        coords[:,0] *= s_h\n        coords[:,1] *= (s_v / vstep_unit)\n        # apply per-row x shifts\n        idx = 0\n        for r,cnt in enumerate(rows):\n            inds = slice(idx, idx+cnt)\n            coords[inds,0] += row_shifts[r]\n            idx += cnt\n        # center pattern in square\n        lo = coords.min(axis=0); hi = coords.max(axis=0)\n        trans = 0.5 - 0.5*(lo + hi)\n        coords = coords + trans + np.array([dx, dy])\n        return np.clip(coords, margin, 1.0 - margin)\n\n    # parameter grids (deterministic)\n    s_h_vals = np.linspace(0.095, 0.185, 20)\n    v_ratio_vals = np.linspace(0.94, 1.06, 7)\n    shift_fracs = [-0.045, 0.0, 0.045]\n    all_shifts = list(product(shift_fracs, repeat=len(rows)))\n    trans_grid = [(0.0,0.0), (-0.002,0.0),(0.002,0.0),(0.0,-0.002),(0.0,0.002),\n                  (-0.002,-0.002),(0.002,0.002)]\n\n    best_sum = -1.0\n    best_params = None\n    best_centers = None\n    best_radii = None\n\n    # coarse grid search\n    for s_h in s_h_vals:\n        for v_ratio in v_ratio_vals:\n            for frac_combo in all_shifts:\n                row_shifts = np.array(frac_combo) * s_h\n                for dx,dy in trans_grid:\n                    centers = build_centers(s_h, v_ratio, row_shifts, dx=dx, dy=dy)\n                    r = compute_radii(centers)\n                    ssum = float(r.sum())\n                    if ssum > best_sum + 1e-12:\n                        best_sum = ssum\n                        best_centers = centers.copy()\n                        best_radii = r.copy()\n                        best_params = (s_h, v_ratio, row_shifts.copy(), dx, dy)\n\n    # if nothing found (shouldn't happen), fallback to a centered modest scale\n    if best_params is None:\n        best_centers = build_centers(0.12, 1.0, np.zeros(len(rows)))\n        best_radii = compute_radii(best_centers)\n        best_sum = float(best_radii.sum())\n        best_params = (0.12, 1.0, np.zeros(len(rows)), 0.0, 0.0)\n\n    # deterministic coordinate-descent refinement around best params\n    cur_s_h, cur_v_ratio, cur_row_shifts, cur_dx, cur_dy = best_params\n    s_h_steps = [0.012, 0.006, 0.003, 0.0015]\n    v_steps = [0.03, 0.015, 0.007, 0.0035]\n    row_steps = [0.03*cur_s_h, 0.015*cur_s_h, 0.007*cur_s_h, 0.003*cur_s_h]\n    trans_steps = [0.004, 0.002, 0.001, 0.0005]\n\n    for level in range(len(s_h_steps)):\n        improved = True\n        it = 0\n        while improved and it < 6:\n            improved = False\n            it += 1\n            # tweak s_h\n            for sign in (-1,1):\n                cand = cur_s_h + sign * s_h_steps[level]\n                if 0.04 < cand < 0.35:\n                    centers = build_centers(cand, cur_v_ratio, cur_row_shifts, cur_dx, cur_dy)\n                    r = compute_radii(centers)\n                    ssum = float(r.sum())\n                    if ssum > best_sum + 1e-12:\n                        best_sum = ssum; best_centers = centers.copy(); best_radii = r.copy()\n                        cur_s_h = cand; improved = True\n            # tweak v_ratio\n            for sign in (-1,1):\n                cand = cur_v_ratio + sign * v_steps[level]\n                if 0.7 < cand < 1.4:\n                    centers = build_centers(cur_s_h, cand, cur_row_shifts, cur_dx, cur_dy)\n                    r = compute_radii(centers)\n                    ssum = float(r.sum())\n                    if ssum > best_sum + 1e-12:\n                        best_sum = ssum; best_centers = centers.copy(); best_radii = r.copy()\n                        cur_v_ratio = cand; improved = True\n            # per-row shifts\n            for ridx in range(len(rows)):\n                for sign in (-1,1):\n                    cand_shifts = cur_row_shifts.copy()\n                    cand_shifts[ridx] += sign * row_steps[level]\n                    centers = build_centers(cur_s_h, cur_v_ratio, cand_shifts, cur_dx, cur_dy)\n                    r = compute_radii(centers)\n                    ssum = float(r.sum())\n                    if ssum > best_sum + 1e-12:\n                        best_sum = ssum; best_centers = centers.copy(); best_radii = r.copy()\n                        cur_row_shifts = cand_shifts; improved = True\n            # translations\n            for tx_mult, ty_mult in product((-1,0,1), repeat=2):\n                if tx_mult==0 and ty_mult==0: continue\n                cand_dx = cur_dx + tx_mult * trans_steps[level]\n                cand_dy = cur_dy + ty_mult * trans_steps[level]\n                centers = build_centers(cur_s_h, cur_v_ratio, cur_row_shifts, cand_dx, cand_dy)\n                r = compute_radii(centers)\n                ssum = float(r.sum())\n                if ssum > best_sum + 1e-12:\n                    best_sum = ssum; best_centers = centers.copy(); best_radii = r.copy()\n                    cur_dx, cur_dy = cand_dx, cand_dy; improved = True\n\n    # final tiny nudges\n    for dx in (-5e-4, 0.0, 5e-4):\n        for dy in (-5e-4, 0.0, 5e-4):\n            centers = build_centers(cur_s_h, cur_v_ratio, cur_row_shifts, cur_dx+dx, cur_dy+dy)\n            r = compute_radii(centers)\n            ssum = float(r.sum())\n            if ssum > best_sum + 1e-12:\n                best_sum = ssum; best_centers = centers.copy(); best_radii = r.copy()\n                cur_dx += dx; cur_dy += dy\n\n    return np.array(best_centers), np.array(best_radii), float(best_radii.sum())\n# EVOLVE-BLOCK-END\n\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\nif __name__ == \"__main__\":\n    c, r, s = run_packing()\n    print(f\"Sum of radii: {s:.6f}\")", "language": "python", "parent_id": "35ffb346-5808-400e-acc7-a390bb3e56bb", "generation": 2, "timestamp": 1759962270.4685419, "iteration_found": 51, "metrics": {"validity": 1.0, "sum_radii": 2.3054999999999994, "target_ratio": 0.874952561669829, "combined_score": 0.874952561669829, "eval_time": 8.098933935165405}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Full rewrite", "parent_metrics": {"validity": 1.0, "sum_radii": 2.0776306444240342, "target_ratio": 0.7884746278649087, "combined_score": 0.7884746278649087, "eval_time": 0.060484886169433594}, "island": 0}, "prompts": {"full_rewrite_user": {"system": "You are an expert mathematician specializing in circle packing problems and computational geometry. Your task is to improve a constructor function that directly produces a specific arrangement of 26 circles in a unit square, maximizing the sum of their radii. The AlphaEvolve paper achieved a sum of 2.635 for n=26.\n\nKey geometric insights:\n- Circle packings often follow hexagonal patterns in the densest regions\n- Maximum density for infinite circle packing is pi/(2*sqrt(3)) \u2248 0.9069\n- Edge effects make square container packing harder than infinite packing\n- Circles can be placed in layers or shells when confined to a square\n- Similar radius circles often form regular patterns, while varied radii allow better space utilization\n- Perfect symmetry may not yield the optimal packing due to edge effects\n\nFocus on designing an explicit constructor that places each circle in a specific position, rather than an iterative search algorithm.\n\n\n## Evolution Insights (Global Learnings)\n\n### Common Pitfalls:\n\u26a0\ufe0f (seen 5x) The child replaced the iterative pairwise shrinkage loop with a fully vectorized computation of all pairwise distances and simply set each radius to min(distance-to-nearest-neighbor/2, distance-to-border), while also changing the row layout and spacing search \u2014 this eliminated the inner iterative passes, giving a much faster eval_time and a larger sum_radii. When making similar optimizations, avoid assuming nearest-neighbor/half-distance always enforces global non-overlap (it can overestimate feasible radii in multi-overlap configurations) and be mindful that the O(n^2) distance matrix approach uses more memory and may not scale; validate results against the original iterative solver or a constrained optimizer.\n\u26a0\ufe0f (seen 3x) The regression was caused by replacing the original ring-based placement and proportional downscaling with a dense hex-like layered layout (s=0.192) plus an LP-like iterative tightening that enforces r_i \u2264 min(border_i, min_j(d_ij \u2212 r_j)) and breaks overlaps by reducing the larger radius; that layout produced many very small inter-center and border distances so the tightening collapsed many radii, dropping sum_radii from ~0.96 to ~0.34.  \nWhen making similar changes, avoid fixed spacings/layouts that create infeasible small distances without first checking feasible radii, and be wary of greedy one-sided tightening rules\u2014add pre-checks (min pairwise distance vs desired radii), unit tests for sum-of-radii expectations, or use global/proportional constraint solvers to prevent asymmetric collapse.\n\u26a0\ufe0f (seen 3x) You replaced the original ring-based layout and proportional pairwise rescaling with a dense hex-like layered layout (rows=[5,5,6,5,5], s=0.192) and an LP-like iterative tightening radii solver that only decreases radii; the new geometry + solver combination produced much smaller pairwise distances so the iterative clamp drove many radii toward zero and the total sum dropped ~65%. When changing packing geometry or the constraint solver, re-tune spacing/row layout and verify pairwise distances (and sum_radii) before committing\u2014add regression tests, visualize configurations, and parameterize or add safeguards in the solver so it cannot monotonically collapse radii without detection.\n\u26a0\ufe0f (seen 3x) The regression was caused by replacing the original ring layout and proportional pairwise scaling with a denser hex-like row layout (s=0.192) combined with a greedy, monotone \"iterative tightening\" radii solver; the new center configuration yields many small inter-center distances and the order-dependent decrease + final \u201creduce the larger one\u201d fixstep aggressively shrinks radii, dropping the total sum substantially.  \n\nAvoid changing both the packing geometry and the constraint-enforcement strategy at once \u2014 when you alter spacing or the radii algorithm, first inspect pairwise distances, run small diagnostic/visual tests, and prefer order-independent approaches (LP solver, symmetric scaling, or provably convergent methods) or add unit tests to detect order-dependent over-shrinking.", "user": "# Current Program Information\n- Fitness: 0.7885\n- Feature coordinates: No feature coordinates\n- Focus areas: - Fitness declined: 0.8471 \u2192 0.7885. Consider revising recent changes.\n- Consider simplifying - code length exceeds 500 characters\n\n## Last Execution Output\n\n### stdout\n```\nRunning in subprocess, Python version: 3.12.1 (v3.12.1:2305ca5144, Dec  7 2023, 17:23:38) [Clang 13.0.0 (clang-1300.0.29.30)]\nProgram path: /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmpjb3palu1.py\nCalling run_packing()...\nrun_packing() returned successfully: sum_radii = 2.0776306444240342\nResults saved to /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmpcjeu62nl.py.results\n\n```\n\n### stderr\n```\n\n```\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 3\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.2322, target_ratio: 0.8471, combined_score: 0.8471, eval_time: 0.3555\n- Outcome: Mixed results\n\n### Attempt 2\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.2325, target_ratio: 0.8472, combined_score: 0.8472, eval_time: 0.1405\n- Outcome: Mixed results\n\n### Attempt 1\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.3055, target_ratio: 0.8750, combined_score: 0.8750, eval_time: 8.0680\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.8750)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for 26 circles in the unit square.\nImproved explicit pattern with a wider, deterministic search over\nhorizontal spacing, anisotropic vertical ratio, per-row shifts and\nsmall translations. Uses the exact maximal radii for fixed centers:\nr_i = min(dist_to_border, 0.5 * nearest_neighbor_distance).\n\nThis version does a coarse grid search and then a simple deterministic\ncoordinate-descent refinement to squeeze more total radius out of the\nsame near-hex cluster layout.\n\"\"\"\nimport numpy as np\nfrom itertools import product\n\ndef construct_packing():\n    n = 26\n    rows = [5, 5, 6, 5, 5]  # compact near-hex cluster\n    vstep_hex = np.sqrt(3.0) / 2.0\n\n    # base integer layout (centered about origin in base units)\n    base = []\n    for r_idx, cnt in enumerate(rows):\n        xs = np.arange(cnt) - (cnt - 1) / 2.0\n        x_offset = 0.5 * (r_idx % 2)\n        y = r_idx * vstep_hex\n        for x in xs:\n            base.append([x + x_offset, y])\n    base = np.array(base)  # (26,2)\n\n    # helper: compute radii for a given centers array\n    def compute_radii(centers, margin=0.0):\n        # distance to border (consider margin for safety)\n        db = np.minimum.reduce([centers[:, 0] - margin,\n                                centers[:, 1] - margin,\n                                1.0 - margin - centers[:, 0],\n                                1.0 - margin - centers[:, 1]])\n        # pairwise center distances\n        dif = centers[:, None, :] - centers[None, :, :]\n        D = np.sqrt(np.maximum(0.0, (dif ** 2).sum(axis=2)))\n        np.fill_diagonal(D, np.inf)\n        nearest = D.min(axis=1)\n        radii = np.minimum(db, 0.5 * nearest)\n        return np.maximum(radii, 0.0)\n\n    # Build centers from parameters: horizontal spacing s_h, vertical ratio r_v,\n    # per-row shifts (absolute units), and translation (dx,dy)\n    def build_centers(s_h, v_ratio, row_shifts, dx=0.0, dy=0.0, margin=1e-9):\n        s_v = v_ratio * vstep_hex * s_h  # absolute vertical step\n        coords = base.copy()\n        # scale: base horizontal units become s_h, vertical base units become s_v/vstep_hex\n        coords[:, 0] = coords[:, 0] * s_h\n        coords[:, 1] = coords[:, 1] * (s_v / vstep_hex)\n        # apply per-row shifts\n        idx = 0\n        for r, cnt in enumerate(rows):\n            inds = slice(idx, idx + cnt)\n            coords[inds, 0] += row_shifts[r]\n            idx += cnt\n        # center pattern in the unit square (before applying dx,dy) so translations are meaningful\n        lo = coords.min(axis=0); hi = coords.max(axis=0)\n        trans = 0.5 - 0.5 * (lo + hi)\n        coords = coords + trans\n        # apply small translation and clip to margin inside square\n        coords = coords + np.array([dx, dy])\n        coords = np.clip(coords, margin, 1.0 - margin)\n        return coords\n\n    # Coarse search ranges (deterministic)\n    # horizontal spacing range chosen to allow tight packing but avoid overlaps that force tiny radii\n    s_h_vals = np.linspace(0.095, 0.185, 20)      # 20 values\n    v_ratio_vals = np.linspace(0.94, 1.06, 7)     # vertical anisotropy multiplier\n    shift_fracs = [-0.045, 0.0, 0.045]            # absolute row shifts (will be scaled below)\n    # We'll interpret these as fractions of s_h (so shift = frac * s_h)\n    # small translation candidates (to nudge cluster into corners/edges)\n    trans_grid = [(0.0, 0.0),\n                  (-0.002, 0.0), (0.002, 0.0),\n                  (0.0, -0.002), (0.0, 0.002),\n                  (-0.002, -0.002), (0.002, 0.002)]\n\n    best_sum = -1.0\n    best_params = None\n    best_centers = None\n    best_radii = None\n\n    # Pre-generate all combinations of per-row shift fractions (3^5 = 243)\n    all_shift_combos = list(product(shift_fracs, repeat=len(rows)))\n\n    # Coarse grid search\n    for s_h in s_h_vals:\n        for v_ratio in v_ratio_vals:\n            # compute row shifts in absolute units = frac * s_h\n            for frac_combo in all_shift_combos:\n                row_shifts = np.array(frac_combo) * s_h\n                # try a few translations too\n                for dx, dy in trans_grid:\n                    centers = build_centers(s_h, v_ratio, row_shifts, dx=dx, dy=dy)\n                    r = compute_radii(centers, margin=1e-9)\n                    ssum = float(r.sum())\n                    if ssum > best_sum + 1e-12:\n                        best_sum = ssum\n                        best_params = (s_h, v_ratio, row_shifts.copy(), dx, dy)\n                        best_centers = centers.copy()\n                        best_radii = r.copy()\n\n    # Deterministic coordinate-descent refinement around best found coarse params\n    if best_params is None:\n        # fallback: place a modestly scaled base centered\n        s_h = 0.12\n        v_ratio = 1.0\n        row_shifts = np.zeros(len(rows))\n        best_centers = build_centers(s_h, v_ratio, row_shifts)\n        best_radii = compute_radii(best_centers)\n        best_sum = float(best_radii.sum())\n        best_params = (s_h, v_ratio, row_shifts.copy(), 0.0, 0.0)\n\n    # unpack\n    cur_s_h, cur_v_ratio, cur_row_shifts, cur_dx, cur_dy = best_params\n\n    # refinement schedule: progressively smaller step sizes\n    s_h_steps = [0.012, 0.006, 0.003, 0.0015]\n    v_ratio_steps = [0.03, 0.015, 0.007, 0.0035]\n    row_shift_steps = [0.03 * cur_s_h, 0.015 * cur_s_h, 0.007 * cur_s_h, 0.003 * cur_s_h]\n    trans_steps = [0.004, 0.002, 0.001, 0.0005]\n\n    # deterministic local search: try increasing/decreasing each parameter if improves sum\n    for level in range(len(s_h_steps)):\n        improved = True\n        iter_count = 0\n        while improved and iter_count < 6:\n            improved = False\n            iter_count += 1\n            # try s_h adjustments\n            for dx_mult in (-1, 1):\n                cand_s_h = cur_s_h + dx_mult * s_h_steps[level]\n                if cand_s_h <= 0.04 or cand_s_h >= 0.35:\n                    continue\n                centers = build_centers(cand_s_h, cur_v_ratio, cur_row_shifts, cur_dx, cur_dy)\n                r = compute_radii(centers)\n                ssum = float(r.sum())\n                if ssum > best_sum + 1e-12:\n                    best_sum = ssum\n                    best_centers = centers.copy()\n                    best_radii = r.copy()\n                    cur_s_h = cand_s_h\n                    improved = True\n            # try v_ratio adjustments\n            for dv in (-1, 1):\n                cand_v = cur_v_ratio + dv * v_ratio_steps[level]\n                if cand_v <= 0.7 or cand_v >= 1.4:\n                    continue\n                centers = build_centers(cur_s_h, cand_v, cur_row_shifts, cur_dx, cur_dy)\n                r = compute_radii(centers)\n                ssum = float(r.sum())\n                if ssum > best_sum + 1e-12:\n                    best_sum = ssum\n                    best_centers = centers.copy()\n                    best_radii = r.copy()\n                    cur_v_ratio = cand_v\n                    improved = True\n            # try per-row shift adjustments (each row independently)\n            for r_idx in range(len(rows)):\n                for dir_mult in (-1, 1):\n                    cand_row_shifts = cur_row_shifts.copy()\n                    step = row_shift_steps[level]\n                    cand_row_shifts[r_idx] += dir_mult * step\n                    centers = build_centers(cur_s_h, cur_v_ratio, cand_row_shifts, cur_dx, cur_dy)\n                    r = compute_radii(centers)\n                    ssum = float(r.sum())\n                    if ssum > best_sum + 1e-12:\n                        best_sum = ssum\n                        best_centers = centers.copy()\n                        best_radii = r.copy()\n                        cur_row_shifts = cand_row_shifts\n                        improved = True\n            # try translations\n            for tx_mult, ty_mult in product((-1, 0, 1), repeat=2):\n                if tx_mult == 0 and ty_mult == 0:\n                    continue\n                cand_dx = cur_dx + tx_mult * trans_steps[level]\n                cand_dy = cur_dy + ty_mult * trans_steps[level]\n                centers = build_centers(cur_s_h, cur_v_ratio, cur_row_shifts, cand_dx, cand_dy)\n                r = compute_radii(centers)\n                ssum = float(r.sum())\n                if ssum > best_sum + 1e-12:\n                    best_sum = ssum\n                    best_centers = centers.copy()\n                    best_radii = r.copy()\n                    cur_dx, cur_dy = cand_dx, cand_dy\n                    improved = True\n\n    # Final tiny nudges deterministically (very small translations)\n    for dx_try in (-5e-4, 0.0, 5e-4):\n        for dy_try in (-5e-4, 0.0, 5e-4):\n            centers = build_centers(cur_s_h, cur_v_ratio, cur_row_shifts, cur_dx + dx_try, cur_dy + dy_try)\n            r = compute_radii(centers)\n            ssum = float(r.sum())\n            if ssum > best_sum + 1e-12:\n                best_sum = ssum\n                best_centers = centers.copy()\n                best_radii = r.copy()\n                cur_dx += dx_try; cur_dy += dy_try\n\n    # Safety clamp if any negative or zero radii\n    if best_radii is None:\n        best_centers = build_centers(0.12, 1.0, np.zeros(len(rows)))\n        best_radii = compute_radii(best_centers)\n\n    return np.array(best_centers), np.array(best_radii), float(best_radii.sum())\n\n# EVOLVE-BLOCK-END\n\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.3055), Performs well on target_ratio (0.8750), Performs well on combined_score (0.8750), Performs well on eval_time (8.0680)\n\n### Program 2 (Score: 0.8472)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for packing 26 circles in the unit square.\n\nUses a compact near-hex cluster (rows [5,5,6,5,5]), a vectorized radius\ncomputation r_i = min(dist_to_border, 0.5*nearest_neighbor), a coarse scale\nsweep to find a strong global fit, and small anisotropic + per-row-shift\nrefinements to break symmetry and improve edge utilization.\n\"\"\"\nimport numpy as np\nfrom itertools import product\n\ndef construct_packing():\n    n = 26\n    rows = [5, 5, 6, 5, 5]  # sums to 26\n    vstep = np.sqrt(3.0) / 2.0\n\n    # build base integer-layout coords (unit horizontal spacing)\n    base = []\n    for r_idx, cnt in enumerate(rows):\n        xs = np.arange(cnt) - (cnt - 1) / 2.0\n        x_offset = 0.5 * (r_idx % 2)\n        for xi in xs:\n            base.append([xi + x_offset, r_idx * vstep])\n    base = np.array(base)\n    assert base.shape[0] == n\n\n    # bounding box of base pattern\n    minb = base.min(axis=0)\n    maxb = base.max(axis=0)\n    width = maxb[0] - minb[0]\n    height = maxb[1] - minb[1]\n\n    # small margin to avoid exact-border degeneracies\n    margin = 1e-6\n    Smax = min((1 - 2 * margin) / width, (1 - 2 * margin) / height)\n\n    def compute_radii(centers):\n        # distance to border\n        d_border = np.minimum.reduce([centers[:, 0], centers[:, 1], 1 - centers[:, 0], 1 - centers[:, 1]])\n        # pairwise distances\n        dif = centers[:, None, :] - centers[None, :, :]\n        D = np.sqrt(np.maximum(0.0, (dif ** 2).sum(axis=2)))\n        np.fill_diagonal(D, np.inf)\n        nearest = D.min(axis=1)\n        radii = np.minimum(d_border, 0.5 * nearest)\n        return np.maximum(radii, 0.0)\n\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n    best_s = None\n\n    # Coarse uniform scale sweep (cover a wide fraction of Smax)\n    for alpha in np.linspace(0.42, 1.00, 61):\n        s = alpha * Smax\n        scaled = (base - minb) * s\n        # center cluster inside unit square\n        translate = 0.5 - 0.5 * (scaled.min(axis=0) + scaled.max(axis=0))\n        centers = scaled + translate\n        centers = np.clip(centers, margin, 1 - margin)\n        radii = compute_radii(centers)\n        ssum = float(radii.sum())\n        if ssum > best_sum:\n            best_sum = ssum\n            best_centers = centers.copy()\n            best_radii = radii.copy()\n            best_s = s\n\n    # Refinement: small anisotropic scaling around best_s and per-row horizontal shifts\n    shift_fracs = [-0.04, 0.0, 0.04]\n    tx_vals = [0.996, 1.0, 1.004]\n    ty_vals = [0.996, 1.0, 1.004]\n\n    # Precompute row indices\n    row_indices = []\n    idx = 0\n    for cnt in rows:\n        row_indices.append(list(range(idx, idx + cnt)))\n        idx += cnt\n\n    if best_centers is not None:\n        for tx, ty in product(tx_vals, ty_vals):\n            s_x = best_s * tx\n            # Keep vertical spacing consistent with hex geometry\n            s_y = best_s * ty\n            anis_base = base.copy()\n            anis_base[:, 0] = (anis_base[:, 0] - minb[0]) * (s_x)\n            anis_base[:, 1] = (anis_base[:, 1] - minb[1]) * (s_y / vstep)\n            # try all small per-row shifts (3^5 = 243 combos)\n            for shifts in product(shift_fracs, repeat=len(rows)):\n                centers = anis_base.copy()\n                for r, shift_frac in enumerate(shifts):\n                    inds = row_indices[r]\n                    centers[inds, 0] += shift_frac * best_s\n                # center & clip\n                translate = 0.5 - 0.5 * (centers.min(axis=0) + centers.max(axis=0))\n                centers = np.clip(centers + translate, margin, 1 - margin)\n                radii = compute_radii(centers)\n                ssum = float(radii.sum())\n                if ssum > best_sum:\n                    best_sum = ssum\n                    best_centers = centers.copy()\n                    best_radii = radii.copy()\n\n    # Final tiny nudges to potentially snug pattern against walls\n    for dx, dy in [(0.0, 0.0), (-1e-3, 0.0), (1e-3, 0.0), (0.0, -1e-3), (0.0, 1e-3)]:\n        centers = np.clip(best_centers + np.array([dx, dy]), margin, 1 - margin)\n        radii = compute_radii(centers)\n        ssum = float(radii.sum())\n        if ssum > best_sum:\n            best_sum = ssum\n            best_centers = centers.copy()\n            best_radii = radii.copy()\n\n    # Safety fallback (should not happen)\n    if best_centers is None:\n        scaled = (base - minb) * (Smax * 0.5)\n        translate = 0.5 - 0.5 * (scaled.min(axis=0) + scaled.max(axis=0))\n        best_centers = np.clip(scaled + translate, margin, 1 - margin)\n        best_radii = compute_radii(best_centers)\n        best_sum = float(best_radii.sum())\n\n    return np.array(best_centers), np.array(best_radii), float(best_radii.sum())\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.2325), Performs well on target_ratio (0.8472), Performs well on combined_score (0.8472), Performs well on eval_time (0.1405)\n\n### Program 3 (Score: 0.8471)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for 26 circles in the unit square.\nImproved explicit pattern: compact near-hex cluster (rows 5+5+6+5+5),\ncoarse uniform scale sweep to find a tight fit, followed by small\nanisotropic, per-row-shift and translation refinements. Radii are\nassigned as r_i = min(distance-to-border, 0.5*nearest-center-distance).\nThis keeps the constructor simple, deterministic and fast.\n\"\"\"\nimport numpy as np\nfrom itertools import product\n\ndef construct_packing():\n    n = 26\n    # Use the high-performing row pattern seen in top solutions\n    rows = [5, 5, 6, 5, 5]  # sums to 26\n    vstep = np.sqrt(3.0) / 2.0\n\n    # Build a canonical unit-spaced hex-like base layout (not yet scaled)\n    base = []\n    for r_idx, cnt in enumerate(rows):\n        xs = np.arange(cnt) - (cnt - 1) / 2.0\n        x_offset = 0.5 * (r_idx % 2)\n        y = r_idx * vstep\n        for x in xs:\n            base.append([x + x_offset, y])\n    base = np.array(base)  # (26,2)\n\n    # compute bounding extents of base\n    minb = base.min(axis=0)\n    maxb = base.max(axis=0)\n    width = maxb[0] - minb[0]\n    height = maxb[1] - minb[1]\n\n    margin = 1e-6  # tiny margin to avoid exact border-touch\n    # maximum uniform scale to ensure the pattern can fit inside the unit square\n    Smax = min((1 - 2 * margin) / width, (1 - 2 * margin) / height)\n\n    def compute_radii(centers):\n        # distance to border\n        db = np.minimum.reduce([centers[:, 0], centers[:, 1], 1 - centers[:, 0], 1 - centers[:, 1]])\n        # pairwise distances (vectorized)\n        dif = centers[:, None, :] - centers[None, :, :]\n        D = np.sqrt(np.maximum(0.0, (dif ** 2).sum(axis=2)))\n        np.fill_diagonal(D, np.inf)\n        nearest = D.min(axis=1)\n        radii = np.minimum(db, 0.5 * nearest)\n        # ensure non-negative\n        return np.maximum(radii, 0.0)\n\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    # Coarse uniform scale sweep (explore slightly beyond Smax with clipping)\n    for alpha in np.linspace(0.45, 1.02, 61):\n        s = alpha * Smax\n        # scale base and center it in unit square\n        scaled = (base - minb) * s\n        lo = scaled.min(axis=0); hi = scaled.max(axis=0)\n        trans = 0.5 - 0.5 * (lo + hi)\n        centers = scaled + trans\n        centers = np.clip(centers, margin, 1 - margin)\n        r = compute_radii(centers)\n        ssum = float(r.sum())\n        if ssum > best_sum:\n            best_sum = ssum\n            best_centers = centers.copy()\n            best_radii = r.copy()\n\n    # Local refinements: anisotropic scaling, small per-row horizontal shifts,\n    # and tiny translations to let the cluster \"snug\" against edges.\n    if best_centers is None:\n        # fallback\n        best_centers = np.clip((base - minb) * (0.14) + 0.5, margin, 1 - margin)\n        best_radii = compute_radii(best_centers)\n        best_sum = float(best_radii.sum())\n\n    # Prepare per-row indices for applying row shifts\n    row_indices = []\n    idx = 0\n    for cnt in rows:\n        row_indices.append(list(range(idx, idx + cnt)))\n        idx += cnt\n\n    # small sets of anisotropic multipliers (around 1.0) and shift fractions\n    tx_vals = [0.997, 1.0, 1.003]\n    ty_vals = [0.997, 1.0, 1.003]\n    shift_fracs = [-0.035, 0.0, 0.035]  # fractions of Smax for horizontal shifts\n    translations = [(-0.002, 0.0), (0.0, -0.002), (0.0, 0.0), (0.002, 0.0), (0.0, 0.002)]\n\n    base_coords = base.copy()\n    # We'll apply anisotropic scaling relative to the canonical base bounding box\n    for tx, ty in product(tx_vals, ty_vals):\n        sx = tx * Smax\n        sy = ty * Smax * (vstep) / (vstep)  # keep consistent vertical unit (explicit)\n        # scale base coordinates\n        scaled = base_coords.copy()\n        scaled[:, 0] = (scaled[:, 0] - minb[0]) * sx\n        scaled[:, 1] = (scaled[:, 1] - minb[1]) * sy / vstep\n        # try per-row horizontal shifts combinations (3^5 = 243)\n        for shifts in product(shift_fracs, repeat=len(rows)):\n            centers = scaled.copy()\n            for r, frac in enumerate(shifts):\n                inds = row_indices[r]\n                centers[inds, 0] += frac * Smax\n            # center and try small translations\n            lo = centers.min(axis=0); hi = centers.max(axis=0)\n            trans = 0.5 - 0.5 * (lo + hi)\n            centers = centers + trans\n            for dx, dy in translations:\n                C = np.clip(centers + np.array([dx, dy]), margin, 1 - margin)\n                r = compute_radii(C)\n                ssum = float(r.sum())\n                if ssum > best_sum:\n                    best_sum = ssum\n                    best_centers = C.copy()\n                    best_radii = r.copy()\n\n    # Final tiny nudges along each axis (deterministic small moves)\n    for dx in (-1e-3, 0.0, 1e-3):\n        for dy in (-1e-3, 0.0, 1e-3):\n            C = np.clip(best_centers + np.array([dx, dy]), margin, 1 - margin)\n            r = compute_radii(C)\n            ssum = float(r.sum())\n            if ssum > best_sum:\n                best_sum = ssum\n                best_centers = C.copy()\n                best_radii = r.copy()\n\n    return np.array(best_centers), np.array(best_radii), float(best_radii.sum())\n\n# EVOLVE-BLOCK-END\n\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(7,7))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect('equal')\n    for i,(c,r) in enumerate(zip(centers, radii)):\n        ax.add_patch(Circle(c,r,alpha=0.6,edgecolor='k'))\n        ax.text(c[0],c[1],str(i),ha='center',va='center',fontsize=8)\n    plt.title(f\"n={len(centers)}, sum={sum(radii):.6f}\"); plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # visualize(centers, radii)\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.2322), Performs well on target_ratio (0.8471), Performs well on combined_score (0.8471), Performs well on eval_time (0.3555)\n\n\n\n## Diverse Programs\n\n### Program D1 (Score: 0.8443)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (deterministic hex cluster + staged refinement)\"\"\"\nimport numpy as np\nfrom itertools import product\n\n\ndef construct_packing():\n    \"\"\"\n    Build a deterministic layout of 26 circle centers based on a compact\n    hex-like cluster (rows 5+5+6+5+5). Use a coarse uniform scale search\n    to find a good global packing, then refine with small anisotropic\n    tweaks and per-row horizontal shifts to break perfect symmetry.\n    Returns centers (26,2), radii (26,), sum_radii\n    \"\"\"\n    n = 26\n    # Compact hex-like row counts summing to 26\n    rows = [5, 5, 6, 5, 5]\n\n    # Base integer layout coordinates (unit horizontal spacing)\n    base = []\n    vstep = np.sqrt(3) / 2.0  # hex vertical step for unit horiz spacing\n    for r_idx, cnt in enumerate(rows):\n        xs = np.arange(cnt) - (cnt - 1) / 2.0\n        x_offset = 0.5 * (r_idx % 2)\n        for xi in xs:\n            base.append([xi + x_offset, r_idx * vstep])\n    base = np.array(base)  # (26,2)\n\n    # bounding box of base pattern\n    minb = base.min(axis=0)\n    maxb = base.max(axis=0)\n    width = maxb[0] - minb[0]\n    height = maxb[1] - minb[1]\n\n    # small margin to avoid numerical border-touch\n    margin = 1e-4\n    # maximum uniform scale to fit inside [margin,1-margin]\n    Smax = min((1 - 2 * margin) / width, (1 - 2 * margin) / height)\n\n    def compute_radii(centers):\n        # distance to border\n        d_border = np.minimum.reduce([centers[:, 0], centers[:, 1], 1 - centers[:, 0], 1 - centers[:, 1]])\n        # pairwise distances\n        C = centers\n        dif = C[:, None, :] - C[None, :, :]\n        D = np.sqrt(np.maximum(0.0, (dif ** 2).sum(axis=2)))\n        np.fill_diagonal(D, np.inf)\n        nearest = D.min(axis=1)\n        radii = np.minimum(d_border, 0.5 * nearest)\n        radii = np.maximum(radii, 0.0)\n        return radii\n\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n    best_s = None\n\n    # Coarse uniform scale sweep\n    for alpha in np.linspace(0.40, 1.00, 61):\n        s = alpha * Smax\n        scaled = (base - minb) * s\n        # center cluster inside unit square\n        scaled_min = scaled.min(axis=0)\n        scaled_max = scaled.max(axis=0)\n        translate = 0.5 - 0.5 * (scaled_min + scaled_max)\n        centers = scaled + translate\n        centers = np.clip(centers, margin, 1 - margin)\n        radii = compute_radii(centers)\n        ssum = radii.sum()\n        if ssum > best_sum:\n            best_sum = ssum\n            best_centers = centers.copy()\n            best_radii = radii.copy()\n            best_s = s\n\n    # Refinement: small anisotropic scaling around best_s and per-row horizontal shifts\n    # Per-row shifts (in units of current spacing s) to break symmetry; try -0.03,0,0.03 fractions\n    shift_fracs = [-0.03, 0.0, 0.03]\n    tx_vals = [0.996, 1.0, 1.004]\n    ty_vals = [0.996, 1.0, 1.004]\n\n    # Precompute base x positions per row for convenience\n    row_indices = []\n    idx = 0\n    for cnt in rows:\n        row_indices.append(list(range(idx, idx + cnt)))\n        idx += cnt\n    # horizontal spacing in base units is 1.0, so shift in absolute units = frac * best_s\n    for tx, ty in product(tx_vals, ty_vals):\n        s_x = best_s * tx\n        s_y = best_s * ty * (np.sqrt(3) / 2.0) / (np.sqrt(3) / 2.0)  # keep vertical ratio consistent\n        # build scaled base with these anisotropic scales\n        anis_base = base.copy()\n        # apply anisotropic scale: scale x by (s_x), y by (s_y / vstep) so that unit vertical spacing becomes s_y\n        anis_base[:, 0] = (anis_base[:, 0] - minb[0]) * (s_x)\n        anis_base[:, 1] = (anis_base[:, 1] - minb[1]) * (s_y / vstep)\n        # try all combinations of small per-row horizontal shifts (3^5 = 243)\n        for shifts in product(shift_fracs, repeat=len(rows)):\n            centers = anis_base.copy()\n            # apply per-row x shift to each member in row (shift is fraction of best_s)\n            for r, shift_frac in enumerate(shifts):\n                inds = row_indices[r]\n                centers[inds, 0] += shift_frac * best_s\n            # center into unit square\n            cmin = centers.min(axis=0)\n            cmax = centers.max(axis=0)\n            translate = 0.5 - 0.5 * (cmin + cmax)\n            centers = centers + translate\n            centers = np.clip(centers, margin, 1 - margin)\n            radii = compute_radii(centers)\n            ssum = radii.sum()\n            if ssum > best_sum:\n                best_sum = ssum\n                best_centers = centers.copy()\n                best_radii = radii.copy()\n\n    # As a final small local tweak, try translating the whole pattern to snug against edges slightly\n    # (keeps deterministic)\n    for dx, dy in [(0.0, 0.0), (-1e-3, 0.0), (1e-3, 0.0), (0.0, -1e-3), (0.0, 1e-3)]:\n        centers = np.clip(best_centers + np.array([dx, dy]), margin, 1 - margin)\n        radii = compute_radii(centers)\n        ssum = radii.sum()\n        if ssum > best_sum:\n            best_sum = ssum\n            best_centers = centers.copy()\n            best_radii = radii.copy()\n\n    return np.array(best_centers), np.array(best_radii), float(best_radii.sum())\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\", linewidth=0.6)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nKey features: Alternative approach to validity, Alternative approach to sum_radii\n\n### Program D2 (Score: 0.8442)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for packing 26 circles in the unit square.\n\nUses a compact near-hex cluster (rows [5,5,6,5,5]), a vectorized radius\ncomputation r_i = min(dist_to_border, 0.5*nearest_neighbor), a coarse scale\nsweep to find a good global fit, and a small anisotropic + per-row-shift\nrefinement to break symmetry and improve edge utilization.\n\"\"\"\nimport numpy as np\nfrom itertools import product\n\ndef construct_packing():\n    n = 26\n    rows = [5, 5, 6, 5, 5]  # sums to 26\n\n    # Build base integer lattice coords (unit horizontal spacing)\n    base = []\n    vstep = np.sqrt(3) / 2.0\n    for r_idx, cnt in enumerate(rows):\n        xs = np.arange(cnt) - (cnt - 1) / 2.0\n        x_offset = 0.5 * (r_idx % 2)\n        for xi in xs:\n            base.append([xi + x_offset, r_idx * vstep])\n    base = np.array(base)\n    assert base.shape[0] == n\n\n    # bounding box of base pattern\n    minb = base.min(axis=0)\n    maxb = base.max(axis=0)\n    width = maxb[0] - minb[0]\n    height = maxb[1] - minb[1]\n\n    # tiny margin to keep centers strictly inside unit square\n    margin = 1e-6\n    Smax = min((1 - 2 * margin) / width, (1 - 2 * margin) / height)\n\n    def compute_radii(centers):\n        # distance to border\n        d_border = np.minimum.reduce([centers[:, 0], centers[:, 1], 1 - centers[:, 0], 1 - centers[:, 1]])\n        # pairwise distances (vectorized)\n        C = centers\n        dif = C[:, None, :] - C[None, :, :]\n        D = np.sqrt(np.maximum(0.0, (dif ** 2).sum(axis=2)))\n        np.fill_diagonal(D, np.inf)\n        nearest = D.min(axis=1)\n        radii = np.minimum(d_border, 0.5 * nearest)\n        radii = np.maximum(radii, 0.0)\n        return radii\n\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n    best_s = None\n\n    # Coarse uniform scale sweep (cover a wide fraction of Smax)\n    for alpha in np.linspace(0.45, 1.00, 61):\n        s = alpha * Smax\n        scaled = (base - minb) * s\n        # center cluster inside unit square\n        translate = 0.5 - 0.5 * (scaled.min(axis=0) + scaled.max(axis=0))\n        centers = scaled + translate\n        centers = np.clip(centers, margin, 1 - margin)\n        radii = compute_radii(centers)\n        ssum = float(radii.sum())\n        if ssum > best_sum:\n            best_sum = ssum\n            best_centers = centers.copy()\n            best_radii = radii.copy()\n            best_s = s\n\n    # Small anisotropic tweaks and per-row horizontal shifts to break symmetry\n    if best_centers is not None:\n        # precompute row indices\n        row_indices = []\n        idx = 0\n        for cnt in rows:\n            row_indices.append(list(range(idx, idx + cnt)))\n            idx += cnt\n\n        shift_fracs = [-0.03, 0.0, 0.03]  # fractions of spacing\n        tx_vals = [0.998, 1.0, 1.002]\n        ty_vals = [0.998, 1.0, 1.002]\n\n        for tx, ty in product(tx_vals, ty_vals):\n            s_x = best_s * tx\n            # keep vertical spacing tied to hex vertical step\n            s_y = best_s * ty * (vstep) / vstep\n            anis_base = base.copy()\n            anis_base[:, 0] = (anis_base[:, 0] - minb[0]) * (s_x)\n            anis_base[:, 1] = (anis_base[:, 1] - minb[1]) * (s_y / vstep)\n            # try a limited subset of per-row shift combinations: center shifts and small offsets\n            for shifts in product(shift_fracs, repeat=len(rows)):\n                centers = anis_base.copy()\n                for r, shift_frac in enumerate(shifts):\n                    inds = row_indices[r]\n                    centers[inds, 0] += shift_frac * best_s\n                # re-center and clip\n                translate = 0.5 - 0.5 * (centers.min(axis=0) + centers.max(axis=0))\n                centers = np.clip(centers + translate, margin, 1 - margin)\n                radii = compute_radii(centers)\n                ssum = float(radii.sum())\n                if ssum > best_sum:\n                    best_sum = ssum\n                    best_centers = centers.copy()\n                    best_radii = radii.copy()\n\n        # tiny nudges to snug against walls if beneficial\n        for dx, dy in [(0.0, 0.0), (-1e-3, 0.0), (1e-3, 0.0), (0.0, -1e-3), (0.0, 1e-3)]:\n            centers = np.clip(best_centers + np.array([dx, dy]), margin, 1 - margin)\n            radii = compute_radii(centers)\n            ssum = float(radii.sum())\n            if ssum > best_sum:\n                best_sum = ssum\n                best_centers = centers.copy()\n                best_radii = radii.copy()\n\n    # Fallback safety (should not trigger)\n    if best_centers is None:\n        scaled = (base - minb) * (Smax * 0.5)\n        translate = 0.5 - 0.5 * (scaled.min(axis=0) + scaled.max(axis=0))\n        best_centers = np.clip(scaled + translate, margin, 1 - margin)\n        best_radii = compute_radii(best_centers)\n        best_sum = float(best_radii.sum())\n\n    return np.array(best_centers), np.array(best_radii), float(best_radii.sum())\n# EVOLVE-BLOCK-END\n\n\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(7, 7))\n    ax.set_xlim(0, 1); ax.set_ylim(0, 1); ax.set_aspect('equal')\n    for i, (c, r) in enumerate(zip(centers, radii)):\n        ax.add_patch(Circle(c, r, alpha=0.6, edgecolor='k'))\n        ax.text(c[0], c[1], str(i), ha='center', va='center', fontsize=8)\n    plt.title(f\"n={len(centers)}, sum={sum(radii):.6f}\"); plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # visualize(centers, radii)\n```\nKey features: Alternative approach to validity, Alternative approach to sum_radii\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 0.7825, Type: Alternative)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for 26 circles in the unit square.\n\nImprovements over previous version:\n- Try two sensible row patterns (original good pattern and a slightly\n  asymmetric alternative) to reduce edge waste.\n- For each candidate center layout perform a modest parameter sweep\n  (uniform scale, slight anisotropy, small per-row horizontal shifts,\n  small translations).\n- Radii are found by a stable, order-independent iterative constraint\n  solver: start from a safe upper bound r0 = min(distance-to-border, 0.5*nearest)\n  and repeatedly apply the constraints r_i <= min(border_i, min_j(d_ij - r_j))\n  in synchronous updates (monotone decrease). This enforces all pairwise\n  and border constraints and converges to a consistent feasible set of radii.\n- Keep the search deterministic, compact and focused on promising variations.\n\nThis constructor aims to improve sum(radii) while remaining fast and robust.\n\"\"\"\nimport numpy as np\nfrom itertools import product\n\ndef compute_radii_iterative(centers, tol=1e-7, max_iter=500):\n    # centers: (n,2)\n    n = centers.shape[0]\n    # distance to border\n    db = np.minimum.reduce([centers[:,0], centers[:,1], 1-centers[:,0], 1-centers[:,1]])\n    # pairwise distances\n    dif = centers[:,None,:] - centers[None,:,:]\n    D = np.sqrt(np.maximum(0.0, (dif**2).sum(axis=2)))\n    np.fill_diagonal(D, np.inf)\n    # initial upper bound: can't exceed half nearest or border\n    nearest = D.min(axis=1)\n    r = np.minimum(db, 0.5*nearest)\n    r = np.maximum(r, 0.0)\n    # iterative synchronous tightening: r <- min(r, min_j (d_ij - r_j), border)\n    for _ in range(max_iter):\n        # compute matrix D - r_j (broadcast r over rows)\n        M = D - r[None,:]\n        # min across j gives the tightest pairwise constraint for each i\n        T = np.minimum(db, M.min(axis=1))\n        # ensure nonnegative\n        T = np.maximum(T, 0.0)\n        # only decrease (monotone), so take min with current r\n        r_new = np.minimum(r, T)\n        if np.max(np.abs(r_new - r)) < tol:\n            r = r_new\n            break\n        r = r_new\n    return r\n\ndef build_hex_base(rows):\n    vstep = np.sqrt(3.0)/2.0\n    base = []\n    for r_idx, cnt in enumerate(rows):\n        xs = np.arange(cnt) - (cnt - 1)/2.0\n        x_offset = 0.5 * (r_idx % 2)\n        y = r_idx * vstep\n        for x in xs:\n            base.append([x + x_offset, y])\n    return np.array(base), vstep\n\ndef construct_packing():\n    n = 26\n    # two candidate row patterns (both sum to 26)\n    patterns = [\n        [5,5,6,5,5],  # high-performing symmetric pattern\n        [4,5,6,5,6],  # slightly asymmetric alternative to exploit edge effects\n    ]\n\n    margin = 1e-8\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    # small parameter grids (kept modest for speed)\n    alphas = np.linspace(0.45, 1.02, 12)  # uniform scale multipliers\n    sx_vals = [0.999, 1.0, 1.001]         # tiny anisotropy in x\n    sy_vals = [0.999, 1.0, 1.001]         # tiny anisotropy in y\n    translations = [(-0.002, 0.0), (0.0, -0.002), (0.0,0.0), (0.002,0.0), (0.0,0.002)]\n\n    # per-row horizontal shift options; restrict combinations to <=2 nonzero shifts\n    shift_options = [-0.035, 0.0, 0.035]\n\n    for rows in patterns:\n        base, vstep = build_hex_base(rows)\n        minb = base.min(axis=0)\n        maxb = base.max(axis=0)\n        width = maxb[0] - minb[0]\n        height = maxb[1] - minb[1]\n        Smax = min((1 - 2*margin)/width, (1 - 2*margin)/height)\n\n        # enumerate allowed shift combinations (only up to 2 nonzero shifts to avoid collapse)\n        all_shifts = []\n        for comb in product(shift_options, repeat=len(rows)):\n            nz = sum(1 for v in comb if abs(v) > 1e-12)\n            if nz <= 2:\n                all_shifts.append(comb)\n\n        for alpha in alphas:\n            base_scaled = (base - minb) * (alpha * Smax)\n            # center base_scaled in unit square\n            lo = base_scaled.min(axis=0); hi = base_scaled.max(axis=0)\n            trans0 = 0.5 - 0.5*(lo + hi)\n            base_scaled = base_scaled + trans0\n\n            for sx, sy in product(sx_vals, sy_vals):\n                # apply tiny anisotropic scaling relative to center of pattern\n                centers = base_scaled.copy()\n                # scale around center of bounding box\n                lo = centers.min(axis=0); hi = centers.max(axis=0)\n                center_box = 0.5*(lo + hi)\n                centers = (centers - center_box) * np.array([sx, sy]) + center_box\n\n                # try a limited set of per-row shifts (horizontal)\n                idx = 0\n                row_inds = []\n                for cnt in rows:\n                    row_inds.append(list(range(idx, idx+cnt)))\n                    idx += cnt\n\n                for shifts in all_shifts:\n                    C = centers.copy()\n                    for r_idx, frac in enumerate(shifts):\n                        inds = row_inds[r_idx]\n                        C[inds,0] += frac * Smax  # horizontal shift\n                    # center again and try small translations\n                    lo = C.min(axis=0); hi = C.max(axis=0)\n                    trans = 0.5 - 0.5*(lo + hi)\n                    C = C + trans\n                    for dx, dy in translations:\n                        CC = np.clip(C + np.array([dx, dy]), margin, 1.0 - margin)\n                        r = compute_radii_iterative(CC, tol=1e-7, max_iter=400)\n                        ssum = float(r.sum())\n                        if ssum > best_sum:\n                            best_sum = ssum\n                            best_centers = CC.copy()\n                            best_radii = r.copy()\n\n    # final tiny nudges to try to squeeze a bit more\n    if best_centers is not None:\n        for dx in (-5e-4, 0.0, 5e-4):\n            for dy in (-5e-4, 0.0, 5e-4):\n                C = np.clip(best_centers + np.array([dx, dy]), margin, 1.0 - margin)\n                r = compute_radii_iterative(C, tol=1e-8, max_iter=600)\n                ssum = float(r.sum())\n                if ssum > best_sum:\n                    best_sum = ssum\n                    best_centers = C.copy()\n                    best_radii = r.copy()\n\n    # fallback in the unlikely event nothing was found\n    if best_centers is None:\n        base, vstep = build_hex_base([5,5,6,5,5])\n        minb = base.min(axis=0)\n        best_centers = np.clip((base - minb) * 0.14 + 0.5, margin, 1.0 - margin)\n        best_radii = compute_radii_iterative(best_centers)\n\n    return np.array(best_centers), np.array(best_radii), float(best_radii.sum())\n\n# EVOLVE-BLOCK-END\n\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(7,7))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect('equal')\n    for i,(c,r) in enumerate(zip(centers, radii)):\n        ax.add_patch(Circle(c,r,alpha=0.6,edgecolor='k'))\n        ax.text(c[0],c[1],str(i),ha='center',va='center',fontsize=8)\n    plt.title(f\"n={len(centers)}, sum={sum(radii):.6f}\"); plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    #visualize(centers, radii)\n```\nUnique approach: Modification: Full rewrite, Excellent validity (1.000), Excellent sum_radii (2.062)\n\n### Inspiration 2 (Score: 0.8471, Type: High-Performer)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for 26 circles in the unit square.\nImproved explicit pattern: compact near-hex cluster (rows 5+5+6+5+5),\ncoarse uniform scale sweep to find a tight fit, followed by small\nanisotropic, per-row-shift and translation refinements. Radii are\nassigned as r_i = min(distance-to-border, 0.5*nearest-center-distance).\nThis keeps the constructor simple, deterministic and fast.\n\"\"\"\nimport numpy as np\nfrom itertools import product\n\ndef construct_packing():\n    n = 26\n    # Use the high-performing row pattern seen in top solutions\n    rows = [5, 5, 6, 5, 5]  # sums to 26\n    vstep = np.sqrt(3.0) / 2.0\n\n    # Build a canonical unit-spaced hex-like base layout (not yet scaled)\n    base = []\n    for r_idx, cnt in enumerate(rows):\n        xs = np.arange(cnt) - (cnt - 1) / 2.0\n        x_offset = 0.5 * (r_idx % 2)\n        y = r_idx * vstep\n        for x in xs:\n            base.append([x + x_offset, y])\n    base = np.array(base)  # (26,2)\n\n    # compute bounding extents of base\n    minb = base.min(axis=0)\n    maxb = base.max(axis=0)\n    width = maxb[0] - minb[0]\n    height = maxb[1] - minb[1]\n\n    margin = 1e-6  # tiny margin to avoid exact border-touch\n    # maximum uniform scale to ensure the pattern can fit inside the unit square\n    Smax = min((1 - 2 * margin) / width, (1 - 2 * margin) / height)\n\n    def compute_radii(centers):\n        # distance to border\n        db = np.minimum.reduce([centers[:, 0], centers[:, 1], 1 - centers[:, 0], 1 - centers[:, 1]])\n        # pairwise distances (vectorized)\n        dif = centers[:, None, :] - centers[None, :, :]\n        D = np.sqrt(np.maximum(0.0, (dif ** 2).sum(axis=2)))\n        np.fill_diagonal(D, np.inf)\n        nearest = D.min(axis=1)\n        radii = np.minimum(db, 0.5 * nearest)\n        # ensure non-negative\n        return np.maximum(radii, 0.0)\n\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    # Coarse uniform scale sweep (explore slightly beyond Smax with clipping)\n    for alpha in np.linspace(0.45, 1.02, 61):\n        s = alpha * Smax\n        # scale base and center it in unit square\n        scaled = (base - minb) * s\n        lo = scaled.min(axis=0); hi = scaled.max(axis=0)\n        trans = 0.5 - 0.5 * (lo + hi)\n        centers = scaled + trans\n        centers = np.clip(centers, margin, 1 - margin)\n        r = compute_radii(centers)\n        ssum = float(r.sum())\n        if ssum > best_sum:\n            best_sum = ssum\n            best_centers = centers.copy()\n            best_radii = r.copy()\n\n    # Local refinements: anisotropic scaling, small per-row horizontal shifts,\n    # and tiny translations to let the cluster \"snug\" against edges.\n    if best_centers is None:\n        # fallback\n        best_centers = np.clip((base - minb) * (0.14) + 0.5, margin, 1 - margin)\n        best_radii = compute_radii(best_centers)\n        best_sum = float(best_radii.sum())\n\n    # Prepare per-row indices for applying row shifts\n    row_indices = []\n    idx = 0\n    for cnt in rows:\n        row_indices.append(list(range(idx, idx + cnt)))\n        idx += cnt\n\n    # small sets of anisotropic multipliers (around 1.0) and shift fractions\n    tx_vals = [0.997, 1.0, 1.003]\n    ty_vals = [0.997, 1.0, 1.003]\n    shift_fracs = [-0.035, 0.0, 0.035]  # fractions of Smax for horizontal shifts\n    translations = [(-0.002, 0.0), (0.0, -0.002), (0.0, 0.0), (0.002, 0.0), (0.0, 0.002)]\n\n    base_coords = base.copy()\n    # We'll apply anisotropic scaling relative to the canonical base bounding box\n    for tx, ty in product(tx_vals, ty_vals):\n        sx = tx * Smax\n        sy = ty * Smax * (vstep) / (vstep)  # keep consistent vertical unit (explicit)\n        # scale base coordinates\n        scaled = base_coords.copy()\n        scaled[:, 0] = (scaled[:, 0] - minb[0]) * sx\n        scaled[:, 1] = (scaled[:, 1] - minb[1]) * sy / vstep\n        # try per-row horizontal shifts combinations (3^5 = 243)\n        for shifts in product(shift_fracs, repeat=len(rows)):\n            centers = scaled.copy()\n            for r, frac in enumerate(shifts):\n                inds = row_indices[r]\n                centers[inds, 0] += frac * Smax\n            # center and try small translations\n            lo = centers.min(axis=0); hi = centers.max(axis=0)\n            trans = 0.5 - 0.5 * (lo + hi)\n            centers = centers + trans\n            for dx, dy in translations:\n                C = np.clip(centers + np.array([dx, dy]), margin, 1 - margin)\n                r = compute_radii(C)\n                ssum = float(r.sum())\n                if ssum > best_sum:\n                    best_sum = ssum\n                    best_centers = C.copy()\n                    best_radii = r.copy()\n\n    # Final tiny nudges along each axis (deterministic small moves)\n    for dx in (-1e-3, 0.0, 1e-3):\n        for dy in (-1e-3, 0.0, 1e-3):\n            C = np.clip(best_centers + np.array([dx, dy]), margin, 1 - margin)\n            r = compute_radii(C)\n            ssum = float(r.sum())\n            if ssum > best_sum:\n                best_sum = ssum\n                best_centers = C.copy()\n                best_radii = r.copy()\n\n    return np.array(best_centers), np.array(best_radii), float(best_radii.sum())\n\n# EVOLVE-BLOCK-END\n\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(7,7))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect('equal')\n    for i,(c,r) in enumerate(zip(centers, radii)):\n        ax.add_patch(Circle(c,r,alpha=0.6,edgecolor='k'))\n        ax.text(c[0],c[1],str(i),ha='center',va='center',fontsize=8)\n    plt.title(f\"n={len(centers)}, sum={sum(radii):.6f}\"); plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # visualize(centers, radii)\n```\nUnique approach: Excellent validity (1.000), Excellent sum_radii (2.232), NumPy-based implementation\n\n# Current Program\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (hex-grid constructor with scale search)\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square.\n    Returns: centers (26,2), radii (26,), sum_of_radii\n    \"\"\"\n    n = 26\n    # Row counts forming a compact hex-like cluster: 5+5+6+5+5 = 26\n    rows = [5, 5, 6, 5, 5]\n    # Build base hex coordinates with unit horizontal spacing and vertical spacing sqrt(3)/2\n    coords = []\n    vstep = np.sqrt(3) / 2.0\n    r_index = 0\n    for r, cnt in enumerate(rows):\n        # center x positions symmetric about 0\n        xs = np.arange(cnt) - (cnt - 1) / 2.0\n        # stagger every other row (hex packing)\n        x_offset = 0.5 * (r % 2)\n        for xi in xs:\n            coords.append([xi + x_offset, r_index * vstep])\n        r_index += 1\n    base = np.array(coords)  # shape (26,2)\n\n    # Compute bounding box of base coords\n    minb = base.min(axis=0)\n    maxb = base.max(axis=0)\n    width = maxb[0] - minb[0]\n    height = maxb[1] - minb[1]\n\n    # Allow a tiny margin from the unit square border\n    margin = 0.005\n\n    # Maximum uniform scale so the cluster fits into [margin,1-margin]\n    Smax = min((1 - 2 * margin) / width, (1 - 2 * margin) / height)\n\n    # Search scale fraction alpha to maximize sum of radii (coarse-to-fine)\n    def compute_radii(centers):\n        # distance to border\n        d_border = np.minimum(np.minimum(centers[:, 0], 1 - centers[:, 0]),\n                              np.minimum(centers[:, 1], 1 - centers[:, 1]))\n        # pairwise distances\n        C = centers\n        D = np.sqrt(np.maximum(0.0, ((C[:, None, :] - C[None, :, :]) ** 2).sum(axis=2)))\n        # set large self-dist to inf to ignore\n        np.fill_diagonal(D, np.inf)\n        d_nearest = D.min(axis=1)\n        radii = np.minimum(d_border, 0.5 * d_nearest)\n        # numerical safety: non-negative\n        radii = np.maximum(radii, 0.0)\n        return radii\n\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    # coarse search\n    for alpha in np.linspace(0.35, 1.0, 66):\n        s = alpha * Smax\n        scaled = (base - minb) * s  # now in [0, width*s] x [0, height*s]\n        # translate to center inside unit square with margin\n        scaled_min = scaled.min(axis=0)\n        scaled_max = scaled.max(axis=0)\n        translate = 0.5 - 0.5 * (scaled_min + scaled_max)\n        centers = scaled + translate\n        # clamp (tiny numerical fix)\n        centers = np.clip(centers, margin, 1 - margin)\n        radii = compute_radii(centers)\n        ssum = radii.sum()\n        if ssum > best_sum:\n            best_sum = ssum\n            best_centers = centers.copy()\n            best_radii = radii.copy()\n\n    # fine local refinement around best alpha by tiny jittering of row offsets\n    # allow slight horizontal compress/expand to better use border space\n    if best_centers is not None:\n        # try small anisotropic scale tweaks\n        for tx in np.linspace(0.98, 1.02, 5):\n            for ty in np.linspace(0.98, 1.02, 5):\n                scaled = ((base - minb) * np.array([tx, ty])) * (Smax * 0.98)\n                scaled_min = scaled.min(axis=0)\n                scaled_max = scaled.max(axis=0)\n                translate = 0.5 - 0.5 * (scaled_min + scaled_max)\n                centers = scaled + translate\n                centers = np.clip(centers, margin, 1 - margin)\n                radii = compute_radii(centers)\n                ssum = radii.sum()\n                if ssum > best_sum:\n                    best_sum = ssum\n                    best_centers = centers.copy()\n                    best_radii = radii.copy()\n\n    # Ensure outputs are proper shape and types\n    centers = np.array(best_centers)\n    radii = np.array(best_radii)\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\n\n# Task\nRewrite the program to improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: complexity, diversity\nDifferent solutions with similar fitness but different features are valuable.\nProvide the complete new program code.\n\nIMPORTANT: Make sure your rewritten program maintains the same inputs and outputs\nas the original program, but with improved internal implementation.\n\n```python\n# Your rewritten program here\n```", "responses": ["```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for 26 circles in the unit square.\nPattern: compact near-hex cluster rows [5,5,6,5,5]. Deterministic coarse\ngrid search over horizontal spacing and vertical anisotropy, per-row shifts\nand small translations. Radii set by r = min(dist_to_border, 0.5*nearest).\nA small coordinate-descent refinement tightens the best candidate.\n\"\"\"\nimport numpy as np\nfrom itertools import product\n\ndef construct_packing():\n    n = 26\n    rows = [5, 5, 6, 5, 5]\n    vstep_unit = np.sqrt(3.0) / 2.0\n\n    # base integer layout (horiz spacing = 1, vertical units = vstep_unit)\n    base = []\n    for r_idx, cnt in enumerate(rows):\n        xs = np.arange(cnt) - (cnt - 1) / 2.0\n        x_offset = 0.5 * (r_idx % 2)\n        y = r_idx * vstep_unit\n        for x in xs:\n            base.append([x + x_offset, y])\n    base = np.array(base)  # (26,2)\n\n    def compute_radii(centers, margin=0.0):\n        db = np.minimum.reduce([centers[:,0]-margin,\n                                centers[:,1]-margin,\n                                1.0-margin-centers[:,0],\n                                1.0-margin-centers[:,1]])\n        dif = centers[:, None, :] - centers[None, :, :]\n        D = np.sqrt(np.maximum(0.0, (dif**2).sum(axis=2)))\n        np.fill_diagonal(D, np.inf)\n        nearest = D.min(axis=1)\n        radii = np.minimum(db, 0.5*nearest)\n        return np.maximum(radii, 0.0)\n\n    def build_centers(s_h, v_ratio, row_shifts, dx=0.0, dy=0.0, margin=1e-12):\n        s_v = v_ratio * vstep_unit * s_h\n        coords = base.copy()\n        coords[:,0] *= s_h\n        coords[:,1] *= (s_v / vstep_unit)\n        # apply per-row x shifts\n        idx = 0\n        for r,cnt in enumerate(rows):\n            inds = slice(idx, idx+cnt)\n            coords[inds,0] += row_shifts[r]\n            idx += cnt\n        # center pattern in square\n        lo = coords.min(axis=0); hi = coords.max(axis=0)\n        trans = 0.5 - 0.5*(lo + hi)\n        coords = coords + trans + np.array([dx, dy])\n        return np.clip(coords, margin, 1.0 - margin)\n\n    # parameter grids (deterministic)\n    s_h_vals = np.linspace(0.095, 0.185, 20)\n    v_ratio_vals = np.linspace(0.94, 1.06, 7)\n    shift_fracs = [-0.045, 0.0, 0.045]\n    all_shifts = list(product(shift_fracs, repeat=len(rows)))\n    trans_grid = [(0.0,0.0), (-0.002,0.0),(0.002,0.0),(0.0,-0.002),(0.0,0.002),\n                  (-0.002,-0.002),(0.002,0.002)]\n\n    best_sum = -1.0\n    best_params = None\n    best_centers = None\n    best_radii = None\n\n    # coarse grid search\n    for s_h in s_h_vals:\n        for v_ratio in v_ratio_vals:\n            for frac_combo in all_shifts:\n                row_shifts = np.array(frac_combo) * s_h\n                for dx,dy in trans_grid:\n                    centers = build_centers(s_h, v_ratio, row_shifts, dx=dx, dy=dy)\n                    r = compute_radii(centers)\n                    ssum = float(r.sum())\n                    if ssum > best_sum + 1e-12:\n                        best_sum = ssum\n                        best_centers = centers.copy()\n                        best_radii = r.copy()\n                        best_params = (s_h, v_ratio, row_shifts.copy(), dx, dy)\n\n    # if nothing found (shouldn't happen), fallback to a centered modest scale\n    if best_params is None:\n        best_centers = build_centers(0.12, 1.0, np.zeros(len(rows)))\n        best_radii = compute_radii(best_centers)\n        best_sum = float(best_radii.sum())\n        best_params = (0.12, 1.0, np.zeros(len(rows)), 0.0, 0.0)\n\n    # deterministic coordinate-descent refinement around best params\n    cur_s_h, cur_v_ratio, cur_row_shifts, cur_dx, cur_dy = best_params\n    s_h_steps = [0.012, 0.006, 0.003, 0.0015]\n    v_steps = [0.03, 0.015, 0.007, 0.0035]\n    row_steps = [0.03*cur_s_h, 0.015*cur_s_h, 0.007*cur_s_h, 0.003*cur_s_h]\n    trans_steps = [0.004, 0.002, 0.001, 0.0005]\n\n    for level in range(len(s_h_steps)):\n        improved = True\n        it = 0\n        while improved and it < 6:\n            improved = False\n            it += 1\n            # tweak s_h\n            for sign in (-1,1):\n                cand = cur_s_h + sign * s_h_steps[level]\n                if 0.04 < cand < 0.35:\n                    centers = build_centers(cand, cur_v_ratio, cur_row_shifts, cur_dx, cur_dy)\n                    r = compute_radii(centers)\n                    ssum = float(r.sum())\n                    if ssum > best_sum + 1e-12:\n                        best_sum = ssum; best_centers = centers.copy(); best_radii = r.copy()\n                        cur_s_h = cand; improved = True\n            # tweak v_ratio\n            for sign in (-1,1):\n                cand = cur_v_ratio + sign * v_steps[level]\n                if 0.7 < cand < 1.4:\n                    centers = build_centers(cur_s_h, cand, cur_row_shifts, cur_dx, cur_dy)\n                    r = compute_radii(centers)\n                    ssum = float(r.sum())\n                    if ssum > best_sum + 1e-12:\n                        best_sum = ssum; best_centers = centers.copy(); best_radii = r.copy()\n                        cur_v_ratio = cand; improved = True\n            # per-row shifts\n            for ridx in range(len(rows)):\n                for sign in (-1,1):\n                    cand_shifts = cur_row_shifts.copy()\n                    cand_shifts[ridx] += sign * row_steps[level]\n                    centers = build_centers(cur_s_h, cur_v_ratio, cand_shifts, cur_dx, cur_dy)\n                    r = compute_radii(centers)\n                    ssum = float(r.sum())\n                    if ssum > best_sum + 1e-12:\n                        best_sum = ssum; best_centers = centers.copy(); best_radii = r.copy()\n                        cur_row_shifts = cand_shifts; improved = True\n            # translations\n            for tx_mult, ty_mult in product((-1,0,1), repeat=2):\n                if tx_mult==0 and ty_mult==0: continue\n                cand_dx = cur_dx + tx_mult * trans_steps[level]\n                cand_dy = cur_dy + ty_mult * trans_steps[level]\n                centers = build_centers(cur_s_h, cur_v_ratio, cur_row_shifts, cand_dx, cand_dy)\n                r = compute_radii(centers)\n                ssum = float(r.sum())\n                if ssum > best_sum + 1e-12:\n                    best_sum = ssum; best_centers = centers.copy(); best_radii = r.copy()\n                    cur_dx, cur_dy = cand_dx, cand_dy; improved = True\n\n    # final tiny nudges\n    for dx in (-5e-4, 0.0, 5e-4):\n        for dy in (-5e-4, 0.0, 5e-4):\n            centers = build_centers(cur_s_h, cur_v_ratio, cur_row_shifts, cur_dx+dx, cur_dy+dy)\n            r = compute_radii(centers)\n            ssum = float(r.sum())\n            if ssum > best_sum + 1e-12:\n                best_sum = ssum; best_centers = centers.copy(); best_radii = r.copy()\n                cur_dx += dx; cur_dy += dy\n\n    return np.array(best_centers), np.array(best_radii), float(best_radii.sum())\n# EVOLVE-BLOCK-END\n\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\nif __name__ == \"__main__\":\n    c, r, s = run_packing()\n    print(f\"Sum of radii: {s:.6f}\")\n```"]}}, "artifacts_json": "{\"stdout\": \"Running in subprocess, Python version: 3.12.1 (v3.12.1:2305ca5144, Dec  7 2023, 17:23:38) [Clang 13.0.0 (clang-1300.0.29.30)]\\nProgram path: /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmpga_5vn51.py\\nCalling run_packing()...\\nrun_packing() returned successfully: sum_radii = 2.3054999999999994\\nResults saved to /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmpewr0nh16.py.results\\n\", \"stderr\": \"\"}", "artifact_dir": null}