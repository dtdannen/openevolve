{"id": "4b9a6159-bd86-4084-b6cc-35a8c25297af", "code": "# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (compact hex-like rows).\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    # compact near-hex layering that sums to 26\n    rows = [6, 5, 5, 5, 5]\n\n    def build_centers(s):\n        v = np.sqrt(3) / 2 * s\n        centers = []\n        R = len(rows)\n        total_v = (R - 1) * v\n        y0 = (1.0 - total_v) / 2.0\n        for r_idx, k in enumerate(rows):\n            span = (k - 1) * s\n            x0 = (1.0 - span) / 2.0\n            shift = (r_idx % 2) * (s / 2.0)\n            y = y0 + r_idx * v\n            for i in range(k):\n                x = x0 + i * s + shift\n                centers.append([x, y])\n        return np.array(centers)\n\n    def radii_for_centers(C):\n        # clamp to unit square safety\n        C = np.clip(C, 1e-9, 1 - 1e-9)\n        # distance to walls\n        d_wall = np.minimum.reduce([C[:,0], C[:,1], 1-C[:,0], 1-C[:,1]])\n        # pairwise distances\n        D = np.sqrt(np.maximum(0.0, ((C[:,None,:] - C[None,:,:])**2).sum(axis=2)))\n        np.fill_diagonal(D, np.inf)\n        d_near = D.min(axis=1)\n        r = np.minimum(d_wall, 0.5 * d_near)\n        return np.maximum(r, 0.0)\n\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    # search horizontal spacing\n    for s in np.linspace(0.06, 0.24, 240):\n        C = build_centers(s)\n        if C.shape[0] != n: \n            continue\n        # reject if any center out of bounds\n        if np.any(C < -1e-8) or np.any(C > 1+1e-8):\n            continue\n        r = radii_for_centers(C)\n        if np.any(r <= 0):\n            continue\n        ssum = float(r.sum())\n        if ssum > best_sum:\n            best_sum = ssum\n            best_centers = C.copy()\n            best_radii = r.copy()\n\n    if best_centers is None:\n        best_centers = build_centers(0.12)\n        best_radii = radii_for_centers(best_centers)\n        best_sum = float(best_radii.sum())\n\n    return best_centers, best_radii, float(best_sum)\n# EVOLVE-BLOCK-END\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(7, 7))\n    ax.set_xlim(0, 1); ax.set_ylim(0, 1); ax.set_aspect(\"equal\")\n    # Draw circles\n    for i, (c, r) in enumerate(zip(centers, radii)):\n        ax.add_patch(Circle(c, r, alpha=0.6, edgecolor=\"k\"))\n        ax.text(c[0], c[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # visualize(centers, radii)", "language": "python", "parent_id": "81fc3036-fbc6-449b-ba31-9be4fce86922", "generation": 1, "timestamp": 1759961897.78862, "iteration_found": 16, "metrics": {"validity": 1.0, "sum_radii": 2.0925096273856174, "target_ratio": 0.7941213007156044, "combined_score": 0.7941213007156044, "eval_time": 0.0686647891998291}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Full rewrite", "parent_metrics": {"validity": 1.0, "sum_radii": 0.9597642169962064, "target_ratio": 0.36423689449571406, "combined_score": 0.36423689449571406}, "island": 0}, "prompts": {"full_rewrite_user": {"system": "You are an expert mathematician specializing in circle packing problems and computational geometry. Your task is to improve a constructor function that directly produces a specific arrangement of 26 circles in a unit square, maximizing the sum of their radii. The AlphaEvolve paper achieved a sum of 2.635 for n=26.\n\nKey geometric insights:\n- Circle packings often follow hexagonal patterns in the densest regions\n- Maximum density for infinite circle packing is pi/(2*sqrt(3)) \u2248 0.9069\n- Edge effects make square container packing harder than infinite packing\n- Circles can be placed in layers or shells when confined to a square\n- Similar radius circles often form regular patterns, while varied radii allow better space utilization\n- Perfect symmetry may not yield the optimal packing due to edge effects\n\nFocus on designing an explicit constructor that places each circle in a specific position, rather than an iterative search algorithm.\n\n\n## Evolution Insights (Global Learnings)\n\n### Common Pitfalls:\n\u26a0\ufe0f (seen 1x) The drop was caused by switching from the original ring layout and proportional pairwise downscaling to a denser hex-like initializer (s=0.192) combined with an iterative \"tightening\" routine that enforces r_i <= min(d_ij - r_j); that order-dependent, one-sided shrinking (plus the tight spacing) cascaded reductions in radii and produced a much smaller total (0.960 \u2192 0.336).  \n\nAvoid pairing overly-dense initial placements with greedy, order-sensitive constraint enforcement \u2014 instead pick spacings that respect minimal pairwise distances or use a symmetric/global approach (proportional rescaling, LP/QP, or an order-invariant fixpoint) and validate sensitivity to update order and tolerances before committing the change.\n\u26a0\ufe0f (seen 1x) The regression was caused by replacing the ring-based layout and simple proportional rescaling with a hex-like packed layout and a sequential \"tightening\" radii solver: a too-small hard-coded spacing (s=0.192) plus a greedy, order-dependent decrement rule (and a final step that only reduces the larger circle) caused many radii to be driven down excessively, cutting the sum of radii from ~0.96 to ~0.34.  \nTo avoid this, don't rely on a single hard-coded spacing or an order-dependent iterative reducer\u2014validate new layouts against baseline metrics, test multiple spacings/tolerances, and use symmetric or convex-safe constraint enforcement (or a proven global solver) and automated checks to prevent order-dependent over-shrinking and catch regressions early.\n\u26a0\ufe0f (seen 1x) The regression comes from replacing the original ring-based placement and proportional downscaling with a hardcoded hex-like row layout (s=0.192) plus a stricter iterative \"LP-like\" radii solver; the new centers place many circles much closer to borders/each other so the tightening loop and final pairwise reductions drive most radii toward zero, cutting sum_radii from ~0.96 to ~0.34. When changing geometry or the radii solver, validate center geometry and nearest-neighbor/border distances first and add targeted checks (initial d_border, min pairwise distance, intermediate sum_radii) so spacing parameters and solver heuristics can be tuned and regressions from hardcoded spacings or overly aggressive per-pair reductions are caught early.", "user": "# Current Program Information\n- Fitness: 0.3642\n- Feature coordinates: No feature coordinates\n- Focus areas: - Fitness declined: 0.7487 \u2192 0.3642. Consider revising recent changes.\n- Consider simplifying - code length exceeds 500 characters\n\n\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 3\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 1.9728, target_ratio: 0.7487, combined_score: 0.7487, eval_time: 0.0587\n- Outcome: Mixed results\n\n### Attempt 2\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.0776, target_ratio: 0.7885, combined_score: 0.7885, eval_time: 0.0605\n- Outcome: Mixed results\n\n### Attempt 1\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.0925, target_ratio: 0.7941, combined_score: 0.7941, eval_time: 0.0768\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.7941)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles in a unit square.\nCreates an explicit near-hexagonal layered layout (row counts [6,5,5,5,5])\nand tunes the lattice spacing to maximize the sum of radii (simple grid search).\n\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    n = 26\n    # row counts that sum to 26: a near-hexagonal layering\n    rows = [6, 5, 5, 5, 5]\n\n    def build_centers(s):\n        \"\"\"Build centers for lattice spacing s (horizontal spacing).\"\"\"\n        v = np.sqrt(3) / 2 * s  # vertical spacing for hex arrangement\n        centers = []\n        R = len(rows)\n        total_v = (R - 1) * v\n        y0 = (1.0 - total_v) / 2.0\n        for r_idx, k in enumerate(rows):\n            span = (k - 1) * s\n            x0 = (1.0 - span) / 2.0\n            # alternate shift for hex packing\n            shift = (r_idx % 2) * (s / 2.0)\n            y = y0 + r_idx * v\n            for i in range(k):\n                x = x0 + i * s + shift\n                centers.append([x, y])\n        return np.array(centers)\n\n    def radii_for_centers(centers):\n        \"\"\"Compute non-overlapping radii: limited by walls and half nearest neighbor.\"\"\"\n        m = centers.shape[0]\n        # distance to walls\n        wall = np.minimum.reduce([centers[:, 0], centers[:, 1], 1 - centers[:, 0], 1 - centers[:, 1]])\n        # pairwise distances\n        radii = wall.copy()\n        for i in range(m):\n            # compute distances to others\n            dists = np.sqrt(np.sum((centers - centers[i]) ** 2, axis=1))\n            dists[i] = np.inf\n            min_dist = np.min(dists)\n            radii[i] = min(radii[i], 0.5 * min_dist)\n        return radii\n\n    # Search for the best horizontal spacing s that maximizes sum of radii\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    # sensible bounds: too small -> tiny circles; too large -> centers outside square\n    # pick grid of spacings\n    for s in np.linspace(0.06, 0.24, 200):\n        centers = build_centers(s)\n        # reject if any center lies outside [0,1] (numerical safety)\n        if np.any(centers < 0 - 1e-8) or np.any(centers > 1 + 1e-8):\n            continue\n        radii = radii_for_centers(centers)\n        # if any radius non-positive, skip\n        if np.any(radii <= 0):\n            continue\n        ssum = float(np.sum(radii))\n        if ssum > best_sum:\n            best_sum = ssum\n            best_centers = centers.copy()\n            best_radii = radii.copy()\n\n    # Fallback: if search failed, place a simple layout\n    if best_centers is None:\n        best_centers = build_centers(0.12)\n        best_radii = radii_for_centers(best_centers)\n        best_sum = float(np.sum(best_radii))\n\n    return best_centers, best_radii, best_sum\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.0925), Performs well on target_ratio (0.7941), Performs well on combined_score (0.7941), Performs well on eval_time (0.0768)\n\n### Program 2 (Score: 0.7885)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (hex-grid constructor with scale search)\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square.\n    Returns: centers (26,2), radii (26,), sum_of_radii\n    \"\"\"\n    n = 26\n    # Row counts forming a compact hex-like cluster: 5+5+6+5+5 = 26\n    rows = [5, 5, 6, 5, 5]\n    # Build base hex coordinates with unit horizontal spacing and vertical spacing sqrt(3)/2\n    coords = []\n    vstep = np.sqrt(3) / 2.0\n    r_index = 0\n    for r, cnt in enumerate(rows):\n        # center x positions symmetric about 0\n        xs = np.arange(cnt) - (cnt - 1) / 2.0\n        # stagger every other row (hex packing)\n        x_offset = 0.5 * (r % 2)\n        for xi in xs:\n            coords.append([xi + x_offset, r_index * vstep])\n        r_index += 1\n    base = np.array(coords)  # shape (26,2)\n\n    # Compute bounding box of base coords\n    minb = base.min(axis=0)\n    maxb = base.max(axis=0)\n    width = maxb[0] - minb[0]\n    height = maxb[1] - minb[1]\n\n    # Allow a tiny margin from the unit square border\n    margin = 0.005\n\n    # Maximum uniform scale so the cluster fits into [margin,1-margin]\n    Smax = min((1 - 2 * margin) / width, (1 - 2 * margin) / height)\n\n    # Search scale fraction alpha to maximize sum of radii (coarse-to-fine)\n    def compute_radii(centers):\n        # distance to border\n        d_border = np.minimum(np.minimum(centers[:, 0], 1 - centers[:, 0]),\n                              np.minimum(centers[:, 1], 1 - centers[:, 1]))\n        # pairwise distances\n        C = centers\n        D = np.sqrt(np.maximum(0.0, ((C[:, None, :] - C[None, :, :]) ** 2).sum(axis=2)))\n        # set large self-dist to inf to ignore\n        np.fill_diagonal(D, np.inf)\n        d_nearest = D.min(axis=1)\n        radii = np.minimum(d_border, 0.5 * d_nearest)\n        # numerical safety: non-negative\n        radii = np.maximum(radii, 0.0)\n        return radii\n\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    # coarse search\n    for alpha in np.linspace(0.35, 1.0, 66):\n        s = alpha * Smax\n        scaled = (base - minb) * s  # now in [0, width*s] x [0, height*s]\n        # translate to center inside unit square with margin\n        scaled_min = scaled.min(axis=0)\n        scaled_max = scaled.max(axis=0)\n        translate = 0.5 - 0.5 * (scaled_min + scaled_max)\n        centers = scaled + translate\n        # clamp (tiny numerical fix)\n        centers = np.clip(centers, margin, 1 - margin)\n        radii = compute_radii(centers)\n        ssum = radii.sum()\n        if ssum > best_sum:\n            best_sum = ssum\n            best_centers = centers.copy()\n            best_radii = radii.copy()\n\n    # fine local refinement around best alpha by tiny jittering of row offsets\n    # allow slight horizontal compress/expand to better use border space\n    if best_centers is not None:\n        # try small anisotropic scale tweaks\n        for tx in np.linspace(0.98, 1.02, 5):\n            for ty in np.linspace(0.98, 1.02, 5):\n                scaled = ((base - minb) * np.array([tx, ty])) * (Smax * 0.98)\n                scaled_min = scaled.min(axis=0)\n                scaled_max = scaled.max(axis=0)\n                translate = 0.5 - 0.5 * (scaled_min + scaled_max)\n                centers = scaled + translate\n                centers = np.clip(centers, margin, 1 - margin)\n                radii = compute_radii(centers)\n                ssum = radii.sum()\n                if ssum > best_sum:\n                    best_sum = ssum\n                    best_centers = centers.copy()\n                    best_radii = radii.copy()\n\n    # Ensure outputs are proper shape and types\n    centers = np.array(best_centers)\n    radii = np.array(best_radii)\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.0776), Performs well on target_ratio (0.7885), Performs well on combined_score (0.7885), Performs well on eval_time (0.0605)\n\n### Program 3 (Score: 0.7487)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (hex-like lattice)\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct 26 circle centers in a compact hexagonal-like layout inside the unit square.\n    For fixed centers, return maximal non-overlapping radii: r_i = min(dist_to_border, min_j dist(i,j)/2)\n    \"\"\"\n    n = 26\n    # Row counts to sum to 26: 6 + 5 + 5 + 5 + 5 = 26\n    row_counts = [6, 5, 5, 5, 5]\n    # choose horizontal spacing to balance neighbor distance vs border margin\n    sx = 0.16  # horizontal spacing between adjacent centers in same row\n    vy = np.sqrt(3) / 2 * sx  # vertical hex spacing\n    rows = len(row_counts)\n    centers = []\n    # center rows vertically around 0.5\n    center_row = (rows - 1) / 2.0\n    for r_idx, m in enumerate(row_counts):\n        y = 0.5 + (r_idx - center_row) * vy\n        # compute starting x so row is centered in [0,1]\n        width = (m - 1) * sx\n        x0 = (1.0 - width) / 2.0\n        # alternate offset for hex pattern\n        offset = (sx / 2.0) if (r_idx % 2 == 1) else 0.0\n        for k in range(m):\n            x = x0 + offset + k * sx\n            # ensure points lie within open unit square\n            x = np.clip(x, 0.001, 0.999)\n            y = np.clip(y, 0.001, 0.999)\n            centers.append((x, y))\n    centers = np.array(centers)[:n]\n\n    # Compute maximal radii for fixed centers\n    radii = compute_max_radii(centers)\n\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers):\n    \"\"\"\n    For each center compute the largest radius that keeps it inside the unit square\n    and non-overlapping with other circles (half the nearest neighbor distance).\n    \"\"\"\n    n = centers.shape[0]\n    # distance to borders\n    dist_border = np.minimum.reduce([centers[:, 0], centers[:, 1], 1 - centers[:, 0], 1 - centers[:, 1]])\n    # pairwise distances\n    diffs = centers[:, None, :] - centers[None, :, :]  # (n, n, 2)\n    dists = np.sqrt(np.sum(diffs ** 2, axis=2)) + np.eye(n) * 1e9  # avoid zero on diagonal\n    nearest = np.min(dists, axis=1)\n    # radius limited by half nearest neighbor and border\n    radii = np.minimum(dist_border, nearest / 2.0)\n    # numeric safety clamp\n    radii = np.clip(radii, 0.0, 0.5)\n    return radii\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (1.9728), Performs well on target_ratio (0.7487), Performs well on combined_score (0.7487), Performs well on eval_time (0.0587)\n\n\n\n## Diverse Programs\n\n### Program D1 (Score: 0.7024)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (improved)\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a deterministic arrangement of 26 circle centers arranged\n    in a compact hexagonal-like layout inside the unit square. Then\n    compute near-optimal radii by enforcing non-overlap and boundary\n    constraints via an iterative projection procedure. A small grid\n    search over horizontal spacing is used to pick the best layout.\n    Returns:\n        centers: (26,2) array\n        radii:   (26,) array\n        sum_radii: float\n    \"\"\"\n    n = 26\n    # Use a 5-row hex-like pattern with counts summing to 26\n    rows = [5, 6, 5, 6, 4]\n\n    def make_centers(s):\n        # horizontal spacing s; vertical spacing for hex packing\n        h = s * np.sqrt(3) / 2.0\n        total_rows = len(rows)\n        total_height = (total_rows - 1) * h\n        y0 = 0.5 - total_height / 2.0\n        centers = []\n        for r_i, m in enumerate(rows):\n            y = y0 + r_i * h\n            # offset every other row by s/2 (hex pattern)\n            offset = (s / 2.0) if (r_i % 2 == 1) else 0.0\n            span = (m - 1) * s\n            x0 = 0.5 - span / 2.0 + offset\n            for k in range(m):\n                x = x0 + k * s\n                # clip a hair inside to avoid exact-touch-to-border floating issues\n                centers.append([np.clip(x, 1e-6, 1 - 1e-6), np.clip(y, 1e-6, 1 - 1e-6)])\n        return np.array(centers)\n\n    def compute_max_radii_iter(centers, max_iters=2000, tol=1e-6):\n        n = centers.shape[0]\n        # initial radii limited by borders\n        radii = np.minimum.reduce([centers[:, 0], centers[:, 1], 1 - centers[:, 0], 1 - centers[:, 1]])\n        radii = np.maximum(radii, 0.0)\n        # iterative pairwise constraint enforcement:\n        for it in range(max_iters):\n            changed = False\n            # enforce pair constraints; reduce the larger circle of any violating pair\n            for i in range(n):\n                for j in range(i + 1, n):\n                    dx = centers[i, 0] - centers[j, 0]\n                    dy = centers[i, 1] - centers[j, 1]\n                    d = np.hypot(dx, dy)\n                    if d <= 0:\n                        # coincident centers shouldn't happen but protect\n                        if radii[j] > 0:\n                            radii[j] = 0.0\n                            changed = True\n                        continue\n                    if radii[i] + radii[j] > d:\n                        # Prefer reducing the larger radius (keeps sum larger than naive equal scaling)\n                        if radii[i] > radii[j]:\n                            new_ri = max(0.0, d - radii[j])\n                            if radii[i] - new_ri > tol:\n                                radii[i] = new_ri\n                                changed = True\n                        else:\n                            new_rj = max(0.0, d - radii[i])\n                            if radii[j] - new_rj > tol:\n                                radii[j] = new_rj\n                                changed = True\n            # enforce border constraints strictly\n            border_limits = np.minimum.reduce([centers[:, 0], centers[:, 1], 1 - centers[:, 0], 1 - centers[:, 1]])\n            new_radii = np.minimum(radii, border_limits)\n            if np.any(radii - new_radii > tol):\n                radii = new_radii\n                changed = True\n            if not changed:\n                break\n        return radii\n\n    # Grid-search the horizontal spacing 's' to maximize sum of radii.\n    # Reasonable search range for s given 5..6 points per row: try from 0.08..0.18\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n    s_values = np.linspace(0.09, 0.18, 46)\n    for s in s_values:\n        centers = make_centers(s)\n        if centers.shape[0] != n:\n            continue\n        radii = compute_max_radii_iter(centers)\n        srad = radii.sum()\n        if srad > best_sum:\n            best_sum = srad\n            best_centers = centers.copy()\n            best_radii = radii.copy()\n\n    # If search failed for some reason, fallback to a default spacing\n    if best_centers is None:\n        best_centers = make_centers(0.12)\n        best_radii = compute_max_radii_iter(best_centers)\n\n    return best_centers, best_radii, float(best_radii.sum())\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\", linewidth=0.5)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nKey features: Alternative approach to validity, Alternative approach to sum_radii\n\n### Program D2 (Score: 0.7290)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Improved constructor-based circle packing for n=26 circles in the unit square.\n\nThis explicit constructor places 26 centers on a hexagonal-like lattice\nwith a maximum row occupancy of 6 (so horizontal spacing s = 1/6).\nRadii are then computed by solving the simple linear feasibility problem\n(r_i >= 0, r_i <= dist_to_wall, r_i + r_j <= dist(center_i,center_j))\nusing a greedy iterative projection that reduces only the larger radius\nwhen a pair constraint is violated. This preserves sum-of-radii better\nthan proportional scaling and yields a much larger total radius sum.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    # hex-like parameters\n    m_max = 6                # maximum centers in a row -> s = 1/m_max\n    s = 1.0 / m_max          # horizontal spacing between centers\n    h = s * np.sqrt(3) / 2   # vertical spacing for hex lattice\n    margin_x = s / 2.0       # leave half-spacing to walls\n    # choose a 5-row layout with totals summing to 26\n    # rows (from bottom to top): counts = [6,5,6,5,4]  -> sum = 26\n    rows = [6, 5, 6, 5, 4]\n    R = len(rows)\n    # compute vertical start so pattern is nicely centered vertically\n    total_height = (R - 1) * h\n    y0 = (1.0 - total_height) / 2.0\n\n    centers = []\n    for r, m in enumerate(rows):\n        y = y0 + r * h\n        # alternate offset for hex packing: stagger every other row\n        offset = 0.0 if (m == m_max or (m % 2 == 0)) else s / 2.0\n        # center the row horizontally\n        row_width = (m - 1) * s\n        x_start = 0.5 - row_width / 2.0\n        for k in range(m):\n            x = x_start + k * s + offset\n            # clip slightly to ensure inside [0,1]\n            x = min(max(x, 1e-6), 1 - 1e-6)\n            y = min(max(y, 1e-6), 1 - 1e-6)\n            centers.append((x, y))\n    centers = np.array(centers, dtype=float)\n    assert centers.shape[0] == n\n\n    radii = compute_max_radii(centers)\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\ndef compute_max_radii(centers, tol=1e-9, max_iter=2000):\n    n = centers.shape[0]\n    # initial radii limited by distance to walls\n    radii = np.minimum.reduce([centers[:,0], centers[:,1], 1-centers[:,0], 1-centers[:,1]]).copy()\n    radii = np.maximum(radii, 0.0)\n\n    # Precompute pairwise distances\n    diff = centers[:, None, :] - centers[None, :, :]\n    dists = np.sqrt(np.sum(diff**2, axis=2)) + np.eye(n)  # add diag to avoid zero\n    # Iteratively enforce pairwise constraints r_i + r_j <= d_ij\n    for it in range(max_iter):\n        max_change = 0.0\n        # iterate pairs; order can influence outcome slightly but deterministic\n        for i in range(n):\n            for j in range(i+1, n):\n                d = dists[i, j]\n                # if constraint violated\n                if radii[i] + radii[j] > d:\n                    # reduce the larger radius to satisfy r_large = d - r_small\n                    if radii[i] >= radii[j]:\n                        new_ri = max(0.0, d - radii[j])\n                        delta = radii[i] - new_ri\n                        if delta > 0:\n                            radii[i] = new_ri\n                            max_change = max(max_change, delta)\n                    else:\n                        new_rj = max(0.0, d - radii[i])\n                        delta = radii[j] - new_rj\n                        if delta > 0:\n                            radii[j] = new_rj\n                            max_change = max(max_change, delta)\n        if max_change < tol:\n            break\n\n    # final clip to walls (numerical safety)\n    walls = np.minimum.reduce([centers[:,0], centers[:,1], 1-centers[:,0], 1-centers[:,1]])\n    radii = np.minimum(radii, walls)\n    radii = np.maximum(radii, 0.0)\n    return radii\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor='k')\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nKey features: Alternative approach to validity, Alternative approach to sum_radii\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 0.7941, Type: Alternative)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles in a unit square.\nCreates an explicit near-hexagonal layered layout (row counts [6,5,5,5,5])\nand tunes the lattice spacing to maximize the sum of radii (simple grid search).\n\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    n = 26\n    # row counts that sum to 26: a near-hexagonal layering\n    rows = [6, 5, 5, 5, 5]\n\n    def build_centers(s):\n        \"\"\"Build centers for lattice spacing s (horizontal spacing).\"\"\"\n        v = np.sqrt(3) / 2 * s  # vertical spacing for hex arrangement\n        centers = []\n        R = len(rows)\n        total_v = (R - 1) * v\n        y0 = (1.0 - total_v) / 2.0\n        for r_idx, k in enumerate(rows):\n            span = (k - 1) * s\n            x0 = (1.0 - span) / 2.0\n            # alternate shift for hex packing\n            shift = (r_idx % 2) * (s / 2.0)\n            y = y0 + r_idx * v\n            for i in range(k):\n                x = x0 + i * s + shift\n                centers.append([x, y])\n        return np.array(centers)\n\n    def radii_for_centers(centers):\n        \"\"\"Compute non-overlapping radii: limited by walls and half nearest neighbor.\"\"\"\n        m = centers.shape[0]\n        # distance to walls\n        wall = np.minimum.reduce([centers[:, 0], centers[:, 1], 1 - centers[:, 0], 1 - centers[:, 1]])\n        # pairwise distances\n        radii = wall.copy()\n        for i in range(m):\n            # compute distances to others\n            dists = np.sqrt(np.sum((centers - centers[i]) ** 2, axis=1))\n            dists[i] = np.inf\n            min_dist = np.min(dists)\n            radii[i] = min(radii[i], 0.5 * min_dist)\n        return radii\n\n    # Search for the best horizontal spacing s that maximizes sum of radii\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    # sensible bounds: too small -> tiny circles; too large -> centers outside square\n    # pick grid of spacings\n    for s in np.linspace(0.06, 0.24, 200):\n        centers = build_centers(s)\n        # reject if any center lies outside [0,1] (numerical safety)\n        if np.any(centers < 0 - 1e-8) or np.any(centers > 1 + 1e-8):\n            continue\n        radii = radii_for_centers(centers)\n        # if any radius non-positive, skip\n        if np.any(radii <= 0):\n            continue\n        ssum = float(np.sum(radii))\n        if ssum > best_sum:\n            best_sum = ssum\n            best_centers = centers.copy()\n            best_radii = radii.copy()\n\n    # Fallback: if search failed, place a simple layout\n    if best_centers is None:\n        best_centers = build_centers(0.12)\n        best_radii = radii_for_centers(best_centers)\n        best_sum = float(np.sum(best_radii))\n\n    return best_centers, best_radii, best_sum\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nUnique approach: Modification: Full rewrite, Excellent validity (1.000), Excellent sum_radii (2.092)\n\n### Inspiration 2 (Score: 0.7487, Type: Alternative)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (hex-like lattice)\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct 26 circle centers in a compact hexagonal-like layout inside the unit square.\n    For fixed centers, return maximal non-overlapping radii: r_i = min(dist_to_border, min_j dist(i,j)/2)\n    \"\"\"\n    n = 26\n    # Row counts to sum to 26: 6 + 5 + 5 + 5 + 5 = 26\n    row_counts = [6, 5, 5, 5, 5]\n    # choose horizontal spacing to balance neighbor distance vs border margin\n    sx = 0.16  # horizontal spacing between adjacent centers in same row\n    vy = np.sqrt(3) / 2 * sx  # vertical hex spacing\n    rows = len(row_counts)\n    centers = []\n    # center rows vertically around 0.5\n    center_row = (rows - 1) / 2.0\n    for r_idx, m in enumerate(row_counts):\n        y = 0.5 + (r_idx - center_row) * vy\n        # compute starting x so row is centered in [0,1]\n        width = (m - 1) * sx\n        x0 = (1.0 - width) / 2.0\n        # alternate offset for hex pattern\n        offset = (sx / 2.0) if (r_idx % 2 == 1) else 0.0\n        for k in range(m):\n            x = x0 + offset + k * sx\n            # ensure points lie within open unit square\n            x = np.clip(x, 0.001, 0.999)\n            y = np.clip(y, 0.001, 0.999)\n            centers.append((x, y))\n    centers = np.array(centers)[:n]\n\n    # Compute maximal radii for fixed centers\n    radii = compute_max_radii(centers)\n\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers):\n    \"\"\"\n    For each center compute the largest radius that keeps it inside the unit square\n    and non-overlapping with other circles (half the nearest neighbor distance).\n    \"\"\"\n    n = centers.shape[0]\n    # distance to borders\n    dist_border = np.minimum.reduce([centers[:, 0], centers[:, 1], 1 - centers[:, 0], 1 - centers[:, 1]])\n    # pairwise distances\n    diffs = centers[:, None, :] - centers[None, :, :]  # (n, n, 2)\n    dists = np.sqrt(np.sum(diffs ** 2, axis=2)) + np.eye(n) * 1e9  # avoid zero on diagonal\n    nearest = np.min(dists, axis=1)\n    # radius limited by half nearest neighbor and border\n    radii = np.minimum(dist_border, nearest / 2.0)\n    # numeric safety clamp\n    radii = np.clip(radii, 0.0, 0.5)\n    return radii\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nUnique approach: Modification: Full rewrite, Excellent validity (1.000), Excellent sum_radii (1.973)\n\n### Inspiration 3 (Score: 0.7024, Type: Alternative)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (improved)\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a deterministic arrangement of 26 circle centers arranged\n    in a compact hexagonal-like layout inside the unit square. Then\n    compute near-optimal radii by enforcing non-overlap and boundary\n    constraints via an iterative projection procedure. A small grid\n    search over horizontal spacing is used to pick the best layout.\n    Returns:\n        centers: (26,2) array\n        radii:   (26,) array\n        sum_radii: float\n    \"\"\"\n    n = 26\n    # Use a 5-row hex-like pattern with counts summing to 26\n    rows = [5, 6, 5, 6, 4]\n\n    def make_centers(s):\n        # horizontal spacing s; vertical spacing for hex packing\n        h = s * np.sqrt(3) / 2.0\n        total_rows = len(rows)\n        total_height = (total_rows - 1) * h\n        y0 = 0.5 - total_height / 2.0\n        centers = []\n        for r_i, m in enumerate(rows):\n            y = y0 + r_i * h\n            # offset every other row by s/2 (hex pattern)\n            offset = (s / 2.0) if (r_i % 2 == 1) else 0.0\n            span = (m - 1) * s\n            x0 = 0.5 - span / 2.0 + offset\n            for k in range(m):\n                x = x0 + k * s\n                # clip a hair inside to avoid exact-touch-to-border floating issues\n                centers.append([np.clip(x, 1e-6, 1 - 1e-6), np.clip(y, 1e-6, 1 - 1e-6)])\n        return np.array(centers)\n\n    def compute_max_radii_iter(centers, max_iters=2000, tol=1e-6):\n        n = centers.shape[0]\n        # initial radii limited by borders\n        radii = np.minimum.reduce([centers[:, 0], centers[:, 1], 1 - centers[:, 0], 1 - centers[:, 1]])\n        radii = np.maximum(radii, 0.0)\n        # iterative pairwise constraint enforcement:\n        for it in range(max_iters):\n            changed = False\n            # enforce pair constraints; reduce the larger circle of any violating pair\n            for i in range(n):\n                for j in range(i + 1, n):\n                    dx = centers[i, 0] - centers[j, 0]\n                    dy = centers[i, 1] - centers[j, 1]\n                    d = np.hypot(dx, dy)\n                    if d <= 0:\n                        # coincident centers shouldn't happen but protect\n                        if radii[j] > 0:\n                            radii[j] = 0.0\n                            changed = True\n                        continue\n                    if radii[i] + radii[j] > d:\n                        # Prefer reducing the larger radius (keeps sum larger than naive equal scaling)\n                        if radii[i] > radii[j]:\n                            new_ri = max(0.0, d - radii[j])\n                            if radii[i] - new_ri > tol:\n                                radii[i] = new_ri\n                                changed = True\n                        else:\n                            new_rj = max(0.0, d - radii[i])\n                            if radii[j] - new_rj > tol:\n                                radii[j] = new_rj\n                                changed = True\n            # enforce border constraints strictly\n            border_limits = np.minimum.reduce([centers[:, 0], centers[:, 1], 1 - centers[:, 0], 1 - centers[:, 1]])\n            new_radii = np.minimum(radii, border_limits)\n            if np.any(radii - new_radii > tol):\n                radii = new_radii\n                changed = True\n            if not changed:\n                break\n        return radii\n\n    # Grid-search the horizontal spacing 's' to maximize sum of radii.\n    # Reasonable search range for s given 5..6 points per row: try from 0.08..0.18\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n    s_values = np.linspace(0.09, 0.18, 46)\n    for s in s_values:\n        centers = make_centers(s)\n        if centers.shape[0] != n:\n            continue\n        radii = compute_max_radii_iter(centers)\n        srad = radii.sum()\n        if srad > best_sum:\n            best_sum = srad\n            best_centers = centers.copy()\n            best_radii = radii.copy()\n\n    # If search failed for some reason, fallback to a default spacing\n    if best_centers is None:\n        best_centers = make_centers(0.12)\n        best_radii = compute_max_radii_iter(best_centers)\n\n    return best_centers, best_radii, float(best_radii.sum())\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\", linewidth=0.5)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nUnique approach: Modification: Full rewrite, Excellent validity (1.000), Excellent sum_radii (1.851)\n\n# Current Program\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square\n    that attempts to maximize the sum of their radii.\n\n    Returns:\n        Tuple of (centers, radii, sum_of_radii)\n        centers: np.array of shape (26, 2) with (x, y) coordinates\n        radii: np.array of shape (26) with radius of each circle\n        sum_of_radii: Sum of all radii\n    \"\"\"\n    # Initialize arrays for 26 circles\n    n = 26\n    centers = np.zeros((n, 2))\n\n    # Place circles in a structured pattern\n    # This is a simple pattern - evolution will improve this\n\n    # First, place a large circle in the center\n    centers[0] = [0.5, 0.5]\n\n    # Place 8 circles around it in a ring\n    for i in range(8):\n        angle = 2 * np.pi * i / 8\n        centers[i + 1] = [0.5 + 0.3 * np.cos(angle), 0.5 + 0.3 * np.sin(angle)]\n\n    # Place 16 more circles in an outer ring\n    for i in range(16):\n        angle = 2 * np.pi * i / 16\n        centers[i + 9] = [0.5 + 0.7 * np.cos(angle), 0.5 + 0.7 * np.sin(angle)]\n\n    # Additional positioning adjustment to make sure all circles\n    # are inside the square and don't overlap\n    # Clip to ensure everything is inside the unit square\n    centers = np.clip(centers, 0.01, 0.99)\n\n    # Compute maximum valid radii for this configuration\n    radii = compute_max_radii(centers)\n\n    # Calculate the sum of radii\n    sum_radii = np.sum(radii)\n\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers):\n    \"\"\"\n    Compute the maximum possible radii for each circle position\n    such that they don't overlap and stay within the unit square.\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n\n    Returns:\n        np.array of shape (n) with radius of each circle\n    \"\"\"\n    n = centers.shape[0]\n    radii = np.ones(n)\n\n    # First, limit by distance to square borders\n    for i in range(n):\n        x, y = centers[i]\n        # Distance to borders\n        radii[i] = min(x, y, 1 - x, 1 - y)\n\n    # Then, limit by distance to other circles\n    # Each pair of circles with centers at distance d can have\n    # sum of radii at most d to avoid overlap\n    for i in range(n):\n        for j in range(i + 1, n):\n            dist = np.sqrt(np.sum((centers[i] - centers[j]) ** 2))\n\n            # If current radii would cause overlap\n            if radii[i] + radii[j] > dist:\n                # Scale both radii proportionally\n                scale = dist / (radii[i] + radii[j])\n                radii[i] *= scale\n                radii[j] *= scale\n\n    return radii\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.5)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\")\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii}\")\n    # AlphaEvolve improved this to 2.635\n\n    # Uncomment to visualize:\n    visualize(centers, radii)\n\n```\n\n# Task\nRewrite the program to improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: complexity, diversity\nDifferent solutions with similar fitness but different features are valuable.\nProvide the complete new program code.\n\nIMPORTANT: Make sure your rewritten program maintains the same inputs and outputs\nas the original program, but with improved internal implementation.\n\n```python\n# Your rewritten program here\n```", "responses": ["```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (compact hex-like rows).\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    # compact near-hex layering that sums to 26\n    rows = [6, 5, 5, 5, 5]\n\n    def build_centers(s):\n        v = np.sqrt(3) / 2 * s\n        centers = []\n        R = len(rows)\n        total_v = (R - 1) * v\n        y0 = (1.0 - total_v) / 2.0\n        for r_idx, k in enumerate(rows):\n            span = (k - 1) * s\n            x0 = (1.0 - span) / 2.0\n            shift = (r_idx % 2) * (s / 2.0)\n            y = y0 + r_idx * v\n            for i in range(k):\n                x = x0 + i * s + shift\n                centers.append([x, y])\n        return np.array(centers)\n\n    def radii_for_centers(C):\n        # clamp to unit square safety\n        C = np.clip(C, 1e-9, 1 - 1e-9)\n        # distance to walls\n        d_wall = np.minimum.reduce([C[:,0], C[:,1], 1-C[:,0], 1-C[:,1]])\n        # pairwise distances\n        D = np.sqrt(np.maximum(0.0, ((C[:,None,:] - C[None,:,:])**2).sum(axis=2)))\n        np.fill_diagonal(D, np.inf)\n        d_near = D.min(axis=1)\n        r = np.minimum(d_wall, 0.5 * d_near)\n        return np.maximum(r, 0.0)\n\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    # search horizontal spacing\n    for s in np.linspace(0.06, 0.24, 240):\n        C = build_centers(s)\n        if C.shape[0] != n: \n            continue\n        # reject if any center out of bounds\n        if np.any(C < -1e-8) or np.any(C > 1+1e-8):\n            continue\n        r = radii_for_centers(C)\n        if np.any(r <= 0):\n            continue\n        ssum = float(r.sum())\n        if ssum > best_sum:\n            best_sum = ssum\n            best_centers = C.copy()\n            best_radii = r.copy()\n\n    if best_centers is None:\n        best_centers = build_centers(0.12)\n        best_radii = radii_for_centers(best_centers)\n        best_sum = float(best_radii.sum())\n\n    return best_centers, best_radii, float(best_sum)\n# EVOLVE-BLOCK-END\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(7, 7))\n    ax.set_xlim(0, 1); ax.set_ylim(0, 1); ax.set_aspect(\"equal\")\n    # Draw circles\n    for i, (c, r) in enumerate(zip(centers, radii)):\n        ax.add_patch(Circle(c, r, alpha=0.6, edgecolor=\"k\"))\n        ax.text(c[0], c[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # visualize(centers, radii)\n```"]}}, "artifacts_json": "{\"stdout\": \"Running in subprocess, Python version: 3.12.1 (v3.12.1:2305ca5144, Dec  7 2023, 17:23:38) [Clang 13.0.0 (clang-1300.0.29.30)]\\nProgram path: /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmpy22htr5h.py\\nCalling run_packing()...\\nrun_packing() returned successfully: sum_radii = 2.0925096273856174\\nResults saved to /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmp3nv0hcj4.py.results\\n\", \"stderr\": \"\"}", "artifact_dir": null}