{"id": "86147b1d-ecba-4a64-89df-bd828869305c", "code": "# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for 26 circles in the unit square.\nImproved explicit pattern: compact near-hex cluster (rows 5+5+6+5+5),\ncoarse uniform scale sweep to find a tight fit, followed by small\nanisotropic, per-row-shift and translation refinements. Radii are\nassigned as r_i = min(distance-to-border, 0.5*nearest-center-distance).\nThis keeps the constructor simple, deterministic and fast.\n\"\"\"\nimport numpy as np\nfrom itertools import product\n\ndef construct_packing():\n    n = 26\n    # Use the high-performing row pattern seen in top solutions\n    rows = [5, 5, 6, 5, 5]  # sums to 26\n    vstep = np.sqrt(3.0) / 2.0\n\n    # Build a canonical unit-spaced hex-like base layout (not yet scaled)\n    base = []\n    for r_idx, cnt in enumerate(rows):\n        xs = np.arange(cnt) - (cnt - 1) / 2.0\n        x_offset = 0.5 * (r_idx % 2)\n        y = r_idx * vstep\n        for x in xs:\n            base.append([x + x_offset, y])\n    base = np.array(base)  # (26,2)\n\n    # compute bounding extents of base\n    minb = base.min(axis=0)\n    maxb = base.max(axis=0)\n    width = maxb[0] - minb[0]\n    height = maxb[1] - minb[1]\n\n    margin = 1e-6  # tiny margin to avoid exact border-touch\n    # maximum uniform scale to ensure the pattern can fit inside the unit square\n    Smax = min((1 - 2 * margin) / width, (1 - 2 * margin) / height)\n\n    def compute_radii(centers):\n        # distance to border\n        db = np.minimum.reduce([centers[:, 0], centers[:, 1], 1 - centers[:, 0], 1 - centers[:, 1]])\n        # pairwise distances (vectorized)\n        dif = centers[:, None, :] - centers[None, :, :]\n        D = np.sqrt(np.maximum(0.0, (dif ** 2).sum(axis=2)))\n        np.fill_diagonal(D, np.inf)\n        nearest = D.min(axis=1)\n        radii = np.minimum(db, 0.5 * nearest)\n        # ensure non-negative\n        return np.maximum(radii, 0.0)\n\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    # Coarse uniform scale sweep (explore slightly beyond Smax with clipping)\n    for alpha in np.linspace(0.45, 1.02, 61):\n        s = alpha * Smax\n        # scale base and center it in unit square\n        scaled = (base - minb) * s\n        lo = scaled.min(axis=0); hi = scaled.max(axis=0)\n        trans = 0.5 - 0.5 * (lo + hi)\n        centers = scaled + trans\n        centers = np.clip(centers, margin, 1 - margin)\n        r = compute_radii(centers)\n        ssum = float(r.sum())\n        if ssum > best_sum:\n            best_sum = ssum\n            best_centers = centers.copy()\n            best_radii = r.copy()\n\n    # Local refinements: anisotropic scaling, small per-row horizontal shifts,\n    # and tiny translations to let the cluster \"snug\" against edges.\n    if best_centers is None:\n        # fallback\n        best_centers = np.clip((base - minb) * (0.14) + 0.5, margin, 1 - margin)\n        best_radii = compute_radii(best_centers)\n        best_sum = float(best_radii.sum())\n\n    # Prepare per-row indices for applying row shifts\n    row_indices = []\n    idx = 0\n    for cnt in rows:\n        row_indices.append(list(range(idx, idx + cnt)))\n        idx += cnt\n\n    # small sets of anisotropic multipliers (around 1.0) and shift fractions\n    tx_vals = [0.997, 1.0, 1.003]\n    ty_vals = [0.997, 1.0, 1.003]\n    shift_fracs = [-0.035, 0.0, 0.035]  # fractions of Smax for horizontal shifts\n    translations = [(-0.002, 0.0), (0.0, -0.002), (0.0, 0.0), (0.002, 0.0), (0.0, 0.002)]\n\n    base_coords = base.copy()\n    # We'll apply anisotropic scaling relative to the canonical base bounding box\n    for tx, ty in product(tx_vals, ty_vals):\n        sx = tx * Smax\n        sy = ty * Smax * (vstep) / (vstep)  # keep consistent vertical unit (explicit)\n        # scale base coordinates\n        scaled = base_coords.copy()\n        scaled[:, 0] = (scaled[:, 0] - minb[0]) * sx\n        scaled[:, 1] = (scaled[:, 1] - minb[1]) * sy / vstep\n        # try per-row horizontal shifts combinations (3^5 = 243)\n        for shifts in product(shift_fracs, repeat=len(rows)):\n            centers = scaled.copy()\n            for r, frac in enumerate(shifts):\n                inds = row_indices[r]\n                centers[inds, 0] += frac * Smax\n            # center and try small translations\n            lo = centers.min(axis=0); hi = centers.max(axis=0)\n            trans = 0.5 - 0.5 * (lo + hi)\n            centers = centers + trans\n            for dx, dy in translations:\n                C = np.clip(centers + np.array([dx, dy]), margin, 1 - margin)\n                r = compute_radii(C)\n                ssum = float(r.sum())\n                if ssum > best_sum:\n                    best_sum = ssum\n                    best_centers = C.copy()\n                    best_radii = r.copy()\n\n    # Final tiny nudges along each axis (deterministic small moves)\n    for dx in (-1e-3, 0.0, 1e-3):\n        for dy in (-1e-3, 0.0, 1e-3):\n            C = np.clip(best_centers + np.array([dx, dy]), margin, 1 - margin)\n            r = compute_radii(C)\n            ssum = float(r.sum())\n            if ssum > best_sum:\n                best_sum = ssum\n                best_centers = C.copy()\n                best_radii = r.copy()\n\n    return np.array(best_centers), np.array(best_radii), float(best_radii.sum())\n\n# EVOLVE-BLOCK-END\n\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(7,7))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect('equal')\n    for i,(c,r) in enumerate(zip(centers, radii)):\n        ax.add_patch(Circle(c,r,alpha=0.6,edgecolor='k'))\n        ax.text(c[0],c[1],str(i),ha='center',va='center',fontsize=8)\n    plt.title(f\"n={len(centers)}, sum={sum(radii):.6f}\"); plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # visualize(centers, radii)", "language": "python", "parent_id": "0b0bf1bc-3c88-4016-bddb-355371d586f4", "generation": 3, "timestamp": 1759962059.69223, "iteration_found": 32, "metrics": {"validity": 1.0, "sum_radii": 2.2321515406999994, "target_ratio": 0.8471163342314989, "combined_score": 0.8471163342314989, "eval_time": 0.35547399520874023}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Full rewrite", "parent_metrics": {"validity": 1.0, "sum_radii": 2.125958303070675, "target_ratio": 0.8068152952829887, "combined_score": 0.8068152952829887, "eval_time": 0.0702967643737793}, "island": 0}, "prompts": {"full_rewrite_user": {"system": "You are an expert mathematician specializing in circle packing problems and computational geometry. Your task is to improve a constructor function that directly produces a specific arrangement of 26 circles in a unit square, maximizing the sum of their radii. The AlphaEvolve paper achieved a sum of 2.635 for n=26.\n\nKey geometric insights:\n- Circle packings often follow hexagonal patterns in the densest regions\n- Maximum density for infinite circle packing is pi/(2*sqrt(3)) \u2248 0.9069\n- Edge effects make square container packing harder than infinite packing\n- Circles can be placed in layers or shells when confined to a square\n- Similar radius circles often form regular patterns, while varied radii allow better space utilization\n- Perfect symmetry may not yield the optimal packing due to edge effects\n\nFocus on designing an explicit constructor that places each circle in a specific position, rather than an iterative search algorithm.\n\n\n## Evolution Insights (Global Learnings)\n\n### Common Pitfalls:\n\u26a0\ufe0f (seen 4x) The child rewrite removed the iterative pairwise radius-reduction and explicit border-enforcement loop and instead computes a dense pairwise distance matrix via vectorized broadcasting, takes nearest-neighbor distance/2 and min with border distance to set radii, while also changing the row pattern and spacing search; this eliminated costly Python loops (and iterative convergence) so eval_time dropped and the computed sum of radii increased.  \nAvoid substituting a single-shot nearest-neighbor/half-distance heuristic for iterative constraint solving because it can be overly optimistic (misses multi-way overlap interactions), incurs O(n^2) memory/time for dense distance matrices as n grows, and requires careful handling of diagonal offsets and clipping to avoid numeric/degenerate cases.\n\u26a0\ufe0f (seen 3x) The regression was caused by replacing the original ring-based placement and proportional downscaling with a dense hex-like layered layout (s=0.192) plus an LP-like iterative tightening that enforces r_i \u2264 min(border_i, min_j(d_ij \u2212 r_j)) and breaks overlaps by reducing the larger radius; that layout produced many very small inter-center and border distances so the tightening collapsed many radii, dropping sum_radii from ~0.96 to ~0.34.  \nWhen making similar changes, avoid fixed spacings/layouts that create infeasible small distances without first checking feasible radii, and be wary of greedy one-sided tightening rules\u2014add pre-checks (min pairwise distance vs desired radii), unit tests for sum-of-radii expectations, or use global/proportional constraint solvers to prevent asymmetric collapse.\n\u26a0\ufe0f (seen 3x) You replaced the original ring-based layout and proportional pairwise rescaling with a dense hex-like layered layout (rows=[5,5,6,5,5], s=0.192) and an LP-like iterative tightening radii solver that only decreases radii; the new geometry + solver combination produced much smaller pairwise distances so the iterative clamp drove many radii toward zero and the total sum dropped ~65%. When changing packing geometry or the constraint solver, re-tune spacing/row layout and verify pairwise distances (and sum_radii) before committing\u2014add regression tests, visualize configurations, and parameterize or add safeguards in the solver so it cannot monotonically collapse radii without detection.\n\u26a0\ufe0f (seen 3x) The regression was caused by replacing the original ring layout and proportional pairwise scaling with a denser hex-like row layout (s=0.192) combined with a greedy, monotone \"iterative tightening\" radii solver; the new center configuration yields many small inter-center distances and the order-dependent decrease + final \u201creduce the larger one\u201d fixstep aggressively shrinks radii, dropping the total sum substantially.  \n\nAvoid changing both the packing geometry and the constraint-enforcement strategy at once \u2014 when you alter spacing or the radii algorithm, first inspect pairwise distances, run small diagnostic/visual tests, and prefer order-independent approaches (LP solver, symmetric scaling, or provably convergent methods) or add unit tests to detect order-dependent over-shrinking.", "user": "# Current Program Information\n- Fitness: 0.8068\n- Feature coordinates: No feature coordinates\n- Focus areas: - Fitness declined: 0.8073 \u2192 0.8068. Consider revising recent changes.\n- Consider simplifying - code length exceeds 500 characters\n\n## Last Execution Output\n\n### stdout\n```\nRunning in subprocess, Python version: 3.12.1 (v3.12.1:2305ca5144, Dec  7 2023, 17:23:38) [Clang 13.0.0 (clang-1300.0.29.30)]\nProgram path: /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmppqk172aa.py\nCalling run_packing()...\nrun_packing() returned successfully: sum_radii = 2.125958303070675\nResults saved to /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmpttetuqpc.py.results\n\n```\n\n### stderr\n```\n\n```\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 3\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.1272, target_ratio: 0.8073, combined_score: 0.8073, eval_time: 2.1832\n- Outcome: Mixed results\n\n### Attempt 2\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.2240, target_ratio: 0.8440, combined_score: 0.8440, eval_time: 0.1421\n- Outcome: Mixed results\n\n### Attempt 1\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.2247, target_ratio: 0.8443, combined_score: 0.8443, eval_time: 0.1479\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.8443)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (deterministic hex cluster + staged refinement)\"\"\"\nimport numpy as np\nfrom itertools import product\n\n\ndef construct_packing():\n    \"\"\"\n    Build a deterministic layout of 26 circle centers based on a compact\n    hex-like cluster (rows 5+5+6+5+5). Use a coarse uniform scale search\n    to find a good global packing, then refine with small anisotropic\n    tweaks and per-row horizontal shifts to break perfect symmetry.\n    Returns centers (26,2), radii (26,), sum_radii\n    \"\"\"\n    n = 26\n    # Compact hex-like row counts summing to 26\n    rows = [5, 5, 6, 5, 5]\n\n    # Base integer layout coordinates (unit horizontal spacing)\n    base = []\n    vstep = np.sqrt(3) / 2.0  # hex vertical step for unit horiz spacing\n    for r_idx, cnt in enumerate(rows):\n        xs = np.arange(cnt) - (cnt - 1) / 2.0\n        x_offset = 0.5 * (r_idx % 2)\n        for xi in xs:\n            base.append([xi + x_offset, r_idx * vstep])\n    base = np.array(base)  # (26,2)\n\n    # bounding box of base pattern\n    minb = base.min(axis=0)\n    maxb = base.max(axis=0)\n    width = maxb[0] - minb[0]\n    height = maxb[1] - minb[1]\n\n    # small margin to avoid numerical border-touch\n    margin = 1e-4\n    # maximum uniform scale to fit inside [margin,1-margin]\n    Smax = min((1 - 2 * margin) / width, (1 - 2 * margin) / height)\n\n    def compute_radii(centers):\n        # distance to border\n        d_border = np.minimum.reduce([centers[:, 0], centers[:, 1], 1 - centers[:, 0], 1 - centers[:, 1]])\n        # pairwise distances\n        C = centers\n        dif = C[:, None, :] - C[None, :, :]\n        D = np.sqrt(np.maximum(0.0, (dif ** 2).sum(axis=2)))\n        np.fill_diagonal(D, np.inf)\n        nearest = D.min(axis=1)\n        radii = np.minimum(d_border, 0.5 * nearest)\n        radii = np.maximum(radii, 0.0)\n        return radii\n\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n    best_s = None\n\n    # Coarse uniform scale sweep\n    for alpha in np.linspace(0.40, 1.00, 61):\n        s = alpha * Smax\n        scaled = (base - minb) * s\n        # center cluster inside unit square\n        scaled_min = scaled.min(axis=0)\n        scaled_max = scaled.max(axis=0)\n        translate = 0.5 - 0.5 * (scaled_min + scaled_max)\n        centers = scaled + translate\n        centers = np.clip(centers, margin, 1 - margin)\n        radii = compute_radii(centers)\n        ssum = radii.sum()\n        if ssum > best_sum:\n            best_sum = ssum\n            best_centers = centers.copy()\n            best_radii = radii.copy()\n            best_s = s\n\n    # Refinement: small anisotropic scaling around best_s and per-row horizontal shifts\n    # Per-row shifts (in units of current spacing s) to break symmetry; try -0.03,0,0.03 fractions\n    shift_fracs = [-0.03, 0.0, 0.03]\n    tx_vals = [0.996, 1.0, 1.004]\n    ty_vals = [0.996, 1.0, 1.004]\n\n    # Precompute base x positions per row for convenience\n    row_indices = []\n    idx = 0\n    for cnt in rows:\n        row_indices.append(list(range(idx, idx + cnt)))\n        idx += cnt\n    # horizontal spacing in base units is 1.0, so shift in absolute units = frac * best_s\n    for tx, ty in product(tx_vals, ty_vals):\n        s_x = best_s * tx\n        s_y = best_s * ty * (np.sqrt(3) / 2.0) / (np.sqrt(3) / 2.0)  # keep vertical ratio consistent\n        # build scaled base with these anisotropic scales\n        anis_base = base.copy()\n        # apply anisotropic scale: scale x by (s_x), y by (s_y / vstep) so that unit vertical spacing becomes s_y\n        anis_base[:, 0] = (anis_base[:, 0] - minb[0]) * (s_x)\n        anis_base[:, 1] = (anis_base[:, 1] - minb[1]) * (s_y / vstep)\n        # try all combinations of small per-row horizontal shifts (3^5 = 243)\n        for shifts in product(shift_fracs, repeat=len(rows)):\n            centers = anis_base.copy()\n            # apply per-row x shift to each member in row (shift is fraction of best_s)\n            for r, shift_frac in enumerate(shifts):\n                inds = row_indices[r]\n                centers[inds, 0] += shift_frac * best_s\n            # center into unit square\n            cmin = centers.min(axis=0)\n            cmax = centers.max(axis=0)\n            translate = 0.5 - 0.5 * (cmin + cmax)\n            centers = centers + translate\n            centers = np.clip(centers, margin, 1 - margin)\n            radii = compute_radii(centers)\n            ssum = radii.sum()\n            if ssum > best_sum:\n                best_sum = ssum\n                best_centers = centers.copy()\n                best_radii = radii.copy()\n\n    # As a final small local tweak, try translating the whole pattern to snug against edges slightly\n    # (keeps deterministic)\n    for dx, dy in [(0.0, 0.0), (-1e-3, 0.0), (1e-3, 0.0), (0.0, -1e-3), (0.0, 1e-3)]:\n        centers = np.clip(best_centers + np.array([dx, dy]), margin, 1 - margin)\n        radii = compute_radii(centers)\n        ssum = radii.sum()\n        if ssum > best_sum:\n            best_sum = ssum\n            best_centers = centers.copy()\n            best_radii = radii.copy()\n\n    return np.array(best_centers), np.array(best_radii), float(best_radii.sum())\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\", linewidth=0.6)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.2247), Performs well on target_ratio (0.8443), Performs well on combined_score (0.8443), Performs well on eval_time (0.1479)\n\n### Program 2 (Score: 0.8440)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for n=26 circle packing in the unit square.\nUses a compact near-hexagonal layout (rows 5+5+6+5+5), a scale sweep to\nmaximize sum of radii (border-limited and half-nearest-neighbor), and\nsmall anisotropic + per-row shift refinements.\n\"\"\"\nimport numpy as np\nfrom itertools import product\n\ndef construct_packing():\n    n = 26\n    rows = [5, 5, 6, 5, 5]  # sums to 26\n    vstep = np.sqrt(3) / 2.0\n    # build base integer-layout coords (unit horiz spacing)\n    base = []\n    for r_idx, cnt in enumerate(rows):\n        xs = np.arange(cnt) - (cnt - 1) / 2.0\n        x_offset = 0.5 * (r_idx % 2)\n        for xi in xs:\n            base.append([xi + x_offset, r_idx * vstep])\n    base = np.array(base)  # (26,2)\n\n    minb = base.min(axis=0)\n    maxb = base.max(axis=0)\n    width = maxb[0] - minb[0]\n    height = maxb[1] - minb[1]\n    margin = 1e-6\n    Smax = min((1 - 2 * margin) / width, (1 - 2 * margin) / height)\n\n    def radii_for_centers(C):\n        # distance to border\n        dborder = np.minimum.reduce([C[:,0], C[:,1], 1 - C[:,0], 1 - C[:,1]])\n        dif = C[:, None, :] - C[None, :, :]\n        D = np.sqrt(np.maximum(0.0, (dif**2).sum(axis=2)))\n        np.fill_diagonal(D, np.inf)\n        nearest = D.min(axis=1)\n        r = np.minimum(dborder, 0.5 * nearest)\n        return np.maximum(r, 0.0)\n\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    # coarse uniform scale sweep\n    for alpha in np.linspace(0.40, 1.00, 61):\n        s = alpha * Smax\n        scaled = (base - minb) * s\n        # center\n        lo = scaled.min(axis=0); hi = scaled.max(axis=0)\n        trans = 0.5 - 0.5 * (lo + hi)\n        centers = scaled + trans\n        centers = np.clip(centers, margin, 1 - margin)\n        radii = radii_for_centers(centers)\n        ssum = radii.sum()\n        if ssum > best_sum:\n            best_sum = ssum; best_centers = centers.copy(); best_radii = radii.copy()\n\n    # small anisotropic and per-row shifts refinement\n    if best_centers is None:\n        best_centers = (base - minb) * (0.12) + np.array([0.5, 0.5])\n        best_radii = radii_for_centers(best_centers)\n        best_sum = best_radii.sum()\n\n    # row indices for shifts\n    row_indices = []\n    idx = 0\n    for cnt in rows:\n        row_indices.append(list(range(idx, idx + cnt)))\n        idx += cnt\n\n    # try a few tiny anisotropic scalings and row shifts\n    tx_vals = [0.998, 1.0, 1.002]\n    ty_vals = [0.998, 1.0, 1.002]\n    shift_fracs = [-0.03, 0.0, 0.03]  # fraction of best scale\n    for tx, ty in product(tx_vals, ty_vals):\n        s_x = Smax * tx\n        s_y = Smax * ty\n        anis = base.copy()\n        anis[:,0] = (anis[:,0] - minb[0]) * s_x\n        anis[:,1] = (anis[:,1] - minb[1]) * s_y / vstep  # keep vertical units consistent\n        for shifts in product(shift_fracs, repeat=len(rows)):\n            centers = anis.copy()\n            for r, sf in enumerate(shifts):\n                inds = row_indices[r]\n                centers[inds,0] += sf * (Smax)\n            lo = centers.min(axis=0); hi = centers.max(axis=0)\n            trans = 0.5 - 0.5 * (lo + hi)\n            centers = np.clip(centers + trans, margin, 1 - margin)\n            radii = radii_for_centers(centers)\n            ssum = radii.sum()\n            if ssum > best_sum:\n                best_sum = ssum; best_centers = centers.copy(); best_radii = radii.copy()\n\n    return np.array(best_centers), np.array(best_radii), float(best_radii.sum())\n# EVOLVE-BLOCK-END\n\n# fixed runtime interface\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(7,7))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect('equal')\n    for i,(c,r) in enumerate(zip(centers,radii)):\n        ax.add_patch(Circle(c,r,alpha=0.6,edgecolor='k'))\n        ax.text(c[0], c[1], str(i), ha='center', va='center', fontsize=8)\n    plt.title(f\"n={len(centers)}, sum={sum(radii):.6f}\"); plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # visualize(centers, radii)\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.2240), Performs well on target_ratio (0.8440), Performs well on combined_score (0.8440), Performs well on eval_time (0.1421)\n\n### Program 3 (Score: 0.8073)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for packing 26 circles in the unit square.\nBuilds a compact near-hexagonal cluster (rows [6,5,5,5,5]) and searches\nover horizontal spacing, small anisotropic scale tweaks and tiny translations.\nRadii are computed as min(distance-to-border, half nearest-neighbor) (order-invariant).\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    rows = [6, 5, 5, 5, 5]  # compact hex-like layering summing to 26\n\n    def base_centers(s):\n        v = np.sqrt(3.0) / 2.0 * s\n        R = len(rows)\n        total_v = (R - 1) * v\n        y0 = (1.0 - total_v) / 2.0\n        C = []\n        for r_idx, k in enumerate(rows):\n            span = (k - 1) * s\n            # center horizontally\n            x0 = (1.0 - span) / 2.0\n            shift = (r_idx % 2) * (s / 2.0)\n            y = y0 + r_idx * v\n            for i in range(k):\n                x = x0 + i * s + shift\n                C.append([x, y])\n        return np.array(C)  # shape (26,2)\n\n    def radii_for_centers(centers):\n        # distances to borders\n        db = np.minimum.reduce([centers[:,0], centers[:,1], 1-centers[:,0], 1-centers[:,1]])\n        # pairwise distances\n        diffs = centers[:, None, :] - centers[None, :, :]\n        D = np.sqrt(np.maximum(0.0, (diffs**2).sum(axis=2)))\n        np.fill_diagonal(D, np.inf)\n        dmin = D.min(axis=1)\n        r = np.minimum(db, 0.5 * dmin)\n        return np.clip(r, 0.0, 0.5)\n\n    # Search parameters\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    # spacing range informed by row counts (must keep centers inside unit square)\n    for s in np.linspace(0.09, 0.20, 60):\n        C0 = base_centers(s)\n        if C0.shape[0] != n:\n            continue\n        # try small anisotropic scales around 1.0 and tiny translations\n        for sx in np.linspace(0.985, 1.015, 7):\n            for sy in np.linspace(0.985, 1.015, 7):\n                # scale about the square center 0.5 to retain placement\n                C = 0.5 + (C0 - 0.5) * np.array([sx, sy])\n                for tx in np.linspace(-0.008, 0.008, 5):\n                    for ty in np.linspace(-0.008, 0.008, 5):\n                        Ctr = C + np.array([tx, ty])\n                        # reject if any center outside bounds\n                        if np.any(Ctr < -1e-9) or np.any(Ctr > 1 + 1e-9):\n                            continue\n                        R = radii_for_centers(Ctr)\n                        if np.any(R <= 0):\n                            # allow but still evaluate sum (rare), skip if degenerate\n                            if np.any(R < 1e-9):\n                                continue\n                        ssum = float(R.sum())\n                        if ssum > best_sum:\n                            best_sum = ssum\n                            best_centers = Ctr.copy()\n                            best_radii = R.copy()\n\n    # Fallback simple placement if search fails\n    if best_centers is None:\n        C0 = base_centers(0.14)\n        best_centers = np.clip(C0, 1e-6, 1-1e-6)\n        best_radii = radii_for_centers(best_centers)\n        best_sum = float(best_radii.sum())\n\n    return np.array(best_centers), np.array(best_radii), float(best_radii.sum())\n# EVOLVE-BLOCK-END\n\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(7,7))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect('equal')\n    for i,(c,r) in enumerate(zip(centers, radii)):\n        ax.add_patch(Circle(c,r,alpha=0.6,edgecolor='k'))\n        ax.text(c[0],c[1],str(i),ha='center',va='center',fontsize=8)\n    plt.title(f\"n={len(centers)}, sum={sum(radii):.6f}\"); plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, s = run_packing()\n    print(f\"Sum of radii: {s:.6f}\")\n    # visualize(centers, radii)\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.1272), Performs well on target_ratio (0.8073), Performs well on combined_score (0.8073), Performs well on eval_time (2.1832)\n\n\n\n## Diverse Programs\n\n### Program D1 (Score: 0.7941)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (compact hex-like rows).\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    # compact near-hex layering that sums to 26\n    rows = [6, 5, 5, 5, 5]\n\n    def build_centers(s):\n        v = np.sqrt(3) / 2 * s\n        centers = []\n        R = len(rows)\n        total_v = (R - 1) * v\n        y0 = (1.0 - total_v) / 2.0\n        for r_idx, k in enumerate(rows):\n            span = (k - 1) * s\n            x0 = (1.0 - span) / 2.0\n            shift = (r_idx % 2) * (s / 2.0)\n            y = y0 + r_idx * v\n            for i in range(k):\n                x = x0 + i * s + shift\n                centers.append([x, y])\n        return np.array(centers)\n\n    def radii_for_centers(C):\n        # clamp to unit square safety\n        C = np.clip(C, 1e-9, 1 - 1e-9)\n        # distance to walls\n        d_wall = np.minimum.reduce([C[:,0], C[:,1], 1-C[:,0], 1-C[:,1]])\n        # pairwise distances\n        D = np.sqrt(np.maximum(0.0, ((C[:,None,:] - C[None,:,:])**2).sum(axis=2)))\n        np.fill_diagonal(D, np.inf)\n        d_near = D.min(axis=1)\n        r = np.minimum(d_wall, 0.5 * d_near)\n        return np.maximum(r, 0.0)\n\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    # search horizontal spacing\n    for s in np.linspace(0.06, 0.24, 240):\n        C = build_centers(s)\n        if C.shape[0] != n: \n            continue\n        # reject if any center out of bounds\n        if np.any(C < -1e-8) or np.any(C > 1+1e-8):\n            continue\n        r = radii_for_centers(C)\n        if np.any(r <= 0):\n            continue\n        ssum = float(r.sum())\n        if ssum > best_sum:\n            best_sum = ssum\n            best_centers = C.copy()\n            best_radii = r.copy()\n\n    if best_centers is None:\n        best_centers = build_centers(0.12)\n        best_radii = radii_for_centers(best_centers)\n        best_sum = float(best_radii.sum())\n\n    return best_centers, best_radii, float(best_sum)\n# EVOLVE-BLOCK-END\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(7, 7))\n    ax.set_xlim(0, 1); ax.set_ylim(0, 1); ax.set_aspect(\"equal\")\n    # Draw circles\n    for i, (c, r) in enumerate(zip(centers, radii)):\n        ax.add_patch(Circle(c, r, alpha=0.6, edgecolor=\"k\"))\n        ax.text(c[0], c[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # visualize(centers, radii)\n```\nKey features: Alternative approach to validity, Alternative approach to sum_radii\n\n### Program D2 (Score: 0.8068)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for 26 circles in the unit square.\nPlaces centers in a compact hex-like layering [6,5,5,5,5], searches horizontal\nspacing s to maximize sum(radii) where r_i = min(dist_to_border, 0.5*nearest_neighbor).\nVectorized computations and a tiny anisotropic tweak around the best s improve fit.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    rows = [6, 5, 5, 5, 5]  # compact near-hex cluster\n\n    def build_centers(s):\n        v = np.sqrt(3) / 2.0 * s\n        R = len(rows)\n        total_v = (R - 1) * v\n        y0 = (1.0 - total_v) / 2.0\n        C = []\n        for r_idx, k in enumerate(rows):\n            span = (k - 1) * s\n            x0 = (1.0 - span) / 2.0\n            shift = (r_idx % 2) * (s / 2.0)\n            y = y0 + r_idx * v\n            for i in range(k):\n                x = x0 + i * s + shift\n                C.append((x, y))\n        return np.array(C)\n\n    def radii_for_centers(C):\n        # distance to walls\n        db = np.minimum.reduce([C[:,0], C[:,1], 1-C[:,0], 1-C[:,1]])\n        # pairwise distances\n        diff = C[:, None, :] - C[None, :, :]\n        D = np.sqrt(np.maximum(0.0, (diff**2).sum(axis=2)))\n        np.fill_diagonal(D, np.inf)\n        nearest = D.min(axis=1)\n        r = np.minimum(db, 0.5 * nearest)\n        return np.clip(r, 0.0, 0.5)\n\n    best_sum = -1.0\n    best_C = None\n    best_r = None\n\n    # search horizontal spacing (dense)\n    for s in np.linspace(0.11, 0.22, 300):\n        C = build_centers(s)\n        if C.shape[0] != n: continue\n        if np.any(C < -1e-8) or np.any(C > 1+1e-8): continue\n        r = radii_for_centers(C)\n        if np.any(r <= 0): continue\n        ssum = float(r.sum())\n        if ssum > best_sum:\n            best_sum = ssum; best_C = C.copy(); best_r = r.copy()\n\n    # small anisotropic tweak around best solution\n    if best_C is not None:\n        for tx in np.linspace(0.995, 1.005, 5):\n            for ty in np.linspace(0.995, 1.005, 5):\n                # scale around center 0.5 to slightly stretch/compress horizontally/vertically\n                C = (best_C - 0.5) * np.array([tx, ty]) + 0.5\n                C = np.clip(C, 1e-8, 1-1e-8)\n                r = radii_for_centers(C)\n                ssum = float(r.sum())\n                if ssum > best_sum:\n                    best_sum = ssum; best_C = C.copy(); best_r = r.copy()\n\n    # Fallback if search failed\n    if best_C is None:\n        best_C = build_centers(0.15)\n        best_r = radii_for_centers(best_C)\n        best_sum = float(best_r.sum())\n\n    return best_C, best_r, float(best_sum)\n# EVOLVE-BLOCK-END\n\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(6,6))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect('equal')\n    for i,(c,r) in enumerate(zip(centers, radii)):\n        ax.add_patch(Circle(c,r,alpha=0.6,edgecolor='k'))\n        ax.text(c[0],c[1],str(i),ha='center',va='center',fontsize=8)\n    plt.title(f\"n={len(centers)}, sum={sum(radii):.6f}\"); plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # visualize(centers, radii)\n```\nKey features: Alternative approach to validity, Alternative approach to sum_radii\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 0.7024, Type: Alternative)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (improved)\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a deterministic arrangement of 26 circle centers arranged\n    in a compact hexagonal-like layout inside the unit square. Then\n    compute near-optimal radii by enforcing non-overlap and boundary\n    constraints via an iterative projection procedure. A small grid\n    search over horizontal spacing is used to pick the best layout.\n    Returns:\n        centers: (26,2) array\n        radii:   (26,) array\n        sum_radii: float\n    \"\"\"\n    n = 26\n    # Use a 5-row hex-like pattern with counts summing to 26\n    rows = [5, 6, 5, 6, 4]\n\n    def make_centers(s):\n        # horizontal spacing s; vertical spacing for hex packing\n        h = s * np.sqrt(3) / 2.0\n        total_rows = len(rows)\n        total_height = (total_rows - 1) * h\n        y0 = 0.5 - total_height / 2.0\n        centers = []\n        for r_i, m in enumerate(rows):\n            y = y0 + r_i * h\n            # offset every other row by s/2 (hex pattern)\n            offset = (s / 2.0) if (r_i % 2 == 1) else 0.0\n            span = (m - 1) * s\n            x0 = 0.5 - span / 2.0 + offset\n            for k in range(m):\n                x = x0 + k * s\n                # clip a hair inside to avoid exact-touch-to-border floating issues\n                centers.append([np.clip(x, 1e-6, 1 - 1e-6), np.clip(y, 1e-6, 1 - 1e-6)])\n        return np.array(centers)\n\n    def compute_max_radii_iter(centers, max_iters=2000, tol=1e-6):\n        n = centers.shape[0]\n        # initial radii limited by borders\n        radii = np.minimum.reduce([centers[:, 0], centers[:, 1], 1 - centers[:, 0], 1 - centers[:, 1]])\n        radii = np.maximum(radii, 0.0)\n        # iterative pairwise constraint enforcement:\n        for it in range(max_iters):\n            changed = False\n            # enforce pair constraints; reduce the larger circle of any violating pair\n            for i in range(n):\n                for j in range(i + 1, n):\n                    dx = centers[i, 0] - centers[j, 0]\n                    dy = centers[i, 1] - centers[j, 1]\n                    d = np.hypot(dx, dy)\n                    if d <= 0:\n                        # coincident centers shouldn't happen but protect\n                        if radii[j] > 0:\n                            radii[j] = 0.0\n                            changed = True\n                        continue\n                    if radii[i] + radii[j] > d:\n                        # Prefer reducing the larger radius (keeps sum larger than naive equal scaling)\n                        if radii[i] > radii[j]:\n                            new_ri = max(0.0, d - radii[j])\n                            if radii[i] - new_ri > tol:\n                                radii[i] = new_ri\n                                changed = True\n                        else:\n                            new_rj = max(0.0, d - radii[i])\n                            if radii[j] - new_rj > tol:\n                                radii[j] = new_rj\n                                changed = True\n            # enforce border constraints strictly\n            border_limits = np.minimum.reduce([centers[:, 0], centers[:, 1], 1 - centers[:, 0], 1 - centers[:, 1]])\n            new_radii = np.minimum(radii, border_limits)\n            if np.any(radii - new_radii > tol):\n                radii = new_radii\n                changed = True\n            if not changed:\n                break\n        return radii\n\n    # Grid-search the horizontal spacing 's' to maximize sum of radii.\n    # Reasonable search range for s given 5..6 points per row: try from 0.08..0.18\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n    s_values = np.linspace(0.09, 0.18, 46)\n    for s in s_values:\n        centers = make_centers(s)\n        if centers.shape[0] != n:\n            continue\n        radii = compute_max_radii_iter(centers)\n        srad = radii.sum()\n        if srad > best_sum:\n            best_sum = srad\n            best_centers = centers.copy()\n            best_radii = radii.copy()\n\n    # If search failed for some reason, fallback to a default spacing\n    if best_centers is None:\n        best_centers = make_centers(0.12)\n        best_radii = compute_max_radii_iter(best_centers)\n\n    return best_centers, best_radii, float(best_radii.sum())\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\", linewidth=0.5)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nUnique approach: Modification: Full rewrite, Excellent validity (1.000), Excellent sum_radii (1.851)\n\n### Inspiration 2 (Score: 0.6918, Type: Alternative)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (hexagonal-like rows)\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square.\n    Returns: (centers, radii, sum_of_radii)\n    \"\"\"\n    n = 26\n\n    # Hexagonal-like row counts that sum to 26\n    row_counts = [6, 5, 6, 5, 4]  # 5 rows -> 6+5+6+5+4 = 26\n    rows = len(row_counts)\n\n    # Choose horizontal spacing s. Larger s increases possible radii but must fit rows.\n    # We pick s close to the largest that keeps rows inside the unit square with a small margin.\n    margin = 0.02\n    # Upper bound for s from largest row (m_max): (m_max - 1) * s <= 1 - 2*margin\n    m_max = max(row_counts)\n    s = min(0.16, (1 - 2 * margin) / (m_max - 1 + 1e-9))  # clamp s for safety\n    v = np.sqrt(3) / 2 * s  # vertical spacing for hex pattern\n\n    # Compute vertical placement so rows are centered vertically with margin\n    total_height = (rows - 1) * v\n    y_bottom = margin + max(0.0, (1 - 2 * margin - total_height) / 2)\n\n    centers = []\n    for ri, m in enumerate(row_counts):\n        y = y_bottom + ri * v\n        # Alternate offset for hex packing\n        offset = (s / 2.0) if (ri % 2 == 1) else 0.0\n        # Center the row horizontally\n        row_width = (m - 1) * s\n        x_start = 0.5 - row_width / 2.0 + offset\n        for j in range(m):\n            x = x_start + j * s\n            # Clip to ensure inside the unit square with tiny epsilon\n            x = np.clip(x, margin, 1 - margin)\n            y_clipped = np.clip(y, margin, 1 - margin)\n            centers.append([x, y_clipped])\n\n    centers = np.array(centers, dtype=float)\n    assert centers.shape[0] == n, \"constructed wrong number of centers\"\n\n    radii = compute_max_radii(centers)\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\ndef compute_max_radii(centers):\n    \"\"\"\n    For fixed centers, produce a feasible set of radii by taking for each circle:\n      r_i = min(distance_to_border_i, 0.5 * min_{j!=i} dist(center_i, center_j))\n    This is a simple, fast, and valid assignment guaranteeing no overlaps.\n    \"\"\"\n    n = centers.shape[0]\n    # Distance to borders\n    border = np.minimum.reduce([centers[:, 0], centers[:, 1], 1 - centers[:, 0], 1 - centers[:, 1]])\n    # Pairwise distances\n    diff = centers[:, None, :] - centers[None, :, :]  # (n, n, 2)\n    dists = np.linalg.norm(diff, axis=2)\n    # Avoid self-distance\n    np.fill_diagonal(dists, np.inf)\n    min_pair = np.min(dists, axis=1)\n    radii = np.minimum(border, 0.5 * min_pair)\n    # Ensure non-negative\n    radii = np.maximum(radii, 0.0)\n    return radii\n\n# EVOLVE-BLOCK-END\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, ec=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nUnique approach: Modification: Full rewrite, Excellent validity (1.000), Excellent sum_radii (1.823)\n\n### Inspiration 3 (Score: 0.7941, Type: Alternative)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (compact hex-like rows).\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    # compact near-hex layering that sums to 26\n    rows = [6, 5, 5, 5, 5]\n\n    def build_centers(s):\n        v = np.sqrt(3) / 2 * s\n        centers = []\n        R = len(rows)\n        total_v = (R - 1) * v\n        y0 = (1.0 - total_v) / 2.0\n        for r_idx, k in enumerate(rows):\n            span = (k - 1) * s\n            x0 = (1.0 - span) / 2.0\n            shift = (r_idx % 2) * (s / 2.0)\n            y = y0 + r_idx * v\n            for i in range(k):\n                x = x0 + i * s + shift\n                centers.append([x, y])\n        return np.array(centers)\n\n    def radii_for_centers(C):\n        # clamp to unit square safety\n        C = np.clip(C, 1e-9, 1 - 1e-9)\n        # distance to walls\n        d_wall = np.minimum.reduce([C[:,0], C[:,1], 1-C[:,0], 1-C[:,1]])\n        # pairwise distances\n        D = np.sqrt(np.maximum(0.0, ((C[:,None,:] - C[None,:,:])**2).sum(axis=2)))\n        np.fill_diagonal(D, np.inf)\n        d_near = D.min(axis=1)\n        r = np.minimum(d_wall, 0.5 * d_near)\n        return np.maximum(r, 0.0)\n\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    # search horizontal spacing\n    for s in np.linspace(0.06, 0.24, 240):\n        C = build_centers(s)\n        if C.shape[0] != n: \n            continue\n        # reject if any center out of bounds\n        if np.any(C < -1e-8) or np.any(C > 1+1e-8):\n            continue\n        r = radii_for_centers(C)\n        if np.any(r <= 0):\n            continue\n        ssum = float(r.sum())\n        if ssum > best_sum:\n            best_sum = ssum\n            best_centers = C.copy()\n            best_radii = r.copy()\n\n    if best_centers is None:\n        best_centers = build_centers(0.12)\n        best_radii = radii_for_centers(best_centers)\n        best_sum = float(best_radii.sum())\n\n    return best_centers, best_radii, float(best_sum)\n# EVOLVE-BLOCK-END\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(7, 7))\n    ax.set_xlim(0, 1); ax.set_ylim(0, 1); ax.set_aspect(\"equal\")\n    # Draw circles\n    for i, (c, r) in enumerate(zip(centers, radii)):\n        ax.add_patch(Circle(c, r, alpha=0.6, edgecolor=\"k\"))\n        ax.text(c[0], c[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # visualize(centers, radii)\n```\nUnique approach: Modification: Full rewrite, Excellent validity (1.000), Excellent sum_radii (2.093)\n\n# Current Program\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for 26 circles in the unit square.\nPlaces centers in a compact hex-like layering [6,5,5,5,5], searches horizontal\nspacing s to maximize sum(radii) where r_i = min(dist_to_border, 0.5*nearest_neighbor).\nVectorized computations and a tiny anisotropic tweak around the best s improve fit.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    rows = [6, 5, 5, 5, 5]  # compact near-hex cluster\n\n    def build_centers(s):\n        v = np.sqrt(3) / 2.0 * s\n        R = len(rows)\n        total_v = (R - 1) * v\n        y0 = (1.0 - total_v) / 2.0\n        C = []\n        for r_idx, k in enumerate(rows):\n            span = (k - 1) * s\n            x0 = (1.0 - span) / 2.0\n            shift = (r_idx % 2) * (s / 2.0)\n            y = y0 + r_idx * v\n            for i in range(k):\n                x = x0 + i * s + shift\n                C.append((x, y))\n        return np.array(C)\n\n    def radii_for_centers(C):\n        # distance to walls\n        db = np.minimum.reduce([C[:,0], C[:,1], 1-C[:,0], 1-C[:,1]])\n        # pairwise distances\n        diff = C[:, None, :] - C[None, :, :]\n        D = np.sqrt(np.maximum(0.0, (diff**2).sum(axis=2)))\n        np.fill_diagonal(D, np.inf)\n        nearest = D.min(axis=1)\n        r = np.minimum(db, 0.5 * nearest)\n        return np.clip(r, 0.0, 0.5)\n\n    best_sum = -1.0\n    best_C = None\n    best_r = None\n\n    # search horizontal spacing (dense)\n    for s in np.linspace(0.11, 0.22, 300):\n        C = build_centers(s)\n        if C.shape[0] != n: continue\n        if np.any(C < -1e-8) or np.any(C > 1+1e-8): continue\n        r = radii_for_centers(C)\n        if np.any(r <= 0): continue\n        ssum = float(r.sum())\n        if ssum > best_sum:\n            best_sum = ssum; best_C = C.copy(); best_r = r.copy()\n\n    # small anisotropic tweak around best solution\n    if best_C is not None:\n        for tx in np.linspace(0.995, 1.005, 5):\n            for ty in np.linspace(0.995, 1.005, 5):\n                # scale around center 0.5 to slightly stretch/compress horizontally/vertically\n                C = (best_C - 0.5) * np.array([tx, ty]) + 0.5\n                C = np.clip(C, 1e-8, 1-1e-8)\n                r = radii_for_centers(C)\n                ssum = float(r.sum())\n                if ssum > best_sum:\n                    best_sum = ssum; best_C = C.copy(); best_r = r.copy()\n\n    # Fallback if search failed\n    if best_C is None:\n        best_C = build_centers(0.15)\n        best_r = radii_for_centers(best_C)\n        best_sum = float(best_r.sum())\n\n    return best_C, best_r, float(best_sum)\n# EVOLVE-BLOCK-END\n\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(6,6))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect('equal')\n    for i,(c,r) in enumerate(zip(centers, radii)):\n        ax.add_patch(Circle(c,r,alpha=0.6,edgecolor='k'))\n        ax.text(c[0],c[1],str(i),ha='center',va='center',fontsize=8)\n    plt.title(f\"n={len(centers)}, sum={sum(radii):.6f}\"); plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # visualize(centers, radii)\n```\n\n# Task\nRewrite the program to improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: complexity, diversity\nDifferent solutions with similar fitness but different features are valuable.\nProvide the complete new program code.\n\nIMPORTANT: Make sure your rewritten program maintains the same inputs and outputs\nas the original program, but with improved internal implementation.\n\n```python\n# Your rewritten program here\n```", "responses": ["```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for 26 circles in the unit square.\nImproved explicit pattern: compact near-hex cluster (rows 5+5+6+5+5),\ncoarse uniform scale sweep to find a tight fit, followed by small\nanisotropic, per-row-shift and translation refinements. Radii are\nassigned as r_i = min(distance-to-border, 0.5*nearest-center-distance).\nThis keeps the constructor simple, deterministic and fast.\n\"\"\"\nimport numpy as np\nfrom itertools import product\n\ndef construct_packing():\n    n = 26\n    # Use the high-performing row pattern seen in top solutions\n    rows = [5, 5, 6, 5, 5]  # sums to 26\n    vstep = np.sqrt(3.0) / 2.0\n\n    # Build a canonical unit-spaced hex-like base layout (not yet scaled)\n    base = []\n    for r_idx, cnt in enumerate(rows):\n        xs = np.arange(cnt) - (cnt - 1) / 2.0\n        x_offset = 0.5 * (r_idx % 2)\n        y = r_idx * vstep\n        for x in xs:\n            base.append([x + x_offset, y])\n    base = np.array(base)  # (26,2)\n\n    # compute bounding extents of base\n    minb = base.min(axis=0)\n    maxb = base.max(axis=0)\n    width = maxb[0] - minb[0]\n    height = maxb[1] - minb[1]\n\n    margin = 1e-6  # tiny margin to avoid exact border-touch\n    # maximum uniform scale to ensure the pattern can fit inside the unit square\n    Smax = min((1 - 2 * margin) / width, (1 - 2 * margin) / height)\n\n    def compute_radii(centers):\n        # distance to border\n        db = np.minimum.reduce([centers[:, 0], centers[:, 1], 1 - centers[:, 0], 1 - centers[:, 1]])\n        # pairwise distances (vectorized)\n        dif = centers[:, None, :] - centers[None, :, :]\n        D = np.sqrt(np.maximum(0.0, (dif ** 2).sum(axis=2)))\n        np.fill_diagonal(D, np.inf)\n        nearest = D.min(axis=1)\n        radii = np.minimum(db, 0.5 * nearest)\n        # ensure non-negative\n        return np.maximum(radii, 0.0)\n\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    # Coarse uniform scale sweep (explore slightly beyond Smax with clipping)\n    for alpha in np.linspace(0.45, 1.02, 61):\n        s = alpha * Smax\n        # scale base and center it in unit square\n        scaled = (base - minb) * s\n        lo = scaled.min(axis=0); hi = scaled.max(axis=0)\n        trans = 0.5 - 0.5 * (lo + hi)\n        centers = scaled + trans\n        centers = np.clip(centers, margin, 1 - margin)\n        r = compute_radii(centers)\n        ssum = float(r.sum())\n        if ssum > best_sum:\n            best_sum = ssum\n            best_centers = centers.copy()\n            best_radii = r.copy()\n\n    # Local refinements: anisotropic scaling, small per-row horizontal shifts,\n    # and tiny translations to let the cluster \"snug\" against edges.\n    if best_centers is None:\n        # fallback\n        best_centers = np.clip((base - minb) * (0.14) + 0.5, margin, 1 - margin)\n        best_radii = compute_radii(best_centers)\n        best_sum = float(best_radii.sum())\n\n    # Prepare per-row indices for applying row shifts\n    row_indices = []\n    idx = 0\n    for cnt in rows:\n        row_indices.append(list(range(idx, idx + cnt)))\n        idx += cnt\n\n    # small sets of anisotropic multipliers (around 1.0) and shift fractions\n    tx_vals = [0.997, 1.0, 1.003]\n    ty_vals = [0.997, 1.0, 1.003]\n    shift_fracs = [-0.035, 0.0, 0.035]  # fractions of Smax for horizontal shifts\n    translations = [(-0.002, 0.0), (0.0, -0.002), (0.0, 0.0), (0.002, 0.0), (0.0, 0.002)]\n\n    base_coords = base.copy()\n    # We'll apply anisotropic scaling relative to the canonical base bounding box\n    for tx, ty in product(tx_vals, ty_vals):\n        sx = tx * Smax\n        sy = ty * Smax * (vstep) / (vstep)  # keep consistent vertical unit (explicit)\n        # scale base coordinates\n        scaled = base_coords.copy()\n        scaled[:, 0] = (scaled[:, 0] - minb[0]) * sx\n        scaled[:, 1] = (scaled[:, 1] - minb[1]) * sy / vstep\n        # try per-row horizontal shifts combinations (3^5 = 243)\n        for shifts in product(shift_fracs, repeat=len(rows)):\n            centers = scaled.copy()\n            for r, frac in enumerate(shifts):\n                inds = row_indices[r]\n                centers[inds, 0] += frac * Smax\n            # center and try small translations\n            lo = centers.min(axis=0); hi = centers.max(axis=0)\n            trans = 0.5 - 0.5 * (lo + hi)\n            centers = centers + trans\n            for dx, dy in translations:\n                C = np.clip(centers + np.array([dx, dy]), margin, 1 - margin)\n                r = compute_radii(C)\n                ssum = float(r.sum())\n                if ssum > best_sum:\n                    best_sum = ssum\n                    best_centers = C.copy()\n                    best_radii = r.copy()\n\n    # Final tiny nudges along each axis (deterministic small moves)\n    for dx in (-1e-3, 0.0, 1e-3):\n        for dy in (-1e-3, 0.0, 1e-3):\n            C = np.clip(best_centers + np.array([dx, dy]), margin, 1 - margin)\n            r = compute_radii(C)\n            ssum = float(r.sum())\n            if ssum > best_sum:\n                best_sum = ssum\n                best_centers = C.copy()\n                best_radii = r.copy()\n\n    return np.array(best_centers), np.array(best_radii), float(best_radii.sum())\n\n# EVOLVE-BLOCK-END\n\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(7,7))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect('equal')\n    for i,(c,r) in enumerate(zip(centers, radii)):\n        ax.add_patch(Circle(c,r,alpha=0.6,edgecolor='k'))\n        ax.text(c[0],c[1],str(i),ha='center',va='center',fontsize=8)\n    plt.title(f\"n={len(centers)}, sum={sum(radii):.6f}\"); plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # visualize(centers, radii)\n```"]}}, "artifacts_json": "{\"stdout\": \"Running in subprocess, Python version: 3.12.1 (v3.12.1:2305ca5144, Dec  7 2023, 17:23:38) [Clang 13.0.0 (clang-1300.0.29.30)]\\nProgram path: /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmps24kdrla.py\\nCalling run_packing()...\\nrun_packing() returned successfully: sum_radii = 2.2321515406999994\\nResults saved to /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmpfnosd2df.py.results\\n\", \"stderr\": \"\"}", "artifact_dir": null}