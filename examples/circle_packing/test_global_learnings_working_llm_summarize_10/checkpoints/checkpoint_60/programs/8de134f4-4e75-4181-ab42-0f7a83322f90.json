{"id": "8de134f4-4e75-4181-ab42-0f7a83322f90", "code": "# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for 26 circles in the unit square.\n\nPattern: compact near-hex cluster rows [5,5,6,5,5]. Coarse deterministic grid\nsearch over horizontal spacing and vertical anisotropy, try small per-row\nshifts and translations, then a short coordinate-descent refinement.\nRadii for fixed centers: r_i = min(dist_to_border, 0.5 * nearest_center_distance).\n\"\"\"\nimport numpy as np\nfrom itertools import product\n\ndef construct_packing():\n    n = 26\n    rows = [5, 5, 6, 5, 5]\n    vstep_hex = np.sqrt(3.0) / 2.0\n\n    # base integer-layout (centered in base units)\n    base = []\n    for r_idx, cnt in enumerate(rows):\n        xs = np.arange(cnt) - (cnt - 1) / 2.0\n        x_offset = 0.5 * (r_idx % 2)\n        y = r_idx * vstep_hex\n        for x in xs:\n            base.append([x + x_offset, y])\n    base = np.array(base)\n\n    def compute_radii(centers, margin=0.0):\n        # distance to border\n        db = np.minimum.reduce([centers[:,0]-margin, centers[:,1]-margin,\n                                1.0-margin-centers[:,0], 1.0-margin-centers[:,1]])\n        # pairwise distances\n        dif = centers[:, None, :] - centers[None, :, :]\n        D = np.sqrt(np.maximum(0.0, (dif**2).sum(axis=2)))\n        np.fill_diagonal(D, np.inf)\n        nearest = D.min(axis=1)\n        r = np.minimum(db, 0.5 * nearest)\n        return np.maximum(r, 0.0)\n\n    def build_centers(s_h, v_ratio, row_shifts, dx=0.0, dy=0.0, margin=1e-12):\n        s_v = v_ratio * vstep_hex * s_h\n        coords = base.copy()\n        coords[:,0] = coords[:,0] * s_h\n        coords[:,1] = coords[:,1] * (s_v / vstep_hex)\n        # apply per-row shifts\n        idx = 0\n        for r, cnt in enumerate(rows):\n            inds = slice(idx, idx + cnt)\n            coords[inds, 0] += row_shifts[r]\n            idx += cnt\n        # center pattern in unit square\n        lo = coords.min(axis=0); hi = coords.max(axis=0)\n        trans = 0.5 - 0.5 * (lo + hi)\n        coords = coords + trans + np.array([dx, dy])\n        return np.clip(coords, margin, 1.0 - margin)\n\n    # coarse grids (deterministic)\n    s_h_vals = np.linspace(0.095, 0.185, 18)\n    v_ratio_vals = np.linspace(0.94, 1.06, 7)\n    shift_fracs = [-0.045, 0.0, 0.045]   # interpreted as fractions of s_h\n    trans_grid = [(0.0,0.0), (-0.002,0.0), (0.002,0.0), (0.0,-0.002), (0.0,0.002),\n                  (-0.002,-0.002),(0.002,0.002)]\n\n    all_shift_combos = list(product(shift_fracs, repeat=len(rows)))\n\n    best_sum = -1.0\n    best_params = None\n    best_centers = None\n    best_radii = None\n\n    # coarse search\n    for s_h in s_h_vals:\n        for v_ratio in v_ratio_vals:\n            for frac_combo in all_shift_combos:\n                row_shifts = np.array(frac_combo) * s_h\n                for dx, dy in trans_grid:\n                    centers = build_centers(s_h, v_ratio, row_shifts, dx=dx, dy=dy)\n                    r = compute_radii(centers)\n                    ssum = float(r.sum())\n                    if ssum > best_sum + 1e-12:\n                        best_sum = ssum\n                        best_params = (s_h, v_ratio, row_shifts.copy(), dx, dy)\n                        best_centers = centers.copy()\n                        best_radii = r.copy()\n\n    # fallback if none found (shouldn't happen)\n    if best_params is None:\n        s_h = 0.12; v_ratio = 1.0; row_shifts = np.zeros(len(rows))\n        best_centers = build_centers(s_h, v_ratio, row_shifts)\n        best_radii = compute_radii(best_centers)\n        best_sum = float(best_radii.sum())\n        best_params = (s_h, v_ratio, row_shifts.copy(), 0.0, 0.0)\n\n    # deterministic coordinate-descent refinement around best params\n    cur_s_h, cur_v_ratio, cur_row_shifts, cur_dx, cur_dy = best_params\n    s_h_steps = [0.012, 0.006, 0.003, 0.0015]\n    v_ratio_steps = [0.03, 0.015, 0.007, 0.0035]\n    row_shift_steps = [0.03 * cur_s_h, 0.015 * cur_s_h, 0.007 * cur_s_h, 0.003 * cur_s_h]\n    trans_steps = [0.004, 0.002, 0.001, 0.0005]\n\n    for level in range(len(s_h_steps)):\n        improved = True\n        iter_count = 0\n        while improved and iter_count < 6:\n            improved = False\n            iter_count += 1\n            # s_h\n            for sign in (-1,1):\n                cand = cur_s_h + sign * s_h_steps[level]\n                if 0.04 < cand < 0.35:\n                    centers = build_centers(cand, cur_v_ratio, cur_row_shifts, cur_dx, cur_dy)\n                    r = compute_radii(centers)\n                    if r.sum() > best_sum + 1e-12:\n                        best_sum = float(r.sum()); best_centers = centers.copy(); best_radii = r.copy(); cur_s_h = cand; improved = True\n            # v_ratio\n            for sign in (-1,1):\n                cand = cur_v_ratio + sign * v_ratio_steps[level]\n                if 0.7 < cand < 1.4:\n                    centers = build_centers(cur_s_h, cand, cur_row_shifts, cur_dx, cur_dy)\n                    r = compute_radii(centers)\n                    if r.sum() > best_sum + 1e-12:\n                        best_sum = float(r.sum()); best_centers = centers.copy(); best_radii = r.copy(); cur_v_ratio = cand; improved = True\n            # per-row shifts\n            for ridx in range(len(rows)):\n                for sign in (-1,1):\n                    cand_shifts = cur_row_shifts.copy()\n                    cand_shifts[ridx] += sign * row_shift_steps[level]\n                    centers = build_centers(cur_s_h, cur_v_ratio, cand_shifts, cur_dx, cur_dy)\n                    r = compute_radii(centers)\n                    if r.sum() > best_sum + 1e-12:\n                        best_sum = float(r.sum()); best_centers = centers.copy(); best_radii = r.copy(); cur_row_shifts = cand_shifts; improved = True\n            # translations\n            for tx_mult, ty_mult in product((-1,0,1), repeat=2):\n                if tx_mult==0 and ty_mult==0: continue\n                cand_dx = cur_dx + tx_mult * trans_steps[level]\n                cand_dy = cur_dy + ty_mult * trans_steps[level]\n                centers = build_centers(cur_s_h, cur_v_ratio, cur_row_shifts, cand_dx, cand_dy)\n                r = compute_radii(centers)\n                if r.sum() > best_sum + 1e-12:\n                    best_sum = float(r.sum()); best_centers = centers.copy(); best_radii = r.copy(); cur_dx, cur_dy = cand_dx, cand_dy; improved = True\n\n    # tiny final nudges\n    for dx_try in (-5e-4, 0.0, 5e-4):\n        for dy_try in (-5e-4, 0.0, 5e-4):\n            centers = build_centers(cur_s_h, cur_v_ratio, cur_row_shifts, cur_dx + dx_try, cur_dy + dy_try)\n            r = compute_radii(centers)\n            if r.sum() > best_sum + 1e-12:\n                best_sum = float(r.sum()); best_centers = centers.copy(); best_radii = r.copy(); cur_dx += dx_try; cur_dy += dy_try\n\n    return np.array(best_centers), np.array(best_radii), float(best_radii.sum())\n# EVOLVE-BLOCK-END\n\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")", "language": "python", "parent_id": "3f8080da-7c6f-4e36-b195-06b175008f37", "generation": 4, "timestamp": 1759962358.995193, "iteration_found": 58, "metrics": {"validity": 1.0, "sum_radii": 2.3054999999999994, "target_ratio": 0.874952561669829, "combined_score": 0.874952561669829, "eval_time": 7.247186899185181}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Full rewrite", "parent_metrics": {"validity": 1.0, "sum_radii": 2.2242515519999992, "target_ratio": 0.8441182360531307, "combined_score": 0.8441182360531307, "eval_time": 0.14809298515319824}, "island": 0}, "prompts": {"full_rewrite_user": {"system": "You are an expert mathematician specializing in circle packing problems and computational geometry. Your task is to improve a constructor function that directly produces a specific arrangement of 26 circles in a unit square, maximizing the sum of their radii. The AlphaEvolve paper achieved a sum of 2.635 for n=26.\n\nKey geometric insights:\n- Circle packings often follow hexagonal patterns in the densest regions\n- Maximum density for infinite circle packing is pi/(2*sqrt(3)) \u2248 0.9069\n- Edge effects make square container packing harder than infinite packing\n- Circles can be placed in layers or shells when confined to a square\n- Similar radius circles often form regular patterns, while varied radii allow better space utilization\n- Perfect symmetry may not yield the optimal packing due to edge effects\n\nFocus on designing an explicit constructor that places each circle in a specific position, rather than an iterative search algorithm.\n\n\n## Evolution Insights (Global Learnings)\n\n### Common Pitfalls:\n\u26a0\ufe0f (seen 8x) The change replaced the original iterative pairwise reduction with a fully vectorized strategy that computes the full distance matrix and sets each radius to min(distance-to-border, half the nearest-neighbor distance) (and also altered the row layout/search), which made evaluation much faster and increased the reported sum of radii. When swapping iterative constraint enforcement for a vectorized heuristic, don't assume \"nearest/2\" guarantees global non-overlap \u2014 always validate all pairwise constraints (or run a final mutual-consistency/correction pass) because the shortcut can produce invalid overlaps even while improving speed and objective.\n\u26a0\ufe0f (seen 5x) The regression was caused by replacing the original ring layout and proportional pairwise-rescaling solver with a denser hex-like placement (s=0.192) and an iterative \"only-decrease\" tightening routine plus a final policy that reduces the larger circle in any overlapping pair; the tighter spacing combined with conservative per-circle reductions aggressively shrank many radii and dropped sum_radii from ~0.96 to ~0.34.  \nWhen changing placement or constraint-resolution logic, avoid overly conservative monotone-only shrinking or heuristics that preferentially zero out large circles \u2014 add automated checks (total-sum and pairwise-constraint tests), compare against the prior proportional-scaling baseline, and tune spacing/solver behavior before accepting large structural changes.\n\u26a0\ufe0f (seen 5x) The new version replaced the original ring layout with a hex-like layered placement (rows=[5,5,6,5,5] and hard-coded spacing s=0.192) and switched from proportional downscaling to an iterative \"LP-like\" tightening that enforces r_i + r_j <= d_ij; because the chosen spacing (and some centers near borders) produced many very small inter-center distances, the tightening and final tie-breaking aggressively reduced radii and the total sum fell sharply (\u22480.96 \u2192 0.34).  \nAvoid using hard-coded spacings or unvalidated layouts without first checking minimal pairwise and border distances (or running a quick feasibility/radius-growth check); when changing the solver, compare against the baseline objective, tune spacing/initial radii, and consider solver update order or softer adjustments to prevent overly conservative shrinkage.\n\u26a0\ufe0f (seen 5x) The evolved change replaced the original ring layout with a denser hex-like row layout (s=0.192) and swapped the proportional pairwise scaling for a greedy, one-sided iterative \"tightening\" that enforces r_i \u2264 min_j (d_ij \u2212 r_j); with the chosen spacing and update order many d_ij \u2212 r_j became negative and the sequential reductions cascaded, collapsing many radii and cutting the total sum by ~65%.  \nWhen making similar changes, avoid aggressive one-sided sequential reductions and overly dense initial placements \u2014 use synchronous updates or symmetric/proportional scaling, add damping or lower bounds, or solve the global feasibility problem (LP/QP) to prevent cascading shrinkage, and include checks that the total radius sum doesn't drop unexpectedly.", "user": "# Current Program Information\n- Fitness: 0.8441\n- Feature coordinates: No feature coordinates\n- Focus areas: - Fitness declined: 0.8623 \u2192 0.8441. Consider revising recent changes.\n- Consider simplifying - code length exceeds 500 characters\n\n## Last Execution Output\n\n### stdout\n```\nRunning in subprocess, Python version: 3.12.1 (v3.12.1:2305ca5144, Dec  7 2023, 17:23:38) [Clang 13.0.0 (clang-1300.0.29.30)]\nProgram path: /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmpvek3tbus.py\nCalling run_packing()...\nrun_packing() returned successfully: sum_radii = 2.2242515519999992\nResults saved to /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmp3cv8zjox.py.results\n\n```\n\n### stderr\n```\n\n```\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 3\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.2722, target_ratio: 0.8623, combined_score: 0.8623, eval_time: 0.3676\n- Outcome: Mixed results\n\n### Attempt 2\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.3055, target_ratio: 0.8750, combined_score: 0.8750, eval_time: 7.8949\n- Outcome: Mixed results\n\n### Attempt 1\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.3055, target_ratio: 0.8750, combined_score: 0.8750, eval_time: 8.0680\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.8750)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for 26 circles in the unit square.\nImproved explicit pattern with a wider, deterministic search over\nhorizontal spacing, anisotropic vertical ratio, per-row shifts and\nsmall translations. Uses the exact maximal radii for fixed centers:\nr_i = min(dist_to_border, 0.5 * nearest_neighbor_distance).\n\nThis version does a coarse grid search and then a simple deterministic\ncoordinate-descent refinement to squeeze more total radius out of the\nsame near-hex cluster layout.\n\"\"\"\nimport numpy as np\nfrom itertools import product\n\ndef construct_packing():\n    n = 26\n    rows = [5, 5, 6, 5, 5]  # compact near-hex cluster\n    vstep_hex = np.sqrt(3.0) / 2.0\n\n    # base integer layout (centered about origin in base units)\n    base = []\n    for r_idx, cnt in enumerate(rows):\n        xs = np.arange(cnt) - (cnt - 1) / 2.0\n        x_offset = 0.5 * (r_idx % 2)\n        y = r_idx * vstep_hex\n        for x in xs:\n            base.append([x + x_offset, y])\n    base = np.array(base)  # (26,2)\n\n    # helper: compute radii for a given centers array\n    def compute_radii(centers, margin=0.0):\n        # distance to border (consider margin for safety)\n        db = np.minimum.reduce([centers[:, 0] - margin,\n                                centers[:, 1] - margin,\n                                1.0 - margin - centers[:, 0],\n                                1.0 - margin - centers[:, 1]])\n        # pairwise center distances\n        dif = centers[:, None, :] - centers[None, :, :]\n        D = np.sqrt(np.maximum(0.0, (dif ** 2).sum(axis=2)))\n        np.fill_diagonal(D, np.inf)\n        nearest = D.min(axis=1)\n        radii = np.minimum(db, 0.5 * nearest)\n        return np.maximum(radii, 0.0)\n\n    # Build centers from parameters: horizontal spacing s_h, vertical ratio r_v,\n    # per-row shifts (absolute units), and translation (dx,dy)\n    def build_centers(s_h, v_ratio, row_shifts, dx=0.0, dy=0.0, margin=1e-9):\n        s_v = v_ratio * vstep_hex * s_h  # absolute vertical step\n        coords = base.copy()\n        # scale: base horizontal units become s_h, vertical base units become s_v/vstep_hex\n        coords[:, 0] = coords[:, 0] * s_h\n        coords[:, 1] = coords[:, 1] * (s_v / vstep_hex)\n        # apply per-row shifts\n        idx = 0\n        for r, cnt in enumerate(rows):\n            inds = slice(idx, idx + cnt)\n            coords[inds, 0] += row_shifts[r]\n            idx += cnt\n        # center pattern in the unit square (before applying dx,dy) so translations are meaningful\n        lo = coords.min(axis=0); hi = coords.max(axis=0)\n        trans = 0.5 - 0.5 * (lo + hi)\n        coords = coords + trans\n        # apply small translation and clip to margin inside square\n        coords = coords + np.array([dx, dy])\n        coords = np.clip(coords, margin, 1.0 - margin)\n        return coords\n\n    # Coarse search ranges (deterministic)\n    # horizontal spacing range chosen to allow tight packing but avoid overlaps that force tiny radii\n    s_h_vals = np.linspace(0.095, 0.185, 20)      # 20 values\n    v_ratio_vals = np.linspace(0.94, 1.06, 7)     # vertical anisotropy multiplier\n    shift_fracs = [-0.045, 0.0, 0.045]            # absolute row shifts (will be scaled below)\n    # We'll interpret these as fractions of s_h (so shift = frac * s_h)\n    # small translation candidates (to nudge cluster into corners/edges)\n    trans_grid = [(0.0, 0.0),\n                  (-0.002, 0.0), (0.002, 0.0),\n                  (0.0, -0.002), (0.0, 0.002),\n                  (-0.002, -0.002), (0.002, 0.002)]\n\n    best_sum = -1.0\n    best_params = None\n    best_centers = None\n    best_radii = None\n\n    # Pre-generate all combinations of per-row shift fractions (3^5 = 243)\n    all_shift_combos = list(product(shift_fracs, repeat=len(rows)))\n\n    # Coarse grid search\n    for s_h in s_h_vals:\n        for v_ratio in v_ratio_vals:\n            # compute row shifts in absolute units = frac * s_h\n            for frac_combo in all_shift_combos:\n                row_shifts = np.array(frac_combo) * s_h\n                # try a few translations too\n                for dx, dy in trans_grid:\n                    centers = build_centers(s_h, v_ratio, row_shifts, dx=dx, dy=dy)\n                    r = compute_radii(centers, margin=1e-9)\n                    ssum = float(r.sum())\n                    if ssum > best_sum + 1e-12:\n                        best_sum = ssum\n                        best_params = (s_h, v_ratio, row_shifts.copy(), dx, dy)\n                        best_centers = centers.copy()\n                        best_radii = r.copy()\n\n    # Deterministic coordinate-descent refinement around best found coarse params\n    if best_params is None:\n        # fallback: place a modestly scaled base centered\n        s_h = 0.12\n        v_ratio = 1.0\n        row_shifts = np.zeros(len(rows))\n        best_centers = build_centers(s_h, v_ratio, row_shifts)\n        best_radii = compute_radii(best_centers)\n        best_sum = float(best_radii.sum())\n        best_params = (s_h, v_ratio, row_shifts.copy(), 0.0, 0.0)\n\n    # unpack\n    cur_s_h, cur_v_ratio, cur_row_shifts, cur_dx, cur_dy = best_params\n\n    # refinement schedule: progressively smaller step sizes\n    s_h_steps = [0.012, 0.006, 0.003, 0.0015]\n    v_ratio_steps = [0.03, 0.015, 0.007, 0.0035]\n    row_shift_steps = [0.03 * cur_s_h, 0.015 * cur_s_h, 0.007 * cur_s_h, 0.003 * cur_s_h]\n    trans_steps = [0.004, 0.002, 0.001, 0.0005]\n\n    # deterministic local search: try increasing/decreasing each parameter if improves sum\n    for level in range(len(s_h_steps)):\n        improved = True\n        iter_count = 0\n        while improved and iter_count < 6:\n            improved = False\n            iter_count += 1\n            # try s_h adjustments\n            for dx_mult in (-1, 1):\n                cand_s_h = cur_s_h + dx_mult * s_h_steps[level]\n                if cand_s_h <= 0.04 or cand_s_h >= 0.35:\n                    continue\n                centers = build_centers(cand_s_h, cur_v_ratio, cur_row_shifts, cur_dx, cur_dy)\n                r = compute_radii(centers)\n                ssum = float(r.sum())\n                if ssum > best_sum + 1e-12:\n                    best_sum = ssum\n                    best_centers = centers.copy()\n                    best_radii = r.copy()\n                    cur_s_h = cand_s_h\n                    improved = True\n            # try v_ratio adjustments\n            for dv in (-1, 1):\n                cand_v = cur_v_ratio + dv * v_ratio_steps[level]\n                if cand_v <= 0.7 or cand_v >= 1.4:\n                    continue\n                centers = build_centers(cur_s_h, cand_v, cur_row_shifts, cur_dx, cur_dy)\n                r = compute_radii(centers)\n                ssum = float(r.sum())\n                if ssum > best_sum + 1e-12:\n                    best_sum = ssum\n                    best_centers = centers.copy()\n                    best_radii = r.copy()\n                    cur_v_ratio = cand_v\n                    improved = True\n            # try per-row shift adjustments (each row independently)\n            for r_idx in range(len(rows)):\n                for dir_mult in (-1, 1):\n                    cand_row_shifts = cur_row_shifts.copy()\n                    step = row_shift_steps[level]\n                    cand_row_shifts[r_idx] += dir_mult * step\n                    centers = build_centers(cur_s_h, cur_v_ratio, cand_row_shifts, cur_dx, cur_dy)\n                    r = compute_radii(centers)\n                    ssum = float(r.sum())\n                    if ssum > best_sum + 1e-12:\n                        best_sum = ssum\n                        best_centers = centers.copy()\n                        best_radii = r.copy()\n                        cur_row_shifts = cand_row_shifts\n                        improved = True\n            # try translations\n            for tx_mult, ty_mult in product((-1, 0, 1), repeat=2):\n                if tx_mult == 0 and ty_mult == 0:\n                    continue\n                cand_dx = cur_dx + tx_mult * trans_steps[level]\n                cand_dy = cur_dy + ty_mult * trans_steps[level]\n                centers = build_centers(cur_s_h, cur_v_ratio, cur_row_shifts, cand_dx, cand_dy)\n                r = compute_radii(centers)\n                ssum = float(r.sum())\n                if ssum > best_sum + 1e-12:\n                    best_sum = ssum\n                    best_centers = centers.copy()\n                    best_radii = r.copy()\n                    cur_dx, cur_dy = cand_dx, cand_dy\n                    improved = True\n\n    # Final tiny nudges deterministically (very small translations)\n    for dx_try in (-5e-4, 0.0, 5e-4):\n        for dy_try in (-5e-4, 0.0, 5e-4):\n            centers = build_centers(cur_s_h, cur_v_ratio, cur_row_shifts, cur_dx + dx_try, cur_dy + dy_try)\n            r = compute_radii(centers)\n            ssum = float(r.sum())\n            if ssum > best_sum + 1e-12:\n                best_sum = ssum\n                best_centers = centers.copy()\n                best_radii = r.copy()\n                cur_dx += dx_try; cur_dy += dy_try\n\n    # Safety clamp if any negative or zero radii\n    if best_radii is None:\n        best_centers = build_centers(0.12, 1.0, np.zeros(len(rows)))\n        best_radii = compute_radii(best_centers)\n\n    return np.array(best_centers), np.array(best_radii), float(best_radii.sum())\n\n# EVOLVE-BLOCK-END\n\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.3055), Performs well on target_ratio (0.8750), Performs well on combined_score (0.8750), Performs well on eval_time (8.0680)\n\n### Program 2 (Score: 0.8750)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for 26 circles in the unit square.\nPattern: compact near-hex cluster rows [5,5,6,5,5]. Coarse grid search over\nhorizontal spacing and vertical anisotropy, per-row shifts and small\ntranslations, followed by a deterministic coordinate-descent refinement.\nRadii are exact maxima for fixed centers: r_i = min(dist_to_border, 0.5*nearest).\n\"\"\"\nimport numpy as np\nfrom itertools import product\n\ndef construct_packing():\n    n = 26\n    rows = [5, 5, 6, 5, 5]\n    vstep_hex = np.sqrt(3.0) / 2.0\n\n    # build integer-layout base coordinates (centered in base units)\n    base = []\n    for r_idx, cnt in enumerate(rows):\n        xs = np.arange(cnt) - (cnt - 1) / 2.0\n        x_offset = 0.5 * (r_idx % 2)\n        y = r_idx * vstep_hex\n        for x in xs:\n            base.append([x + x_offset, y])\n    base = np.array(base)\n\n    def compute_radii(centers, margin=0.0):\n        # distance to border (with optional margin)\n        db = np.minimum.reduce([centers[:, 0] - margin,\n                                centers[:, 1] - margin,\n                                1.0 - margin - centers[:, 0],\n                                1.0 - margin - centers[:, 1]])\n        dif = centers[:, None, :] - centers[None, :, :]\n        D = np.sqrt(np.maximum(0.0, (dif ** 2).sum(axis=2)))\n        np.fill_diagonal(D, np.inf)\n        nearest = D.min(axis=1)\n        radii = np.minimum(db, 0.5 * nearest)\n        return np.maximum(radii, 0.0)\n\n    # build centers from parameters\n    def build_centers(s_h, v_ratio, row_shifts, dx=0.0, dy=0.0, margin=1e-9):\n        s_v = v_ratio * vstep_hex * s_h\n        coords = base.copy()\n        coords[:, 0] = coords[:, 0] * s_h\n        coords[:, 1] = coords[:, 1] * (s_v / vstep_hex)\n        idx = 0\n        for r, cnt in enumerate(rows):\n            inds = slice(idx, idx + cnt)\n            coords[inds, 0] += row_shifts[r]\n            idx += cnt\n        lo = coords.min(axis=0); hi = coords.max(axis=0)\n        trans = 0.5 - 0.5 * (lo + hi)\n        coords = coords + trans\n        coords = coords + np.array([dx, dy])\n        coords = np.clip(coords, margin, 1.0 - margin)\n        return coords\n\n    # coarse parameter grids\n    s_h_vals = np.linspace(0.095, 0.185, 20)\n    v_ratio_vals = np.linspace(0.94, 1.06, 7)\n    shift_fracs = [-0.045, 0.0, 0.045]  # fractions of s_h\n    trans_grid = [(0.0, 0.0), (-0.002, 0.0), (0.002, 0.0), (0.0, -0.002), (0.0, 0.002),\n                  (-0.002, -0.002), (0.002, 0.002)]\n\n    best_sum = -1.0\n    best_params = None\n    best_centers = None\n    best_radii = None\n\n    all_shift_combos = list(product(shift_fracs, repeat=len(rows)))\n\n    for s_h in s_h_vals:\n        for v_ratio in v_ratio_vals:\n            for frac_combo in all_shift_combos:\n                row_shifts = np.array(frac_combo) * s_h\n                for dx, dy in trans_grid:\n                    centers = build_centers(s_h, v_ratio, row_shifts, dx=dx, dy=dy)\n                    r = compute_radii(centers, margin=1e-9)\n                    ssum = float(r.sum())\n                    if ssum > best_sum + 1e-12:\n                        best_sum = ssum\n                        best_params = (s_h, v_ratio, row_shifts.copy(), dx, dy)\n                        best_centers = centers.copy()\n                        best_radii = r.copy()\n\n    # fallback\n    if best_params is None:\n        s_h = 0.12\n        v_ratio = 1.0\n        row_shifts = np.zeros(len(rows))\n        best_centers = build_centers(s_h, v_ratio, row_shifts)\n        best_radii = compute_radii(best_centers)\n        best_sum = float(best_radii.sum())\n        best_params = (s_h, v_ratio, row_shifts.copy(), 0.0, 0.0)\n\n    cur_s_h, cur_v_ratio, cur_row_shifts, cur_dx, cur_dy = best_params\n\n    # refinement schedule (coarse -> fine)\n    s_h_steps = [0.012, 0.006, 0.003, 0.0015]\n    v_ratio_steps = [0.03, 0.015, 0.007, 0.0035]\n    row_shift_steps = [0.03 * cur_s_h, 0.015 * cur_s_h, 0.007 * cur_s_h, 0.003 * cur_s_h]\n    trans_steps = [0.004, 0.002, 0.001, 0.0005]\n\n    for level in range(len(s_h_steps)):\n        improved = True\n        iter_count = 0\n        while improved and iter_count < 6:\n            improved = False\n            iter_count += 1\n            # s_h adjustments\n            for dx_mult in (-1, 1):\n                cand_s_h = cur_s_h + dx_mult * s_h_steps[level]\n                if cand_s_h <= 0.04 or cand_s_h >= 0.35:\n                    continue\n                centers = build_centers(cand_s_h, cur_v_ratio, cur_row_shifts, cur_dx, cur_dy)\n                r = compute_radii(centers)\n                ssum = float(r.sum())\n                if ssum > best_sum + 1e-12:\n                    best_sum = ssum; best_centers = centers.copy(); best_radii = r.copy()\n                    cur_s_h = cand_s_h; improved = True\n            # v_ratio adjustments\n            for dv in (-1, 1):\n                cand_v = cur_v_ratio + dv * v_ratio_steps[level]\n                if cand_v <= 0.7 or cand_v >= 1.4:\n                    continue\n                centers = build_centers(cur_s_h, cand_v, cur_row_shifts, cur_dx, cur_dy)\n                r = compute_radii(centers)\n                ssum = float(r.sum())\n                if ssum > best_sum + 1e-12:\n                    best_sum = ssum; best_centers = centers.copy(); best_radii = r.copy()\n                    cur_v_ratio = cand_v; improved = True\n            # per-row shifts\n            for r_idx in range(len(rows)):\n                for dir_mult in (-1, 1):\n                    cand_row_shifts = cur_row_shifts.copy()\n                    step = row_shift_steps[level]\n                    cand_row_shifts[r_idx] += dir_mult * step\n                    centers = build_centers(cur_s_h, cur_v_ratio, cand_row_shifts, cur_dx, cur_dy)\n                    r = compute_radii(centers)\n                    ssum = float(r.sum())\n                    if ssum > best_sum + 1e-12:\n                        best_sum = ssum; best_centers = centers.copy(); best_radii = r.copy()\n                        cur_row_shifts = cand_row_shifts; improved = True\n            # translations\n            for tx_mult, ty_mult in product((-1, 0, 1), repeat=2):\n                if tx_mult == 0 and ty_mult == 0:\n                    continue\n                cand_dx = cur_dx + tx_mult * trans_steps[level]\n                cand_dy = cur_dy + ty_mult * trans_steps[level]\n                centers = build_centers(cur_s_h, cur_v_ratio, cur_row_shifts, cand_dx, cand_dy)\n                r = compute_radii(centers)\n                ssum = float(r.sum())\n                if ssum > best_sum + 1e-12:\n                    best_sum = ssum; best_centers = centers.copy(); best_radii = r.copy()\n                    cur_dx, cur_dy = cand_dx, cand_dy; improved = True\n\n    # tiny final nudges\n    for dx_try in (-5e-4, 0.0, 5e-4):\n        for dy_try in (-5e-4, 0.0, 5e-4):\n            centers = build_centers(cur_s_h, cur_v_ratio, cur_row_shifts, cur_dx + dx_try, cur_dy + dy_try)\n            r = compute_radii(centers)\n            ssum = float(r.sum())\n            if ssum > best_sum + 1e-12:\n                best_sum = ssum; best_centers = centers.copy(); best_radii = r.copy()\n                cur_dx += dx_try; cur_dy += dy_try\n\n    if best_radii is None:\n        best_centers = build_centers(0.12, 1.0, np.zeros(len(rows)))\n        best_radii = compute_radii(best_centers)\n\n    return np.array(best_centers), np.array(best_radii), float(best_radii.sum())\n# EVOLVE-BLOCK-END\n\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.3055), Performs well on target_ratio (0.8750), Performs well on combined_score (0.8750), Performs well on eval_time (7.8949)\n\n### Program 3 (Score: 0.8623)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for 26 circles in the unit square.\nUses a compact near-hex cluster (rows 5+5+6+5+5), a coarse uniform-scale\nsweep to find a tight global fit, then small anisotropic + per-row-shift\nrefinements and tiny translations. Radii are r_i = min(dist_to_border, 0.5*nearest_center).\n\"\"\"\nimport numpy as np\nfrom itertools import product\n\ndef construct_packing():\n    n = 26\n    rows = [5, 5, 6, 5, 5]  # sums to 26\n    vstep = np.sqrt(3.0) / 2.0\n\n    # Build canonical base hex-like coordinates (unit horizontal spacing)\n    base = []\n    for r_idx, cnt in enumerate(rows):\n        xs = np.arange(cnt) - (cnt - 1) / 2.0\n        x_off = 0.5 * (r_idx % 2)\n        y = r_idx * vstep\n        for x in xs:\n            base.append([x + x_off, y])\n    base = np.array(base)\n    assert base.shape[0] == n\n\n    # bounding extents of base\n    bmin = base.min(axis=0)\n    bmax = base.max(axis=0)\n    width, height = (bmax - bmin)\n    margin = 1e-6\n    # maximum uniform scale so pattern can fit inside unit square\n    Smax = min((1 - 2 * margin) / max(width, 1e-12), (1 - 2 * margin) / max(height, 1e-12))\n\n    def compute_radii(centers):\n        # distance to border\n        db = np.minimum.reduce([centers[:, 0], centers[:, 1], 1 - centers[:, 0], 1 - centers[:, 1]])\n        # pairwise distances (vectorized)\n        dif = centers[:, None, :] - centers[None, :, :]\n        D = np.sqrt(np.maximum(0.0, (dif ** 2).sum(axis=2)))\n        np.fill_diagonal(D, np.inf)\n        nearest = D.min(axis=1)\n        r = np.minimum(db, 0.5 * nearest)\n        return np.maximum(r, 0.0)\n\n    # center scaled pattern in unit square\n    def place_scaled(sx, sy):\n        scaled = base.copy()\n        scaled[:, 0] = (scaled[:, 0] - bmin[0]) * sx\n        scaled[:, 1] = (scaled[:, 1] - bmin[1]) * sy / vstep\n        lo = scaled.min(axis=0); hi = scaled.max(axis=0)\n        trans = 0.5 - 0.5 * (lo + hi)\n        centers = scaled + trans\n        # clip tiny epsilon\n        return np.clip(centers, margin, 1 - margin)\n\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    # Coarse uniform scale sweep (cover a good fraction of Smax)\n    for alpha in np.linspace(0.45, 1.02, 61):\n        s = alpha * Smax\n        centers = place_scaled(s, s)\n        radii = compute_radii(centers)\n        ssum = float(radii.sum())\n        if ssum > best_sum:\n            best_sum = ssum; best_centers = centers.copy(); best_radii = radii.copy()\n            best_s = s\n\n    # Per-row index lists\n    row_inds = []\n    idx = 0\n    for cnt in rows:\n        row_inds.append(list(range(idx, idx + cnt)))\n        idx += cnt\n\n    # Small anisotropic tweaks and per-row horizontal shifts to break symmetry\n    shift_fracs = [-0.035, 0.0, 0.035]\n    tx_vals = [0.997, 1.0, 1.003]\n    ty_vals = [0.997, 1.0, 1.003]\n    translations = [(0.0, 0.0), (-1e-3, 0.0), (1e-3, 0.0), (0.0, -1e-3), (0.0, 1e-3)]\n\n    for tx, ty in product(tx_vals, ty_vals):\n        sx = best_s * tx\n        sy = best_s * ty\n        base_scaled = place_scaled(sx, sy)  # already centered\n        for shifts in product(shift_fracs, repeat=len(rows)):\n            centers = base_scaled.copy()\n            # apply per-row horizontal shifts (fractions of best_s)\n            for r, frac in enumerate(shifts):\n                centers[row_inds[r], 0] += frac * best_s\n            # recenter after shifts\n            lo = centers.min(axis=0); hi = centers.max(axis=0)\n            trans = 0.5 - 0.5 * (lo + hi)\n            centers = np.clip(centers + trans, margin, 1 - margin)\n            # small translations tries\n            for dx, dy in translations:\n                C = np.clip(centers + np.array([dx, dy]), margin, 1 - margin)\n                radii = compute_radii(C)\n                ssum = float(radii.sum())\n                if ssum > best_sum + 1e-12:\n                    best_sum = ssum; best_centers = C.copy(); best_radii = radii.copy()\n\n    # Deterministic tiny coordinate-descent refinement on translations only\n    trans_steps = [5e-4, 2e-4, 8e-5]\n    cur_centers = best_centers.copy()\n    cur_radii = best_radii.copy()\n    cur_sum = best_sum\n    for step in trans_steps:\n        improved = True\n        while improved:\n            improved = False\n            for dx, dy in [(0,0), (step,0), (-step,0), (0,step), (0,-step)]:\n                C = np.clip(cur_centers + np.array([dx, dy]), margin, 1 - margin)\n                r = compute_radii(C)\n                ssum = float(r.sum())\n                if ssum > cur_sum + 1e-12:\n                    cur_sum = ssum; cur_centers = C.copy(); cur_radii = r.copy(); improved = True\n    # adopt refined result\n    best_centers = cur_centers\n    best_radii = cur_radii\n    best_sum = float(cur_radii.sum())\n\n    # Safety fallback if something went wrong\n    if best_centers is None:\n        best_centers = place_scaled(0.12, 0.12)\n        best_radii = compute_radii(best_centers)\n        best_sum = float(best_radii.sum())\n\n    return np.array(best_centers), np.array(best_radii), float(best_sum)\n\n# EVOLVE-BLOCK-END\n\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\nif __name__ == \"__main__\":\n    c, r, s = run_packing()\n    print(f\"Sum of radii: {s:.6f}\")\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.2722), Performs well on target_ratio (0.8623), Performs well on combined_score (0.8623), Performs well on eval_time (0.3676)\n\n\n\n## Diverse Programs\n\n### Program D1 (Score: 0.8472)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for packing 26 circles in the unit square.\n\nUses a compact near-hex cluster (rows [5,5,6,5,5]), a vectorized radius\ncomputation r_i = min(dist_to_border, 0.5*nearest_neighbor), a coarse scale\nsweep to find a strong global fit, and small anisotropic + per-row-shift\nrefinements to break symmetry and improve edge utilization.\n\"\"\"\nimport numpy as np\nfrom itertools import product\n\ndef construct_packing():\n    n = 26\n    rows = [5, 5, 6, 5, 5]  # sums to 26\n    vstep = np.sqrt(3.0) / 2.0\n\n    # build base integer-layout coords (unit horizontal spacing)\n    base = []\n    for r_idx, cnt in enumerate(rows):\n        xs = np.arange(cnt) - (cnt - 1) / 2.0\n        x_offset = 0.5 * (r_idx % 2)\n        for xi in xs:\n            base.append([xi + x_offset, r_idx * vstep])\n    base = np.array(base)\n    assert base.shape[0] == n\n\n    # bounding box of base pattern\n    minb = base.min(axis=0)\n    maxb = base.max(axis=0)\n    width = maxb[0] - minb[0]\n    height = maxb[1] - minb[1]\n\n    # small margin to avoid exact-border degeneracies\n    margin = 1e-6\n    Smax = min((1 - 2 * margin) / width, (1 - 2 * margin) / height)\n\n    def compute_radii(centers):\n        # distance to border\n        d_border = np.minimum.reduce([centers[:, 0], centers[:, 1], 1 - centers[:, 0], 1 - centers[:, 1]])\n        # pairwise distances\n        dif = centers[:, None, :] - centers[None, :, :]\n        D = np.sqrt(np.maximum(0.0, (dif ** 2).sum(axis=2)))\n        np.fill_diagonal(D, np.inf)\n        nearest = D.min(axis=1)\n        radii = np.minimum(d_border, 0.5 * nearest)\n        return np.maximum(radii, 0.0)\n\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n    best_s = None\n\n    # Coarse uniform scale sweep (cover a wide fraction of Smax)\n    for alpha in np.linspace(0.42, 1.00, 61):\n        s = alpha * Smax\n        scaled = (base - minb) * s\n        # center cluster inside unit square\n        translate = 0.5 - 0.5 * (scaled.min(axis=0) + scaled.max(axis=0))\n        centers = scaled + translate\n        centers = np.clip(centers, margin, 1 - margin)\n        radii = compute_radii(centers)\n        ssum = float(radii.sum())\n        if ssum > best_sum:\n            best_sum = ssum\n            best_centers = centers.copy()\n            best_radii = radii.copy()\n            best_s = s\n\n    # Refinement: small anisotropic scaling around best_s and per-row horizontal shifts\n    shift_fracs = [-0.04, 0.0, 0.04]\n    tx_vals = [0.996, 1.0, 1.004]\n    ty_vals = [0.996, 1.0, 1.004]\n\n    # Precompute row indices\n    row_indices = []\n    idx = 0\n    for cnt in rows:\n        row_indices.append(list(range(idx, idx + cnt)))\n        idx += cnt\n\n    if best_centers is not None:\n        for tx, ty in product(tx_vals, ty_vals):\n            s_x = best_s * tx\n            # Keep vertical spacing consistent with hex geometry\n            s_y = best_s * ty\n            anis_base = base.copy()\n            anis_base[:, 0] = (anis_base[:, 0] - minb[0]) * (s_x)\n            anis_base[:, 1] = (anis_base[:, 1] - minb[1]) * (s_y / vstep)\n            # try all small per-row shifts (3^5 = 243 combos)\n            for shifts in product(shift_fracs, repeat=len(rows)):\n                centers = anis_base.copy()\n                for r, shift_frac in enumerate(shifts):\n                    inds = row_indices[r]\n                    centers[inds, 0] += shift_frac * best_s\n                # center & clip\n                translate = 0.5 - 0.5 * (centers.min(axis=0) + centers.max(axis=0))\n                centers = np.clip(centers + translate, margin, 1 - margin)\n                radii = compute_radii(centers)\n                ssum = float(radii.sum())\n                if ssum > best_sum:\n                    best_sum = ssum\n                    best_centers = centers.copy()\n                    best_radii = radii.copy()\n\n    # Final tiny nudges to potentially snug pattern against walls\n    for dx, dy in [(0.0, 0.0), (-1e-3, 0.0), (1e-3, 0.0), (0.0, -1e-3), (0.0, 1e-3)]:\n        centers = np.clip(best_centers + np.array([dx, dy]), margin, 1 - margin)\n        radii = compute_radii(centers)\n        ssum = float(radii.sum())\n        if ssum > best_sum:\n            best_sum = ssum\n            best_centers = centers.copy()\n            best_radii = radii.copy()\n\n    # Safety fallback (should not happen)\n    if best_centers is None:\n        scaled = (base - minb) * (Smax * 0.5)\n        translate = 0.5 - 0.5 * (scaled.min(axis=0) + scaled.max(axis=0))\n        best_centers = np.clip(scaled + translate, margin, 1 - margin)\n        best_radii = compute_radii(best_centers)\n        best_sum = float(best_radii.sum())\n\n    return np.array(best_centers), np.array(best_radii), float(best_radii.sum())\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nKey features: Alternative approach to validity, Alternative approach to sum_radii\n\n### Program D2 (Score: 0.8471)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for 26 circles in the unit square.\nImproved explicit pattern: compact near-hex cluster (rows 5+5+6+5+5),\ncoarse uniform scale sweep to find a tight fit, followed by small\nanisotropic, per-row-shift and translation refinements. Radii are\nassigned as r_i = min(distance-to-border, 0.5*nearest-center-distance).\nThis keeps the constructor simple, deterministic and fast.\n\"\"\"\nimport numpy as np\nfrom itertools import product\n\ndef construct_packing():\n    n = 26\n    # Use the high-performing row pattern seen in top solutions\n    rows = [5, 5, 6, 5, 5]  # sums to 26\n    vstep = np.sqrt(3.0) / 2.0\n\n    # Build a canonical unit-spaced hex-like base layout (not yet scaled)\n    base = []\n    for r_idx, cnt in enumerate(rows):\n        xs = np.arange(cnt) - (cnt - 1) / 2.0\n        x_offset = 0.5 * (r_idx % 2)\n        y = r_idx * vstep\n        for x in xs:\n            base.append([x + x_offset, y])\n    base = np.array(base)  # (26,2)\n\n    # compute bounding extents of base\n    minb = base.min(axis=0)\n    maxb = base.max(axis=0)\n    width = maxb[0] - minb[0]\n    height = maxb[1] - minb[1]\n\n    margin = 1e-6  # tiny margin to avoid exact border-touch\n    # maximum uniform scale to ensure the pattern can fit inside the unit square\n    Smax = min((1 - 2 * margin) / width, (1 - 2 * margin) / height)\n\n    def compute_radii(centers):\n        # distance to border\n        db = np.minimum.reduce([centers[:, 0], centers[:, 1], 1 - centers[:, 0], 1 - centers[:, 1]])\n        # pairwise distances (vectorized)\n        dif = centers[:, None, :] - centers[None, :, :]\n        D = np.sqrt(np.maximum(0.0, (dif ** 2).sum(axis=2)))\n        np.fill_diagonal(D, np.inf)\n        nearest = D.min(axis=1)\n        radii = np.minimum(db, 0.5 * nearest)\n        # ensure non-negative\n        return np.maximum(radii, 0.0)\n\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    # Coarse uniform scale sweep (explore slightly beyond Smax with clipping)\n    for alpha in np.linspace(0.45, 1.02, 61):\n        s = alpha * Smax\n        # scale base and center it in unit square\n        scaled = (base - minb) * s\n        lo = scaled.min(axis=0); hi = scaled.max(axis=0)\n        trans = 0.5 - 0.5 * (lo + hi)\n        centers = scaled + trans\n        centers = np.clip(centers, margin, 1 - margin)\n        r = compute_radii(centers)\n        ssum = float(r.sum())\n        if ssum > best_sum:\n            best_sum = ssum\n            best_centers = centers.copy()\n            best_radii = r.copy()\n\n    # Local refinements: anisotropic scaling, small per-row horizontal shifts,\n    # and tiny translations to let the cluster \"snug\" against edges.\n    if best_centers is None:\n        # fallback\n        best_centers = np.clip((base - minb) * (0.14) + 0.5, margin, 1 - margin)\n        best_radii = compute_radii(best_centers)\n        best_sum = float(best_radii.sum())\n\n    # Prepare per-row indices for applying row shifts\n    row_indices = []\n    idx = 0\n    for cnt in rows:\n        row_indices.append(list(range(idx, idx + cnt)))\n        idx += cnt\n\n    # small sets of anisotropic multipliers (around 1.0) and shift fractions\n    tx_vals = [0.997, 1.0, 1.003]\n    ty_vals = [0.997, 1.0, 1.003]\n    shift_fracs = [-0.035, 0.0, 0.035]  # fractions of Smax for horizontal shifts\n    translations = [(-0.002, 0.0), (0.0, -0.002), (0.0, 0.0), (0.002, 0.0), (0.0, 0.002)]\n\n    base_coords = base.copy()\n    # We'll apply anisotropic scaling relative to the canonical base bounding box\n    for tx, ty in product(tx_vals, ty_vals):\n        sx = tx * Smax\n        sy = ty * Smax * (vstep) / (vstep)  # keep consistent vertical unit (explicit)\n        # scale base coordinates\n        scaled = base_coords.copy()\n        scaled[:, 0] = (scaled[:, 0] - minb[0]) * sx\n        scaled[:, 1] = (scaled[:, 1] - minb[1]) * sy / vstep\n        # try per-row horizontal shifts combinations (3^5 = 243)\n        for shifts in product(shift_fracs, repeat=len(rows)):\n            centers = scaled.copy()\n            for r, frac in enumerate(shifts):\n                inds = row_indices[r]\n                centers[inds, 0] += frac * Smax\n            # center and try small translations\n            lo = centers.min(axis=0); hi = centers.max(axis=0)\n            trans = 0.5 - 0.5 * (lo + hi)\n            centers = centers + trans\n            for dx, dy in translations:\n                C = np.clip(centers + np.array([dx, dy]), margin, 1 - margin)\n                r = compute_radii(C)\n                ssum = float(r.sum())\n                if ssum > best_sum:\n                    best_sum = ssum\n                    best_centers = C.copy()\n                    best_radii = r.copy()\n\n    # Final tiny nudges along each axis (deterministic small moves)\n    for dx in (-1e-3, 0.0, 1e-3):\n        for dy in (-1e-3, 0.0, 1e-3):\n            C = np.clip(best_centers + np.array([dx, dy]), margin, 1 - margin)\n            r = compute_radii(C)\n            ssum = float(r.sum())\n            if ssum > best_sum:\n                best_sum = ssum\n                best_centers = C.copy()\n                best_radii = r.copy()\n\n    return np.array(best_centers), np.array(best_radii), float(best_radii.sum())\n\n# EVOLVE-BLOCK-END\n\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(7,7))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect('equal')\n    for i,(c,r) in enumerate(zip(centers, radii)):\n        ax.add_patch(Circle(c,r,alpha=0.6,edgecolor='k'))\n        ax.text(c[0],c[1],str(i),ha='center',va='center',fontsize=8)\n    plt.title(f\"n={len(centers)}, sum={sum(radii):.6f}\"); plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # visualize(centers, radii)\n```\nKey features: Alternative approach to validity, Alternative approach to sum_radii\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 0.8750, Type: High-Performer)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for 26 circles in the unit square.\nImproved explicit pattern with a wider, deterministic search over\nhorizontal spacing, anisotropic vertical ratio, per-row shifts and\nsmall translations. Uses the exact maximal radii for fixed centers:\nr_i = min(dist_to_border, 0.5 * nearest_neighbor_distance).\n\nThis version does a coarse grid search and then a simple deterministic\ncoordinate-descent refinement to squeeze more total radius out of the\nsame near-hex cluster layout.\n\"\"\"\nimport numpy as np\nfrom itertools import product\n\ndef construct_packing():\n    n = 26\n    rows = [5, 5, 6, 5, 5]  # compact near-hex cluster\n    vstep_hex = np.sqrt(3.0) / 2.0\n\n    # base integer layout (centered about origin in base units)\n    base = []\n    for r_idx, cnt in enumerate(rows):\n        xs = np.arange(cnt) - (cnt - 1) / 2.0\n        x_offset = 0.5 * (r_idx % 2)\n        y = r_idx * vstep_hex\n        for x in xs:\n            base.append([x + x_offset, y])\n    base = np.array(base)  # (26,2)\n\n    # helper: compute radii for a given centers array\n    def compute_radii(centers, margin=0.0):\n        # distance to border (consider margin for safety)\n        db = np.minimum.reduce([centers[:, 0] - margin,\n                                centers[:, 1] - margin,\n                                1.0 - margin - centers[:, 0],\n                                1.0 - margin - centers[:, 1]])\n        # pairwise center distances\n        dif = centers[:, None, :] - centers[None, :, :]\n        D = np.sqrt(np.maximum(0.0, (dif ** 2).sum(axis=2)))\n        np.fill_diagonal(D, np.inf)\n        nearest = D.min(axis=1)\n        radii = np.minimum(db, 0.5 * nearest)\n        return np.maximum(radii, 0.0)\n\n    # Build centers from parameters: horizontal spacing s_h, vertical ratio r_v,\n    # per-row shifts (absolute units), and translation (dx,dy)\n    def build_centers(s_h, v_ratio, row_shifts, dx=0.0, dy=0.0, margin=1e-9):\n        s_v = v_ratio * vstep_hex * s_h  # absolute vertical step\n        coords = base.copy()\n        # scale: base horizontal units become s_h, vertical base units become s_v/vstep_hex\n        coords[:, 0] = coords[:, 0] * s_h\n        coords[:, 1] = coords[:, 1] * (s_v / vstep_hex)\n        # apply per-row shifts\n        idx = 0\n        for r, cnt in enumerate(rows):\n            inds = slice(idx, idx + cnt)\n            coords[inds, 0] += row_shifts[r]\n            idx += cnt\n        # center pattern in the unit square (before applying dx,dy) so translations are meaningful\n        lo = coords.min(axis=0); hi = coords.max(axis=0)\n        trans = 0.5 - 0.5 * (lo + hi)\n        coords = coords + trans\n        # apply small translation and clip to margin inside square\n        coords = coords + np.array([dx, dy])\n        coords = np.clip(coords, margin, 1.0 - margin)\n        return coords\n\n    # Coarse search ranges (deterministic)\n    # horizontal spacing range chosen to allow tight packing but avoid overlaps that force tiny radii\n    s_h_vals = np.linspace(0.095, 0.185, 20)      # 20 values\n    v_ratio_vals = np.linspace(0.94, 1.06, 7)     # vertical anisotropy multiplier\n    shift_fracs = [-0.045, 0.0, 0.045]            # absolute row shifts (will be scaled below)\n    # We'll interpret these as fractions of s_h (so shift = frac * s_h)\n    # small translation candidates (to nudge cluster into corners/edges)\n    trans_grid = [(0.0, 0.0),\n                  (-0.002, 0.0), (0.002, 0.0),\n                  (0.0, -0.002), (0.0, 0.002),\n                  (-0.002, -0.002), (0.002, 0.002)]\n\n    best_sum = -1.0\n    best_params = None\n    best_centers = None\n    best_radii = None\n\n    # Pre-generate all combinations of per-row shift fractions (3^5 = 243)\n    all_shift_combos = list(product(shift_fracs, repeat=len(rows)))\n\n    # Coarse grid search\n    for s_h in s_h_vals:\n        for v_ratio in v_ratio_vals:\n            # compute row shifts in absolute units = frac * s_h\n            for frac_combo in all_shift_combos:\n                row_shifts = np.array(frac_combo) * s_h\n                # try a few translations too\n                for dx, dy in trans_grid:\n                    centers = build_centers(s_h, v_ratio, row_shifts, dx=dx, dy=dy)\n                    r = compute_radii(centers, margin=1e-9)\n                    ssum = float(r.sum())\n                    if ssum > best_sum + 1e-12:\n                        best_sum = ssum\n                        best_params = (s_h, v_ratio, row_shifts.copy(), dx, dy)\n                        best_centers = centers.copy()\n                        best_radii = r.copy()\n\n    # Deterministic coordinate-descent refinement around best found coarse params\n    if best_params is None:\n        # fallback: place a modestly scaled base centered\n        s_h = 0.12\n        v_ratio = 1.0\n        row_shifts = np.zeros(len(rows))\n        best_centers = build_centers(s_h, v_ratio, row_shifts)\n        best_radii = compute_radii(best_centers)\n        best_sum = float(best_radii.sum())\n        best_params = (s_h, v_ratio, row_shifts.copy(), 0.0, 0.0)\n\n    # unpack\n    cur_s_h, cur_v_ratio, cur_row_shifts, cur_dx, cur_dy = best_params\n\n    # refinement schedule: progressively smaller step sizes\n    s_h_steps = [0.012, 0.006, 0.003, 0.0015]\n    v_ratio_steps = [0.03, 0.015, 0.007, 0.0035]\n    row_shift_steps = [0.03 * cur_s_h, 0.015 * cur_s_h, 0.007 * cur_s_h, 0.003 * cur_s_h]\n    trans_steps = [0.004, 0.002, 0.001, 0.0005]\n\n    # deterministic local search: try increasing/decreasing each parameter if improves sum\n    for level in range(len(s_h_steps)):\n        improved = True\n        iter_count = 0\n        while improved and iter_count < 6:\n            improved = False\n            iter_count += 1\n            # try s_h adjustments\n            for dx_mult in (-1, 1):\n                cand_s_h = cur_s_h + dx_mult * s_h_steps[level]\n                if cand_s_h <= 0.04 or cand_s_h >= 0.35:\n                    continue\n                centers = build_centers(cand_s_h, cur_v_ratio, cur_row_shifts, cur_dx, cur_dy)\n                r = compute_radii(centers)\n                ssum = float(r.sum())\n                if ssum > best_sum + 1e-12:\n                    best_sum = ssum\n                    best_centers = centers.copy()\n                    best_radii = r.copy()\n                    cur_s_h = cand_s_h\n                    improved = True\n            # try v_ratio adjustments\n            for dv in (-1, 1):\n                cand_v = cur_v_ratio + dv * v_ratio_steps[level]\n                if cand_v <= 0.7 or cand_v >= 1.4:\n                    continue\n                centers = build_centers(cur_s_h, cand_v, cur_row_shifts, cur_dx, cur_dy)\n                r = compute_radii(centers)\n                ssum = float(r.sum())\n                if ssum > best_sum + 1e-12:\n                    best_sum = ssum\n                    best_centers = centers.copy()\n                    best_radii = r.copy()\n                    cur_v_ratio = cand_v\n                    improved = True\n            # try per-row shift adjustments (each row independently)\n            for r_idx in range(len(rows)):\n                for dir_mult in (-1, 1):\n                    cand_row_shifts = cur_row_shifts.copy()\n                    step = row_shift_steps[level]\n                    cand_row_shifts[r_idx] += dir_mult * step\n                    centers = build_centers(cur_s_h, cur_v_ratio, cand_row_shifts, cur_dx, cur_dy)\n                    r = compute_radii(centers)\n                    ssum = float(r.sum())\n                    if ssum > best_sum + 1e-12:\n                        best_sum = ssum\n                        best_centers = centers.copy()\n                        best_radii = r.copy()\n                        cur_row_shifts = cand_row_shifts\n                        improved = True\n            # try translations\n            for tx_mult, ty_mult in product((-1, 0, 1), repeat=2):\n                if tx_mult == 0 and ty_mult == 0:\n                    continue\n                cand_dx = cur_dx + tx_mult * trans_steps[level]\n                cand_dy = cur_dy + ty_mult * trans_steps[level]\n                centers = build_centers(cur_s_h, cur_v_ratio, cur_row_shifts, cand_dx, cand_dy)\n                r = compute_radii(centers)\n                ssum = float(r.sum())\n                if ssum > best_sum + 1e-12:\n                    best_sum = ssum\n                    best_centers = centers.copy()\n                    best_radii = r.copy()\n                    cur_dx, cur_dy = cand_dx, cand_dy\n                    improved = True\n\n    # Final tiny nudges deterministically (very small translations)\n    for dx_try in (-5e-4, 0.0, 5e-4):\n        for dy_try in (-5e-4, 0.0, 5e-4):\n            centers = build_centers(cur_s_h, cur_v_ratio, cur_row_shifts, cur_dx + dx_try, cur_dy + dy_try)\n            r = compute_radii(centers)\n            ssum = float(r.sum())\n            if ssum > best_sum + 1e-12:\n                best_sum = ssum\n                best_centers = centers.copy()\n                best_radii = r.copy()\n                cur_dx += dx_try; cur_dy += dy_try\n\n    # Safety clamp if any negative or zero radii\n    if best_radii is None:\n        best_centers = build_centers(0.12, 1.0, np.zeros(len(rows)))\n        best_radii = compute_radii(best_centers)\n\n    return np.array(best_centers), np.array(best_radii), float(best_radii.sum())\n\n# EVOLVE-BLOCK-END\n\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n```\nUnique approach: Modification: Full rewrite, Excellent validity (1.000), Excellent sum_radii (2.305)\n\n### Inspiration 2 (Score: 0.1275, Type: Exploratory)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (improved constructor)\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square\n    and compute maximal feasible radii (LP-like iterative tightening).\n    Returns: centers (26,2), radii (26,), sum_of_radii\n    \"\"\"\n    n = 26\n\n    # Hex-like layered layout but with row counts chosen to fit 26 circles:\n    rows = [5, 5, 6, 5, 5]  # sums to 26\n    max_cols = max(rows)\n\n    # Choose a spacing that fills the square reasonably densely without overlap\n    s = 0.192  # horizontal spacing (tuned)\n    sy = s * np.sqrt(3) / 2.0  # vertical spacing for hex/triangular packing\n\n    total_rows = len(rows)\n    y0 = (1.0 - (total_rows - 1) * sy) / 2.0  # center rows vertically\n\n    centers = np.zeros((n, 2))\n    idx = 0\n    for r_idx, count in enumerate(rows):\n        y = y0 + r_idx * sy\n        # center the row horizontally\n        x_start = (1.0 - (count - 1) * s) / 2.0\n        for k in range(count):\n            x = x_start + k * s\n            centers[idx] = [x, y]\n            idx += 1\n\n    # Ensure centers remain inside square numerically\n    centers = np.clip(centers, 1e-8, 1.0 - 1e-8)\n\n    # Compute maximal feasible radii using an LP-like fixpoint:\n    radii = compute_max_radii(centers)  # improved routine below\n    sum_radii = float(np.sum(radii))\n\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers, tol=1e-9, max_iter=2000):\n    \"\"\"\n    Compute maximal radii satisfying:\n      r_i >= 0, r_i <= dist_to_border_i, and r_i + r_j <= dist(i,j)\n    This uses an iterative tightening: start with border-limited radii and\n    repeatedly enforce pairwise constraints by decreasing any radius that\n    violates a constraint (setting it to the tightest allowable).\n    \"\"\"\n    n = centers.shape[0]\n    # distance to borders\n    d_border = np.minimum.reduce([centers[:, 0], centers[:, 1], 1.0 - centers[:, 0], 1.0 - centers[:, 1]])\n    radii = d_border.copy()\n\n    # pairwise distances\n    diffs = centers[:, None, :] - centers[None, :, :]\n    dists = np.sqrt(np.sum(diffs * diffs, axis=2))\n\n    # iterative tightening: for each circle i, r_i <= min(border_i, min_j max(0, d_ij - r_j))\n    # This only decreases radii and converges to a feasible point. It tends to give larger\n    # total sum than naive proportional downscaling because it preserves smaller radii.\n    it = 0\n    while it < max_iter:\n        it += 1\n        prev = radii.copy()\n        for i in range(n):\n            # compute tightest allowed from pairwise constraints given current r_j\n            # skip j==i\n            # allow small negative numerical tolerance but clamp to >=0\n            limits = d_border[i]\n            # compute d_ij - r_j for all j != i\n            vals = dists[i] - radii\n            # ignore self\n            vals[i] = np.inf\n            # allowable r_i must be <= each val (and >=0)\n            min_val = np.min(vals)\n            if min_val < limits:\n                limits = min_val\n            # clamp\n            if limits < 0.0:\n                limits = 0.0\n            # only decrease (monotone)\n            if limits < radii[i] - tol:\n                radii[i] = limits\n        # convergence check\n        if np.max(prev - radii) < 1e-12:\n            break\n\n    # Final safety: enforce all constraints (clamp small residual violations)\n    radii = np.maximum(radii, 0.0)\n    for i in range(n):\n        radii[i] = min(radii[i], d_border[i])\n    for i in range(n):\n        for j in range(i + 1, n):\n            if radii[i] + radii[j] > dists[i, j]:\n                # reduce the larger one to make equality (better for sum than scaling both)\n                if radii[i] >= radii[j]:\n                    radii[i] = max(0.0, dists[i, j] - radii[j])\n                else:\n                    radii[j] = max(0.0, dists[i, j] - radii[i])\n\n    return radii\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor='k')\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nUnique approach: Modification: Full rewrite, Excellent validity (1.000), Alternative target_ratio approach\n\n### Inspiration 3 (Score: 0.7852, Type: Alternative)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for packing 26 circles in the unit square.\nUses a compact hex-like cluster (rows 5+5+6+5+5), scales and slightly anisotropically\ntweaks it to maximize the sum of radii computed as min(dist_to_border, half nearest neighbor).\n\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    n = 26\n    rows = [5, 5, 6, 5, 5]  # compact hex-like cluster summing to 26\n\n    # build base hex lattice coordinates (unit horizontal spacing)\n    coords = []\n    vstep = np.sqrt(3) / 2.0\n    for r, cnt in enumerate(rows):\n        xs = np.arange(cnt) - (cnt - 1) / 2.0\n        x_offset = 0.5 * (r % 2)\n        y = r * vstep\n        for x in xs:\n            coords.append([x + x_offset, y])\n    base = np.array(coords)  # (26,2)\n\n    # normalize base to start at origin\n    minb = base.min(axis=0)\n    base -= minb\n    width, height = base.max(axis=0)\n\n    # margin to keep centers strictly inside unit square\n    margin = 0.004\n\n    # maximum uniform scale to fit in [margin,1-margin]\n    Smax = min((1 - 2 * margin) / (width if width > 0 else 1e-9),\n               (1 - 2 * margin) / (height if height > 0 else 1e-9))\n\n    def radii_for_centers(C):\n        # distance to border\n        d_border = np.minimum.reduce([C[:, 0], C[:, 1], 1 - C[:, 0], 1 - C[:, 1]])\n        # pairwise distances\n        dif = C[:, None, :] - C[None, :, :]\n        D = np.sqrt(np.maximum(0.0, (dif ** 2).sum(axis=2)))\n        np.fill_diagonal(D, np.inf)\n        d_near = D.min(axis=1)\n        r = np.minimum(d_border, 0.5 * d_near)\n        return np.maximum(r, 0.0)\n\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    # coarse search over uniform scale fraction\n    for alpha in np.linspace(0.36, 1.0, 66):\n        s = alpha * Smax\n        scaled = base * s\n        # center cluster in unit square\n        t = 0.5 - 0.5 * (scaled.min(axis=0) + scaled.max(axis=0))\n        centers = scaled + t\n        centers = np.clip(centers, margin, 1 - margin)\n        r = radii_for_centers(centers)\n        ssum = r.sum()\n        if ssum > best_sum:\n            best_sum = ssum\n            best_centers = centers.copy()\n            best_radii = r.copy()\n\n    # small anisotropic tweaks around best found\n    if best_centers is not None:\n        for tx in np.linspace(0.985, 1.015, 7):\n            for ty in np.linspace(0.985, 1.015, 7):\n                scaled = (base * np.array([tx, ty])) * (Smax * 0.995)\n                t = 0.5 - 0.5 * (scaled.min(axis=0) + scaled.max(axis=0))\n                centers = scaled + t\n                centers = np.clip(centers, margin, 1 - margin)\n                r = radii_for_centers(centers)\n                ssum = r.sum()\n                if ssum > best_sum:\n                    best_sum = ssum\n                    best_centers = centers.copy()\n                    best_radii = r.copy()\n\n    # fallback safety\n    if best_centers is None:\n        scaled = base * (Smax * 0.5)\n        t = 0.5 - 0.5 * (scaled.min(axis=0) + scaled.max(axis=0))\n        best_centers = np.clip(scaled + t, margin, 1 - margin)\n        best_radii = radii_for_centers(best_centers)\n\n    centers = np.array(best_centers, dtype=float)\n    radii = np.array(best_radii, dtype=float)\n    return centers, radii, float(radii.sum())\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.7, edgecolor=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Optional: visualize(centers, radii)\n```\nUnique approach: Modification: Full rewrite, Excellent validity (1.000), Excellent sum_radii (2.069)\n\n# Current Program\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for packing 26 circles in the unit square.\n\nBuilds a compact near-hex cluster (rows [5,5,6,5,5]), searches a uniform scale\nfraction of the maximal fit, and applies small anisotropic and per-row horizontal\nshifts to break symmetry and improve edge usage. Radii for fixed centers are\nr_i = min(dist_to_border, 0.5 * nearest_neighbor_distance).\n\"\"\"\nimport numpy as np\nfrom itertools import product\n\ndef construct_packing():\n    n = 26\n    # well-performing compact row pattern summing to 26\n    rows = [5, 5, 6, 5, 5]\n    vstep = np.sqrt(3.0) / 2.0\n\n    # build base integer lattice coords (unit horiz spacing)\n    base = []\n    for r_idx, cnt in enumerate(rows):\n        xs = np.arange(cnt) - (cnt - 1) / 2.0\n        x_offset = 0.5 * (r_idx % 2)\n        y = r_idx * vstep\n        for xi in xs:\n            base.append([xi + x_offset, y])\n    base = np.array(base)  # (26,2)\n    assert base.shape[0] == n\n\n    minb = base.min(axis=0)\n    maxb = base.max(axis=0)\n    width = maxb[0] - minb[0]\n    height = maxb[1] - minb[1]\n\n    margin = 1e-6\n    # maximum uniform scale so scaled pattern fits inside [margin, 1-margin]\n    Smax = min((1 - 2 * margin) / width, (1 - 2 * margin) / height)\n\n    def compute_radii(centers):\n        # distance to border\n        db = np.minimum.reduce([centers[:, 0], centers[:, 1], 1 - centers[:, 0], 1 - centers[:, 1]])\n        # pairwise distances (vectorized)\n        dif = centers[:, None, :] - centers[None, :, :]\n        D = np.sqrt(np.maximum(0.0, (dif ** 2).sum(axis=2)))\n        np.fill_diagonal(D, np.inf)\n        nearest = D.min(axis=1)\n        r = np.minimum(db, 0.5 * nearest)\n        return np.maximum(r, 0.0)\n\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n    best_s = None\n\n    # Coarse uniform scale sweep over a wide fraction of Smax\n    for alpha in np.linspace(0.45, 1.00, 61):\n        s = alpha * Smax\n        scaled = (base - minb) * s\n        # center cluster inside unit square\n        bbox_min = scaled.min(axis=0)\n        bbox_max = scaled.max(axis=0)\n        translate = 0.5 - 0.5 * (bbox_min + bbox_max)\n        centers = scaled + translate\n        centers = np.clip(centers, margin, 1 - margin)\n        radii = compute_radii(centers)\n        ssum = float(radii.sum())\n        if ssum > best_sum:\n            best_sum = ssum\n            best_centers = centers.copy()\n            best_radii = radii.copy()\n            best_s = s\n\n    # If nothing good found (unlikely), pick moderate scale\n    if best_centers is None:\n        best_s = 0.12\n        scaled = (base - minb) * best_s\n        bbox_min = scaled.min(axis=0); bbox_max = scaled.max(axis=0)\n        translate = 0.5 - 0.5 * (bbox_min + bbox_max)\n        best_centers = np.clip(scaled + translate, margin, 1 - margin)\n        best_radii = compute_radii(best_centers)\n        best_sum = float(best_radii.sum())\n\n    # Precompute row indices for per-row shifts\n    row_indices = []\n    idx = 0\n    for cnt in rows:\n        row_indices.append(list(range(idx, idx + cnt)))\n        idx += cnt\n\n    # Small anisotropic tweaks and limited per-row horizontal shifts to break symmetry\n    shift_fracs = [-0.03, 0.0, 0.03]  # fraction of best_s\n    tx_vals = [0.998, 1.0, 1.002]\n    ty_vals = [0.998, 1.0, 1.002]\n\n    for tx, ty in product(tx_vals, ty_vals):\n        s_x = best_s * tx\n        s_y = best_s * ty\n        anis_base = base.copy()\n        # scale so one horizontal base step -> s_x, vertical base unit (vstep) -> s_y\n        anis_base[:, 0] = (anis_base[:, 0] - minb[0]) * s_x\n        anis_base[:, 1] = (anis_base[:, 1] - minb[1]) * (s_y / vstep)\n        # try a modest subset of per-row shift combinations (3^5 = 243)\n        for shifts in product(shift_fracs, repeat=len(rows)):\n            centers = anis_base.copy()\n            for r, frac in enumerate(shifts):\n                if frac != 0.0:\n                    centers[row_indices[r], 0] += frac * best_s\n            # recenter and clip\n            cmin = centers.min(axis=0); cmax = centers.max(axis=0)\n            translate = 0.5 - 0.5 * (cmin + cmax)\n            centers = np.clip(centers + translate, margin, 1 - margin)\n            radii = compute_radii(centers)\n            # skip degenerate placements where any radius is extremely small\n            if np.any(radii < 1e-9):\n                continue\n            ssum = float(radii.sum())\n            if ssum > best_sum + 1e-12:\n                best_sum = ssum\n                best_centers = centers.copy()\n                best_radii = radii.copy()\n\n    # Small deterministic nudges to try to exploit edge contact\n    for dx, dy in [(0.0, 0.0), (-5e-4, 0.0), (5e-4, 0.0), (0.0, -5e-4), (0.0, 5e-4)]:\n        centers = np.clip(best_centers + np.array([dx, dy]), margin, 1 - margin)\n        radii = compute_radii(centers)\n        if np.any(radii < 1e-9):\n            continue\n        ssum = float(radii.sum())\n        if ssum > best_sum:\n            best_sum = ssum\n            best_centers = centers.copy()\n            best_radii = radii.copy()\n\n    return np.array(best_centers), np.array(best_radii), float(best_radii.sum())\n# EVOLVE-BLOCK-END\n\n# fixed runtime interface\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(7,7))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect('equal')\n    for i,(c,r) in enumerate(zip(centers,radii)):\n        ax.add_patch(Circle(c,r,alpha=0.6,edgecolor='k'))\n        ax.text(c[0], c[1], str(i), ha='center', va='center', fontsize=8)\n    plt.title(f\"n={len(centers)}, sum={sum(radii):.6f}\"); plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # visualize(centers, radii)\n```\n\n# Task\nRewrite the program to improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: complexity, diversity\nDifferent solutions with similar fitness but different features are valuable.\nProvide the complete new program code.\n\nIMPORTANT: Make sure your rewritten program maintains the same inputs and outputs\nas the original program, but with improved internal implementation.\n\n```python\n# Your rewritten program here\n```", "responses": ["```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for 26 circles in the unit square.\n\nPattern: compact near-hex cluster rows [5,5,6,5,5]. Coarse deterministic grid\nsearch over horizontal spacing and vertical anisotropy, try small per-row\nshifts and translations, then a short coordinate-descent refinement.\nRadii for fixed centers: r_i = min(dist_to_border, 0.5 * nearest_center_distance).\n\"\"\"\nimport numpy as np\nfrom itertools import product\n\ndef construct_packing():\n    n = 26\n    rows = [5, 5, 6, 5, 5]\n    vstep_hex = np.sqrt(3.0) / 2.0\n\n    # base integer-layout (centered in base units)\n    base = []\n    for r_idx, cnt in enumerate(rows):\n        xs = np.arange(cnt) - (cnt - 1) / 2.0\n        x_offset = 0.5 * (r_idx % 2)\n        y = r_idx * vstep_hex\n        for x in xs:\n            base.append([x + x_offset, y])\n    base = np.array(base)\n\n    def compute_radii(centers, margin=0.0):\n        # distance to border\n        db = np.minimum.reduce([centers[:,0]-margin, centers[:,1]-margin,\n                                1.0-margin-centers[:,0], 1.0-margin-centers[:,1]])\n        # pairwise distances\n        dif = centers[:, None, :] - centers[None, :, :]\n        D = np.sqrt(np.maximum(0.0, (dif**2).sum(axis=2)))\n        np.fill_diagonal(D, np.inf)\n        nearest = D.min(axis=1)\n        r = np.minimum(db, 0.5 * nearest)\n        return np.maximum(r, 0.0)\n\n    def build_centers(s_h, v_ratio, row_shifts, dx=0.0, dy=0.0, margin=1e-12):\n        s_v = v_ratio * vstep_hex * s_h\n        coords = base.copy()\n        coords[:,0] = coords[:,0] * s_h\n        coords[:,1] = coords[:,1] * (s_v / vstep_hex)\n        # apply per-row shifts\n        idx = 0\n        for r, cnt in enumerate(rows):\n            inds = slice(idx, idx + cnt)\n            coords[inds, 0] += row_shifts[r]\n            idx += cnt\n        # center pattern in unit square\n        lo = coords.min(axis=0); hi = coords.max(axis=0)\n        trans = 0.5 - 0.5 * (lo + hi)\n        coords = coords + trans + np.array([dx, dy])\n        return np.clip(coords, margin, 1.0 - margin)\n\n    # coarse grids (deterministic)\n    s_h_vals = np.linspace(0.095, 0.185, 18)\n    v_ratio_vals = np.linspace(0.94, 1.06, 7)\n    shift_fracs = [-0.045, 0.0, 0.045]   # interpreted as fractions of s_h\n    trans_grid = [(0.0,0.0), (-0.002,0.0), (0.002,0.0), (0.0,-0.002), (0.0,0.002),\n                  (-0.002,-0.002),(0.002,0.002)]\n\n    all_shift_combos = list(product(shift_fracs, repeat=len(rows)))\n\n    best_sum = -1.0\n    best_params = None\n    best_centers = None\n    best_radii = None\n\n    # coarse search\n    for s_h in s_h_vals:\n        for v_ratio in v_ratio_vals:\n            for frac_combo in all_shift_combos:\n                row_shifts = np.array(frac_combo) * s_h\n                for dx, dy in trans_grid:\n                    centers = build_centers(s_h, v_ratio, row_shifts, dx=dx, dy=dy)\n                    r = compute_radii(centers)\n                    ssum = float(r.sum())\n                    if ssum > best_sum + 1e-12:\n                        best_sum = ssum\n                        best_params = (s_h, v_ratio, row_shifts.copy(), dx, dy)\n                        best_centers = centers.copy()\n                        best_radii = r.copy()\n\n    # fallback if none found (shouldn't happen)\n    if best_params is None:\n        s_h = 0.12; v_ratio = 1.0; row_shifts = np.zeros(len(rows))\n        best_centers = build_centers(s_h, v_ratio, row_shifts)\n        best_radii = compute_radii(best_centers)\n        best_sum = float(best_radii.sum())\n        best_params = (s_h, v_ratio, row_shifts.copy(), 0.0, 0.0)\n\n    # deterministic coordinate-descent refinement around best params\n    cur_s_h, cur_v_ratio, cur_row_shifts, cur_dx, cur_dy = best_params\n    s_h_steps = [0.012, 0.006, 0.003, 0.0015]\n    v_ratio_steps = [0.03, 0.015, 0.007, 0.0035]\n    row_shift_steps = [0.03 * cur_s_h, 0.015 * cur_s_h, 0.007 * cur_s_h, 0.003 * cur_s_h]\n    trans_steps = [0.004, 0.002, 0.001, 0.0005]\n\n    for level in range(len(s_h_steps)):\n        improved = True\n        iter_count = 0\n        while improved and iter_count < 6:\n            improved = False\n            iter_count += 1\n            # s_h\n            for sign in (-1,1):\n                cand = cur_s_h + sign * s_h_steps[level]\n                if 0.04 < cand < 0.35:\n                    centers = build_centers(cand, cur_v_ratio, cur_row_shifts, cur_dx, cur_dy)\n                    r = compute_radii(centers)\n                    if r.sum() > best_sum + 1e-12:\n                        best_sum = float(r.sum()); best_centers = centers.copy(); best_radii = r.copy(); cur_s_h = cand; improved = True\n            # v_ratio\n            for sign in (-1,1):\n                cand = cur_v_ratio + sign * v_ratio_steps[level]\n                if 0.7 < cand < 1.4:\n                    centers = build_centers(cur_s_h, cand, cur_row_shifts, cur_dx, cur_dy)\n                    r = compute_radii(centers)\n                    if r.sum() > best_sum + 1e-12:\n                        best_sum = float(r.sum()); best_centers = centers.copy(); best_radii = r.copy(); cur_v_ratio = cand; improved = True\n            # per-row shifts\n            for ridx in range(len(rows)):\n                for sign in (-1,1):\n                    cand_shifts = cur_row_shifts.copy()\n                    cand_shifts[ridx] += sign * row_shift_steps[level]\n                    centers = build_centers(cur_s_h, cur_v_ratio, cand_shifts, cur_dx, cur_dy)\n                    r = compute_radii(centers)\n                    if r.sum() > best_sum + 1e-12:\n                        best_sum = float(r.sum()); best_centers = centers.copy(); best_radii = r.copy(); cur_row_shifts = cand_shifts; improved = True\n            # translations\n            for tx_mult, ty_mult in product((-1,0,1), repeat=2):\n                if tx_mult==0 and ty_mult==0: continue\n                cand_dx = cur_dx + tx_mult * trans_steps[level]\n                cand_dy = cur_dy + ty_mult * trans_steps[level]\n                centers = build_centers(cur_s_h, cur_v_ratio, cur_row_shifts, cand_dx, cand_dy)\n                r = compute_radii(centers)\n                if r.sum() > best_sum + 1e-12:\n                    best_sum = float(r.sum()); best_centers = centers.copy(); best_radii = r.copy(); cur_dx, cur_dy = cand_dx, cand_dy; improved = True\n\n    # tiny final nudges\n    for dx_try in (-5e-4, 0.0, 5e-4):\n        for dy_try in (-5e-4, 0.0, 5e-4):\n            centers = build_centers(cur_s_h, cur_v_ratio, cur_row_shifts, cur_dx + dx_try, cur_dy + dy_try)\n            r = compute_radii(centers)\n            if r.sum() > best_sum + 1e-12:\n                best_sum = float(r.sum()); best_centers = centers.copy(); best_radii = r.copy(); cur_dx += dx_try; cur_dy += dy_try\n\n    return np.array(best_centers), np.array(best_radii), float(best_radii.sum())\n# EVOLVE-BLOCK-END\n\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n```"]}}, "artifacts_json": "{\"stdout\": \"Running in subprocess, Python version: 3.12.1 (v3.12.1:2305ca5144, Dec  7 2023, 17:23:38) [Clang 13.0.0 (clang-1300.0.29.30)]\\nProgram path: /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmpp4_r62pj.py\\nCalling run_packing()...\\nrun_packing() returned successfully: sum_radii = 2.3054999999999994\\nResults saved to /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmpoiel81qw.py.results\\n\", \"stderr\": \"\"}", "artifact_dir": null}