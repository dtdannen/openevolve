{"id": "d6b4fa58-d475-40f9-861a-523a473741df", "code": "# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for packing 26 circles in the unit square.\nUses a compact near-hexagonal cluster (rows 5+5+6+5+5), a uniform scale sweep\nto find a strong starting placement, then small anisotropic tweaks and per-row\nhorizontal shifts to break symmetry and improve total radii sum.\n\"\"\"\nimport numpy as np\nfrom itertools import product\n\ndef construct_packing():\n    n = 26\n    rows = [5, 5, 6, 5, 5]  # sums to 26\n    vstep = np.sqrt(3.0) / 2.0\n\n    # Build base integer-layout coords (unit horizontal spacing)\n    base = []\n    for r_idx, cnt in enumerate(rows):\n        xs = np.arange(cnt) - (cnt - 1) / 2.0\n        x_off = 0.5 * (r_idx % 2)\n        y = r_idx * vstep\n        for x in xs:\n            base.append([x + x_off, y])\n    base = np.array(base)  # (26,2)\n\n    # normalize base to origin\n    minb = base.min(axis=0)\n    base -= minb\n    width, height = base.max(axis=0)\n    margin = 1e-6\n    # maximum uniform scale to fit inside unit square with margin\n    Smax = min((1 - 2 * margin) / max(width, 1e-12), (1 - 2 * margin) / max(height, 1e-12))\n\n    def radii_for_centers(C):\n        # distance to border\n        db = np.minimum.reduce([C[:, 0], C[:, 1], 1 - C[:, 0], 1 - C[:, 1]])\n        # pairwise distances\n        dif = C[:, None, :] - C[None, :, :]\n        D = np.sqrt(np.maximum(0.0, (dif ** 2).sum(axis=2)))\n        np.fill_diagonal(D, np.inf)\n        nearest = D.min(axis=1)\n        r = np.minimum(db, 0.5 * nearest)\n        return np.clip(r, 0.0, 0.5)\n\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    # coarse uniform scale sweep to find a good packing\n    for alpha in np.linspace(0.42, 1.00, 61):\n        s = alpha * Smax\n        scaled = base * s\n        # center cluster in unit square\n        lo = scaled.min(axis=0); hi = scaled.max(axis=0)\n        trans = 0.5 - 0.5 * (lo + hi)\n        centers = scaled + trans\n        centers = np.clip(centers, margin, 1 - margin)\n        r = radii_for_centers(centers)\n        ssum = float(r.sum())\n        if ssum > best_sum:\n            best_sum = ssum; best_centers = centers.copy(); best_radii = r.copy()\n            best_s = s\n\n    # small anisotropic tweaks + per-row horizontal shifts to break symmetry\n    shift_fracs = [-0.025, 0.0, 0.025]\n    tx_vals = [0.998, 1.0, 1.002]\n    ty_vals = [0.998, 1.0, 1.002]\n\n    # precompute indices per row\n    row_indices = []\n    idx = 0\n    for cnt in rows:\n        row_indices.append(list(range(idx, idx + cnt)))\n        idx += cnt\n\n    for tx, ty in product(tx_vals, ty_vals):\n        s_x = best_s * tx\n        s_y = best_s * ty\n        anis = base.copy()\n        anis[:, 0] = anis[:, 0] * s_x\n        anis[:, 1] = anis[:, 1] * s_y\n        for shifts in product(shift_fracs, repeat=len(rows)):\n            centers = anis.copy()\n            for r_idx, sf in enumerate(shifts):\n                inds = row_indices[r_idx]\n                centers[inds, 0] += sf * best_s\n            # recenter & clamp\n            lo = centers.min(axis=0); hi = centers.max(axis=0)\n            trans = 0.5 - 0.5 * (lo + hi)\n            centers = np.clip(centers + trans, margin, 1 - margin)\n            r = radii_for_centers(centers)\n            ssum = float(r.sum())\n            if ssum > best_sum:\n                best_sum = ssum; best_centers = centers.copy(); best_radii = r.copy()\n\n    # tiny final nudges (deterministic)\n    for dx, dy in [(0.0, 0.0), (-1e-3, 0.0), (1e-3, 0.0), (0.0, -1e-3), (0.0, 1e-3)]:\n        centers = np.clip(best_centers + np.array([dx, dy]), margin, 1 - margin)\n        r = radii_for_centers(centers)\n        ssum = float(r.sum())\n        if ssum > best_sum:\n            best_sum = ssum; best_centers = centers.copy(); best_radii = r.copy()\n\n    return np.array(best_centers), np.array(best_radii), float(best_radii.sum())\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\", linewidth=0.6)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # visualize(centers, radii)", "language": "python", "parent_id": "bc924ea3-8798-48d3-9476-e9960a7b05d7", "generation": 2, "timestamp": 1759962039.784416, "iteration_found": 29, "metrics": {"validity": 1.0, "sum_radii": 2.049659222812083, "target_ratio": 0.7778592875947186, "combined_score": 0.7778592875947186, "eval_time": 0.15373897552490234}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Full rewrite", "parent_metrics": {"validity": 1.0, "sum_radii": 1.972820323027551, "target_ratio": 0.7486984148112148, "combined_score": 0.7486984148112148, "eval_time": 0.05870795249938965}, "island": 0}, "prompts": {"full_rewrite_user": {"system": "You are an expert mathematician specializing in circle packing problems and computational geometry. Your task is to improve a constructor function that directly produces a specific arrangement of 26 circles in a unit square, maximizing the sum of their radii. The AlphaEvolve paper achieved a sum of 2.635 for n=26.\n\nKey geometric insights:\n- Circle packings often follow hexagonal patterns in the densest regions\n- Maximum density for infinite circle packing is pi/(2*sqrt(3)) \u2248 0.9069\n- Edge effects make square container packing harder than infinite packing\n- Circles can be placed in layers or shells when confined to a square\n- Similar radius circles often form regular patterns, while varied radii allow better space utilization\n- Perfect symmetry may not yield the optimal packing due to edge effects\n\nFocus on designing an explicit constructor that places each circle in a specific position, rather than an iterative search algorithm.\n\n\n## Evolution Insights (Global Learnings)\n\n### Common Pitfalls:\n\u26a0\ufe0f (seen 3x) The regression was caused by replacing the original ring-based placement and proportional downscaling with a dense hex-like layered layout (s=0.192) plus an LP-like iterative tightening that enforces r_i \u2264 min(border_i, min_j(d_ij \u2212 r_j)) and breaks overlaps by reducing the larger radius; that layout produced many very small inter-center and border distances so the tightening collapsed many radii, dropping sum_radii from ~0.96 to ~0.34.  \nWhen making similar changes, avoid fixed spacings/layouts that create infeasible small distances without first checking feasible radii, and be wary of greedy one-sided tightening rules\u2014add pre-checks (min pairwise distance vs desired radii), unit tests for sum-of-radii expectations, or use global/proportional constraint solvers to prevent asymmetric collapse.\n\u26a0\ufe0f (seen 3x) You replaced the original ring-based layout and proportional pairwise rescaling with a dense hex-like layered layout (rows=[5,5,6,5,5], s=0.192) and an LP-like iterative tightening radii solver that only decreases radii; the new geometry + solver combination produced much smaller pairwise distances so the iterative clamp drove many radii toward zero and the total sum dropped ~65%. When changing packing geometry or the constraint solver, re-tune spacing/row layout and verify pairwise distances (and sum_radii) before committing\u2014add regression tests, visualize configurations, and parameterize or add safeguards in the solver so it cannot monotonically collapse radii without detection.\n\u26a0\ufe0f (seen 3x) The regression was caused by replacing the original ring layout and proportional pairwise scaling with a denser hex-like row layout (s=0.192) combined with a greedy, monotone \"iterative tightening\" radii solver; the new center configuration yields many small inter-center distances and the order-dependent decrease + final \u201creduce the larger one\u201d fixstep aggressively shrinks radii, dropping the total sum substantially.  \n\nAvoid changing both the packing geometry and the constraint-enforcement strategy at once \u2014 when you alter spacing or the radii algorithm, first inspect pairwise distances, run small diagnostic/visual tests, and prefer order-independent approaches (LP solver, symmetric scaling, or provably convergent methods) or add unit tests to detect order-dependent over-shrinking.\n\u26a0\ufe0f (seen 3x) The child replaced the original iterative pairwise radius-reduction solver and changed the row layout with a vectorized strategy that builds the full pairwise distance matrix and sets each radius to min(distance-to-border, nearest_neighbor/2), eliminating the O(iterations * pairs) loop and yielding much faster evaluation and a larger sum of radii. When making similar changes, don't assume nearest-neighbor/half-distance is globally valid (it can overestimate radii by ignoring transitive/chain constraints) and beware the O(n^2) memory/time cost of dense distance matrices\u2014validate against the iterative solver, add unit tests, or limit computations via k-nearest neighbors/KD-trees for larger n.", "user": "# Current Program Information\n- Fitness: 0.7487\n- Feature coordinates: No feature coordinates\n- Focus areas: - Fitness declined: 0.8073 \u2192 0.7487. Consider revising recent changes.\n- Consider simplifying - code length exceeds 500 characters\n\n## Last Execution Output\n\n### stdout\n```\nRunning in subprocess, Python version: 3.12.1 (v3.12.1:2305ca5144, Dec  7 2023, 17:23:38) [Clang 13.0.0 (clang-1300.0.29.30)]\nProgram path: /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmpewt63v3x.py\nCalling run_packing()...\nrun_packing() returned successfully: sum_radii = 1.972820323027551\nResults saved to /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmp2a7gxd_1.py.results\n\n```\n\n### stderr\n```\n\n```\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 3\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.1272, target_ratio: 0.8073, combined_score: 0.8073, eval_time: 2.1832\n- Outcome: Mixed results\n\n### Attempt 2\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.2240, target_ratio: 0.8440, combined_score: 0.8440, eval_time: 0.1421\n- Outcome: Mixed results\n\n### Attempt 1\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.2247, target_ratio: 0.8443, combined_score: 0.8443, eval_time: 0.1479\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.8443)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (deterministic hex cluster + staged refinement)\"\"\"\nimport numpy as np\nfrom itertools import product\n\n\ndef construct_packing():\n    \"\"\"\n    Build a deterministic layout of 26 circle centers based on a compact\n    hex-like cluster (rows 5+5+6+5+5). Use a coarse uniform scale search\n    to find a good global packing, then refine with small anisotropic\n    tweaks and per-row horizontal shifts to break perfect symmetry.\n    Returns centers (26,2), radii (26,), sum_radii\n    \"\"\"\n    n = 26\n    # Compact hex-like row counts summing to 26\n    rows = [5, 5, 6, 5, 5]\n\n    # Base integer layout coordinates (unit horizontal spacing)\n    base = []\n    vstep = np.sqrt(3) / 2.0  # hex vertical step for unit horiz spacing\n    for r_idx, cnt in enumerate(rows):\n        xs = np.arange(cnt) - (cnt - 1) / 2.0\n        x_offset = 0.5 * (r_idx % 2)\n        for xi in xs:\n            base.append([xi + x_offset, r_idx * vstep])\n    base = np.array(base)  # (26,2)\n\n    # bounding box of base pattern\n    minb = base.min(axis=0)\n    maxb = base.max(axis=0)\n    width = maxb[0] - minb[0]\n    height = maxb[1] - minb[1]\n\n    # small margin to avoid numerical border-touch\n    margin = 1e-4\n    # maximum uniform scale to fit inside [margin,1-margin]\n    Smax = min((1 - 2 * margin) / width, (1 - 2 * margin) / height)\n\n    def compute_radii(centers):\n        # distance to border\n        d_border = np.minimum.reduce([centers[:, 0], centers[:, 1], 1 - centers[:, 0], 1 - centers[:, 1]])\n        # pairwise distances\n        C = centers\n        dif = C[:, None, :] - C[None, :, :]\n        D = np.sqrt(np.maximum(0.0, (dif ** 2).sum(axis=2)))\n        np.fill_diagonal(D, np.inf)\n        nearest = D.min(axis=1)\n        radii = np.minimum(d_border, 0.5 * nearest)\n        radii = np.maximum(radii, 0.0)\n        return radii\n\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n    best_s = None\n\n    # Coarse uniform scale sweep\n    for alpha in np.linspace(0.40, 1.00, 61):\n        s = alpha * Smax\n        scaled = (base - minb) * s\n        # center cluster inside unit square\n        scaled_min = scaled.min(axis=0)\n        scaled_max = scaled.max(axis=0)\n        translate = 0.5 - 0.5 * (scaled_min + scaled_max)\n        centers = scaled + translate\n        centers = np.clip(centers, margin, 1 - margin)\n        radii = compute_radii(centers)\n        ssum = radii.sum()\n        if ssum > best_sum:\n            best_sum = ssum\n            best_centers = centers.copy()\n            best_radii = radii.copy()\n            best_s = s\n\n    # Refinement: small anisotropic scaling around best_s and per-row horizontal shifts\n    # Per-row shifts (in units of current spacing s) to break symmetry; try -0.03,0,0.03 fractions\n    shift_fracs = [-0.03, 0.0, 0.03]\n    tx_vals = [0.996, 1.0, 1.004]\n    ty_vals = [0.996, 1.0, 1.004]\n\n    # Precompute base x positions per row for convenience\n    row_indices = []\n    idx = 0\n    for cnt in rows:\n        row_indices.append(list(range(idx, idx + cnt)))\n        idx += cnt\n    # horizontal spacing in base units is 1.0, so shift in absolute units = frac * best_s\n    for tx, ty in product(tx_vals, ty_vals):\n        s_x = best_s * tx\n        s_y = best_s * ty * (np.sqrt(3) / 2.0) / (np.sqrt(3) / 2.0)  # keep vertical ratio consistent\n        # build scaled base with these anisotropic scales\n        anis_base = base.copy()\n        # apply anisotropic scale: scale x by (s_x), y by (s_y / vstep) so that unit vertical spacing becomes s_y\n        anis_base[:, 0] = (anis_base[:, 0] - minb[0]) * (s_x)\n        anis_base[:, 1] = (anis_base[:, 1] - minb[1]) * (s_y / vstep)\n        # try all combinations of small per-row horizontal shifts (3^5 = 243)\n        for shifts in product(shift_fracs, repeat=len(rows)):\n            centers = anis_base.copy()\n            # apply per-row x shift to each member in row (shift is fraction of best_s)\n            for r, shift_frac in enumerate(shifts):\n                inds = row_indices[r]\n                centers[inds, 0] += shift_frac * best_s\n            # center into unit square\n            cmin = centers.min(axis=0)\n            cmax = centers.max(axis=0)\n            translate = 0.5 - 0.5 * (cmin + cmax)\n            centers = centers + translate\n            centers = np.clip(centers, margin, 1 - margin)\n            radii = compute_radii(centers)\n            ssum = radii.sum()\n            if ssum > best_sum:\n                best_sum = ssum\n                best_centers = centers.copy()\n                best_radii = radii.copy()\n\n    # As a final small local tweak, try translating the whole pattern to snug against edges slightly\n    # (keeps deterministic)\n    for dx, dy in [(0.0, 0.0), (-1e-3, 0.0), (1e-3, 0.0), (0.0, -1e-3), (0.0, 1e-3)]:\n        centers = np.clip(best_centers + np.array([dx, dy]), margin, 1 - margin)\n        radii = compute_radii(centers)\n        ssum = radii.sum()\n        if ssum > best_sum:\n            best_sum = ssum\n            best_centers = centers.copy()\n            best_radii = radii.copy()\n\n    return np.array(best_centers), np.array(best_radii), float(best_radii.sum())\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\", linewidth=0.6)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.2247), Performs well on target_ratio (0.8443), Performs well on combined_score (0.8443), Performs well on eval_time (0.1479)\n\n### Program 2 (Score: 0.8440)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for n=26 circle packing in the unit square.\nUses a compact near-hexagonal layout (rows 5+5+6+5+5), a scale sweep to\nmaximize sum of radii (border-limited and half-nearest-neighbor), and\nsmall anisotropic + per-row shift refinements.\n\"\"\"\nimport numpy as np\nfrom itertools import product\n\ndef construct_packing():\n    n = 26\n    rows = [5, 5, 6, 5, 5]  # sums to 26\n    vstep = np.sqrt(3) / 2.0\n    # build base integer-layout coords (unit horiz spacing)\n    base = []\n    for r_idx, cnt in enumerate(rows):\n        xs = np.arange(cnt) - (cnt - 1) / 2.0\n        x_offset = 0.5 * (r_idx % 2)\n        for xi in xs:\n            base.append([xi + x_offset, r_idx * vstep])\n    base = np.array(base)  # (26,2)\n\n    minb = base.min(axis=0)\n    maxb = base.max(axis=0)\n    width = maxb[0] - minb[0]\n    height = maxb[1] - minb[1]\n    margin = 1e-6\n    Smax = min((1 - 2 * margin) / width, (1 - 2 * margin) / height)\n\n    def radii_for_centers(C):\n        # distance to border\n        dborder = np.minimum.reduce([C[:,0], C[:,1], 1 - C[:,0], 1 - C[:,1]])\n        dif = C[:, None, :] - C[None, :, :]\n        D = np.sqrt(np.maximum(0.0, (dif**2).sum(axis=2)))\n        np.fill_diagonal(D, np.inf)\n        nearest = D.min(axis=1)\n        r = np.minimum(dborder, 0.5 * nearest)\n        return np.maximum(r, 0.0)\n\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    # coarse uniform scale sweep\n    for alpha in np.linspace(0.40, 1.00, 61):\n        s = alpha * Smax\n        scaled = (base - minb) * s\n        # center\n        lo = scaled.min(axis=0); hi = scaled.max(axis=0)\n        trans = 0.5 - 0.5 * (lo + hi)\n        centers = scaled + trans\n        centers = np.clip(centers, margin, 1 - margin)\n        radii = radii_for_centers(centers)\n        ssum = radii.sum()\n        if ssum > best_sum:\n            best_sum = ssum; best_centers = centers.copy(); best_radii = radii.copy()\n\n    # small anisotropic and per-row shifts refinement\n    if best_centers is None:\n        best_centers = (base - minb) * (0.12) + np.array([0.5, 0.5])\n        best_radii = radii_for_centers(best_centers)\n        best_sum = best_radii.sum()\n\n    # row indices for shifts\n    row_indices = []\n    idx = 0\n    for cnt in rows:\n        row_indices.append(list(range(idx, idx + cnt)))\n        idx += cnt\n\n    # try a few tiny anisotropic scalings and row shifts\n    tx_vals = [0.998, 1.0, 1.002]\n    ty_vals = [0.998, 1.0, 1.002]\n    shift_fracs = [-0.03, 0.0, 0.03]  # fraction of best scale\n    for tx, ty in product(tx_vals, ty_vals):\n        s_x = Smax * tx\n        s_y = Smax * ty\n        anis = base.copy()\n        anis[:,0] = (anis[:,0] - minb[0]) * s_x\n        anis[:,1] = (anis[:,1] - minb[1]) * s_y / vstep  # keep vertical units consistent\n        for shifts in product(shift_fracs, repeat=len(rows)):\n            centers = anis.copy()\n            for r, sf in enumerate(shifts):\n                inds = row_indices[r]\n                centers[inds,0] += sf * (Smax)\n            lo = centers.min(axis=0); hi = centers.max(axis=0)\n            trans = 0.5 - 0.5 * (lo + hi)\n            centers = np.clip(centers + trans, margin, 1 - margin)\n            radii = radii_for_centers(centers)\n            ssum = radii.sum()\n            if ssum > best_sum:\n                best_sum = ssum; best_centers = centers.copy(); best_radii = radii.copy()\n\n    return np.array(best_centers), np.array(best_radii), float(best_radii.sum())\n# EVOLVE-BLOCK-END\n\n# fixed runtime interface\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(7,7))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect('equal')\n    for i,(c,r) in enumerate(zip(centers,radii)):\n        ax.add_patch(Circle(c,r,alpha=0.6,edgecolor='k'))\n        ax.text(c[0], c[1], str(i), ha='center', va='center', fontsize=8)\n    plt.title(f\"n={len(centers)}, sum={sum(radii):.6f}\"); plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # visualize(centers, radii)\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.2240), Performs well on target_ratio (0.8440), Performs well on combined_score (0.8440), Performs well on eval_time (0.1421)\n\n### Program 3 (Score: 0.8073)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for packing 26 circles in the unit square.\nBuilds a compact near-hexagonal cluster (rows [6,5,5,5,5]) and searches\nover horizontal spacing, small anisotropic scale tweaks and tiny translations.\nRadii are computed as min(distance-to-border, half nearest-neighbor) (order-invariant).\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    rows = [6, 5, 5, 5, 5]  # compact hex-like layering summing to 26\n\n    def base_centers(s):\n        v = np.sqrt(3.0) / 2.0 * s\n        R = len(rows)\n        total_v = (R - 1) * v\n        y0 = (1.0 - total_v) / 2.0\n        C = []\n        for r_idx, k in enumerate(rows):\n            span = (k - 1) * s\n            # center horizontally\n            x0 = (1.0 - span) / 2.0\n            shift = (r_idx % 2) * (s / 2.0)\n            y = y0 + r_idx * v\n            for i in range(k):\n                x = x0 + i * s + shift\n                C.append([x, y])\n        return np.array(C)  # shape (26,2)\n\n    def radii_for_centers(centers):\n        # distances to borders\n        db = np.minimum.reduce([centers[:,0], centers[:,1], 1-centers[:,0], 1-centers[:,1]])\n        # pairwise distances\n        diffs = centers[:, None, :] - centers[None, :, :]\n        D = np.sqrt(np.maximum(0.0, (diffs**2).sum(axis=2)))\n        np.fill_diagonal(D, np.inf)\n        dmin = D.min(axis=1)\n        r = np.minimum(db, 0.5 * dmin)\n        return np.clip(r, 0.0, 0.5)\n\n    # Search parameters\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    # spacing range informed by row counts (must keep centers inside unit square)\n    for s in np.linspace(0.09, 0.20, 60):\n        C0 = base_centers(s)\n        if C0.shape[0] != n:\n            continue\n        # try small anisotropic scales around 1.0 and tiny translations\n        for sx in np.linspace(0.985, 1.015, 7):\n            for sy in np.linspace(0.985, 1.015, 7):\n                # scale about the square center 0.5 to retain placement\n                C = 0.5 + (C0 - 0.5) * np.array([sx, sy])\n                for tx in np.linspace(-0.008, 0.008, 5):\n                    for ty in np.linspace(-0.008, 0.008, 5):\n                        Ctr = C + np.array([tx, ty])\n                        # reject if any center outside bounds\n                        if np.any(Ctr < -1e-9) or np.any(Ctr > 1 + 1e-9):\n                            continue\n                        R = radii_for_centers(Ctr)\n                        if np.any(R <= 0):\n                            # allow but still evaluate sum (rare), skip if degenerate\n                            if np.any(R < 1e-9):\n                                continue\n                        ssum = float(R.sum())\n                        if ssum > best_sum:\n                            best_sum = ssum\n                            best_centers = Ctr.copy()\n                            best_radii = R.copy()\n\n    # Fallback simple placement if search fails\n    if best_centers is None:\n        C0 = base_centers(0.14)\n        best_centers = np.clip(C0, 1e-6, 1-1e-6)\n        best_radii = radii_for_centers(best_centers)\n        best_sum = float(best_radii.sum())\n\n    return np.array(best_centers), np.array(best_radii), float(best_radii.sum())\n# EVOLVE-BLOCK-END\n\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(7,7))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect('equal')\n    for i,(c,r) in enumerate(zip(centers, radii)):\n        ax.add_patch(Circle(c,r,alpha=0.6,edgecolor='k'))\n        ax.text(c[0],c[1],str(i),ha='center',va='center',fontsize=8)\n    plt.title(f\"n={len(centers)}, sum={sum(radii):.6f}\"); plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, s = run_packing()\n    print(f\"Sum of radii: {s:.6f}\")\n    # visualize(centers, radii)\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.1272), Performs well on target_ratio (0.8073), Performs well on combined_score (0.8073), Performs well on eval_time (2.1832)\n\n\n\n## Diverse Programs\n\n### Program D1 (Score: 0.7941)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (compact hex-like rows).\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    # compact near-hex layering that sums to 26\n    rows = [6, 5, 5, 5, 5]\n\n    def build_centers(s):\n        v = np.sqrt(3) / 2 * s\n        centers = []\n        R = len(rows)\n        total_v = (R - 1) * v\n        y0 = (1.0 - total_v) / 2.0\n        for r_idx, k in enumerate(rows):\n            span = (k - 1) * s\n            x0 = (1.0 - span) / 2.0\n            shift = (r_idx % 2) * (s / 2.0)\n            y = y0 + r_idx * v\n            for i in range(k):\n                x = x0 + i * s + shift\n                centers.append([x, y])\n        return np.array(centers)\n\n    def radii_for_centers(C):\n        # clamp to unit square safety\n        C = np.clip(C, 1e-9, 1 - 1e-9)\n        # distance to walls\n        d_wall = np.minimum.reduce([C[:,0], C[:,1], 1-C[:,0], 1-C[:,1]])\n        # pairwise distances\n        D = np.sqrt(np.maximum(0.0, ((C[:,None,:] - C[None,:,:])**2).sum(axis=2)))\n        np.fill_diagonal(D, np.inf)\n        d_near = D.min(axis=1)\n        r = np.minimum(d_wall, 0.5 * d_near)\n        return np.maximum(r, 0.0)\n\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    # search horizontal spacing\n    for s in np.linspace(0.06, 0.24, 240):\n        C = build_centers(s)\n        if C.shape[0] != n: \n            continue\n        # reject if any center out of bounds\n        if np.any(C < -1e-8) or np.any(C > 1+1e-8):\n            continue\n        r = radii_for_centers(C)\n        if np.any(r <= 0):\n            continue\n        ssum = float(r.sum())\n        if ssum > best_sum:\n            best_sum = ssum\n            best_centers = C.copy()\n            best_radii = r.copy()\n\n    if best_centers is None:\n        best_centers = build_centers(0.12)\n        best_radii = radii_for_centers(best_centers)\n        best_sum = float(best_radii.sum())\n\n    return best_centers, best_radii, float(best_sum)\n# EVOLVE-BLOCK-END\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(7, 7))\n    ax.set_xlim(0, 1); ax.set_ylim(0, 1); ax.set_aspect(\"equal\")\n    # Draw circles\n    for i, (c, r) in enumerate(zip(centers, radii)):\n        ax.add_patch(Circle(c, r, alpha=0.6, edgecolor=\"k\"))\n        ax.text(c[0], c[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # visualize(centers, radii)\n```\nKey features: Alternative approach to validity, Alternative approach to sum_radii\n\n### Program D2 (Score: 0.8068)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for 26 circles in the unit square.\nPlaces centers in a compact hex-like layering [6,5,5,5,5], searches horizontal\nspacing s to maximize sum(radii) where r_i = min(dist_to_border, 0.5*nearest_neighbor).\nVectorized computations and a tiny anisotropic tweak around the best s improve fit.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    rows = [6, 5, 5, 5, 5]  # compact near-hex cluster\n\n    def build_centers(s):\n        v = np.sqrt(3) / 2.0 * s\n        R = len(rows)\n        total_v = (R - 1) * v\n        y0 = (1.0 - total_v) / 2.0\n        C = []\n        for r_idx, k in enumerate(rows):\n            span = (k - 1) * s\n            x0 = (1.0 - span) / 2.0\n            shift = (r_idx % 2) * (s / 2.0)\n            y = y0 + r_idx * v\n            for i in range(k):\n                x = x0 + i * s + shift\n                C.append((x, y))\n        return np.array(C)\n\n    def radii_for_centers(C):\n        # distance to walls\n        db = np.minimum.reduce([C[:,0], C[:,1], 1-C[:,0], 1-C[:,1]])\n        # pairwise distances\n        diff = C[:, None, :] - C[None, :, :]\n        D = np.sqrt(np.maximum(0.0, (diff**2).sum(axis=2)))\n        np.fill_diagonal(D, np.inf)\n        nearest = D.min(axis=1)\n        r = np.minimum(db, 0.5 * nearest)\n        return np.clip(r, 0.0, 0.5)\n\n    best_sum = -1.0\n    best_C = None\n    best_r = None\n\n    # search horizontal spacing (dense)\n    for s in np.linspace(0.11, 0.22, 300):\n        C = build_centers(s)\n        if C.shape[0] != n: continue\n        if np.any(C < -1e-8) or np.any(C > 1+1e-8): continue\n        r = radii_for_centers(C)\n        if np.any(r <= 0): continue\n        ssum = float(r.sum())\n        if ssum > best_sum:\n            best_sum = ssum; best_C = C.copy(); best_r = r.copy()\n\n    # small anisotropic tweak around best solution\n    if best_C is not None:\n        for tx in np.linspace(0.995, 1.005, 5):\n            for ty in np.linspace(0.995, 1.005, 5):\n                # scale around center 0.5 to slightly stretch/compress horizontally/vertically\n                C = (best_C - 0.5) * np.array([tx, ty]) + 0.5\n                C = np.clip(C, 1e-8, 1-1e-8)\n                r = radii_for_centers(C)\n                ssum = float(r.sum())\n                if ssum > best_sum:\n                    best_sum = ssum; best_C = C.copy(); best_r = r.copy()\n\n    # Fallback if search failed\n    if best_C is None:\n        best_C = build_centers(0.15)\n        best_r = radii_for_centers(best_C)\n        best_sum = float(best_r.sum())\n\n    return best_C, best_r, float(best_sum)\n# EVOLVE-BLOCK-END\n\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(6,6))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect('equal')\n    for i,(c,r) in enumerate(zip(centers, radii)):\n        ax.add_patch(Circle(c,r,alpha=0.6,edgecolor='k'))\n        ax.text(c[0],c[1],str(i),ha='center',va='center',fontsize=8)\n    plt.title(f\"n={len(centers)}, sum={sum(radii):.6f}\"); plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # visualize(centers, radii)\n```\nKey features: Alternative approach to validity, Alternative approach to sum_radii\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 0.7852, Type: Alternative)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for packing 26 circles in the unit square.\nUses a compact hex-like cluster (rows 5+5+6+5+5), scales and slightly anisotropically\ntweaks it to maximize the sum of radii computed as min(dist_to_border, half nearest neighbor).\n\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    n = 26\n    rows = [5, 5, 6, 5, 5]  # compact hex-like cluster summing to 26\n\n    # build base hex lattice coordinates (unit horizontal spacing)\n    coords = []\n    vstep = np.sqrt(3) / 2.0\n    for r, cnt in enumerate(rows):\n        xs = np.arange(cnt) - (cnt - 1) / 2.0\n        x_offset = 0.5 * (r % 2)\n        y = r * vstep\n        for x in xs:\n            coords.append([x + x_offset, y])\n    base = np.array(coords)  # (26,2)\n\n    # normalize base to start at origin\n    minb = base.min(axis=0)\n    base -= minb\n    width, height = base.max(axis=0)\n\n    # margin to keep centers strictly inside unit square\n    margin = 0.004\n\n    # maximum uniform scale to fit in [margin,1-margin]\n    Smax = min((1 - 2 * margin) / (width if width > 0 else 1e-9),\n               (1 - 2 * margin) / (height if height > 0 else 1e-9))\n\n    def radii_for_centers(C):\n        # distance to border\n        d_border = np.minimum.reduce([C[:, 0], C[:, 1], 1 - C[:, 0], 1 - C[:, 1]])\n        # pairwise distances\n        dif = C[:, None, :] - C[None, :, :]\n        D = np.sqrt(np.maximum(0.0, (dif ** 2).sum(axis=2)))\n        np.fill_diagonal(D, np.inf)\n        d_near = D.min(axis=1)\n        r = np.minimum(d_border, 0.5 * d_near)\n        return np.maximum(r, 0.0)\n\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    # coarse search over uniform scale fraction\n    for alpha in np.linspace(0.36, 1.0, 66):\n        s = alpha * Smax\n        scaled = base * s\n        # center cluster in unit square\n        t = 0.5 - 0.5 * (scaled.min(axis=0) + scaled.max(axis=0))\n        centers = scaled + t\n        centers = np.clip(centers, margin, 1 - margin)\n        r = radii_for_centers(centers)\n        ssum = r.sum()\n        if ssum > best_sum:\n            best_sum = ssum\n            best_centers = centers.copy()\n            best_radii = r.copy()\n\n    # small anisotropic tweaks around best found\n    if best_centers is not None:\n        for tx in np.linspace(0.985, 1.015, 7):\n            for ty in np.linspace(0.985, 1.015, 7):\n                scaled = (base * np.array([tx, ty])) * (Smax * 0.995)\n                t = 0.5 - 0.5 * (scaled.min(axis=0) + scaled.max(axis=0))\n                centers = scaled + t\n                centers = np.clip(centers, margin, 1 - margin)\n                r = radii_for_centers(centers)\n                ssum = r.sum()\n                if ssum > best_sum:\n                    best_sum = ssum\n                    best_centers = centers.copy()\n                    best_radii = r.copy()\n\n    # fallback safety\n    if best_centers is None:\n        scaled = base * (Smax * 0.5)\n        t = 0.5 - 0.5 * (scaled.min(axis=0) + scaled.max(axis=0))\n        best_centers = np.clip(scaled + t, margin, 1 - margin)\n        best_radii = radii_for_centers(best_centers)\n\n    centers = np.array(best_centers, dtype=float)\n    radii = np.array(best_radii, dtype=float)\n    return centers, radii, float(radii.sum())\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.7, edgecolor=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Optional: visualize(centers, radii)\n```\nUnique approach: Modification: Full rewrite, Excellent validity (1.000), Excellent sum_radii (2.069)\n\n### Inspiration 2 (Score: 0.7941, Type: Alternative)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles in a unit square.\nBuilds an explicit near-hexagonal cluster and searches lattice spacing\n(and small anisotropic tweaks) to maximize the sum of circle radii.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    # near-hex rows summing to 26\n    rows = [6, 5, 5, 5, 5]\n\n    # base integer lattice coords (unit horizontal spacing, vertical sqrt(3)/2)\n    def base_coords():\n        coords = []\n        v = np.sqrt(3) / 2.0\n        for r_idx, k in enumerate(rows):\n            shift = 0.5 * (r_idx % 2)\n            xs = (np.arange(k) - (k - 1) / 2.0) + shift\n            y = r_idx * v\n            for x in xs:\n                coords.append([x, y])\n        return np.array(coords)\n\n    base = base_coords()  # shape (26,2)\n\n    def placed_centers(s, aniso=(1.0, 1.0)):\n        # scale base coordinates and place centered in unit square\n        scaled = base * np.array([s * aniso[0], s * aniso[1]])\n        minb = scaled.min(axis=0); maxb = scaled.max(axis=0)\n        span = maxb - minb\n        # translate to center of unit square\n        translate = 0.5 - 0.5 * (minb + maxb)\n        centers = scaled + translate\n        return centers\n\n    def radii_for_centers(centers):\n        # distance to border\n        bord = np.minimum.reduce([centers[:,0], centers[:,1], 1-centers[:,0], 1-centers[:,1]])\n        # pairwise distances\n        C = centers\n        D = np.sqrt(np.maximum(0.0, ((C[:,None,:] - C[None,:,:])**2).sum(axis=2)))\n        np.fill_diagonal(D, np.inf)\n        nearest = D.min(axis=1)\n        radii = np.minimum(bord, 0.5 * nearest)\n        # numerical safety\n        radii = np.maximum(radii, 0.0)\n        return radii\n\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    # Coarse search over horizontal spacing s\n    for s in np.linspace(0.06, 0.24, 240):\n        centers = placed_centers(s)\n        # reject if centers stray outside unit square\n        if np.any(centers < -1e-8) or np.any(centers > 1 + 1e-8):\n            continue\n        radii = radii_for_centers(centers)\n        if np.any(radii <= 0):\n            continue\n        ssum = float(radii.sum())\n        if ssum > best_sum:\n            best_sum = ssum; best_centers = centers.copy(); best_radii = radii.copy()\n\n    # Local anisotropic refinement around best s\n    if best_centers is not None:\n        # compute approx s from base scaling using first two distinct x coords\n        # try small anisotropic tweaks to better utilize square borders\n        for aniso_x in np.linspace(0.98, 1.02, 5):\n            for aniso_y in np.linspace(0.98, 1.02, 5):\n                # derive s from current best by estimating horizontal unit length:\n                # find pair in base with x difference 1 (adjacent in same row)\n                s_guess = None\n                # estimate s by least squares: best_centers ~ base * [s, s*v] * aniso\n                # Using x-coordinates: s \u2248 avg(delta_x / delta_base_x)\n                base_x = base[:,0]; best_x = best_centers[:,0]\n                # solve s * aniso_x \u2248 scale factor from base to best_x (centered -> linear)\n                # Use robust median ratio where base_x different\n                mask = np.abs(base_x - np.median(base_x)) > 1e-6\n                if np.any(mask):\n                    ratios = (best_x[mask] / base_x[mask])\n                    s_guess = np.median(ratios) / aniso_x\n                if s_guess is None or not np.isfinite(s_guess) or s_guess <= 0:\n                    s_guess = 0.12\n                centers = placed_centers(s_guess, aniso=(aniso_x, aniso_y))\n                if np.any(centers < -1e-8) or np.any(centers > 1 + 1e-8):\n                    continue\n                radii = radii_for_centers(centers)\n                ssum = float(radii.sum())\n                if ssum > best_sum:\n                    best_sum = ssum; best_centers = centers.copy(); best_radii = radii.copy()\n\n    # Fallback: if no valid layout found, place a safe small-cluster\n    if best_centers is None:\n        centers = placed_centers(0.12)\n        best_radii = radii_for_centers(centers)\n        best_centers = centers\n        best_sum = float(best_radii.sum())\n\n    return np.array(best_centers[:n]), np.array(best_radii[:n]), float(best_sum)\n\n# EVOLVE-BLOCK-END\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nUnique approach: Modification: Full rewrite, Excellent validity (1.000), Excellent sum_radii (2.093)\n\n### Inspiration 3 (Score: 0.1275, Type: Exploratory)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (improved constructor)\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square\n    and compute maximal feasible radii (LP-like iterative tightening).\n    Returns: centers (26,2), radii (26,), sum_of_radii\n    \"\"\"\n    n = 26\n\n    # Hex-like layered layout but with row counts chosen to fit 26 circles:\n    rows = [5, 5, 6, 5, 5]  # sums to 26\n    max_cols = max(rows)\n\n    # Choose a spacing that fills the square reasonably densely without overlap\n    s = 0.192  # horizontal spacing (tuned)\n    sy = s * np.sqrt(3) / 2.0  # vertical spacing for hex/triangular packing\n\n    total_rows = len(rows)\n    y0 = (1.0 - (total_rows - 1) * sy) / 2.0  # center rows vertically\n\n    centers = np.zeros((n, 2))\n    idx = 0\n    for r_idx, count in enumerate(rows):\n        y = y0 + r_idx * sy\n        # center the row horizontally\n        x_start = (1.0 - (count - 1) * s) / 2.0\n        for k in range(count):\n            x = x_start + k * s\n            centers[idx] = [x, y]\n            idx += 1\n\n    # Ensure centers remain inside square numerically\n    centers = np.clip(centers, 1e-8, 1.0 - 1e-8)\n\n    # Compute maximal feasible radii using an LP-like fixpoint:\n    radii = compute_max_radii(centers)  # improved routine below\n    sum_radii = float(np.sum(radii))\n\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers, tol=1e-9, max_iter=2000):\n    \"\"\"\n    Compute maximal radii satisfying:\n      r_i >= 0, r_i <= dist_to_border_i, and r_i + r_j <= dist(i,j)\n    This uses an iterative tightening: start with border-limited radii and\n    repeatedly enforce pairwise constraints by decreasing any radius that\n    violates a constraint (setting it to the tightest allowable).\n    \"\"\"\n    n = centers.shape[0]\n    # distance to borders\n    d_border = np.minimum.reduce([centers[:, 0], centers[:, 1], 1.0 - centers[:, 0], 1.0 - centers[:, 1]])\n    radii = d_border.copy()\n\n    # pairwise distances\n    diffs = centers[:, None, :] - centers[None, :, :]\n    dists = np.sqrt(np.sum(diffs * diffs, axis=2))\n\n    # iterative tightening: for each circle i, r_i <= min(border_i, min_j max(0, d_ij - r_j))\n    # This only decreases radii and converges to a feasible point. It tends to give larger\n    # total sum than naive proportional downscaling because it preserves smaller radii.\n    it = 0\n    while it < max_iter:\n        it += 1\n        prev = radii.copy()\n        for i in range(n):\n            # compute tightest allowed from pairwise constraints given current r_j\n            # skip j==i\n            # allow small negative numerical tolerance but clamp to >=0\n            limits = d_border[i]\n            # compute d_ij - r_j for all j != i\n            vals = dists[i] - radii\n            # ignore self\n            vals[i] = np.inf\n            # allowable r_i must be <= each val (and >=0)\n            min_val = np.min(vals)\n            if min_val < limits:\n                limits = min_val\n            # clamp\n            if limits < 0.0:\n                limits = 0.0\n            # only decrease (monotone)\n            if limits < radii[i] - tol:\n                radii[i] = limits\n        # convergence check\n        if np.max(prev - radii) < 1e-12:\n            break\n\n    # Final safety: enforce all constraints (clamp small residual violations)\n    radii = np.maximum(radii, 0.0)\n    for i in range(n):\n        radii[i] = min(radii[i], d_border[i])\n    for i in range(n):\n        for j in range(i + 1, n):\n            if radii[i] + radii[j] > dists[i, j]:\n                # reduce the larger one to make equality (better for sum than scaling both)\n                if radii[i] >= radii[j]:\n                    radii[i] = max(0.0, dists[i, j] - radii[j])\n                else:\n                    radii[j] = max(0.0, dists[i, j] - radii[i])\n\n    return radii\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor='k')\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nUnique approach: Modification: Full rewrite, Excellent validity (1.000), Alternative target_ratio approach\n\n# Current Program\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (hex-like lattice)\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct 26 circle centers in a compact hexagonal-like layout inside the unit square.\n    For fixed centers, return maximal non-overlapping radii: r_i = min(dist_to_border, min_j dist(i,j)/2)\n    \"\"\"\n    n = 26\n    # Row counts to sum to 26: 6 + 5 + 5 + 5 + 5 = 26\n    row_counts = [6, 5, 5, 5, 5]\n    # choose horizontal spacing to balance neighbor distance vs border margin\n    sx = 0.16  # horizontal spacing between adjacent centers in same row\n    vy = np.sqrt(3) / 2 * sx  # vertical hex spacing\n    rows = len(row_counts)\n    centers = []\n    # center rows vertically around 0.5\n    center_row = (rows - 1) / 2.0\n    for r_idx, m in enumerate(row_counts):\n        y = 0.5 + (r_idx - center_row) * vy\n        # compute starting x so row is centered in [0,1]\n        width = (m - 1) * sx\n        x0 = (1.0 - width) / 2.0\n        # alternate offset for hex pattern\n        offset = (sx / 2.0) if (r_idx % 2 == 1) else 0.0\n        for k in range(m):\n            x = x0 + offset + k * sx\n            # ensure points lie within open unit square\n            x = np.clip(x, 0.001, 0.999)\n            y = np.clip(y, 0.001, 0.999)\n            centers.append((x, y))\n    centers = np.array(centers)[:n]\n\n    # Compute maximal radii for fixed centers\n    radii = compute_max_radii(centers)\n\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers):\n    \"\"\"\n    For each center compute the largest radius that keeps it inside the unit square\n    and non-overlapping with other circles (half the nearest neighbor distance).\n    \"\"\"\n    n = centers.shape[0]\n    # distance to borders\n    dist_border = np.minimum.reduce([centers[:, 0], centers[:, 1], 1 - centers[:, 0], 1 - centers[:, 1]])\n    # pairwise distances\n    diffs = centers[:, None, :] - centers[None, :, :]  # (n, n, 2)\n    dists = np.sqrt(np.sum(diffs ** 2, axis=2)) + np.eye(n) * 1e9  # avoid zero on diagonal\n    nearest = np.min(dists, axis=1)\n    # radius limited by half nearest neighbor and border\n    radii = np.minimum(dist_border, nearest / 2.0)\n    # numeric safety clamp\n    radii = np.clip(radii, 0.0, 0.5)\n    return radii\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\n\n# Task\nRewrite the program to improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: complexity, diversity\nDifferent solutions with similar fitness but different features are valuable.\nProvide the complete new program code.\n\nIMPORTANT: Make sure your rewritten program maintains the same inputs and outputs\nas the original program, but with improved internal implementation.\n\n```python\n# Your rewritten program here\n```", "responses": ["```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for packing 26 circles in the unit square.\nUses a compact near-hexagonal cluster (rows 5+5+6+5+5), a uniform scale sweep\nto find a strong starting placement, then small anisotropic tweaks and per-row\nhorizontal shifts to break symmetry and improve total radii sum.\n\"\"\"\nimport numpy as np\nfrom itertools import product\n\ndef construct_packing():\n    n = 26\n    rows = [5, 5, 6, 5, 5]  # sums to 26\n    vstep = np.sqrt(3.0) / 2.0\n\n    # Build base integer-layout coords (unit horizontal spacing)\n    base = []\n    for r_idx, cnt in enumerate(rows):\n        xs = np.arange(cnt) - (cnt - 1) / 2.0\n        x_off = 0.5 * (r_idx % 2)\n        y = r_idx * vstep\n        for x in xs:\n            base.append([x + x_off, y])\n    base = np.array(base)  # (26,2)\n\n    # normalize base to origin\n    minb = base.min(axis=0)\n    base -= minb\n    width, height = base.max(axis=0)\n    margin = 1e-6\n    # maximum uniform scale to fit inside unit square with margin\n    Smax = min((1 - 2 * margin) / max(width, 1e-12), (1 - 2 * margin) / max(height, 1e-12))\n\n    def radii_for_centers(C):\n        # distance to border\n        db = np.minimum.reduce([C[:, 0], C[:, 1], 1 - C[:, 0], 1 - C[:, 1]])\n        # pairwise distances\n        dif = C[:, None, :] - C[None, :, :]\n        D = np.sqrt(np.maximum(0.0, (dif ** 2).sum(axis=2)))\n        np.fill_diagonal(D, np.inf)\n        nearest = D.min(axis=1)\n        r = np.minimum(db, 0.5 * nearest)\n        return np.clip(r, 0.0, 0.5)\n\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    # coarse uniform scale sweep to find a good packing\n    for alpha in np.linspace(0.42, 1.00, 61):\n        s = alpha * Smax\n        scaled = base * s\n        # center cluster in unit square\n        lo = scaled.min(axis=0); hi = scaled.max(axis=0)\n        trans = 0.5 - 0.5 * (lo + hi)\n        centers = scaled + trans\n        centers = np.clip(centers, margin, 1 - margin)\n        r = radii_for_centers(centers)\n        ssum = float(r.sum())\n        if ssum > best_sum:\n            best_sum = ssum; best_centers = centers.copy(); best_radii = r.copy()\n            best_s = s\n\n    # small anisotropic tweaks + per-row horizontal shifts to break symmetry\n    shift_fracs = [-0.025, 0.0, 0.025]\n    tx_vals = [0.998, 1.0, 1.002]\n    ty_vals = [0.998, 1.0, 1.002]\n\n    # precompute indices per row\n    row_indices = []\n    idx = 0\n    for cnt in rows:\n        row_indices.append(list(range(idx, idx + cnt)))\n        idx += cnt\n\n    for tx, ty in product(tx_vals, ty_vals):\n        s_x = best_s * tx\n        s_y = best_s * ty\n        anis = base.copy()\n        anis[:, 0] = anis[:, 0] * s_x\n        anis[:, 1] = anis[:, 1] * s_y\n        for shifts in product(shift_fracs, repeat=len(rows)):\n            centers = anis.copy()\n            for r_idx, sf in enumerate(shifts):\n                inds = row_indices[r_idx]\n                centers[inds, 0] += sf * best_s\n            # recenter & clamp\n            lo = centers.min(axis=0); hi = centers.max(axis=0)\n            trans = 0.5 - 0.5 * (lo + hi)\n            centers = np.clip(centers + trans, margin, 1 - margin)\n            r = radii_for_centers(centers)\n            ssum = float(r.sum())\n            if ssum > best_sum:\n                best_sum = ssum; best_centers = centers.copy(); best_radii = r.copy()\n\n    # tiny final nudges (deterministic)\n    for dx, dy in [(0.0, 0.0), (-1e-3, 0.0), (1e-3, 0.0), (0.0, -1e-3), (0.0, 1e-3)]:\n        centers = np.clip(best_centers + np.array([dx, dy]), margin, 1 - margin)\n        r = radii_for_centers(centers)\n        ssum = float(r.sum())\n        if ssum > best_sum:\n            best_sum = ssum; best_centers = centers.copy(); best_radii = r.copy()\n\n    return np.array(best_centers), np.array(best_radii), float(best_radii.sum())\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\", linewidth=0.6)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # visualize(centers, radii)\n```"]}}, "artifacts_json": "{\"stdout\": \"Running in subprocess, Python version: 3.12.1 (v3.12.1:2305ca5144, Dec  7 2023, 17:23:38) [Clang 13.0.0 (clang-1300.0.29.30)]\\nProgram path: /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmpess0i67l.py\\nCalling run_packing()...\\nrun_packing() returned successfully: sum_radii = 2.049659222812083\\nResults saved to /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmpaucqoaco.py.results\\n\", \"stderr\": \"\"}", "artifact_dir": null}