{"id": "35d7748a-2d77-4993-80b2-fe1a9d5a5ef5", "code": "# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 in the unit square.\nUse a near-hex layered layout rows=[6,5,5,5,5] and search horizontal spacing s\nto maximize sum of radii (each radius limited by walls and half nearest-neighbor).\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    rows = [6,5,5,5,5]  # sums to 26\n\n    def build_centers(s):\n        v = (np.sqrt(3)/2.0)*s\n        R = len(rows)\n        total_v = (R-1)*v\n        y0 = (1.0 - total_v)/2.0\n        pts = []\n        for r_idx,k in enumerate(rows):\n            span = (k-1)*s\n            x0 = (1.0 - span)/2.0\n            shift = (r_idx % 2)*(s/2.0)\n            y = y0 + r_idx*v\n            for i in range(k):\n                x = x0 + i*s + shift\n                pts.append([x,y])\n        return np.array(pts)\n\n    def radii_for_centers(centers):\n        # distance to walls\n        wall = np.minimum.reduce([centers[:,0], centers[:,1], 1-centers[:,0], 1-centers[:,1]])\n        # pairwise distances\n        dif = centers[:,None,:] - centers[None,:,:]\n        dmat = np.hypot(dif[...,0], dif[...,1])\n        np.fill_diagonal(dmat, np.inf)\n        nearest = 0.5*np.min(dmat, axis=1)\n        return np.minimum(wall, nearest)\n\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    for s in np.linspace(0.06, 0.24, 300):\n        C = build_centers(s)\n        if np.any(C < -1e-9) or np.any(C > 1+1e-9):\n            continue\n        r = radii_for_centers(C)\n        if np.any(r <= 1e-12):\n            continue\n        ssum = float(np.sum(r))\n        if ssum > best_sum:\n            best_sum = ssum\n            best_centers = C.copy()\n            best_radii = r.copy()\n\n    if best_centers is None:\n        best_centers = build_centers(0.12)\n        best_radii = radii_for_centers(best_centers)\n        best_sum = float(np.sum(best_radii))\n\n    return best_centers, best_radii, best_sum\n# EVOLVE-BLOCK-END\n\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\nif __name__ == \"__main__\":\n    centers, radii, s = run_packing()\n    print(f\"Sum of radii: {s:.6f}\")", "language": "python", "parent_id": "11c3b752-11a8-46b3-a280-a4b892f89aeb", "generation": 3, "timestamp": 1759962032.1124659, "iteration_found": 30, "metrics": {"validity": 1.0, "sum_radii": 2.0926651050960663, "target_ratio": 0.7941803055393042, "combined_score": 0.7941803055393042, "eval_time": 0.06433892250061035}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Full rewrite", "parent_metrics": {"validity": 1.0, "sum_radii": 1.941180729934942, "target_ratio": 0.7366909791024449, "combined_score": 0.7366909791024449, "eval_time": 1.0149569511413574}, "island": 2}, "prompts": {"full_rewrite_user": {"system": "You are an expert mathematician specializing in circle packing problems and computational geometry. Your task is to improve a constructor function that directly produces a specific arrangement of 26 circles in a unit square, maximizing the sum of their radii. The AlphaEvolve paper achieved a sum of 2.635 for n=26.\n\nKey geometric insights:\n- Circle packings often follow hexagonal patterns in the densest regions\n- Maximum density for infinite circle packing is pi/(2*sqrt(3)) \u2248 0.9069\n- Edge effects make square container packing harder than infinite packing\n- Circles can be placed in layers or shells when confined to a square\n- Similar radius circles often form regular patterns, while varied radii allow better space utilization\n- Perfect symmetry may not yield the optimal packing due to edge effects\n\nFocus on designing an explicit constructor that places each circle in a specific position, rather than an iterative search algorithm.\n\n\n## Evolution Insights (Global Learnings)\n\n### Common Pitfalls:\n\u26a0\ufe0f (seen 4x) The child rewrite removed the iterative pairwise radius-reduction and explicit border-enforcement loop and instead computes a dense pairwise distance matrix via vectorized broadcasting, takes nearest-neighbor distance/2 and min with border distance to set radii, while also changing the row pattern and spacing search; this eliminated costly Python loops (and iterative convergence) so eval_time dropped and the computed sum of radii increased.  \nAvoid substituting a single-shot nearest-neighbor/half-distance heuristic for iterative constraint solving because it can be overly optimistic (misses multi-way overlap interactions), incurs O(n^2) memory/time for dense distance matrices as n grows, and requires careful handling of diagonal offsets and clipping to avoid numeric/degenerate cases.\n\u26a0\ufe0f (seen 3x) The regression was caused by replacing the original ring-based placement and proportional downscaling with a dense hex-like layered layout (s=0.192) plus an LP-like iterative tightening that enforces r_i \u2264 min(border_i, min_j(d_ij \u2212 r_j)) and breaks overlaps by reducing the larger radius; that layout produced many very small inter-center and border distances so the tightening collapsed many radii, dropping sum_radii from ~0.96 to ~0.34.  \nWhen making similar changes, avoid fixed spacings/layouts that create infeasible small distances without first checking feasible radii, and be wary of greedy one-sided tightening rules\u2014add pre-checks (min pairwise distance vs desired radii), unit tests for sum-of-radii expectations, or use global/proportional constraint solvers to prevent asymmetric collapse.\n\u26a0\ufe0f (seen 3x) You replaced the original ring-based layout and proportional pairwise rescaling with a dense hex-like layered layout (rows=[5,5,6,5,5], s=0.192) and an LP-like iterative tightening radii solver that only decreases radii; the new geometry + solver combination produced much smaller pairwise distances so the iterative clamp drove many radii toward zero and the total sum dropped ~65%. When changing packing geometry or the constraint solver, re-tune spacing/row layout and verify pairwise distances (and sum_radii) before committing\u2014add regression tests, visualize configurations, and parameterize or add safeguards in the solver so it cannot monotonically collapse radii without detection.\n\u26a0\ufe0f (seen 3x) The regression was caused by replacing the original ring layout and proportional pairwise scaling with a denser hex-like row layout (s=0.192) combined with a greedy, monotone \"iterative tightening\" radii solver; the new center configuration yields many small inter-center distances and the order-dependent decrease + final \u201creduce the larger one\u201d fixstep aggressively shrinks radii, dropping the total sum substantially.  \n\nAvoid changing both the packing geometry and the constraint-enforcement strategy at once \u2014 when you alter spacing or the radii algorithm, first inspect pairwise distances, run small diagnostic/visual tests, and prefer order-independent approaches (LP solver, symmetric scaling, or provably convergent methods) or add unit tests to detect order-dependent over-shrinking.", "user": "# Current Program Information\n- Fitness: 0.7367\n- Feature coordinates: No feature coordinates\n- Focus areas: - Fitness unchanged at 0.7367\n- Consider simplifying - code length exceeds 500 characters\n\n## Last Execution Output\n\n### stdout\n```\nRunning in subprocess, Python version: 3.12.1 (v3.12.1:2305ca5144, Dec  7 2023, 17:23:38) [Clang 13.0.0 (clang-1300.0.29.30)]\nProgram path: /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmpsrmy4pd8.py\nCalling run_packing()...\nrun_packing() returned successfully: sum_radii = 1.941180729934942\nResults saved to /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmp8qkkud97.py.results\n\n```\n\n### stderr\n```\n\n```\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 3\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 1.9412, target_ratio: 0.7367, combined_score: 0.7367, eval_time: 1.0150\n- Outcome: Mixed results\n\n### Attempt 2\n- Changes: Unknown changes\n- Metrics: validity: 1.0000, sum_radii: 2.0925, target_ratio: 0.7941, combined_score: 0.7941, eval_time: 0.0768\n- Outcome: Improvement in all metrics\n\n### Attempt 1\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.0927, target_ratio: 0.7942, combined_score: 0.7942, eval_time: 0.0639\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.7942)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 in the unit square.\nUses a near-hex layered layout rows=[6,5,5,5,5] and a 1D search over\nhorizontal spacing to maximize the sum of radii (wall- and neighbor-limited).\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    rows = [6, 5, 5, 5, 5]  # sums to 26\n\n    def build_centers(s):\n        v = (np.sqrt(3) / 2.0) * s\n        R = len(rows)\n        total_v = (R - 1) * v\n        y0 = (1.0 - total_v) / 2.0\n        pts = []\n        for r_idx, k in enumerate(rows):\n            span = (k - 1) * s\n            x0 = (1.0 - span) / 2.0\n            shift = (r_idx % 2) * (s / 2.0)\n            y = y0 + r_idx * v\n            for i in range(k):\n                x = x0 + i * s + shift\n                pts.append([x, y])\n        return np.array(pts)\n\n    def radii_for_centers(centers):\n        # wall distance\n        wall = np.minimum.reduce([centers[:, 0], centers[:, 1],\n                                  1 - centers[:, 0], 1 - centers[:, 1]])\n        # pairwise distances (vectorized)\n        dif = centers[:, None, :] - centers[None, :, :]\n        dmat = np.hypot(dif[..., 0], dif[..., 1])\n        np.fill_diagonal(dmat, np.inf)\n        nearest = 0.5 * np.min(dmat, axis=1)\n        return np.minimum(wall, nearest)\n\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    # search horizontal spacing s; bounds chosen to keep centers inside box\n    for s in np.linspace(0.06, 0.24, 300):\n        centers = build_centers(s)\n        if np.any(centers < -1e-10) or np.any(centers > 1 + 1e-10):\n            continue\n        radii = radii_for_centers(centers)\n        if np.any(radii <= 1e-12):\n            continue\n        ssum = float(np.sum(radii))\n        if ssum > best_sum:\n            best_sum = ssum\n            best_centers = centers.copy()\n            best_radii = radii.copy()\n\n    if best_centers is None:\n        best_centers = build_centers(0.12)\n        best_radii = radii_for_centers(best_centers)\n        best_sum = float(np.sum(best_radii))\n\n    return best_centers, best_radii, best_sum\n# EVOLVE-BLOCK-END\n\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\nif __name__ == \"__main__\":\n    centers, radii, s = run_packing()\n    print(f\"Sum of radii: {s:.6f}\")\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.0927), Performs well on target_ratio (0.7942), Performs well on combined_score (0.7942), Performs well on eval_time (0.0639)\n\n### Program 2 (Score: 0.7941)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles in a unit square.\nCreates an explicit near-hexagonal layered layout (row counts [6,5,5,5,5])\nand tunes the lattice spacing to maximize the sum of radii (simple grid search).\n\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    n = 26\n    # row counts that sum to 26: a near-hexagonal layering\n    rows = [6, 5, 5, 5, 5]\n\n    def build_centers(s):\n        \"\"\"Build centers for lattice spacing s (horizontal spacing).\"\"\"\n        v = np.sqrt(3) / 2 * s  # vertical spacing for hex arrangement\n        centers = []\n        R = len(rows)\n        total_v = (R - 1) * v\n        y0 = (1.0 - total_v) / 2.0\n        for r_idx, k in enumerate(rows):\n            span = (k - 1) * s\n            x0 = (1.0 - span) / 2.0\n            # alternate shift for hex packing\n            shift = (r_idx % 2) * (s / 2.0)\n            y = y0 + r_idx * v\n            for i in range(k):\n                x = x0 + i * s + shift\n                centers.append([x, y])\n        return np.array(centers)\n\n    def radii_for_centers(centers):\n        \"\"\"Compute non-overlapping radii: limited by walls and half nearest neighbor.\"\"\"\n        m = centers.shape[0]\n        # distance to walls\n        wall = np.minimum.reduce([centers[:, 0], centers[:, 1], 1 - centers[:, 0], 1 - centers[:, 1]])\n        # pairwise distances\n        radii = wall.copy()\n        for i in range(m):\n            # compute distances to others\n            dists = np.sqrt(np.sum((centers - centers[i]) ** 2, axis=1))\n            dists[i] = np.inf\n            min_dist = np.min(dists)\n            radii[i] = min(radii[i], 0.5 * min_dist)\n        return radii\n\n    # Search for the best horizontal spacing s that maximizes sum of radii\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    # sensible bounds: too small -> tiny circles; too large -> centers outside square\n    # pick grid of spacings\n    for s in np.linspace(0.06, 0.24, 200):\n        centers = build_centers(s)\n        # reject if any center lies outside [0,1] (numerical safety)\n        if np.any(centers < 0 - 1e-8) or np.any(centers > 1 + 1e-8):\n            continue\n        radii = radii_for_centers(centers)\n        # if any radius non-positive, skip\n        if np.any(radii <= 0):\n            continue\n        ssum = float(np.sum(radii))\n        if ssum > best_sum:\n            best_sum = ssum\n            best_centers = centers.copy()\n            best_radii = radii.copy()\n\n    # Fallback: if search failed, place a simple layout\n    if best_centers is None:\n        best_centers = build_centers(0.12)\n        best_radii = radii_for_centers(best_centers)\n        best_sum = float(np.sum(best_radii))\n\n    return best_centers, best_radii, best_sum\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.0925), Performs well on target_ratio (0.7941), Performs well on combined_score (0.7941), Performs well on eval_time (0.0768)\n\n### Program 3 (Score: 0.7367)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Improved constructor for 26 circles in the unit square.\nUses a denser near-hex layering [7,6,5,4,4] and a small 2D search over\nhorizontal spacing and vertical compression to maximize sum of radii.\n\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    rows = [7, 6, 5, 4, 4]  # sums to 26\n\n    def build_centers(s, vfac, alt_shift):\n        v = (np.sqrt(3) / 2.0) * s * vfac\n        R = len(rows)\n        total_v = (R - 1) * v\n        y0 = (1.0 - total_v) / 2.0\n        pts = []\n        for r_idx, k in enumerate(rows):\n            span = (k - 1) * s\n            shift = (r_idx % 2) * (alt_shift * s)\n            # center the row after applying shift\n            x0 = (1.0 - span) / 2.0 - shift\n            y = y0 + r_idx * v\n            for i in range(k):\n                x = x0 + shift + i * s\n                pts.append([x, y])\n        return np.array(pts)\n\n    def radii_for_centers(centers):\n        m = centers.shape[0]\n        # distance to walls\n        wall = np.minimum.reduce([centers[:, 0], centers[:, 1], 1 - centers[:, 0], 1 - centers[:, 1]])\n        # pairwise distances\n        dif = centers[:, None, :] - centers[None, :, :]\n        dmat = np.hypot(dif[..., 0], dif[..., 1])\n        np.fill_diagonal(dmat, np.inf)\n        nearest = 0.5 * np.min(dmat, axis=1)\n        radii = np.minimum(wall, nearest)\n        return radii\n\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    s_range = np.linspace(0.06, 0.22, 220)\n    vfac_range = np.linspace(0.86, 1.06, 56)\n    # try two shift patterns: full hex (0.5) and half/no shift (0.0/0.4) to probe asymmetry\n    shift_modes = [0.5, 0.4, 0.0]\n\n    for s in s_range:\n        for vfac in vfac_range:\n            for alt_shift in shift_modes:\n                centers = build_centers(s, vfac, alt_shift)\n                # quick reject if any center outside box\n                if np.any(centers < -1e-9) or np.any(centers > 1 + 1e-9):\n                    continue\n                radii = radii_for_centers(centers)\n                if np.any(radii <= 1e-12):\n                    continue\n                ssum = float(np.sum(radii))\n                if ssum > best_sum:\n                    best_sum = ssum\n                    best_centers = centers.copy()\n                    best_radii = radii.copy()\n\n    # fallback if search failed\n    if best_centers is None:\n        best_centers = build_centers(0.12, 1.0, 0.5)\n        best_radii = radii_for_centers(best_centers)\n        best_sum = float(np.sum(best_radii))\n\n    return best_centers, best_radii, best_sum\n\n\n# EVOLVE-BLOCK-END\n\n\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(6, 6))\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    for c, r in zip(centers, radii):\n        ax.add_patch(Circle(c, r, alpha=0.6, edgecolor=\"k\"))\n    ax.grid(True)\n    plt.title(f\"n={len(centers)}, sum={sum(radii):.6f}\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # visualize(centers, radii)\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (1.9412), Performs well on target_ratio (0.7367), Performs well on combined_score (0.7367), Performs well on eval_time (1.0150)\n\n\n\n## Diverse Programs\n\n### Program D1 (Score: 0.5928)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Improved constructor for n=26 circle packing in the unit square.\nUses a symmetric 5-row near-hex layering [4,5,6,6,5] and a\nstable, order-invariant pairwise \"multiplicative projection\" solver\nto maximize radii subject to non-overlap and wall constraints.\nSearches horizontal spacing s to find the best layout.\n\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    n = 26\n    # symmetric rows summing to 26\n    rows = [4, 5, 6, 6, 5]\n\n    def build_centers(s):\n        v = (np.sqrt(3) / 2.0) * s\n        centers = []\n        R = len(rows)\n        total_v = (R - 1) * v\n        y0 = (1.0 - total_v) / 2.0\n        for r_idx, k in enumerate(rows):\n            span = (k - 1) * s\n            x0 = (1.0 - span) / 2.0\n            shift = (r_idx % 2) * (s / 2.0)\n            y = y0 + r_idx * v\n            for i in range(k):\n                x = x0 + i * s + shift\n                centers.append([x, y])\n        C = np.array(centers)\n        return C\n\n    def stabilize_radii(centers, maxiter=2000, tol=1e-10):\n        m = centers.shape[0]\n        # initial radii limited by walls\n        wall = np.minimum.reduce([centers[:, 0], centers[:, 1], 1 - centers[:, 0], 1 - centers[:, 1]])\n        r = wall.copy()\n        # small floor to avoid division issues\n        eps = 1e-12\n        for it in range(maxiter):\n            # pairwise distances\n            diff = centers[:, None, :] - centers[None, :, :]\n            D = np.sqrt((diff ** 2).sum(axis=2))\n            np.fill_diagonal(D, np.inf)\n            S = r[:, None] + r[None, :]\n            # violations where sum radii exceed distance\n            over = S - D\n            max_over = np.max(over)\n            if max_over <= tol:\n                break\n            # compute multiplicative factors per pair f_ij = D / S (<=1)\n            # avoid division by zero\n            with np.errstate(divide='ignore', invalid='ignore'):\n                F = np.where(S > eps, D / S, 1.0)\n            # pairs without violation have factor >=1, but we clamp to 1\n            F = np.minimum(F, 1.0)\n            # per-circle factor is minimum across its row (must satisfy all pairs)\n            f = F.min(axis=1)\n            # ensure we don't increase radii accidentally\n            f = np.minimum(f, 1.0)\n            # if no effective change, break\n            if np.max(1 - f) <= tol:\n                break\n            r *= f\n            # re-enforce wall bounds (numerical safety)\n            r = np.minimum(r, wall)\n            # floor\n            r = np.maximum(r, eps)\n        return r\n\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    # search s; bounds chosen to allow rows to fit comfortably\n    for s in np.linspace(0.06, 0.28, 280):\n        centers = build_centers(s)\n        # reject if any center outside unit square\n        if np.any(centers < -1e-9) or np.any(centers > 1 + 1e-9):\n            continue\n        radii = stabilize_radii(centers)\n        if np.any(radii <= 0):\n            continue\n        ssum = float(radii.sum())\n        if ssum > best_sum:\n            best_sum = ssum\n            best_centers = centers.copy()\n            best_radii = radii.copy()\n\n    if best_centers is None:\n        # fallback: reasonable default spacing\n        best_centers = build_centers(0.12)\n        best_radii = stabilize_radii(best_centers)\n        best_sum = float(best_radii.sum())\n\n    return best_centers, best_radii, best_sum\n\n\n# EVOLVE-BLOCK-END\n\n\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(7, 7))\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    for i, (c, r) in enumerate(zip(centers, radii)):\n        ax.add_patch(Circle(c, r, alpha=0.6, edgecolor=\"k\"))\n        ax.text(c[0], c[1], str(i), ha=\"center\", va=\"center\", fontsize=7)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # visualize(centers, radii)\n```\nKey features: Alternative approach to validity, Alternative approach to sum_radii\n\n### Program D2 (Score: 0.6657)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Improved constructor-based circle packing for n=26 in a unit square.\n\nThis version:\n- keeps an explicit layered (near-hex) layout but adds two geometric degrees:\n  vertical scaling (y_scale) and per-row horizontal compression (gamma).\n- searches over (s, y_scale, gamma) grid to find best center geometry.\n- for a fixed set of centers, solves for radii by maximizing sum of radii\n  under linear constraints r_i <= wall_i and r_i + r_j <= d_ij using an\n  iterative multiplicative projection. This is an order-insensitive,\n  conservative approximation of the LP optimum and typically beats the\n  naive \"half nearest neighbor\" rule.\n\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    n = 26\n    rows = [6, 5, 5, 5, 5]  # sum = 26\n\n    def build_centers(s, y_scale, gamma):\n        \"\"\"Build centers with horizontal spacing s, vertical scale y_scale,\n        and per-row compression amplitude gamma (0..~0.2).\"\"\"\n        v = (np.sqrt(3) / 2.0) * s * y_scale\n        centers = []\n        R = len(rows)\n        total_v = (R - 1) * v\n        y0 = (1.0 - total_v) / 2.0\n        mid = (R - 1) / 2.0\n        for r_idx, k in enumerate(rows):\n            # rscale compresses outer rows a bit when gamma>0\n            if mid > 0:\n                rscale = 1.0 - gamma * (abs(r_idx - mid) / mid)\n            else:\n                rscale = 1.0\n            rscale = max(0.75, min(1.05, rscale))  # safety clamp\n            span = (k - 1) * s * rscale\n            x0 = (1.0 - span) / 2.0\n            shift = (r_idx % 2) * (s * rscale) / 2.0\n            y = y0 + r_idx * v\n            for i in range(k):\n                x = x0 + i * s * rscale + shift\n                centers.append([x, y])\n        return np.array(centers)\n\n    def solve_radii(centers):\n        \"\"\"Maximize sum r_i subject to r_i <= wall_i and r_i + r_j <= d_ij\n        via iterative multiplicative projection (fast, order-insensitive).\"\"\"\n        m = centers.shape[0]\n        # distances\n        diff = centers[:, None, :] - centers[None, :, :]  # m x m x 2\n        dists = np.sqrt(np.sum(diff ** 2, axis=2))\n        np.fill_diagonal(dists, np.inf)\n        # walls\n        wall = np.minimum.reduce([centers[:, 0], centers[:, 1], 1 - centers[:, 0], 1 - centers[:, 1]])\n        # start from wall limits (largest each circle could be alone)\n        r = wall.copy()\n        # iterative multiplicative adjustments to satisfy pairwise constraints\n        tol = 1e-7\n        for _ in range(300):\n            # compute pairwise sums\n            sum_mat = r[:, None] + r[None, :]\n            # find violations where sum > dists\n            violate_mask = sum_mat > dists\n            if not np.any(violate_mask):\n                break\n            # for each circle compute the most restrictive scale factor needed\n            scales = np.ones(m)\n            # For efficiency iterate over upper triangle of violations\n            vi, vj = np.where(np.triu(violate_mask, k=1))\n            if vi.size == 0:\n                break\n            for a, b in zip(vi, vj):\n                cur_sum = r[a] + r[b]\n                if cur_sum <= 0:\n                    continue\n                # scale needed so that r[a] + r[b] == dists[a,b]\n                s = dists[a, b] / cur_sum\n                if s < scales[a]:\n                    scales[a] = s\n                if s < scales[b]:\n                    scales[b] = s\n            new_r = r * scales\n            change = np.max(np.abs(new_r - r))\n            r = new_r\n            if change < tol:\n                break\n        # final pass to clean any tiny numerical violations\n        sum_mat = r[:, None] + r[None, :]\n        vi, vj = np.where(np.triu(sum_mat - dists > 1e-12, k=1))\n        for a, b in zip(vi, vj):\n            s = dists[a, b] / (r[a] + r[b])\n            r[a] *= s\n            r[b] *= s\n        # enforce walls strictly (numerical safety)\n        r = np.minimum(r, wall)\n        r[r < 0] = 0.0\n        return r\n\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    # parameter grids (coarse but effective)\n    s_vals = np.linspace(0.065, 0.24, 220)      # horizontal spacing\n    y_scales = np.linspace(0.92, 1.08, 7)       # vertical stretch/shrink\n    gammas = np.linspace(0.0, 0.18, 7)          # per-row compression amplitude\n\n    for s in s_vals:\n        for y_scale in y_scales:\n            for gamma in gammas:\n                centers = build_centers(s, y_scale, gamma)\n                # early reject centers outside the square\n                if np.any(centers < -1e-9) or np.any(centers > 1 + 1e-9):\n                    continue\n                radii = solve_radii(centers)\n                if np.any(radii <= 0):\n                    continue\n                ssum = float(np.sum(radii))\n                if ssum > best_sum:\n                    best_sum = ssum\n                    best_centers = centers.copy()\n                    best_radii = radii.copy()\n\n    # fallback conservative layout (should rarely be used)\n    if best_centers is None:\n        centers = build_centers(0.12, 1.0, 0.0)\n        best_radii = solve_radii(centers)\n        best_centers = centers\n        best_sum = float(np.sum(best_radii))\n\n    return best_centers, best_radii, best_sum\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n    for i, (c, r) in enumerate(zip(centers, radii)):\n        ax.add_patch(Circle(c, r, alpha=0.6, edgecolor=\"k\"))\n        ax.text(c[0], c[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # visualize(centers, radii)\n```\nKey features: Alternative approach to validity, Alternative approach to sum_radii\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 0.7942, Type: Alternative)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 in the unit square.\nUses a near-hex layered layout rows=[6,5,5,5,5] and a 1D search over\nhorizontal spacing to maximize the sum of radii (wall- and neighbor-limited).\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    rows = [6, 5, 5, 5, 5]  # sums to 26\n\n    def build_centers(s):\n        v = (np.sqrt(3) / 2.0) * s\n        R = len(rows)\n        total_v = (R - 1) * v\n        y0 = (1.0 - total_v) / 2.0\n        pts = []\n        for r_idx, k in enumerate(rows):\n            span = (k - 1) * s\n            x0 = (1.0 - span) / 2.0\n            shift = (r_idx % 2) * (s / 2.0)\n            y = y0 + r_idx * v\n            for i in range(k):\n                x = x0 + i * s + shift\n                pts.append([x, y])\n        return np.array(pts)\n\n    def radii_for_centers(centers):\n        # wall distance\n        wall = np.minimum.reduce([centers[:, 0], centers[:, 1],\n                                  1 - centers[:, 0], 1 - centers[:, 1]])\n        # pairwise distances (vectorized)\n        dif = centers[:, None, :] - centers[None, :, :]\n        dmat = np.hypot(dif[..., 0], dif[..., 1])\n        np.fill_diagonal(dmat, np.inf)\n        nearest = 0.5 * np.min(dmat, axis=1)\n        return np.minimum(wall, nearest)\n\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    # search horizontal spacing s; bounds chosen to keep centers inside box\n    for s in np.linspace(0.06, 0.24, 300):\n        centers = build_centers(s)\n        if np.any(centers < -1e-10) or np.any(centers > 1 + 1e-10):\n            continue\n        radii = radii_for_centers(centers)\n        if np.any(radii <= 1e-12):\n            continue\n        ssum = float(np.sum(radii))\n        if ssum > best_sum:\n            best_sum = ssum\n            best_centers = centers.copy()\n            best_radii = radii.copy()\n\n    if best_centers is None:\n        best_centers = build_centers(0.12)\n        best_radii = radii_for_centers(best_centers)\n        best_sum = float(np.sum(best_radii))\n\n    return best_centers, best_radii, best_sum\n# EVOLVE-BLOCK-END\n\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\nif __name__ == \"__main__\":\n    centers, radii, s = run_packing()\n    print(f\"Sum of radii: {s:.6f}\")\n```\nUnique approach: Modification: Full rewrite, Excellent validity (1.000), Excellent sum_radii (2.093)\n\n### Inspiration 2 (Score: 0.7941, Type: Alternative)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles in a unit square.\nCreates an explicit near-hexagonal layered layout (row counts [6,5,5,5,5])\nand tunes the lattice spacing to maximize the sum of radii (simple grid search).\n\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    n = 26\n    # row counts that sum to 26: a near-hexagonal layering\n    rows = [6, 5, 5, 5, 5]\n\n    def build_centers(s):\n        \"\"\"Build centers for lattice spacing s (horizontal spacing).\"\"\"\n        v = np.sqrt(3) / 2 * s  # vertical spacing for hex arrangement\n        centers = []\n        R = len(rows)\n        total_v = (R - 1) * v\n        y0 = (1.0 - total_v) / 2.0\n        for r_idx, k in enumerate(rows):\n            span = (k - 1) * s\n            x0 = (1.0 - span) / 2.0\n            # alternate shift for hex packing\n            shift = (r_idx % 2) * (s / 2.0)\n            y = y0 + r_idx * v\n            for i in range(k):\n                x = x0 + i * s + shift\n                centers.append([x, y])\n        return np.array(centers)\n\n    def radii_for_centers(centers):\n        \"\"\"Compute non-overlapping radii: limited by walls and half nearest neighbor.\"\"\"\n        m = centers.shape[0]\n        # distance to walls\n        wall = np.minimum.reduce([centers[:, 0], centers[:, 1], 1 - centers[:, 0], 1 - centers[:, 1]])\n        # pairwise distances\n        radii = wall.copy()\n        for i in range(m):\n            # compute distances to others\n            dists = np.sqrt(np.sum((centers - centers[i]) ** 2, axis=1))\n            dists[i] = np.inf\n            min_dist = np.min(dists)\n            radii[i] = min(radii[i], 0.5 * min_dist)\n        return radii\n\n    # Search for the best horizontal spacing s that maximizes sum of radii\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    # sensible bounds: too small -> tiny circles; too large -> centers outside square\n    # pick grid of spacings\n    for s in np.linspace(0.06, 0.24, 200):\n        centers = build_centers(s)\n        # reject if any center lies outside [0,1] (numerical safety)\n        if np.any(centers < 0 - 1e-8) or np.any(centers > 1 + 1e-8):\n            continue\n        radii = radii_for_centers(centers)\n        # if any radius non-positive, skip\n        if np.any(radii <= 0):\n            continue\n        ssum = float(np.sum(radii))\n        if ssum > best_sum:\n            best_sum = ssum\n            best_centers = centers.copy()\n            best_radii = radii.copy()\n\n    # Fallback: if search failed, place a simple layout\n    if best_centers is None:\n        best_centers = build_centers(0.12)\n        best_radii = radii_for_centers(best_centers)\n        best_sum = float(np.sum(best_radii))\n\n    return best_centers, best_radii, best_sum\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nUnique approach: Excellent validity (1.000), Excellent sum_radii (2.092), Alternative eval_time approach\n\n### Inspiration 3 (Score: 0.6657, Type: Alternative)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Improved constructor-based circle packing for n=26 in a unit square.\n\nThis version:\n- keeps an explicit layered (near-hex) layout but adds two geometric degrees:\n  vertical scaling (y_scale) and per-row horizontal compression (gamma).\n- searches over (s, y_scale, gamma) grid to find best center geometry.\n- for a fixed set of centers, solves for radii by maximizing sum of radii\n  under linear constraints r_i <= wall_i and r_i + r_j <= d_ij using an\n  iterative multiplicative projection. This is an order-insensitive,\n  conservative approximation of the LP optimum and typically beats the\n  naive \"half nearest neighbor\" rule.\n\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    n = 26\n    rows = [6, 5, 5, 5, 5]  # sum = 26\n\n    def build_centers(s, y_scale, gamma):\n        \"\"\"Build centers with horizontal spacing s, vertical scale y_scale,\n        and per-row compression amplitude gamma (0..~0.2).\"\"\"\n        v = (np.sqrt(3) / 2.0) * s * y_scale\n        centers = []\n        R = len(rows)\n        total_v = (R - 1) * v\n        y0 = (1.0 - total_v) / 2.0\n        mid = (R - 1) / 2.0\n        for r_idx, k in enumerate(rows):\n            # rscale compresses outer rows a bit when gamma>0\n            if mid > 0:\n                rscale = 1.0 - gamma * (abs(r_idx - mid) / mid)\n            else:\n                rscale = 1.0\n            rscale = max(0.75, min(1.05, rscale))  # safety clamp\n            span = (k - 1) * s * rscale\n            x0 = (1.0 - span) / 2.0\n            shift = (r_idx % 2) * (s * rscale) / 2.0\n            y = y0 + r_idx * v\n            for i in range(k):\n                x = x0 + i * s * rscale + shift\n                centers.append([x, y])\n        return np.array(centers)\n\n    def solve_radii(centers):\n        \"\"\"Maximize sum r_i subject to r_i <= wall_i and r_i + r_j <= d_ij\n        via iterative multiplicative projection (fast, order-insensitive).\"\"\"\n        m = centers.shape[0]\n        # distances\n        diff = centers[:, None, :] - centers[None, :, :]  # m x m x 2\n        dists = np.sqrt(np.sum(diff ** 2, axis=2))\n        np.fill_diagonal(dists, np.inf)\n        # walls\n        wall = np.minimum.reduce([centers[:, 0], centers[:, 1], 1 - centers[:, 0], 1 - centers[:, 1]])\n        # start from wall limits (largest each circle could be alone)\n        r = wall.copy()\n        # iterative multiplicative adjustments to satisfy pairwise constraints\n        tol = 1e-7\n        for _ in range(300):\n            # compute pairwise sums\n            sum_mat = r[:, None] + r[None, :]\n            # find violations where sum > dists\n            violate_mask = sum_mat > dists\n            if not np.any(violate_mask):\n                break\n            # for each circle compute the most restrictive scale factor needed\n            scales = np.ones(m)\n            # For efficiency iterate over upper triangle of violations\n            vi, vj = np.where(np.triu(violate_mask, k=1))\n            if vi.size == 0:\n                break\n            for a, b in zip(vi, vj):\n                cur_sum = r[a] + r[b]\n                if cur_sum <= 0:\n                    continue\n                # scale needed so that r[a] + r[b] == dists[a,b]\n                s = dists[a, b] / cur_sum\n                if s < scales[a]:\n                    scales[a] = s\n                if s < scales[b]:\n                    scales[b] = s\n            new_r = r * scales\n            change = np.max(np.abs(new_r - r))\n            r = new_r\n            if change < tol:\n                break\n        # final pass to clean any tiny numerical violations\n        sum_mat = r[:, None] + r[None, :]\n        vi, vj = np.where(np.triu(sum_mat - dists > 1e-12, k=1))\n        for a, b in zip(vi, vj):\n            s = dists[a, b] / (r[a] + r[b])\n            r[a] *= s\n            r[b] *= s\n        # enforce walls strictly (numerical safety)\n        r = np.minimum(r, wall)\n        r[r < 0] = 0.0\n        return r\n\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    # parameter grids (coarse but effective)\n    s_vals = np.linspace(0.065, 0.24, 220)      # horizontal spacing\n    y_scales = np.linspace(0.92, 1.08, 7)       # vertical stretch/shrink\n    gammas = np.linspace(0.0, 0.18, 7)          # per-row compression amplitude\n\n    for s in s_vals:\n        for y_scale in y_scales:\n            for gamma in gammas:\n                centers = build_centers(s, y_scale, gamma)\n                # early reject centers outside the square\n                if np.any(centers < -1e-9) or np.any(centers > 1 + 1e-9):\n                    continue\n                radii = solve_radii(centers)\n                if np.any(radii <= 0):\n                    continue\n                ssum = float(np.sum(radii))\n                if ssum > best_sum:\n                    best_sum = ssum\n                    best_centers = centers.copy()\n                    best_radii = radii.copy()\n\n    # fallback conservative layout (should rarely be used)\n    if best_centers is None:\n        centers = build_centers(0.12, 1.0, 0.0)\n        best_radii = solve_radii(centers)\n        best_centers = centers\n        best_sum = float(np.sum(best_radii))\n\n    return best_centers, best_radii, best_sum\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n    for i, (c, r) in enumerate(zip(centers, radii)):\n        ax.add_patch(Circle(c, r, alpha=0.6, edgecolor=\"k\"))\n        ax.text(c[0], c[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # visualize(centers, radii)\n```\nUnique approach: Modification: Full rewrite, Excellent validity (1.000), Excellent sum_radii (1.754)\n\n# Current Program\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Improved constructor for 26 circles in the unit square.\nUses a denser near-hex layering [7,6,5,4,4] and a small 2D search over\nhorizontal spacing and vertical compression to maximize sum of radii.\n\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    rows = [7, 6, 5, 4, 4]  # sums to 26\n\n    def build_centers(s, vfac, alt_shift):\n        v = (np.sqrt(3) / 2.0) * s * vfac\n        R = len(rows)\n        total_v = (R - 1) * v\n        y0 = (1.0 - total_v) / 2.0\n        pts = []\n        for r_idx, k in enumerate(rows):\n            span = (k - 1) * s\n            shift = (r_idx % 2) * (alt_shift * s)\n            # center the row after applying shift\n            x0 = (1.0 - span) / 2.0 - shift\n            y = y0 + r_idx * v\n            for i in range(k):\n                x = x0 + shift + i * s\n                pts.append([x, y])\n        return np.array(pts)\n\n    def radii_for_centers(centers):\n        m = centers.shape[0]\n        # distance to walls\n        wall = np.minimum.reduce([centers[:, 0], centers[:, 1], 1 - centers[:, 0], 1 - centers[:, 1]])\n        # pairwise distances\n        dif = centers[:, None, :] - centers[None, :, :]\n        dmat = np.hypot(dif[..., 0], dif[..., 1])\n        np.fill_diagonal(dmat, np.inf)\n        nearest = 0.5 * np.min(dmat, axis=1)\n        radii = np.minimum(wall, nearest)\n        return radii\n\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    s_range = np.linspace(0.06, 0.22, 220)\n    vfac_range = np.linspace(0.86, 1.06, 56)\n    # try two shift patterns: full hex (0.5) and half/no shift (0.0/0.4) to probe asymmetry\n    shift_modes = [0.5, 0.4, 0.0]\n\n    for s in s_range:\n        for vfac in vfac_range:\n            for alt_shift in shift_modes:\n                centers = build_centers(s, vfac, alt_shift)\n                # quick reject if any center outside box\n                if np.any(centers < -1e-9) or np.any(centers > 1 + 1e-9):\n                    continue\n                radii = radii_for_centers(centers)\n                if np.any(radii <= 1e-12):\n                    continue\n                ssum = float(np.sum(radii))\n                if ssum > best_sum:\n                    best_sum = ssum\n                    best_centers = centers.copy()\n                    best_radii = radii.copy()\n\n    # fallback if search failed\n    if best_centers is None:\n        best_centers = build_centers(0.12, 1.0, 0.5)\n        best_radii = radii_for_centers(best_centers)\n        best_sum = float(np.sum(best_radii))\n\n    return best_centers, best_radii, best_sum\n\n\n# EVOLVE-BLOCK-END\n\n\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(6, 6))\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    for c, r in zip(centers, radii):\n        ax.add_patch(Circle(c, r, alpha=0.6, edgecolor=\"k\"))\n    ax.grid(True)\n    plt.title(f\"n={len(centers)}, sum={sum(radii):.6f}\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # visualize(centers, radii)\n```\n\n# Task\nRewrite the program to improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: complexity, diversity\nDifferent solutions with similar fitness but different features are valuable.\nProvide the complete new program code.\n\nIMPORTANT: Make sure your rewritten program maintains the same inputs and outputs\nas the original program, but with improved internal implementation.\n\n```python\n# Your rewritten program here\n```", "responses": ["```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 in the unit square.\nUse a near-hex layered layout rows=[6,5,5,5,5] and search horizontal spacing s\nto maximize sum of radii (each radius limited by walls and half nearest-neighbor).\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    rows = [6,5,5,5,5]  # sums to 26\n\n    def build_centers(s):\n        v = (np.sqrt(3)/2.0)*s\n        R = len(rows)\n        total_v = (R-1)*v\n        y0 = (1.0 - total_v)/2.0\n        pts = []\n        for r_idx,k in enumerate(rows):\n            span = (k-1)*s\n            x0 = (1.0 - span)/2.0\n            shift = (r_idx % 2)*(s/2.0)\n            y = y0 + r_idx*v\n            for i in range(k):\n                x = x0 + i*s + shift\n                pts.append([x,y])\n        return np.array(pts)\n\n    def radii_for_centers(centers):\n        # distance to walls\n        wall = np.minimum.reduce([centers[:,0], centers[:,1], 1-centers[:,0], 1-centers[:,1]])\n        # pairwise distances\n        dif = centers[:,None,:] - centers[None,:,:]\n        dmat = np.hypot(dif[...,0], dif[...,1])\n        np.fill_diagonal(dmat, np.inf)\n        nearest = 0.5*np.min(dmat, axis=1)\n        return np.minimum(wall, nearest)\n\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    for s in np.linspace(0.06, 0.24, 300):\n        C = build_centers(s)\n        if np.any(C < -1e-9) or np.any(C > 1+1e-9):\n            continue\n        r = radii_for_centers(C)\n        if np.any(r <= 1e-12):\n            continue\n        ssum = float(np.sum(r))\n        if ssum > best_sum:\n            best_sum = ssum\n            best_centers = C.copy()\n            best_radii = r.copy()\n\n    if best_centers is None:\n        best_centers = build_centers(0.12)\n        best_radii = radii_for_centers(best_centers)\n        best_sum = float(np.sum(best_radii))\n\n    return best_centers, best_radii, best_sum\n# EVOLVE-BLOCK-END\n\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\nif __name__ == \"__main__\":\n    centers, radii, s = run_packing()\n    print(f\"Sum of radii: {s:.6f}\")\n```"]}}, "artifacts_json": "{\"stdout\": \"Running in subprocess, Python version: 3.12.1 (v3.12.1:2305ca5144, Dec  7 2023, 17:23:38) [Clang 13.0.0 (clang-1300.0.29.30)]\\nProgram path: /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmp9osx0x9q.py\\nCalling run_packing()...\\nrun_packing() returned successfully: sum_radii = 2.0926651050960663\\nResults saved to /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmpzrm3l7uj.py.results\\n\", \"stderr\": \"\"}", "artifact_dir": null}