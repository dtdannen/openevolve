{
  "failure_patterns": {
    "performance_regression:sum_radii decreased by n.n% (n.n \u2192 n.n)": {
      "pattern_type": "performance_regression",
      "description": "sum_radii decreased by 65.0% (0.960 \u2192 0.336)",
      "count": 3,
      "first_seen": 7,
      "last_seen": 20,
      "example_error": null,
      "parent_code": "# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square\n    that attempts to maximize the sum of their radii.\n\n    Returns:\n        Tuple of (centers, radii, sum_of_radii)\n        centers: np.array of shape (26, 2) with (x, y) coordinates\n        radii: np.array of shape (26) with radius of each circle\n        sum_of_radii: Sum of all radii\n    \"\"\"\n    # Initialize arrays for 26 circles\n    n = 26\n    centers = np.zeros((n, 2))\n\n    # Place circles in a structured pattern\n    # This is a simple pattern - evolution will improve this\n\n    # First, place a large circle in the center\n    centers[0] = [0.5, 0.5]\n\n    # Place 8 circles around it in a ring\n    for i in range(8):\n        angle = 2 * np.pi * i / 8\n        centers[i + 1] = [0.5 + 0.3 * np.cos(angle), 0.5 + 0.3 * np.sin(angle)]\n\n    # Place 16 more circles in an outer ring\n    for i in range(16):\n        angle = 2 * np.pi * i / 16\n        centers[i + 9] = [0.5 + 0.7 * np.cos(angle), 0.5 + 0.7 * np.sin(angle)]\n\n    # Additional positioning adjustment to make sure all circles\n    # are inside the square and don't overlap\n    # Clip to ensure everything is inside the unit square\n    centers = np.clip(centers, 0.01, 0.99)\n\n    # Compute maximum valid radii for this configuration\n    radii = compute_max_radii(centers)\n\n    # Calculate the sum of radii\n    sum_radii = np.sum(radii)\n\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers):\n    \"\"\"\n    Compute the maximum possible radii for each circle position\n    such that they don't overlap and stay within the unit square.\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n\n    Returns:\n        np.array of shape (n) with radius of each circle\n    \"\"\"\n    n = centers.shape[0]\n    radii = np.ones(n)\n\n    # First, limit by distance to square borders\n    for i in range(n):\n        x, y = centers[i]\n        # Distance to borders\n        radii[i] = min(x, y, 1 - x, 1 - y)\n\n    # Then, limit by distance to other circles\n    # Each pair of circles with centers at distance d can have\n    # sum of radii at most d to avoid overlap\n    for i in range(n):\n        for j in range(i + 1, n):\n            dist = np.sqrt(np.sum((centers[i] - centers[j]) ** 2))\n\n            # If current radii would cause overlap\n            if radii[i] + radii[j] > dist:\n                # Scale both radii proportionally\n                scale = dist / (radii[i] + radii[j])\n                radii[i] *= scale\n                radii[j] *= scale\n\n    return radii\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.5)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\")\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii}\")\n    # AlphaEvolve improved this to 2.635\n\n    # Uncomment to visualize:\n    visualize(centers, radii)\n",
      "child_code": "# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (improved constructor)\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square\n    and compute maximal feasible radii (LP-like iterative tightening).\n    Returns: centers (26,2), radii (26,), sum_of_radii\n    \"\"\"\n    n = 26\n\n    # Hex-like layered layout but with row counts chosen to fit 26 circles:\n    rows = [5, 5, 6, 5, 5]  # sums to 26\n    max_cols = max(rows)\n\n    # Choose a spacing that fills the square reasonably densely without overlap\n    s = 0.192  # horizontal spacing (tuned)\n    sy = s * np.sqrt(3) / 2.0  # vertical spacing for hex/triangular packing\n\n    total_rows = len(rows)\n    y0 = (1.0 - (total_rows - 1) * sy) / 2.0  # center rows vertically\n\n    centers = np.zeros((n, 2))\n    idx = 0\n    for r_idx, count in enumerate(rows):\n        y = y0 + r_idx * sy\n        # center the row horizontally\n        x_start = (1.0 - (count - 1) * s) / 2.0\n        for k in range(count):\n            x = x_start + k * s\n            centers[idx] = [x, y]\n            idx += 1\n\n    # Ensure centers remain inside square numerically\n    centers = np.clip(centers, 1e-8, 1.0 - 1e-8)\n\n    # Compute maximal feasible radii using an LP-like fixpoint:\n    radii = compute_max_radii(centers)  # improved routine below\n    sum_radii = float(np.sum(radii))\n\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers, tol=1e-9, max_iter=2000):\n    \"\"\"\n    Compute maximal radii satisfying:\n      r_i >= 0, r_i <= dist_to_border_i, and r_i + r_j <= dist(i,j)\n    This uses an iterative tightening: start with border-limited radii and\n    repeatedly enforce pairwise constraints by decreasing any radius that\n    violates a constraint (setting it to the tightest allowable).\n    \"\"\"\n    n = centers.shape[0]\n    # distance to borders\n    d_border = np.minimum.reduce([centers[:, 0], centers[:, 1], 1.0 - centers[:, 0], 1.0 - centers[:, 1]])\n    radii = d_border.copy()\n\n    # pairwise distances\n    diffs = centers[:, None, :] - centers[None, :, :]\n    dists = np.sqrt(np.sum(diffs * diffs, axis=2))\n\n    # iterative tightening: for each circle i, r_i <= min(border_i, min_j max(0, d_ij - r_j))\n    # This only decreases radii and converges to a feasible point. It tends to give larger\n    # total sum than naive proportional downscaling because it preserves smaller radii.\n    it = 0\n    while it < max_iter:\n        it += 1\n        prev = radii.copy()\n        for i in range(n):\n            # compute tightest allowed from pairwise constraints given current r_j\n            # skip j==i\n            # allow small negative numerical tolerance but clamp to >=0\n            limits = d_border[i]\n            # compute d_ij - r_j for all j != i\n            vals = dists[i] - radii\n            # ignore self\n            vals[i] = np.inf\n            # allowable r_i must be <= each val (and >=0)\n            min_val = np.min(vals)\n            if min_val < limits:\n                limits = min_val\n            # clamp\n            if limits < 0.0:\n                limits = 0.0\n            # only decrease (monotone)\n            if limits < radii[i] - tol:\n                radii[i] = limits\n        # convergence check\n        if np.max(prev - radii) < 1e-12:\n            break\n\n    # Final safety: enforce all constraints (clamp small residual violations)\n    radii = np.maximum(radii, 0.0)\n    for i in range(n):\n        radii[i] = min(radii[i], d_border[i])\n    for i in range(n):\n        for j in range(i + 1, n):\n            if radii[i] + radii[j] > dists[i, j]:\n                # reduce the larger one to make equality (better for sum than scaling both)\n                if radii[i] >= radii[j]:\n                    radii[i] = max(0.0, dists[i, j] - radii[j])\n                else:\n                    radii[j] = max(0.0, dists[i, j] - radii[i])\n\n    return radii\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor='k')\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n\n    # Uncomment to visualize:\n    # visualize(centers, radii)",
      "code_diff": "Full rewrite",
      "parent_metrics": {
        "validity": 1.0,
        "sum_radii": 0.9597642169962064,
        "target_ratio": 0.36423689449571406,
        "combined_score": 0.36423689449571406
      },
      "child_metrics": {
        "validity": 1.0,
        "sum_radii": 0.33593814268628586,
        "target_ratio": 0.12749075623767966,
        "combined_score": 0.12749075623767966
      }
    },
    "performance_regression:target_ratio decreased by n.n% (n.n \u2192 n.n)": {
      "pattern_type": "performance_regression",
      "description": "target_ratio decreased by 65.0% (0.364 \u2192 0.127)",
      "count": 3,
      "first_seen": 7,
      "last_seen": 20,
      "example_error": null,
      "parent_code": "# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square\n    that attempts to maximize the sum of their radii.\n\n    Returns:\n        Tuple of (centers, radii, sum_of_radii)\n        centers: np.array of shape (26, 2) with (x, y) coordinates\n        radii: np.array of shape (26) with radius of each circle\n        sum_of_radii: Sum of all radii\n    \"\"\"\n    # Initialize arrays for 26 circles\n    n = 26\n    centers = np.zeros((n, 2))\n\n    # Place circles in a structured pattern\n    # This is a simple pattern - evolution will improve this\n\n    # First, place a large circle in the center\n    centers[0] = [0.5, 0.5]\n\n    # Place 8 circles around it in a ring\n    for i in range(8):\n        angle = 2 * np.pi * i / 8\n        centers[i + 1] = [0.5 + 0.3 * np.cos(angle), 0.5 + 0.3 * np.sin(angle)]\n\n    # Place 16 more circles in an outer ring\n    for i in range(16):\n        angle = 2 * np.pi * i / 16\n        centers[i + 9] = [0.5 + 0.7 * np.cos(angle), 0.5 + 0.7 * np.sin(angle)]\n\n    # Additional positioning adjustment to make sure all circles\n    # are inside the square and don't overlap\n    # Clip to ensure everything is inside the unit square\n    centers = np.clip(centers, 0.01, 0.99)\n\n    # Compute maximum valid radii for this configuration\n    radii = compute_max_radii(centers)\n\n    # Calculate the sum of radii\n    sum_radii = np.sum(radii)\n\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers):\n    \"\"\"\n    Compute the maximum possible radii for each circle position\n    such that they don't overlap and stay within the unit square.\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n\n    Returns:\n        np.array of shape (n) with radius of each circle\n    \"\"\"\n    n = centers.shape[0]\n    radii = np.ones(n)\n\n    # First, limit by distance to square borders\n    for i in range(n):\n        x, y = centers[i]\n        # Distance to borders\n        radii[i] = min(x, y, 1 - x, 1 - y)\n\n    # Then, limit by distance to other circles\n    # Each pair of circles with centers at distance d can have\n    # sum of radii at most d to avoid overlap\n    for i in range(n):\n        for j in range(i + 1, n):\n            dist = np.sqrt(np.sum((centers[i] - centers[j]) ** 2))\n\n            # If current radii would cause overlap\n            if radii[i] + radii[j] > dist:\n                # Scale both radii proportionally\n                scale = dist / (radii[i] + radii[j])\n                radii[i] *= scale\n                radii[j] *= scale\n\n    return radii\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.5)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\")\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii}\")\n    # AlphaEvolve improved this to 2.635\n\n    # Uncomment to visualize:\n    visualize(centers, radii)\n",
      "child_code": "# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (improved constructor)\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square\n    and compute maximal feasible radii (LP-like iterative tightening).\n    Returns: centers (26,2), radii (26,), sum_of_radii\n    \"\"\"\n    n = 26\n\n    # Hex-like layered layout but with row counts chosen to fit 26 circles:\n    rows = [5, 5, 6, 5, 5]  # sums to 26\n    max_cols = max(rows)\n\n    # Choose a spacing that fills the square reasonably densely without overlap\n    s = 0.192  # horizontal spacing (tuned)\n    sy = s * np.sqrt(3) / 2.0  # vertical spacing for hex/triangular packing\n\n    total_rows = len(rows)\n    y0 = (1.0 - (total_rows - 1) * sy) / 2.0  # center rows vertically\n\n    centers = np.zeros((n, 2))\n    idx = 0\n    for r_idx, count in enumerate(rows):\n        y = y0 + r_idx * sy\n        # center the row horizontally\n        x_start = (1.0 - (count - 1) * s) / 2.0\n        for k in range(count):\n            x = x_start + k * s\n            centers[idx] = [x, y]\n            idx += 1\n\n    # Ensure centers remain inside square numerically\n    centers = np.clip(centers, 1e-8, 1.0 - 1e-8)\n\n    # Compute maximal feasible radii using an LP-like fixpoint:\n    radii = compute_max_radii(centers)  # improved routine below\n    sum_radii = float(np.sum(radii))\n\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers, tol=1e-9, max_iter=2000):\n    \"\"\"\n    Compute maximal radii satisfying:\n      r_i >= 0, r_i <= dist_to_border_i, and r_i + r_j <= dist(i,j)\n    This uses an iterative tightening: start with border-limited radii and\n    repeatedly enforce pairwise constraints by decreasing any radius that\n    violates a constraint (setting it to the tightest allowable).\n    \"\"\"\n    n = centers.shape[0]\n    # distance to borders\n    d_border = np.minimum.reduce([centers[:, 0], centers[:, 1], 1.0 - centers[:, 0], 1.0 - centers[:, 1]])\n    radii = d_border.copy()\n\n    # pairwise distances\n    diffs = centers[:, None, :] - centers[None, :, :]\n    dists = np.sqrt(np.sum(diffs * diffs, axis=2))\n\n    # iterative tightening: for each circle i, r_i <= min(border_i, min_j max(0, d_ij - r_j))\n    # This only decreases radii and converges to a feasible point. It tends to give larger\n    # total sum than naive proportional downscaling because it preserves smaller radii.\n    it = 0\n    while it < max_iter:\n        it += 1\n        prev = radii.copy()\n        for i in range(n):\n            # compute tightest allowed from pairwise constraints given current r_j\n            # skip j==i\n            # allow small negative numerical tolerance but clamp to >=0\n            limits = d_border[i]\n            # compute d_ij - r_j for all j != i\n            vals = dists[i] - radii\n            # ignore self\n            vals[i] = np.inf\n            # allowable r_i must be <= each val (and >=0)\n            min_val = np.min(vals)\n            if min_val < limits:\n                limits = min_val\n            # clamp\n            if limits < 0.0:\n                limits = 0.0\n            # only decrease (monotone)\n            if limits < radii[i] - tol:\n                radii[i] = limits\n        # convergence check\n        if np.max(prev - radii) < 1e-12:\n            break\n\n    # Final safety: enforce all constraints (clamp small residual violations)\n    radii = np.maximum(radii, 0.0)\n    for i in range(n):\n        radii[i] = min(radii[i], d_border[i])\n    for i in range(n):\n        for j in range(i + 1, n):\n            if radii[i] + radii[j] > dists[i, j]:\n                # reduce the larger one to make equality (better for sum than scaling both)\n                if radii[i] >= radii[j]:\n                    radii[i] = max(0.0, dists[i, j] - radii[j])\n                else:\n                    radii[j] = max(0.0, dists[i, j] - radii[i])\n\n    return radii\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor='k')\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n\n    # Uncomment to visualize:\n    # visualize(centers, radii)",
      "code_diff": "Full rewrite",
      "parent_metrics": {
        "validity": 1.0,
        "sum_radii": 0.9597642169962064,
        "target_ratio": 0.36423689449571406,
        "combined_score": 0.36423689449571406
      },
      "child_metrics": {
        "validity": 1.0,
        "sum_radii": 0.33593814268628586,
        "target_ratio": 0.12749075623767966,
        "combined_score": 0.12749075623767966
      }
    },
    "performance_regression:combined_score decreased by n.n% (n.n \u2192 n.n)": {
      "pattern_type": "performance_regression",
      "description": "combined_score decreased by 65.0% (0.364 \u2192 0.127)",
      "count": 3,
      "first_seen": 7,
      "last_seen": 20,
      "example_error": null,
      "parent_code": "# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square\n    that attempts to maximize the sum of their radii.\n\n    Returns:\n        Tuple of (centers, radii, sum_of_radii)\n        centers: np.array of shape (26, 2) with (x, y) coordinates\n        radii: np.array of shape (26) with radius of each circle\n        sum_of_radii: Sum of all radii\n    \"\"\"\n    # Initialize arrays for 26 circles\n    n = 26\n    centers = np.zeros((n, 2))\n\n    # Place circles in a structured pattern\n    # This is a simple pattern - evolution will improve this\n\n    # First, place a large circle in the center\n    centers[0] = [0.5, 0.5]\n\n    # Place 8 circles around it in a ring\n    for i in range(8):\n        angle = 2 * np.pi * i / 8\n        centers[i + 1] = [0.5 + 0.3 * np.cos(angle), 0.5 + 0.3 * np.sin(angle)]\n\n    # Place 16 more circles in an outer ring\n    for i in range(16):\n        angle = 2 * np.pi * i / 16\n        centers[i + 9] = [0.5 + 0.7 * np.cos(angle), 0.5 + 0.7 * np.sin(angle)]\n\n    # Additional positioning adjustment to make sure all circles\n    # are inside the square and don't overlap\n    # Clip to ensure everything is inside the unit square\n    centers = np.clip(centers, 0.01, 0.99)\n\n    # Compute maximum valid radii for this configuration\n    radii = compute_max_radii(centers)\n\n    # Calculate the sum of radii\n    sum_radii = np.sum(radii)\n\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers):\n    \"\"\"\n    Compute the maximum possible radii for each circle position\n    such that they don't overlap and stay within the unit square.\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n\n    Returns:\n        np.array of shape (n) with radius of each circle\n    \"\"\"\n    n = centers.shape[0]\n    radii = np.ones(n)\n\n    # First, limit by distance to square borders\n    for i in range(n):\n        x, y = centers[i]\n        # Distance to borders\n        radii[i] = min(x, y, 1 - x, 1 - y)\n\n    # Then, limit by distance to other circles\n    # Each pair of circles with centers at distance d can have\n    # sum of radii at most d to avoid overlap\n    for i in range(n):\n        for j in range(i + 1, n):\n            dist = np.sqrt(np.sum((centers[i] - centers[j]) ** 2))\n\n            # If current radii would cause overlap\n            if radii[i] + radii[j] > dist:\n                # Scale both radii proportionally\n                scale = dist / (radii[i] + radii[j])\n                radii[i] *= scale\n                radii[j] *= scale\n\n    return radii\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.5)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\")\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii}\")\n    # AlphaEvolve improved this to 2.635\n\n    # Uncomment to visualize:\n    visualize(centers, radii)\n",
      "child_code": "# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (improved constructor)\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square\n    and compute maximal feasible radii (LP-like iterative tightening).\n    Returns: centers (26,2), radii (26,), sum_of_radii\n    \"\"\"\n    n = 26\n\n    # Hex-like layered layout but with row counts chosen to fit 26 circles:\n    rows = [5, 5, 6, 5, 5]  # sums to 26\n    max_cols = max(rows)\n\n    # Choose a spacing that fills the square reasonably densely without overlap\n    s = 0.192  # horizontal spacing (tuned)\n    sy = s * np.sqrt(3) / 2.0  # vertical spacing for hex/triangular packing\n\n    total_rows = len(rows)\n    y0 = (1.0 - (total_rows - 1) * sy) / 2.0  # center rows vertically\n\n    centers = np.zeros((n, 2))\n    idx = 0\n    for r_idx, count in enumerate(rows):\n        y = y0 + r_idx * sy\n        # center the row horizontally\n        x_start = (1.0 - (count - 1) * s) / 2.0\n        for k in range(count):\n            x = x_start + k * s\n            centers[idx] = [x, y]\n            idx += 1\n\n    # Ensure centers remain inside square numerically\n    centers = np.clip(centers, 1e-8, 1.0 - 1e-8)\n\n    # Compute maximal feasible radii using an LP-like fixpoint:\n    radii = compute_max_radii(centers)  # improved routine below\n    sum_radii = float(np.sum(radii))\n\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers, tol=1e-9, max_iter=2000):\n    \"\"\"\n    Compute maximal radii satisfying:\n      r_i >= 0, r_i <= dist_to_border_i, and r_i + r_j <= dist(i,j)\n    This uses an iterative tightening: start with border-limited radii and\n    repeatedly enforce pairwise constraints by decreasing any radius that\n    violates a constraint (setting it to the tightest allowable).\n    \"\"\"\n    n = centers.shape[0]\n    # distance to borders\n    d_border = np.minimum.reduce([centers[:, 0], centers[:, 1], 1.0 - centers[:, 0], 1.0 - centers[:, 1]])\n    radii = d_border.copy()\n\n    # pairwise distances\n    diffs = centers[:, None, :] - centers[None, :, :]\n    dists = np.sqrt(np.sum(diffs * diffs, axis=2))\n\n    # iterative tightening: for each circle i, r_i <= min(border_i, min_j max(0, d_ij - r_j))\n    # This only decreases radii and converges to a feasible point. It tends to give larger\n    # total sum than naive proportional downscaling because it preserves smaller radii.\n    it = 0\n    while it < max_iter:\n        it += 1\n        prev = radii.copy()\n        for i in range(n):\n            # compute tightest allowed from pairwise constraints given current r_j\n            # skip j==i\n            # allow small negative numerical tolerance but clamp to >=0\n            limits = d_border[i]\n            # compute d_ij - r_j for all j != i\n            vals = dists[i] - radii\n            # ignore self\n            vals[i] = np.inf\n            # allowable r_i must be <= each val (and >=0)\n            min_val = np.min(vals)\n            if min_val < limits:\n                limits = min_val\n            # clamp\n            if limits < 0.0:\n                limits = 0.0\n            # only decrease (monotone)\n            if limits < radii[i] - tol:\n                radii[i] = limits\n        # convergence check\n        if np.max(prev - radii) < 1e-12:\n            break\n\n    # Final safety: enforce all constraints (clamp small residual violations)\n    radii = np.maximum(radii, 0.0)\n    for i in range(n):\n        radii[i] = min(radii[i], d_border[i])\n    for i in range(n):\n        for j in range(i + 1, n):\n            if radii[i] + radii[j] > dists[i, j]:\n                # reduce the larger one to make equality (better for sum than scaling both)\n                if radii[i] >= radii[j]:\n                    radii[i] = max(0.0, dists[i, j] - radii[j])\n                else:\n                    radii[j] = max(0.0, dists[i, j] - radii[i])\n\n    return radii\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor='k')\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n\n    # Uncomment to visualize:\n    # visualize(centers, radii)",
      "code_diff": "Full rewrite",
      "parent_metrics": {
        "validity": 1.0,
        "sum_radii": 0.9597642169962064,
        "target_ratio": 0.36423689449571406,
        "combined_score": 0.36423689449571406
      },
      "child_metrics": {
        "validity": 1.0,
        "sum_radii": 0.33593814268628586,
        "target_ratio": 0.12749075623767966,
        "combined_score": 0.12749075623767966
      }
    },
    "performance_regression:eval_time decreased by n.n% (n.n \u2192 n.n)": {
      "pattern_type": "performance_regression",
      "description": "eval_time decreased by 55.0% (0.142 \u2192 0.064)",
      "count": 5,
      "first_seen": 10,
      "last_seen": 30,
      "example_error": null,
      "parent_code": "# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (improved)\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a deterministic arrangement of 26 circle centers arranged\n    in a compact hexagonal-like layout inside the unit square. Then\n    compute near-optimal radii by enforcing non-overlap and boundary\n    constraints via an iterative projection procedure. A small grid\n    search over horizontal spacing is used to pick the best layout.\n    Returns:\n        centers: (26,2) array\n        radii:   (26,) array\n        sum_radii: float\n    \"\"\"\n    n = 26\n    # Use a 5-row hex-like pattern with counts summing to 26\n    rows = [5, 6, 5, 6, 4]\n\n    def make_centers(s):\n        # horizontal spacing s; vertical spacing for hex packing\n        h = s * np.sqrt(3) / 2.0\n        total_rows = len(rows)\n        total_height = (total_rows - 1) * h\n        y0 = 0.5 - total_height / 2.0\n        centers = []\n        for r_i, m in enumerate(rows):\n            y = y0 + r_i * h\n            # offset every other row by s/2 (hex pattern)\n            offset = (s / 2.0) if (r_i % 2 == 1) else 0.0\n            span = (m - 1) * s\n            x0 = 0.5 - span / 2.0 + offset\n            for k in range(m):\n                x = x0 + k * s\n                # clip a hair inside to avoid exact-touch-to-border floating issues\n                centers.append([np.clip(x, 1e-6, 1 - 1e-6), np.clip(y, 1e-6, 1 - 1e-6)])\n        return np.array(centers)\n\n    def compute_max_radii_iter(centers, max_iters=2000, tol=1e-6):\n        n = centers.shape[0]\n        # initial radii limited by borders\n        radii = np.minimum.reduce([centers[:, 0], centers[:, 1], 1 - centers[:, 0], 1 - centers[:, 1]])\n        radii = np.maximum(radii, 0.0)\n        # iterative pairwise constraint enforcement:\n        for it in range(max_iters):\n            changed = False\n            # enforce pair constraints; reduce the larger circle of any violating pair\n            for i in range(n):\n                for j in range(i + 1, n):\n                    dx = centers[i, 0] - centers[j, 0]\n                    dy = centers[i, 1] - centers[j, 1]\n                    d = np.hypot(dx, dy)\n                    if d <= 0:\n                        # coincident centers shouldn't happen but protect\n                        if radii[j] > 0:\n                            radii[j] = 0.0\n                            changed = True\n                        continue\n                    if radii[i] + radii[j] > d:\n                        # Prefer reducing the larger radius (keeps sum larger than naive equal scaling)\n                        if radii[i] > radii[j]:\n                            new_ri = max(0.0, d - radii[j])\n                            if radii[i] - new_ri > tol:\n                                radii[i] = new_ri\n                                changed = True\n                        else:\n                            new_rj = max(0.0, d - radii[i])\n                            if radii[j] - new_rj > tol:\n                                radii[j] = new_rj\n                                changed = True\n            # enforce border constraints strictly\n            border_limits = np.minimum.reduce([centers[:, 0], centers[:, 1], 1 - centers[:, 0], 1 - centers[:, 1]])\n            new_radii = np.minimum(radii, border_limits)\n            if np.any(radii - new_radii > tol):\n                radii = new_radii\n                changed = True\n            if not changed:\n                break\n        return radii\n\n    # Grid-search the horizontal spacing 's' to maximize sum of radii.\n    # Reasonable search range for s given 5..6 points per row: try from 0.08..0.18\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n    s_values = np.linspace(0.09, 0.18, 46)\n    for s in s_values:\n        centers = make_centers(s)\n        if centers.shape[0] != n:\n            continue\n        radii = compute_max_radii_iter(centers)\n        srad = radii.sum()\n        if srad > best_sum:\n            best_sum = srad\n            best_centers = centers.copy()\n            best_radii = radii.copy()\n\n    # If search failed for some reason, fallback to a default spacing\n    if best_centers is None:\n        best_centers = make_centers(0.12)\n        best_radii = compute_max_radii_iter(best_centers)\n\n    return best_centers, best_radii, float(best_radii.sum())\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\", linewidth=0.5)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)",
      "child_code": "# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (hex-like lattice, spacing search)\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    # row pattern summing to 26\n    row_counts = [6, 5, 5, 5, 5]\n\n    def make_centers(s):\n        vy = np.sqrt(3) / 2 * s\n        rows = len(row_counts)\n        center_row = (rows - 1) / 2.0\n        centers = []\n        for r_idx, m in enumerate(row_counts):\n            y = 0.5 + (r_idx - center_row) * vy\n            width = (m - 1) * s\n            x0 = (1.0 - width) / 2.0\n            offset = 0.5 * s if (r_idx % 2 == 1) else 0.0\n            for k in range(m):\n                x = x0 + offset + k * s\n                centers.append((np.clip(x, 1e-6, 1-1e-6), np.clip(y, 1e-6, 1-1e-6)))\n        return np.array(centers)\n\n    def max_radii(centers):\n        n = centers.shape[0]\n        # distance to border\n        db = np.minimum.reduce([centers[:,0], centers[:,1], 1-centers[:,0], 1-centers[:,1]])\n        diffs = centers[:,None,:] - centers[None,:,:]\n        dists = np.sqrt(np.sum(diffs**2, axis=2)) + np.eye(n)*1e9\n        nearest = np.min(dists, axis=1)\n        r = np.minimum(db, nearest/2.0)\n        return np.clip(r, 0.0, 0.5)\n\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n    # search spacing range that balances neighbor distance and border clearance\n    for s in np.linspace(0.12, 0.18, 61):\n        C = make_centers(s)\n        if C.shape[0] != n:\n            continue\n        R = max_radii(C)\n        ssum = float(R.sum())\n        if ssum > best_sum:\n            best_sum = ssum\n            best_centers = C.copy()\n            best_radii = R.copy()\n\n    # fallback\n    if best_centers is None:\n        best_centers = make_centers(0.15)\n        best_radii = max_radii(best_centers)\n    return best_centers, best_radii, float(best_radii.sum())\n# EVOLVE-BLOCK-END\n\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(6,6))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect('equal')\n    for i,(c,r) in enumerate(zip(centers,radii)):\n        ax.add_patch(Circle(c,r,alpha=0.6,edgecolor='k'))\n        ax.text(c[0],c[1],str(i),ha='center',va='center',fontsize=8)\n    plt.title(f\"n={len(centers)}, sum={sum(radii):.6f}\"); plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, s = run_packing()\n    print(f\"Sum of radii: {s:.6f}\")\n    # visualize(centers, radii)",
      "code_diff": "Full rewrite",
      "parent_metrics": {
        "validity": 1.0,
        "sum_radii": 1.8508433454579651,
        "target_ratio": 0.7024073417297781,
        "combined_score": 0.7024073417297781,
        "eval_time": 0.14167499542236328
      },
      "child_metrics": {
        "validity": 1.0,
        "sum_radii": 2.0835382907247952,
        "target_ratio": 0.7907166188708901,
        "combined_score": 0.7907166188708901,
        "eval_time": 0.06378698348999023
      }
    }
  },
  "success_patterns": {},
  "iteration_history": [
    4,
    3,
    1,
    2,
    6,
    5,
    7,
    8,
    10,
    12,
    9,
    13,
    11,
    14,
    16,
    18,
    17,
    15,
    19,
    20,
    21,
    22,
    23,
    24,
    25,
    26,
    27,
    30
  ],
  "last_update_iteration": 30
}