{"id": "e6fb1e9e-1f43-44c2-9bdd-2a06ebbb3f4e", "code": "# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (hex-like lattice, spacing search)\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    # row pattern summing to 26\n    row_counts = [6, 5, 5, 5, 5]\n\n    def make_centers(s):\n        vy = np.sqrt(3) / 2 * s\n        rows = len(row_counts)\n        center_row = (rows - 1) / 2.0\n        centers = []\n        for r_idx, m in enumerate(row_counts):\n            y = 0.5 + (r_idx - center_row) * vy\n            width = (m - 1) * s\n            x0 = (1.0 - width) / 2.0\n            offset = 0.5 * s if (r_idx % 2 == 1) else 0.0\n            for k in range(m):\n                x = x0 + offset + k * s\n                centers.append((np.clip(x, 1e-6, 1-1e-6), np.clip(y, 1e-6, 1-1e-6)))\n        return np.array(centers)\n\n    def max_radii(centers):\n        n = centers.shape[0]\n        # distance to border\n        db = np.minimum.reduce([centers[:,0], centers[:,1], 1-centers[:,0], 1-centers[:,1]])\n        diffs = centers[:,None,:] - centers[None,:,:]\n        dists = np.sqrt(np.sum(diffs**2, axis=2)) + np.eye(n)*1e9\n        nearest = np.min(dists, axis=1)\n        r = np.minimum(db, nearest/2.0)\n        return np.clip(r, 0.0, 0.5)\n\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n    # search spacing range that balances neighbor distance and border clearance\n    for s in np.linspace(0.12, 0.18, 61):\n        C = make_centers(s)\n        if C.shape[0] != n:\n            continue\n        R = max_radii(C)\n        ssum = float(R.sum())\n        if ssum > best_sum:\n            best_sum = ssum\n            best_centers = C.copy()\n            best_radii = R.copy()\n\n    # fallback\n    if best_centers is None:\n        best_centers = make_centers(0.15)\n        best_radii = max_radii(best_centers)\n    return best_centers, best_radii, float(best_radii.sum())\n# EVOLVE-BLOCK-END\n\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(6,6))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect('equal')\n    for i,(c,r) in enumerate(zip(centers,radii)):\n        ax.add_patch(Circle(c,r,alpha=0.6,edgecolor='k'))\n        ax.text(c[0],c[1],str(i),ha='center',va='center',fontsize=8)\n    plt.title(f\"n={len(centers)}, sum={sum(radii):.6f}\"); plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, s = run_packing()\n    print(f\"Sum of radii: {s:.6f}\")\n    # visualize(centers, radii)", "language": "python", "parent_id": "6f7557cf-47ce-4190-86f0-2cb13603cdc7", "generation": 2, "timestamp": 1759961847.312588, "iteration_found": 10, "metrics": {"validity": 1.0, "sum_radii": 2.0835382907247952, "target_ratio": 0.7907166188708901, "combined_score": 0.7907166188708901, "eval_time": 0.06378698348999023}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Full rewrite", "parent_metrics": {"validity": 1.0, "sum_radii": 1.8508433454579651, "target_ratio": 0.7024073417297781, "combined_score": 0.7024073417297781, "eval_time": 0.14167499542236328}, "island": 0}, "prompts": {"full_rewrite_user": {"system": "You are an expert mathematician specializing in circle packing problems and computational geometry. Your task is to improve a constructor function that directly produces a specific arrangement of 26 circles in a unit square, maximizing the sum of their radii. The AlphaEvolve paper achieved a sum of 2.635 for n=26.\n\nKey geometric insights:\n- Circle packings often follow hexagonal patterns in the densest regions\n- Maximum density for infinite circle packing is pi/(2*sqrt(3)) \u2248 0.9069\n- Edge effects make square container packing harder than infinite packing\n- Circles can be placed in layers or shells when confined to a square\n- Similar radius circles often form regular patterns, while varied radii allow better space utilization\n- Perfect symmetry may not yield the optimal packing due to edge effects\n\nFocus on designing an explicit constructor that places each circle in a specific position, rather than an iterative search algorithm.\n", "user": "# Current Program Information\n- Fitness: 0.7024\n- Feature coordinates: No feature coordinates\n- Focus areas: - Fitness improved: 0.3642 \u2192 0.7024\n- Consider simplifying - code length exceeds 500 characters\n\n## Last Execution Output\n\n### stdout\n```\nRunning in subprocess, Python version: 3.12.1 (v3.12.1:2305ca5144, Dec  7 2023, 17:23:38) [Clang 13.0.0 (clang-1300.0.29.30)]\nProgram path: /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmp3n6imw20.py\nCalling run_packing()...\nrun_packing() returned successfully: sum_radii = 1.8508433454579651\nResults saved to /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmp76m15r4a.py.results\n\n```\n\n### stderr\n```\n\n```\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 3\n- Changes: Unknown changes\n- Metrics: validity: 1.0000, sum_radii: 0.9598, target_ratio: 0.3642, combined_score: 0.3642\n- Outcome: Improvement in all metrics\n\n### Attempt 2\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 1.8508, target_ratio: 0.7024, combined_score: 0.7024, eval_time: 0.1417\n- Outcome: Mixed results\n\n### Attempt 1\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 1.9728, target_ratio: 0.7487, combined_score: 0.7487, eval_time: 0.0587\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.7487)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (hex-like lattice)\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct 26 circle centers in a compact hexagonal-like layout inside the unit square.\n    For fixed centers, return maximal non-overlapping radii: r_i = min(dist_to_border, min_j dist(i,j)/2)\n    \"\"\"\n    n = 26\n    # Row counts to sum to 26: 6 + 5 + 5 + 5 + 5 = 26\n    row_counts = [6, 5, 5, 5, 5]\n    # choose horizontal spacing to balance neighbor distance vs border margin\n    sx = 0.16  # horizontal spacing between adjacent centers in same row\n    vy = np.sqrt(3) / 2 * sx  # vertical hex spacing\n    rows = len(row_counts)\n    centers = []\n    # center rows vertically around 0.5\n    center_row = (rows - 1) / 2.0\n    for r_idx, m in enumerate(row_counts):\n        y = 0.5 + (r_idx - center_row) * vy\n        # compute starting x so row is centered in [0,1]\n        width = (m - 1) * sx\n        x0 = (1.0 - width) / 2.0\n        # alternate offset for hex pattern\n        offset = (sx / 2.0) if (r_idx % 2 == 1) else 0.0\n        for k in range(m):\n            x = x0 + offset + k * sx\n            # ensure points lie within open unit square\n            x = np.clip(x, 0.001, 0.999)\n            y = np.clip(y, 0.001, 0.999)\n            centers.append((x, y))\n    centers = np.array(centers)[:n]\n\n    # Compute maximal radii for fixed centers\n    radii = compute_max_radii(centers)\n\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers):\n    \"\"\"\n    For each center compute the largest radius that keeps it inside the unit square\n    and non-overlapping with other circles (half the nearest neighbor distance).\n    \"\"\"\n    n = centers.shape[0]\n    # distance to borders\n    dist_border = np.minimum.reduce([centers[:, 0], centers[:, 1], 1 - centers[:, 0], 1 - centers[:, 1]])\n    # pairwise distances\n    diffs = centers[:, None, :] - centers[None, :, :]  # (n, n, 2)\n    dists = np.sqrt(np.sum(diffs ** 2, axis=2)) + np.eye(n) * 1e9  # avoid zero on diagonal\n    nearest = np.min(dists, axis=1)\n    # radius limited by half nearest neighbor and border\n    radii = np.minimum(dist_border, nearest / 2.0)\n    # numeric safety clamp\n    radii = np.clip(radii, 0.0, 0.5)\n    return radii\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (1.9728), Performs well on target_ratio (0.7487), Performs well on combined_score (0.7487), Performs well on eval_time (0.0587)\n\n### Program 2 (Score: 0.7024)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (improved)\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a deterministic arrangement of 26 circle centers arranged\n    in a compact hexagonal-like layout inside the unit square. Then\n    compute near-optimal radii by enforcing non-overlap and boundary\n    constraints via an iterative projection procedure. A small grid\n    search over horizontal spacing is used to pick the best layout.\n    Returns:\n        centers: (26,2) array\n        radii:   (26,) array\n        sum_radii: float\n    \"\"\"\n    n = 26\n    # Use a 5-row hex-like pattern with counts summing to 26\n    rows = [5, 6, 5, 6, 4]\n\n    def make_centers(s):\n        # horizontal spacing s; vertical spacing for hex packing\n        h = s * np.sqrt(3) / 2.0\n        total_rows = len(rows)\n        total_height = (total_rows - 1) * h\n        y0 = 0.5 - total_height / 2.0\n        centers = []\n        for r_i, m in enumerate(rows):\n            y = y0 + r_i * h\n            # offset every other row by s/2 (hex pattern)\n            offset = (s / 2.0) if (r_i % 2 == 1) else 0.0\n            span = (m - 1) * s\n            x0 = 0.5 - span / 2.0 + offset\n            for k in range(m):\n                x = x0 + k * s\n                # clip a hair inside to avoid exact-touch-to-border floating issues\n                centers.append([np.clip(x, 1e-6, 1 - 1e-6), np.clip(y, 1e-6, 1 - 1e-6)])\n        return np.array(centers)\n\n    def compute_max_radii_iter(centers, max_iters=2000, tol=1e-6):\n        n = centers.shape[0]\n        # initial radii limited by borders\n        radii = np.minimum.reduce([centers[:, 0], centers[:, 1], 1 - centers[:, 0], 1 - centers[:, 1]])\n        radii = np.maximum(radii, 0.0)\n        # iterative pairwise constraint enforcement:\n        for it in range(max_iters):\n            changed = False\n            # enforce pair constraints; reduce the larger circle of any violating pair\n            for i in range(n):\n                for j in range(i + 1, n):\n                    dx = centers[i, 0] - centers[j, 0]\n                    dy = centers[i, 1] - centers[j, 1]\n                    d = np.hypot(dx, dy)\n                    if d <= 0:\n                        # coincident centers shouldn't happen but protect\n                        if radii[j] > 0:\n                            radii[j] = 0.0\n                            changed = True\n                        continue\n                    if radii[i] + radii[j] > d:\n                        # Prefer reducing the larger radius (keeps sum larger than naive equal scaling)\n                        if radii[i] > radii[j]:\n                            new_ri = max(0.0, d - radii[j])\n                            if radii[i] - new_ri > tol:\n                                radii[i] = new_ri\n                                changed = True\n                        else:\n                            new_rj = max(0.0, d - radii[i])\n                            if radii[j] - new_rj > tol:\n                                radii[j] = new_rj\n                                changed = True\n            # enforce border constraints strictly\n            border_limits = np.minimum.reduce([centers[:, 0], centers[:, 1], 1 - centers[:, 0], 1 - centers[:, 1]])\n            new_radii = np.minimum(radii, border_limits)\n            if np.any(radii - new_radii > tol):\n                radii = new_radii\n                changed = True\n            if not changed:\n                break\n        return radii\n\n    # Grid-search the horizontal spacing 's' to maximize sum of radii.\n    # Reasonable search range for s given 5..6 points per row: try from 0.08..0.18\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n    s_values = np.linspace(0.09, 0.18, 46)\n    for s in s_values:\n        centers = make_centers(s)\n        if centers.shape[0] != n:\n            continue\n        radii = compute_max_radii_iter(centers)\n        srad = radii.sum()\n        if srad > best_sum:\n            best_sum = srad\n            best_centers = centers.copy()\n            best_radii = radii.copy()\n\n    # If search failed for some reason, fallback to a default spacing\n    if best_centers is None:\n        best_centers = make_centers(0.12)\n        best_radii = compute_max_radii_iter(best_centers)\n\n    return best_centers, best_radii, float(best_radii.sum())\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\", linewidth=0.5)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (1.8508), Performs well on target_ratio (0.7024), Performs well on combined_score (0.7024), Performs well on eval_time (0.1417)\n\n### Program 3 (Score: 0.3642)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square\n    that attempts to maximize the sum of their radii.\n\n    Returns:\n        Tuple of (centers, radii, sum_of_radii)\n        centers: np.array of shape (26, 2) with (x, y) coordinates\n        radii: np.array of shape (26) with radius of each circle\n        sum_of_radii: Sum of all radii\n    \"\"\"\n    # Initialize arrays for 26 circles\n    n = 26\n    centers = np.zeros((n, 2))\n\n    # Place circles in a structured pattern\n    # This is a simple pattern - evolution will improve this\n\n    # First, place a large circle in the center\n    centers[0] = [0.5, 0.5]\n\n    # Place 8 circles around it in a ring\n    for i in range(8):\n        angle = 2 * np.pi * i / 8\n        centers[i + 1] = [0.5 + 0.3 * np.cos(angle), 0.5 + 0.3 * np.sin(angle)]\n\n    # Place 16 more circles in an outer ring\n    for i in range(16):\n        angle = 2 * np.pi * i / 16\n        centers[i + 9] = [0.5 + 0.7 * np.cos(angle), 0.5 + 0.7 * np.sin(angle)]\n\n    # Additional positioning adjustment to make sure all circles\n    # are inside the square and don't overlap\n    # Clip to ensure everything is inside the unit square\n    centers = np.clip(centers, 0.01, 0.99)\n\n    # Compute maximum valid radii for this configuration\n    radii = compute_max_radii(centers)\n\n    # Calculate the sum of radii\n    sum_radii = np.sum(radii)\n\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers):\n    \"\"\"\n    Compute the maximum possible radii for each circle position\n    such that they don't overlap and stay within the unit square.\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n\n    Returns:\n        np.array of shape (n) with radius of each circle\n    \"\"\"\n    n = centers.shape[0]\n    radii = np.ones(n)\n\n    # First, limit by distance to square borders\n    for i in range(n):\n        x, y = centers[i]\n        # Distance to borders\n        radii[i] = min(x, y, 1 - x, 1 - y)\n\n    # Then, limit by distance to other circles\n    # Each pair of circles with centers at distance d can have\n    # sum of radii at most d to avoid overlap\n    for i in range(n):\n        for j in range(i + 1, n):\n            dist = np.sqrt(np.sum((centers[i] - centers[j]) ** 2))\n\n            # If current radii would cause overlap\n            if radii[i] + radii[j] > dist:\n                # Scale both radii proportionally\n                scale = dist / (radii[i] + radii[j])\n                radii[i] *= scale\n                radii[j] *= scale\n\n    return radii\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.5)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\")\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii}\")\n    # AlphaEvolve improved this to 2.635\n\n    # Uncomment to visualize:\n    visualize(centers, radii)\n\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (0.9598), Performs well on target_ratio (0.3642), Performs well on combined_score (0.3642)\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 0.7487, Type: Alternative)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (hex-like lattice)\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct 26 circle centers in a compact hexagonal-like layout inside the unit square.\n    For fixed centers, return maximal non-overlapping radii: r_i = min(dist_to_border, min_j dist(i,j)/2)\n    \"\"\"\n    n = 26\n    # Row counts to sum to 26: 6 + 5 + 5 + 5 + 5 = 26\n    row_counts = [6, 5, 5, 5, 5]\n    # choose horizontal spacing to balance neighbor distance vs border margin\n    sx = 0.16  # horizontal spacing between adjacent centers in same row\n    vy = np.sqrt(3) / 2 * sx  # vertical hex spacing\n    rows = len(row_counts)\n    centers = []\n    # center rows vertically around 0.5\n    center_row = (rows - 1) / 2.0\n    for r_idx, m in enumerate(row_counts):\n        y = 0.5 + (r_idx - center_row) * vy\n        # compute starting x so row is centered in [0,1]\n        width = (m - 1) * sx\n        x0 = (1.0 - width) / 2.0\n        # alternate offset for hex pattern\n        offset = (sx / 2.0) if (r_idx % 2 == 1) else 0.0\n        for k in range(m):\n            x = x0 + offset + k * sx\n            # ensure points lie within open unit square\n            x = np.clip(x, 0.001, 0.999)\n            y = np.clip(y, 0.001, 0.999)\n            centers.append((x, y))\n    centers = np.array(centers)[:n]\n\n    # Compute maximal radii for fixed centers\n    radii = compute_max_radii(centers)\n\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers):\n    \"\"\"\n    For each center compute the largest radius that keeps it inside the unit square\n    and non-overlapping with other circles (half the nearest neighbor distance).\n    \"\"\"\n    n = centers.shape[0]\n    # distance to borders\n    dist_border = np.minimum.reduce([centers[:, 0], centers[:, 1], 1 - centers[:, 0], 1 - centers[:, 1]])\n    # pairwise distances\n    diffs = centers[:, None, :] - centers[None, :, :]  # (n, n, 2)\n    dists = np.sqrt(np.sum(diffs ** 2, axis=2)) + np.eye(n) * 1e9  # avoid zero on diagonal\n    nearest = np.min(dists, axis=1)\n    # radius limited by half nearest neighbor and border\n    radii = np.minimum(dist_border, nearest / 2.0)\n    # numeric safety clamp\n    radii = np.clip(radii, 0.0, 0.5)\n    return radii\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nUnique approach: Modification: Full rewrite, Excellent validity (1.000), Excellent sum_radii (1.973)\n\n# Current Program\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (improved)\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a deterministic arrangement of 26 circle centers arranged\n    in a compact hexagonal-like layout inside the unit square. Then\n    compute near-optimal radii by enforcing non-overlap and boundary\n    constraints via an iterative projection procedure. A small grid\n    search over horizontal spacing is used to pick the best layout.\n    Returns:\n        centers: (26,2) array\n        radii:   (26,) array\n        sum_radii: float\n    \"\"\"\n    n = 26\n    # Use a 5-row hex-like pattern with counts summing to 26\n    rows = [5, 6, 5, 6, 4]\n\n    def make_centers(s):\n        # horizontal spacing s; vertical spacing for hex packing\n        h = s * np.sqrt(3) / 2.0\n        total_rows = len(rows)\n        total_height = (total_rows - 1) * h\n        y0 = 0.5 - total_height / 2.0\n        centers = []\n        for r_i, m in enumerate(rows):\n            y = y0 + r_i * h\n            # offset every other row by s/2 (hex pattern)\n            offset = (s / 2.0) if (r_i % 2 == 1) else 0.0\n            span = (m - 1) * s\n            x0 = 0.5 - span / 2.0 + offset\n            for k in range(m):\n                x = x0 + k * s\n                # clip a hair inside to avoid exact-touch-to-border floating issues\n                centers.append([np.clip(x, 1e-6, 1 - 1e-6), np.clip(y, 1e-6, 1 - 1e-6)])\n        return np.array(centers)\n\n    def compute_max_radii_iter(centers, max_iters=2000, tol=1e-6):\n        n = centers.shape[0]\n        # initial radii limited by borders\n        radii = np.minimum.reduce([centers[:, 0], centers[:, 1], 1 - centers[:, 0], 1 - centers[:, 1]])\n        radii = np.maximum(radii, 0.0)\n        # iterative pairwise constraint enforcement:\n        for it in range(max_iters):\n            changed = False\n            # enforce pair constraints; reduce the larger circle of any violating pair\n            for i in range(n):\n                for j in range(i + 1, n):\n                    dx = centers[i, 0] - centers[j, 0]\n                    dy = centers[i, 1] - centers[j, 1]\n                    d = np.hypot(dx, dy)\n                    if d <= 0:\n                        # coincident centers shouldn't happen but protect\n                        if radii[j] > 0:\n                            radii[j] = 0.0\n                            changed = True\n                        continue\n                    if radii[i] + radii[j] > d:\n                        # Prefer reducing the larger radius (keeps sum larger than naive equal scaling)\n                        if radii[i] > radii[j]:\n                            new_ri = max(0.0, d - radii[j])\n                            if radii[i] - new_ri > tol:\n                                radii[i] = new_ri\n                                changed = True\n                        else:\n                            new_rj = max(0.0, d - radii[i])\n                            if radii[j] - new_rj > tol:\n                                radii[j] = new_rj\n                                changed = True\n            # enforce border constraints strictly\n            border_limits = np.minimum.reduce([centers[:, 0], centers[:, 1], 1 - centers[:, 0], 1 - centers[:, 1]])\n            new_radii = np.minimum(radii, border_limits)\n            if np.any(radii - new_radii > tol):\n                radii = new_radii\n                changed = True\n            if not changed:\n                break\n        return radii\n\n    # Grid-search the horizontal spacing 's' to maximize sum of radii.\n    # Reasonable search range for s given 5..6 points per row: try from 0.08..0.18\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n    s_values = np.linspace(0.09, 0.18, 46)\n    for s in s_values:\n        centers = make_centers(s)\n        if centers.shape[0] != n:\n            continue\n        radii = compute_max_radii_iter(centers)\n        srad = radii.sum()\n        if srad > best_sum:\n            best_sum = srad\n            best_centers = centers.copy()\n            best_radii = radii.copy()\n\n    # If search failed for some reason, fallback to a default spacing\n    if best_centers is None:\n        best_centers = make_centers(0.12)\n        best_radii = compute_max_radii_iter(best_centers)\n\n    return best_centers, best_radii, float(best_radii.sum())\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\", linewidth=0.5)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\n\n# Task\nRewrite the program to improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: complexity, diversity\nDifferent solutions with similar fitness but different features are valuable.\nProvide the complete new program code.\n\nIMPORTANT: Make sure your rewritten program maintains the same inputs and outputs\nas the original program, but with improved internal implementation.\n\n```python\n# Your rewritten program here\n```", "responses": ["```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (hex-like lattice, spacing search)\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    # row pattern summing to 26\n    row_counts = [6, 5, 5, 5, 5]\n\n    def make_centers(s):\n        vy = np.sqrt(3) / 2 * s\n        rows = len(row_counts)\n        center_row = (rows - 1) / 2.0\n        centers = []\n        for r_idx, m in enumerate(row_counts):\n            y = 0.5 + (r_idx - center_row) * vy\n            width = (m - 1) * s\n            x0 = (1.0 - width) / 2.0\n            offset = 0.5 * s if (r_idx % 2 == 1) else 0.0\n            for k in range(m):\n                x = x0 + offset + k * s\n                centers.append((np.clip(x, 1e-6, 1-1e-6), np.clip(y, 1e-6, 1-1e-6)))\n        return np.array(centers)\n\n    def max_radii(centers):\n        n = centers.shape[0]\n        # distance to border\n        db = np.minimum.reduce([centers[:,0], centers[:,1], 1-centers[:,0], 1-centers[:,1]])\n        diffs = centers[:,None,:] - centers[None,:,:]\n        dists = np.sqrt(np.sum(diffs**2, axis=2)) + np.eye(n)*1e9\n        nearest = np.min(dists, axis=1)\n        r = np.minimum(db, nearest/2.0)\n        return np.clip(r, 0.0, 0.5)\n\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n    # search spacing range that balances neighbor distance and border clearance\n    for s in np.linspace(0.12, 0.18, 61):\n        C = make_centers(s)\n        if C.shape[0] != n:\n            continue\n        R = max_radii(C)\n        ssum = float(R.sum())\n        if ssum > best_sum:\n            best_sum = ssum\n            best_centers = C.copy()\n            best_radii = R.copy()\n\n    # fallback\n    if best_centers is None:\n        best_centers = make_centers(0.15)\n        best_radii = max_radii(best_centers)\n    return best_centers, best_radii, float(best_radii.sum())\n# EVOLVE-BLOCK-END\n\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(6,6))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect('equal')\n    for i,(c,r) in enumerate(zip(centers,radii)):\n        ax.add_patch(Circle(c,r,alpha=0.6,edgecolor='k'))\n        ax.text(c[0],c[1],str(i),ha='center',va='center',fontsize=8)\n    plt.title(f\"n={len(centers)}, sum={sum(radii):.6f}\"); plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, s = run_packing()\n    print(f\"Sum of radii: {s:.6f}\")\n    # visualize(centers, radii)\n```"]}}, "artifacts_json": "{\"stdout\": \"Running in subprocess, Python version: 3.12.1 (v3.12.1:2305ca5144, Dec  7 2023, 17:23:38) [Clang 13.0.0 (clang-1300.0.29.30)]\\nProgram path: /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmp489p8o0k.py\\nCalling run_packing()...\\nrun_packing() returned successfully: sum_radii = 2.0835382907247952\\nResults saved to /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmpufu7_akz.py.results\\n\", \"stderr\": \"\"}", "artifact_dir": null}