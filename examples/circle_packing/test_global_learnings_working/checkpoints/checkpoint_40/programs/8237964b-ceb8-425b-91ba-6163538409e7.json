{"id": "8237964b-ceb8-425b-91ba-6163538409e7", "code": "# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for 26 circles in the unit square.\nBuilds a truncated hex lattice skeleton (rows sum to 26), scans spacings\nand a few translations, and for each candidate computes large feasible\nradii by a deterministic greedy relaxation (reduce larger circle when\na pair constraint ri+rj>d_ij is violated). Returns best configuration.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    rows = [4, 5, 6, 6, 5]  # sums to 26\n\n    # build relative hex-lattice (unit spacing)\n    rel = []\n    h_unit = np.sqrt(3.0) / 2.0\n    for r, cnt in enumerate(rows):\n        y = r * h_unit\n        offset = 0.5 if (r % 2 == 1) else 0.0\n        xs = np.arange(cnt) + offset\n        for x in xs:\n            rel.append((x, y))\n    rel = np.array(rel, dtype=float)\n    # normalize to start at (0,0)\n    rel -= rel.min(axis=0)\n    Xmax, Ymax = rel.max(axis=0)\n\n    # feasible spacing upper bound so the pattern can fit in [0,1]\n    s_max_possible = min(1.0 / max(1.0, Xmax), 1.0 / max(1.0, Ymax))\n    s_min = 0.06\n    s_max = max(s_min, s_max_possible * 0.98)\n    s_vals = np.linspace(s_min, s_max, 60)\n\n    def compute_max_radii_relax(C, tol=1e-9, max_iter=800):\n        m = C.shape[0]\n        border = np.minimum.reduce([C[:,0], C[:,1], 1.0 - C[:,0], 1.0 - C[:,1]])\n        r = border.copy()\n        # pairwise distances\n        diff = C[:, None, :] - C[None, :, :]\n        dmat = np.sqrt((diff**2).sum(axis=2))\n        np.fill_diagonal(dmat, np.inf)\n        # prepare sorted pairs by distance (tightest first)\n        ii, jj = np.triu_indices(m, k=1)\n        pairs = list(zip(ii.tolist(), jj.tolist(), dmat[ii, jj].tolist()))\n        pairs.sort(key=lambda t: t[2])\n        for it in range(max_iter):\n            changed = False\n            maxdelta = 0.0\n            for i, j, dij in pairs:\n                if not np.isfinite(dij) or dij <= 0.0:\n                    # degenerate: force small\n                    if r[i] + r[j] > 1e-12:\n                        if r[i] >= r[j]:\n                            newi = max(0.0, dij - r[j])\n                            delta = r[i] - newi\n                            if delta > tol:\n                                r[i] = newi; changed = True; maxdelta = max(maxdelta, delta)\n                        else:\n                            newj = max(0.0, dij - r[i])\n                            delta = r[j] - newj\n                            if delta > tol:\n                                r[j] = newj; changed = True; maxdelta = max(maxdelta, delta)\n                    continue\n                if r[i] + r[j] > dij + 1e-12:\n                    # reduce larger one\n                    if r[i] > r[j]:\n                        newi = max(0.0, dij - r[j])\n                        delta = r[i] - newi\n                        if delta > tol:\n                            r[i] = newi; changed = True; maxdelta = max(maxdelta, delta)\n                    else:\n                        newj = max(0.0, dij - r[i])\n                        delta = r[j] - newj\n                        if delta > tol:\n                            r[j] = newj; changed = True; maxdelta = max(maxdelta, delta)\n            # enforce border cap\n            over = r > border\n            if np.any(over):\n                r[over] = border[over]; changed = True\n            if not changed or maxdelta < tol:\n                break\n        # tiny final fix for any remaining overlaps\n        for i in range(m):\n            for j in range(i+1, m):\n                d = dmat[i,j]\n                if d < 1e-12:\n                    r[i] = r[j] = 0.0\n                elif r[i] + r[j] > d:\n                    s = d / (r[i] + r[j])\n                    r[i] *= s; r[j] *= s\n        r = np.clip(r, 0.0, border)\n        return r\n\n    best_sum = -1.0\n    best_C = None\n    best_r = None\n\n    # sample a few translations for each spacing (include centered placement)\n    for s in s_vals:\n        W, H = s * Xmax, s * Ymax\n        if W > 1.0 + 1e-12 or H > 1.0 + 1e-12:\n            continue\n        max_mx = 1.0 - W\n        max_my = 1.0 - H\n        # choose up to 5 translation samples in each direction (edges and center)\n        tx_vals = np.linspace(0.0, max_mx, min(5, 1 + int(round(max_mx / 0.02 + 1e-9))))\n        ty_vals = np.linspace(0.0, max_my, min(5, 1 + int(round(max_my / 0.02 + 1e-9))))\n\n        for mx in tx_vals:\n            for my in ty_vals:\n                C = rel * s + np.array([mx, my])\n                if C.min() < -1e-12 or C.max() > 1.0 + 1e-12:\n                    continue\n                r = compute_max_radii_relax(C)\n                ssum = float(r.sum())\n                if ssum > best_sum + 1e-12:\n                    best_sum = ssum\n                    best_C = C.copy()\n                    best_r = r.copy()\n\n    # fallback if nothing found (shouldn't happen)\n    if best_C is None:\n        s = 0.12\n        mx = 0.5 * (1.0 - s * Xmax)\n        my = 0.5 * (1.0 - s * Ymax)\n        best_C = rel * s + np.array([mx, my])\n        best_r = compute_max_radii_relax(best_C)\n        best_sum = float(best_r.sum())\n\n    return best_C, best_r, float(best_sum)\n# EVOLVE-BLOCK-END\n\n\n# fixed interface\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(7,7))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect(\"equal\")\n    for i,(c,r) in enumerate(zip(centers, radii)):\n        ax.add_patch(Circle(c, r, alpha=0.6, edgecolor='k'))\n        ax.text(c[0], c[1], str(i), ha='center', va='center', fontsize=7)\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, s = run_packing()\n    print(f\"Sum of radii: {s:.6f}\")\n    # visualize(centers, radii)", "language": "python", "parent_id": "ecd8f65c-55f5-48f2-87e1-10ccf622e826", "generation": 2, "timestamp": 1759957330.0914729, "iteration_found": 27, "metrics": {"validity": 1.0, "sum_radii": 1.9761016949152528, "target_ratio": 0.7499437172353901, "combined_score": 0.7499437172353901, "eval_time": 0.7880339622497559}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Full rewrite", "parent_metrics": {"validity": 0.0, "sum_radii": 0.0, "target_ratio": 0.0, "combined_score": 0.0}, "island": 0}, "prompts": {"full_rewrite_user": {"system": "You are an expert mathematician specializing in circle packing problems and computational geometry. Your task is to improve a constructor function that directly produces a specific arrangement of 26 circles in a unit square, maximizing the sum of their radii. The AlphaEvolve paper achieved a sum of 2.635 for n=26.\n\nKey geometric insights:\n- Circle packings often follow hexagonal patterns in the densest regions\n- Maximum density for infinite circle packing is pi/(2*sqrt(3)) \u2248 0.9069\n- Edge effects make square container packing harder than infinite packing\n- Circles can be placed in layers or shells when confined to a square\n- Similar radius circles often form regular patterns, while varied radii allow better space utilization\n- Perfect symmetry may not yield the optimal packing due to edge effects\n\nFocus on designing an explicit constructor that places each circle in a specific position, rather than an iterative search algorithm.\n", "user": "# Current Program Information\n- Fitness: 0.0000\n- Feature coordinates: No feature coordinates\n- Focus areas: - Fitness declined: 0.8286 \u2192 0.0000. Consider revising recent changes.\n- Consider simplifying - code length exceeds 500 characters\n\n## Last Execution Output\n\n### stdout\n```\nRunning in subprocess, Python version: 3.12.1 (v3.12.1:2305ca5144, Dec  7 2023, 17:23:38) [Clang 13.0.0 (clang-1300.0.29.30)]\nProgram path: /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmp4mgg8d8n.py\nCalling run_packing()...\nrun_packing() returned successfully: sum_radii = 2.107206859387559\nResults saved to /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmpjkci9558.py.results\n\n```\n\n### stderr\n```\n\n```\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 3\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.1833, target_ratio: 0.8286, combined_score: 0.8286, eval_time: 0.4969\n- Outcome: Mixed results\n\n### Attempt 2\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.1847, target_ratio: 0.8291, combined_score: 0.8291, eval_time: 1.2916\n- Outcome: Improvement in all metrics\n\n### Attempt 1\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.2549, target_ratio: 0.8558, combined_score: 0.8558, eval_time: 0.0646\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.8558)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (margin search + Gauss-Seidel radius maximizer)\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    \"\"\"\n    Build 26 circle centers in a staggered near-hex layout and maximize radii\n    for fixed centers using a Gauss-Seidel style solver that enforces:\n      r_i <= border_i,  r_i <= d_ij - r_j  for all j!=i\n    We sample several margins to mitigate edge effects and pick the best sum.\n    \"\"\"\n    n = 26\n    cols_per_row = [5, 6, 5, 5, 5]  # sums to 26\n    rows = len(cols_per_row)\n\n    def make_centers(margin):\n        available_w = 1.0 - 2.0 * margin\n        if available_w <= 0:\n            return None\n        centers = []\n        # compute horizontal steps per row, then choose vertical hex-like step\n        steps = []\n        for cnt in cols_per_row:\n            if cnt > 1:\n                steps.append(available_w / (cnt - 1))\n        if len(steps) == 0:\n            avg_step = 0.5 * available_w\n        else:\n            avg_step = float(np.mean(steps))\n        h = avg_step * np.sqrt(3.0) / 2.0 if avg_step > 0 else 0.0\n\n        # vertical placement centered\n        total_h = h * (rows - 1)\n        y0 = 0.5 - total_h / 2.0\n        for r, cnt in enumerate(cols_per_row):\n            if cnt == 1:\n                xs = np.array([0.5])\n            else:\n                xs = margin + np.linspace(0.0, available_w, cnt)\n            if r % 2 == 1 and cnt > 1:\n                step = xs[1] - xs[0]\n                xs = xs + 0.5 * step\n            xs = np.clip(xs, margin, 1.0 - margin)\n            y = y0 + r * h\n            for x in xs:\n                centers.append((x, y))\n        C = np.array(centers)\n        # if any center lies outside [0,1], reject this margin\n        if (C.min() < 0.0) or (C.max() > 1.0):\n            return None\n        return C\n\n    def maximize_radii(C, tol=1e-9, max_iter=500):\n        n = C.shape[0]\n        # border limits\n        border = np.minimum.reduce([C[:,0], C[:,1], 1.0 - C[:,0], 1.0 - C[:,1]]).astype(float)\n        # pairwise distances\n        diff = C[:, None, :] - C[None, :, :]\n        dists = np.sqrt((diff**2).sum(axis=2))\n        np.fill_diagonal(dists, np.inf)\n\n        # initial radii: border-limited\n        r = border.copy()\n        for it in range(max_iter):\n            prev = r.copy()\n            # Gauss-Seidel pass: set each r_i to its tightest feasible upper bound\n            for i in range(n):\n                ub = border[i]\n                # compute d_ij - r_j for all j != i\n                vals = dists[i] - r\n                vals[i] = np.inf\n                ub = min(ub, float(np.min(vals)))\n                r[i] = max(0.0, ub)\n            if np.max(np.abs(r - prev)) < tol:\n                break\n\n        # final safety: fix any slight overlaps by proportional scaling per violating pair\n        for i in range(n):\n            for j in range(i+1, n):\n                d = dists[i,j]\n                if d <= 1e-12:\n                    r[i] = r[j] = 0.0\n                elif r[i] + r[j] > d:\n                    s = d / (r[i] + r[j])\n                    r[i] *= s\n                    r[j] *= s\n        return r\n\n    best_sum = -1.0\n    best_C = None\n    best_r = None\n    # sample margins (denser near small margins)\n    for margin in np.linspace(0.02, 0.12, 21):\n        C = make_centers(margin)\n        if C is None or C.shape[0] != n:\n            continue\n        r = maximize_radii(C)\n        s = float(r.sum())\n        if s > best_sum:\n            best_sum = s\n            best_C = C\n            best_r = r\n\n    # fallback: if none found (shouldn't happen), use a conservative center layout\n    if best_C is None:\n        margin = 0.06\n        best_C = make_centers(margin)\n        best_r = maximize_radii(best_C)\n\n    return best_C, best_r, float(np.sum(best_r))\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.2549), Performs well on target_ratio (0.8558), Performs well on combined_score (0.8558), Performs well on eval_time (0.0646)\n\n### Program 2 (Score: 0.8291)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for 26 circles in the unit square.\n\nApproach:\n- Build a truncated hex/triangular lattice skeleton with row counts that sum to 26.\n- Deterministically scale and translate the skeleton into the unit square,\n  scanning over a grid of spacings and translations.\n- For each candidate center set compute maximal feasible radii by a robust\n  greedy relaxation: initialize by border-limits and iteratively enforce\n  ri + rj <= d_ij by reducing the larger circle when violated (tightest\n  pairs processed first). This is simple, deterministic and performs well.\n\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    n = 26\n    # Row layout that sums to 26 and resembles a truncated hex block\n    rows = [4, 5, 6, 5, 4, 2]  # sum = 26\n\n    # Build relative hex-like coordinates (unit lattice)\n    rel = []\n    h = np.sqrt(3.0) / 2.0\n    for r, k in enumerate(rows):\n        y = r * h\n        offset = 0.5 if (r % 2 == 1) else 0.0\n        xs = np.arange(k) + offset\n        for x in xs:\n            rel.append((x, y))\n    rel = np.array(rel, dtype=float)\n\n    # Normalize so min corner at (0,0) for easy scaling/translation\n    rel -= rel.min(axis=0)\n    Xmax, Ymax = rel.max(axis=0)\n\n    # Search grid parameters (deterministic)\n    # spacing range: choose values that can plausibly fit in [0,1]\n    s_min = 0.06\n    # upper bound based on fitting into unit square with zero margin\n    s_max_possible = min(1.0 / max(1.0, Xmax), 1.0 / max(1.0, Ymax))\n    s_max = min(0.22, s_max_possible * 0.98)\n    s_vals = np.linspace(s_min, s_max, 75)\n\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    # For each spacing try a small grid of translations (mx,my)\n    for s in s_vals:\n        W = s * Xmax\n        H = s * Ymax\n        if W > 1.0 + 1e-12 or H > 1.0 + 1e-12:\n            continue\n        # allowable translation ranges so centers lie in [0,1]\n        max_mx = 1.0 - W\n        max_my = 1.0 - H\n        # sample small grid including centered placement\n        cx = 0.5 * max_mx\n        cy = 0.5 * max_my\n        # Build translation candidates: include edges, center, and a couple of intermediate offsets\n        t_x_vals = np.linspace(0.0, max_mx, min(6, max(2, int(1 + max_mx / (0.02 + 1e-12)))))\n        t_y_vals = np.linspace(0.0, max_my, min(6, max(2, int(1 + max_my / (0.02 + 1e-12)))))\n\n        for mx in t_x_vals:\n            for my in t_y_vals:\n                centers = rel * s + np.array([mx, my])\n                # numerical safety: ensure inside [0,1]\n                if centers.min() < -1e-12 or centers.max() > 1.0 + 1e-12:\n                    continue\n\n                radii = compute_max_radii_relax(centers)\n                ssum = radii.sum()\n                if ssum > best_sum + 1e-12:\n                    best_sum = ssum\n                    best_centers = centers.copy()\n                    best_radii = radii.copy()\n\n    # Fallback (shouldn't happen) - center a default scaled layout\n    if best_centers is None:\n        s = 0.12\n        mx = 0.5 * (1.0 - s * Xmax)\n        my = 0.5 * (1.0 - s * Ymax)\n        best_centers = rel * s + np.array([mx, my])\n        best_radii = compute_max_radii_relax(best_centers)\n        best_sum = float(best_radii.sum())\n\n    return best_centers, best_radii, float(best_sum)\n\n\ndef compute_max_radii_relax(centers, tol=1e-9, max_iter=800):\n    \"\"\"\n    Greedy relaxation to get large feasible radii for fixed centers.\n\n    - Initialize radii to distance-to-border.\n    - Precompute pairwise distances and process pairs from smallest to largest.\n    - If ri + rj > d_ij, reduce the larger radius so ri + rj = d_ij (reducing the larger\n      preserves flexibility of smaller ones). Iterate until no meaningful change.\n    This deterministic strategy often produces near-optimal sums for fixed centers.\n    \"\"\"\n    n = centers.shape[0]\n    # border-limited initial radii\n    border = np.minimum.reduce([centers[:, 0], centers[:, 1], 1.0 - centers[:, 0], 1.0 - centers[:, 1]])\n    radii = border.copy()\n\n    # pairwise distances\n    diff = centers[:, None, :] - centers[None, :, :]\n    dmat = np.sqrt(np.sum(diff * diff, axis=2))\n    # set diagonal to inf for convenience\n    np.fill_diagonal(dmat, np.inf)\n\n    # build sorted list of pairs by distance (tight constraints first)\n    idx_i, idx_j = np.triu_indices(n, k=1)\n    pairs = list(zip(idx_i.tolist(), idx_j.tolist(), dmat[idx_i, idx_j].tolist()))\n    pairs.sort(key=lambda t: t[2])\n\n    # iterative enforcement\n    for it in range(max_iter):\n        changed = False\n        max_delta = 0.0\n        for (i, j, dij) in pairs:\n            # numerical protection\n            if not np.isfinite(dij) or dij <= 0.0:\n                # coincident or extremely close centers -> force one small if necessary\n                if radii[i] + radii[j] > 1e-12:\n                    if radii[i] >= radii[j]:\n                        new_i = max(0.0, dij - radii[j])\n                        delta = radii[i] - new_i\n                        if delta > tol:\n                            radii[i] = new_i\n                            changed = True\n                            max_delta = max(max_delta, delta)\n                    else:\n                        new_j = max(0.0, dij - radii[i])\n                        delta = radii[j] - new_j\n                        if delta > tol:\n                            radii[j] = new_j\n                            changed = True\n                            max_delta = max(max_delta, delta)\n                continue\n\n            if radii[i] + radii[j] > dij + 1e-12:\n                # reduce the larger one to satisfy constraint\n                if radii[i] > radii[j]:\n                    new_i = max(0.0, dij - radii[j])\n                    delta = radii[i] - new_i\n                    if delta > tol:\n                        radii[i] = new_i\n                        changed = True\n                        max_delta = max(max_delta, delta)\n                else:\n                    new_j = max(0.0, dij - radii[i])\n                    delta = radii[j] - new_j\n                    if delta > tol:\n                        radii[j] = new_j\n                        changed = True\n                        max_delta = max(max_delta, delta)\n\n        # ensure border constraints\n        over_border = radii > border\n        if np.any(over_border):\n            radii[over_border] = border[over_border]\n            changed = True\n\n        if not changed or max_delta < tol:\n            break\n\n    # Final safety: small proportional scaling if any tiny overlaps remain\n    for i in range(n):\n        for j in range(i + 1, n):\n            d = dmat[i, j]\n            if d < 1e-12:\n                # coincident, zero both\n                radii[i] = 0.0\n                radii[j] = 0.0\n            elif radii[i] + radii[j] > d:\n                s = d / (radii[i] + radii[j])\n                radii[i] *= s\n                radii[j] *= s\n\n    # clamp non-negativity and border limits\n    radii = np.clip(radii, 0.0, border)\n    return radii\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(7, 7))\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    for i, (c, r) in enumerate(zip(centers, radii)):\n        circ = Circle(c, r, alpha=0.6, edgecolor=\"k\")\n        ax.add_patch(circ)\n        ax.text(c[0], c[1], str(i), ha=\"center\", va=\"center\", fontsize=7)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # To inspect visually, uncomment:\n    # visualize(centers, radii)\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.1847), Performs well on target_ratio (0.8291), Performs well on combined_score (0.8291), Performs well on eval_time (1.2916)\n\n### Program 3 (Score: 0.8286)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Improved explicit constructor for packing 26 circles in the unit square.\nUses a layered hex-like layout [4,5,6,6,5] and searches a small parameter\nspace (spacing, vertical scale, stagger amplitude, small vertical shift)\nto maximize the sum of radii where each radius = min(distance to border, half nearest neighbor).\n\"\"\"\nimport numpy as np\nfrom math import sqrt\n\ndef construct_packing():\n    rows = [4, 5, 6, 6, 5]  # sums to 26\n    R = len(rows)\n    sqrt3 = sqrt(3.0)\n\n    def make_centers(s, t, a, ty):\n        # vertical spacing scaled by t\n        h = s * sqrt3 / 2.0 * t\n        y0 = 0.5 - (R - 1) * h / 2.0 + ty\n        centers = []\n        for r, L in enumerate(rows):\n            y = y0 + r * h\n            # stagger odd rows by a*s (can be negative)\n            offset = (a * s) if (r % 2 == 1) else 0.0\n            x0 = 0.5 - (L - 1) * s / 2.0 + offset\n            xs = x0 + np.arange(L) * s\n            for x in xs:\n                centers.append([x, y])\n        C = np.array(centers, dtype=float)\n        return C\n\n    def score_config(C):\n        # must be strictly inside unit square (allow touching)\n        if C.min() < -1e-9 or C.max() > 1.0 + 1e-9:\n            return -1e9, None, None\n        # distance to border\n        db = np.minimum.reduce([C[:,0], C[:,1], 1 - C[:,0], 1 - C[:,1]])\n        # pairwise distances\n        dif = C[:, None, :] - C[None, :, :]\n        d = np.sqrt((dif**2).sum(axis=2))\n        np.fill_diagonal(d, np.inf)\n        nearest = d.min(axis=1)\n        r = np.minimum(db, 0.5 * nearest)\n        r = np.maximum(r, 0.0)\n        return float(r.sum()), C, r\n\n    # coarse search ranges\n    s_vals = np.linspace(0.06, 0.30, 40)   # inter-center spacing\n    t_vals = np.linspace(0.92, 1.08, 6)    # vertical scale (allow slight anisotropy)\n    a_vals = np.linspace(-0.35, 0.35, 7)   # stagger amplitude (fraction of s)\n    ty_vals = np.linspace(-0.02, 0.02, 3)  # small vertical translation to exploit border\n    best_score = -1.0\n    best_C = None\n    best_r = None\n    best_params = None\n\n    for s in s_vals:\n        for t in t_vals:\n            for a in a_vals:\n                for ty in ty_vals:\n                    C = make_centers(s, t, a, ty)\n                    sc, Cc, rc = score_config(C)\n                    if sc > best_score:\n                        best_score = sc\n                        best_C, best_r = Cc, rc\n                        best_params = (s, t, a, ty)\n\n    # local refinement around best params (finer search)\n    if best_params is not None:\n        s0, t0, a0, ty0 = best_params\n        s_vals = np.linspace(max(0.02, s0 * 0.9), min(0.45, s0 * 1.1), 30)\n        t_vals = np.linspace(max(0.85, t0 - 0.05), min(1.15, t0 + 0.05), 8)\n        a_vals = np.linspace(a0 - 0.08, a0 + 0.08, 9)\n        ty_vals = np.linspace(ty0 - 0.01, ty0 + 0.01, 5)\n        for s in s_vals:\n            for t in t_vals:\n                for a in a_vals:\n                    for ty in ty_vals:\n                        C = make_centers(s, t, a, ty)\n                        sc, Cc, rc = score_config(C)\n                        if sc > best_score:\n                            best_score = sc\n                            best_C, best_r = Cc, rc\n                            best_params = (s, t, a, ty)\n\n    # final safety: recompute precise radii (exclude self)\n    C = np.clip(best_C, 1e-12, 1.0 - 1e-12)\n    dif = C[:, None, :] - C[None, :, :]\n    d = np.sqrt((dif**2).sum(axis=2))\n    np.fill_diagonal(d, np.inf)\n    half_nearest = 0.5 * d.min(axis=1)\n    dist_border = np.minimum.reduce([C[:,0], C[:,1], 1 - C[:,0], 1 - C[:,1]])\n    radii = np.minimum(dist_border, half_nearest)\n    radii = np.maximum(radii, 0.0)\n    sum_radii = float(radii.sum())\n    return C, radii, sum_radii\n\n# EVOLVE-BLOCK-END\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor='k')\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.1833), Performs well on target_ratio (0.8286), Performs well on combined_score (0.8286), Performs well on eval_time (0.4969)\n\n\n\n## Diverse Programs\n\n### Program D1 (Score: 0.8255)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Improved explicit constructor for packing 26 circles in the unit square.\n\nThis places 26 centers in a layered near-hex pattern rows=[4,5,6,6,5] and\nperforms a two-stage parameter search (coarse then local refinement) over:\n  - s : inter-center horizontal spacing\n  - t : vertical anisotropy of hex spacing\n  - a : stagger amplitude for odd rows (fraction of s)\n  - ty: small overall vertical shift\n\nRadii are set to r_i = min(dist_to_border_i, 0.5 * nearest_neighbor_dist).\n\"\"\"\nimport numpy as np\nfrom math import sqrt\n\ndef construct_packing():\n    rows = [4, 5, 6, 6, 5]  # sums to 26\n    R = len(rows)\n    sqrt3 = sqrt(3.0)\n\n    def make_centers(s, t, a, ty):\n        # vertical spacing scaled by t\n        h = s * sqrt3 / 2.0 * t\n        y0 = 0.5 - (R - 1) * h / 2.0 + ty\n        centers = []\n        for r, L in enumerate(rows):\n            y = y0 + r * h\n            offset = (a * s) if (r % 2 == 1) else 0.0\n            x0 = 0.5 - (L - 1) * s / 2.0 + offset\n            xs = x0 + np.arange(L) * s\n            for x in xs:\n                centers.append([x, y])\n        return np.array(centers, dtype=float)\n\n    def score_config(C):\n        # quick bounds check: must lie in [0,1] (allow tiny numerical slack)\n        if C.size == 0 or C.min() < -1e-9 or C.max() > 1.0 + 1e-9:\n            return -1e9, None, None\n        # distance to borders\n        db = np.minimum.reduce([C[:,0], C[:,1], 1.0 - C[:,0], 1.0 - C[:,1]])\n        # pairwise distances\n        dif = C[:, None, :] - C[None, :, :]\n        d = np.sqrt((dif**2).sum(axis=2))\n        np.fill_diagonal(d, np.inf)\n        nearest = d.min(axis=1)\n        r = np.minimum(db, 0.5 * nearest)\n        # if any radius nonpositive, treat as invalid\n        if np.any(r <= 0):\n            return -1e9, None, None\n        return float(r.sum()), C, r\n\n    # Coarse search\n    s_vals = np.linspace(0.06, 0.28, 36)\n    t_vals = np.linspace(0.94, 1.06, 5)\n    a_vals = np.linspace(-0.32, 0.32, 9)\n    ty_vals = np.linspace(-0.03, 0.03, 5)\n\n    best_score = -1.0\n    best_C = None\n    best_r = None\n    best_params = None\n\n    for s in s_vals:\n        for t in t_vals:\n            for a in a_vals:\n                for ty in ty_vals:\n                    C = make_centers(s, t, a, ty)\n                    sc, Cc, rc = score_config(C)\n                    if sc > best_score:\n                        best_score = sc\n                        best_C, best_r = Cc, rc\n                        best_params = (s, t, a, ty)\n\n    # Local refinement around best params\n    if best_params is not None:\n        s0, t0, a0, ty0 = best_params\n        s_vals = np.linspace(max(0.02, s0 * 0.88), min(0.45, s0 * 1.12), 28)\n        t_vals = np.linspace(max(0.85, t0 - 0.06), min(1.15, t0 + 0.06), 9)\n        a_vals = np.linspace(a0 - 0.12, a0 + 0.12, 11)\n        ty_vals = np.linspace(ty0 - 0.02, ty0 + 0.02, 7)\n        for s in s_vals:\n            for t in t_vals:\n                for a in a_vals:\n                    for ty in ty_vals:\n                        C = make_centers(s, t, a, ty)\n                        sc, Cc, rc = score_config(C)\n                        if sc > best_score:\n                            best_score = sc\n                            best_C, best_r = Cc, rc\n                            best_params = (s, t, a, ty)\n\n    # Finalize best configuration\n    if best_C is None:\n        # fallback deterministic layout\n        best_C = make_centers(0.12, 1.0, 0.0, 0.0)\n        best_r = score_config(best_C)[2]\n\n    C = np.clip(best_C, 1e-12, 1.0 - 1e-12)\n    # recompute radii precisely\n    dif = C[:, None, :] - C[None, :, :]\n    d = np.sqrt((dif**2).sum(axis=2))\n    np.fill_diagonal(d, np.inf)\n    half_nearest = 0.5 * d.min(axis=1)\n    dist_border = np.minimum.reduce([C[:,0], C[:,1], 1.0 - C[:,0], 1.0 - C[:,1]])\n    radii = np.minimum(dist_border, half_nearest)\n    radii = np.maximum(radii, 0.0)\n    sum_radii = float(radii.sum())\n    return C, radii, sum_radii\n\n# EVOLVE-BLOCK-END\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor='k')\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nKey features: Alternative approach to validity, Alternative approach to sum_radii\n\n### Program D2 (Score: 0.8027)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (improved explicit constructor)\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square.\n    This constructor places points in a compact staggered (nearly hexagonal)\n    layout with an inward margin to balance border effects and inter-circle spacing.\n    Radii are set to the largest non-overlapping values allowed by borders\n    and pairwise distances: r_i = min(dist_to_border_i, min_j d_ij/2).\n\n    Returns:\n        centers: np.array of shape (26,2)\n        radii: np.array of shape (26,)\n        sum_of_radii: float\n    \"\"\"\n    n = 26\n\n    # Row layout (sums to 26). A near-hexagonal packing: some rows longer to use space.\n    cols_per_row = [5, 6, 5, 5, 5]  # total = 26\n    rows = len(cols_per_row)\n\n    # Margin from the square boundary. Chosen to balance border-limited radii vs spacing.\n    margin = 0.08  # tuned so edge circles maintain a reasonable radius\n\n    # Vertical spacing across rows (equal spacing between row centers)\n    available_height = 1.0 - 2.0 * margin\n    if rows == 1:\n        ys = np.array([0.5])\n    else:\n        ys = margin + np.linspace(0.0, available_height, rows)\n\n    centers = []\n    # Place each row centered horizontally, with x positions evenly spaced across [margin, 1-margin]\n    for row_idx, cnt in enumerate(cols_per_row):\n        if cnt == 1:\n            xs = np.array([0.5])\n        else:\n            available_width = 1.0 - 2.0 * margin\n            xs = margin + np.linspace(0.0, available_width, cnt)\n\n        # Stagger alternate rows to approach hexagonal packing (shift by half average step)\n        if row_idx % 2 == 1:\n            # compute an average horizontal step for this row (or fallback)\n            if len(xs) > 1:\n                step = xs[1] - xs[0]\n                xs = xs + 0.5 * step\n            else:\n                xs = xs  # nothing to do\n\n        # Clip to remain inside [margin, 1-margin] (staggering could push endpoints slightly)\n        xs = np.clip(xs, margin, 1.0 - margin)\n        for x in xs:\n            centers.append((x, ys[row_idx]))\n\n    centers = np.array(centers, dtype=float)\n    assert centers.shape[0] == n, \"Center count mismatch\"\n\n    radii = compute_max_radii(centers)\n\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers):\n    \"\"\"\n    Compute radii by the conservative but robust rule:\n      r_i = min( dist_to_border_i,  min_{j != i} d_ij / 2 )\n\n    This guarantees non-overlap and containment in the unit square.\n    \"\"\"\n    n = centers.shape[0]\n    # Distance to borders\n    d_border = np.minimum.reduce([centers[:, 0], centers[:, 1], 1.0 - centers[:, 0], 1.0 - centers[:, 1]])\n\n    # Pairwise distances\n    diff = centers[:, None, :] - centers[None, :, :]  # shape (n,n,2)\n    dists = np.sqrt(np.sum(diff * diff, axis=2))      # shape (n,n)\n    # Avoid zero on diagonal by setting to large number\n    np.fill_diagonal(dists, np.inf)\n\n    half_min_neighbor = np.min(dists, axis=1) / 2.0\n\n    radii = np.minimum(d_border, half_min_neighbor)\n    # Ensure non-negative\n    radii = np.maximum(radii, 0.0)\n    return radii\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nKey features: Alternative approach to validity, Alternative approach to sum_radii\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 0.6449, Type: Alternative)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Explicit constructor for n=26 circles: truncated hex layers with LP (or robust projector) for optimal radii.\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    rows = [4, 5, 6, 6, 5]  # 26 centers in near-hex layout\n\n    def make_centers(s):\n        h = s * np.sqrt(3) / 2.0\n        R = len(rows)\n        y0 = 0.5 - (R - 1) * h / 2.0\n        centers = []\n        for r, L in enumerate(rows):\n            y = y0 + r * h\n            offset = (s / 2.0) if (r % 2 == 1) else 0.0\n            x0 = 0.5 - (L - 1) * s / 2.0 + offset\n            for k in range(L):\n                centers.append([x0 + k * s, y])\n        return np.array(centers)\n\n    # maximal s so centers remain inside [0,1]\n    def fits(s):\n        C = make_centers(s)\n        return (C.min() >= 0.0) and (C.max() <= 1.0)\n\n    lo, hi = 1e-6, 1.0\n    for _ in range(40):\n        mid = 0.5 * (lo + hi)\n        if fits(mid):\n            lo = mid\n        else:\n            hi = mid\n    s_max = lo\n\n    # search near s_max for best sum of radii\n    samples = np.linspace(max(1e-6, 0.55 * s_max), s_max, 160)\n\n    best_sum = -1.0\n    best_C = None\n    best_r = None\n\n    for s in samples:\n        C = make_centers(s)\n        n = C.shape[0]\n        border = np.minimum.reduce([C[:,0], C[:,1], 1 - C[:,0], 1 - C[:,1]])\n        dx = C[:,0][:,None] - C[:,0][None,:]\n        dy = C[:,1][:,None] - C[:,1][None,:]\n        dists = np.sqrt(dx*dx + dy*dy)\n        np.fill_diagonal(dists, 1e9)\n\n        # Try LP solver if available\n        radii = None\n        try:\n            from scipy.optimize import linprog\n            nvar = n\n            c = -np.ones(nvar)  # maximize sum -> minimize -sum\n            # bounds r_i in [0, border_i]\n            bounds = [(0.0, float(border[i])) for i in range(nvar)]\n            # constraints r_i + r_j <= d_ij for i<j\n            m = nvar*(nvar-1)//2\n            A = np.zeros((m, nvar))\n            b = np.zeros(m)\n            idx = 0\n            for i in range(nvar):\n                for j in range(i+1, nvar):\n                    A[idx, i] = 1.0\n                    A[idx, j] = 1.0\n                    b[idx] = float(dists[i,j])\n                    idx += 1\n            res = linprog(c, A_ub=A, b_ub=b, bounds=bounds, method=\"highs\")\n            if res.success:\n                radii = np.maximum(0.0, res.x)\n        except Exception:\n            radii = None\n\n        # Fallback projector (Gauss-Seidel style): tends to a feasible high-sum solution\n        if radii is None:\n            r = border.copy()\n            tol = 1e-8\n            for it in range(5000):\n                maxchg = 0.0\n                for i in range(n):\n                    # each r_i must satisfy r_i <= border_i and r_i <= d_ij - r_j for all j\n                    limits = border[i]\n                    # compute minimal (d_ij - r_j) over j!=i\n                    vals = dists[i,:] - r\n                    vals[i] = np.inf\n                    lim = vals.min()\n                    newri = min(limits, max(0.0, lim))\n                    maxchg = max(maxchg, abs(newri - r[i]))\n                    r[i] = newri\n                if maxchg <= tol:\n                    break\n            radii = r\n\n        ssum = float(np.sum(radii))\n        if ssum > best_sum:\n            best_sum = ssum\n            best_C = C.copy()\n            best_r = radii.copy()\n\n    return best_C, best_r, float(best_sum)\n# EVOLVE-BLOCK-END\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor='k', linewidth=0.5)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nUnique approach: Modification: Full rewrite, Excellent validity (1.000), Excellent sum_radii (1.699)\n\n### Inspiration 2 (Score: 0.8291, Type: High-Performer)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for 26 circles in the unit square.\n\nApproach:\n- Build a truncated hex/triangular lattice skeleton with row counts that sum to 26.\n- Deterministically scale and translate the skeleton into the unit square,\n  scanning over a grid of spacings and translations.\n- For each candidate center set compute maximal feasible radii by a robust\n  greedy relaxation: initialize by border-limits and iteratively enforce\n  ri + rj <= d_ij by reducing the larger circle when violated (tightest\n  pairs processed first). This is simple, deterministic and performs well.\n\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    n = 26\n    # Row layout that sums to 26 and resembles a truncated hex block\n    rows = [4, 5, 6, 5, 4, 2]  # sum = 26\n\n    # Build relative hex-like coordinates (unit lattice)\n    rel = []\n    h = np.sqrt(3.0) / 2.0\n    for r, k in enumerate(rows):\n        y = r * h\n        offset = 0.5 if (r % 2 == 1) else 0.0\n        xs = np.arange(k) + offset\n        for x in xs:\n            rel.append((x, y))\n    rel = np.array(rel, dtype=float)\n\n    # Normalize so min corner at (0,0) for easy scaling/translation\n    rel -= rel.min(axis=0)\n    Xmax, Ymax = rel.max(axis=0)\n\n    # Search grid parameters (deterministic)\n    # spacing range: choose values that can plausibly fit in [0,1]\n    s_min = 0.06\n    # upper bound based on fitting into unit square with zero margin\n    s_max_possible = min(1.0 / max(1.0, Xmax), 1.0 / max(1.0, Ymax))\n    s_max = min(0.22, s_max_possible * 0.98)\n    s_vals = np.linspace(s_min, s_max, 75)\n\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    # For each spacing try a small grid of translations (mx,my)\n    for s in s_vals:\n        W = s * Xmax\n        H = s * Ymax\n        if W > 1.0 + 1e-12 or H > 1.0 + 1e-12:\n            continue\n        # allowable translation ranges so centers lie in [0,1]\n        max_mx = 1.0 - W\n        max_my = 1.0 - H\n        # sample small grid including centered placement\n        cx = 0.5 * max_mx\n        cy = 0.5 * max_my\n        # Build translation candidates: include edges, center, and a couple of intermediate offsets\n        t_x_vals = np.linspace(0.0, max_mx, min(6, max(2, int(1 + max_mx / (0.02 + 1e-12)))))\n        t_y_vals = np.linspace(0.0, max_my, min(6, max(2, int(1 + max_my / (0.02 + 1e-12)))))\n\n        for mx in t_x_vals:\n            for my in t_y_vals:\n                centers = rel * s + np.array([mx, my])\n                # numerical safety: ensure inside [0,1]\n                if centers.min() < -1e-12 or centers.max() > 1.0 + 1e-12:\n                    continue\n\n                radii = compute_max_radii_relax(centers)\n                ssum = radii.sum()\n                if ssum > best_sum + 1e-12:\n                    best_sum = ssum\n                    best_centers = centers.copy()\n                    best_radii = radii.copy()\n\n    # Fallback (shouldn't happen) - center a default scaled layout\n    if best_centers is None:\n        s = 0.12\n        mx = 0.5 * (1.0 - s * Xmax)\n        my = 0.5 * (1.0 - s * Ymax)\n        best_centers = rel * s + np.array([mx, my])\n        best_radii = compute_max_radii_relax(best_centers)\n        best_sum = float(best_radii.sum())\n\n    return best_centers, best_radii, float(best_sum)\n\n\ndef compute_max_radii_relax(centers, tol=1e-9, max_iter=800):\n    \"\"\"\n    Greedy relaxation to get large feasible radii for fixed centers.\n\n    - Initialize radii to distance-to-border.\n    - Precompute pairwise distances and process pairs from smallest to largest.\n    - If ri + rj > d_ij, reduce the larger radius so ri + rj = d_ij (reducing the larger\n      preserves flexibility of smaller ones). Iterate until no meaningful change.\n    This deterministic strategy often produces near-optimal sums for fixed centers.\n    \"\"\"\n    n = centers.shape[0]\n    # border-limited initial radii\n    border = np.minimum.reduce([centers[:, 0], centers[:, 1], 1.0 - centers[:, 0], 1.0 - centers[:, 1]])\n    radii = border.copy()\n\n    # pairwise distances\n    diff = centers[:, None, :] - centers[None, :, :]\n    dmat = np.sqrt(np.sum(diff * diff, axis=2))\n    # set diagonal to inf for convenience\n    np.fill_diagonal(dmat, np.inf)\n\n    # build sorted list of pairs by distance (tight constraints first)\n    idx_i, idx_j = np.triu_indices(n, k=1)\n    pairs = list(zip(idx_i.tolist(), idx_j.tolist(), dmat[idx_i, idx_j].tolist()))\n    pairs.sort(key=lambda t: t[2])\n\n    # iterative enforcement\n    for it in range(max_iter):\n        changed = False\n        max_delta = 0.0\n        for (i, j, dij) in pairs:\n            # numerical protection\n            if not np.isfinite(dij) or dij <= 0.0:\n                # coincident or extremely close centers -> force one small if necessary\n                if radii[i] + radii[j] > 1e-12:\n                    if radii[i] >= radii[j]:\n                        new_i = max(0.0, dij - radii[j])\n                        delta = radii[i] - new_i\n                        if delta > tol:\n                            radii[i] = new_i\n                            changed = True\n                            max_delta = max(max_delta, delta)\n                    else:\n                        new_j = max(0.0, dij - radii[i])\n                        delta = radii[j] - new_j\n                        if delta > tol:\n                            radii[j] = new_j\n                            changed = True\n                            max_delta = max(max_delta, delta)\n                continue\n\n            if radii[i] + radii[j] > dij + 1e-12:\n                # reduce the larger one to satisfy constraint\n                if radii[i] > radii[j]:\n                    new_i = max(0.0, dij - radii[j])\n                    delta = radii[i] - new_i\n                    if delta > tol:\n                        radii[i] = new_i\n                        changed = True\n                        max_delta = max(max_delta, delta)\n                else:\n                    new_j = max(0.0, dij - radii[i])\n                    delta = radii[j] - new_j\n                    if delta > tol:\n                        radii[j] = new_j\n                        changed = True\n                        max_delta = max(max_delta, delta)\n\n        # ensure border constraints\n        over_border = radii > border\n        if np.any(over_border):\n            radii[over_border] = border[over_border]\n            changed = True\n\n        if not changed or max_delta < tol:\n            break\n\n    # Final safety: small proportional scaling if any tiny overlaps remain\n    for i in range(n):\n        for j in range(i + 1, n):\n            d = dmat[i, j]\n            if d < 1e-12:\n                # coincident, zero both\n                radii[i] = 0.0\n                radii[j] = 0.0\n            elif radii[i] + radii[j] > d:\n                s = d / (radii[i] + radii[j])\n                radii[i] *= s\n                radii[j] *= s\n\n    # clamp non-negativity and border limits\n    radii = np.clip(radii, 0.0, border)\n    return radii\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(7, 7))\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    for i, (c, r) in enumerate(zip(centers, radii)):\n        circ = Circle(c, r, alpha=0.6, edgecolor=\"k\")\n        ax.add_patch(circ)\n        ax.text(c[0], c[1], str(i), ha=\"center\", va=\"center\", fontsize=7)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # To inspect visually, uncomment:\n    # visualize(centers, radii)\n```\nUnique approach: Modification: Full rewrite, Excellent validity (1.000), Excellent sum_radii (2.185)\n\n### Inspiration 3 (Score: 0.6890, Type: Alternative)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (compact hex-layer constructor)\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    # row pattern summing to 26\n    rows = [4, 5, 6, 6, 5]\n    R = len(rows)\n\n    def make_centers(s):\n        h = s * np.sqrt(3) / 2.0\n        centers = []\n        y0 = 0.5 - (R - 1) * h / 2.0\n        for r, L in enumerate(rows):\n            y = y0 + r * h\n            offset = (s / 2.0) if (r % 2 == 1) else 0.0\n            x0 = 0.5 - (L - 1) * s / 2.0 + offset\n            for k in range(L):\n                centers.append([x0 + k * s, y])\n        return np.array(centers)\n\n    # find largest s so all centers lie in [0,1]\n    lo, hi = 1e-6, 1.0\n    for _ in range(40):\n        mid = 0.5 * (lo + hi)\n        C = make_centers(mid)\n        if C.min() >= 0 and C.max() <= 1.0:\n            lo = mid\n        else:\n            hi = mid\n    s_max = lo\n\n    # sample s near s_max and pick one maximizing sum of radii\n    samples = np.linspace(max(1e-6, 0.6 * s_max), s_max, 80)\n    best = None\n    best_sum = -1.0\n    for s in samples:\n        C = make_centers(s)\n        # border limit\n        border = np.minimum.reduce([C[:,0], C[:,1], 1 - C[:,0], 1 - C[:,1]])\n        # pairwise distances\n        dif = C[:,None,:] - C[None,:,:]\n        d = np.sqrt((dif**2).sum(axis=2))\n        np.fill_diagonal(d, np.inf)\n        rad_nn = 0.5 * d.min(axis=1)\n        r = np.minimum(border, rad_nn)\n        ssum = float(r.sum())\n        if ssum > best_sum:\n            best_sum = ssum\n            best = (C, r)\n\n    # final precise recompute to ensure safety\n    C, r = best\n    dif = C[:,None,:] - C[None,:,:]\n    d = np.sqrt((dif**2).sum(axis=2))\n    np.fill_diagonal(d, np.inf)\n    r = np.minimum(np.minimum.reduce([C[:,0], C[:,1], 1 - C[:,0], 1 - C[:,1]]), 0.5 * d.min(axis=1))\n    r = np.maximum(r, 0.0)\n    return C, r, float(r.sum())\n# EVOLVE-BLOCK-END\n\n# fixed interface\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(6,6))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect(\"equal\")\n    for i,(c,rr) in enumerate(zip(centers, radii)):\n        ax.add_patch(Circle(c, rr, alpha=0.6, edgecolor='k'))\n        ax.text(c[0], c[1], str(i), ha='center', va='center', fontsize=7)\n    plt.title(f\"n={len(centers)} sum={sum(radii):.6f}\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, s = run_packing()\n    print(f\"Sum of radii: {s:.6f}\")\n    # visualize(centers, radii)\n```\nUnique approach: Modification: Full rewrite, Excellent validity (1.000), Excellent sum_radii (1.815)\n\n# Current Program\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (hex-like explicit layout)\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    \"\"\"\n    Construct an explicit arrangement of 26 circle centers in a unit square\n    following a truncated hexagonal lattice (rows: 5,6,6,5,4) and compute\n    maximal non-overlapping radii by iterative relaxation.\n    Returns: (centers (26,2), radii (26,), sum_radii)\n    \"\"\"\n    n = 26\n    # Row layout chosen to sum to 26: 5+6+6+5+4 = 26\n    row_counts = [5, 6, 6, 5, 4]\n    rows = len(row_counts)\n\n    # Small margin so circles may touch borders but do not exceed\n    border_margin = 0.01\n\n    # Compute maximum lattice spacing s that fits horizontally for each row\n    # and vertically for all rows (hex vertical spacing sqrt(3)/2 * s)\n    available = 1.0 - 2 * border_margin\n    s_candidates = []\n    for k in row_counts:\n        if k > 1:\n            s_candidates.append(available / (k - 1))\n    # vertical constraint\n    if rows > 1:\n        s_candidates.append(available / ((rows - 1) * (np.sqrt(3) / 2)))\n    s = min(s_candidates) * 0.98  # slight shrink to avoid exact touching issues\n    v = s * np.sqrt(3) / 2\n\n    # Build centers: center each row horizontally, stack rows vertically centered\n    vertical_span = (rows - 1) * v\n    y_start = (1.0 - vertical_span) / 2.0\n    centers = []\n    for r, k in enumerate(row_counts):\n        span = (k - 1) * s if k > 1 else 0.0\n        x_start = (1.0 - span) / 2.0\n        y = y_start + r * v\n        # For hex-like staggering, shift alternate rows by s/2 where it helps center shorter rows\n        # but since we compute x_start from span this centers each row already.\n        for i in range(k):\n            x = x_start + i * s\n            centers.append([x, y])\n    centers = np.array(centers[:n], dtype=float)\n\n    # Ensure within [border_margin, 1-border_margin]\n    centers = np.clip(centers, border_margin, 1.0 - border_margin)\n\n    # Compute maximal radii via iterative relaxation:\n    radii = np.minimum(np.minimum(centers[:, 0], 1 - centers[:, 0]),\n                       np.minimum(centers[:, 1], 1 - centers[:, 1]))\n    # Start with a slightly larger allowance (so we can grow some if possible),\n    # but keep cap by border distance\n    radii = radii.copy()\n\n    max_iters = 2000\n    tol = 1e-6\n    for it in range(max_iters):\n        max_change = 0.0\n        # enforce border constraints\n        border_caps = np.minimum(np.minimum(centers[:, 0], 1 - centers[:, 0]),\n                                 np.minimum(centers[:, 1], 1 - centers[:, 1]))\n        # occasionally allow slight growth up to border\n        radii = np.minimum(radii * 1.01, border_caps)\n\n        # pairwise resolve overlaps: reduce the larger circle preferentially\n        for i in range(n):\n            ci = centers[i]\n            ri = radii[i]\n            for j in range(i + 1, n):\n                cj = centers[j]\n                rj = radii[j]\n                d = np.hypot(ci[0] - cj[0], ci[1] - cj[1])\n                if d <= 0:\n                    # identical centers: push one tiny\n                    continue\n                overlap = ri + rj - d\n                if overlap > 1e-12:\n                    # reduce radii to remove overlap; reduce larger more than smaller\n                    if ri > rj:\n                        reduce_i = overlap * 0.6\n                        reduce_j = overlap * 0.4\n                    else:\n                        reduce_i = overlap * 0.4\n                        reduce_j = overlap * 0.6\n                    new_ri = max(1e-8, ri - reduce_i)\n                    new_rj = max(1e-8, rj - reduce_j)\n                    max_change = max(max_change, abs(new_ri - ri), abs(new_rj - rj))\n                    radii[i] = new_ri\n                    radii[j] = new_rj\n\n        if max_change < tol:\n            break\n\n    # Final safety clip to borders and non-negativity\n    radii = np.clip(radii, 1e-8, border_caps)\n\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\n\n# Task\nRewrite the program to improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: complexity, diversity\nDifferent solutions with similar fitness but different features are valuable.\nProvide the complete new program code.\n\nIMPORTANT: Make sure your rewritten program maintains the same inputs and outputs\nas the original program, but with improved internal implementation.\n\n```python\n# Your rewritten program here\n```", "responses": ["```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for 26 circles in the unit square.\nBuilds a truncated hex lattice skeleton (rows sum to 26), scans spacings\nand a few translations, and for each candidate computes large feasible\nradii by a deterministic greedy relaxation (reduce larger circle when\na pair constraint ri+rj>d_ij is violated). Returns best configuration.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    rows = [4, 5, 6, 6, 5]  # sums to 26\n\n    # build relative hex-lattice (unit spacing)\n    rel = []\n    h_unit = np.sqrt(3.0) / 2.0\n    for r, cnt in enumerate(rows):\n        y = r * h_unit\n        offset = 0.5 if (r % 2 == 1) else 0.0\n        xs = np.arange(cnt) + offset\n        for x in xs:\n            rel.append((x, y))\n    rel = np.array(rel, dtype=float)\n    # normalize to start at (0,0)\n    rel -= rel.min(axis=0)\n    Xmax, Ymax = rel.max(axis=0)\n\n    # feasible spacing upper bound so the pattern can fit in [0,1]\n    s_max_possible = min(1.0 / max(1.0, Xmax), 1.0 / max(1.0, Ymax))\n    s_min = 0.06\n    s_max = max(s_min, s_max_possible * 0.98)\n    s_vals = np.linspace(s_min, s_max, 60)\n\n    def compute_max_radii_relax(C, tol=1e-9, max_iter=800):\n        m = C.shape[0]\n        border = np.minimum.reduce([C[:,0], C[:,1], 1.0 - C[:,0], 1.0 - C[:,1]])\n        r = border.copy()\n        # pairwise distances\n        diff = C[:, None, :] - C[None, :, :]\n        dmat = np.sqrt((diff**2).sum(axis=2))\n        np.fill_diagonal(dmat, np.inf)\n        # prepare sorted pairs by distance (tightest first)\n        ii, jj = np.triu_indices(m, k=1)\n        pairs = list(zip(ii.tolist(), jj.tolist(), dmat[ii, jj].tolist()))\n        pairs.sort(key=lambda t: t[2])\n        for it in range(max_iter):\n            changed = False\n            maxdelta = 0.0\n            for i, j, dij in pairs:\n                if not np.isfinite(dij) or dij <= 0.0:\n                    # degenerate: force small\n                    if r[i] + r[j] > 1e-12:\n                        if r[i] >= r[j]:\n                            newi = max(0.0, dij - r[j])\n                            delta = r[i] - newi\n                            if delta > tol:\n                                r[i] = newi; changed = True; maxdelta = max(maxdelta, delta)\n                        else:\n                            newj = max(0.0, dij - r[i])\n                            delta = r[j] - newj\n                            if delta > tol:\n                                r[j] = newj; changed = True; maxdelta = max(maxdelta, delta)\n                    continue\n                if r[i] + r[j] > dij + 1e-12:\n                    # reduce larger one\n                    if r[i] > r[j]:\n                        newi = max(0.0, dij - r[j])\n                        delta = r[i] - newi\n                        if delta > tol:\n                            r[i] = newi; changed = True; maxdelta = max(maxdelta, delta)\n                    else:\n                        newj = max(0.0, dij - r[i])\n                        delta = r[j] - newj\n                        if delta > tol:\n                            r[j] = newj; changed = True; maxdelta = max(maxdelta, delta)\n            # enforce border cap\n            over = r > border\n            if np.any(over):\n                r[over] = border[over]; changed = True\n            if not changed or maxdelta < tol:\n                break\n        # tiny final fix for any remaining overlaps\n        for i in range(m):\n            for j in range(i+1, m):\n                d = dmat[i,j]\n                if d < 1e-12:\n                    r[i] = r[j] = 0.0\n                elif r[i] + r[j] > d:\n                    s = d / (r[i] + r[j])\n                    r[i] *= s; r[j] *= s\n        r = np.clip(r, 0.0, border)\n        return r\n\n    best_sum = -1.0\n    best_C = None\n    best_r = None\n\n    # sample a few translations for each spacing (include centered placement)\n    for s in s_vals:\n        W, H = s * Xmax, s * Ymax\n        if W > 1.0 + 1e-12 or H > 1.0 + 1e-12:\n            continue\n        max_mx = 1.0 - W\n        max_my = 1.0 - H\n        # choose up to 5 translation samples in each direction (edges and center)\n        tx_vals = np.linspace(0.0, max_mx, min(5, 1 + int(round(max_mx / 0.02 + 1e-9))))\n        ty_vals = np.linspace(0.0, max_my, min(5, 1 + int(round(max_my / 0.02 + 1e-9))))\n\n        for mx in tx_vals:\n            for my in ty_vals:\n                C = rel * s + np.array([mx, my])\n                if C.min() < -1e-12 or C.max() > 1.0 + 1e-12:\n                    continue\n                r = compute_max_radii_relax(C)\n                ssum = float(r.sum())\n                if ssum > best_sum + 1e-12:\n                    best_sum = ssum\n                    best_C = C.copy()\n                    best_r = r.copy()\n\n    # fallback if nothing found (shouldn't happen)\n    if best_C is None:\n        s = 0.12\n        mx = 0.5 * (1.0 - s * Xmax)\n        my = 0.5 * (1.0 - s * Ymax)\n        best_C = rel * s + np.array([mx, my])\n        best_r = compute_max_radii_relax(best_C)\n        best_sum = float(best_r.sum())\n\n    return best_C, best_r, float(best_sum)\n# EVOLVE-BLOCK-END\n\n\n# fixed interface\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(7,7))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect(\"equal\")\n    for i,(c,r) in enumerate(zip(centers, radii)):\n        ax.add_patch(Circle(c, r, alpha=0.6, edgecolor='k'))\n        ax.text(c[0], c[1], str(i), ha='center', va='center', fontsize=7)\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, s = run_packing()\n    print(f\"Sum of radii: {s:.6f}\")\n    # visualize(centers, radii)\n```"]}}, "artifacts_json": "{\"stdout\": \"Running in subprocess, Python version: 3.12.1 (v3.12.1:2305ca5144, Dec  7 2023, 17:23:38) [Clang 13.0.0 (clang-1300.0.29.30)]\\nProgram path: /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmp5pkj0ry9.py\\nCalling run_packing()...\\nrun_packing() returned successfully: sum_radii = 1.9761016949152528\\nResults saved to /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmpfavnkz6f.py.results\\n\", \"stderr\": \"\"}", "artifact_dir": null}