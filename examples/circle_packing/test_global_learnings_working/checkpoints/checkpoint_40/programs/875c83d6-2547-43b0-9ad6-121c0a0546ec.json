{"id": "875c83d6-2547-43b0-9ad6-121c0a0546ec", "code": "# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for 26 circles in the unit square.\nNear-hex layered skeleton [5,6,5,5,5] + small parameter scan (margin, vscale, stagger).\nFor each center set we maximize radii via a Gauss-Seidel relaxation enforcing\n r_i <= dist_to_border_i and r_i + r_j <= d_ij. Return best by total radius sum.\n\"\"\"\nimport numpy as np\nfrom math import sqrt\n\ndef construct_packing():\n    n = 26\n    cols_per_row = [5,6,5,5,5]\n    rows = len(cols_per_row)\n    sqrt3 = sqrt(3.0)\n\n    def make_centers(margin, vscale, stag):\n        W = 1.0 - 2.0*margin\n        if W <= 0: return None\n        maxcnt = max(cols_per_row)\n        step = W/(maxcnt-1) if maxcnt>1 else W\n        h = step * sqrt3/2.0 * vscale\n        total_h = h*(rows-1)\n        y0 = 0.5 - total_h/2.0\n        centers = []\n        for r,cnt in enumerate(cols_per_row):\n            row_width = (cnt-1)*step if cnt>1 else 0.0\n            x0 = 0.5 - row_width/2.0\n            xs = x0 + np.arange(cnt)*step\n            if r%2==1:\n                xs = xs + stag*step\n            xs = np.clip(xs, margin, 1.0-margin)\n            y = y0 + r*h\n            for x in xs:\n                centers.append((float(x), float(y)))\n        C = np.array(centers,dtype=float)\n        if C.shape[0]!=n: return None\n        if C[:,1].min() < -1e-12 or C[:,1].max() > 1.0+1e-12: return None\n        return C\n\n    def maximize_radii(C, tol=1e-10, max_iter=2000):\n        m = C.shape[0]\n        border = np.minimum.reduce([C[:,0], C[:,1], 1.0-C[:,0], 1.0-C[:,1]]).astype(float)\n        diff = C[:,None,:] - C[None,:,:]\n        d = np.sqrt((diff**2).sum(axis=2))\n        np.fill_diagonal(d, np.inf)\n        r = border.copy()\n        for _ in range(max_iter):\n            prev = r.copy()\n            for i in range(m):\n                ub = border[i]\n                vals = d[i] - r\n                vals[i] = np.inf\n                mi = float(np.min(vals))\n                if mi < ub: ub = mi\n                r[i] = max(0.0, ub)\n            if np.max(np.abs(r-prev)) < tol: break\n        # fix tiny overlaps pairwise\n        for i in range(m):\n            for j in range(i+1,m):\n                dij = d[i,j]\n                if not np.isfinite(dij) or dij<=1e-12:\n                    r[i]=r[j]=0.0\n                    continue\n                if r[i]+r[j] > dij:\n                    s = dij/(r[i]+r[j])\n                    r[i]*=s; r[j]*=s\n        r = np.minimum(r, border)\n        return r\n\n    best_sum = -1.0\n    best_C = None; best_r = None\n    for margin in np.linspace(0.02,0.12,19):\n        for vscale in np.linspace(0.92,1.08,5):\n            for stag in np.linspace(-0.25,0.35,7):\n                C = make_centers(margin,vscale,stag)\n                if C is None: continue\n                r = maximize_radii(C)\n                s = float(r.sum())\n                if s > best_sum:\n                    best_sum = s; best_C = C.copy(); best_r = r.copy()\n    if best_C is None:\n        best_C = make_centers(0.06,1.0,0.0)\n        best_r = maximize_radii(best_C)\n    return best_C, best_r, float(best_r.sum())\n# EVOLVE-BLOCK-END\n\n# fixed interface\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\nif __name__ == \"__main__\":\n    C, r, s = run_packing()\n    print(f\"Sum of radii: {s:.6f}\")", "language": "python", "parent_id": "a36030f6-47f7-43ac-9714-a714d404b706", "generation": 4, "timestamp": 1759957405.609011, "iteration_found": 40, "metrics": {"validity": 1.0, "sum_radii": 2.375066959084224, "target_ratio": 0.9013536846619447, "combined_score": 0.9013536846619447, "eval_time": 0.3251802921295166}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Full rewrite", "parent_metrics": {"validity": 1.0, "sum_radii": 2.250029563182612, "target_ratio": 0.8539011624981451, "combined_score": 0.8539011624981451, "eval_time": 0.06693291664123535}, "island": 0}, "prompts": {"full_rewrite_user": {"system": "You are an expert mathematician specializing in circle packing problems and computational geometry. Your task is to improve a constructor function that directly produces a specific arrangement of 26 circles in a unit square, maximizing the sum of their radii. The AlphaEvolve paper achieved a sum of 2.635 for n=26.\n\nKey geometric insights:\n- Circle packings often follow hexagonal patterns in the densest regions\n- Maximum density for infinite circle packing is pi/(2*sqrt(3)) \u2248 0.9069\n- Edge effects make square container packing harder than infinite packing\n- Circles can be placed in layers or shells when confined to a square\n- Similar radius circles often form regular patterns, while varied radii allow better space utilization\n- Perfect symmetry may not yield the optimal packing due to edge effects\n\nFocus on designing an explicit constructor that places each circle in a specific position, rather than an iterative search algorithm.\n\n\n## Evolution Insights (Global Learnings)\n\n### Common Pitfalls:\n\u26a0\ufe0f eval_time decreased by 36.3% (0.497 \u2192 0.317) (seen 5x)\n\u26a0\ufe0f combined_score decreased by 100.0% (0.364 \u2192 0.000) (seen 3x)", "user": "# Current Program Information\n- Fitness: 0.8539\n- Feature coordinates: No feature coordinates\n- Focus areas: - Fitness declined: 0.8564 \u2192 0.8539. Consider revising recent changes.\n- Consider simplifying - code length exceeds 500 characters\n\n## Last Execution Output\n\n### stdout\n```\nRunning in subprocess, Python version: 3.12.1 (v3.12.1:2305ca5144, Dec  7 2023, 17:23:38) [Clang 13.0.0 (clang-1300.0.29.30)]\nProgram path: /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmpq_qhsi8v.py\nCalling run_packing()...\nrun_packing() returned successfully: sum_radii = 2.250029563182612\nResults saved to /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmpparf99ii.py.results\n\n```\n\n### stderr\n```\n\n```\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 3\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.2565, target_ratio: 0.8564, combined_score: 0.8564, eval_time: 1.3158\n- Outcome: Mixed results\n\n### Attempt 2\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.3467, target_ratio: 0.8906, combined_score: 0.8906, eval_time: 0.4550\n- Outcome: Improvement in all metrics\n\n### Attempt 1\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.3751, target_ratio: 0.9014, combined_score: 0.9014, eval_time: 0.3167\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.9014)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor for 26-circle packing: layered hex-like layout + Gauss-Seidel radius maximizer.\nPresents a deterministic explicit placement of centers (no global optimizer), but scans a\nsmall parameter grid (margin, vertical scale, stagger) to mitigate edge effects and picks\nthe best total radius produced by a Gauss-Seidel-style radii maximizer.\n\"\"\"\nimport numpy as np\nfrom math import sqrt\n\ndef construct_packing():\n    n = 26\n    cols_per_row = [5, 6, 5, 5, 5]  # 26\n    rows = len(cols_per_row)\n    sqrt3 = sqrt(3.0)\n\n    def make_centers(margin, vscale, stag):\n        # available horizontal length between margins\n        W = 1.0 - 2.0 * margin\n        if W <= 0: \n            return None\n        # pick a reference horizontal step using the widest row (to get near-hex)\n        maxcnt = max(cols_per_row)\n        if maxcnt > 1:\n            step = W / (maxcnt - 1)\n        else:\n            step = W\n        h = step * sqrt3 / 2.0 * vscale\n        total_h = h * (rows - 1)\n        y0 = 0.5 - total_h / 2.0\n        centers = []\n        for r, cnt in enumerate(cols_per_row):\n            # center the row horizontally\n            row_width = (cnt - 1) * step if cnt > 1 else 0.0\n            x0 = 0.5 - row_width / 2.0\n            xs = x0 + np.arange(cnt) * step\n            # apply stagger on odd rows\n            if r % 2 == 1:\n                xs = xs + stag * step\n            # clip into [margin,1-margin]\n            xs = np.clip(xs, margin, 1.0 - margin)\n            y = y0 + r * h\n            for x in xs:\n                centers.append((x, y))\n        C = np.array(centers, dtype=float)\n        if C.shape[0] != n:\n            return None\n        # reject if rows exceed vertical bounds\n        if C[:,1].min() < -1e-12 or C[:,1].max() > 1.0 + 1e-12:\n            return None\n        return C\n\n    def maximize_radii(C, tol=1e-10, max_iter=2000):\n        nC = C.shape[0]\n        border = np.minimum.reduce([C[:,0], C[:,1], 1.0 - C[:,0], 1.0 - C[:,1]]).astype(float)\n        # pairwise distances\n        diff = C[:, None, :] - C[None, :, :]\n        d = np.sqrt((diff**2).sum(axis=2))\n        np.fill_diagonal(d, np.inf)\n        r = border.copy()  # init\n        for it in range(max_iter):\n            prev = r.copy()\n            # Gauss-Seidel style: tighten each radius to its feasible upper bound\n            for i in range(nC):\n                ub = border[i]\n                # d_ij - r_j for all j\n                vals = d[i] - r\n                vals[i] = np.inf\n                ub = min(ub, float(np.min(vals)))\n                r[i] = max(0.0, ub)\n            if np.max(np.abs(r - prev)) < tol:\n                break\n        # final safety: correct tiny pairwise overlaps by proportional scaling per violating pair\n        for i in range(nC):\n            for j in range(i+1, nC):\n                dij = d[i,j]\n                if not np.isfinite(dij) or dij <= 1e-12:\n                    continue\n                if r[i] + r[j] > dij:\n                    s = dij / (r[i] + r[j])\n                    r[i] *= s\n                    r[j] *= s\n        # clamp to border again\n        r = np.minimum(r, border)\n        r = np.maximum(r, 0.0)\n        return r\n\n    best_sum = -1.0\n    best_C = None\n    best_r = None\n\n    # parameter scan ranges (kept small and deterministic)\n    for margin in np.linspace(0.02, 0.12, 19):\n        for vscale in np.linspace(0.92, 1.08, 5):\n            for stag in np.linspace(-0.25, 0.35, 7):\n                C = make_centers(margin, vscale, stag)\n                if C is None:\n                    continue\n                r = maximize_radii(C)\n                s = float(r.sum())\n                if s > best_sum:\n                    best_sum = s\n                    best_C, best_r = C, r\n\n    # fallback if nothing found (shouldn't happen)\n    if best_C is None:\n        best_C = make_centers(0.06, 1.0, 0.0)\n        best_r = maximize_radii(best_C)\n\n    return best_C, best_r, float(best_r.sum())\n\n# EVOLVE-BLOCK-END\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.3751), Performs well on target_ratio (0.9014), Performs well on combined_score (0.9014), Performs well on eval_time (0.3167)\n\n### Program 2 (Score: 0.8906)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for 26 circles in the unit square.\nStrategy:\n- Place 26 centers in a near-hex truncated block (rows [5,6,5,5,5]).\n- Sweep a small parameter grid (margin, vertical scale, stagger) to mitigate edge effects.\n- For each center set compute maximal radii by a Gauss\u2013Seidel style solver that enforces\n  r_i <= dist_to_border_i and r_i <= d_ij - r_j for all j!=i.\n- Return the best configuration by total radius sum.\n\"\"\"\nimport numpy as np\nfrom math import sqrt\n\ndef construct_packing():\n    n = 26\n    cols_per_row = [5, 6, 5, 5, 5]  # sums to 26\n    rows = len(cols_per_row)\n    sqrt3 = sqrt(3.0)\n\n    def make_centers(margin, vscale, stag):\n        W = 1.0 - 2.0 * margin\n        if W <= 0:\n            return None\n        maxcnt = max(cols_per_row)\n        step = W / (maxcnt - 1) if maxcnt > 1 else W\n        h = step * sqrt3 / 2.0 * vscale\n        total_h = h * (rows - 1)\n        y0 = 0.5 - total_h / 2.0\n        centers = []\n        for r, cnt in enumerate(cols_per_row):\n            row_width = (cnt - 1) * step if cnt > 1 else 0.0\n            x0 = 0.5 - row_width / 2.0\n            xs = x0 + np.arange(cnt) * step\n            if r % 2 == 1:\n                xs = xs + stag * step\n            # ensure within margins by clamping (small stag may push out)\n            xs = np.clip(xs, margin, 1.0 - margin)\n            y = y0 + r * h\n            for x in xs:\n                centers.append((x, y))\n        C = np.array(centers, dtype=float)\n        if C.shape[0] != n:\n            return None\n        if (C[:,1].min() < -1e-12) or (C[:,1].max() > 1.0 + 1e-12):\n            return None\n        return C\n\n    def maximize_radii(C, tol=1e-10, max_iter=2000):\n        nC = C.shape[0]\n        border = np.minimum.reduce([C[:,0], C[:,1], 1.0 - C[:,0], 1.0 - C[:,1]]).astype(float)\n        diff = C[:, None, :] - C[None, :, :]\n        d = np.sqrt((diff**2).sum(axis=2))\n        np.fill_diagonal(d, np.inf)\n        r = border.copy()\n        for it in range(max_iter):\n            prev = r.copy()\n            for i in range(nC):\n                ub = border[i]\n                vals = d[i] - r\n                vals[i] = np.inf\n                # numeric protection if any val is -inf/NaN\n                m = float(np.min(vals))\n                if np.isfinite(m):\n                    if m < ub:\n                        ub = m\n                r[i] = max(0.0, ub)\n            if np.max(np.abs(r - prev)) < tol:\n                break\n        # Final pass: fix tiny overlaps by proportional scaling per violating pair\n        for i in range(nC):\n            for j in range(i+1, nC):\n                dij = d[i,j]\n                if not np.isfinite(dij) or dij <= 1e-12:\n                    # coincident or numeric trouble -> zero both\n                    r[i] = r[j] = 0.0\n                    continue\n                if r[i] + r[j] > dij:\n                    s = dij / (r[i] + r[j])\n                    r[i] *= s\n                    r[j] *= s\n        r = np.minimum(r, border)\n        r = np.maximum(r, 0.0)\n        return r\n\n    best_sum = -1.0\n    best_C = None\n    best_r = None\n\n    margins = np.linspace(0.02, 0.12, 19)\n    vscales = np.linspace(0.94, 1.06, 7)\n    stags = np.linspace(-0.25, 0.35, 7)\n\n    for margin in margins:\n        for vscale in vscales:\n            for stag in stags:\n                C = make_centers(margin, vscale, stag)\n                if C is None:\n                    continue\n                r = maximize_radii(C)\n                s = float(r.sum())\n                if s > best_sum + 1e-12:\n                    best_sum = s\n                    best_C = C.copy()\n                    best_r = r.copy()\n\n    if best_C is None:\n        best_C = make_centers(0.06, 1.0, 0.0)\n        best_r = maximize_radii(best_C)\n\n    return best_C, best_r, float(best_r.sum())\n# EVOLVE-BLOCK-END\n\n# fixed interface\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(7,7))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect('equal')\n    ax.add_patch(plt.Rectangle((0,0),1,1,fill=False,linewidth=1))\n    for i,(c,r) in enumerate(zip(centers, radii)):\n        ax.add_patch(Circle(c, r, alpha=0.6, edgecolor='k'))\n        ax.text(c[0], c[1], str(i), ha='center', va='center', fontsize=8)\n    plt.title(f\"n={len(centers)} sum={sum(radii):.6f}\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # visualize(centers, radii)\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.3467), Performs well on target_ratio (0.8906), Performs well on combined_score (0.8906), Performs well on eval_time (0.4550)\n\n### Program 3 (Score: 0.8564)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for 26 circles in the unit square.\nUses a truncated hex-like layered skeleton (rows sum to 26) and a small\ngrid search over (margin, vertical scale, stagger, vertical shift).\nFor each center set we maximize radii via a Gauss-Seidel-style relaxation\n(r_i <= dist_to_border and r_i + r_j <= d_ij). Returns centers, radii, sum.\n\"\"\"\nimport numpy as np\nfrom math import sqrt\n\ndef construct_packing():\n    n = 26\n    cols_per_row = [5, 6, 5, 5, 5]  # sums to 26\n    rows = len(cols_per_row)\n    sqrt3 = sqrt(3.0)\n\n    def make_centers(margin, vscale, stag, ty):\n        W = 1.0 - 2.0 * margin\n        if W <= 0: \n            return None\n        maxcnt = max(cols_per_row)\n        step = W / (maxcnt - 1) if maxcnt > 1 else W\n        h = step * sqrt3 / 2.0 * vscale\n        total_h = h * (rows - 1)\n        y0 = 0.5 - total_h / 2.0 + ty\n        centers = []\n        for r, cnt in enumerate(cols_per_row):\n            row_width = (cnt - 1) * step if cnt > 1 else 0.0\n            x0 = 0.5 - row_width / 2.0\n            xs = x0 + np.arange(cnt) * step\n            if r % 2 == 1:\n                xs = xs + stag * step\n            # quick reject if outside allowed band\n            if xs.min() < margin - 1e-12 or xs.max() > 1.0 - margin + 1e-12:\n                # clip but still accept; clip improves feasibility\n                xs = np.clip(xs, margin, 1.0 - margin)\n            y = y0 + r * h\n            centers.extend([(float(x), float(y)) for x in xs])\n        C = np.array(centers, dtype=float)\n        if C.shape[0] != n:\n            return None\n        # reject if vertical positions go way outside [0,1]\n        if C[:,1].min() < -1e-12 or C[:,1].max() > 1.0 + 1e-12:\n            return None\n        return C\n\n    def maximize_radii(C, tol=1e-11, max_iter=3000):\n        nC = C.shape[0]\n        border = np.minimum.reduce([C[:,0], C[:,1], 1.0 - C[:,0], 1.0 - C[:,1]]).astype(float)\n        diff = C[:, None, :] - C[None, :, :]\n        d = np.sqrt((diff**2).sum(axis=2))\n        np.fill_diagonal(d, np.inf)\n        r = border.copy()\n        for it in range(max_iter):\n            prev = r.copy()\n            for i in range(nC):\n                ub = border[i]\n                vals = d[i] - r\n                vals[i] = np.inf\n                m = float(np.min(vals))\n                if m < ub:\n                    ub = m\n                r[i] = max(0.0, ub)\n            if np.max(np.abs(r - prev)) < tol:\n                break\n        # fix small violations by proportional scaling per overlapping pair\n        for i in range(nC):\n            for j in range(i+1, nC):\n                dij = d[i,j]\n                if not np.isfinite(dij) or dij <= 1e-12:\n                    # coincident or nearly so\n                    if r[i] + r[j] > 0:\n                        r[i] = r[j] = 0.0\n                    continue\n                if r[i] + r[j] > dij:\n                    s = dij / (r[i] + r[j])\n                    r[i] *= s\n                    r[j] *= s\n        r = np.minimum(r, border)\n        r = np.maximum(r, 0.0)\n        return r\n\n    best_sum = -1.0\n    best_C = None\n    best_r = None\n\n    # focused deterministic grid tuned for good layouts\n    margins = np.linspace(0.03, 0.10, 12)\n    vscales = np.linspace(0.96, 1.06, 6)\n    stags = np.linspace(-0.28, 0.36, 9)\n    tys = np.linspace(-0.03, 0.03, 5)\n\n    for margin in margins:\n        for v in vscales:\n            for s in stags:\n                for ty in tys:\n                    C = make_centers(margin, v, s, ty)\n                    if C is None:\n                        continue\n                    r = maximize_radii(C)\n                    ssum = float(r.sum())\n                    if ssum > best_sum + 1e-12:\n                        best_sum = ssum\n                        best_C = C.copy()\n                        best_r = r.copy()\n\n    if best_C is None:\n        # fallback conservative layout\n        best_C = make_centers(0.06, 1.0, 0.0, 0.0)\n        best_r = maximize_radii(best_C)\n\n    return best_C, best_r, float(best_r.sum())\n\n# EVOLVE-BLOCK-END\n\n# fixed interface\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\nif __name__ == \"__main__\":\n    C, r, s = run_packing()\n    print(f\"Sum of radii: {s:.6f}\")\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.2565), Performs well on target_ratio (0.8564), Performs well on combined_score (0.8564), Performs well on eval_time (1.3158)\n\n\n\n## Diverse Programs\n\n### Program D1 (Score: 0.8558)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for 26 circles in the unit square.\nLayout: a compact near-hex block with rows [5,6,5,5,5]. For a range of edge\nmargins we build centers and maximize radii for fixed centers via a\nGauss-Seidel style solver that enforces r_i <= border_i and r_i <= d_ij - r_j.\nThe best configuration (largest sum of radii) is returned.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    cols_per_row = [5, 6, 5, 5, 5]  # sums to 26\n    rows = len(cols_per_row)\n\n    def make_centers(margin):\n        available_w = 1.0 - 2.0 * margin\n        if available_w <= 0:\n            return None\n        # pick an average horizontal step consistent with rows\n        steps = []\n        for cnt in cols_per_row:\n            if cnt > 1:\n                steps.append(available_w / (cnt - 1))\n        avg_step = float(np.mean(steps)) if steps else 0.5 * available_w\n        h = avg_step * np.sqrt(3.0) / 2.0  # hex vertical spacing\n        total_h = h * (rows - 1)\n        y0 = 0.5 - total_h / 2.0\n        centers = []\n        for r, cnt in enumerate(cols_per_row):\n            if cnt == 1:\n                xs = np.array([0.5])\n            else:\n                xs = margin + np.linspace(0.0, available_w, cnt)\n            # stagger odd rows\n            if (r % 2 == 1) and cnt > 1:\n                step = xs[1] - xs[0]\n                xs = xs + 0.5 * step\n            xs = np.clip(xs, margin, 1.0 - margin)\n            y = y0 + r * h\n            for x in xs:\n                centers.append((x, y))\n        C = np.array(centers, dtype=float)\n        if C.shape[0] != n:\n            return None\n        if C.min() < -1e-12 or C.max() > 1.0 + 1e-12:\n            return None\n        return C\n\n    def maximize_radii(C, tol=1e-10, max_iter=2000):\n        n = C.shape[0]\n        border = np.minimum.reduce([C[:,0], C[:,1], 1.0 - C[:,0], 1.0 - C[:,1]]).astype(float)\n        diff = C[:, None, :] - C[None, :, :]\n        dists = np.sqrt((diff**2).sum(axis=2))\n        np.fill_diagonal(dists, np.inf)\n        # start with border-limited radii\n        r = border.copy()\n        for it in range(max_iter):\n            prev = r.copy()\n            # Gauss-Seidel pass\n            for i in range(n):\n                ub = border[i]\n                # compute d_ij - r_j for all j != i\n                vals = dists[i] - r\n                vals[i] = np.inf\n                m = float(np.min(vals))\n                if m < ub:\n                    ub = m\n                r[i] = max(0.0, ub)\n            if np.max(np.abs(r - prev)) < tol:\n                break\n        # safety: fix any tiny overlaps by proportional scaling pairwise\n        for i in range(n):\n            for j in range(i+1, n):\n                d = dists[i,j]\n                if d <= 1e-12:\n                    r[i] = r[j] = 0.0\n                elif r[i] + r[j] > d:\n                    s = d / (r[i] + r[j])\n                    r[i] *= s\n                    r[j] *= s\n        r = np.clip(r, 0.0, border)\n        return r\n\n    best_sum = -1.0\n    best_C = None\n    best_r = None\n    # sample margins focused on small margins (to get larger interior radii but avoid clipping)\n    for margin in np.linspace(0.02, 0.12, 41):\n        C = make_centers(margin)\n        if C is None:\n            continue\n        r = maximize_radii(C)\n        s = float(r.sum())\n        if s > best_sum + 1e-12:\n            best_sum = s\n            best_C = C.copy()\n            best_r = r.copy()\n\n    if best_C is None:\n        # fallback conservative layout\n        margin = 0.07\n        best_C = make_centers(margin)\n        best_r = maximize_radii(best_C)\n\n    return best_C, best_r, float(np.sum(best_r))\n\n# EVOLVE-BLOCK-END\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(7, 7))\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    for i, (c, r) in enumerate(zip(centers, radii)):\n        ax.add_patch(Circle(c, r, alpha=0.6, edgecolor=\"k\"))\n        ax.text(c[0], c[1], str(i), ha=\"center\", va=\"center\", fontsize=7)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # visualize(centers, radii)\n```\nKey features: Alternative approach to validity, Alternative approach to sum_radii\n\n### Program D2 (Score: 0.8558)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for packing 26 circles in the unit square.\n\nStrategy:\n- Place 26 centers in a near-hex layered layout (rows = [5,6,5,5,5]).\n- For a range of small margins, build centers that span the available width\n  and stagger alternate rows by half-step to approximate hex packing.\n- For each center set compute maximal feasible radii using a Gauss-Seidel\n  style solver that enforces border and pairwise non-overlap constraints:\n    r_i <= dist_to_border_i,  r_i <= d_ij - r_j  (for all j)\n- Pick the margin giving the largest sum of radii. Final safety pass fixes\n  tiny remaining overlaps by proportional scaling.\nThis is an explicit constructor (no heavy optimization loops) but reliably\nproduces good sums of radii for n=26.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    cols_per_row = [5, 6, 5, 5, 5]  # sums to 26\n    rows = len(cols_per_row)\n    n = sum(cols_per_row)\n\n    def make_centers(margin):\n        avail_w = 1.0 - 2.0 * margin\n        if avail_w <= 0:\n            return None\n        centers = []\n        # compute an average step from row counts to make rows consistent\n        steps = []\n        for cnt in cols_per_row:\n            if cnt > 1:\n                steps.append(avail_w / (cnt - 1))\n        if len(steps) == 0:\n            step = avail_w * 0.5\n        else:\n            step = float(np.mean(steps))\n        h = step * np.sqrt(3.0) / 2.0\n        total_h = h * (rows - 1)\n        y0 = 0.5 - total_h / 2.0\n        for r, cnt in enumerate(cols_per_row):\n            if cnt == 1:\n                xs = np.array([0.5])\n            else:\n                xs = margin + np.linspace(0.0, avail_w, cnt)\n            # stagger odd rows by half-step\n            if (r % 2 == 1) and (len(xs) > 1):\n                xs = xs + 0.5 * (xs[1] - xs[0])\n            xs = np.clip(xs, margin, 1.0 - margin)\n            y = y0 + r * h\n            for x in xs:\n                centers.append((x, y))\n        C = np.array(centers, dtype=float)\n        if C.shape[0] != n:\n            return None\n        if (C.min() < -1e-12) or (C.max() > 1.0 + 1e-12):\n            return None\n        return C\n\n    def maximize_radii(C, tol=1e-10, max_iter=800):\n        nloc = C.shape[0]\n        border = np.minimum.reduce([C[:,0], C[:,1], 1.0 - C[:,0], 1.0 - C[:,1]]).astype(float)\n        diff = C[:, None, :] - C[None, :, :]\n        dists = np.sqrt((diff**2).sum(axis=2))\n        np.fill_diagonal(dists, np.inf)\n        # init radii at border limit\n        r = border.copy()\n        for it in range(max_iter):\n            prev = r.copy()\n            for i in range(nloc):\n                ub = border[i]\n                # ub_j = d_ij - r_j\n                vals = dists[i] - r\n                vals[i] = np.inf\n                m = np.min(vals)\n                if np.isfinite(m):\n                    ub = min(ub, float(m))\n                r[i] = max(0.0, ub)\n            if np.max(np.abs(r - prev)) < tol:\n                break\n        # final tiny-overlap fix: scale any violating pair proportionally\n        for i in range(nloc):\n            for j in range(i+1, nloc):\n                d = dists[i,j]\n                if d <= 1e-12:\n                    r[i] = r[j] = 0.0\n                elif r[i] + r[j] > d:\n                    s = d / (r[i] + r[j])\n                    r[i] *= s\n                    r[j] *= s\n        # ensure border\n        r = np.minimum(r, border)\n        r = np.maximum(r, 0.0)\n        return r\n\n    best_sum = -1.0\n    best_C = None\n    best_r = None\n    # sample margins (denser near small margins)\n    for margin in np.linspace(0.02, 0.12, 21):\n        C = make_centers(margin)\n        if C is None:\n            continue\n        r = maximize_radii(C)\n        s = float(r.sum())\n        if s > best_sum:\n            best_sum = s\n            best_C = C.copy()\n            best_r = r.copy()\n\n    # fallback (shouldn't happen)\n    if best_C is None:\n        best_C = make_centers(0.06)\n        best_r = maximize_radii(best_C)\n\n    return best_C, best_r, float(best_r.sum())\n# EVOLVE-BLOCK-END\n\n# fixed interface\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(7,7))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect(\"equal\")\n    for i,(c,r) in enumerate(zip(centers, radii)):\n        ax.add_patch(Circle(c, r, alpha=0.6, edgecolor='k'))\n        ax.text(c[0], c[1], str(i), ha='center', va='center', fontsize=7)\n    plt.title(f\"n={len(centers)} sum={sum(radii):.6f}\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, s = run_packing()\n    print(f\"Sum of radii: {s:.6f}\")\n    # visualize(centers, radii)\n```\nKey features: Alternative approach to validity, Alternative approach to sum_radii\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 0.9014, Type: High-Performer)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor for 26-circle packing: layered hex-like layout + Gauss-Seidel radius maximizer.\nPresents a deterministic explicit placement of centers (no global optimizer), but scans a\nsmall parameter grid (margin, vertical scale, stagger) to mitigate edge effects and picks\nthe best total radius produced by a Gauss-Seidel-style radii maximizer.\n\"\"\"\nimport numpy as np\nfrom math import sqrt\n\ndef construct_packing():\n    n = 26\n    cols_per_row = [5, 6, 5, 5, 5]  # 26\n    rows = len(cols_per_row)\n    sqrt3 = sqrt(3.0)\n\n    def make_centers(margin, vscale, stag):\n        # available horizontal length between margins\n        W = 1.0 - 2.0 * margin\n        if W <= 0: \n            return None\n        # pick a reference horizontal step using the widest row (to get near-hex)\n        maxcnt = max(cols_per_row)\n        if maxcnt > 1:\n            step = W / (maxcnt - 1)\n        else:\n            step = W\n        h = step * sqrt3 / 2.0 * vscale\n        total_h = h * (rows - 1)\n        y0 = 0.5 - total_h / 2.0\n        centers = []\n        for r, cnt in enumerate(cols_per_row):\n            # center the row horizontally\n            row_width = (cnt - 1) * step if cnt > 1 else 0.0\n            x0 = 0.5 - row_width / 2.0\n            xs = x0 + np.arange(cnt) * step\n            # apply stagger on odd rows\n            if r % 2 == 1:\n                xs = xs + stag * step\n            # clip into [margin,1-margin]\n            xs = np.clip(xs, margin, 1.0 - margin)\n            y = y0 + r * h\n            for x in xs:\n                centers.append((x, y))\n        C = np.array(centers, dtype=float)\n        if C.shape[0] != n:\n            return None\n        # reject if rows exceed vertical bounds\n        if C[:,1].min() < -1e-12 or C[:,1].max() > 1.0 + 1e-12:\n            return None\n        return C\n\n    def maximize_radii(C, tol=1e-10, max_iter=2000):\n        nC = C.shape[0]\n        border = np.minimum.reduce([C[:,0], C[:,1], 1.0 - C[:,0], 1.0 - C[:,1]]).astype(float)\n        # pairwise distances\n        diff = C[:, None, :] - C[None, :, :]\n        d = np.sqrt((diff**2).sum(axis=2))\n        np.fill_diagonal(d, np.inf)\n        r = border.copy()  # init\n        for it in range(max_iter):\n            prev = r.copy()\n            # Gauss-Seidel style: tighten each radius to its feasible upper bound\n            for i in range(nC):\n                ub = border[i]\n                # d_ij - r_j for all j\n                vals = d[i] - r\n                vals[i] = np.inf\n                ub = min(ub, float(np.min(vals)))\n                r[i] = max(0.0, ub)\n            if np.max(np.abs(r - prev)) < tol:\n                break\n        # final safety: correct tiny pairwise overlaps by proportional scaling per violating pair\n        for i in range(nC):\n            for j in range(i+1, nC):\n                dij = d[i,j]\n                if not np.isfinite(dij) or dij <= 1e-12:\n                    continue\n                if r[i] + r[j] > dij:\n                    s = dij / (r[i] + r[j])\n                    r[i] *= s\n                    r[j] *= s\n        # clamp to border again\n        r = np.minimum(r, border)\n        r = np.maximum(r, 0.0)\n        return r\n\n    best_sum = -1.0\n    best_C = None\n    best_r = None\n\n    # parameter scan ranges (kept small and deterministic)\n    for margin in np.linspace(0.02, 0.12, 19):\n        for vscale in np.linspace(0.92, 1.08, 5):\n            for stag in np.linspace(-0.25, 0.35, 7):\n                C = make_centers(margin, vscale, stag)\n                if C is None:\n                    continue\n                r = maximize_radii(C)\n                s = float(r.sum())\n                if s > best_sum:\n                    best_sum = s\n                    best_C, best_r = C, r\n\n    # fallback if nothing found (shouldn't happen)\n    if best_C is None:\n        best_C = make_centers(0.06, 1.0, 0.0)\n        best_r = maximize_radii(best_C)\n\n    return best_C, best_r, float(best_r.sum())\n\n# EVOLVE-BLOCK-END\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nUnique approach: Excellent validity (1.000), Excellent sum_radii (2.375), Excellent target_ratio (0.901)\n\n# Current Program\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for 26 circles in the unit square.\n\nStrategy:\n- Place 26 centers in a near-hex truncated block (rows = [5,6,5,5,5]).\n- Sweep a small range of outer margins to trade border room vs internal spacing.\n- For each center set compute maximal radii using a Gauss\u2013Seidel style solver:\n    r_i <= dist_to_border_i and r_i <= d_ij - r_j  for all j != i\n  This converges to a tight feasible radius vector for fixed centers.\n- Pick the configuration with largest sum of radii.\n\"\"\"\nimport numpy as np\nfrom math import sqrt\n\ndef construct_packing():\n    n = 26\n    cols_per_row = [5, 6, 5, 5, 5]  # sums to 26\n    rows = len(cols_per_row)\n\n    def make_centers(margin):\n        avail = 1.0 - 2.0 * margin\n        if avail <= 0:\n            return None\n        centers = []\n        # per-row horizontal step (if cnt>1)\n        steps = [avail / (cnt - 1) for cnt in cols_per_row if cnt > 1]\n        avg_step = float(np.mean(steps)) if steps else 0.5 * avail\n        h = avg_step * sqrt(3.0) / 2.0\n        total_h = h * (rows - 1)\n        y0 = 0.5 - total_h / 2.0\n        for r, cnt in enumerate(cols_per_row):\n            if cnt == 1:\n                xs = np.array([0.5])\n            else:\n                xs = margin + np.linspace(0.0, avail, cnt)\n            if (r % 2 == 1) and (cnt > 1):\n                step = xs[1] - xs[0]\n                xs = xs + 0.5 * step\n            xs = np.clip(xs, margin, 1.0 - margin)\n            y = y0 + r * h\n            for x in xs:\n                centers.append((x, y))\n        C = np.array(centers, dtype=float)\n        if C.shape[0] != n:  # sanity\n            return None\n        if (C.min() < -1e-12) or (C.max() > 1.0 + 1e-12):\n            return None\n        return C\n\n    def maximize_radii(C, tol=1e-10, max_iter=2000):\n        n = C.shape[0]\n        border = np.minimum.reduce([C[:,0], C[:,1], 1.0 - C[:,0], 1.0 - C[:,1]]).astype(float)\n        diff = C[:, None, :] - C[None, :, :]\n        dists = np.sqrt((diff**2).sum(axis=2))\n        np.fill_diagonal(dists, np.inf)\n\n        # initialize radii at border limits\n        r = border.copy()\n        for it in range(max_iter):\n            prev = r.copy()\n            # Gauss-Seidel update: set each r_i to min(border_i, min_j d_ij - r_j)\n            for i in range(n):\n                ub = border[i]\n                vals = dists[i] - r\n                vals[i] = np.inf\n                m = float(np.min(vals))\n                if m < ub:\n                    ub = m\n                r[i] = max(0.0, ub)\n            if np.max(np.abs(r - prev)) < tol:\n                break\n\n        # Final safety: remove any residual overlaps by proportional scaling per pair\n        for i in range(n):\n            for j in range(i+1, n):\n                d = dists[i, j]\n                if d <= 1e-12:\n                    r[i] = r[j] = 0.0\n                else:\n                    s = (d) / (r[i] + r[j]) if (r[i] + r[j]) > d else 1.0\n                    if s < 1.0:\n                        r[i] *= s\n                        r[j] *= s\n        # Clamp to borders and non-negativity\n        r = np.clip(r, 0.0, border)\n        return r\n\n    best_sum = -1.0\n    best_C = None\n    best_r = None\n    # sample margins (denser near small margins)\n    for margin in np.linspace(0.01, 0.12, 24):\n        C = make_centers(margin)\n        if C is None:\n            continue\n        r = maximize_radii(C)\n        s = float(r.sum())\n        if s > best_sum + 1e-12:\n            best_sum = s\n            best_C = C.copy()\n            best_r = r.copy()\n\n    # fallback if none found\n    if best_C is None:\n        best_C = make_centers(0.06)\n        best_r = maximize_radii(best_C)\n\n    return best_C, best_r, float(np.sum(best_r))\n\n# EVOLVE-BLOCK-END\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\n\n# Task\nRewrite the program to improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: complexity, diversity\nDifferent solutions with similar fitness but different features are valuable.\nProvide the complete new program code.\n\nIMPORTANT: Make sure your rewritten program maintains the same inputs and outputs\nas the original program, but with improved internal implementation.\n\n```python\n# Your rewritten program here\n```", "responses": ["```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for 26 circles in the unit square.\nNear-hex layered skeleton [5,6,5,5,5] + small parameter scan (margin, vscale, stagger).\nFor each center set we maximize radii via a Gauss-Seidel relaxation enforcing\n r_i <= dist_to_border_i and r_i + r_j <= d_ij. Return best by total radius sum.\n\"\"\"\nimport numpy as np\nfrom math import sqrt\n\ndef construct_packing():\n    n = 26\n    cols_per_row = [5,6,5,5,5]\n    rows = len(cols_per_row)\n    sqrt3 = sqrt(3.0)\n\n    def make_centers(margin, vscale, stag):\n        W = 1.0 - 2.0*margin\n        if W <= 0: return None\n        maxcnt = max(cols_per_row)\n        step = W/(maxcnt-1) if maxcnt>1 else W\n        h = step * sqrt3/2.0 * vscale\n        total_h = h*(rows-1)\n        y0 = 0.5 - total_h/2.0\n        centers = []\n        for r,cnt in enumerate(cols_per_row):\n            row_width = (cnt-1)*step if cnt>1 else 0.0\n            x0 = 0.5 - row_width/2.0\n            xs = x0 + np.arange(cnt)*step\n            if r%2==1:\n                xs = xs + stag*step\n            xs = np.clip(xs, margin, 1.0-margin)\n            y = y0 + r*h\n            for x in xs:\n                centers.append((float(x), float(y)))\n        C = np.array(centers,dtype=float)\n        if C.shape[0]!=n: return None\n        if C[:,1].min() < -1e-12 or C[:,1].max() > 1.0+1e-12: return None\n        return C\n\n    def maximize_radii(C, tol=1e-10, max_iter=2000):\n        m = C.shape[0]\n        border = np.minimum.reduce([C[:,0], C[:,1], 1.0-C[:,0], 1.0-C[:,1]]).astype(float)\n        diff = C[:,None,:] - C[None,:,:]\n        d = np.sqrt((diff**2).sum(axis=2))\n        np.fill_diagonal(d, np.inf)\n        r = border.copy()\n        for _ in range(max_iter):\n            prev = r.copy()\n            for i in range(m):\n                ub = border[i]\n                vals = d[i] - r\n                vals[i] = np.inf\n                mi = float(np.min(vals))\n                if mi < ub: ub = mi\n                r[i] = max(0.0, ub)\n            if np.max(np.abs(r-prev)) < tol: break\n        # fix tiny overlaps pairwise\n        for i in range(m):\n            for j in range(i+1,m):\n                dij = d[i,j]\n                if not np.isfinite(dij) or dij<=1e-12:\n                    r[i]=r[j]=0.0\n                    continue\n                if r[i]+r[j] > dij:\n                    s = dij/(r[i]+r[j])\n                    r[i]*=s; r[j]*=s\n        r = np.minimum(r, border)\n        return r\n\n    best_sum = -1.0\n    best_C = None; best_r = None\n    for margin in np.linspace(0.02,0.12,19):\n        for vscale in np.linspace(0.92,1.08,5):\n            for stag in np.linspace(-0.25,0.35,7):\n                C = make_centers(margin,vscale,stag)\n                if C is None: continue\n                r = maximize_radii(C)\n                s = float(r.sum())\n                if s > best_sum:\n                    best_sum = s; best_C = C.copy(); best_r = r.copy()\n    if best_C is None:\n        best_C = make_centers(0.06,1.0,0.0)\n        best_r = maximize_radii(best_C)\n    return best_C, best_r, float(best_r.sum())\n# EVOLVE-BLOCK-END\n\n# fixed interface\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\nif __name__ == \"__main__\":\n    C, r, s = run_packing()\n    print(f\"Sum of radii: {s:.6f}\")\n```"]}}, "artifacts_json": "{\"stdout\": \"Running in subprocess, Python version: 3.12.1 (v3.12.1:2305ca5144, Dec  7 2023, 17:23:38) [Clang 13.0.0 (clang-1300.0.29.30)]\\nProgram path: /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmpkjwi24i3.py\\nCalling run_packing()...\\nrun_packing() returned successfully: sum_radii = 2.375066959084224\\nResults saved to /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmph8xrec7c.py.results\\n\", \"stderr\": \"\"}", "artifact_dir": null}