{"id": "d4ea3cab-4401-4bec-a6a7-cb5ef886b863", "code": "# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for packing 26 circles in the unit square.\n\nStrategy:\n - Generate hexagonal-lattice candidate centers for a range of spacings.\n - For each spacing that produces >=26 candidates, pick the 26 centers closest to the square center.\n - Compute feasible radii as min(distance-to-border, 0.5 * nearest-neighbor-distance).\n - Choose the spacing that yields the largest sum of radii.\nThis explicit constructor is deterministic and favors a compact, near-hex arrangement.\n\"\"\"\nimport numpy as np\n\ndef _hex_lattice(spacing):\n    dx = spacing\n    dy = spacing * np.sqrt(3) / 2.0\n    margin = spacing / 2.0\n    pts = []\n    y = margin\n    row = 0\n    # generate points inside [0,1]^2 with margin\n    while y <= 1.0 - margin + 1e-12:\n        x0 = margin + (0.5 * dx if (row % 2 == 1) else 0.0)\n        x = x0\n        while x <= 1.0 - margin + 1e-12:\n            pts.append((x, y))\n            x += dx\n        row += 1\n        y += dy\n    return np.array(pts)\n\ndef _compute_radii(centers):\n    if centers.size == 0:\n        return np.array([])\n    # distance to border\n    dist_border = np.minimum.reduce([centers[:,0], centers[:,1], 1-centers[:,0], 1-centers[:,1]])\n    # pairwise distances\n    diff = centers[:, None, :] - centers[None, :, :]\n    dists = np.sqrt(np.sum(diff**2, axis=2))\n    # mask diagonal\n    np.fill_diagonal(dists, np.inf)\n    nearest = np.min(dists, axis=1)\n    radii = np.minimum(dist_border, 0.5 * nearest)\n    # numeric safety\n    radii = np.maximum(radii, 1e-12)\n    return radii\n\ndef construct_packing():\n    target_n = 26\n    center_point = np.array([0.5, 0.5])\n\n    # Try a dense set of spacings to trade count vs size\n    spacings = np.linspace(0.055, 0.20, 48)\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    for a in spacings:\n        pts = _hex_lattice(a)\n        if pts.shape[0] < target_n:\n            continue\n        # choose the 26 closest to the center (favors compact cluster)\n        idx = np.argsort(np.linalg.norm(pts - center_point, axis=1))[:target_n]\n        centers = pts[idx]\n        radii = _compute_radii(centers)\n        s = float(np.sum(radii))\n        if s > best_sum:\n            best_sum = s\n            best_centers = centers.copy()\n            best_radii = radii.copy()\n\n    # fallback: ensure we return something\n    if best_centers is None:\n        pts = _hex_lattice(0.09)\n        idx = np.argsort(np.linalg.norm(pts - center_point, axis=1))[:target_n]\n        best_centers = pts[idx]\n        best_radii = _compute_radii(best_centers)\n        best_sum = float(np.sum(best_radii))\n\n    # clamp tiny numeric issues\n    eps = 1e-12\n    best_centers = np.clip(best_centers, eps, 1.0 - eps)\n    best_radii = np.maximum(best_radii, 0.0)\n    return best_centers, best_radii, float(np.sum(best_radii))\n\n# EVOLVE-BLOCK-END\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor='k', linewidth=0.5)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)", "language": "python", "parent_id": "3f8e5858-f3ce-4911-b1f5-a5454550ea52", "generation": 2, "timestamp": 1759956783.418101, "iteration_found": 19, "metrics": {"validity": 1.0, "sum_radii": 2.399468085106382, "target_ratio": 0.9106140740441678, "combined_score": 0.9106140740441678, "eval_time": 0.060865163803100586}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Full rewrite", "parent_metrics": {"validity": 1.0, "sum_radii": 2.1776957993813895, "target_ratio": 0.8264500187405653, "combined_score": 0.8264500187405653, "eval_time": 0.07315707206726074}, "island": 0}, "prompts": {"full_rewrite_user": {"system": "You are an expert mathematician specializing in circle packing problems and computational geometry. Your task is to improve a constructor function that directly produces a specific arrangement of 26 circles in a unit square, maximizing the sum of their radii. The AlphaEvolve paper achieved a sum of 2.635 for n=26.\n\nKey geometric insights:\n- Circle packings often follow hexagonal patterns in the densest regions\n- Maximum density for infinite circle packing is pi/(2*sqrt(3)) \u2248 0.9069\n- Edge effects make square container packing harder than infinite packing\n- Circles can be placed in layers or shells when confined to a square\n- Similar radius circles often form regular patterns, while varied radii allow better space utilization\n- Perfect symmetry may not yield the optimal packing due to edge effects\n\nFocus on designing an explicit constructor that places each circle in a specific position, rather than an iterative search algorithm.\n", "user": "# Current Program Information\n- Fitness: 0.8265\n- Feature coordinates: No feature coordinates\n- Focus areas: - Fitness declined: 0.8880 \u2192 0.8265. Consider revising recent changes.\n- Consider simplifying - code length exceeds 500 characters\n\n## Last Execution Output\n\n### stdout\n```\nRunning in subprocess, Python version: 3.12.1 (v3.12.1:2305ca5144, Dec  7 2023, 17:23:38) [Clang 13.0.0 (clang-1300.0.29.30)]\nProgram path: /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmp7c6ec21s.py\nCalling run_packing()...\nrun_packing() returned successfully: sum_radii = 2.1776957993813895\nResults saved to /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmp6l4nqf70.py.results\n\n```\n\n### stderr\n```\n\n```\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 3\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.3400, target_ratio: 0.8880, combined_score: 0.8880, eval_time: 0.0610\n- Outcome: Mixed results\n\n### Attempt 2\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.3400, target_ratio: 0.8880, combined_score: 0.8880, eval_time: 0.3141\n- Outcome: Mixed results\n\n### Attempt 1\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.4117, target_ratio: 0.9153, combined_score: 0.9153, eval_time: 0.0584\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.9153)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for packing 26 circles in the unit square.\n\nStrategy:\n - Generate several hexagonal-lattice candidate center sets with different spacings.\n - For each spacing that produces >=26 candidates, pick the 26 centers closest to the square center.\n - Compute feasible radii as the minimum of (distance-to-border) and (half nearest-neighbor distance).\n - Choose the spacing that yields the largest sum of radii.\nThis yields a compact, near-hexagonal center cluster that improves total radii.\n\"\"\"\nimport numpy as np\n\ndef _hex_lattice(spacing):\n    dx = spacing\n    dy = spacing * np.sqrt(3) / 2.0\n    margin = spacing / 2.0\n    pts = []\n    y = margin\n    row = 0\n    while y <= 1.0 - margin + 1e-12:\n        x0 = margin + (0.5 * dx if (row % 2 == 1) else 0.0)\n        x = x0\n        while x <= 1.0 - margin + 1e-12:\n            pts.append((x, y))\n            x += dx\n        row += 1\n        y += dy\n    return np.array(pts)\n\ndef _compute_radii(centers):\n    n = centers.shape[0]\n    # distance to border\n    dist_border = np.minimum.reduce([centers[:,0], centers[:,1], 1-centers[:,0], 1-centers[:,1]])\n    # pairwise distances (mask diagonal)\n    diff = centers[:, None, :] - centers[None, :, :]\n    dists = np.sqrt(np.sum(diff**2, axis=2))\n    np.fill_diagonal(dists, np.inf)\n    nearest = np.min(dists, axis=1)\n    radii = np.minimum(dist_border, 0.5 * nearest)\n    # safety\n    radii = np.maximum(radii, 1e-8)\n    return radii\n\ndef construct_packing():\n    target_n = 26\n    center_point = np.array([0.5, 0.5])\n\n    # Try a range of spacings; include fine and coarse to trade count vs size\n    spacings = np.linspace(0.06, 0.20, 30)  # finer resolution to find good spacing\n    best = None\n    best_sum = -1.0\n\n    for a in spacings:\n        pts = _hex_lattice(a)\n        if pts.shape[0] < target_n:\n            continue\n        # choose the 26 closest to center (favors larger feasible radii)\n        idx = np.argsort(np.linalg.norm(pts - center_point, axis=1))[:target_n]\n        centers = pts[idx]\n        radii = _compute_radii(centers)\n        s = float(np.sum(radii))\n        if s > best_sum:\n            best_sum = s\n            best = (centers.copy(), radii.copy(), s)\n\n    # Fallback: if none spacing produced enough points, use a dense lattice and pick best 26\n    if best is None:\n        pts = _hex_lattice(0.055)\n        idx = np.argsort(np.linalg.norm(pts - center_point, axis=1))[:target_n]\n        centers = pts[idx]\n        radii = _compute_radii(centers)\n        best = (centers, radii, float(np.sum(radii)))\n\n    return best\n\n# EVOLVE-BLOCK-END\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor='k', linewidth=0.5)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.4117), Performs well on target_ratio (0.9153), Performs well on combined_score (0.9153), Performs well on eval_time (0.0584)\n\n### Program 2 (Score: 0.8880)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Improved constructor for packing 26 circles in the unit square.\nStrategy:\n - Generate hexagonal-lattice candidate centers for a range of spacings.\n - For each candidate set, pick a moderate pool of central candidates.\n - Use a greedy selection to choose 26 centers that maximize the sum of feasible radii,\n   where radii = min(distance-to-border, 0.5 * nearest-neighbor-distance).\n - Keep the best arrangement across spacings.\nThis explicit constructor is deterministic and favors compact, near-hex arrangements\nwhile allowing edge effects to select slightly smaller circles near borders.\n\"\"\"\nimport numpy as np\nfrom math import sqrt\n\ndef _hex_grid(spacing):\n    dx = spacing\n    dy = spacing * sqrt(3) / 2.0\n    margin = spacing / 2.0\n    pts = []\n    row = 0\n    y = margin\n    while y <= 1.0 - margin + 1e-12:\n        x0 = margin + (0.5 * dx if (row % 2 == 1) else 0.0)\n        x = x0\n        while x <= 1.0 - margin + 1e-12:\n            pts.append((x, y))\n            x += dx\n        row += 1\n        y += dy\n    return np.array(pts)\n\ndef _radii_for_centers(centers):\n    # radii = min(dist_to_border, 0.5 * nearest_neighbor_distance)\n    if centers.shape[0] == 0:\n        return np.array([])\n    border = np.minimum.reduce([centers[:,0], centers[:,1], 1-centers[:,0], 1-centers[:,1]])\n    diff = centers[:, None, :] - centers[None, :, :]\n    dmat = np.sqrt((diff**2).sum(axis=2))\n    if dmat.shape[0] > 1:\n        np.fill_diagonal(dmat, np.inf)\n        nearest = np.min(dmat, axis=1)\n        radii = np.minimum(border, 0.5 * nearest)\n    else:\n        radii = border.copy()\n    radii = np.maximum(radii, 0.0)\n    return radii\n\ndef construct_packing():\n    target_n = 26\n    center_point = np.array([0.5, 0.5])\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    # Try a few spacings (coarse->fine). Include slightly denser and sparser grids.\n    for spacing in np.linspace(0.10, 0.18, 17):\n        pts = _hex_grid(spacing)\n        if pts.shape[0] < target_n:\n            continue\n\n        # Pick a manageable candidate pool: closest to center (prefer central packing)\n        d2 = np.sum((pts - center_point)**2, axis=1)\n        order = np.argsort(d2)\n        pool_size = min(80, pts.shape[0])  # limit pool size for speed\n        pool_idx = order[:pool_size]\n        pool = pts[pool_idx]\n\n        # Greedy selection: iteratively add the candidate that yields the largest total sum of radii\n        selected_idx = []\n        remaining = list(range(pool.shape[0]))\n        current_centers = np.empty((0,2))\n        for k in range(target_n):\n            best_local_gain = -1.0\n            best_choice = None\n            # Try each remaining candidate\n            for j in remaining:\n                trial_centers = np.vstack([current_centers, pool[j:j+1]])\n                trial_radii = _radii_for_centers(trial_centers)\n                trial_sum = float(np.sum(trial_radii))\n                if trial_sum > best_local_gain:\n                    best_local_gain = trial_sum\n                    best_choice = j\n            if best_choice is None:\n                break\n            # commit the best choice\n            current_centers = np.vstack([current_centers, pool[best_choice:best_choice+1]])\n            remaining.remove(best_choice)\n\n        if current_centers.shape[0] != target_n:\n            continue\n\n        current_radii = _radii_for_centers(current_centers)\n        current_sum = float(np.sum(current_radii))\n        if current_sum > best_sum:\n            best_sum = current_sum\n            best_centers = current_centers.copy()\n            best_radii = current_radii.copy()\n\n    # Fallback: if no spacing produced a result (very unlikely), use a moderate spacing\n    if best_centers is None:\n        pts = _hex_grid(0.12)\n        d2 = np.sum((pts - center_point)**2, axis=1)\n        idx = np.argsort(d2)[:target_n]\n        best_centers = pts[idx].copy()\n        best_radii = _radii_for_centers(best_centers)\n        best_sum = float(np.sum(best_radii))\n\n    # Clip tiny numerical issues\n    eps = 1e-12\n    best_centers = np.clip(best_centers, eps, 1.0 - eps)\n    best_radii = np.maximum(best_radii, 0.0)\n    return best_centers, best_radii, float(np.sum(best_radii))\n\n# EVOLVE-BLOCK-END\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor='k')\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.3400), Performs well on target_ratio (0.8880), Performs well on combined_score (0.8880), Performs well on eval_time (0.3141)\n\n### Program 3 (Score: 0.8880)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles using a hexagonal lattice selector.\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    \"\"\"\n    Construct a deterministic arrangement of 26 circles in the unit square.\n    Strategy:\n      - Generate a family of hexagonal-lattice candidate centers with varying spacing.\n      - Choose a spacing that yields >=26 candidate centers inside the square.\n      - Select the 26 centers closest to the square center (favoring larger feasible radii).\n      - Assign each selected circle radius = min(distance-to-border, 0.5 * nearest-neighbor-distance).\n    This explicit constructor yields a compact, nearly hexagonal packing in the center,\n    which typically increases the sum of radii vs. naive ring placements.\n    Returns:\n        centers: np.array (26,2)\n        radii: np.array (26,)\n        sum_radii: float\n    \"\"\"\n    target_n = 26\n    # Try spacings from coarse to fine until we get at least target_n candidates\n    spacings = np.linspace(0.18, 0.10, 17)  # try larger spacing first (fewer points), then refine\n    best_candidates = None\n    for a in spacings:\n        dx = a\n        dy = a * np.sqrt(3) / 2\n        margin = a / 2.0\n        pts = []\n        y = margin\n        row = 0\n        while y <= 1.0 - margin + 1e-12:\n            # offset alternate rows by half dx\n            x0 = margin + (0.5 * dx if (row % 2 == 1) else 0.0)\n            x = x0\n            while x <= 1.0 - margin + 1e-12:\n                pts.append((x, y))\n                x += dx\n            row += 1\n            y += dy\n        pts = np.array(pts)\n        if pts.shape[0] >= target_n:\n            best_candidates = pts\n            break\n    if best_candidates is None:\n        # fallback: very fine spacing to guarantee enough points\n        a = 0.095\n        dx = a; dy = a * np.sqrt(3) / 2; margin = a/2\n        pts = []\n        y = margin\n        row = 0\n        while y <= 1.0 - margin + 1e-12:\n            x0 = margin + (0.5 * dx if (row % 2 == 1) else 0.0)\n            x = x0\n            while x <= 1.0 - margin + 1e-12:\n                pts.append((x, y))\n                x += dx\n            row += 1\n            y += dy\n        best_candidates = np.array(pts)\n\n    # Select the target_n centers closest to the square center (0.5,0.5)\n    center_point = np.array([0.5, 0.5])\n    dists_to_center = np.linalg.norm(best_candidates - center_point, axis=1)\n    idx = np.argsort(dists_to_center)[:target_n]\n    centers = best_candidates[idx]\n\n    # Compute radii: distance to border and half nearest-neighbor distance\n    n = centers.shape[0]\n    # distance to borders\n    dist_border = np.minimum.reduce([centers[:,0], centers[:,1], 1-centers[:,0], 1-centers[:,1]])\n    # pairwise distances\n    diff = centers[:, np.newaxis, :] - centers[np.newaxis, :, :]  # (n,n,2)\n    pdist = np.sqrt(np.sum(diff**2, axis=2)) + np.eye(n) * 1e6  # mask self by large number\n    nearest = np.min(pdist, axis=1)\n    radii = np.minimum(dist_border, 0.5 * nearest)\n    # small safety floor\n    radii = np.maximum(radii, 1e-6)\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n# EVOLVE-BLOCK-END\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor='k', linewidth=0.5)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.3400), Performs well on target_ratio (0.8880), Performs well on combined_score (0.8880), Performs well on eval_time (0.0610)\n\n\n\n## Diverse Programs\n\n### Program D1 (Score: 0.8345)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (hex-like explicit constructor)\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct an explicit arrangement of 26 circles in the unit square.\n    Returns: centers (26,2), radii (26,), sum_of_radii\n    \"\"\"\n    n = 26\n\n    # We'll build a 5-row hex-like layout with one row having 6 centers and others 5:\n    row_count_options = []\n    for k in range(5):\n        # place the row with 6 centers at index k (0..4)\n        row_counts = [5] * 5\n        row_counts[k] = 6\n        row_count_options.append(tuple(row_counts))\n\n    best = None\n    # search over which row has 6 and over spacing s to maximize sum of radii\n    for row_counts in row_count_options:\n        # vertical spacing factor for hex lattice\n        for s in np.linspace(0.12, 0.20, 161):  # coarse but effective scan\n            v = s * np.sqrt(3) / 2.0\n            # compute y coordinates centered in the unit square\n            rows = len(row_counts)\n            total_height = (rows - 1) * v\n            y0 = 0.5 - total_height / 2.0\n            centers = []\n            valid = True\n            for i, cnt in enumerate(row_counts):\n                y = y0 + i * v\n                # for hex pattern, alternate rows are offset by s/2\n                if cnt == 6:\n                    x_start = 0.5 - (cnt - 1) * s / 2.0\n                else:\n                    x_start = 0.5 - (cnt - 1) * s / 2.0\n                xs = [x_start + j * s for j in range(cnt)]\n                for x in xs:\n                    # ensure inside square\n                    if not (0.0 <= x <= 1.0 and 0.0 <= y <= 1.0):\n                        valid = False\n                        break\n                    centers.append((x, y))\n                if not valid:\n                    break\n            if not valid or len(centers) != n:\n                continue\n            centers = np.array(centers)\n\n            # compute nearest neighbor distances\n            D = np.sqrt(((centers[:, None, :] - centers[None, :, :]) ** 2).sum(axis=2))\n            # set diagonal to large to ignore self-distance\n            np.fill_diagonal(D, np.inf)\n            nearest = D.min(axis=1)\n\n            # distance to borders\n            border = np.minimum.reduce([centers[:, 0], centers[:, 1], 1 - centers[:, 0], 1 - centers[:, 1]])\n\n            # radii capped by half nearest neighbor and by border\n            radii = np.minimum(border, 0.5 * nearest)\n\n            sum_r = radii.sum()\n\n            if (best is None) or (sum_r > best[0]):\n                best = (sum_r, centers.copy(), radii.copy(), s, row_counts)\n\n    if best is None:\n        # fallback: simple center and two rings as before but improved radii calc\n        centers = np.zeros((n, 2))\n        centers[0] = [0.5, 0.5]\n        for i in range(8):\n            angle = 2 * np.pi * i / 8\n            centers[i + 1] = [0.5 + 0.25 * np.cos(angle), 0.5 + 0.25 * np.sin(angle)]\n        for i in range(17):\n            angle = 2 * np.pi * i / 17\n            centers[i + 9] = [0.5 + 0.6 * np.cos(angle), 0.5 + 0.6 * np.sin(angle)]\n        radii = compute_max_radii(centers)\n        sum_r = radii.sum()\n        return centers, radii, sum_r\n\n    sum_r, centers, radii, s_used, row_counts_used = best\n    # final safety: enforce non-negativity and tiny eps\n    radii = np.maximum(radii, 1e-12)\n    return centers, radii, sum_r\n\n\ndef compute_max_radii(centers):\n    \"\"\"Helper: robust max radii (used only in fallback).\"\"\"\n    n = centers.shape[0]\n    border = np.minimum.reduce([centers[:, 0], centers[:, 1], 1 - centers[:, 0], 1 - centers[:, 1]])\n    D = np.sqrt(((centers[:, None, :] - centers[None, :, :]) ** 2).sum(axis=2))\n    np.fill_diagonal(D, np.inf)\n    nearest = D.min(axis=1)\n    radii = np.minimum(border, 0.5 * nearest)\n    radii = np.maximum(radii, 0.0)\n    return radii\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nKey features: Alternative approach to validity, Alternative approach to sum_radii\n\n### Program D2 (Score: 0.8287)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (explicit hex-like layout + iterative radius balancing)\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square\n    and compute near-maximal non-overlapping radii for the fixed centers.\n    Returns: (centers, radii, sum_of_radii)\n    \"\"\"\n    n = 26\n\n    # Row-wise hex-like arrangement with counts summing to 26\n    row_counts = [5, 6, 5, 5, 5]  # total 26\n    max_cols = max(row_counts)\n    margin = 0.02\n\n    # choose horizontal spacing so the widest row fits inside [margin,1-margin]\n    if max_cols > 1:\n        a = (1.0 - 2 * margin) / (max_cols - 1)\n    else:\n        a = 0.2\n    v = a * np.sqrt(3) / 2.0  # vertical spacing for hex packing\n\n    total_rows = len(row_counts)\n    height_used = (total_rows - 1) * v\n    y0 = margin + max(0.0, (1.0 - 2 * margin - height_used) / 2.0)\n\n    centers = []\n    for r, count in enumerate(row_counts):\n        y = y0 + r * v\n        # center each row horizontally\n        row_width = (count - 1) * a\n        x_start = 0.5 - row_width / 2.0\n        # offset every other row for hex-like staggering\n        if (r % 2) == 1:\n            x_start += a / 2.0\n        for c in range(count):\n            x = x_start + c * a\n            # clip to unit square with small tolerance\n            x = min(max(x, margin), 1 - margin)\n            y = min(max(y, margin), 1 - margin)\n            centers.append([x, y])\n\n    centers = np.array(centers[:n])\n    # Ensure we have exactly n centers\n    if centers.shape[0] < n:\n        # pad with center points if necessary (shouldn't happen)\n        while centers.shape[0] < n:\n            centers = np.vstack([centers, [0.5, 0.5]])\n    elif centers.shape[0] > n:\n        centers = centers[:n]\n\n    radii = compute_max_radii(centers)\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers, max_iter=2000, tol=1e-6):\n    \"\"\"\n    Iterative balancing: start from border-limited radii and iteratively\n    enforce pairwise non-overlap (shrink) and local growth (where possible).\n    \"\"\"\n    n = centers.shape[0]\n    # precompute pairwise distances\n    diff = centers[:, None, :] - centers[None, :, :]\n    dists = np.sqrt((diff ** 2).sum(axis=2)) + np.eye(n)  # add diag nonzero to avoid zero\n    # border distances\n    border = np.minimum.reduce([centers[:, 0], centers[:, 1],\n                                1 - centers[:, 0], 1 - centers[:, 1]])\n    border = np.maximum(border, 0.0)\n\n    # start with border distances (cannot exceed border)\n    r = border.copy()\n\n    for it in range(max_iter):\n        prev = r.copy()\n\n        # Pairwise shrink pass: ensure r_i + r_j <= dist_ij (with small safety)\n        for i in range(n):\n            for j in range(i + 1, n):\n                dij = dists[i, j]\n                # effective allowed sum (tiny safety margin)\n                allowed = max(0.0, dij - 1e-12)\n                sumr = r[i] + r[j]\n                if sumr > allowed and sumr > 0:\n                    scale = allowed / sumr\n                    # scale both radii proportionally but don't exceed borders\n                    r[i] *= scale\n                    r[j] *= scale\n\n        # Growth pass: for each circle, if there's slack given others fixed, try to grow (damped)\n        for i in range(n):\n            # max allowed by borders\n            allowed = border[i]\n            # max allowed by neighbors: r_i <= dist_ij - r_j for all j != i\n            # compute minimal (dist_ij - r_j)\n            neighbors_limit = np.min(dists[i, :] - r)\n            allowed = min(allowed, neighbors_limit)\n            allowed = max(allowed, 0.0)\n            if allowed > r[i] + 1e-12:\n                # damped increase to avoid oscillations\n                r[i] = 0.5 * (r[i] + allowed)\n\n        # enforce border safety and non-negativity\n        r = np.minimum(r, border)\n        r = np.maximum(r, 0.0)\n\n        if np.max(np.abs(r - prev)) < tol:\n            break\n\n    return r\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nKey features: Alternative approach to validity, Alternative approach to sum_radii\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 0.9153, Type: High-Performer)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for packing 26 circles in the unit square.\n\nStrategy:\n - Generate several hexagonal-lattice candidate center sets with different spacings.\n - For each spacing that produces >=26 candidates, pick the 26 centers closest to the square center.\n - Compute feasible radii as the minimum of (distance-to-border) and (half nearest-neighbor distance).\n - Choose the spacing that yields the largest sum of radii.\nThis yields a compact, near-hexagonal center cluster that improves total radii.\n\"\"\"\nimport numpy as np\n\ndef _hex_lattice(spacing):\n    dx = spacing\n    dy = spacing * np.sqrt(3) / 2.0\n    margin = spacing / 2.0\n    pts = []\n    y = margin\n    row = 0\n    while y <= 1.0 - margin + 1e-12:\n        x0 = margin + (0.5 * dx if (row % 2 == 1) else 0.0)\n        x = x0\n        while x <= 1.0 - margin + 1e-12:\n            pts.append((x, y))\n            x += dx\n        row += 1\n        y += dy\n    return np.array(pts)\n\ndef _compute_radii(centers):\n    n = centers.shape[0]\n    # distance to border\n    dist_border = np.minimum.reduce([centers[:,0], centers[:,1], 1-centers[:,0], 1-centers[:,1]])\n    # pairwise distances (mask diagonal)\n    diff = centers[:, None, :] - centers[None, :, :]\n    dists = np.sqrt(np.sum(diff**2, axis=2))\n    np.fill_diagonal(dists, np.inf)\n    nearest = np.min(dists, axis=1)\n    radii = np.minimum(dist_border, 0.5 * nearest)\n    # safety\n    radii = np.maximum(radii, 1e-8)\n    return radii\n\ndef construct_packing():\n    target_n = 26\n    center_point = np.array([0.5, 0.5])\n\n    # Try a range of spacings; include fine and coarse to trade count vs size\n    spacings = np.linspace(0.06, 0.20, 30)  # finer resolution to find good spacing\n    best = None\n    best_sum = -1.0\n\n    for a in spacings:\n        pts = _hex_lattice(a)\n        if pts.shape[0] < target_n:\n            continue\n        # choose the 26 closest to center (favors larger feasible radii)\n        idx = np.argsort(np.linalg.norm(pts - center_point, axis=1))[:target_n]\n        centers = pts[idx]\n        radii = _compute_radii(centers)\n        s = float(np.sum(radii))\n        if s > best_sum:\n            best_sum = s\n            best = (centers.copy(), radii.copy(), s)\n\n    # Fallback: if none spacing produced enough points, use a dense lattice and pick best 26\n    if best is None:\n        pts = _hex_lattice(0.055)\n        idx = np.argsort(np.linalg.norm(pts - center_point, axis=1))[:target_n]\n        centers = pts[idx]\n        radii = _compute_radii(centers)\n        best = (centers, radii, float(np.sum(radii)))\n\n    return best\n\n# EVOLVE-BLOCK-END\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor='k', linewidth=0.5)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nUnique approach: Modification: Full rewrite, Excellent validity (1.000), Excellent sum_radii (2.412)\n\n### Inspiration 2 (Score: 0.6320, Type: Alternative)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (hex-inspired explicit layout).\nReturns deterministic centers and radii (feasible, near-hex packing) with improved sum of radii.\n\"\"\"\nimport numpy as np\nfrom math import sqrt\n\ndef construct_packing():\n    n = 26\n    # Hex-like column counts (symmetric) producing 26 points: 5+5+6+5+5\n    col_counts = [5, 5, 6, 5, 5]\n    # Seed radius to get dense hex-like spacing but still fit in unit square\n    r_seed = 0.093  # tuned value (about 0.09-0.10 gives good aggregate)\n    hx = 2.0 * r_seed           # horizontal spacing between adjacent column centers\n    vy = sqrt(3.0) * r_seed    # vertical spacing between adjacent row centers\n\n    # x positions centered at 0.5\n    col_indices = np.arange(len(col_counts)) - (len(col_counts) - 1) / 2.0\n    xs = 0.5 + col_indices * hx\n\n    centers = []\n    for ci, (x, rows) in enumerate(zip(xs, col_counts)):\n        # Alternate columns in hex lattice are vertically offset by vy/2\n        col_offset = (ci % 2) * (vy / 2.0)\n        span = (rows - 1) * vy\n        start_y = 0.5 - span / 2.0 + col_offset\n        for r in range(rows):\n            y = start_y + r * vy\n            centers.append([x, y])\n\n    centers = np.array(centers[:n])  # ensure length 26\n\n    # Ensure all centers lie inside the unit square padding a tiny epsilon\n    eps = 1e-9\n    centers = np.clip(centers, eps, 1.0 - eps)\n\n    radii = compute_max_radii(centers)\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\ndef compute_max_radii(centers, max_iter=2000, tol=1e-10):\n    \"\"\"\n    Greedy maximization for radii with linear constraints:\n      r_i <= dist_to_border_i\n      r_i + r_j <= dist_ij\n    Start with border-limited radii and resolve pairwise violations by\n    reducing the larger partner. This simple greedy tends to maximize the sum.\n    \"\"\"\n    n = centers.shape[0]\n    # initial radii limited by distance to borders\n    radii = np.minimum.reduce([centers[:,0], centers[:,1], 1-centers[:,0], 1-centers[:,1]]).astype(float)\n\n    # Pairwise distances\n    dmat = np.sqrt(((centers[:,None,:] - centers[None,:,:])**2).sum(axis=2))\n    # small diagonal values -> large, but we won't use dmat[i,i]\n    for it in range(max_iter):\n        changed = False\n        # iterate pairs in order of increasing distance (tightest constraints first)\n        i_idxs, j_idxs = np.triu_indices(n, k=1)\n        order = np.argsort(dmat[i_idxs, j_idxs])\n        for idx in order:\n            i = i_idxs[idx]; j = j_idxs[idx]\n            dij = dmat[i, j]\n            if dij <= 0:\n                # coincident centers -> force one to zero\n                if radii[i] >= radii[j]:\n                    if radii[i] > 0:\n                        radii[i] = 0.0; changed = True\n                else:\n                    if radii[j] > 0:\n                        radii[j] = 0.0; changed = True\n                continue\n            if radii[i] + radii[j] > dij + tol:\n                # reduce the larger radius to satisfy r_large = dij - r_small\n                if radii[i] > radii[j]:\n                    new_ri = max(dij - radii[j], 0.0)\n                    if radii[i] - new_ri > tol:\n                        radii[i] = new_ri\n                        changed = True\n                else:\n                    new_rj = max(dij - radii[i], 0.0)\n                    if radii[j] - new_rj > tol:\n                        radii[j] = new_rj\n                        changed = True\n        # Re-apply border constraints (numerical safety)\n        border_limits = np.minimum.reduce([centers[:,0], centers[:,1], 1-centers[:,0], 1-centers[:,1]])\n        for k in range(n):\n            if radii[k] > border_limits[k] + tol:\n                radii[k] = max(border_limits[k], 0.0)\n                changed = True\n        if not changed:\n            break\n\n    # Final numeric clean-up\n    radii = np.maximum(radii, 0.0)\n    return radii\n\n# EVOLVE-BLOCK-END\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor='k')\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nUnique approach: Modification: Full rewrite, Excellent validity (1.000), Excellent sum_radii (1.665)\n\n### Inspiration 3 (Score: 0.6532, Type: Alternative)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Hex-lattice selector + iterative radius balancing for n=26 circles in unit square.\nTries several hex spacings and picks the arrangement with maximal sum of radii.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    target_n = 26\n    center_point = np.array([0.5, 0.5])\n\n    # Candidate spacings to try (coarse -> fine). Tuned to produce compact hex patches.\n    spacings = np.linspace(0.095, 0.14, 16)\n\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    for a in spacings:\n        dx = a\n        dy = a * np.sqrt(3) / 2.0\n        margin = a / 2.0\n        pts = []\n        y = margin\n        row = 0\n        # generate hex-lattice points inside unit square\n        while y <= 1.0 - margin + 1e-12:\n            x0 = margin + (0.5 * dx if (row % 2 == 1) else 0.0)\n            x = x0\n            while x <= 1.0 - margin + 1e-12:\n                pts.append((x, y))\n                x += dx\n            row += 1\n            y += dy\n        pts = np.array(pts)\n        if pts.shape[0] < target_n:\n            continue\n\n        # choose the target_n centers closest to the square center\n        dists = np.linalg.norm(pts - center_point, axis=1)\n        idx = np.argsort(dists)[:target_n]\n        centers = pts[idx]\n\n        # compute radii using iterative balancing to maximize feasible sizes\n        radii = compute_max_radii(centers)\n        s = float(np.sum(radii))\n        if s > best_sum:\n            best_sum = s\n            best_centers = centers.copy()\n            best_radii = radii.copy()\n\n    # fallback: if no spacing produced enough points, use a fine spacing\n    if best_centers is None:\n        a = 0.09\n        dx = a; dy = a * np.sqrt(3) / 2.0; margin = a/2.0\n        pts = []\n        y = margin; row = 0\n        while y <= 1.0 - margin + 1e-12:\n            x0 = margin + (0.5 * dx if (row % 2 == 1) else 0.0)\n            x = x0\n            while x <= 1.0 - margin + 1e-12:\n                pts.append((x, y))\n                x += dx\n            row += 1; y += dy\n        pts = np.array(pts)\n        dists = np.linalg.norm(pts - center_point, axis=1)\n        idx = np.argsort(dists)[:target_n]\n        best_centers = pts[idx]\n        best_radii = compute_max_radii(best_centers)\n        best_sum = float(np.sum(best_radii))\n\n    return np.array(best_centers), np.array(best_radii), float(best_sum)\n\n\ndef compute_max_radii(centers, max_iter=2000, tol=1e-7):\n    \"\"\"\n    Iterative balancing:\n      - start with border-limited radii\n      - shrink pairs that overlap (scale both), then attempt small growth toward local limits\n      - repeat until convergence\n    This tends to produce larger total radii than a pure greedy shrink.\n    \"\"\"\n    n = centers.shape[0]\n    if n == 0:\n        return np.array([])\n\n    # distances to borders\n    border = np.minimum.reduce([centers[:,0], centers[:,1], 1-centers[:,0], 1-centers[:,1]])\n    border = np.maximum(border, 0.0)\n    # start radii at border limit\n    r = border.copy()\n\n    # pairwise distances (fixed)\n    diff = centers[:, None, :] - centers[None, :, :]\n    dists = np.sqrt(np.sum(diff**2, axis=2))\n    # set diag to large\n    np.fill_diagonal(dists, 1e6)\n\n    for it in range(max_iter):\n        prev = r.copy()\n\n        # Pairwise shrink: ensure r_i + r_j <= d_ij\n        # iterate pairs in order of smallest available slack first\n        i_idx, j_idx = np.triu_indices(n, k=1)\n        pair_d = dists[i_idx, j_idx]\n        order = np.argsort(pair_d)\n        for k in order:\n            i = i_idx[k]; j = j_idx[k]\n            dij = dists[i, j]\n            if r[i] + r[j] > dij:\n                # scale both proportionally to preserve balance\n                total = r[i] + r[j]\n                if total <= 0:\n                    # if both zero continue\n                    continue\n                scale = max(0.0, dij / total)\n                r[i] *= scale\n                r[j] *= scale\n\n        # Growth pass: attempt to grow each circle up to min(border, min(d_ij - r_j))\n        for i in range(n):\n            allowed = border[i]\n            # neighbors limit\n            neigh_lim = np.min(dists[i, :] - r)\n            allowed = min(allowed, neigh_lim)\n            allowed = max(allowed, 0.0)\n            # damped growth to avoid oscillation\n            if allowed > r[i] + 1e-12:\n                r[i] = 0.5 * (r[i] + allowed)\n\n        r = np.minimum(r, border)\n        r = np.maximum(r, 0.0)\n\n        if np.max(np.abs(r - prev)) < tol:\n            break\n\n    return r\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(6,6))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect(\"equal\")\n    for c,r in zip(centers, radii):\n        ax.add_patch(Circle(c, r, alpha=0.6, edgecolor='k'))\n    plt.title(f\"n={len(centers)}, sum={sum(radii):.6f}\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # visualize(centers, radii)\n```\nUnique approach: Modification: Full rewrite, Excellent validity (1.000), Excellent sum_radii (1.721)\n\n# Current Program\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Improved constructor-based circle packing for n=26 circles (explicit pattern + relaxation)\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct an explicit arrangement of 26 circle centers in the unit square\n    and compute maximal non-overlapping radii by iterative relaxation.\n    Returns:\n        centers: np.array (26,2)\n        radii: np.array (26,)\n        sum_of_radii: float\n    \"\"\"\n    n = 26\n\n    # Use 5 rows with counts summing to 26: [5,6,5,5,5]\n    rows = [5, 6, 5, 5, 5]\n    n_rows = len(rows)\n    assert sum(rows) == n\n\n    # Horizontal margins and vertical span chosen to allow hex-like packing\n    left, right = 0.06, 0.94\n    y_top, y_bot = 0.12, 0.88\n\n    centers = np.zeros((n, 2))\n    idx = 0\n    ys = np.linspace(y_top, y_bot, n_rows)\n    for r_idx, count in enumerate(rows):\n        y = ys[r_idx]\n        if count == 1:\n            xs = np.array([(left + right) / 2.0])\n        else:\n            xs = np.linspace(left, right, count)\n            # Hex-like offset for alternating rows to improve packing\n            if r_idx % 2 == 1:\n                # compute average spacing and shift by half-step\n                spacing = (right - left) / max(1, count - 1)\n                xs = xs + 0.5 * spacing\n                xs = np.clip(xs, left, right)\n        for x in xs:\n            centers[idx] = [x, y]\n            idx += 1\n\n    # small random but deterministic perturbation to break perfect symmetry (improves packing)\n    # use a fixed tiny perturbation to keep reproducible\n    rng = np.random.RandomState(42)\n    centers += (rng.rand(*centers.shape) - 0.5) * 1e-3\n    centers = np.clip(centers, 0.001, 0.999)\n\n    # Compute maximal radii with iterative pairwise relaxation:\n    radii = compute_max_radii_relax(centers)\n\n    return centers, radii, float(np.sum(radii))\n\n\ndef compute_max_radii_relax(centers, max_iter=2000, tol=1e-8):\n    \"\"\"\n    Iteratively compute maximal radii so circles stay in unit square and do not overlap.\n    Strategy:\n      - Start with border-limited radii (distance to nearest wall).\n      - For each pair that overlaps, reduce the larger radius so the pair is tangent.\n      - Repeat until changes are negligible.\n    This preserves small circles and generally yields larger total radius than uniform scaling.\n    \"\"\"\n    n = centers.shape[0]\n    # distance to walls\n    border_r = np.minimum.reduce([centers[:, 0], centers[:, 1], 1.0 - centers[:, 0], 1.0 - centers[:, 1]])\n    # start with border radii\n    radii = border_r.copy()\n\n    if n <= 1:\n        return radii\n\n    # Precompute pairwise distances\n    # We'll recompute distances each iteration (positions fixed) \u2014 can compute once\n    diffs = centers[:, None, :] - centers[None, :, :]\n    dists = np.sqrt(np.sum(diffs ** 2, axis=2))\n    # Avoid zero on diagonal\n    dists += np.eye(n) * 1.0\n\n    for it in range(max_iter):\n        max_change = 0.0\n\n        # enforce border constraint\n        new_radii = np.minimum(radii, border_r)\n\n        # pairwise relaxation: if overlap, reduce the larger radius to make them tangent\n        for i in range(n):\n            ri = new_radii[i]\n            for j in range(i + 1, n):\n                rj = new_radii[j]\n                d = dists[i, j]\n                if ri + rj > d:\n                    # reduce the larger one (preserve smaller one)\n                    if ri > rj:\n                        target = max(0.0, d - rj)\n                        if target < ri:\n                            change = ri - target\n                            new_radii[i] = target\n                            if change > max_change:\n                                max_change = change\n                            ri = target\n                    else:\n                        target = max(0.0, d - ri)\n                        if target < rj:\n                            change = rj - target\n                            new_radii[j] = target\n                            if change > max_change:\n                                max_change = change\n                            rj = target\n\n        radii = new_radii\n\n        if max_change < tol:\n            break\n\n    # Final safety clamp\n    radii = np.maximum(radii, 0.0)\n    return radii\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\n\n# Task\nRewrite the program to improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: complexity, diversity\nDifferent solutions with similar fitness but different features are valuable.\nProvide the complete new program code.\n\nIMPORTANT: Make sure your rewritten program maintains the same inputs and outputs\nas the original program, but with improved internal implementation.\n\n```python\n# Your rewritten program here\n```", "responses": ["```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for packing 26 circles in the unit square.\n\nStrategy:\n - Generate hexagonal-lattice candidate centers for a range of spacings.\n - For each spacing that produces >=26 candidates, pick the 26 centers closest to the square center.\n - Compute feasible radii as min(distance-to-border, 0.5 * nearest-neighbor-distance).\n - Choose the spacing that yields the largest sum of radii.\nThis explicit constructor is deterministic and favors a compact, near-hex arrangement.\n\"\"\"\nimport numpy as np\n\ndef _hex_lattice(spacing):\n    dx = spacing\n    dy = spacing * np.sqrt(3) / 2.0\n    margin = spacing / 2.0\n    pts = []\n    y = margin\n    row = 0\n    # generate points inside [0,1]^2 with margin\n    while y <= 1.0 - margin + 1e-12:\n        x0 = margin + (0.5 * dx if (row % 2 == 1) else 0.0)\n        x = x0\n        while x <= 1.0 - margin + 1e-12:\n            pts.append((x, y))\n            x += dx\n        row += 1\n        y += dy\n    return np.array(pts)\n\ndef _compute_radii(centers):\n    if centers.size == 0:\n        return np.array([])\n    # distance to border\n    dist_border = np.minimum.reduce([centers[:,0], centers[:,1], 1-centers[:,0], 1-centers[:,1]])\n    # pairwise distances\n    diff = centers[:, None, :] - centers[None, :, :]\n    dists = np.sqrt(np.sum(diff**2, axis=2))\n    # mask diagonal\n    np.fill_diagonal(dists, np.inf)\n    nearest = np.min(dists, axis=1)\n    radii = np.minimum(dist_border, 0.5 * nearest)\n    # numeric safety\n    radii = np.maximum(radii, 1e-12)\n    return radii\n\ndef construct_packing():\n    target_n = 26\n    center_point = np.array([0.5, 0.5])\n\n    # Try a dense set of spacings to trade count vs size\n    spacings = np.linspace(0.055, 0.20, 48)\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    for a in spacings:\n        pts = _hex_lattice(a)\n        if pts.shape[0] < target_n:\n            continue\n        # choose the 26 closest to the center (favors compact cluster)\n        idx = np.argsort(np.linalg.norm(pts - center_point, axis=1))[:target_n]\n        centers = pts[idx]\n        radii = _compute_radii(centers)\n        s = float(np.sum(radii))\n        if s > best_sum:\n            best_sum = s\n            best_centers = centers.copy()\n            best_radii = radii.copy()\n\n    # fallback: ensure we return something\n    if best_centers is None:\n        pts = _hex_lattice(0.09)\n        idx = np.argsort(np.linalg.norm(pts - center_point, axis=1))[:target_n]\n        best_centers = pts[idx]\n        best_radii = _compute_radii(best_centers)\n        best_sum = float(np.sum(best_radii))\n\n    # clamp tiny numeric issues\n    eps = 1e-12\n    best_centers = np.clip(best_centers, eps, 1.0 - eps)\n    best_radii = np.maximum(best_radii, 0.0)\n    return best_centers, best_radii, float(np.sum(best_radii))\n\n# EVOLVE-BLOCK-END\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor='k', linewidth=0.5)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```"]}}, "artifacts_json": "{\"stdout\": \"Running in subprocess, Python version: 3.12.1 (v3.12.1:2305ca5144, Dec  7 2023, 17:23:38) [Clang 13.0.0 (clang-1300.0.29.30)]\\nProgram path: /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmpfvcysmab.py\\nCalling run_packing()...\\nrun_packing() returned successfully: sum_radii = 2.399468085106382\\nResults saved to /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmpaf1n_g6s.py.results\\n\", \"stderr\": \"\"}", "artifact_dir": null}