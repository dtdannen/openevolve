{"id": "99bbbf63-70cc-4a76-9983-c0d16fe5acf7", "code": "# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for packing 26 circles in the unit square.\n\nStrategy:\n - Place 26 centers in 5 rows with counts [5,6,5,5,5] (a near-hexagonal cluster).\n - Search over horizontal spacing (s) and a small odd-row offset to break perfect symmetry.\n - For each candidate set of centers compute feasible radii = min(distance-to-border, 0.5*nearest-neighbor).\n - Return the configuration with maximal sum of radii.\nThis explicit constructor is fast, deterministic, and yields a compact near-hexagonal packing.\n\"\"\"\nimport numpy as np\n\ndef _compute_radii(centers):\n    # distance to border\n    db = np.minimum.reduce([centers[:,0], centers[:,1], 1-centers[:,0], 1-centers[:,1]])\n    # pairwise distances\n    diff = centers[:,None,:] - centers[None,:,:]\n    d = np.sqrt((diff**2).sum(axis=2))\n    np.fill_diagonal(d, np.inf)\n    nearest = np.min(d, axis=1)\n    r = np.minimum(db, 0.5*nearest)\n    return np.maximum(r, 1e-12)\n\ndef construct_packing():\n    target_n = 26\n    rows = [5,6,5,5,5]\n    best = None\n    best_sum = -1.0\n    sqrt3 = np.sqrt(3.0)\n\n    # search over horizontal spacing and small odd-row offset to improve packing\n    for s in np.linspace(0.078, 0.18, 62):  # spacing candidates\n        v = s * sqrt3 / 2.0\n        total_h = max(rows) - 1\n        total_height = (len(rows)-1) * v\n        y0 = 0.5 - total_height/2.0\n\n        # try slight offsets for odd rows (to break symmetry and better use square edges)\n        for off in np.linspace(-0.03, 0.03, 13):\n            centers = []\n            ok = True\n            for ri, cnt in enumerate(rows):\n                y = y0 + ri * v\n                row_width = (cnt - 1) * s\n                x_start = 0.5 - row_width/2.0\n                # apply odd-row micro-offset\n                if (ri % 2) == 1:\n                    x_start += off + s/2.0\n                for c in range(cnt):\n                    x = x_start + c * s\n                    # if any center falls outside, mark invalid candidate\n                    if not (0.0 <= x <= 1.0 and 0.0 <= y <= 1.0):\n                        ok = False\n                        break\n                    centers.append((x,y))\n                if not ok:\n                    break\n            if not ok or len(centers) != target_n:\n                continue\n            centers = np.array(centers)\n            radii = _compute_radii(centers)\n            ssum = float(radii.sum())\n            if ssum > best_sum:\n                best_sum = ssum\n                best = (centers.copy(), radii.copy(), ssum)\n\n    # fallback: dense hex-lattice cluster near center (guarantee feasibility)\n    if best is None:\n        dx = 0.09\n        dy = dx * np.sqrt(3)/2.0\n        pts = []\n        margin = dx/2.0\n        row = 0\n        y = margin\n        while y <= 1 - margin + 1e-12:\n            x0 = margin + (0.5*dx if row%2 else 0.0)\n            x = x0\n            while x <= 1 - margin + 1e-12:\n                pts.append((x,y))\n                x += dx\n            row += 1\n            y += dy\n        pts = np.array(pts)\n        center = np.array([0.5,0.5])\n        idx = np.argsort(np.linalg.norm(pts-center,axis=1))[:target_n]\n        centers = pts[idx]\n        radii = _compute_radii(centers)\n        best = (centers, radii, float(radii.sum()))\n\n    return best\n\n# EVOLVE-BLOCK-END\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor='k', linewidth=0.5)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # visualize(centers, radii)", "language": "python", "parent_id": "1a8c4abc-fc00-4976-8c1c-c1cd886489e3", "generation": 2, "timestamp": 1759956766.953065, "iteration_found": 17, "metrics": {"validity": 1.0, "sum_radii": 1.9879488614197967, "target_ratio": 0.754439795605236, "combined_score": 0.754439795605236, "eval_time": 0.07816195487976074}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Full rewrite", "parent_metrics": {"validity": 1.0, "sum_radii": 2.411724137931034, "target_ratio": 0.9152653274880586, "combined_score": 0.9152653274880586, "eval_time": 0.05837678909301758}, "island": 0}, "prompts": {"full_rewrite_user": {"system": "You are an expert mathematician specializing in circle packing problems and computational geometry. Your task is to improve a constructor function that directly produces a specific arrangement of 26 circles in a unit square, maximizing the sum of their radii. The AlphaEvolve paper achieved a sum of 2.635 for n=26.\n\nKey geometric insights:\n- Circle packings often follow hexagonal patterns in the densest regions\n- Maximum density for infinite circle packing is pi/(2*sqrt(3)) \u2248 0.9069\n- Edge effects make square container packing harder than infinite packing\n- Circles can be placed in layers or shells when confined to a square\n- Similar radius circles often form regular patterns, while varied radii allow better space utilization\n- Perfect symmetry may not yield the optimal packing due to edge effects\n\nFocus on designing an explicit constructor that places each circle in a specific position, rather than an iterative search algorithm.\n", "user": "# Current Program Information\n- Fitness: 0.9153\n- Feature coordinates: No feature coordinates\n- Focus areas: - Fitness improved: 0.8345 \u2192 0.9153\n- Consider simplifying - code length exceeds 500 characters\n\n## Last Execution Output\n\n### stdout\n```\nRunning in subprocess, Python version: 3.12.1 (v3.12.1:2305ca5144, Dec  7 2023, 17:23:38) [Clang 13.0.0 (clang-1300.0.29.30)]\nProgram path: /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmpb9jrf74l.py\nCalling run_packing()...\nrun_packing() returned successfully: sum_radii = 2.411724137931034\nResults saved to /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmpjwg9z0o9.py.results\n\n```\n\n### stderr\n```\n\n```\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 3\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.1990, target_ratio: 0.8345, combined_score: 0.8345, eval_time: 0.0880\n- Outcome: Mixed results\n\n### Attempt 2\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.3400, target_ratio: 0.8880, combined_score: 0.8880, eval_time: 0.0610\n- Outcome: Mixed results\n\n### Attempt 1\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.4117, target_ratio: 0.9153, combined_score: 0.9153, eval_time: 0.0584\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.9153)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for packing 26 circles in the unit square.\n\nStrategy:\n - Generate several hexagonal-lattice candidate center sets with different spacings.\n - For each spacing that produces >=26 candidates, pick the 26 centers closest to the square center.\n - Compute feasible radii as the minimum of (distance-to-border) and (half nearest-neighbor distance).\n - Choose the spacing that yields the largest sum of radii.\nThis yields a compact, near-hexagonal center cluster that improves total radii.\n\"\"\"\nimport numpy as np\n\ndef _hex_lattice(spacing):\n    dx = spacing\n    dy = spacing * np.sqrt(3) / 2.0\n    margin = spacing / 2.0\n    pts = []\n    y = margin\n    row = 0\n    while y <= 1.0 - margin + 1e-12:\n        x0 = margin + (0.5 * dx if (row % 2 == 1) else 0.0)\n        x = x0\n        while x <= 1.0 - margin + 1e-12:\n            pts.append((x, y))\n            x += dx\n        row += 1\n        y += dy\n    return np.array(pts)\n\ndef _compute_radii(centers):\n    n = centers.shape[0]\n    # distance to border\n    dist_border = np.minimum.reduce([centers[:,0], centers[:,1], 1-centers[:,0], 1-centers[:,1]])\n    # pairwise distances (mask diagonal)\n    diff = centers[:, None, :] - centers[None, :, :]\n    dists = np.sqrt(np.sum(diff**2, axis=2))\n    np.fill_diagonal(dists, np.inf)\n    nearest = np.min(dists, axis=1)\n    radii = np.minimum(dist_border, 0.5 * nearest)\n    # safety\n    radii = np.maximum(radii, 1e-8)\n    return radii\n\ndef construct_packing():\n    target_n = 26\n    center_point = np.array([0.5, 0.5])\n\n    # Try a range of spacings; include fine and coarse to trade count vs size\n    spacings = np.linspace(0.06, 0.20, 30)  # finer resolution to find good spacing\n    best = None\n    best_sum = -1.0\n\n    for a in spacings:\n        pts = _hex_lattice(a)\n        if pts.shape[0] < target_n:\n            continue\n        # choose the 26 closest to center (favors larger feasible radii)\n        idx = np.argsort(np.linalg.norm(pts - center_point, axis=1))[:target_n]\n        centers = pts[idx]\n        radii = _compute_radii(centers)\n        s = float(np.sum(radii))\n        if s > best_sum:\n            best_sum = s\n            best = (centers.copy(), radii.copy(), s)\n\n    # Fallback: if none spacing produced enough points, use a dense lattice and pick best 26\n    if best is None:\n        pts = _hex_lattice(0.055)\n        idx = np.argsort(np.linalg.norm(pts - center_point, axis=1))[:target_n]\n        centers = pts[idx]\n        radii = _compute_radii(centers)\n        best = (centers, radii, float(np.sum(radii)))\n\n    return best\n\n# EVOLVE-BLOCK-END\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor='k', linewidth=0.5)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.4117), Performs well on target_ratio (0.9153), Performs well on combined_score (0.9153), Performs well on eval_time (0.0584)\n\n### Program 2 (Score: 0.8880)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles using a hexagonal lattice selector.\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    \"\"\"\n    Construct a deterministic arrangement of 26 circles in the unit square.\n    Strategy:\n      - Generate a family of hexagonal-lattice candidate centers with varying spacing.\n      - Choose a spacing that yields >=26 candidate centers inside the square.\n      - Select the 26 centers closest to the square center (favoring larger feasible radii).\n      - Assign each selected circle radius = min(distance-to-border, 0.5 * nearest-neighbor-distance).\n    This explicit constructor yields a compact, nearly hexagonal packing in the center,\n    which typically increases the sum of radii vs. naive ring placements.\n    Returns:\n        centers: np.array (26,2)\n        radii: np.array (26,)\n        sum_radii: float\n    \"\"\"\n    target_n = 26\n    # Try spacings from coarse to fine until we get at least target_n candidates\n    spacings = np.linspace(0.18, 0.10, 17)  # try larger spacing first (fewer points), then refine\n    best_candidates = None\n    for a in spacings:\n        dx = a\n        dy = a * np.sqrt(3) / 2\n        margin = a / 2.0\n        pts = []\n        y = margin\n        row = 0\n        while y <= 1.0 - margin + 1e-12:\n            # offset alternate rows by half dx\n            x0 = margin + (0.5 * dx if (row % 2 == 1) else 0.0)\n            x = x0\n            while x <= 1.0 - margin + 1e-12:\n                pts.append((x, y))\n                x += dx\n            row += 1\n            y += dy\n        pts = np.array(pts)\n        if pts.shape[0] >= target_n:\n            best_candidates = pts\n            break\n    if best_candidates is None:\n        # fallback: very fine spacing to guarantee enough points\n        a = 0.095\n        dx = a; dy = a * np.sqrt(3) / 2; margin = a/2\n        pts = []\n        y = margin\n        row = 0\n        while y <= 1.0 - margin + 1e-12:\n            x0 = margin + (0.5 * dx if (row % 2 == 1) else 0.0)\n            x = x0\n            while x <= 1.0 - margin + 1e-12:\n                pts.append((x, y))\n                x += dx\n            row += 1\n            y += dy\n        best_candidates = np.array(pts)\n\n    # Select the target_n centers closest to the square center (0.5,0.5)\n    center_point = np.array([0.5, 0.5])\n    dists_to_center = np.linalg.norm(best_candidates - center_point, axis=1)\n    idx = np.argsort(dists_to_center)[:target_n]\n    centers = best_candidates[idx]\n\n    # Compute radii: distance to border and half nearest-neighbor distance\n    n = centers.shape[0]\n    # distance to borders\n    dist_border = np.minimum.reduce([centers[:,0], centers[:,1], 1-centers[:,0], 1-centers[:,1]])\n    # pairwise distances\n    diff = centers[:, np.newaxis, :] - centers[np.newaxis, :, :]  # (n,n,2)\n    pdist = np.sqrt(np.sum(diff**2, axis=2)) + np.eye(n) * 1e6  # mask self by large number\n    nearest = np.min(pdist, axis=1)\n    radii = np.minimum(dist_border, 0.5 * nearest)\n    # small safety floor\n    radii = np.maximum(radii, 1e-6)\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n# EVOLVE-BLOCK-END\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor='k', linewidth=0.5)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.3400), Performs well on target_ratio (0.8880), Performs well on combined_score (0.8880), Performs well on eval_time (0.0610)\n\n### Program 3 (Score: 0.8345)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (hex-like explicit constructor)\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct an explicit arrangement of 26 circles in the unit square.\n    Returns: centers (26,2), radii (26,), sum_of_radii\n    \"\"\"\n    n = 26\n\n    # We'll build a 5-row hex-like layout with one row having 6 centers and others 5:\n    row_count_options = []\n    for k in range(5):\n        # place the row with 6 centers at index k (0..4)\n        row_counts = [5] * 5\n        row_counts[k] = 6\n        row_count_options.append(tuple(row_counts))\n\n    best = None\n    # search over which row has 6 and over spacing s to maximize sum of radii\n    for row_counts in row_count_options:\n        # vertical spacing factor for hex lattice\n        for s in np.linspace(0.12, 0.20, 161):  # coarse but effective scan\n            v = s * np.sqrt(3) / 2.0\n            # compute y coordinates centered in the unit square\n            rows = len(row_counts)\n            total_height = (rows - 1) * v\n            y0 = 0.5 - total_height / 2.0\n            centers = []\n            valid = True\n            for i, cnt in enumerate(row_counts):\n                y = y0 + i * v\n                # for hex pattern, alternate rows are offset by s/2\n                if cnt == 6:\n                    x_start = 0.5 - (cnt - 1) * s / 2.0\n                else:\n                    x_start = 0.5 - (cnt - 1) * s / 2.0\n                xs = [x_start + j * s for j in range(cnt)]\n                for x in xs:\n                    # ensure inside square\n                    if not (0.0 <= x <= 1.0 and 0.0 <= y <= 1.0):\n                        valid = False\n                        break\n                    centers.append((x, y))\n                if not valid:\n                    break\n            if not valid or len(centers) != n:\n                continue\n            centers = np.array(centers)\n\n            # compute nearest neighbor distances\n            D = np.sqrt(((centers[:, None, :] - centers[None, :, :]) ** 2).sum(axis=2))\n            # set diagonal to large to ignore self-distance\n            np.fill_diagonal(D, np.inf)\n            nearest = D.min(axis=1)\n\n            # distance to borders\n            border = np.minimum.reduce([centers[:, 0], centers[:, 1], 1 - centers[:, 0], 1 - centers[:, 1]])\n\n            # radii capped by half nearest neighbor and by border\n            radii = np.minimum(border, 0.5 * nearest)\n\n            sum_r = radii.sum()\n\n            if (best is None) or (sum_r > best[0]):\n                best = (sum_r, centers.copy(), radii.copy(), s, row_counts)\n\n    if best is None:\n        # fallback: simple center and two rings as before but improved radii calc\n        centers = np.zeros((n, 2))\n        centers[0] = [0.5, 0.5]\n        for i in range(8):\n            angle = 2 * np.pi * i / 8\n            centers[i + 1] = [0.5 + 0.25 * np.cos(angle), 0.5 + 0.25 * np.sin(angle)]\n        for i in range(17):\n            angle = 2 * np.pi * i / 17\n            centers[i + 9] = [0.5 + 0.6 * np.cos(angle), 0.5 + 0.6 * np.sin(angle)]\n        radii = compute_max_radii(centers)\n        sum_r = radii.sum()\n        return centers, radii, sum_r\n\n    sum_r, centers, radii, s_used, row_counts_used = best\n    # final safety: enforce non-negativity and tiny eps\n    radii = np.maximum(radii, 1e-12)\n    return centers, radii, sum_r\n\n\ndef compute_max_radii(centers):\n    \"\"\"Helper: robust max radii (used only in fallback).\"\"\"\n    n = centers.shape[0]\n    border = np.minimum.reduce([centers[:, 0], centers[:, 1], 1 - centers[:, 0], 1 - centers[:, 1]])\n    D = np.sqrt(((centers[:, None, :] - centers[None, :, :]) ** 2).sum(axis=2))\n    np.fill_diagonal(D, np.inf)\n    nearest = D.min(axis=1)\n    radii = np.minimum(border, 0.5 * nearest)\n    radii = np.maximum(radii, 0.0)\n    return radii\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.1990), Performs well on target_ratio (0.8345), Performs well on combined_score (0.8345), Performs well on eval_time (0.0880)\n\n\n\n## Diverse Programs\n\n### Program D1 (Score: 0.8265)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Improved constructor-based circle packing for n=26 circles (explicit pattern + relaxation)\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct an explicit arrangement of 26 circle centers in the unit square\n    and compute maximal non-overlapping radii by iterative relaxation.\n    Returns:\n        centers: np.array (26,2)\n        radii: np.array (26,)\n        sum_of_radii: float\n    \"\"\"\n    n = 26\n\n    # Use 5 rows with counts summing to 26: [5,6,5,5,5]\n    rows = [5, 6, 5, 5, 5]\n    n_rows = len(rows)\n    assert sum(rows) == n\n\n    # Horizontal margins and vertical span chosen to allow hex-like packing\n    left, right = 0.06, 0.94\n    y_top, y_bot = 0.12, 0.88\n\n    centers = np.zeros((n, 2))\n    idx = 0\n    ys = np.linspace(y_top, y_bot, n_rows)\n    for r_idx, count in enumerate(rows):\n        y = ys[r_idx]\n        if count == 1:\n            xs = np.array([(left + right) / 2.0])\n        else:\n            xs = np.linspace(left, right, count)\n            # Hex-like offset for alternating rows to improve packing\n            if r_idx % 2 == 1:\n                # compute average spacing and shift by half-step\n                spacing = (right - left) / max(1, count - 1)\n                xs = xs + 0.5 * spacing\n                xs = np.clip(xs, left, right)\n        for x in xs:\n            centers[idx] = [x, y]\n            idx += 1\n\n    # small random but deterministic perturbation to break perfect symmetry (improves packing)\n    # use a fixed tiny perturbation to keep reproducible\n    rng = np.random.RandomState(42)\n    centers += (rng.rand(*centers.shape) - 0.5) * 1e-3\n    centers = np.clip(centers, 0.001, 0.999)\n\n    # Compute maximal radii with iterative pairwise relaxation:\n    radii = compute_max_radii_relax(centers)\n\n    return centers, radii, float(np.sum(radii))\n\n\ndef compute_max_radii_relax(centers, max_iter=2000, tol=1e-8):\n    \"\"\"\n    Iteratively compute maximal radii so circles stay in unit square and do not overlap.\n    Strategy:\n      - Start with border-limited radii (distance to nearest wall).\n      - For each pair that overlaps, reduce the larger radius so the pair is tangent.\n      - Repeat until changes are negligible.\n    This preserves small circles and generally yields larger total radius than uniform scaling.\n    \"\"\"\n    n = centers.shape[0]\n    # distance to walls\n    border_r = np.minimum.reduce([centers[:, 0], centers[:, 1], 1.0 - centers[:, 0], 1.0 - centers[:, 1]])\n    # start with border radii\n    radii = border_r.copy()\n\n    if n <= 1:\n        return radii\n\n    # Precompute pairwise distances\n    # We'll recompute distances each iteration (positions fixed) \u2014 can compute once\n    diffs = centers[:, None, :] - centers[None, :, :]\n    dists = np.sqrt(np.sum(diffs ** 2, axis=2))\n    # Avoid zero on diagonal\n    dists += np.eye(n) * 1.0\n\n    for it in range(max_iter):\n        max_change = 0.0\n\n        # enforce border constraint\n        new_radii = np.minimum(radii, border_r)\n\n        # pairwise relaxation: if overlap, reduce the larger radius to make them tangent\n        for i in range(n):\n            ri = new_radii[i]\n            for j in range(i + 1, n):\n                rj = new_radii[j]\n                d = dists[i, j]\n                if ri + rj > d:\n                    # reduce the larger one (preserve smaller one)\n                    if ri > rj:\n                        target = max(0.0, d - rj)\n                        if target < ri:\n                            change = ri - target\n                            new_radii[i] = target\n                            if change > max_change:\n                                max_change = change\n                            ri = target\n                    else:\n                        target = max(0.0, d - ri)\n                        if target < rj:\n                            change = rj - target\n                            new_radii[j] = target\n                            if change > max_change:\n                                max_change = change\n                            rj = target\n\n        radii = new_radii\n\n        if max_change < tol:\n            break\n\n    # Final safety clamp\n    radii = np.maximum(radii, 0.0)\n    return radii\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nKey features: Alternative approach to validity, Alternative approach to sum_radii\n\n### Program D2 (Score: 0.8287)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (explicit hex-like layout + iterative radius balancing)\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square\n    and compute near-maximal non-overlapping radii for the fixed centers.\n    Returns: (centers, radii, sum_of_radii)\n    \"\"\"\n    n = 26\n\n    # Row-wise hex-like arrangement with counts summing to 26\n    row_counts = [5, 6, 5, 5, 5]  # total 26\n    max_cols = max(row_counts)\n    margin = 0.02\n\n    # choose horizontal spacing so the widest row fits inside [margin,1-margin]\n    if max_cols > 1:\n        a = (1.0 - 2 * margin) / (max_cols - 1)\n    else:\n        a = 0.2\n    v = a * np.sqrt(3) / 2.0  # vertical spacing for hex packing\n\n    total_rows = len(row_counts)\n    height_used = (total_rows - 1) * v\n    y0 = margin + max(0.0, (1.0 - 2 * margin - height_used) / 2.0)\n\n    centers = []\n    for r, count in enumerate(row_counts):\n        y = y0 + r * v\n        # center each row horizontally\n        row_width = (count - 1) * a\n        x_start = 0.5 - row_width / 2.0\n        # offset every other row for hex-like staggering\n        if (r % 2) == 1:\n            x_start += a / 2.0\n        for c in range(count):\n            x = x_start + c * a\n            # clip to unit square with small tolerance\n            x = min(max(x, margin), 1 - margin)\n            y = min(max(y, margin), 1 - margin)\n            centers.append([x, y])\n\n    centers = np.array(centers[:n])\n    # Ensure we have exactly n centers\n    if centers.shape[0] < n:\n        # pad with center points if necessary (shouldn't happen)\n        while centers.shape[0] < n:\n            centers = np.vstack([centers, [0.5, 0.5]])\n    elif centers.shape[0] > n:\n        centers = centers[:n]\n\n    radii = compute_max_radii(centers)\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers, max_iter=2000, tol=1e-6):\n    \"\"\"\n    Iterative balancing: start from border-limited radii and iteratively\n    enforce pairwise non-overlap (shrink) and local growth (where possible).\n    \"\"\"\n    n = centers.shape[0]\n    # precompute pairwise distances\n    diff = centers[:, None, :] - centers[None, :, :]\n    dists = np.sqrt((diff ** 2).sum(axis=2)) + np.eye(n)  # add diag nonzero to avoid zero\n    # border distances\n    border = np.minimum.reduce([centers[:, 0], centers[:, 1],\n                                1 - centers[:, 0], 1 - centers[:, 1]])\n    border = np.maximum(border, 0.0)\n\n    # start with border distances (cannot exceed border)\n    r = border.copy()\n\n    for it in range(max_iter):\n        prev = r.copy()\n\n        # Pairwise shrink pass: ensure r_i + r_j <= dist_ij (with small safety)\n        for i in range(n):\n            for j in range(i + 1, n):\n                dij = dists[i, j]\n                # effective allowed sum (tiny safety margin)\n                allowed = max(0.0, dij - 1e-12)\n                sumr = r[i] + r[j]\n                if sumr > allowed and sumr > 0:\n                    scale = allowed / sumr\n                    # scale both radii proportionally but don't exceed borders\n                    r[i] *= scale\n                    r[j] *= scale\n\n        # Growth pass: for each circle, if there's slack given others fixed, try to grow (damped)\n        for i in range(n):\n            # max allowed by borders\n            allowed = border[i]\n            # max allowed by neighbors: r_i <= dist_ij - r_j for all j != i\n            # compute minimal (dist_ij - r_j)\n            neighbors_limit = np.min(dists[i, :] - r)\n            allowed = min(allowed, neighbors_limit)\n            allowed = max(allowed, 0.0)\n            if allowed > r[i] + 1e-12:\n                # damped increase to avoid oscillations\n                r[i] = 0.5 * (r[i] + allowed)\n\n        # enforce border safety and non-negativity\n        r = np.minimum(r, border)\n        r = np.maximum(r, 0.0)\n\n        if np.max(np.abs(r - prev)) < tol:\n            break\n\n    return r\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nKey features: Alternative approach to validity, Alternative approach to sum_radii\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 0.4167, Type: Experimental)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles using a hex-grid + greedy selection.\nThis explicit constructor builds candidate centers on a hexagonal lattice inside the unit\nsquare, then greedily selects 26 centers maximizing the radius each would get given the\nalready-selected circles (respecting walls). A short iterative refinement on radii\nenforces non-overlap more tightly.\n\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    n = 26\n\n    # Build hexagonal lattice of candidate centers (dense enough to choose 26 good spots)\n    s = 0.105  # hex spacing; tuned small to give many candidates\n    dy = s * np.sqrt(3) / 2.0\n    cand = []\n    row = 0\n    y = 0.0\n    while y <= 1.0 + 1e-12:\n        x0 = 0.5 * s if (row % 2) else 0.0\n        x = x0\n        while x <= 1.0 + 1e-12:\n            if 0.0 <= x <= 1.0 and 0.0 <= y <= 1.0:\n                cand.append((x, y))\n            x += s\n        row += 1\n        y = row * dy\n    cand = np.array(cand)\n    m = cand.shape[0]\n\n    # Precompute distance to walls for each candidate\n    wall_d = np.minimum.reduce([cand[:, 0], cand[:, 1], 1.0 - cand[:, 0], 1.0 - cand[:, 1]])\n\n    # Greedy selection: pick the candidate that can have the largest radius given already selected circles\n    selected_idx = []\n    selected_centers = []\n    selected_radii = []\n\n    available = set(range(m))\n    for k in range(n):\n        best_i = None\n        best_r = -1.0\n        for i in list(available):\n            # initial limit is distance to walls\n            r_i = wall_d[i]\n            if r_i <= 0:\n                continue\n            ci = cand[i]\n            ok = True\n            # limit by already selected circles: r_i <= dist(ci, cj) - rj\n            for cj, rj in zip(selected_centers, selected_radii):\n                d = np.hypot(ci[0] - cj[0], ci[1] - cj[1])\n                limit = d - rj\n                if limit <= 0:\n                    ok = False\n                    break\n                if limit < r_i:\n                    r_i = limit\n            if not ok:\n                continue\n            # keep candidate with largest feasible radius\n            if r_i > best_r:\n                best_r = r_i\n                best_i = i\n        if best_i is None:\n            # If no available candidate fits, we relax by allowing small positive radii near zero\n            break\n        selected_idx.append(best_i)\n        selected_centers.append(tuple(cand[best_i]))\n        selected_radii.append(best_r)\n        available.remove(best_i)\n\n    # If greedy finished early (rare), fill remaining by placing small circles near center\n    while len(selected_centers) < n:\n        selected_centers.append((0.5, 0.5))\n        selected_radii.append(1e-6)\n\n    centers = np.array(selected_centers[:n])\n    radii = np.maximum(np.array(selected_radii[:n]), 0.0)\n\n    # Iterative refinement of radii to enforce pairwise non-overlap and wall constraints.\n    # We set r_i <- min(wall_dist_i, min_j (d_ij - r_j)) using previous iterate r_j.\n    sel_wall = np.minimum.reduce([centers[:, 0], centers[:, 1], 1.0 - centers[:, 0], 1.0 - centers[:, 1]])\n    r = radii.copy()\n    for it in range(500):\n        r_new = r.copy()\n        max_change = 0.0\n        for i in range(n):\n            limits = [sel_wall[i]]\n            ci = centers[i]\n            for j in range(n):\n                if i == j:\n                    continue\n                d = np.hypot(ci[0] - centers[j, 0], ci[1] - centers[j, 1])\n                limits.append(d - r[j])\n            desired = min(limits)\n            if desired < 0:\n                desired = 0.0\n            # don't grow more than 1.5x in one iteration to help stability\n            allowed = r[i] * 1.5 if r[i] > 0 else desired\n            newri = min(desired, allowed) if desired > r[i] else desired\n            max_change = max(max_change, abs(newri - r[i]))\n            r_new[i] = newri\n        r = r_new\n        if max_change < 1e-6:\n            break\n\n    radii = r\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nUnique approach: Modification: Full rewrite, Excellent validity (1.000), Excellent sum_radii (1.098)\n\n### Inspiration 2 (Score: 0.3642, Type: Exploratory)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square\n    that attempts to maximize the sum of their radii.\n\n    Returns:\n        Tuple of (centers, radii, sum_of_radii)\n        centers: np.array of shape (26, 2) with (x, y) coordinates\n        radii: np.array of shape (26) with radius of each circle\n        sum_of_radii: Sum of all radii\n    \"\"\"\n    # Initialize arrays for 26 circles\n    n = 26\n    centers = np.zeros((n, 2))\n\n    # Place circles in a structured pattern\n    # This is a simple pattern - evolution will improve this\n\n    # First, place a large circle in the center\n    centers[0] = [0.5, 0.5]\n\n    # Place 8 circles around it in a ring\n    for i in range(8):\n        angle = 2 * np.pi * i / 8\n        centers[i + 1] = [0.5 + 0.3 * np.cos(angle), 0.5 + 0.3 * np.sin(angle)]\n\n    # Place 16 more circles in an outer ring\n    for i in range(16):\n        angle = 2 * np.pi * i / 16\n        centers[i + 9] = [0.5 + 0.7 * np.cos(angle), 0.5 + 0.7 * np.sin(angle)]\n\n    # Additional positioning adjustment to make sure all circles\n    # are inside the square and don't overlap\n    # Clip to ensure everything is inside the unit square\n    centers = np.clip(centers, 0.01, 0.99)\n\n    # Compute maximum valid radii for this configuration\n    radii = compute_max_radii(centers)\n\n    # Calculate the sum of radii\n    sum_radii = np.sum(radii)\n\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers):\n    \"\"\"\n    Compute the maximum possible radii for each circle position\n    such that they don't overlap and stay within the unit square.\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n\n    Returns:\n        np.array of shape (n) with radius of each circle\n    \"\"\"\n    n = centers.shape[0]\n    radii = np.ones(n)\n\n    # First, limit by distance to square borders\n    for i in range(n):\n        x, y = centers[i]\n        # Distance to borders\n        radii[i] = min(x, y, 1 - x, 1 - y)\n\n    # Then, limit by distance to other circles\n    # Each pair of circles with centers at distance d can have\n    # sum of radii at most d to avoid overlap\n    for i in range(n):\n        for j in range(i + 1, n):\n            dist = np.sqrt(np.sum((centers[i] - centers[j]) ** 2))\n\n            # If current radii would cause overlap\n            if radii[i] + radii[j] > dist:\n                # Scale both radii proportionally\n                scale = dist / (radii[i] + radii[j])\n                radii[i] *= scale\n                radii[j] *= scale\n\n    return radii\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.5)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\")\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii}\")\n    # AlphaEvolve improved this to 2.635\n\n    # Uncomment to visualize:\n    visualize(centers, radii)\n\n```\nUnique approach: Excellent validity (1.000), Excellent sum_radii (0.960), NumPy-based implementation\n\n### Inspiration 3 (Score: 0.8287, Type: High-Performer)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (explicit hex-like layout + iterative radius balancing)\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square\n    and compute near-maximal non-overlapping radii for the fixed centers.\n    Returns: (centers, radii, sum_of_radii)\n    \"\"\"\n    n = 26\n\n    # Row-wise hex-like arrangement with counts summing to 26\n    row_counts = [5, 6, 5, 5, 5]  # total 26\n    max_cols = max(row_counts)\n    margin = 0.02\n\n    # choose horizontal spacing so the widest row fits inside [margin,1-margin]\n    if max_cols > 1:\n        a = (1.0 - 2 * margin) / (max_cols - 1)\n    else:\n        a = 0.2\n    v = a * np.sqrt(3) / 2.0  # vertical spacing for hex packing\n\n    total_rows = len(row_counts)\n    height_used = (total_rows - 1) * v\n    y0 = margin + max(0.0, (1.0 - 2 * margin - height_used) / 2.0)\n\n    centers = []\n    for r, count in enumerate(row_counts):\n        y = y0 + r * v\n        # center each row horizontally\n        row_width = (count - 1) * a\n        x_start = 0.5 - row_width / 2.0\n        # offset every other row for hex-like staggering\n        if (r % 2) == 1:\n            x_start += a / 2.0\n        for c in range(count):\n            x = x_start + c * a\n            # clip to unit square with small tolerance\n            x = min(max(x, margin), 1 - margin)\n            y = min(max(y, margin), 1 - margin)\n            centers.append([x, y])\n\n    centers = np.array(centers[:n])\n    # Ensure we have exactly n centers\n    if centers.shape[0] < n:\n        # pad with center points if necessary (shouldn't happen)\n        while centers.shape[0] < n:\n            centers = np.vstack([centers, [0.5, 0.5]])\n    elif centers.shape[0] > n:\n        centers = centers[:n]\n\n    radii = compute_max_radii(centers)\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers, max_iter=2000, tol=1e-6):\n    \"\"\"\n    Iterative balancing: start from border-limited radii and iteratively\n    enforce pairwise non-overlap (shrink) and local growth (where possible).\n    \"\"\"\n    n = centers.shape[0]\n    # precompute pairwise distances\n    diff = centers[:, None, :] - centers[None, :, :]\n    dists = np.sqrt((diff ** 2).sum(axis=2)) + np.eye(n)  # add diag nonzero to avoid zero\n    # border distances\n    border = np.minimum.reduce([centers[:, 0], centers[:, 1],\n                                1 - centers[:, 0], 1 - centers[:, 1]])\n    border = np.maximum(border, 0.0)\n\n    # start with border distances (cannot exceed border)\n    r = border.copy()\n\n    for it in range(max_iter):\n        prev = r.copy()\n\n        # Pairwise shrink pass: ensure r_i + r_j <= dist_ij (with small safety)\n        for i in range(n):\n            for j in range(i + 1, n):\n                dij = dists[i, j]\n                # effective allowed sum (tiny safety margin)\n                allowed = max(0.0, dij - 1e-12)\n                sumr = r[i] + r[j]\n                if sumr > allowed and sumr > 0:\n                    scale = allowed / sumr\n                    # scale both radii proportionally but don't exceed borders\n                    r[i] *= scale\n                    r[j] *= scale\n\n        # Growth pass: for each circle, if there's slack given others fixed, try to grow (damped)\n        for i in range(n):\n            # max allowed by borders\n            allowed = border[i]\n            # max allowed by neighbors: r_i <= dist_ij - r_j for all j != i\n            # compute minimal (dist_ij - r_j)\n            neighbors_limit = np.min(dists[i, :] - r)\n            allowed = min(allowed, neighbors_limit)\n            allowed = max(allowed, 0.0)\n            if allowed > r[i] + 1e-12:\n                # damped increase to avoid oscillations\n                r[i] = 0.5 * (r[i] + allowed)\n\n        # enforce border safety and non-negativity\n        r = np.minimum(r, border)\n        r = np.maximum(r, 0.0)\n\n        if np.max(np.abs(r - prev)) < tol:\n            break\n\n    return r\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nUnique approach: Modification: Full rewrite, Excellent validity (1.000), Excellent sum_radii (2.184)\n\n# Current Program\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for packing 26 circles in the unit square.\n\nStrategy:\n - Generate several hexagonal-lattice candidate center sets with different spacings.\n - For each spacing that produces >=26 candidates, pick the 26 centers closest to the square center.\n - Compute feasible radii as the minimum of (distance-to-border) and (half nearest-neighbor distance).\n - Choose the spacing that yields the largest sum of radii.\nThis yields a compact, near-hexagonal center cluster that improves total radii.\n\"\"\"\nimport numpy as np\n\ndef _hex_lattice(spacing):\n    dx = spacing\n    dy = spacing * np.sqrt(3) / 2.0\n    margin = spacing / 2.0\n    pts = []\n    y = margin\n    row = 0\n    while y <= 1.0 - margin + 1e-12:\n        x0 = margin + (0.5 * dx if (row % 2 == 1) else 0.0)\n        x = x0\n        while x <= 1.0 - margin + 1e-12:\n            pts.append((x, y))\n            x += dx\n        row += 1\n        y += dy\n    return np.array(pts)\n\ndef _compute_radii(centers):\n    n = centers.shape[0]\n    # distance to border\n    dist_border = np.minimum.reduce([centers[:,0], centers[:,1], 1-centers[:,0], 1-centers[:,1]])\n    # pairwise distances (mask diagonal)\n    diff = centers[:, None, :] - centers[None, :, :]\n    dists = np.sqrt(np.sum(diff**2, axis=2))\n    np.fill_diagonal(dists, np.inf)\n    nearest = np.min(dists, axis=1)\n    radii = np.minimum(dist_border, 0.5 * nearest)\n    # safety\n    radii = np.maximum(radii, 1e-8)\n    return radii\n\ndef construct_packing():\n    target_n = 26\n    center_point = np.array([0.5, 0.5])\n\n    # Try a range of spacings; include fine and coarse to trade count vs size\n    spacings = np.linspace(0.06, 0.20, 30)  # finer resolution to find good spacing\n    best = None\n    best_sum = -1.0\n\n    for a in spacings:\n        pts = _hex_lattice(a)\n        if pts.shape[0] < target_n:\n            continue\n        # choose the 26 closest to center (favors larger feasible radii)\n        idx = np.argsort(np.linalg.norm(pts - center_point, axis=1))[:target_n]\n        centers = pts[idx]\n        radii = _compute_radii(centers)\n        s = float(np.sum(radii))\n        if s > best_sum:\n            best_sum = s\n            best = (centers.copy(), radii.copy(), s)\n\n    # Fallback: if none spacing produced enough points, use a dense lattice and pick best 26\n    if best is None:\n        pts = _hex_lattice(0.055)\n        idx = np.argsort(np.linalg.norm(pts - center_point, axis=1))[:target_n]\n        centers = pts[idx]\n        radii = _compute_radii(centers)\n        best = (centers, radii, float(np.sum(radii)))\n\n    return best\n\n# EVOLVE-BLOCK-END\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor='k', linewidth=0.5)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\n\n# Task\nRewrite the program to improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: complexity, diversity\nDifferent solutions with similar fitness but different features are valuable.\nProvide the complete new program code.\n\nIMPORTANT: Make sure your rewritten program maintains the same inputs and outputs\nas the original program, but with improved internal implementation.\n\n```python\n# Your rewritten program here\n```", "responses": ["```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for packing 26 circles in the unit square.\n\nStrategy:\n - Place 26 centers in 5 rows with counts [5,6,5,5,5] (a near-hexagonal cluster).\n - Search over horizontal spacing (s) and a small odd-row offset to break perfect symmetry.\n - For each candidate set of centers compute feasible radii = min(distance-to-border, 0.5*nearest-neighbor).\n - Return the configuration with maximal sum of radii.\nThis explicit constructor is fast, deterministic, and yields a compact near-hexagonal packing.\n\"\"\"\nimport numpy as np\n\ndef _compute_radii(centers):\n    # distance to border\n    db = np.minimum.reduce([centers[:,0], centers[:,1], 1-centers[:,0], 1-centers[:,1]])\n    # pairwise distances\n    diff = centers[:,None,:] - centers[None,:,:]\n    d = np.sqrt((diff**2).sum(axis=2))\n    np.fill_diagonal(d, np.inf)\n    nearest = np.min(d, axis=1)\n    r = np.minimum(db, 0.5*nearest)\n    return np.maximum(r, 1e-12)\n\ndef construct_packing():\n    target_n = 26\n    rows = [5,6,5,5,5]\n    best = None\n    best_sum = -1.0\n    sqrt3 = np.sqrt(3.0)\n\n    # search over horizontal spacing and small odd-row offset to improve packing\n    for s in np.linspace(0.078, 0.18, 62):  # spacing candidates\n        v = s * sqrt3 / 2.0\n        total_h = max(rows) - 1\n        total_height = (len(rows)-1) * v\n        y0 = 0.5 - total_height/2.0\n\n        # try slight offsets for odd rows (to break symmetry and better use square edges)\n        for off in np.linspace(-0.03, 0.03, 13):\n            centers = []\n            ok = True\n            for ri, cnt in enumerate(rows):\n                y = y0 + ri * v\n                row_width = (cnt - 1) * s\n                x_start = 0.5 - row_width/2.0\n                # apply odd-row micro-offset\n                if (ri % 2) == 1:\n                    x_start += off + s/2.0\n                for c in range(cnt):\n                    x = x_start + c * s\n                    # if any center falls outside, mark invalid candidate\n                    if not (0.0 <= x <= 1.0 and 0.0 <= y <= 1.0):\n                        ok = False\n                        break\n                    centers.append((x,y))\n                if not ok:\n                    break\n            if not ok or len(centers) != target_n:\n                continue\n            centers = np.array(centers)\n            radii = _compute_radii(centers)\n            ssum = float(radii.sum())\n            if ssum > best_sum:\n                best_sum = ssum\n                best = (centers.copy(), radii.copy(), ssum)\n\n    # fallback: dense hex-lattice cluster near center (guarantee feasibility)\n    if best is None:\n        dx = 0.09\n        dy = dx * np.sqrt(3)/2.0\n        pts = []\n        margin = dx/2.0\n        row = 0\n        y = margin\n        while y <= 1 - margin + 1e-12:\n            x0 = margin + (0.5*dx if row%2 else 0.0)\n            x = x0\n            while x <= 1 - margin + 1e-12:\n                pts.append((x,y))\n                x += dx\n            row += 1\n            y += dy\n        pts = np.array(pts)\n        center = np.array([0.5,0.5])\n        idx = np.argsort(np.linalg.norm(pts-center,axis=1))[:target_n]\n        centers = pts[idx]\n        radii = _compute_radii(centers)\n        best = (centers, radii, float(radii.sum()))\n\n    return best\n\n# EVOLVE-BLOCK-END\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor='k', linewidth=0.5)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # visualize(centers, radii)\n```"]}}, "artifacts_json": "{\"stdout\": \"Running in subprocess, Python version: 3.12.1 (v3.12.1:2305ca5144, Dec  7 2023, 17:23:38) [Clang 13.0.0 (clang-1300.0.29.30)]\\nProgram path: /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmpy64ot6nt.py\\nCalling run_packing()...\\nrun_packing() returned successfully: sum_radii = 1.9879488614197967\\nResults saved to /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmp4eusfmjw.py.results\\n\", \"stderr\": \"\"}", "artifact_dir": null}