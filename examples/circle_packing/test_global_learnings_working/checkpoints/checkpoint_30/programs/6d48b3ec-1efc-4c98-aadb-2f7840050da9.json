{"id": "6d48b3ec-1efc-4c98-aadb-2f7840050da9", "code": "# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for 26 circles in the unit square.\nLayout: a compact near-hex block with rows [5,6,5,5,5]. For a range of edge\nmargins we build centers and maximize radii for fixed centers via a\nGauss-Seidel style solver that enforces r_i <= border_i and r_i <= d_ij - r_j.\nThe best configuration (largest sum of radii) is returned.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    cols_per_row = [5, 6, 5, 5, 5]  # sums to 26\n    rows = len(cols_per_row)\n\n    def make_centers(margin):\n        available_w = 1.0 - 2.0 * margin\n        if available_w <= 0:\n            return None\n        # pick an average horizontal step consistent with rows\n        steps = []\n        for cnt in cols_per_row:\n            if cnt > 1:\n                steps.append(available_w / (cnt - 1))\n        avg_step = float(np.mean(steps)) if steps else 0.5 * available_w\n        h = avg_step * np.sqrt(3.0) / 2.0  # hex vertical spacing\n        total_h = h * (rows - 1)\n        y0 = 0.5 - total_h / 2.0\n        centers = []\n        for r, cnt in enumerate(cols_per_row):\n            if cnt == 1:\n                xs = np.array([0.5])\n            else:\n                xs = margin + np.linspace(0.0, available_w, cnt)\n            # stagger odd rows\n            if (r % 2 == 1) and cnt > 1:\n                step = xs[1] - xs[0]\n                xs = xs + 0.5 * step\n            xs = np.clip(xs, margin, 1.0 - margin)\n            y = y0 + r * h\n            for x in xs:\n                centers.append((x, y))\n        C = np.array(centers, dtype=float)\n        if C.shape[0] != n:\n            return None\n        if C.min() < -1e-12 or C.max() > 1.0 + 1e-12:\n            return None\n        return C\n\n    def maximize_radii(C, tol=1e-10, max_iter=2000):\n        n = C.shape[0]\n        border = np.minimum.reduce([C[:,0], C[:,1], 1.0 - C[:,0], 1.0 - C[:,1]]).astype(float)\n        diff = C[:, None, :] - C[None, :, :]\n        dists = np.sqrt((diff**2).sum(axis=2))\n        np.fill_diagonal(dists, np.inf)\n        # start with border-limited radii\n        r = border.copy()\n        for it in range(max_iter):\n            prev = r.copy()\n            # Gauss-Seidel pass\n            for i in range(n):\n                ub = border[i]\n                # compute d_ij - r_j for all j != i\n                vals = dists[i] - r\n                vals[i] = np.inf\n                m = float(np.min(vals))\n                if m < ub:\n                    ub = m\n                r[i] = max(0.0, ub)\n            if np.max(np.abs(r - prev)) < tol:\n                break\n        # safety: fix any tiny overlaps by proportional scaling pairwise\n        for i in range(n):\n            for j in range(i+1, n):\n                d = dists[i,j]\n                if d <= 1e-12:\n                    r[i] = r[j] = 0.0\n                elif r[i] + r[j] > d:\n                    s = d / (r[i] + r[j])\n                    r[i] *= s\n                    r[j] *= s\n        r = np.clip(r, 0.0, border)\n        return r\n\n    best_sum = -1.0\n    best_C = None\n    best_r = None\n    # sample margins focused on small margins (to get larger interior radii but avoid clipping)\n    for margin in np.linspace(0.02, 0.12, 41):\n        C = make_centers(margin)\n        if C is None:\n            continue\n        r = maximize_radii(C)\n        s = float(r.sum())\n        if s > best_sum + 1e-12:\n            best_sum = s\n            best_C = C.copy()\n            best_r = r.copy()\n\n    if best_C is None:\n        # fallback conservative layout\n        margin = 0.07\n        best_C = make_centers(margin)\n        best_r = maximize_radii(best_C)\n\n    return best_C, best_r, float(np.sum(best_r))\n\n# EVOLVE-BLOCK-END\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(7, 7))\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    for i, (c, r) in enumerate(zip(centers, radii)):\n        ax.add_patch(Circle(c, r, alpha=0.6, edgecolor=\"k\"))\n        ax.text(c[0], c[1], str(i), ha=\"center\", va=\"center\", fontsize=7)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # visualize(centers, radii)", "language": "python", "parent_id": "ac1d42d5-76a9-4cd7-9eb5-43caeef44398", "generation": 3, "timestamp": 1759957335.134709, "iteration_found": 29, "metrics": {"validity": 1.0, "sum_radii": 2.254931588374731, "target_ratio": 0.8557615136146988, "combined_score": 0.8557615136146988, "eval_time": 0.06862783432006836}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Full rewrite", "parent_metrics": {"validity": 1.0, "sum_radii": 2.1847364177250435, "target_ratio": 0.8291219801613069, "combined_score": 0.8291219801613069, "eval_time": 1.2916259765625}, "island": 0}, "prompts": {"full_rewrite_user": {"system": "You are an expert mathematician specializing in circle packing problems and computational geometry. Your task is to improve a constructor function that directly produces a specific arrangement of 26 circles in a unit square, maximizing the sum of their radii. The AlphaEvolve paper achieved a sum of 2.635 for n=26.\n\nKey geometric insights:\n- Circle packings often follow hexagonal patterns in the densest regions\n- Maximum density for infinite circle packing is pi/(2*sqrt(3)) \u2248 0.9069\n- Edge effects make square container packing harder than infinite packing\n- Circles can be placed in layers or shells when confined to a square\n- Similar radius circles often form regular patterns, while varied radii allow better space utilization\n- Perfect symmetry may not yield the optimal packing due to edge effects\n\nFocus on designing an explicit constructor that places each circle in a specific position, rather than an iterative search algorithm.\n", "user": "# Current Program Information\n- Fitness: 0.8291\n- Feature coordinates: No feature coordinates\n- Focus areas: - Fitness improved: 0.8286 \u2192 0.8291\n- Consider simplifying - code length exceeds 500 characters\n\n## Last Execution Output\n\n### stdout\n```\nRunning in subprocess, Python version: 3.12.1 (v3.12.1:2305ca5144, Dec  7 2023, 17:23:38) [Clang 13.0.0 (clang-1300.0.29.30)]\nProgram path: /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmpbt9a0ttq.py\nCalling run_packing()...\nrun_packing() returned successfully: sum_radii = 2.1847364177250435\nResults saved to /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmptrhwd1gc.py.results\n\n```\n\n### stderr\n```\n\n```\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 3\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.1833, target_ratio: 0.8286, combined_score: 0.8286, eval_time: 0.4969\n- Outcome: Mixed results\n\n### Attempt 2\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.1847, target_ratio: 0.8291, combined_score: 0.8291, eval_time: 1.2916\n- Outcome: Improvement in all metrics\n\n### Attempt 1\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.2549, target_ratio: 0.8558, combined_score: 0.8558, eval_time: 0.0646\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.8558)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (margin search + Gauss-Seidel radius maximizer)\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    \"\"\"\n    Build 26 circle centers in a staggered near-hex layout and maximize radii\n    for fixed centers using a Gauss-Seidel style solver that enforces:\n      r_i <= border_i,  r_i <= d_ij - r_j  for all j!=i\n    We sample several margins to mitigate edge effects and pick the best sum.\n    \"\"\"\n    n = 26\n    cols_per_row = [5, 6, 5, 5, 5]  # sums to 26\n    rows = len(cols_per_row)\n\n    def make_centers(margin):\n        available_w = 1.0 - 2.0 * margin\n        if available_w <= 0:\n            return None\n        centers = []\n        # compute horizontal steps per row, then choose vertical hex-like step\n        steps = []\n        for cnt in cols_per_row:\n            if cnt > 1:\n                steps.append(available_w / (cnt - 1))\n        if len(steps) == 0:\n            avg_step = 0.5 * available_w\n        else:\n            avg_step = float(np.mean(steps))\n        h = avg_step * np.sqrt(3.0) / 2.0 if avg_step > 0 else 0.0\n\n        # vertical placement centered\n        total_h = h * (rows - 1)\n        y0 = 0.5 - total_h / 2.0\n        for r, cnt in enumerate(cols_per_row):\n            if cnt == 1:\n                xs = np.array([0.5])\n            else:\n                xs = margin + np.linspace(0.0, available_w, cnt)\n            if r % 2 == 1 and cnt > 1:\n                step = xs[1] - xs[0]\n                xs = xs + 0.5 * step\n            xs = np.clip(xs, margin, 1.0 - margin)\n            y = y0 + r * h\n            for x in xs:\n                centers.append((x, y))\n        C = np.array(centers)\n        # if any center lies outside [0,1], reject this margin\n        if (C.min() < 0.0) or (C.max() > 1.0):\n            return None\n        return C\n\n    def maximize_radii(C, tol=1e-9, max_iter=500):\n        n = C.shape[0]\n        # border limits\n        border = np.minimum.reduce([C[:,0], C[:,1], 1.0 - C[:,0], 1.0 - C[:,1]]).astype(float)\n        # pairwise distances\n        diff = C[:, None, :] - C[None, :, :]\n        dists = np.sqrt((diff**2).sum(axis=2))\n        np.fill_diagonal(dists, np.inf)\n\n        # initial radii: border-limited\n        r = border.copy()\n        for it in range(max_iter):\n            prev = r.copy()\n            # Gauss-Seidel pass: set each r_i to its tightest feasible upper bound\n            for i in range(n):\n                ub = border[i]\n                # compute d_ij - r_j for all j != i\n                vals = dists[i] - r\n                vals[i] = np.inf\n                ub = min(ub, float(np.min(vals)))\n                r[i] = max(0.0, ub)\n            if np.max(np.abs(r - prev)) < tol:\n                break\n\n        # final safety: fix any slight overlaps by proportional scaling per violating pair\n        for i in range(n):\n            for j in range(i+1, n):\n                d = dists[i,j]\n                if d <= 1e-12:\n                    r[i] = r[j] = 0.0\n                elif r[i] + r[j] > d:\n                    s = d / (r[i] + r[j])\n                    r[i] *= s\n                    r[j] *= s\n        return r\n\n    best_sum = -1.0\n    best_C = None\n    best_r = None\n    # sample margins (denser near small margins)\n    for margin in np.linspace(0.02, 0.12, 21):\n        C = make_centers(margin)\n        if C is None or C.shape[0] != n:\n            continue\n        r = maximize_radii(C)\n        s = float(r.sum())\n        if s > best_sum:\n            best_sum = s\n            best_C = C\n            best_r = r\n\n    # fallback: if none found (shouldn't happen), use a conservative center layout\n    if best_C is None:\n        margin = 0.06\n        best_C = make_centers(margin)\n        best_r = maximize_radii(best_C)\n\n    return best_C, best_r, float(np.sum(best_r))\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.2549), Performs well on target_ratio (0.8558), Performs well on combined_score (0.8558), Performs well on eval_time (0.0646)\n\n### Program 2 (Score: 0.8291)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for 26 circles in the unit square.\n\nApproach:\n- Build a truncated hex/triangular lattice skeleton with row counts that sum to 26.\n- Deterministically scale and translate the skeleton into the unit square,\n  scanning over a grid of spacings and translations.\n- For each candidate center set compute maximal feasible radii by a robust\n  greedy relaxation: initialize by border-limits and iteratively enforce\n  ri + rj <= d_ij by reducing the larger circle when violated (tightest\n  pairs processed first). This is simple, deterministic and performs well.\n\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    n = 26\n    # Row layout that sums to 26 and resembles a truncated hex block\n    rows = [4, 5, 6, 5, 4, 2]  # sum = 26\n\n    # Build relative hex-like coordinates (unit lattice)\n    rel = []\n    h = np.sqrt(3.0) / 2.0\n    for r, k in enumerate(rows):\n        y = r * h\n        offset = 0.5 if (r % 2 == 1) else 0.0\n        xs = np.arange(k) + offset\n        for x in xs:\n            rel.append((x, y))\n    rel = np.array(rel, dtype=float)\n\n    # Normalize so min corner at (0,0) for easy scaling/translation\n    rel -= rel.min(axis=0)\n    Xmax, Ymax = rel.max(axis=0)\n\n    # Search grid parameters (deterministic)\n    # spacing range: choose values that can plausibly fit in [0,1]\n    s_min = 0.06\n    # upper bound based on fitting into unit square with zero margin\n    s_max_possible = min(1.0 / max(1.0, Xmax), 1.0 / max(1.0, Ymax))\n    s_max = min(0.22, s_max_possible * 0.98)\n    s_vals = np.linspace(s_min, s_max, 75)\n\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    # For each spacing try a small grid of translations (mx,my)\n    for s in s_vals:\n        W = s * Xmax\n        H = s * Ymax\n        if W > 1.0 + 1e-12 or H > 1.0 + 1e-12:\n            continue\n        # allowable translation ranges so centers lie in [0,1]\n        max_mx = 1.0 - W\n        max_my = 1.0 - H\n        # sample small grid including centered placement\n        cx = 0.5 * max_mx\n        cy = 0.5 * max_my\n        # Build translation candidates: include edges, center, and a couple of intermediate offsets\n        t_x_vals = np.linspace(0.0, max_mx, min(6, max(2, int(1 + max_mx / (0.02 + 1e-12)))))\n        t_y_vals = np.linspace(0.0, max_my, min(6, max(2, int(1 + max_my / (0.02 + 1e-12)))))\n\n        for mx in t_x_vals:\n            for my in t_y_vals:\n                centers = rel * s + np.array([mx, my])\n                # numerical safety: ensure inside [0,1]\n                if centers.min() < -1e-12 or centers.max() > 1.0 + 1e-12:\n                    continue\n\n                radii = compute_max_radii_relax(centers)\n                ssum = radii.sum()\n                if ssum > best_sum + 1e-12:\n                    best_sum = ssum\n                    best_centers = centers.copy()\n                    best_radii = radii.copy()\n\n    # Fallback (shouldn't happen) - center a default scaled layout\n    if best_centers is None:\n        s = 0.12\n        mx = 0.5 * (1.0 - s * Xmax)\n        my = 0.5 * (1.0 - s * Ymax)\n        best_centers = rel * s + np.array([mx, my])\n        best_radii = compute_max_radii_relax(best_centers)\n        best_sum = float(best_radii.sum())\n\n    return best_centers, best_radii, float(best_sum)\n\n\ndef compute_max_radii_relax(centers, tol=1e-9, max_iter=800):\n    \"\"\"\n    Greedy relaxation to get large feasible radii for fixed centers.\n\n    - Initialize radii to distance-to-border.\n    - Precompute pairwise distances and process pairs from smallest to largest.\n    - If ri + rj > d_ij, reduce the larger radius so ri + rj = d_ij (reducing the larger\n      preserves flexibility of smaller ones). Iterate until no meaningful change.\n    This deterministic strategy often produces near-optimal sums for fixed centers.\n    \"\"\"\n    n = centers.shape[0]\n    # border-limited initial radii\n    border = np.minimum.reduce([centers[:, 0], centers[:, 1], 1.0 - centers[:, 0], 1.0 - centers[:, 1]])\n    radii = border.copy()\n\n    # pairwise distances\n    diff = centers[:, None, :] - centers[None, :, :]\n    dmat = np.sqrt(np.sum(diff * diff, axis=2))\n    # set diagonal to inf for convenience\n    np.fill_diagonal(dmat, np.inf)\n\n    # build sorted list of pairs by distance (tight constraints first)\n    idx_i, idx_j = np.triu_indices(n, k=1)\n    pairs = list(zip(idx_i.tolist(), idx_j.tolist(), dmat[idx_i, idx_j].tolist()))\n    pairs.sort(key=lambda t: t[2])\n\n    # iterative enforcement\n    for it in range(max_iter):\n        changed = False\n        max_delta = 0.0\n        for (i, j, dij) in pairs:\n            # numerical protection\n            if not np.isfinite(dij) or dij <= 0.0:\n                # coincident or extremely close centers -> force one small if necessary\n                if radii[i] + radii[j] > 1e-12:\n                    if radii[i] >= radii[j]:\n                        new_i = max(0.0, dij - radii[j])\n                        delta = radii[i] - new_i\n                        if delta > tol:\n                            radii[i] = new_i\n                            changed = True\n                            max_delta = max(max_delta, delta)\n                    else:\n                        new_j = max(0.0, dij - radii[i])\n                        delta = radii[j] - new_j\n                        if delta > tol:\n                            radii[j] = new_j\n                            changed = True\n                            max_delta = max(max_delta, delta)\n                continue\n\n            if radii[i] + radii[j] > dij + 1e-12:\n                # reduce the larger one to satisfy constraint\n                if radii[i] > radii[j]:\n                    new_i = max(0.0, dij - radii[j])\n                    delta = radii[i] - new_i\n                    if delta > tol:\n                        radii[i] = new_i\n                        changed = True\n                        max_delta = max(max_delta, delta)\n                else:\n                    new_j = max(0.0, dij - radii[i])\n                    delta = radii[j] - new_j\n                    if delta > tol:\n                        radii[j] = new_j\n                        changed = True\n                        max_delta = max(max_delta, delta)\n\n        # ensure border constraints\n        over_border = radii > border\n        if np.any(over_border):\n            radii[over_border] = border[over_border]\n            changed = True\n\n        if not changed or max_delta < tol:\n            break\n\n    # Final safety: small proportional scaling if any tiny overlaps remain\n    for i in range(n):\n        for j in range(i + 1, n):\n            d = dmat[i, j]\n            if d < 1e-12:\n                # coincident, zero both\n                radii[i] = 0.0\n                radii[j] = 0.0\n            elif radii[i] + radii[j] > d:\n                s = d / (radii[i] + radii[j])\n                radii[i] *= s\n                radii[j] *= s\n\n    # clamp non-negativity and border limits\n    radii = np.clip(radii, 0.0, border)\n    return radii\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(7, 7))\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    for i, (c, r) in enumerate(zip(centers, radii)):\n        circ = Circle(c, r, alpha=0.6, edgecolor=\"k\")\n        ax.add_patch(circ)\n        ax.text(c[0], c[1], str(i), ha=\"center\", va=\"center\", fontsize=7)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # To inspect visually, uncomment:\n    # visualize(centers, radii)\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.1847), Performs well on target_ratio (0.8291), Performs well on combined_score (0.8291), Performs well on eval_time (1.2916)\n\n### Program 3 (Score: 0.8286)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Improved explicit constructor for packing 26 circles in the unit square.\nUses a layered hex-like layout [4,5,6,6,5] and searches a small parameter\nspace (spacing, vertical scale, stagger amplitude, small vertical shift)\nto maximize the sum of radii where each radius = min(distance to border, half nearest neighbor).\n\"\"\"\nimport numpy as np\nfrom math import sqrt\n\ndef construct_packing():\n    rows = [4, 5, 6, 6, 5]  # sums to 26\n    R = len(rows)\n    sqrt3 = sqrt(3.0)\n\n    def make_centers(s, t, a, ty):\n        # vertical spacing scaled by t\n        h = s * sqrt3 / 2.0 * t\n        y0 = 0.5 - (R - 1) * h / 2.0 + ty\n        centers = []\n        for r, L in enumerate(rows):\n            y = y0 + r * h\n            # stagger odd rows by a*s (can be negative)\n            offset = (a * s) if (r % 2 == 1) else 0.0\n            x0 = 0.5 - (L - 1) * s / 2.0 + offset\n            xs = x0 + np.arange(L) * s\n            for x in xs:\n                centers.append([x, y])\n        C = np.array(centers, dtype=float)\n        return C\n\n    def score_config(C):\n        # must be strictly inside unit square (allow touching)\n        if C.min() < -1e-9 or C.max() > 1.0 + 1e-9:\n            return -1e9, None, None\n        # distance to border\n        db = np.minimum.reduce([C[:,0], C[:,1], 1 - C[:,0], 1 - C[:,1]])\n        # pairwise distances\n        dif = C[:, None, :] - C[None, :, :]\n        d = np.sqrt((dif**2).sum(axis=2))\n        np.fill_diagonal(d, np.inf)\n        nearest = d.min(axis=1)\n        r = np.minimum(db, 0.5 * nearest)\n        r = np.maximum(r, 0.0)\n        return float(r.sum()), C, r\n\n    # coarse search ranges\n    s_vals = np.linspace(0.06, 0.30, 40)   # inter-center spacing\n    t_vals = np.linspace(0.92, 1.08, 6)    # vertical scale (allow slight anisotropy)\n    a_vals = np.linspace(-0.35, 0.35, 7)   # stagger amplitude (fraction of s)\n    ty_vals = np.linspace(-0.02, 0.02, 3)  # small vertical translation to exploit border\n    best_score = -1.0\n    best_C = None\n    best_r = None\n    best_params = None\n\n    for s in s_vals:\n        for t in t_vals:\n            for a in a_vals:\n                for ty in ty_vals:\n                    C = make_centers(s, t, a, ty)\n                    sc, Cc, rc = score_config(C)\n                    if sc > best_score:\n                        best_score = sc\n                        best_C, best_r = Cc, rc\n                        best_params = (s, t, a, ty)\n\n    # local refinement around best params (finer search)\n    if best_params is not None:\n        s0, t0, a0, ty0 = best_params\n        s_vals = np.linspace(max(0.02, s0 * 0.9), min(0.45, s0 * 1.1), 30)\n        t_vals = np.linspace(max(0.85, t0 - 0.05), min(1.15, t0 + 0.05), 8)\n        a_vals = np.linspace(a0 - 0.08, a0 + 0.08, 9)\n        ty_vals = np.linspace(ty0 - 0.01, ty0 + 0.01, 5)\n        for s in s_vals:\n            for t in t_vals:\n                for a in a_vals:\n                    for ty in ty_vals:\n                        C = make_centers(s, t, a, ty)\n                        sc, Cc, rc = score_config(C)\n                        if sc > best_score:\n                            best_score = sc\n                            best_C, best_r = Cc, rc\n                            best_params = (s, t, a, ty)\n\n    # final safety: recompute precise radii (exclude self)\n    C = np.clip(best_C, 1e-12, 1.0 - 1e-12)\n    dif = C[:, None, :] - C[None, :, :]\n    d = np.sqrt((dif**2).sum(axis=2))\n    np.fill_diagonal(d, np.inf)\n    half_nearest = 0.5 * d.min(axis=1)\n    dist_border = np.minimum.reduce([C[:,0], C[:,1], 1 - C[:,0], 1 - C[:,1]])\n    radii = np.minimum(dist_border, half_nearest)\n    radii = np.maximum(radii, 0.0)\n    sum_radii = float(radii.sum())\n    return C, radii, sum_radii\n\n# EVOLVE-BLOCK-END\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor='k')\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.1833), Performs well on target_ratio (0.8286), Performs well on combined_score (0.8286), Performs well on eval_time (0.4969)\n\n\n\n## Diverse Programs\n\n### Program D1 (Score: 0.8255)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Improved explicit constructor for packing 26 circles in the unit square.\n\nThis places 26 centers in a layered near-hex pattern rows=[4,5,6,6,5] and\nperforms a two-stage parameter search (coarse then local refinement) over:\n  - s : inter-center horizontal spacing\n  - t : vertical anisotropy of hex spacing\n  - a : stagger amplitude for odd rows (fraction of s)\n  - ty: small overall vertical shift\n\nRadii are set to r_i = min(dist_to_border_i, 0.5 * nearest_neighbor_dist).\n\"\"\"\nimport numpy as np\nfrom math import sqrt\n\ndef construct_packing():\n    rows = [4, 5, 6, 6, 5]  # sums to 26\n    R = len(rows)\n    sqrt3 = sqrt(3.0)\n\n    def make_centers(s, t, a, ty):\n        # vertical spacing scaled by t\n        h = s * sqrt3 / 2.0 * t\n        y0 = 0.5 - (R - 1) * h / 2.0 + ty\n        centers = []\n        for r, L in enumerate(rows):\n            y = y0 + r * h\n            offset = (a * s) if (r % 2 == 1) else 0.0\n            x0 = 0.5 - (L - 1) * s / 2.0 + offset\n            xs = x0 + np.arange(L) * s\n            for x in xs:\n                centers.append([x, y])\n        return np.array(centers, dtype=float)\n\n    def score_config(C):\n        # quick bounds check: must lie in [0,1] (allow tiny numerical slack)\n        if C.size == 0 or C.min() < -1e-9 or C.max() > 1.0 + 1e-9:\n            return -1e9, None, None\n        # distance to borders\n        db = np.minimum.reduce([C[:,0], C[:,1], 1.0 - C[:,0], 1.0 - C[:,1]])\n        # pairwise distances\n        dif = C[:, None, :] - C[None, :, :]\n        d = np.sqrt((dif**2).sum(axis=2))\n        np.fill_diagonal(d, np.inf)\n        nearest = d.min(axis=1)\n        r = np.minimum(db, 0.5 * nearest)\n        # if any radius nonpositive, treat as invalid\n        if np.any(r <= 0):\n            return -1e9, None, None\n        return float(r.sum()), C, r\n\n    # Coarse search\n    s_vals = np.linspace(0.06, 0.28, 36)\n    t_vals = np.linspace(0.94, 1.06, 5)\n    a_vals = np.linspace(-0.32, 0.32, 9)\n    ty_vals = np.linspace(-0.03, 0.03, 5)\n\n    best_score = -1.0\n    best_C = None\n    best_r = None\n    best_params = None\n\n    for s in s_vals:\n        for t in t_vals:\n            for a in a_vals:\n                for ty in ty_vals:\n                    C = make_centers(s, t, a, ty)\n                    sc, Cc, rc = score_config(C)\n                    if sc > best_score:\n                        best_score = sc\n                        best_C, best_r = Cc, rc\n                        best_params = (s, t, a, ty)\n\n    # Local refinement around best params\n    if best_params is not None:\n        s0, t0, a0, ty0 = best_params\n        s_vals = np.linspace(max(0.02, s0 * 0.88), min(0.45, s0 * 1.12), 28)\n        t_vals = np.linspace(max(0.85, t0 - 0.06), min(1.15, t0 + 0.06), 9)\n        a_vals = np.linspace(a0 - 0.12, a0 + 0.12, 11)\n        ty_vals = np.linspace(ty0 - 0.02, ty0 + 0.02, 7)\n        for s in s_vals:\n            for t in t_vals:\n                for a in a_vals:\n                    for ty in ty_vals:\n                        C = make_centers(s, t, a, ty)\n                        sc, Cc, rc = score_config(C)\n                        if sc > best_score:\n                            best_score = sc\n                            best_C, best_r = Cc, rc\n                            best_params = (s, t, a, ty)\n\n    # Finalize best configuration\n    if best_C is None:\n        # fallback deterministic layout\n        best_C = make_centers(0.12, 1.0, 0.0, 0.0)\n        best_r = score_config(best_C)[2]\n\n    C = np.clip(best_C, 1e-12, 1.0 - 1e-12)\n    # recompute radii precisely\n    dif = C[:, None, :] - C[None, :, :]\n    d = np.sqrt((dif**2).sum(axis=2))\n    np.fill_diagonal(d, np.inf)\n    half_nearest = 0.5 * d.min(axis=1)\n    dist_border = np.minimum.reduce([C[:,0], C[:,1], 1.0 - C[:,0], 1.0 - C[:,1]])\n    radii = np.minimum(dist_border, half_nearest)\n    radii = np.maximum(radii, 0.0)\n    sum_radii = float(radii.sum())\n    return C, radii, sum_radii\n\n# EVOLVE-BLOCK-END\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor='k')\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nKey features: Alternative approach to validity, Alternative approach to sum_radii\n\n### Program D2 (Score: 0.8177)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Improved explicit constructor for packing 26 circles in the unit square.\n\nConstructs a truncated hex-like layout (rows [4,5,6,6,5]) and performs a\ndeterministic coarse + local grid search over a small parameter space:\n  - s: nominal horizontal spacing\n  - t: vertical scale (anisotropy)\n  - a: stagger fraction (fraction of s to offset alternate rows)\n  - tx, ty: small global translations\n\nFor each candidate it computes radii r_i = min(dist_to_border, 0.5*nearest_neighbor)\nand returns the centers/radii that maximize the sum of radii.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    rows = [4, 5, 6, 6, 5]  # total 26\n    R = len(rows)\n    sqrt3 = np.sqrt(3.0)\n\n    def make_centers(s, t, a_frac, tx, ty):\n        # s: horizontal step, t: vertical scale multiplier, a_frac in [-0.5,0.5]\n        h = s * sqrt3 / 2.0 * t\n        # center the block in the square before translation\n        total_h = (R - 1) * h\n        y0 = 0.5 - total_h / 2.0 + ty\n        centers = []\n        for r, L in enumerate(rows):\n            y = y0 + r * h\n            # offset odd rows by a_frac * s (can be negative)\n            offset = (a_frac * s) if (r % 2 == 1) else 0.0\n            span = (L - 1) * s\n            x0 = 0.5 - span / 2.0 + offset + tx\n            xs = x0 + np.arange(L) * s\n            for x in xs:\n                centers.append([x, y])\n        C = np.array(centers, dtype=float)\n        return C\n\n    def score_config(C):\n        # require centers inside [0,1] (allow touching)\n        if C.size == 0:\n            return -1e9, None, None\n        if C.min() < -1e-10 or C.max() > 1.0 + 1e-10:\n            return -1e9, None, None\n        # distance to border\n        db = np.minimum.reduce([C[:, 0], C[:, 1], 1.0 - C[:, 0], 1.0 - C[:, 1]])\n        # pairwise distances\n        dif = C[:, None, :] - C[None, :, :]\n        d = np.sqrt((dif ** 2).sum(axis=2))\n        np.fill_diagonal(d, np.inf)\n        nearest = d.min(axis=1)\n        r = np.minimum(db, 0.5 * nearest)\n        # small negative numerical values clipped\n        r = np.maximum(r, 0.0)\n        return float(r.sum()), C, r\n\n    # coarse search\n    s_vals = np.linspace(0.08, 0.30, 45)        # horizontal spacing candidates\n    t_vals = np.linspace(0.94, 1.06, 7)         # vertical anisotropy\n    a_vals = np.linspace(-0.30, 0.30, 9)        # stagger fraction of s\n    tx_vals = np.linspace(-0.02, 0.02, 3)       # small horizontal shift\n    ty_vals = np.linspace(-0.03, 0.03, 3)       # small vertical shift\n\n    best_score = -1.0\n    best_C = None\n    best_r = None\n    best_params = None\n\n    for s in s_vals:\n        for t in t_vals:\n            for a in a_vals:\n                for tx in tx_vals:\n                    for ty in ty_vals:\n                        C = make_centers(s, t, a, tx, ty)\n                        sc, Cc, rc = score_config(C)\n                        if sc > best_score:\n                            best_score = sc\n                            best_C, best_r = Cc, rc\n                            best_params = (s, t, a, tx, ty)\n\n    # local refinement around best params (finer grid)\n    if best_params is not None:\n        s0, t0, a0, tx0, ty0 = best_params\n        s_vals = np.linspace(max(0.04, s0 * 0.92), min(0.40, s0 * 1.08), 30)\n        t_vals = np.linspace(max(0.88, t0 - 0.03), min(1.12, t0 + 0.03), 9)\n        a_vals = np.linspace(a0 - 0.08, a0 + 0.08, 11)\n        tx_vals = np.linspace(tx0 - 0.01, tx0 + 0.01, 5)\n        ty_vals = np.linspace(ty0 - 0.01, ty0 + 0.01, 5)\n        for s in s_vals:\n            for t in t_vals:\n                for a in a_vals:\n                    for tx in tx_vals:\n                        for ty in ty_vals:\n                            C = make_centers(s, t, a, tx, ty)\n                            sc, Cc, rc = score_config(C)\n                            if sc > best_score:\n                                best_score = sc\n                                best_C, best_r = Cc, rc\n                                best_params = (s, t, a, tx, ty)\n\n    # final precise radii computation (safety)\n    if best_C is None:\n        # fallback simple centered hex-like block\n        best_C = make_centers(0.14, 1.0, 0.0, 0.0, 0.0)\n    C = np.clip(best_C, 1e-12, 1.0 - 1e-12)\n    dif = C[:, None, :] - C[None, :, :]\n    d = np.sqrt((dif ** 2).sum(axis=2))\n    np.fill_diagonal(d, np.inf)\n    half_nearest = 0.5 * d.min(axis=1)\n    dist_border = np.minimum.reduce([C[:, 0], C[:, 1], 1.0 - C[:, 0], 1.0 - C[:, 1]])\n    radii = np.minimum(dist_border, half_nearest)\n    radii = np.maximum(radii, 0.0)\n    sum_radii = float(radii.sum())\n    return C, radii, sum_radii\n\n# EVOLVE-BLOCK-END\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"Optional matplotlib visualization\"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(7, 7))\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n    for i, (c, r) in enumerate(zip(centers, radii)):\n        ax.add_patch(Circle(c, r, alpha=0.6, edgecolor=\"k\"))\n        ax.text(c[0], c[1], str(i), ha=\"center\", va=\"center\", fontsize=7)\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # visualize(centers, radii)\n```\nKey features: Alternative approach to validity, Alternative approach to sum_radii\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 0.8027, Type: High-Performer)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (improved explicit constructor)\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square.\n    This constructor places points in a compact staggered (nearly hexagonal)\n    layout with an inward margin to balance border effects and inter-circle spacing.\n    Radii are set to the largest non-overlapping values allowed by borders\n    and pairwise distances: r_i = min(dist_to_border_i, min_j d_ij/2).\n\n    Returns:\n        centers: np.array of shape (26,2)\n        radii: np.array of shape (26,)\n        sum_of_radii: float\n    \"\"\"\n    n = 26\n\n    # Row layout (sums to 26). A near-hexagonal packing: some rows longer to use space.\n    cols_per_row = [5, 6, 5, 5, 5]  # total = 26\n    rows = len(cols_per_row)\n\n    # Margin from the square boundary. Chosen to balance border-limited radii vs spacing.\n    margin = 0.08  # tuned so edge circles maintain a reasonable radius\n\n    # Vertical spacing across rows (equal spacing between row centers)\n    available_height = 1.0 - 2.0 * margin\n    if rows == 1:\n        ys = np.array([0.5])\n    else:\n        ys = margin + np.linspace(0.0, available_height, rows)\n\n    centers = []\n    # Place each row centered horizontally, with x positions evenly spaced across [margin, 1-margin]\n    for row_idx, cnt in enumerate(cols_per_row):\n        if cnt == 1:\n            xs = np.array([0.5])\n        else:\n            available_width = 1.0 - 2.0 * margin\n            xs = margin + np.linspace(0.0, available_width, cnt)\n\n        # Stagger alternate rows to approach hexagonal packing (shift by half average step)\n        if row_idx % 2 == 1:\n            # compute an average horizontal step for this row (or fallback)\n            if len(xs) > 1:\n                step = xs[1] - xs[0]\n                xs = xs + 0.5 * step\n            else:\n                xs = xs  # nothing to do\n\n        # Clip to remain inside [margin, 1-margin] (staggering could push endpoints slightly)\n        xs = np.clip(xs, margin, 1.0 - margin)\n        for x in xs:\n            centers.append((x, ys[row_idx]))\n\n    centers = np.array(centers, dtype=float)\n    assert centers.shape[0] == n, \"Center count mismatch\"\n\n    radii = compute_max_radii(centers)\n\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers):\n    \"\"\"\n    Compute radii by the conservative but robust rule:\n      r_i = min( dist_to_border_i,  min_{j != i} d_ij / 2 )\n\n    This guarantees non-overlap and containment in the unit square.\n    \"\"\"\n    n = centers.shape[0]\n    # Distance to borders\n    d_border = np.minimum.reduce([centers[:, 0], centers[:, 1], 1.0 - centers[:, 0], 1.0 - centers[:, 1]])\n\n    # Pairwise distances\n    diff = centers[:, None, :] - centers[None, :, :]  # shape (n,n,2)\n    dists = np.sqrt(np.sum(diff * diff, axis=2))      # shape (n,n)\n    # Avoid zero on diagonal by setting to large number\n    np.fill_diagonal(dists, np.inf)\n\n    half_min_neighbor = np.min(dists, axis=1) / 2.0\n\n    radii = np.minimum(d_border, half_min_neighbor)\n    # Ensure non-negative\n    radii = np.maximum(radii, 0.0)\n    return radii\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nUnique approach: Modification: Full rewrite, Excellent validity (1.000), Excellent sum_radii (2.115)\n\n### Inspiration 2 (Score: 0.8558, Type: High-Performer)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (margin search + Gauss-Seidel radius maximizer)\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    \"\"\"\n    Build 26 circle centers in a staggered near-hex layout and maximize radii\n    for fixed centers using a Gauss-Seidel style solver that enforces:\n      r_i <= border_i,  r_i <= d_ij - r_j  for all j!=i\n    We sample several margins to mitigate edge effects and pick the best sum.\n    \"\"\"\n    n = 26\n    cols_per_row = [5, 6, 5, 5, 5]  # sums to 26\n    rows = len(cols_per_row)\n\n    def make_centers(margin):\n        available_w = 1.0 - 2.0 * margin\n        if available_w <= 0:\n            return None\n        centers = []\n        # compute horizontal steps per row, then choose vertical hex-like step\n        steps = []\n        for cnt in cols_per_row:\n            if cnt > 1:\n                steps.append(available_w / (cnt - 1))\n        if len(steps) == 0:\n            avg_step = 0.5 * available_w\n        else:\n            avg_step = float(np.mean(steps))\n        h = avg_step * np.sqrt(3.0) / 2.0 if avg_step > 0 else 0.0\n\n        # vertical placement centered\n        total_h = h * (rows - 1)\n        y0 = 0.5 - total_h / 2.0\n        for r, cnt in enumerate(cols_per_row):\n            if cnt == 1:\n                xs = np.array([0.5])\n            else:\n                xs = margin + np.linspace(0.0, available_w, cnt)\n            if r % 2 == 1 and cnt > 1:\n                step = xs[1] - xs[0]\n                xs = xs + 0.5 * step\n            xs = np.clip(xs, margin, 1.0 - margin)\n            y = y0 + r * h\n            for x in xs:\n                centers.append((x, y))\n        C = np.array(centers)\n        # if any center lies outside [0,1], reject this margin\n        if (C.min() < 0.0) or (C.max() > 1.0):\n            return None\n        return C\n\n    def maximize_radii(C, tol=1e-9, max_iter=500):\n        n = C.shape[0]\n        # border limits\n        border = np.minimum.reduce([C[:,0], C[:,1], 1.0 - C[:,0], 1.0 - C[:,1]]).astype(float)\n        # pairwise distances\n        diff = C[:, None, :] - C[None, :, :]\n        dists = np.sqrt((diff**2).sum(axis=2))\n        np.fill_diagonal(dists, np.inf)\n\n        # initial radii: border-limited\n        r = border.copy()\n        for it in range(max_iter):\n            prev = r.copy()\n            # Gauss-Seidel pass: set each r_i to its tightest feasible upper bound\n            for i in range(n):\n                ub = border[i]\n                # compute d_ij - r_j for all j != i\n                vals = dists[i] - r\n                vals[i] = np.inf\n                ub = min(ub, float(np.min(vals)))\n                r[i] = max(0.0, ub)\n            if np.max(np.abs(r - prev)) < tol:\n                break\n\n        # final safety: fix any slight overlaps by proportional scaling per violating pair\n        for i in range(n):\n            for j in range(i+1, n):\n                d = dists[i,j]\n                if d <= 1e-12:\n                    r[i] = r[j] = 0.0\n                elif r[i] + r[j] > d:\n                    s = d / (r[i] + r[j])\n                    r[i] *= s\n                    r[j] *= s\n        return r\n\n    best_sum = -1.0\n    best_C = None\n    best_r = None\n    # sample margins (denser near small margins)\n    for margin in np.linspace(0.02, 0.12, 21):\n        C = make_centers(margin)\n        if C is None or C.shape[0] != n:\n            continue\n        r = maximize_radii(C)\n        s = float(r.sum())\n        if s > best_sum:\n            best_sum = s\n            best_C = C\n            best_r = r\n\n    # fallback: if none found (shouldn't happen), use a conservative center layout\n    if best_C is None:\n        margin = 0.06\n        best_C = make_centers(margin)\n        best_r = maximize_radii(best_C)\n\n    return best_C, best_r, float(np.sum(best_r))\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nUnique approach: Modification: Full rewrite, Excellent validity (1.000), Excellent sum_radii (2.255)\n\n### Inspiration 3 (Score: 0.5588, Type: Experimental)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for 26-circle packing in the unit square.\n\nThis builds a near-hex layered layout (rows = [4,5,6,6,5]) in relative\ncoordinates, then searches a small (spacing, margin) grid to maximize the\nsum of radii. For each candidate placement we compute feasible radii by a\nrobust Gauss-Seidel / pairwise-relaxation that enforces ri + rj <= d_ij and\nri <= dist_to_border_i. The best candidate (deterministic grid search) is returned.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    rows = [4, 5, 6, 6, 5]  # sums to 26\n    n = sum(rows)\n    assert n == 26\n\n    # build relative hex-like coords (unit spacing)\n    rel = []\n    h = np.sqrt(3) / 2.0\n    for r, k in enumerate(rows):\n        y = r * h\n        offset = 0.5 if (r % 2 == 1) else 0.0\n        xs = np.arange(k) + offset\n        for x in xs:\n            rel.append((x, y))\n    rel = np.array(rel, dtype=float)\n    # normalize so min corner at (0,0)\n    rel -= rel.min(axis=0)\n    size = rel.max(axis=0)\n\n    # parameter grids (deterministic)\n    s_vals = np.linspace(0.08, 0.22, 60)  # spacing scale\n    margin_vals = np.linspace(0.0, 0.06, 31)\n\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    for m in margin_vals:\n        # maximum s allowed by box: s <= (1 - 2*m) / size\n        max_s = np.inf\n        # avoid division by zero for degenerate sizes\n        with np.errstate(divide='ignore', invalid='ignore'):\n            max_s = np.min((1.0 - 2.0 * m) / (size + 1e-12))\n        if max_s <= 0:\n            continue\n        for s in s_vals:\n            if s > max_s + 1e-12:\n                continue\n            centers = rel * s + m  # translate margin\n            # ensure within bounds\n            if centers.min() < -1e-12 or centers.max() > 1.0 + 1e-12:\n                continue\n            # compute radii by relaxation\n            radii = compute_max_radii_relax(centers)\n            ssum = float(radii.sum())\n            if ssum > best_sum + 1e-12:\n                best_sum = ssum\n                best_centers = centers.copy()\n                best_radii = radii.copy()\n\n    # fallback deterministic layout if search failed (shouldn't happen)\n    if best_centers is None:\n        # center a tight layout\n        s = 0.14\n        m = 0.03\n        best_centers = rel * s + m\n        best_radii = compute_max_radii_relax(best_centers)\n        best_sum = float(best_radii.sum())\n\n    return best_centers, best_radii, float(best_sum)\n\n\ndef compute_max_radii_relax(centers, tol=1e-9, max_iter=1000):\n    \"\"\"\n    Greedy iterative relaxation:\n      - initialize radii to border limits\n      - repeatedly enforce pairwise constraints by reducing the larger circle\n        in any violating pair to meet ri + rj = d_ij (i.e. new_ri = d_ij - rj)\n      - clamp to border limits each pass\n    This heuristic is robust and tends to produce large sums for fixed centers.\n    \"\"\"\n    n = centers.shape[0]\n    # border-limited initial radii\n    rad = np.minimum.reduce([centers[:,0], centers[:,1], 1.0 - centers[:,0], 1.0 - centers[:,1]]).astype(float)\n    rad = np.maximum(rad, 0.0)\n\n    # pairwise distances\n    diff = centers[:, None, :] - centers[None, :, :]\n    dmat = np.sqrt((diff**2).sum(axis=2))\n    # set diagonal to large to ignore self\n    np.fill_diagonal(dmat, np.inf)\n\n    # prepare sorted pairs (tightest constraints first may accelerate convergence)\n    idxs = np.array([(i,j) for i in range(n) for j in range(i+1, n)])\n    if idxs.size == 0:\n        return rad\n    pair_ds = dmat[idxs[:,0], idxs[:,1]]\n    order = np.argsort(pair_ds)\n    pairs = idxs[order]\n\n    for it in range(max_iter):\n        changed = False\n        # sweep pairs\n        for (i,j) in pairs:\n            d = dmat[i,j]\n            if not np.isfinite(d) or d <= 0.0:\n                # coincident centers -> zero both\n                if rad[i] + rad[j] > 0.0 + tol:\n                    if rad[i] >= rad[j]:\n                        if rad[i] > 0:\n                            rad[i] = 0.0\n                            changed = True\n                    else:\n                        if rad[j] > 0:\n                            rad[j] = 0.0\n                            changed = True\n                continue\n            if rad[i] + rad[j] > d + tol:\n                # reduce the larger one\n                if rad[i] > rad[j]:\n                    new_i = max(0.0, d - rad[j])\n                    if new_i + tol < rad[i]:\n                        rad[i] = new_i\n                        changed = True\n                else:\n                    new_j = max(0.0, d - rad[i])\n                    if new_j + tol < rad[j]:\n                        rad[j] = new_j\n                        changed = True\n        # enforce border constraints\n        border_limits = np.minimum.reduce([centers[:,0], centers[:,1], 1.0 - centers[:,0], 1.0 - centers[:,1]])\n        too_big = rad > border_limits + tol\n        if np.any(too_big):\n            rad[too_big] = border_limits[too_big]\n            changed = True\n\n        if not changed:\n            break\n\n    # final safety clamp for any remaining small overlaps (proportional scaling)\n    for i in range(n):\n        for j in range(i+1, n):\n            d = dmat[i,j]\n            if d <= 0:\n                rad[i] = rad[j] = 0.0\n            elif rad[i] + rad[j] > d:\n                s = d / (rad[i] + rad[j])\n                rad[i] *= s\n                rad[j] *= s\n    return np.maximum(rad, 0.0)\n# EVOLVE-BLOCK-END\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(7, 7))\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    for i, (c, r) in enumerate(zip(centers, radii)):\n        circ = Circle(c, r, alpha=0.6, edgecolor=\"k\")\n        ax.add_patch(circ)\n        ax.text(c[0], c[1], str(i), ha=\"center\", va=\"center\", fontsize=7)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # To inspect visually, uncomment:\n    # visualize(centers, radii)\n```\nUnique approach: Modification: Full rewrite, Excellent validity (1.000), Excellent sum_radii (1.472)\n\n# Current Program\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for 26 circles in the unit square.\n\nApproach:\n- Build a truncated hex/triangular lattice skeleton with row counts that sum to 26.\n- Deterministically scale and translate the skeleton into the unit square,\n  scanning over a grid of spacings and translations.\n- For each candidate center set compute maximal feasible radii by a robust\n  greedy relaxation: initialize by border-limits and iteratively enforce\n  ri + rj <= d_ij by reducing the larger circle when violated (tightest\n  pairs processed first). This is simple, deterministic and performs well.\n\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    n = 26\n    # Row layout that sums to 26 and resembles a truncated hex block\n    rows = [4, 5, 6, 5, 4, 2]  # sum = 26\n\n    # Build relative hex-like coordinates (unit lattice)\n    rel = []\n    h = np.sqrt(3.0) / 2.0\n    for r, k in enumerate(rows):\n        y = r * h\n        offset = 0.5 if (r % 2 == 1) else 0.0\n        xs = np.arange(k) + offset\n        for x in xs:\n            rel.append((x, y))\n    rel = np.array(rel, dtype=float)\n\n    # Normalize so min corner at (0,0) for easy scaling/translation\n    rel -= rel.min(axis=0)\n    Xmax, Ymax = rel.max(axis=0)\n\n    # Search grid parameters (deterministic)\n    # spacing range: choose values that can plausibly fit in [0,1]\n    s_min = 0.06\n    # upper bound based on fitting into unit square with zero margin\n    s_max_possible = min(1.0 / max(1.0, Xmax), 1.0 / max(1.0, Ymax))\n    s_max = min(0.22, s_max_possible * 0.98)\n    s_vals = np.linspace(s_min, s_max, 75)\n\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    # For each spacing try a small grid of translations (mx,my)\n    for s in s_vals:\n        W = s * Xmax\n        H = s * Ymax\n        if W > 1.0 + 1e-12 or H > 1.0 + 1e-12:\n            continue\n        # allowable translation ranges so centers lie in [0,1]\n        max_mx = 1.0 - W\n        max_my = 1.0 - H\n        # sample small grid including centered placement\n        cx = 0.5 * max_mx\n        cy = 0.5 * max_my\n        # Build translation candidates: include edges, center, and a couple of intermediate offsets\n        t_x_vals = np.linspace(0.0, max_mx, min(6, max(2, int(1 + max_mx / (0.02 + 1e-12)))))\n        t_y_vals = np.linspace(0.0, max_my, min(6, max(2, int(1 + max_my / (0.02 + 1e-12)))))\n\n        for mx in t_x_vals:\n            for my in t_y_vals:\n                centers = rel * s + np.array([mx, my])\n                # numerical safety: ensure inside [0,1]\n                if centers.min() < -1e-12 or centers.max() > 1.0 + 1e-12:\n                    continue\n\n                radii = compute_max_radii_relax(centers)\n                ssum = radii.sum()\n                if ssum > best_sum + 1e-12:\n                    best_sum = ssum\n                    best_centers = centers.copy()\n                    best_radii = radii.copy()\n\n    # Fallback (shouldn't happen) - center a default scaled layout\n    if best_centers is None:\n        s = 0.12\n        mx = 0.5 * (1.0 - s * Xmax)\n        my = 0.5 * (1.0 - s * Ymax)\n        best_centers = rel * s + np.array([mx, my])\n        best_radii = compute_max_radii_relax(best_centers)\n        best_sum = float(best_radii.sum())\n\n    return best_centers, best_radii, float(best_sum)\n\n\ndef compute_max_radii_relax(centers, tol=1e-9, max_iter=800):\n    \"\"\"\n    Greedy relaxation to get large feasible radii for fixed centers.\n\n    - Initialize radii to distance-to-border.\n    - Precompute pairwise distances and process pairs from smallest to largest.\n    - If ri + rj > d_ij, reduce the larger radius so ri + rj = d_ij (reducing the larger\n      preserves flexibility of smaller ones). Iterate until no meaningful change.\n    This deterministic strategy often produces near-optimal sums for fixed centers.\n    \"\"\"\n    n = centers.shape[0]\n    # border-limited initial radii\n    border = np.minimum.reduce([centers[:, 0], centers[:, 1], 1.0 - centers[:, 0], 1.0 - centers[:, 1]])\n    radii = border.copy()\n\n    # pairwise distances\n    diff = centers[:, None, :] - centers[None, :, :]\n    dmat = np.sqrt(np.sum(diff * diff, axis=2))\n    # set diagonal to inf for convenience\n    np.fill_diagonal(dmat, np.inf)\n\n    # build sorted list of pairs by distance (tight constraints first)\n    idx_i, idx_j = np.triu_indices(n, k=1)\n    pairs = list(zip(idx_i.tolist(), idx_j.tolist(), dmat[idx_i, idx_j].tolist()))\n    pairs.sort(key=lambda t: t[2])\n\n    # iterative enforcement\n    for it in range(max_iter):\n        changed = False\n        max_delta = 0.0\n        for (i, j, dij) in pairs:\n            # numerical protection\n            if not np.isfinite(dij) or dij <= 0.0:\n                # coincident or extremely close centers -> force one small if necessary\n                if radii[i] + radii[j] > 1e-12:\n                    if radii[i] >= radii[j]:\n                        new_i = max(0.0, dij - radii[j])\n                        delta = radii[i] - new_i\n                        if delta > tol:\n                            radii[i] = new_i\n                            changed = True\n                            max_delta = max(max_delta, delta)\n                    else:\n                        new_j = max(0.0, dij - radii[i])\n                        delta = radii[j] - new_j\n                        if delta > tol:\n                            radii[j] = new_j\n                            changed = True\n                            max_delta = max(max_delta, delta)\n                continue\n\n            if radii[i] + radii[j] > dij + 1e-12:\n                # reduce the larger one to satisfy constraint\n                if radii[i] > radii[j]:\n                    new_i = max(0.0, dij - radii[j])\n                    delta = radii[i] - new_i\n                    if delta > tol:\n                        radii[i] = new_i\n                        changed = True\n                        max_delta = max(max_delta, delta)\n                else:\n                    new_j = max(0.0, dij - radii[i])\n                    delta = radii[j] - new_j\n                    if delta > tol:\n                        radii[j] = new_j\n                        changed = True\n                        max_delta = max(max_delta, delta)\n\n        # ensure border constraints\n        over_border = radii > border\n        if np.any(over_border):\n            radii[over_border] = border[over_border]\n            changed = True\n\n        if not changed or max_delta < tol:\n            break\n\n    # Final safety: small proportional scaling if any tiny overlaps remain\n    for i in range(n):\n        for j in range(i + 1, n):\n            d = dmat[i, j]\n            if d < 1e-12:\n                # coincident, zero both\n                radii[i] = 0.0\n                radii[j] = 0.0\n            elif radii[i] + radii[j] > d:\n                s = d / (radii[i] + radii[j])\n                radii[i] *= s\n                radii[j] *= s\n\n    # clamp non-negativity and border limits\n    radii = np.clip(radii, 0.0, border)\n    return radii\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(7, 7))\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    for i, (c, r) in enumerate(zip(centers, radii)):\n        circ = Circle(c, r, alpha=0.6, edgecolor=\"k\")\n        ax.add_patch(circ)\n        ax.text(c[0], c[1], str(i), ha=\"center\", va=\"center\", fontsize=7)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # To inspect visually, uncomment:\n    # visualize(centers, radii)\n```\n\n# Task\nRewrite the program to improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: complexity, diversity\nDifferent solutions with similar fitness but different features are valuable.\nProvide the complete new program code.\n\nIMPORTANT: Make sure your rewritten program maintains the same inputs and outputs\nas the original program, but with improved internal implementation.\n\n```python\n# Your rewritten program here\n```", "responses": ["```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for 26 circles in the unit square.\nLayout: a compact near-hex block with rows [5,6,5,5,5]. For a range of edge\nmargins we build centers and maximize radii for fixed centers via a\nGauss-Seidel style solver that enforces r_i <= border_i and r_i <= d_ij - r_j.\nThe best configuration (largest sum of radii) is returned.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    cols_per_row = [5, 6, 5, 5, 5]  # sums to 26\n    rows = len(cols_per_row)\n\n    def make_centers(margin):\n        available_w = 1.0 - 2.0 * margin\n        if available_w <= 0:\n            return None\n        # pick an average horizontal step consistent with rows\n        steps = []\n        for cnt in cols_per_row:\n            if cnt > 1:\n                steps.append(available_w / (cnt - 1))\n        avg_step = float(np.mean(steps)) if steps else 0.5 * available_w\n        h = avg_step * np.sqrt(3.0) / 2.0  # hex vertical spacing\n        total_h = h * (rows - 1)\n        y0 = 0.5 - total_h / 2.0\n        centers = []\n        for r, cnt in enumerate(cols_per_row):\n            if cnt == 1:\n                xs = np.array([0.5])\n            else:\n                xs = margin + np.linspace(0.0, available_w, cnt)\n            # stagger odd rows\n            if (r % 2 == 1) and cnt > 1:\n                step = xs[1] - xs[0]\n                xs = xs + 0.5 * step\n            xs = np.clip(xs, margin, 1.0 - margin)\n            y = y0 + r * h\n            for x in xs:\n                centers.append((x, y))\n        C = np.array(centers, dtype=float)\n        if C.shape[0] != n:\n            return None\n        if C.min() < -1e-12 or C.max() > 1.0 + 1e-12:\n            return None\n        return C\n\n    def maximize_radii(C, tol=1e-10, max_iter=2000):\n        n = C.shape[0]\n        border = np.minimum.reduce([C[:,0], C[:,1], 1.0 - C[:,0], 1.0 - C[:,1]]).astype(float)\n        diff = C[:, None, :] - C[None, :, :]\n        dists = np.sqrt((diff**2).sum(axis=2))\n        np.fill_diagonal(dists, np.inf)\n        # start with border-limited radii\n        r = border.copy()\n        for it in range(max_iter):\n            prev = r.copy()\n            # Gauss-Seidel pass\n            for i in range(n):\n                ub = border[i]\n                # compute d_ij - r_j for all j != i\n                vals = dists[i] - r\n                vals[i] = np.inf\n                m = float(np.min(vals))\n                if m < ub:\n                    ub = m\n                r[i] = max(0.0, ub)\n            if np.max(np.abs(r - prev)) < tol:\n                break\n        # safety: fix any tiny overlaps by proportional scaling pairwise\n        for i in range(n):\n            for j in range(i+1, n):\n                d = dists[i,j]\n                if d <= 1e-12:\n                    r[i] = r[j] = 0.0\n                elif r[i] + r[j] > d:\n                    s = d / (r[i] + r[j])\n                    r[i] *= s\n                    r[j] *= s\n        r = np.clip(r, 0.0, border)\n        return r\n\n    best_sum = -1.0\n    best_C = None\n    best_r = None\n    # sample margins focused on small margins (to get larger interior radii but avoid clipping)\n    for margin in np.linspace(0.02, 0.12, 41):\n        C = make_centers(margin)\n        if C is None:\n            continue\n        r = maximize_radii(C)\n        s = float(r.sum())\n        if s > best_sum + 1e-12:\n            best_sum = s\n            best_C = C.copy()\n            best_r = r.copy()\n\n    if best_C is None:\n        # fallback conservative layout\n        margin = 0.07\n        best_C = make_centers(margin)\n        best_r = maximize_radii(best_C)\n\n    return best_C, best_r, float(np.sum(best_r))\n\n# EVOLVE-BLOCK-END\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(7, 7))\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    for i, (c, r) in enumerate(zip(centers, radii)):\n        ax.add_patch(Circle(c, r, alpha=0.6, edgecolor=\"k\"))\n        ax.text(c[0], c[1], str(i), ha=\"center\", va=\"center\", fontsize=7)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # visualize(centers, radii)\n```"]}}, "artifacts_json": "{\"stdout\": \"Running in subprocess, Python version: 3.12.1 (v3.12.1:2305ca5144, Dec  7 2023, 17:23:38) [Clang 13.0.0 (clang-1300.0.29.30)]\\nProgram path: /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmpi2fssz7o.py\\nCalling run_packing()...\\nrun_packing() returned successfully: sum_radii = 2.254931588374731\\nResults saved to /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmpj_884fey.py.results\\n\", \"stderr\": \"\"}", "artifact_dir": null}