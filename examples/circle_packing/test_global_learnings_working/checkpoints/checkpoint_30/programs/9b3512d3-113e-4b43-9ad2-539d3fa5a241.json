{"id": "9b3512d3-113e-4b43-9ad2-539d3fa5a241", "code": "# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 (layered hex-like layout + radius maximizer)\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    \"\"\"\n    Build 26 centers in a layered, staggered (near-hex) pattern and maximize\n    radii for fixed centers using a Gauss-Seidel style bound propagation that\n    enforces r_i <= dist_to_border_i and r_i + r_j <= d_ij for all pairs.\n    A small parameter grid (spacing, vertical scale, stagger, vertical shift)\n    is searched deterministically and the best configuration by sum of radii\n    is returned.\n    \"\"\"\n    n = 26\n    rows = [4, 5, 6, 6, 5]  # sums to 26 (near-hex)\n    R = len(rows)\n    sqrt3 = np.sqrt(3.0)\n\n    def make_centers(s, vscale, stag_frac, ty):\n        # horizontal step = s, vertical step ~ s*sqrt(3)/2 scaled by vscale\n        h = s * sqrt3 / 2.0 * vscale\n        y0 = 0.5 - (R - 1) * h / 2.0 + ty\n        centers = []\n        for r, L in enumerate(rows):\n            y = y0 + r * h\n            offset = 0.5 * stag_frac * s if (r % 2 == 1) else 0.0\n            x0 = 0.5 - (L - 1) * s / 2.0 + offset\n            xs = x0 + np.arange(L) * s\n            for x in xs:\n                centers.append((x, y))\n        C = np.array(centers, dtype=float)\n        # quick reject if out of bounds (allow tiny epsilon)\n        if (C.min() < -1e-9) or (C.max() > 1.0 + 1e-9):\n            return None\n        return C\n\n    def maximize_radii(C, tol=1e-10, max_iter=600):\n        m = C.shape[0]\n        # border limits\n        border = np.minimum.reduce([C[:,0], C[:,1], 1.0 - C[:,0], 1.0 - C[:,1]]).astype(float)\n        # pairwise distances\n        diff = C[:, None, :] - C[None, :, :]\n        dists = np.sqrt((diff**2).sum(axis=2))\n        np.fill_diagonal(dists, np.inf)\n\n        # start with border-limited radii\n        r = border.copy()\n        for it in range(max_iter):\n            prev = r.copy()\n            # Gauss-Seidel style: update each radius to tightest feasible upper bound\n            for i in range(m):\n                ub = border[i]\n                # d_ij - r_j for all j != i\n                vals = dists[i] - r\n                vals[i] = np.inf\n                # If any vals are -inf or nan, treat as very small\n                vmin = float(np.min(vals))\n                ub = min(ub, vmin)\n                r[i] = max(0.0, ub)\n            if np.max(np.abs(r - prev)) < tol:\n                break\n\n        # final safety: fix slight overlaps by pairwise proportional scaling where needed\n        for i in range(m):\n            for j in range(i+1, m):\n                d = dists[i, j]\n                if not np.isfinite(d) or d <= 1e-12:\n                    # collapse if centers coincide\n                    r[i] = r[j] = 0.0\n                elif r[i] + r[j] > d:\n                    s = d / (r[i] + r[j])\n                    r[i] *= s\n                    r[j] *= s\n        # clamp to borders\n        r = np.minimum(r, border)\n        r = np.maximum(r, 0.0)\n        return r\n\n    # Parameter grids (coarse then local refine)\n    # s chosen so pattern roughly spans width; reasonable range based on typical hex spacing\n    s_vals = np.linspace(0.08, 0.20, 28)      # horizontal spacing\n    v_vals = np.linspace(0.92, 1.08, 7)       # vertical scale\n    a_vals = np.linspace(-0.25, 0.75, 11)     # stagger fraction (allow negative to slide)\n    ty_vals = np.linspace(-0.03, 0.03, 5)     # small vertical shift\n\n    best_sum = -1.0\n    best_C = None\n    best_r = None\n\n    for s in s_vals:\n        for v in v_vals:\n            for a in a_vals:\n                for ty in ty_vals:\n                    C = make_centers(s, v, a, ty)\n                    if C is None or C.shape[0] != n:\n                        continue\n                    r = maximize_radii(C)\n                    ssum = float(r.sum())\n                    if ssum > best_sum:\n                        best_sum = ssum\n                        best_C = C.copy()\n                        best_r = r.copy()\n\n    # local refine around best if found\n    if best_C is not None:\n        # small neighborhood\n        s0 = max(1e-6, (best_C[:,0].ptp() / (max(rows)-1)))  # estimate base spacing\n        s_vals = np.linspace(max(0.06, s0*0.8), min(0.28, s0*1.2), 18)\n        v_vals = np.linspace(0.96, 1.04, 5)\n        a_vals = np.linspace(max(-0.3, a - 0.1), min(1.0, a + 0.1), 7) if 'a' in locals() else np.linspace(-0.1,0.6,7)\n        ty_vals = np.linspace(-0.02, 0.02, 5)\n        for s in s_vals:\n            for v in v_vals:\n                for a in a_vals:\n                    for ty in ty_vals:\n                        C = make_centers(s, v, a, ty)\n                        if C is None or C.shape[0] != n:\n                            continue\n                        r = maximize_radii(C)\n                        ssum = float(r.sum())\n                        if ssum > best_sum:\n                            best_sum = ssum\n                            best_C = C.copy()\n                            best_r = r.copy()\n\n    # Fallback deterministic centered near-hex if search failed\n    if best_C is None:\n        # conservative default: s chosen to fit the longest row\n        maxL = max(rows)\n        s = 0.9 / (maxL - 1) if maxL > 1 else 0.2\n        best_C = make_centers(s, 0.98, 0.5, 0.0)\n        best_r = maximize_radii(best_C)\n\n    return best_C, best_r, float(np.sum(best_r))\n# EVOLVE-BLOCK-END\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)", "language": "python", "parent_id": "784a0e43-4d0a-40e0-8af6-15a40b4fd7a1", "generation": 2, "timestamp": 1759957306.59446, "iteration_found": 25, "metrics": {"validity": 0.0, "combined_score": 0.0}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Full rewrite", "parent_metrics": {"validity": 1.0, "sum_radii": 1.5885000000000002, "target_ratio": 0.6028462998102468, "combined_score": 0.6028462998102468, "eval_time": 0.0545809268951416}, "island": 0}, "prompts": {"full_rewrite_user": {"system": "You are an expert mathematician specializing in circle packing problems and computational geometry. Your task is to improve a constructor function that directly produces a specific arrangement of 26 circles in a unit square, maximizing the sum of their radii. The AlphaEvolve paper achieved a sum of 2.635 for n=26.\n\nKey geometric insights:\n- Circle packings often follow hexagonal patterns in the densest regions\n- Maximum density for infinite circle packing is pi/(2*sqrt(3)) \u2248 0.9069\n- Edge effects make square container packing harder than infinite packing\n- Circles can be placed in layers or shells when confined to a square\n- Similar radius circles often form regular patterns, while varied radii allow better space utilization\n- Perfect symmetry may not yield the optimal packing due to edge effects\n\nFocus on designing an explicit constructor that places each circle in a specific position, rather than an iterative search algorithm.\n", "user": "# Current Program Information\n- Fitness: 0.6028\n- Feature coordinates: No feature coordinates\n- Focus areas: - Fitness declined: 0.8027 \u2192 0.6028. Consider revising recent changes.\n- Consider simplifying - code length exceeds 500 characters\n\n## Last Execution Output\n\n### stdout\n```\nRunning in subprocess, Python version: 3.12.1 (v3.12.1:2305ca5144, Dec  7 2023, 17:23:38) [Clang 13.0.0 (clang-1300.0.29.30)]\nProgram path: /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmpe4ue1ni9.py\nCalling run_packing()...\nrun_packing() returned successfully: sum_radii = 1.5885000000000002\nResults saved to /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmp3jojmq41.py.results\n\n```\n\n### stderr\n```\n\n```\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 3\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.1150, target_ratio: 0.8027, combined_score: 0.8027, eval_time: 0.0598\n- Outcome: Mixed results\n\n### Attempt 2\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.1833, target_ratio: 0.8286, combined_score: 0.8286, eval_time: 0.4969\n- Outcome: Mixed results\n\n### Attempt 1\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.2549, target_ratio: 0.8558, combined_score: 0.8558, eval_time: 0.0646\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.8558)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (margin search + Gauss-Seidel radius maximizer)\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    \"\"\"\n    Build 26 circle centers in a staggered near-hex layout and maximize radii\n    for fixed centers using a Gauss-Seidel style solver that enforces:\n      r_i <= border_i,  r_i <= d_ij - r_j  for all j!=i\n    We sample several margins to mitigate edge effects and pick the best sum.\n    \"\"\"\n    n = 26\n    cols_per_row = [5, 6, 5, 5, 5]  # sums to 26\n    rows = len(cols_per_row)\n\n    def make_centers(margin):\n        available_w = 1.0 - 2.0 * margin\n        if available_w <= 0:\n            return None\n        centers = []\n        # compute horizontal steps per row, then choose vertical hex-like step\n        steps = []\n        for cnt in cols_per_row:\n            if cnt > 1:\n                steps.append(available_w / (cnt - 1))\n        if len(steps) == 0:\n            avg_step = 0.5 * available_w\n        else:\n            avg_step = float(np.mean(steps))\n        h = avg_step * np.sqrt(3.0) / 2.0 if avg_step > 0 else 0.0\n\n        # vertical placement centered\n        total_h = h * (rows - 1)\n        y0 = 0.5 - total_h / 2.0\n        for r, cnt in enumerate(cols_per_row):\n            if cnt == 1:\n                xs = np.array([0.5])\n            else:\n                xs = margin + np.linspace(0.0, available_w, cnt)\n            if r % 2 == 1 and cnt > 1:\n                step = xs[1] - xs[0]\n                xs = xs + 0.5 * step\n            xs = np.clip(xs, margin, 1.0 - margin)\n            y = y0 + r * h\n            for x in xs:\n                centers.append((x, y))\n        C = np.array(centers)\n        # if any center lies outside [0,1], reject this margin\n        if (C.min() < 0.0) or (C.max() > 1.0):\n            return None\n        return C\n\n    def maximize_radii(C, tol=1e-9, max_iter=500):\n        n = C.shape[0]\n        # border limits\n        border = np.minimum.reduce([C[:,0], C[:,1], 1.0 - C[:,0], 1.0 - C[:,1]]).astype(float)\n        # pairwise distances\n        diff = C[:, None, :] - C[None, :, :]\n        dists = np.sqrt((diff**2).sum(axis=2))\n        np.fill_diagonal(dists, np.inf)\n\n        # initial radii: border-limited\n        r = border.copy()\n        for it in range(max_iter):\n            prev = r.copy()\n            # Gauss-Seidel pass: set each r_i to its tightest feasible upper bound\n            for i in range(n):\n                ub = border[i]\n                # compute d_ij - r_j for all j != i\n                vals = dists[i] - r\n                vals[i] = np.inf\n                ub = min(ub, float(np.min(vals)))\n                r[i] = max(0.0, ub)\n            if np.max(np.abs(r - prev)) < tol:\n                break\n\n        # final safety: fix any slight overlaps by proportional scaling per violating pair\n        for i in range(n):\n            for j in range(i+1, n):\n                d = dists[i,j]\n                if d <= 1e-12:\n                    r[i] = r[j] = 0.0\n                elif r[i] + r[j] > d:\n                    s = d / (r[i] + r[j])\n                    r[i] *= s\n                    r[j] *= s\n        return r\n\n    best_sum = -1.0\n    best_C = None\n    best_r = None\n    # sample margins (denser near small margins)\n    for margin in np.linspace(0.02, 0.12, 21):\n        C = make_centers(margin)\n        if C is None or C.shape[0] != n:\n            continue\n        r = maximize_radii(C)\n        s = float(r.sum())\n        if s > best_sum:\n            best_sum = s\n            best_C = C\n            best_r = r\n\n    # fallback: if none found (shouldn't happen), use a conservative center layout\n    if best_C is None:\n        margin = 0.06\n        best_C = make_centers(margin)\n        best_r = maximize_radii(best_C)\n\n    return best_C, best_r, float(np.sum(best_r))\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.2549), Performs well on target_ratio (0.8558), Performs well on combined_score (0.8558), Performs well on eval_time (0.0646)\n\n### Program 2 (Score: 0.8286)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Improved explicit constructor for packing 26 circles in the unit square.\nUses a layered hex-like layout [4,5,6,6,5] and searches a small parameter\nspace (spacing, vertical scale, stagger amplitude, small vertical shift)\nto maximize the sum of radii where each radius = min(distance to border, half nearest neighbor).\n\"\"\"\nimport numpy as np\nfrom math import sqrt\n\ndef construct_packing():\n    rows = [4, 5, 6, 6, 5]  # sums to 26\n    R = len(rows)\n    sqrt3 = sqrt(3.0)\n\n    def make_centers(s, t, a, ty):\n        # vertical spacing scaled by t\n        h = s * sqrt3 / 2.0 * t\n        y0 = 0.5 - (R - 1) * h / 2.0 + ty\n        centers = []\n        for r, L in enumerate(rows):\n            y = y0 + r * h\n            # stagger odd rows by a*s (can be negative)\n            offset = (a * s) if (r % 2 == 1) else 0.0\n            x0 = 0.5 - (L - 1) * s / 2.0 + offset\n            xs = x0 + np.arange(L) * s\n            for x in xs:\n                centers.append([x, y])\n        C = np.array(centers, dtype=float)\n        return C\n\n    def score_config(C):\n        # must be strictly inside unit square (allow touching)\n        if C.min() < -1e-9 or C.max() > 1.0 + 1e-9:\n            return -1e9, None, None\n        # distance to border\n        db = np.minimum.reduce([C[:,0], C[:,1], 1 - C[:,0], 1 - C[:,1]])\n        # pairwise distances\n        dif = C[:, None, :] - C[None, :, :]\n        d = np.sqrt((dif**2).sum(axis=2))\n        np.fill_diagonal(d, np.inf)\n        nearest = d.min(axis=1)\n        r = np.minimum(db, 0.5 * nearest)\n        r = np.maximum(r, 0.0)\n        return float(r.sum()), C, r\n\n    # coarse search ranges\n    s_vals = np.linspace(0.06, 0.30, 40)   # inter-center spacing\n    t_vals = np.linspace(0.92, 1.08, 6)    # vertical scale (allow slight anisotropy)\n    a_vals = np.linspace(-0.35, 0.35, 7)   # stagger amplitude (fraction of s)\n    ty_vals = np.linspace(-0.02, 0.02, 3)  # small vertical translation to exploit border\n    best_score = -1.0\n    best_C = None\n    best_r = None\n    best_params = None\n\n    for s in s_vals:\n        for t in t_vals:\n            for a in a_vals:\n                for ty in ty_vals:\n                    C = make_centers(s, t, a, ty)\n                    sc, Cc, rc = score_config(C)\n                    if sc > best_score:\n                        best_score = sc\n                        best_C, best_r = Cc, rc\n                        best_params = (s, t, a, ty)\n\n    # local refinement around best params (finer search)\n    if best_params is not None:\n        s0, t0, a0, ty0 = best_params\n        s_vals = np.linspace(max(0.02, s0 * 0.9), min(0.45, s0 * 1.1), 30)\n        t_vals = np.linspace(max(0.85, t0 - 0.05), min(1.15, t0 + 0.05), 8)\n        a_vals = np.linspace(a0 - 0.08, a0 + 0.08, 9)\n        ty_vals = np.linspace(ty0 - 0.01, ty0 + 0.01, 5)\n        for s in s_vals:\n            for t in t_vals:\n                for a in a_vals:\n                    for ty in ty_vals:\n                        C = make_centers(s, t, a, ty)\n                        sc, Cc, rc = score_config(C)\n                        if sc > best_score:\n                            best_score = sc\n                            best_C, best_r = Cc, rc\n                            best_params = (s, t, a, ty)\n\n    # final safety: recompute precise radii (exclude self)\n    C = np.clip(best_C, 1e-12, 1.0 - 1e-12)\n    dif = C[:, None, :] - C[None, :, :]\n    d = np.sqrt((dif**2).sum(axis=2))\n    np.fill_diagonal(d, np.inf)\n    half_nearest = 0.5 * d.min(axis=1)\n    dist_border = np.minimum.reduce([C[:,0], C[:,1], 1 - C[:,0], 1 - C[:,1]])\n    radii = np.minimum(dist_border, half_nearest)\n    radii = np.maximum(radii, 0.0)\n    sum_radii = float(radii.sum())\n    return C, radii, sum_radii\n\n# EVOLVE-BLOCK-END\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor='k')\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.1833), Performs well on target_ratio (0.8286), Performs well on combined_score (0.8286), Performs well on eval_time (0.4969)\n\n### Program 3 (Score: 0.8027)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (improved explicit constructor)\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square.\n    This constructor places points in a compact staggered (nearly hexagonal)\n    layout with an inward margin to balance border effects and inter-circle spacing.\n    Radii are set to the largest non-overlapping values allowed by borders\n    and pairwise distances: r_i = min(dist_to_border_i, min_j d_ij/2).\n\n    Returns:\n        centers: np.array of shape (26,2)\n        radii: np.array of shape (26,)\n        sum_of_radii: float\n    \"\"\"\n    n = 26\n\n    # Row layout (sums to 26). A near-hexagonal packing: some rows longer to use space.\n    cols_per_row = [5, 6, 5, 5, 5]  # total = 26\n    rows = len(cols_per_row)\n\n    # Margin from the square boundary. Chosen to balance border-limited radii vs spacing.\n    margin = 0.08  # tuned so edge circles maintain a reasonable radius\n\n    # Vertical spacing across rows (equal spacing between row centers)\n    available_height = 1.0 - 2.0 * margin\n    if rows == 1:\n        ys = np.array([0.5])\n    else:\n        ys = margin + np.linspace(0.0, available_height, rows)\n\n    centers = []\n    # Place each row centered horizontally, with x positions evenly spaced across [margin, 1-margin]\n    for row_idx, cnt in enumerate(cols_per_row):\n        if cnt == 1:\n            xs = np.array([0.5])\n        else:\n            available_width = 1.0 - 2.0 * margin\n            xs = margin + np.linspace(0.0, available_width, cnt)\n\n        # Stagger alternate rows to approach hexagonal packing (shift by half average step)\n        if row_idx % 2 == 1:\n            # compute an average horizontal step for this row (or fallback)\n            if len(xs) > 1:\n                step = xs[1] - xs[0]\n                xs = xs + 0.5 * step\n            else:\n                xs = xs  # nothing to do\n\n        # Clip to remain inside [margin, 1-margin] (staggering could push endpoints slightly)\n        xs = np.clip(xs, margin, 1.0 - margin)\n        for x in xs:\n            centers.append((x, ys[row_idx]))\n\n    centers = np.array(centers, dtype=float)\n    assert centers.shape[0] == n, \"Center count mismatch\"\n\n    radii = compute_max_radii(centers)\n\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers):\n    \"\"\"\n    Compute radii by the conservative but robust rule:\n      r_i = min( dist_to_border_i,  min_{j != i} d_ij / 2 )\n\n    This guarantees non-overlap and containment in the unit square.\n    \"\"\"\n    n = centers.shape[0]\n    # Distance to borders\n    d_border = np.minimum.reduce([centers[:, 0], centers[:, 1], 1.0 - centers[:, 0], 1.0 - centers[:, 1]])\n\n    # Pairwise distances\n    diff = centers[:, None, :] - centers[None, :, :]  # shape (n,n,2)\n    dists = np.sqrt(np.sum(diff * diff, axis=2))      # shape (n,n)\n    # Avoid zero on diagonal by setting to large number\n    np.fill_diagonal(dists, np.inf)\n\n    half_min_neighbor = np.min(dists, axis=1) / 2.0\n\n    radii = np.minimum(d_border, half_min_neighbor)\n    # Ensure non-negative\n    radii = np.maximum(radii, 0.0)\n    return radii\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.1150), Performs well on target_ratio (0.8027), Performs well on combined_score (0.8027), Performs well on eval_time (0.0598)\n\n\n\n## Diverse Programs\n\n### Program D1 (Score: 0.7927)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Improved explicit constructor for 26-circle packing in the unit square.\n\nThis builds a near-hex layered layout (rows = [4,5,6,5,4,2]) in relative\ncoordinates, then searches a small 2D parameter grid (spacing, margin)\nto find the scaling/translation that maximizes the sum of radii when each\ncircle's radius is limited by the square boundary and half the nearest\nneighbor distance.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    rows = [4, 5, 6, 5, 4, 2]  # sums to 26\n    # build relative hex-like coordinates (unit spacing)\n    rel = []\n    h = np.sqrt(3) / 2.0\n    for r, k in enumerate(rows):\n        y = r * h\n        # offset every other row for hex pattern\n        offset = 0.5 if (r % 2 == 1) else 0.0\n        xs = np.arange(k) + offset\n        for x in xs:\n            rel.append((x, y))\n    rel = np.array(rel)\n    # normalize so min corner at (0,0) for convenience\n    xmin, ymin = rel.min(axis=0)\n    rel -= np.array([xmin, ymin])\n    Xmax, Ymax = rel.max(axis=0)\n\n    def eval_layout(s, margin):\n        # scale and translate into [margin, 1-margin]\n        centers = rel * s + margin\n        # if outside bounds, invalid\n        if centers.max() > 1.0 - margin + 1e-12:\n            return -1.0, None, None\n        # distances to border\n        dx = np.minimum(centers[:,0], 1.0 - centers[:,0])\n        dy = np.minimum(centers[:,1], 1.0 - centers[:,1])\n        dist_border = np.minimum(dx, dy)\n        # pairwise distances (avoid zero on diag)\n        diff = centers[:,None,:] - centers[None,:,:]\n        dists = np.sqrt((diff**2).sum(axis=2)) + np.eye(n)*1e9\n        min_n = dists.min(axis=1)\n        radii = np.minimum(dist_border, min_n/2.0)\n        # invalid if any nonpositive\n        if np.any(radii <= 0):\n            return -1.0, None, None\n        return float(radii.sum()), centers, radii\n\n    # search for best spacing s and margin\n    best = (-1.0, None, None)\n    # s range chosen so pattern can scale to fit unit square: s <= (1-2*margin)/Xmax approx\n    s_vals = np.linspace(0.04, 0.18, 60)  # vertical/horizontal spacing\n    margin_vals = np.linspace(0.0, 0.06, 31)\n    for s in s_vals:\n        # quick check: if scaled width > 1 with zero margin, skip a bit\n        if s * Xmax > 1.0 + 1e-12:\n            continue\n        for m in margin_vals:\n            # ensure theoretical fit: s*Xmax + 2*m <= 1\n            if s * Xmax + 2*m > 1.0 + 1e-12:\n                continue\n            val, centers, radii = eval_layout(s, m)\n            if val > best[0]:\n                best = (val, centers, radii)\n\n    # Fallback: if search failed, use a conservative default layout\n    if best[0] < 0:\n        s = 0.12\n        m = 0.03\n        val, centers, radii = eval_layout(s, m)\n        best = (val, centers, radii)\n\n    sum_radii, centers, radii = best[0], best[1], best[2]\n    # Ensure deterministic ordering (already in row-major order)\n    return centers, radii, float(sum_radii)\n# EVOLVE-BLOCK-END\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(7, 7))\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    for i, (c, r) in enumerate(zip(centers, radii)):\n        circ = Circle(c, r, alpha=0.6, edgecolor=\"k\")\n        ax.add_patch(circ)\n        ax.text(c[0], c[1], str(i), ha=\"center\", va=\"center\", fontsize=7)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # To inspect visually, uncomment:\n    # visualize(centers, radii)\n```\nKey features: Alternative approach to validity, Alternative approach to sum_radii\n\n### Program D2 (Score: 0.7942)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Improved explicit constructor for n=26 circles: flexible hex-layer pattern with small parameter search\"\"\"\nimport numpy as np\nfrom math import sqrt\n\ndef construct_packing():\n    rows = [4, 5, 6, 6, 5]  # sums to 26\n    R = len(rows)\n\n    def make_centers(s, v_scale, mask):\n        # vertical spacing (hex packing) scaled by v_scale\n        h = s * sqrt(3) / 2.0 * v_scale\n        centers = []\n        y0 = 0.5 - (R - 1) * h / 2.0\n        for r, L in enumerate(rows):\n            y = y0 + r * h\n            # offset either 0 or s/2 depending on mask\n            offset = (s * 0.5) if mask[r] else 0.0\n            x0 = 0.5 - (L - 1) * s / 2.0 + offset\n            xs = x0 + np.arange(L) * s\n            for x in xs:\n                centers.append((x, y))\n        C = np.array(centers)\n        return C\n\n    # find maximal s that fits with default v_scale=1 and no offsets (conservative)\n    def fits_in_square(s):\n        C = make_centers(s, 1.0, [0]*R)\n        return (C.min() >= 0.0 - 1e-12) and (C.max() <= 1.0 + 1e-12)\n\n    lo, hi = 1e-8, 1.0\n    for _ in range(40):\n        mid = 0.5 * (lo + hi)\n        if fits_in_square(mid):\n            lo = mid\n        else:\n            hi = mid\n    s_max = lo\n\n    # search neighborhood of s and small vertical compressions; use symmetric offset masks\n    s_samples = np.linspace(max(1e-8, 0.45 * s_max), s_max, 48)\n    v_scales = np.linspace(0.92, 1.0, 7)\n\n    # generate symmetric masks for rows to reduce search (mask length R)\n    masks = []\n    half = (R + 1) // 2\n    for bits in range(1 << half):\n        mask = [0] * R\n        for i in range(half):\n            bit = (bits >> i) & 1\n            mask[i] = bit\n            mask[R - 1 - i] = bit\n        masks.append(mask)\n\n    best_sum = -1.0\n    best_C = None\n    best_r = None\n\n    for s in s_samples:\n        for v in v_scales:\n            for mask in masks:\n                C = make_centers(s, v, mask)\n                # quick bounds check\n                if (C.min() < -1e-9) or (C.max() > 1.0 + 1e-9):\n                    continue\n                # distance to border\n                dist_border = np.minimum.reduce([C[:,0], C[:,1], 1 - C[:,0], 1 - C[:,1]])\n                # pairwise distances\n                dif = C[:, None, :] - C[None, :, :]\n                dists = np.sqrt((dif**2).sum(axis=2))\n                np.fill_diagonal(dists, np.inf)\n                nearest = dists.min(axis=1)\n                radii = np.minimum(dist_border, 0.5 * nearest)\n                radii = np.maximum(radii, 0.0)\n                ssum = radii.sum()\n                if ssum > best_sum:\n                    best_sum = ssum\n                    best_C = C.copy()\n                    best_r = radii.copy()\n\n    # final precise recompute (in case numerical issues)\n    if best_C is None:\n        # fallback to simple centered hex (should not happen)\n        best_C = make_centers(s_max * 0.9, 0.98, [0,1,1,1,0])\n    dif = best_C[:, None, :] - best_C[None, :, :]\n    dists = np.sqrt((dif**2).sum(axis=2))\n    np.fill_diagonal(dists, np.inf)\n    half_nearest = 0.5 * dists.min(axis=1)\n    dist_border = np.minimum.reduce([best_C[:,0], best_C[:,1], 1 - best_C[:,0], 1 - best_C[:,1]])\n    radii = np.minimum(dist_border, half_nearest)\n    radii = np.maximum(radii, 0.0)\n    sum_radii = float(radii.sum())\n\n    return best_C, radii, sum_radii\n# EVOLVE-BLOCK-END\n\n\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(6,6))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect(\"equal\")\n    for i,(c,r) in enumerate(zip(centers, radii)):\n        ax.add_patch(Circle(c, r, alpha=0.6, edgecolor='k'))\n        ax.text(c[0], c[1], str(i), ha='center', va='center', fontsize=7)\n    plt.title(f\"n={len(centers)} sum={sum(radii):.6f}\"); plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # visualize(centers, radii)\n```\nKey features: Alternative approach to validity, Alternative approach to sum_radii\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 0.8558, Type: High-Performer)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (margin search + Gauss-Seidel radius maximizer)\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    \"\"\"\n    Build 26 circle centers in a staggered near-hex layout and maximize radii\n    for fixed centers using a Gauss-Seidel style solver that enforces:\n      r_i <= border_i,  r_i <= d_ij - r_j  for all j!=i\n    We sample several margins to mitigate edge effects and pick the best sum.\n    \"\"\"\n    n = 26\n    cols_per_row = [5, 6, 5, 5, 5]  # sums to 26\n    rows = len(cols_per_row)\n\n    def make_centers(margin):\n        available_w = 1.0 - 2.0 * margin\n        if available_w <= 0:\n            return None\n        centers = []\n        # compute horizontal steps per row, then choose vertical hex-like step\n        steps = []\n        for cnt in cols_per_row:\n            if cnt > 1:\n                steps.append(available_w / (cnt - 1))\n        if len(steps) == 0:\n            avg_step = 0.5 * available_w\n        else:\n            avg_step = float(np.mean(steps))\n        h = avg_step * np.sqrt(3.0) / 2.0 if avg_step > 0 else 0.0\n\n        # vertical placement centered\n        total_h = h * (rows - 1)\n        y0 = 0.5 - total_h / 2.0\n        for r, cnt in enumerate(cols_per_row):\n            if cnt == 1:\n                xs = np.array([0.5])\n            else:\n                xs = margin + np.linspace(0.0, available_w, cnt)\n            if r % 2 == 1 and cnt > 1:\n                step = xs[1] - xs[0]\n                xs = xs + 0.5 * step\n            xs = np.clip(xs, margin, 1.0 - margin)\n            y = y0 + r * h\n            for x in xs:\n                centers.append((x, y))\n        C = np.array(centers)\n        # if any center lies outside [0,1], reject this margin\n        if (C.min() < 0.0) or (C.max() > 1.0):\n            return None\n        return C\n\n    def maximize_radii(C, tol=1e-9, max_iter=500):\n        n = C.shape[0]\n        # border limits\n        border = np.minimum.reduce([C[:,0], C[:,1], 1.0 - C[:,0], 1.0 - C[:,1]]).astype(float)\n        # pairwise distances\n        diff = C[:, None, :] - C[None, :, :]\n        dists = np.sqrt((diff**2).sum(axis=2))\n        np.fill_diagonal(dists, np.inf)\n\n        # initial radii: border-limited\n        r = border.copy()\n        for it in range(max_iter):\n            prev = r.copy()\n            # Gauss-Seidel pass: set each r_i to its tightest feasible upper bound\n            for i in range(n):\n                ub = border[i]\n                # compute d_ij - r_j for all j != i\n                vals = dists[i] - r\n                vals[i] = np.inf\n                ub = min(ub, float(np.min(vals)))\n                r[i] = max(0.0, ub)\n            if np.max(np.abs(r - prev)) < tol:\n                break\n\n        # final safety: fix any slight overlaps by proportional scaling per violating pair\n        for i in range(n):\n            for j in range(i+1, n):\n                d = dists[i,j]\n                if d <= 1e-12:\n                    r[i] = r[j] = 0.0\n                elif r[i] + r[j] > d:\n                    s = d / (r[i] + r[j])\n                    r[i] *= s\n                    r[j] *= s\n        return r\n\n    best_sum = -1.0\n    best_C = None\n    best_r = None\n    # sample margins (denser near small margins)\n    for margin in np.linspace(0.02, 0.12, 21):\n        C = make_centers(margin)\n        if C is None or C.shape[0] != n:\n            continue\n        r = maximize_radii(C)\n        s = float(r.sum())\n        if s > best_sum:\n            best_sum = s\n            best_C = C\n            best_r = r\n\n    # fallback: if none found (shouldn't happen), use a conservative center layout\n    if best_C is None:\n        margin = 0.06\n        best_C = make_centers(margin)\n        best_r = maximize_radii(best_C)\n\n    return best_C, best_r, float(np.sum(best_r))\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nUnique approach: Modification: Full rewrite, Excellent validity (1.000), Excellent sum_radii (2.255)\n\n### Inspiration 2 (Score: 0.7942, Type: Alternative)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Improved explicit constructor for n=26 circles: flexible hex-layer pattern with small parameter search\"\"\"\nimport numpy as np\nfrom math import sqrt\n\ndef construct_packing():\n    rows = [4, 5, 6, 6, 5]  # sums to 26\n    R = len(rows)\n\n    def make_centers(s, v_scale, mask):\n        # vertical spacing (hex packing) scaled by v_scale\n        h = s * sqrt(3) / 2.0 * v_scale\n        centers = []\n        y0 = 0.5 - (R - 1) * h / 2.0\n        for r, L in enumerate(rows):\n            y = y0 + r * h\n            # offset either 0 or s/2 depending on mask\n            offset = (s * 0.5) if mask[r] else 0.0\n            x0 = 0.5 - (L - 1) * s / 2.0 + offset\n            xs = x0 + np.arange(L) * s\n            for x in xs:\n                centers.append((x, y))\n        C = np.array(centers)\n        return C\n\n    # find maximal s that fits with default v_scale=1 and no offsets (conservative)\n    def fits_in_square(s):\n        C = make_centers(s, 1.0, [0]*R)\n        return (C.min() >= 0.0 - 1e-12) and (C.max() <= 1.0 + 1e-12)\n\n    lo, hi = 1e-8, 1.0\n    for _ in range(40):\n        mid = 0.5 * (lo + hi)\n        if fits_in_square(mid):\n            lo = mid\n        else:\n            hi = mid\n    s_max = lo\n\n    # search neighborhood of s and small vertical compressions; use symmetric offset masks\n    s_samples = np.linspace(max(1e-8, 0.45 * s_max), s_max, 48)\n    v_scales = np.linspace(0.92, 1.0, 7)\n\n    # generate symmetric masks for rows to reduce search (mask length R)\n    masks = []\n    half = (R + 1) // 2\n    for bits in range(1 << half):\n        mask = [0] * R\n        for i in range(half):\n            bit = (bits >> i) & 1\n            mask[i] = bit\n            mask[R - 1 - i] = bit\n        masks.append(mask)\n\n    best_sum = -1.0\n    best_C = None\n    best_r = None\n\n    for s in s_samples:\n        for v in v_scales:\n            for mask in masks:\n                C = make_centers(s, v, mask)\n                # quick bounds check\n                if (C.min() < -1e-9) or (C.max() > 1.0 + 1e-9):\n                    continue\n                # distance to border\n                dist_border = np.minimum.reduce([C[:,0], C[:,1], 1 - C[:,0], 1 - C[:,1]])\n                # pairwise distances\n                dif = C[:, None, :] - C[None, :, :]\n                dists = np.sqrt((dif**2).sum(axis=2))\n                np.fill_diagonal(dists, np.inf)\n                nearest = dists.min(axis=1)\n                radii = np.minimum(dist_border, 0.5 * nearest)\n                radii = np.maximum(radii, 0.0)\n                ssum = radii.sum()\n                if ssum > best_sum:\n                    best_sum = ssum\n                    best_C = C.copy()\n                    best_r = radii.copy()\n\n    # final precise recompute (in case numerical issues)\n    if best_C is None:\n        # fallback to simple centered hex (should not happen)\n        best_C = make_centers(s_max * 0.9, 0.98, [0,1,1,1,0])\n    dif = best_C[:, None, :] - best_C[None, :, :]\n    dists = np.sqrt((dif**2).sum(axis=2))\n    np.fill_diagonal(dists, np.inf)\n    half_nearest = 0.5 * dists.min(axis=1)\n    dist_border = np.minimum.reduce([best_C[:,0], best_C[:,1], 1 - best_C[:,0], 1 - best_C[:,1]])\n    radii = np.minimum(dist_border, half_nearest)\n    radii = np.maximum(radii, 0.0)\n    sum_radii = float(radii.sum())\n\n    return best_C, radii, sum_radii\n# EVOLVE-BLOCK-END\n\n\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(6,6))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect(\"equal\")\n    for i,(c,r) in enumerate(zip(centers, radii)):\n        ax.add_patch(Circle(c, r, alpha=0.6, edgecolor='k'))\n        ax.text(c[0], c[1], str(i), ha='center', va='center', fontsize=7)\n    plt.title(f\"n={len(centers)} sum={sum(radii):.6f}\"); plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # visualize(centers, radii)\n```\nUnique approach: Modification: Full rewrite, Excellent validity (1.000), Excellent sum_radii (2.093)\n\n### Inspiration 3 (Score: 0.7918, Type: Alternative)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for packing 26 circles in the unit square.\n\nThis version builds a near-hexagonal row layout (rows summing to 26) and\ndeterministically scans a small set of horizontal margins to trade off\nborder-limited radii vs neighbor spacing. For each candidate layout we\ncompute feasible radii via a robust greedy relaxation that enforces\nri + rj <= d_ij and ri <= dist_to_border_i. The candidate with largest\nsum of radii is returned.\n\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    n = 26\n    # Row layout: near-hex layering that sums to 26\n    cols_per_row = [5, 6, 5, 5, 5]  # total = 26\n    rows = len(cols_per_row)\n\n    # Candidate margins to try (deterministic small scan)\n    margins = np.linspace(0.05, 0.12, 9)\n\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    for margin in margins:\n        # vertical available region\n        avail_h = 1.0 - 2.0 * margin\n        if rows == 1:\n            ys = np.array([0.5])\n        else:\n            ys = margin + np.linspace(0.0, avail_h, rows)\n\n        centers = []\n        for ri, cnt in enumerate(cols_per_row):\n            if cnt == 1:\n                xs = np.array([0.5])\n            else:\n                avail_w = 1.0 - 2.0 * margin\n                xs = margin + np.linspace(0.0, avail_w, cnt)\n            # stagger alternate rows for hex-like packing\n            if ri % 2 == 1 and len(xs) > 1:\n                step = xs[1] - xs[0]\n                xs = xs + 0.5 * step\n            xs = np.clip(xs, margin, 1.0 - margin)\n            for x in xs:\n                centers.append((x, ys[ri]))\n\n        centers = np.array(centers, dtype=float)\n        if centers.shape[0] != n:\n            # skip invalid configurations\n            continue\n\n        radii = compute_max_radii_relax(centers)\n        ssum = float(np.sum(radii))\n        if ssum > best_sum:\n            best_sum = ssum\n            best_centers = centers.copy()\n            best_radii = radii.copy()\n\n    # Fallback: if no candidate worked (shouldn't happen), use central near-hex layout\n    if best_centers is None:\n        best_centers = fallback_centers(cols_per_row)\n        best_radii = compute_max_radii_relax(best_centers)\n        best_sum = float(np.sum(best_radii))\n\n    return best_centers, best_radii, float(best_sum)\n\n\ndef compute_max_radii_relax(centers, tol=1e-9, max_iter=1000):\n    \"\"\"\n    Greedy relaxation to produce feasible radii for fixed centers:\n    - start with radii = distance to nearest border\n    - iteratively enforce ri + rj <= d_ij by reducing the larger radius when violated\n    This method is fast and tends to preserve the total sum of radii.\n    \"\"\"\n    n = centers.shape[0]\n    # border limits\n    border_limits = np.minimum.reduce([centers[:, 0], centers[:, 1],\n                                      1.0 - centers[:, 0], 1.0 - centers[:, 1]])\n    radii = border_limits.copy()\n\n    # pairwise distances\n    diffs = centers[:, None, :] - centers[None, :, :]\n    dmat = np.sqrt(np.sum(diffs * diffs, axis=2))\n    np.fill_diagonal(dmat, np.inf)\n\n    # prepare sorted pairs by distance (tight constraints first)\n    pairs = [(i, j, dmat[i, j]) for i in range(n) for j in range(i + 1, n)]\n    pairs.sort(key=lambda t: t[2])\n\n    for it in range(max_iter):\n        changed = False\n        for (i, j, dij) in pairs:\n            # if dij is inf (shouldn't be), skip\n            if not np.isfinite(dij):\n                continue\n            ri = radii[i]\n            rj = radii[j]\n            if ri + rj > dij + tol:\n                # reduce the larger one preferentially so we retain more total radius\n                if ri >= rj:\n                    new_ri = max(0.0, dij - rj)\n                    if new_ri + tol < ri:\n                        radii[i] = min(new_ri, border_limits[i])\n                        changed = True\n                else:\n                    new_rj = max(0.0, dij - ri)\n                    if new_rj + tol < rj:\n                        radii[j] = min(new_rj, border_limits[j])\n                        changed = True\n\n        # ensure border constraints\n        over = radii > border_limits + tol\n        if np.any(over):\n            radii[over] = border_limits[over]\n            changed = True\n\n        if not changed:\n            break\n\n    # final clamp numerical safety\n    radii = np.maximum(radii, 0.0)\n    radii = np.minimum(radii, border_limits)\n    return radii\n\n\ndef fallback_centers(cols_per_row):\n    # simple centered layout with small margin\n    margin = 0.07\n    rows = len(cols_per_row)\n    avail_h = 1.0 - 2.0 * margin\n    ys = margin + np.linspace(0.0, avail_h, rows) if rows > 1 else np.array([0.5])\n    centers = []\n    for ri, cnt in enumerate(cols_per_row):\n        if cnt == 1:\n            xs = np.array([0.5])\n        else:\n            avail_w = 1.0 - 2.0 * margin\n            xs = margin + np.linspace(0.0, avail_w, cnt)\n        if ri % 2 == 1 and len(xs) > 1:\n            step = xs[1] - xs[0]\n            xs = xs + 0.5 * step\n        xs = np.clip(xs, margin, 1.0 - margin)\n        for x in xs:\n            centers.append((x, ys[ri]))\n    return np.array(centers[:26], dtype=float)\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nUnique approach: Modification: Full rewrite, Excellent validity (1.000), Excellent sum_radii (2.086)\n\n# Current Program\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (improved explicit layout)\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square\n    returning (centers, radii, sum_of_radii).\n    The layout is an explicit layered/hex-like arrangement tuned to\n    give large non-overlapping radii when each circle takes the\n    maximal possible radius limited by neighbors and the square boundary.\n    \"\"\"\n    n = 26\n    # row counts that sum to 26 (a near-hex layering)\n    rows = [5, 6, 5, 6, 4]\n    centers = np.zeros((n, 2))\n\n    margin = 0.03  # small margin to keep circles inside\n    y_vals = np.linspace(margin, 1.0 - margin, len(rows))\n    idx = 0\n    for r, y in enumerate(y_vals):\n        k = rows[r]\n        # base x positions evenly spaced\n        xs = np.linspace(margin, 1.0 - margin, k)\n        # offset every other row to mimic hex packing\n        if r % 2 == 1 and k > 1:\n            dx = (xs[1] - xs[0]) / 2.0\n            xs = np.clip(xs + dx, margin, 1.0 - margin)\n        for x in xs:\n            centers[idx] = [x, y]\n            idx += 1\n\n    # Compute maximal radii: limited by distance to border and half the nearest neighbor distance\n    xs = centers[:, 0]\n    ys = centers[:, 1]\n    dist_border = np.minimum(np.minimum(xs, 1 - xs), np.minimum(ys, 1 - ys))\n\n    # pairwise distances\n    diff = centers[:, None, :] - centers[None, :, :]\n    dists = np.sqrt((diff ** 2).sum(axis=2)) + np.eye(n) * 1e9  # avoid zero on diagonal\n    min_neighbor = dists.min(axis=1)\n\n    # each circle radius at most half the distance to nearest neighbor and at most distance to border\n    radii = np.minimum(dist_border, min_neighbor / 2.0)\n\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n# EVOLVE-BLOCK-END\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\n\n# Task\nRewrite the program to improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: complexity, diversity\nDifferent solutions with similar fitness but different features are valuable.\nProvide the complete new program code.\n\nIMPORTANT: Make sure your rewritten program maintains the same inputs and outputs\nas the original program, but with improved internal implementation.\n\n```python\n# Your rewritten program here\n```", "responses": ["```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 (layered hex-like layout + radius maximizer)\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    \"\"\"\n    Build 26 centers in a layered, staggered (near-hex) pattern and maximize\n    radii for fixed centers using a Gauss-Seidel style bound propagation that\n    enforces r_i <= dist_to_border_i and r_i + r_j <= d_ij for all pairs.\n    A small parameter grid (spacing, vertical scale, stagger, vertical shift)\n    is searched deterministically and the best configuration by sum of radii\n    is returned.\n    \"\"\"\n    n = 26\n    rows = [4, 5, 6, 6, 5]  # sums to 26 (near-hex)\n    R = len(rows)\n    sqrt3 = np.sqrt(3.0)\n\n    def make_centers(s, vscale, stag_frac, ty):\n        # horizontal step = s, vertical step ~ s*sqrt(3)/2 scaled by vscale\n        h = s * sqrt3 / 2.0 * vscale\n        y0 = 0.5 - (R - 1) * h / 2.0 + ty\n        centers = []\n        for r, L in enumerate(rows):\n            y = y0 + r * h\n            offset = 0.5 * stag_frac * s if (r % 2 == 1) else 0.0\n            x0 = 0.5 - (L - 1) * s / 2.0 + offset\n            xs = x0 + np.arange(L) * s\n            for x in xs:\n                centers.append((x, y))\n        C = np.array(centers, dtype=float)\n        # quick reject if out of bounds (allow tiny epsilon)\n        if (C.min() < -1e-9) or (C.max() > 1.0 + 1e-9):\n            return None\n        return C\n\n    def maximize_radii(C, tol=1e-10, max_iter=600):\n        m = C.shape[0]\n        # border limits\n        border = np.minimum.reduce([C[:,0], C[:,1], 1.0 - C[:,0], 1.0 - C[:,1]]).astype(float)\n        # pairwise distances\n        diff = C[:, None, :] - C[None, :, :]\n        dists = np.sqrt((diff**2).sum(axis=2))\n        np.fill_diagonal(dists, np.inf)\n\n        # start with border-limited radii\n        r = border.copy()\n        for it in range(max_iter):\n            prev = r.copy()\n            # Gauss-Seidel style: update each radius to tightest feasible upper bound\n            for i in range(m):\n                ub = border[i]\n                # d_ij - r_j for all j != i\n                vals = dists[i] - r\n                vals[i] = np.inf\n                # If any vals are -inf or nan, treat as very small\n                vmin = float(np.min(vals))\n                ub = min(ub, vmin)\n                r[i] = max(0.0, ub)\n            if np.max(np.abs(r - prev)) < tol:\n                break\n\n        # final safety: fix slight overlaps by pairwise proportional scaling where needed\n        for i in range(m):\n            for j in range(i+1, m):\n                d = dists[i, j]\n                if not np.isfinite(d) or d <= 1e-12:\n                    # collapse if centers coincide\n                    r[i] = r[j] = 0.0\n                elif r[i] + r[j] > d:\n                    s = d / (r[i] + r[j])\n                    r[i] *= s\n                    r[j] *= s\n        # clamp to borders\n        r = np.minimum(r, border)\n        r = np.maximum(r, 0.0)\n        return r\n\n    # Parameter grids (coarse then local refine)\n    # s chosen so pattern roughly spans width; reasonable range based on typical hex spacing\n    s_vals = np.linspace(0.08, 0.20, 28)      # horizontal spacing\n    v_vals = np.linspace(0.92, 1.08, 7)       # vertical scale\n    a_vals = np.linspace(-0.25, 0.75, 11)     # stagger fraction (allow negative to slide)\n    ty_vals = np.linspace(-0.03, 0.03, 5)     # small vertical shift\n\n    best_sum = -1.0\n    best_C = None\n    best_r = None\n\n    for s in s_vals:\n        for v in v_vals:\n            for a in a_vals:\n                for ty in ty_vals:\n                    C = make_centers(s, v, a, ty)\n                    if C is None or C.shape[0] != n:\n                        continue\n                    r = maximize_radii(C)\n                    ssum = float(r.sum())\n                    if ssum > best_sum:\n                        best_sum = ssum\n                        best_C = C.copy()\n                        best_r = r.copy()\n\n    # local refine around best if found\n    if best_C is not None:\n        # small neighborhood\n        s0 = max(1e-6, (best_C[:,0].ptp() / (max(rows)-1)))  # estimate base spacing\n        s_vals = np.linspace(max(0.06, s0*0.8), min(0.28, s0*1.2), 18)\n        v_vals = np.linspace(0.96, 1.04, 5)\n        a_vals = np.linspace(max(-0.3, a - 0.1), min(1.0, a + 0.1), 7) if 'a' in locals() else np.linspace(-0.1,0.6,7)\n        ty_vals = np.linspace(-0.02, 0.02, 5)\n        for s in s_vals:\n            for v in v_vals:\n                for a in a_vals:\n                    for ty in ty_vals:\n                        C = make_centers(s, v, a, ty)\n                        if C is None or C.shape[0] != n:\n                            continue\n                        r = maximize_radii(C)\n                        ssum = float(r.sum())\n                        if ssum > best_sum:\n                            best_sum = ssum\n                            best_C = C.copy()\n                            best_r = r.copy()\n\n    # Fallback deterministic centered near-hex if search failed\n    if best_C is None:\n        # conservative default: s chosen to fit the longest row\n        maxL = max(rows)\n        s = 0.9 / (maxL - 1) if maxL > 1 else 0.2\n        best_C = make_centers(s, 0.98, 0.5, 0.0)\n        best_r = maximize_radii(best_C)\n\n    return best_C, best_r, float(np.sum(best_r))\n# EVOLVE-BLOCK-END\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```"]}}, "artifacts_json": "{\"stderr\": \"Stage 1 evaluation failed: Program execution failed: `ptp` was removed from the ndarray class in NumPy 2.0. Use np.ptp(arr, ...) instead.\\nTraceback (most recent call last):\\n  File \\\"/Users/dustin/Projects/openevolve/.trees/global_learnings_2/examples/circle_packing/evaluator.py\\\", line 312, in evaluate_stage1\\n    centers, radii, sum_radii, stdout, stderr = run_with_timeout(program_path, timeout_seconds=600)\\n                                                ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^\\n  File \\\"/Users/dustin/Projects/openevolve/.trees/global_learnings_2/examples/circle_packing/evaluator.py\\\", line 171, in run_with_timeout\\n    raise RuntimeError(f\\\"Program execution failed: {results['error']}\\\")\\nRuntimeError: Program execution failed: `ptp` was removed from the ndarray class in NumPy 2.0. Use np.ptp(arr, ...) instead.\\n\"}", "artifact_dir": null}