{"id": "545aef0b-f53d-48db-b472-bfc1476e478d", "code": "# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for n=26 circle packing in the unit square.\n\nImproved strategy:\n- Use an explicit layered (near-hex) row pattern that sums to 26: [4,6,6,6,4].\n- Search a small deterministic grid of horizontal spacings, vertical scale factors,\n  parity offsets and tiny global shifts to produce many aligned variants.\n- For each candidate compute maximal radii by a Gauss-Seidel coordinate-wise\n  maximizer enforcing r_i <= wall_i and r_i + r_j <= d_ij.\n- Return the candidate with largest sum of radii.\n\nThe implementation is fully deterministic and avoids stochastic search.\n\"\"\"\nimport numpy as np\n\ndef make_layered_centers(hx, vscale=1.0, parity=0.0, xshift=0.0):\n    \"\"\"Make centers in rows pattern [4,6,6,6,4] using horizontal spacing hx.\"\"\"\n    pattern = [4,6,6,6,4]\n    rows = len(pattern)\n    sqrt3 = np.sqrt(3.0)\n    vy = hx * sqrt3 / 2.0 * vscale\n    centers = []\n    # compute vertical positions so rows are centered in [0,1]\n    total_height = (rows - 1) * vy\n    y0 = 0.5 - total_height / 2.0\n    for r, k in enumerate(pattern):\n        y = y0 + r * vy\n        # horizontal span: place k points centered at 0.5 with spacing hx\n        x_center_start = 0.5 - (k - 1) * hx / 2.0 + xshift\n        # apply parity offset to alternate rows to create hex staggering\n        offset = (parity * hx) if (r % 2 == 1) else 0.0\n        for j in range(k):\n            x = x_center_start + j * hx + offset\n            centers.append([x, y])\n    centers = np.array(centers[:26], dtype=float)\n    # clip tiny numerical overflow\n    centers = np.clip(centers, 1e-9, 1.0 - 1e-9)\n    return centers\n\ndef compute_max_radii(centers, tol=1e-8, max_iter=2000):\n    \"\"\"Gauss-Seidel coordinate-wise maximizer for radii under linear constraints.\"\"\"\n    n = centers.shape[0]\n    # pairwise distances\n    diff = centers[:, None, :] - centers[None, :, :]\n    D = np.sqrt(np.maximum(0.0, (diff**2).sum(axis=2)))\n    np.fill_diagonal(D, np.inf)\n    # wall distances\n    wall = np.minimum.reduce([centers[:,0], centers[:,1], 1.0-centers[:,0], 1.0-centers[:,1]])\n    # init: conservative guess half nearest neighbor but not exceeding wall\n    nearest = np.min(D, axis=1)\n    r = np.minimum(wall, 0.5 * nearest)\n    # iterate Gauss-Seidel in order of closeness to center (faster convergence)\n    centerpt = np.array([0.5,0.5])\n    order = np.argsort(np.sum((centers - centerpt)**2, axis=1))\n    inv_order = np.argsort(order)\n    for _ in range(max_iter):\n        r_old = r.copy()\n        for i in order:\n            # allowable by neighbors: d_ij - r_j\n            allow = D[i] - r\n            ri_new = min(wall[i], np.min(allow))\n            r[i] = max(0.0, ri_new)\n        if np.max(np.abs(r - r_old)) < tol:\n            break\n    # final safety: repair any small violations by proportional scaling pairwise\n    for i in range(n):\n        for j in range(i+1, n):\n            dij = D[i,j]\n            if dij <= 0:\n                r[i] = r[j] = 0.0\n            else:\n                s = r[i] + r[j]\n                if s > dij:\n                    scale = dij / s\n                    r[i] *= scale\n                    r[j] *= scale\n    r = np.minimum(r, wall)\n    r = np.maximum(r, 0.0)\n    return r\n\ndef construct_packing():\n    n = 26\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    # deterministic parameter grid tuned for compact near-hex packings\n    hx_values = np.linspace(0.155, 0.215, 13)   # horizontal spacing candidates\n    vscales = [0.96, 0.99, 1.00, 1.02]         # vertical scale adjustments\n    parities = [0.0, 0.5]                      # parity offset: 0 or half cell\n    xshifts = [0.0, -0.01, 0.01]               # tiny horizontal nudges to improve wall fit\n\n    for hx in hx_values:\n        for vscale in vscales:\n            for parity in parities:\n                for xshift in xshifts:\n                    centers = make_layered_centers(hx, vscale=vscale, parity=parity, xshift=xshift)\n                    # if any center lies very close to border due to clipping, skip small invalid configs\n                    if np.any(centers <= 1e-9) or np.any(centers >= 1.0 - 1e-9):\n                        # still allow but compute radii; don't outright skip to remain deterministic\n                        pass\n                    radii = compute_max_radii(centers)\n                    s = float(np.sum(radii))\n                    if s > best_sum + 1e-12:\n                        best_sum = s\n                        best_centers = centers.copy()\n                        best_radii = radii.copy()\n\n    # fallback safety (shouldn't happen)\n    if best_centers is None:\n        best_centers = make_layered_centers(0.19, vscale=1.0, parity=0.5)\n        best_radii = compute_max_radii(best_centers)\n        best_sum = float(np.sum(best_radii))\n\n    return best_centers, best_radii, float(best_sum)\n# EVOLVE-BLOCK-END\n\n# Fixed external interface\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\nif __name__ == \"__main__\":\n    centers, radii, s = run_packing()\n    print(f\"Sum of radii: {s:.6f}\")", "language": "python", "parent_id": "a5115d16-a6c5-4f84-ac3f-b90ec9ddbe5b", "generation": 3, "timestamp": 1759959975.8575518, "iteration_found": 26, "metrics": {"validity": 1.0, "sum_radii": 2.2149999999999994, "target_ratio": 0.8406072106261858, "combined_score": 0.8406072106261858, "eval_time": 0.11903810501098633}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Full rewrite", "parent_metrics": {"validity": 1.0, "sum_radii": 2.3206215317166197, "target_ratio": 0.8806912833839164, "combined_score": 0.8806912833839164, "eval_time": 0.0621800422668457}, "island": 0}, "prompts": {"full_rewrite_user": {"system": "You are an expert mathematician specializing in circle packing problems and computational geometry. Your task is to improve a constructor function that directly produces a specific arrangement of 26 circles in a unit square, maximizing the sum of their radii. The AlphaEvolve paper achieved a sum of 2.635 for n=26.\n\nKey geometric insights:\n- Circle packings often follow hexagonal patterns in the densest regions\n- Maximum density for infinite circle packing is pi/(2*sqrt(3)) \u2248 0.9069\n- Edge effects make square container packing harder than infinite packing\n- Circles can be placed in layers or shells when confined to a square\n- Similar radius circles often form regular patterns, while varied radii allow better space utilization\n- Perfect symmetry may not yield the optimal packing due to edge effects\n\nFocus on designing an explicit constructor that places each circle in a specific position, rather than an iterative search algorithm.\n\n\n## Evolution Insights (Global Learnings)\n\n### Common Pitfalls:\n\u26a0\ufe0f combined_score decreased by 31.8% (0.364 \u2192 0.249) (seen 6x)\n\u26a0\ufe0f sum_radii decreased by 31.8% (0.960 \u2192 0.655) (seen 5x)\n\u26a0\ufe0f target_ratio decreased by 31.8% (0.364 \u2192 0.249) (seen 5x)\n\u26a0\ufe0f validity decreased by 100.0% (1.000 \u2192 0.000) (seen 3x)", "user": "# Current Program Information\n- Fitness: 0.8807\n- Feature coordinates: No feature coordinates\n- Focus areas: - Fitness improved: 0.8116 \u2192 0.8807\n- Consider simplifying - code length exceeds 500 characters\n\n## Last Execution Output\n\n### stdout\n```\nRunning in subprocess, Python version: 3.12.1 (v3.12.1:2305ca5144, Dec  7 2023, 17:23:38) [Clang 13.0.0 (clang-1300.0.29.30)]\nProgram path: /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmp88nh3gwp.py\nCalling run_packing()...\nrun_packing() returned successfully: sum_radii = 2.3206215317166197\nResults saved to /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmpqal7lmus.py.results\n\n```\n\n### stderr\n```\n\n```\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 3\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.1386, target_ratio: 0.8116, combined_score: 0.8116, eval_time: 0.0591\n- Outcome: Mixed results\n\n### Attempt 2\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.3206, target_ratio: 0.8807, combined_score: 0.8807, eval_time: 0.0622\n- Outcome: Mixed results\n\n### Attempt 1\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.3206, target_ratio: 0.8807, combined_score: 0.8807, eval_time: 0.0627\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.8807)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for packing n=26 circles in the unit square.\n\nStrategy:\n- Generate truncated hexagonal lattices for a small set of horizontal spacings s.\n- For each spacing, take the 26 lattice points closest to the square center to form a dense patch.\n- For the chosen patch compute radii by r_i = min(dist_to_border, 0.5*nearest_neighbor_dist).\n- Pick the spacing that yields the largest sum of radii and return those centers/radii.\n\nThis explicit constructor is deterministic and aims to reproduce the compact,\nnear-hexagonal cluster that yields high total radius sum for n=26.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    center = np.array([0.5, 0.5])\n    # candidate horizontal spacings to try (covers a range around good hex spacings)\n    spacings = np.linspace(0.15, 0.24, 10)  # tuned range; deterministic\n\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    sqrt3 = np.sqrt(3.0)\n    for s in spacings:\n        v = s * sqrt3 / 2.0  # vertical spacing for hex lattice\n        pts = []\n        # Generate enough rows/columns so that we have many lattice points around the unit square\n        # Row index range chosen to cover slightly beyond [0,1]\n        # Start y slightly negative to capture staggered rows\n        y = -v\n        row = 0\n        while y <= 1.0 + v:\n            x_offset = (s / 2.0) if (row % 2 == 1) else 0.0\n            x = -s + x_offset\n            while x <= 1.0 + s:\n                if 0.0 <= x <= 1.0 and 0.0 <= y <= 1.0:\n                    pts.append((x, y))\n                x += s\n            row += 1\n            y += v\n\n        pts = np.array(pts)\n        if pts.shape[0] < n:\n            # fallback regular grid if too few points (shouldn't happen for chosen spacings)\n            m = int(np.ceil(np.sqrt(n)))\n            xs = np.linspace(0.0, 1.0, m)\n            ys = np.linspace(0.0, 1.0, m)\n            pts = np.array([[x, y] for y in ys for x in xs])\n\n        # choose the n points whose squared distance to center is smallest\n        d2 = np.sum((pts - center) ** 2, axis=1)\n        idx = np.argsort(d2)[:n]\n        centers = pts[idx].copy()\n\n        # Shift cluster so its centroid matches the square center (helps symmetry)\n        avg = centers.mean(axis=0)\n        shift = center - avg\n        centers += shift\n        # Clip to remain inside the unit square (numerical safety)\n        centers = np.clip(centers, 0.0, 1.0)\n\n        radii = compute_radii_centers(centers)\n        ssum = float(np.sum(radii))\n        if ssum > best_sum:\n            best_sum = ssum\n            best_centers = centers.copy()\n            best_radii = radii.copy()\n\n    # final safety: ensure shapes and sum returned\n    if best_centers is None:\n        # degrade gracefully: use a simple symmetric hex-like pattern\n        best_centers = make_fallback_centers(n)\n        best_radii = compute_radii_centers(best_centers)\n        best_sum = float(np.sum(best_radii))\n\n    return best_centers, best_radii, float(best_sum)\n\ndef compute_radii_centers(centers):\n    \"\"\"Compute radii as min(distance to border, 0.5*nearest_neighbor_distance).\"\"\"\n    n = centers.shape[0]\n    xs = centers[:, 0]; ys = centers[:, 1]\n    border_dists = np.minimum.reduce([xs, ys, 1.0 - xs, 1.0 - ys])\n\n    # pairwise distances\n    diff = centers[:, np.newaxis, :] - centers[np.newaxis, :, :]\n    dmat = np.sqrt(np.maximum(0.0, (diff ** 2).sum(axis=2)))\n    # exclude self by setting diagonal to +inf\n    np.fill_diagonal(dmat, np.inf)\n    nearest = np.min(dmat, axis=1)\n\n    radii = np.minimum(border_dists, 0.5 * nearest)\n    radii = np.maximum(0.0, radii)\n    return radii\n\ndef make_fallback_centers(n):\n    \"\"\"Fallback symmetric hex-like layout summing to n (used only in degenerate case).\"\"\"\n    # pattern 4,6,6,6,4 = 26\n    pattern = [4,6,6,6,4]\n    max_k = max(pattern)\n    a_x = 1.0 / (max_k + 1.0)\n    a_y = a_x * np.sqrt(3.0) / 2.0\n    rows = len(pattern)\n    centers = []\n    y0 = 0.5 - (rows - 1) / 2.0 * a_y\n    for r, k in enumerate(pattern):\n        y = y0 + r * a_y\n        x_start = 0.5 - (k - 1) / 2.0 * a_x\n        for c in range(k):\n            x = x_start + c * a_x\n            centers.append([x, y])\n    centers = np.array(centers[:n])\n    centers = np.clip(centers, 1e-8, 1 - 1e-8)\n    return centers\n\n# EVOLVE-BLOCK-END\n\n# Fixed external interface\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n# Optional simple visualizer (not used in automated eval)\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(7,7))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect('equal')\n    for c, r in zip(centers, radii):\n        ax.add_patch(Circle(c, r, alpha=0.6, edgecolor='k', linewidth=0.5))\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, s = run_packing()\n    print(f\"Sum of radii: {s:.6f}\")\n    # visualize(centers, radii)\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.3206), Performs well on target_ratio (0.8807), Performs well on combined_score (0.8807), Performs well on eval_time (0.0627)\n\n### Program 2 (Score: 0.8807)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for n=26 circle packing in the unit square.\n\nStrategy:\n- Build several dense hex-like patches (varying lattice spacing).\n- For each patch take the 26 centers closest to the square center and recentre.\n- For each center set compute maximal radii by a Gauss-Seidel coordinate-wise\n  maximizer enforcing r_i <= wall_i and r_i + r_j <= d_ij.\n- Return the best (highest sum of radii) layout.\nThis is an explicit constructor (no stochastic search).\n\"\"\"\nimport numpy as np\n\ndef hex_patch(spacing, origin=(0.0, 0.0), bbox=(0.0, 1.0, 0.0, 1.0)):\n    \"\"\"Generate hex-lattice points covering bbox with given horizontal spacing.\"\"\"\n    x0, y0 = origin\n    x_min, x_max, y_min, y_max = bbox\n    v = spacing * np.sqrt(3) / 2.0\n    pts = []\n    row = 0\n    y = y_min - 2 * v\n    # generate enough rows to cover bbox (extend a bit)\n    while y <= y_max + 2 * v:\n        offset = 0.5 * spacing if (row % 2 == 1) else 0.0\n        x = x_min - 2 * spacing + offset\n        while x <= x_max + 2 * spacing:\n            pts.append([x, y])\n            x += spacing\n        row += 1\n        y += v\n    return np.array(pts)\n\ndef pick_cluster(pts, n):\n    \"\"\"Pick n points closest to center (0.5,0.5), shift their centroid to center and clip.\"\"\"\n    center = np.array([0.5, 0.5])\n    d2 = np.sum((pts - center)**2, axis=1)\n    idx = np.argsort(d2)[:n]\n    centers = pts[idx].astype(float)\n    # shift so centroid is exactly at center (helps symmetry)\n    avg = centers.mean(axis=0)\n    shift = center - avg\n    centers += shift\n    centers = np.clip(centers, 1e-8, 1.0 - 1e-8)\n    return centers\n\ndef compute_max_radii(centers, tol=1e-9, max_iter=5000):\n    \"\"\"Gauss-Seidel coordinate-wise maximizer for radii under linear constraints.\"\"\"\n    n = centers.shape[0]\n    diff = centers[:, None, :] - centers[None, :, :]\n    D = np.sqrt(np.maximum(1e-12, (diff**2).sum(axis=2)))\n    np.fill_diagonal(D, 1e9)\n    wall = np.minimum.reduce([centers[:,0], centers[:,1], 1.0-centers[:,0], 1.0-centers[:,1]])\n    # initial guess: min(wall, half nearest neighbor) (conservative but good)\n    nearest = np.min(D, axis=1)\n    r = np.minimum(wall, 0.5 * nearest)\n    # iterate Gauss-Seidel\n    for _ in range(max_iter):\n        r_old = r.copy()\n        for i in range(n):\n            limit = np.min(D[i] - r)  # r_i <= d_ij - r_j for all j\n            newr = min(wall[i], limit)\n            r[i] = max(0.0, newr)\n        if np.max(np.abs(r - r_old)) < tol:\n            break\n    # final safety pass: repair any remaining tiny overlaps by proportional scaling\n    for i in range(n):\n        for j in range(i+1, n):\n            dij = D[i,j]\n            if dij <= 0:\n                r[i] = r[j] = 0.0\n            else:\n                s = r[i] + r[j]\n                if s > dij:\n                    scale = dij / s\n                    r[i] *= scale\n                    r[j] *= scale\n    # enforce wall bounds\n    r = np.minimum(r, wall)\n    r = np.maximum(r, 0.0)\n    return r\n\ndef construct_packing():\n    n = 26\n    # candidate spacings chosen to explore slightly denser and slightly looser hex packs\n    spacings = [0.165, 0.18, 0.195, 0.21]\n    candidates = []\n    for s in spacings:\n        pts = hex_patch(s, bbox=(0.0,1.0,0.0,1.0))\n        centers = pick_cluster(pts, n)\n        candidates.append(centers)\n        # also try the same lattice but with a half-cell horizontal shift to vary parity\n        pts2 = pts + np.array([0.5*s, 0.0])\n        centers2 = pick_cluster(pts2, n)\n        candidates.append(centers2)\n\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n    for centers in candidates:\n        radii = compute_max_radii(centers)\n        s = float(radii.sum())\n        if s > best_sum:\n            best_sum = s\n            best_centers = centers.copy()\n            best_radii = radii.copy()\n\n    # Defensive: if for some reason we have < n centers (shouldn't), pad tiny circles\n    if best_centers is None or best_centers.shape[0] < n:\n        best_centers = np.zeros((n,2))\n        best_radii = np.zeros(n)\n        # place 26 tiny circles in a coarse grid\n        m = int(np.ceil(np.sqrt(n)))\n        xs = np.linspace(0.05, 0.95, m)\n        ys = np.linspace(0.05, 0.95, m)\n        grid = np.array([[x,y] for y in ys for x in xs])[:n]\n        best_centers[:grid.shape[0]] = grid\n        best_radii[:grid.shape[0]] = 1e-4\n        best_sum = float(best_radii.sum())\n\n    return best_centers, best_radii, float(best_sum)\n# EVOLVE-BLOCK-END\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(7,7))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect(\"equal\")\n    for c,r in zip(centers, radii):\n        ax.add_patch(Circle(c, float(r), alpha=0.6, edgecolor='k'))\n    plt.title(f\"n={len(centers)}, sum={radii.sum():.6f}\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # visualize(centers, radii)\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.3206), Performs well on target_ratio (0.8807), Performs well on combined_score (0.8807), Performs well on eval_time (0.0622)\n\n### Program 3 (Score: 0.8116)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 using a truncated hexagonal lattice.\nDeterministic: generate a hex grid over the unit square, pick the 26 points\nclosest to the center to form a dense cluster, re-center slightly and assign\neach radius = min(dist to border, 0.5 * nearest-neighbor distance).\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    # Hex lattice spacing: tuned for dense packing in center\n    s = 0.20\n    v = s * np.sqrt(3) / 2.0\n\n    pts = []\n    row = 0\n    y = 0.0\n    # generate hex lattice over [0,1]x[0,1]\n    while y <= 1.0 + 1e-12:\n        xoff = (s/2.0) if (row % 2 == 1) else 0.0\n        x = xoff\n        while x <= 1.0 + 1e-12:\n            if -1e-12 <= x <= 1+1e-12 and -1e-12 <= y <= 1+1e-12:\n                pts.append((min(1.0, max(0.0, x)), min(1.0, max(0.0, y))))\n            x += s\n        row += 1\n        y += v\n\n    pts = np.array(pts)\n    if pts.shape[0] < n:\n        # fallback to regular grid\n        m = int(np.ceil(np.sqrt(n)))\n        xs = np.linspace(0.0, 1.0, m)\n        ys = np.linspace(0.0, 1.0, m)\n        grid = np.array([[x, y] for y in ys for x in xs])\n        pts = grid\n\n    center = np.array([0.5, 0.5])\n    d2 = np.sum((pts - center)**2, axis=1)\n    idx = np.argsort(d2)[:n]\n    centers = pts[idx].astype(float)\n\n    # shift cluster so centroid is exactly at center, then clip inside square\n    avg = centers.mean(axis=0)\n    shift = center - avg\n    centers += shift\n    centers = np.clip(centers, 0.0, 1.0)\n\n    radii = compute_max_radii(centers)\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\ndef compute_max_radii(centers):\n    # radius = min(distance to border, 0.5 * nearest neighbor distance)\n    n = centers.shape[0]\n    border = np.minimum.reduce([centers[:,0], centers[:,1], 1.0-centers[:,0], 1.0-centers[:,1]])\n    diff = centers[:, None, :] - centers[None, :, :]\n    D = np.sqrt(np.maximum(0.0, (diff**2).sum(axis=2)))\n    np.fill_diagonal(D, np.inf)\n    nearest = np.min(D, axis=1)\n    radii = np.minimum(border, 0.5 * nearest)\n    radii = np.maximum(0.0, radii)\n    return radii\n# EVOLVE-BLOCK-END\n\n# Fixed interface\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(6,6))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect('equal')\n    for i,(c,r) in enumerate(zip(centers, radii)):\n        ax.add_patch(Circle(c, r, alpha=0.6, edgecolor='k'))\n        ax.text(c[0], c[1], str(i), ha='center', va='center', fontsize=8)\n    plt.title(f\"n={len(centers)}, sum_radii={sum(radii):.6f}\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # visualize(centers, radii)\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.1386), Performs well on target_ratio (0.8116), Performs well on combined_score (0.8116), Performs well on eval_time (0.0591)\n\n\n\n## Diverse Programs\n\n### Program D1 (Score: 0.6955)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for n=26 circle packing in the unit square.\n\nStrategy:\n- Generate candidate compact clusters by sampling a hexagonal lattice\n  for several horizontal spacings and two lattice phases (stagger offsets).\n- For each candidate, pick the 26 lattice points nearest the square center,\n  recentre the cluster to the exact center (clamp to [0,1]).\n- Compute maximal feasible radii for fixed centers by iterating the linear\n  constraints:\n      r_i <= border_i\n      r_i + r_j <= dist(i,j)\n  using a Gauss-Seidel style reduction (r_i := min(border_i, min_j(d_ij - r_j))).\n- Pick the candidate with largest sum of radii and return centers, radii, sum.\nThis explicit constructor is deterministic and focuses on producing a dense,\nwell-packed central cluster that adapts scale/phase to the square container.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    center = np.array([0.5, 0.5])\n\n    # search over plausible hex spacings and two x-phases to align cluster\n    s_values = np.linspace(0.16, 0.26, 11)   # horizontal spacings to try\n    phases = [0.0, 0.5]  # 0 -> no extra offset, 0.5 -> half-step offset\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    for s in s_values:\n        v = s * np.sqrt(3) / 2.0\n        for phase in phases:\n            pts = []\n            # generate rows covering a little beyond the unit square to allow cluster cropping\n            # start a bit negative to allow shifting cluster center\n            y = -v * 2.0\n            row = 0\n            while y <= 1.0 + v * 2.0:\n                x_offset = (s * phase) if (row % 2 == 1) else 0.0\n                x = -s * 2.0 + x_offset\n                while x <= 1.0 + s * 2.0:\n                    pts.append((x, y))\n                    x += s\n                row += 1\n                y += v\n            pts = np.array(pts)\n            if pts.shape[0] < n:\n                continue\n            # choose n points closest to center\n            d2 = np.sum((pts - center) ** 2, axis=1)\n            idx = np.argsort(d2)[:n]\n            centers = pts[idx].astype(float)\n            # shift cluster centroid to exact center for symmetry\n            avg = centers.mean(axis=0)\n            shift = center - avg\n            centers += shift\n            centers = np.clip(centers, 1e-9, 1.0 - 1e-9)\n\n            radii = compute_max_radii_iter(centers)\n            ssum = float(np.sum(radii))\n            if ssum > best_sum + 1e-12:\n                best_sum = ssum\n                best_centers = centers.copy()\n                best_radii = radii.copy()\n\n    # fallback: if none selected (very unlikely), produce a modest hex cluster\n    if best_centers is None:\n        s = 0.20\n        v = s * np.sqrt(3) / 2.0\n        rows = []\n        y0 = 0.5 - 2 * v\n        centers = []\n        for r in range(5):\n            y = y0 + r * v\n            k = 4 + (r == 1) * 2 + (r == 2) * 2 + (r == 3) * 2  # approximate row sizes\n            x0 = 0.5 - (k - 1) * s / 2.0\n            if r % 2 == 1:\n                x0 += s / 2.0\n            for j in range(k):\n                centers.append([x0 + j * s, y])\n        centers = np.array(centers)[:n]\n        centers = np.clip(centers, 1e-9, 1.0 - 1e-9)\n        best_centers = centers\n        best_radii = compute_max_radii_iter(centers)\n        best_sum = float(np.sum(best_radii))\n\n    return best_centers, best_radii, float(best_sum)\n\n\ndef compute_max_radii_iter(centers, tol=1e-7, max_iter=5000):\n    \"\"\"Gauss-Seidel style iterative enforcement of linear non-overlap constraints.\n\n    Update rule (for each i in sequence):\n      r_i := min(border_i, min_j (d_ij - r_j))\n    Starts from border distances (conservative) and reduces until all constraints\n    satisfied and changes are small. Final safety scaling ensures pairwise feasibility.\n    \"\"\"\n    m = centers.shape[0]\n    # distances between centers\n    diff = centers[:, None, :] - centers[None, :, :]\n    D = np.sqrt(np.maximum(0.0, (diff ** 2).sum(axis=2)))\n    np.fill_diagonal(D, np.inf)\n    # distance to borders\n    border = np.minimum.reduce([centers[:, 0], centers[:, 1], 1.0 - centers[:, 0], 1.0 - centers[:, 1]])\n    # initialize radii conservatively at border distances\n    r = border.copy()\n    # Iteratively reduce radii to satisfy r_i + r_j <= D_ij\n    for it in range(max_iter):\n        r_old = r.copy()\n        # Gauss-Seidel: update in fixed order using most recent values\n        for i in range(m):\n            # neighbor bounds: d_ij - r_j\n            allow = D[i] - r\n            # min over neighbors gives upper bound; ignore self because D[i,i]=inf\n            ri_new = min(border[i], np.min(allow))\n            if ri_new < 0.0:\n                ri_new = 0.0\n            r[i] = ri_new\n        if np.max(np.abs(r - r_old)) < tol:\n            break\n\n    # final pass: enforce pairwise non-overlap by scaling violating pairs proportionally\n    for i in range(m):\n        for j in range(i + 1, m):\n            dij = D[i, j]\n            if r[i] + r[j] > dij:\n                if dij <= 0:\n                    r[i] = r[j] = 0.0\n                else:\n                    scale = dij / (r[i] + r[j])\n                    r[i] *= scale\n                    r[j] *= scale\n    # clamp to border and non-negative for safety\n    r = np.maximum(r, 0.0)\n    r = np.minimum(r, border)\n    return r\n\n# EVOLVE-BLOCK-END\n\n# Fixed external interface\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n    ax.set_xlim(0, 1); ax.set_ylim(0, 1); ax.set_aspect(\"equal\")\n    for i, (c, r) in enumerate(zip(centers, radii)):\n        ax.add_patch(Circle(c, float(r), alpha=0.6, edgecolor='k', linewidth=0.5))\n        ax.text(c[0], c[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # visualize(centers, radii)\n```\nKey features: Alternative approach to validity, Alternative approach to sum_radii\n\n### Program D2 (Score: 0.8116)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles using a truncated hexagonal lattice.\nPlaces a dense cluster of 26 centers (closest to square center) from a hex grid\nwith spacing chosen to produce compact packing, then assigns each radius as the\nminimum of distance to the unit-square border and half the nearest-neighbor distance.\nThis explicit constructor maximizes radii for fixed centers (no iterative search).\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n\n    # Hex lattice spacing chosen to give a dense cluster whose per-circle radius ~ spacing/2 ~ 0.1\n    s = 0.20  # horizontal spacing\n    v = s * np.sqrt(3) / 2.0  # vertical spacing for hex packing\n\n    pts = []\n    y = 0.0\n    row = 0\n    # generate hex lattice points inside [0,1] x [0,1]\n    while y <= 1.0 + 1e-12:\n        x_offset = (s / 2.0) if (row % 2 == 1) else 0.0\n        x = x_offset\n        while x <= 1.0 + 1e-12:\n            # clamp numerical border overshoot\n            if -1e-12 <= x <= 1 + 1e-12 and -1e-12 <= y <= 1 + 1e-12:\n                pts.append((min(1.0, max(0.0, x)), min(1.0, max(0.0, y))))\n            x += s\n        row += 1\n        y += v\n\n    pts = np.array(pts)\n    if pts.shape[0] < n:\n        # fallback: regular grid if hex grid too sparse\n        m = int(np.ceil(np.sqrt(n)))\n        xs = np.linspace(0.0, 1.0, m)\n        ys = np.linspace(0.0, 1.0, m)\n        grid = np.array([[x, y] for y in ys for x in xs])\n        pts = grid\n\n    # choose the n points closest to the center (densest patch)\n    center = np.array([0.5, 0.5])\n    dists = np.sum((pts - center) ** 2, axis=1)\n    idx = np.argsort(dists)[:n]\n    centers = pts[idx]\n\n    # Slight jitter removal: re-center cluster by shifting average to exact center\n    avg = centers.mean(axis=0)\n    shift = center - avg\n    centers = centers + shift\n    centers = np.clip(centers, 0.0, 1.0)  # ensure inside unit square\n\n    radii = compute_max_radii(centers)\n\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\ndef compute_max_radii(centers):\n    \"\"\"Assign radius to each center = min(dist to border, 0.5 * nearest neighbor distance).\"\"\"\n    n = centers.shape[0]\n    radii = np.zeros(n)\n    # distance to borders\n    border_dists = np.minimum.reduce([centers[:,0], centers[:,1], 1.0 - centers[:,0], 1.0 - centers[:,1]])\n    # pairwise distances\n    dmat = np.sqrt(np.maximum(0.0, ((centers[:,np.newaxis,:] - centers[np.newaxis,:,:])**2).sum(axis=2)))\n    # replace diagonal with +inf so min excludes self\n    np.fill_diagonal(dmat, np.inf)\n    nearest = np.min(dmat, axis=1)\n    # radius is min(border_dist, half nearest neighbor distance)\n    radii = np.minimum(border_dists, 0.5 * nearest)\n    # ensure non-negative\n    radii = np.maximum(0.0, radii)\n    return radii\n# EVOLVE-BLOCK-END\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor='k', linewidth=0.5)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nKey features: Alternative approach to validity, Alternative approach to sum_radii\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 0.6361, Type: Alternative)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (deterministic hex-like layout +\ngreedy LP-style radius maximizer)\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square\n    that attempts to maximize the sum of their radii using an explicit\n    center layout (hex-like rows) and a coordinate-wise maximization\n    of radii subject to r_i + r_j <= dist(i,j) and r_i <= wall_distance_i.\n    Returns (centers, radii, sum_of_radii).\n    \"\"\"\n    n = 26\n\n    # Row pattern that sums to 26: hex-like layering to fit the square\n    rows = [5, 6, 4, 6, 5]  # sums to 26\n    num_rows = len(rows)\n\n    margin = 0.02  # keep all centers a bit inside the unit square\n    height_avail = 1.0 - 2 * margin\n\n    # Vertical coordinates (evenly spaced)\n    ys = np.linspace(margin + height_avail / (num_rows + 1),\n                     1 - margin - height_avail / (num_rows + 1),\n                     num_rows)\n\n    centers = []\n    for ri, m in enumerate(rows):\n        # horizontal available span\n        span = 1.0 - 2 * margin\n        # spacing: place m points with equal gaps, use offset for alternate rows\n        hspace = span / (m + 1)\n        # offset to create hex-like staggering: rows with 6 get half-step offset\n        offset = 0.5 * hspace if (m % 2 == 0) else 0.0\n        y = ys[ri]\n        for k in range(m):\n            x = margin + (k + 1) * hspace + offset\n            centers.append([x, y])\n\n    centers = np.array(centers[:n])\n    centers = np.clip(centers, margin, 1.0 - margin)\n\n    radii = compute_max_radii(centers)\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers, tol=1e-9, max_iter=2000):\n    \"\"\"\n    Coordinate-wise maximization:\n    r_i <= w_i (distance to walls)\n    r_i + r_j <= d_ij (pairwise non-overlap)\n    We iterate updating each r_i := min(w_i, min_j (d_ij - r_j)) (clamped >=0)\n    in Gauss-Seidel fashion until convergence.\n    This tends to produce near-optimal sum of radii for the linear constraints.\n    \"\"\"\n    n = centers.shape[0]\n    # distances between centers\n    diff = centers[:, None, :] - centers[None, :, :]\n    dmat = np.sqrt(np.sum(diff * diff, axis=2))\n    # avoid self constraints by setting huge values on diagonal\n    np.fill_diagonal(dmat, 1e6)\n\n    # wall distances\n    w = np.minimum.reduce([centers[:, 0], centers[:, 1], 1.0 - centers[:, 0], 1.0 - centers[:, 1]])\n\n    # initialize radii conservatively at wall distances\n    r = w.copy()\n\n    for it in range(max_iter):\n        r_old = r.copy()\n        # Gauss-Seidel updates: update in sequence to accelerate convergence\n        for i in range(n):\n            # allowable by neighbors given current neighbor radii\n            # compute d_ij - r_j for all j; r_i must be <= min_j (d_ij - r_j)\n            # ensure we don't include self (dmat diag large)\n            limit_by_neighbors = np.min(dmat[i] - r)\n            new_r = min(w[i], limit_by_neighbors)\n            if new_r < 0.0:\n                new_r = 0.0\n            r[i] = new_r\n        # check convergence\n        if np.max(np.abs(r - r_old)) < tol:\n            break\n\n    return r\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, float(radius), alpha=0.6, edgecolor=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={np.sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nUnique approach: Modification: Full rewrite, Excellent validity (1.000), Excellent sum_radii (1.676)\n\n### Inspiration 2 (Score: 0.6723, Type: Alternative)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Improved constructor-based circle packing for n=26 circles.\nUses small search over hex-lattice spacings, selects the densest 26\ncenters near the square center, then maximizes radii with a\nGauss-Seidel style coordinate-wise solver for the linear constraints:\n r_i <= wall_i and r_i + r_j <= d_ij.\nThis deterministic constructor returns centers, radii, sum_radii.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    center = np.array([0.5, 0.5])\n\n    def hex_points(s):\n        # horizontal spacing s, vertical spacing v\n        v = s * np.sqrt(3) / 2.0\n        pts = []\n        row = 0\n        y = 0.0\n        while y <= 1.0 + 1e-12:\n            x0 = (s / 2.0) if (row % 2 == 1) else 0.0\n            x = x0\n            while x <= 1.0 + 1e-12:\n                if -1e-12 <= x <= 1 + 1e-12 and -1e-12 <= y <= 1 + 1e-12:\n                    pts.append([min(1.0, max(0.0, x)), min(1.0, max(0.0, y))])\n                x += s\n            row += 1\n            y += v\n        return np.array(pts)\n\n    def compute_max_radii(centers, tol=1e-9, max_iter=2000):\n        m = centers.shape[0]\n        # pairwise distances\n        diff = centers[:, None, :] - centers[None, :, :]\n        D = np.sqrt(np.maximum(0.0, (diff**2).sum(axis=2)))\n        np.fill_diagonal(D, 1e6)\n        # wall distances\n        w = np.minimum.reduce([centers[:,0], centers[:,1], 1.0 - centers[:,0], 1.0 - centers[:,1]])\n        # initialize radii at wall distances (conservative)\n        r = w.copy()\n        for it in range(max_iter):\n            r_old = r.copy()\n            # Gauss-Seidel updates\n            for i in range(m):\n                limit = np.min(D[i] - r)  # d_ij - r_j\n                r[i] = max(0.0, min(w[i], limit))\n            if np.max(np.abs(r - r_old)) < tol:\n                break\n        # final safety clamp (rare)\n        r = np.clip(r, 0.0, w)\n        for i in range(m):\n            for j in range(i+1,m):\n                dij = D[i,j]\n                if r[i] + r[j] > dij:\n                    if dij <= 0:\n                        r[i] = r[j] = 0.0\n                    else:\n                        scale = dij / (r[i] + r[j])\n                        r[i] *= scale\n                        r[j] *= scale\n        return r\n\n    # try a small grid of spacings to find best compact patch\n    spacer_candidates = np.linspace(0.14, 0.26, 7)  # explore tighter and looser hex spacing\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    for s in spacer_candidates:\n        pts = hex_points(s)\n        if pts.shape[0] < n:\n            continue\n        d2 = np.sum((pts - center)**2, axis=1)\n        idx = np.argsort(d2)[:n]\n        centers = pts[idx].copy()\n        # center the patch by shifting centroid to exact center (helps wall distances)\n        avg = centers.mean(axis=0)\n        centers += (center - avg)\n        centers = np.clip(centers, 1e-9, 1.0 - 1e-9)\n        radii = compute_max_radii(centers)\n        ssum = radii.sum()\n        if ssum > best_sum:\n            best_sum = ssum\n            best_centers = centers\n            best_radii = radii\n\n    # fallback: regular centered hex pattern if search failed\n    if best_centers is None:\n        pts = hex_points(0.20)\n        d2 = np.sum((pts - center)**2, axis=1)\n        idx = np.argsort(d2)[:n]\n        best_centers = pts[idx].copy()\n        avg = best_centers.mean(axis=0)\n        best_centers += (center - avg)\n        best_centers = np.clip(best_centers, 1e-9, 1.0 - 1e-9)\n        best_radii = compute_max_radii(best_centers)\n        best_sum = best_radii.sum()\n\n    return best_centers, best_radii, float(best_sum)\n# EVOLVE-BLOCK-END\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, float(radius), alpha=0.6, edgecolor='k', linewidth=0.5)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nUnique approach: Modification: Full rewrite, Excellent validity (1.000), Excellent sum_radii (1.771)\n\n### Inspiration 3 (Score: 0.0000, Type: Exploratory)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Improved constructor-based circle packing for n=26 circles (deterministic)\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square.\n    Returns:\n        centers: np.array (26,2)\n        radii: np.array (26,)\n        sum_of_radii: float\n    Strategy:\n        - Place 26 centers in a near-hexagonal layered layout: column counts [6,5,4,5,6].\n        - Assign radii greedily in order from center-most to edge, taking the maximum\n          allowed by the border and already-assigned neighbors.\n    \"\"\"\n    n = 26\n    # Column counts symmetric to total 26\n    col_counts = [6, 5, 4, 5, 6]\n    nx = len(col_counts)\n    # horizontal positions (leave margins)\n    xs = np.linspace(0.12, 0.88, nx)\n    centers = []\n    for ci, cnt in enumerate(col_counts):\n        x = xs[ci]\n        if cnt == 1:\n            ys = np.array([0.5])\n        else:\n            ys = np.linspace(0.12, 0.88, cnt)\n            # offset every other column to approximate hex packing\n            if ci % 2 == 1:\n                # shift by half spacing while keeping inside bounds\n                s = ys[1] - ys[0]\n                ys = ys + 0.5 * s\n                ys = np.clip(ys, 0.06, 0.94)\n        for y in ys:\n            centers.append([x, y])\n    centers = np.array(centers[:n])  # ensure 26 in case of rounding\n    # Order by proximity to center (central circles first)\n    center_point = np.array([0.5, 0.5])\n    dists_to_center = np.linalg.norm(centers - center_point, axis=1)\n    order = np.argsort(dists_to_center)\n\n    # Greedy radius assignment:\n    radii = np.zeros(n)\n    border_dists = np.minimum.reduce([centers[:, 0], centers[:, 1], 1 - centers[:, 0], 1 - centers[:, 1]])\n    for idx in order:\n        # maximum allowed by border\n        rmax = border_dists[idx]\n        # reduce by already placed neighbors\n        if np.any(radii > 0):\n            # distances to already placed centers\n            placed = radii > 0\n            dists = np.linalg.norm(centers[idx] - centers[placed], axis=1)\n            # for each placed j, r_i <= dist(i,j) - r_j\n            r_allowed = np.min(dists - radii[placed])\n            rmax = min(rmax, r_allowed)\n        # guard\n        radii[idx] = max(0.0, rmax)\n\n    # Minor safety: small numerical relaxation to ensure non-overlap (clamp positives)\n    radii = np.maximum(radii - 1e-12, 0.0)\n\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.5)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nUnique approach: Modification: Full rewrite, Alternative validity approach, Alternative sum_radii approach\n\n# Current Program\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for n=26 circle packing in the unit square.\n\nStrategy:\n- Build several dense hex-like patches (varying lattice spacing).\n- For each patch take the 26 centers closest to the square center and recentre.\n- For each center set compute maximal radii by a Gauss-Seidel coordinate-wise\n  maximizer enforcing r_i <= wall_i and r_i + r_j <= d_ij.\n- Return the best (highest sum of radii) layout.\nThis is an explicit constructor (no stochastic search).\n\"\"\"\nimport numpy as np\n\ndef hex_patch(spacing, origin=(0.0, 0.0), bbox=(0.0, 1.0, 0.0, 1.0)):\n    \"\"\"Generate hex-lattice points covering bbox with given horizontal spacing.\"\"\"\n    x0, y0 = origin\n    x_min, x_max, y_min, y_max = bbox\n    v = spacing * np.sqrt(3) / 2.0\n    pts = []\n    row = 0\n    y = y_min - 2 * v\n    # generate enough rows to cover bbox (extend a bit)\n    while y <= y_max + 2 * v:\n        offset = 0.5 * spacing if (row % 2 == 1) else 0.0\n        x = x_min - 2 * spacing + offset\n        while x <= x_max + 2 * spacing:\n            pts.append([x, y])\n            x += spacing\n        row += 1\n        y += v\n    return np.array(pts)\n\ndef pick_cluster(pts, n):\n    \"\"\"Pick n points closest to center (0.5,0.5), shift their centroid to center and clip.\"\"\"\n    center = np.array([0.5, 0.5])\n    d2 = np.sum((pts - center)**2, axis=1)\n    idx = np.argsort(d2)[:n]\n    centers = pts[idx].astype(float)\n    # shift so centroid is exactly at center (helps symmetry)\n    avg = centers.mean(axis=0)\n    shift = center - avg\n    centers += shift\n    centers = np.clip(centers, 1e-8, 1.0 - 1e-8)\n    return centers\n\ndef compute_max_radii(centers, tol=1e-9, max_iter=5000):\n    \"\"\"Gauss-Seidel coordinate-wise maximizer for radii under linear constraints.\"\"\"\n    n = centers.shape[0]\n    diff = centers[:, None, :] - centers[None, :, :]\n    D = np.sqrt(np.maximum(1e-12, (diff**2).sum(axis=2)))\n    np.fill_diagonal(D, 1e9)\n    wall = np.minimum.reduce([centers[:,0], centers[:,1], 1.0-centers[:,0], 1.0-centers[:,1]])\n    # initial guess: min(wall, half nearest neighbor) (conservative but good)\n    nearest = np.min(D, axis=1)\n    r = np.minimum(wall, 0.5 * nearest)\n    # iterate Gauss-Seidel\n    for _ in range(max_iter):\n        r_old = r.copy()\n        for i in range(n):\n            limit = np.min(D[i] - r)  # r_i <= d_ij - r_j for all j\n            newr = min(wall[i], limit)\n            r[i] = max(0.0, newr)\n        if np.max(np.abs(r - r_old)) < tol:\n            break\n    # final safety pass: repair any remaining tiny overlaps by proportional scaling\n    for i in range(n):\n        for j in range(i+1, n):\n            dij = D[i,j]\n            if dij <= 0:\n                r[i] = r[j] = 0.0\n            else:\n                s = r[i] + r[j]\n                if s > dij:\n                    scale = dij / s\n                    r[i] *= scale\n                    r[j] *= scale\n    # enforce wall bounds\n    r = np.minimum(r, wall)\n    r = np.maximum(r, 0.0)\n    return r\n\ndef construct_packing():\n    n = 26\n    # candidate spacings chosen to explore slightly denser and slightly looser hex packs\n    spacings = [0.165, 0.18, 0.195, 0.21]\n    candidates = []\n    for s in spacings:\n        pts = hex_patch(s, bbox=(0.0,1.0,0.0,1.0))\n        centers = pick_cluster(pts, n)\n        candidates.append(centers)\n        # also try the same lattice but with a half-cell horizontal shift to vary parity\n        pts2 = pts + np.array([0.5*s, 0.0])\n        centers2 = pick_cluster(pts2, n)\n        candidates.append(centers2)\n\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n    for centers in candidates:\n        radii = compute_max_radii(centers)\n        s = float(radii.sum())\n        if s > best_sum:\n            best_sum = s\n            best_centers = centers.copy()\n            best_radii = radii.copy()\n\n    # Defensive: if for some reason we have < n centers (shouldn't), pad tiny circles\n    if best_centers is None or best_centers.shape[0] < n:\n        best_centers = np.zeros((n,2))\n        best_radii = np.zeros(n)\n        # place 26 tiny circles in a coarse grid\n        m = int(np.ceil(np.sqrt(n)))\n        xs = np.linspace(0.05, 0.95, m)\n        ys = np.linspace(0.05, 0.95, m)\n        grid = np.array([[x,y] for y in ys for x in xs])[:n]\n        best_centers[:grid.shape[0]] = grid\n        best_radii[:grid.shape[0]] = 1e-4\n        best_sum = float(best_radii.sum())\n\n    return best_centers, best_radii, float(best_sum)\n# EVOLVE-BLOCK-END\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(7,7))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect(\"equal\")\n    for c,r in zip(centers, radii):\n        ax.add_patch(Circle(c, float(r), alpha=0.6, edgecolor='k'))\n    plt.title(f\"n={len(centers)}, sum={radii.sum():.6f}\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # visualize(centers, radii)\n```\n\n# Task\nRewrite the program to improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: complexity, diversity\nDifferent solutions with similar fitness but different features are valuable.\nProvide the complete new program code.\n\nIMPORTANT: Make sure your rewritten program maintains the same inputs and outputs\nas the original program, but with improved internal implementation.\n\n```python\n# Your rewritten program here\n```", "responses": ["```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for n=26 circle packing in the unit square.\n\nImproved strategy:\n- Use an explicit layered (near-hex) row pattern that sums to 26: [4,6,6,6,4].\n- Search a small deterministic grid of horizontal spacings, vertical scale factors,\n  parity offsets and tiny global shifts to produce many aligned variants.\n- For each candidate compute maximal radii by a Gauss-Seidel coordinate-wise\n  maximizer enforcing r_i <= wall_i and r_i + r_j <= d_ij.\n- Return the candidate with largest sum of radii.\n\nThe implementation is fully deterministic and avoids stochastic search.\n\"\"\"\nimport numpy as np\n\ndef make_layered_centers(hx, vscale=1.0, parity=0.0, xshift=0.0):\n    \"\"\"Make centers in rows pattern [4,6,6,6,4] using horizontal spacing hx.\"\"\"\n    pattern = [4,6,6,6,4]\n    rows = len(pattern)\n    sqrt3 = np.sqrt(3.0)\n    vy = hx * sqrt3 / 2.0 * vscale\n    centers = []\n    # compute vertical positions so rows are centered in [0,1]\n    total_height = (rows - 1) * vy\n    y0 = 0.5 - total_height / 2.0\n    for r, k in enumerate(pattern):\n        y = y0 + r * vy\n        # horizontal span: place k points centered at 0.5 with spacing hx\n        x_center_start = 0.5 - (k - 1) * hx / 2.0 + xshift\n        # apply parity offset to alternate rows to create hex staggering\n        offset = (parity * hx) if (r % 2 == 1) else 0.0\n        for j in range(k):\n            x = x_center_start + j * hx + offset\n            centers.append([x, y])\n    centers = np.array(centers[:26], dtype=float)\n    # clip tiny numerical overflow\n    centers = np.clip(centers, 1e-9, 1.0 - 1e-9)\n    return centers\n\ndef compute_max_radii(centers, tol=1e-8, max_iter=2000):\n    \"\"\"Gauss-Seidel coordinate-wise maximizer for radii under linear constraints.\"\"\"\n    n = centers.shape[0]\n    # pairwise distances\n    diff = centers[:, None, :] - centers[None, :, :]\n    D = np.sqrt(np.maximum(0.0, (diff**2).sum(axis=2)))\n    np.fill_diagonal(D, np.inf)\n    # wall distances\n    wall = np.minimum.reduce([centers[:,0], centers[:,1], 1.0-centers[:,0], 1.0-centers[:,1]])\n    # init: conservative guess half nearest neighbor but not exceeding wall\n    nearest = np.min(D, axis=1)\n    r = np.minimum(wall, 0.5 * nearest)\n    # iterate Gauss-Seidel in order of closeness to center (faster convergence)\n    centerpt = np.array([0.5,0.5])\n    order = np.argsort(np.sum((centers - centerpt)**2, axis=1))\n    inv_order = np.argsort(order)\n    for _ in range(max_iter):\n        r_old = r.copy()\n        for i in order:\n            # allowable by neighbors: d_ij - r_j\n            allow = D[i] - r\n            ri_new = min(wall[i], np.min(allow))\n            r[i] = max(0.0, ri_new)\n        if np.max(np.abs(r - r_old)) < tol:\n            break\n    # final safety: repair any small violations by proportional scaling pairwise\n    for i in range(n):\n        for j in range(i+1, n):\n            dij = D[i,j]\n            if dij <= 0:\n                r[i] = r[j] = 0.0\n            else:\n                s = r[i] + r[j]\n                if s > dij:\n                    scale = dij / s\n                    r[i] *= scale\n                    r[j] *= scale\n    r = np.minimum(r, wall)\n    r = np.maximum(r, 0.0)\n    return r\n\ndef construct_packing():\n    n = 26\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    # deterministic parameter grid tuned for compact near-hex packings\n    hx_values = np.linspace(0.155, 0.215, 13)   # horizontal spacing candidates\n    vscales = [0.96, 0.99, 1.00, 1.02]         # vertical scale adjustments\n    parities = [0.0, 0.5]                      # parity offset: 0 or half cell\n    xshifts = [0.0, -0.01, 0.01]               # tiny horizontal nudges to improve wall fit\n\n    for hx in hx_values:\n        for vscale in vscales:\n            for parity in parities:\n                for xshift in xshifts:\n                    centers = make_layered_centers(hx, vscale=vscale, parity=parity, xshift=xshift)\n                    # if any center lies very close to border due to clipping, skip small invalid configs\n                    if np.any(centers <= 1e-9) or np.any(centers >= 1.0 - 1e-9):\n                        # still allow but compute radii; don't outright skip to remain deterministic\n                        pass\n                    radii = compute_max_radii(centers)\n                    s = float(np.sum(radii))\n                    if s > best_sum + 1e-12:\n                        best_sum = s\n                        best_centers = centers.copy()\n                        best_radii = radii.copy()\n\n    # fallback safety (shouldn't happen)\n    if best_centers is None:\n        best_centers = make_layered_centers(0.19, vscale=1.0, parity=0.5)\n        best_radii = compute_max_radii(best_centers)\n        best_sum = float(np.sum(best_radii))\n\n    return best_centers, best_radii, float(best_sum)\n# EVOLVE-BLOCK-END\n\n# Fixed external interface\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\nif __name__ == \"__main__\":\n    centers, radii, s = run_packing()\n    print(f\"Sum of radii: {s:.6f}\")\n```"]}}, "artifacts_json": "{\"stdout\": \"Running in subprocess, Python version: 3.12.1 (v3.12.1:2305ca5144, Dec  7 2023, 17:23:38) [Clang 13.0.0 (clang-1300.0.29.30)]\\nProgram path: /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmp8uf8gqrx.py\\nCalling run_packing()...\\nrun_packing() returned successfully: sum_radii = 2.2149999999999994\\nResults saved to /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmplxokmraj.py.results\\n\", \"stderr\": \"\"}", "artifact_dir": null}