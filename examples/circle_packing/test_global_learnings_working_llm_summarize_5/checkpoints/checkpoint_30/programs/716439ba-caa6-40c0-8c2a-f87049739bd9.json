{"id": "716439ba-caa6-40c0-8c2a-f87049739bd9", "code": "# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for n=26 circles in the unit square.\n\nStrategy (explicit constructor):\n- Generate truncated hexagonal lattices for a small set of horizontal spacings s.\n- For each spacing take the 26 lattice points closest to the square center to form a dense patch.\n- Recentre the patch and compute radii by a deterministic Gauss-Seidel maximizer\n  enforcing r_i <= wall_i and r_i + r_j <= d_ij (pairwise non-overlap).\n- Return the best (largest sum of radii) layout found.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    center = np.array([0.5, 0.5])\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    # Candidate horizontal spacings near values that produce dense hex patches\n    spacings = np.linspace(0.155, 0.225, 15)\n    sqrt3 = np.sqrt(3.0)\n\n    for s in spacings:\n        v = s * sqrt3 / 2.0\n        pts = []\n        # generate a hex lattice covering slightly beyond the unit square\n        y = -v\n        row = 0\n        while y <= 1.0 + v:\n            xoff = (s / 2.0) if (row % 2 == 1) else 0.0\n            x = -s + xoff\n            while x <= 1.0 + s:\n                if 0.0 <= x <= 1.0 and 0.0 <= y <= 1.0:\n                    pts.append((x, y))\n                x += s\n            row += 1\n            y += v\n        pts = np.array(pts)\n        if pts.shape[0] < n:\n            # fallback regular grid\n            m = int(np.ceil(np.sqrt(n)))\n            xs = np.linspace(0.0, 1.0, m)\n            ys = np.linspace(0.0, 1.0, m)\n            pts = np.array([[x, y] for y in ys for x in xs])\n\n        # choose the n points closest to the square center\n        d2 = np.sum((pts - center)**2, axis=1)\n        idx = np.argsort(d2)[:n]\n        centers = pts[idx].astype(float)\n\n        # shift cluster centroid to the square center\n        centers += (center - centers.mean(axis=0))\n        centers = np.clip(centers, 1e-10, 1.0 - 1e-10)\n\n        radii = maximize_radii_gs(centers)\n        ssum = float(np.sum(radii))\n        if ssum > best_sum:\n            best_sum = ssum\n            best_centers = centers.copy()\n            best_radii = radii.copy()\n\n    # safety fallback: symmetric hex-like pattern [4,6,6,6,4]\n    if best_centers is None:\n        best_centers = make_fallback_centers(n)\n        best_radii = maximize_radii_gs(best_centers)\n        best_sum = float(np.sum(best_radii))\n\n    return best_centers, best_radii, float(best_sum)\n\ndef maximize_radii_gs(centers, tol=1e-9, max_iter=3000):\n    \"\"\"Gauss-Seidel coordinate-wise maximizer enforcing linear non-overlap constraints.\"\"\"\n    n = centers.shape[0]\n    # pairwise distances\n    diff = centers[:, None, :] - centers[None, :, :]\n    D = np.sqrt(np.maximum(0.0, (diff**2).sum(axis=2)))\n    np.fill_diagonal(D, np.inf)\n\n    # wall distances\n    w = np.minimum.reduce([centers[:,0], centers[:,1], 1.0 - centers[:,0], 1.0 - centers[:,1]])\n\n    # initial feasible radii: conservative\n    nearest = np.min(D, axis=1)\n    r = np.minimum(w, 0.5 * nearest)\n    r = np.clip(r, 0.0, w)\n\n    for _ in range(max_iter):\n        r_old = r.copy()\n        # update each radius deterministically\n        for i in range(n):\n            # r_i <= w[i] and r_i <= D[i,j] - r_j for all j\n            limits = D[i] - r\n            newr = min(w[i], np.min(limits))\n            r[i] = max(0.0, newr)\n        if np.max(np.abs(r - r_old)) < tol:\n            break\n\n    # final overlap repair (proportional scaling for violating pairs)\n    for i in range(n):\n        for j in range(i+1, n):\n            dij = D[i, j]\n            if dij <= 0:\n                r[i] = r[j] = 0.0\n            else:\n                s = r[i] + r[j]\n                if s > dij:\n                    factor = dij / s\n                    r[i] *= factor\n                    r[j] *= factor\n\n    r = np.clip(r, 0.0, w)\n    return r\n\ndef make_fallback_centers(n):\n    \"\"\"Pattern rows [4,6,6,6,4] centered in unit square (total 26).\"\"\"\n    pattern = [4,6,6,6,4]\n    max_k = max(pattern)\n    a_x = 1.0 / (max_k + 1.0)\n    a_y = a_x * np.sqrt(3.0) / 2.0\n    rows = len(pattern)\n    centers = []\n    y0 = 0.5 - (rows - 1) / 2.0 * a_y\n    for r, k in enumerate(pattern):\n        y = y0 + r * a_y\n        x_start = 0.5 - (k - 1) / 2.0 * a_x\n        for c in range(k):\n            x = x_start + c * a_x\n            centers.append([x, y])\n    centers = np.array(centers[:n])\n    return np.clip(centers, 1e-8, 1 - 1e-8)\n\n# EVOLVE-BLOCK-END\n\n# Fixed external interface\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\nif __name__ == \"__main__\":\n    c, r, s = run_packing()\n    print(f\"Sum of radii: {s:.6f}\")", "language": "python", "parent_id": "16c728c7-3aa8-4171-b469-bebcf53b03d1", "generation": 2, "timestamp": 1759960006.446442, "iteration_found": 31, "metrics": {"validity": 1.0, "sum_radii": 2.32062153171662, "target_ratio": 0.8806912833839167, "combined_score": 0.8806912833839167, "eval_time": 0.06184506416320801}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Full rewrite", "parent_metrics": {"validity": 0.0, "sum_radii": 0.0, "target_ratio": 0.0, "combined_score": 0.0}, "island": 0}, "prompts": {"full_rewrite_user": {"system": "You are an expert mathematician specializing in circle packing problems and computational geometry. Your task is to improve a constructor function that directly produces a specific arrangement of 26 circles in a unit square, maximizing the sum of their radii. The AlphaEvolve paper achieved a sum of 2.635 for n=26.\n\nKey geometric insights:\n- Circle packings often follow hexagonal patterns in the densest regions\n- Maximum density for infinite circle packing is pi/(2*sqrt(3)) \u2248 0.9069\n- Edge effects make square container packing harder than infinite packing\n- Circles can be placed in layers or shells when confined to a square\n- Similar radius circles often form regular patterns, while varied radii allow better space utilization\n- Perfect symmetry may not yield the optimal packing due to edge effects\n\nFocus on designing an explicit constructor that places each circle in a specific position, rather than an iterative search algorithm.\n\n\n## Evolution Insights (Global Learnings)\n\n### Common Pitfalls:\n\u26a0\ufe0f combined_score decreased by 31.8% (0.364 \u2192 0.249) (seen 7x)\n\u26a0\ufe0f sum_radii decreased by 31.8% (0.960 \u2192 0.655) (seen 6x)\n\u26a0\ufe0f target_ratio decreased by 31.8% (0.364 \u2192 0.249) (seen 6x)\n\u26a0\ufe0f validity decreased by 100.0% (1.000 \u2192 0.000) (seen 3x)", "user": "# Current Program Information\n- Fitness: 0.0000\n- Feature coordinates: No feature coordinates\n- Focus areas: - Fitness declined: 0.8807 \u2192 0.0000. Consider revising recent changes.\n- Consider simplifying - code length exceeds 500 characters\n\n## Last Execution Output\n\n### stdout\n```\nRunning in subprocess, Python version: 3.12.1 (v3.12.1:2305ca5144, Dec  7 2023, 17:23:38) [Clang 13.0.0 (clang-1300.0.29.30)]\nProgram path: /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmpci9w2c3q.py\nCalling run_packing()...\nrun_packing() returned successfully: sum_radii = 1.4188189698722466\nResults saved to /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmpjkpnqq4s.py.results\n\n```\n\n### stderr\n```\n\n```\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 3\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.3206, target_ratio: 0.8807, combined_score: 0.8807, eval_time: 0.0627\n- Outcome: Mixed results\n\n### Attempt 2\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.3206, target_ratio: 0.8807, combined_score: 0.8807, eval_time: 0.0581\n- Outcome: Mixed results\n\n### Attempt 1\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.3206, target_ratio: 0.8807, combined_score: 0.8807, eval_time: 0.0616\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.8807)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for n=26 circles in the unit square.\n\nStrategy:\n- Generate hexagonal lattices for a small set of horizontal spacings s.\n- For each spacing take the 26 lattice points closest to the square center,\n  shift their centroid to the center and compute radii.\n- Radii are refined by a deterministic Gauss-Seidel style maximizer that\n  enforces r_i <= wall_i and r_i + r_j <= d_ij (non-overlap).\n- Pick the spacing that yields the largest total radius sum.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    center = np.array([0.5, 0.5])\n    # Search spacings around values known to produce dense central hex patches\n    spacings = np.linspace(0.16, 0.22, 13)\n\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    for s in spacings:\n        v = s * np.sqrt(3.0) / 2.0\n        pts = []\n        # generate hex lattice over a slightly extended region to capture center patch\n        y = -v\n        row = 0\n        while y <= 1.0 + v:\n            xoff = (s / 2.0) if (row % 2 == 1) else 0.0\n            x = -s + xoff\n            while x <= 1.0 + s:\n                if 0.0 <= x <= 1.0 and 0.0 <= y <= 1.0:\n                    pts.append((x, y))\n                x += s\n            row += 1\n            y += v\n        pts = np.array(pts)\n        if pts.shape[0] < n:\n            # fallback regular grid\n            m = int(np.ceil(np.sqrt(n)))\n            xs = np.linspace(0.0, 1.0, m)\n            ys = np.linspace(0.0, 1.0, m)\n            pts = np.array([[x, y] for y in ys for x in xs])\n\n        # select n points closest to center\n        d2 = np.sum((pts - center) ** 2, axis=1)\n        idx = np.argsort(d2)[:n]\n        centers = pts[idx].astype(float)\n\n        # center the cluster\n        avg = centers.mean(axis=0)\n        centers += (center - avg)\n        centers = np.clip(centers, 1e-10, 1 - 1e-10)\n\n        # compute radii and refine via Gauss-Seidel\n        radii = maximize_radii_gs(centers)\n        ssum = float(np.sum(radii))\n        if ssum > best_sum:\n            best_sum = ssum\n            best_centers = centers.copy()\n            best_radii = radii.copy()\n\n    # safety fallback\n    if best_centers is None:\n        # simple centered hex-like fallback (pattern sums to 26)\n        best_centers = make_fallback_centers(n)\n        best_radii = maximize_radii_gs(best_centers)\n        best_sum = float(np.sum(best_radii))\n\n    return best_centers, best_radii, float(best_sum)\n\ndef maximize_radii_gs(centers, tol=1e-9, max_iter=2000):\n    \"\"\"Gauss-Seidel coordinate-wise maximizer for linear packing constraints.\"\"\"\n    n = centers.shape[0]\n    # distances between centers\n    diff = centers[:, None, :] - centers[None, :, :]\n    D = np.sqrt(np.maximum(0.0, np.sum(diff * diff, axis=2)))\n    np.fill_diagonal(D, np.inf)  # ignore self\n\n    # wall distances\n    w = np.minimum.reduce([centers[:,0], centers[:,1], 1.0 - centers[:,0], 1.0 - centers[:,1]])\n\n    # initial radii: conservative estimate\n    # use min(w, 0.5*nearest) to be feasible\n    nearest = np.min(D, axis=1)\n    r = np.minimum(w, 0.5 * nearest)\n\n    for it in range(max_iter):\n        r_old = r.copy()\n        # update each variable in sequence (deterministic)\n        for i in range(n):\n            # r_i <= w[i]\n            # and r_i <= min_j (D[i,j] - r_j)\n            limits = D[i] - r  # for all j, includes inf on diagonal\n            new_r = np.minimum(w[i], np.min(limits))\n            if new_r < 0.0:\n                new_r = 0.0\n            r[i] = new_r\n        if np.max(np.abs(r - r_old)) < tol:\n            break\n\n    # final safety clamp\n    r = np.clip(r, 0.0, w)\n    # enforce pairwise non-overlap strictly (tiny numerical correction)\n    for i in range(n):\n        for j in range(i+1, n):\n            dij = D[i, j]\n            if r[i] + r[j] > dij:\n                if dij <= 0:\n                    r[i] = r[j] = 0.0\n                else:\n                    factor = dij / (r[i] + r[j])\n                    r[i] *= factor\n                    r[j] *= factor\n    return r\n\ndef make_fallback_centers(n):\n    \"\"\"Fallback symmetric hex-like layout summing to 26: rows [4,6,6,6,4].\"\"\"\n    pattern = [4,6,6,6,4]\n    sqrt3 = np.sqrt(3.0)\n    max_k = max(pattern)\n    a_x = 1.0 / (max_k + 1.0)\n    a_y = a_x * sqrt3 / 2.0\n    rows = len(pattern)\n    centers = []\n    y0 = 0.5 - (rows - 1) / 2.0 * a_y\n    for r, k in enumerate(pattern):\n        y = y0 + r * a_y\n        x_start = 0.5 - (k - 1) / 2.0 * a_x\n        for c in range(k):\n            x = x_start + c * a_x\n            centers.append([x, y])\n    centers = np.array(centers[:n])\n    centers = np.clip(centers, 1e-8, 1 - 1e-8)\n    return centers\n\n# EVOLVE-BLOCK-END\n\n# Fixed external interface\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n# Optional visualizer (not used in automated eval)\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(6,6))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect('equal')\n    for c,r in zip(centers, radii):\n        ax.add_patch(Circle(c, r, alpha=0.6, edgecolor='k', linewidth=0.4))\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, s = run_packing()\n    print(f\"Sum of radii: {s:.6f}\")\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.3206), Performs well on target_ratio (0.8807), Performs well on combined_score (0.8807), Performs well on eval_time (0.0616)\n\n### Program 2 (Score: 0.8807)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for n=26 circles in the unit square.\nCreates truncated hex lattices for a range of spacings, selects the 26\npoints closest to the center for a dense patch, recenters the patch,\nand assigns radii r = min(dist_to_border, 0.5*nearest_neighbor_dist).\nReturns centers, radii, sum_radii.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    center = np.array([0.5, 0.5])\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n    sqrt3 = np.sqrt(3.0)\n    # try a small range of horizontal spacings around the empirically good value\n    for s in np.linspace(0.15, 0.24, 10):\n        v = s * sqrt3 / 2.0\n        pts = []\n        # generate hex lattice covering a little beyond the unit square\n        y = -v\n        row = 0\n        while y <= 1.0 + v:\n            xoff = (s/2.0) if (row % 2 == 1) else 0.0\n            x = -s + xoff\n            while x <= 1.0 + s:\n                if 0.0 <= x <= 1.0 and 0.0 <= y <= 1.0:\n                    pts.append((x,y))\n                x += s\n            row += 1\n            y += v\n        pts = np.array(pts)\n        if pts.shape[0] < n:\n            m = int(np.ceil(np.sqrt(n)))\n            xs = np.linspace(0.0,1.0,m); ys = np.linspace(0.0,1.0,m)\n            pts = np.array([[x,y] for y in ys for x in xs])\n        # pick closest n to center\n        d2 = np.sum((pts - center)**2,axis=1)\n        idx = np.argsort(d2)[:n]\n        centers = pts[idx].astype(float)\n        # recentre cluster\n        shift = center - centers.mean(axis=0)\n        centers += shift\n        centers = np.clip(centers, 0.0, 1.0)\n        radii = compute_radii(centers)\n        ssum = float(np.sum(radii))\n        if ssum > best_sum:\n            best_sum = ssum; best_centers = centers.copy(); best_radii = radii.copy()\n    # fallback\n    if best_centers is None:\n        best_centers = fallback_centers(n)\n        best_radii = compute_radii(best_centers)\n        best_sum = float(np.sum(best_radii))\n    return best_centers, best_radii, float(best_sum)\n\ndef compute_radii(centers):\n    xs = centers[:,0]; ys = centers[:,1]\n    border = np.minimum.reduce([xs, ys, 1.0-xs, 1.0-ys])\n    diff = centers[:,None,:] - centers[None,:,:]\n    D = np.sqrt(np.maximum(0.0, (diff**2).sum(axis=2)))\n    np.fill_diagonal(D, np.inf)\n    nearest = np.min(D, axis=1)\n    radii = np.minimum(border, 0.5*nearest)\n    radii = np.maximum(0.0, radii)\n    return radii\n\ndef fallback_centers(n):\n    # pattern 4,6,6,6,4 = 26\n    pattern = [4,6,6,6,4]\n    max_k = max(pattern)\n    a_x = 1.0/(max_k+1.0)\n    a_y = a_x * np.sqrt(3.0)/2.0\n    rows = len(pattern)\n    centers = []\n    y0 = 0.5 - (rows-1)/2.0 * a_y\n    for r,k in enumerate(pattern):\n        y = y0 + r*a_y\n        x_start = 0.5 - (k-1)/2.0 * a_x\n        for c in range(k):\n            centers.append([x_start + c*a_x, y])\n    centers = np.array(centers[:n])\n    return np.clip(centers, 1e-8, 1-1e-8)\n# EVOLVE-BLOCK-END\n\n# Fixed interface\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\nif __name__ == \"__main__\":\n    c, r, s = run_packing()\n    print(f\"Sum of radii: {s:.6f}\")\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.3206), Performs well on target_ratio (0.8807), Performs well on combined_score (0.8807), Performs well on eval_time (0.0581)\n\n### Program 3 (Score: 0.8807)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for packing n=26 circles in the unit square.\n\nStrategy:\n- Generate truncated hexagonal lattices for a small set of horizontal spacings s.\n- For each spacing, take the 26 lattice points closest to the square center to form a dense patch.\n- For the chosen patch compute radii by r_i = min(dist_to_border, 0.5*nearest_neighbor_dist).\n- Pick the spacing that yields the largest sum of radii and return those centers/radii.\n\nThis explicit constructor is deterministic and aims to reproduce the compact,\nnear-hexagonal cluster that yields high total radius sum for n=26.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    center = np.array([0.5, 0.5])\n    # candidate horizontal spacings to try (covers a range around good hex spacings)\n    spacings = np.linspace(0.15, 0.24, 10)  # tuned range; deterministic\n\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    sqrt3 = np.sqrt(3.0)\n    for s in spacings:\n        v = s * sqrt3 / 2.0  # vertical spacing for hex lattice\n        pts = []\n        # Generate enough rows/columns so that we have many lattice points around the unit square\n        # Row index range chosen to cover slightly beyond [0,1]\n        # Start y slightly negative to capture staggered rows\n        y = -v\n        row = 0\n        while y <= 1.0 + v:\n            x_offset = (s / 2.0) if (row % 2 == 1) else 0.0\n            x = -s + x_offset\n            while x <= 1.0 + s:\n                if 0.0 <= x <= 1.0 and 0.0 <= y <= 1.0:\n                    pts.append((x, y))\n                x += s\n            row += 1\n            y += v\n\n        pts = np.array(pts)\n        if pts.shape[0] < n:\n            # fallback regular grid if too few points (shouldn't happen for chosen spacings)\n            m = int(np.ceil(np.sqrt(n)))\n            xs = np.linspace(0.0, 1.0, m)\n            ys = np.linspace(0.0, 1.0, m)\n            pts = np.array([[x, y] for y in ys for x in xs])\n\n        # choose the n points whose squared distance to center is smallest\n        d2 = np.sum((pts - center) ** 2, axis=1)\n        idx = np.argsort(d2)[:n]\n        centers = pts[idx].copy()\n\n        # Shift cluster so its centroid matches the square center (helps symmetry)\n        avg = centers.mean(axis=0)\n        shift = center - avg\n        centers += shift\n        # Clip to remain inside the unit square (numerical safety)\n        centers = np.clip(centers, 0.0, 1.0)\n\n        radii = compute_radii_centers(centers)\n        ssum = float(np.sum(radii))\n        if ssum > best_sum:\n            best_sum = ssum\n            best_centers = centers.copy()\n            best_radii = radii.copy()\n\n    # final safety: ensure shapes and sum returned\n    if best_centers is None:\n        # degrade gracefully: use a simple symmetric hex-like pattern\n        best_centers = make_fallback_centers(n)\n        best_radii = compute_radii_centers(best_centers)\n        best_sum = float(np.sum(best_radii))\n\n    return best_centers, best_radii, float(best_sum)\n\ndef compute_radii_centers(centers):\n    \"\"\"Compute radii as min(distance to border, 0.5*nearest_neighbor_distance).\"\"\"\n    n = centers.shape[0]\n    xs = centers[:, 0]; ys = centers[:, 1]\n    border_dists = np.minimum.reduce([xs, ys, 1.0 - xs, 1.0 - ys])\n\n    # pairwise distances\n    diff = centers[:, np.newaxis, :] - centers[np.newaxis, :, :]\n    dmat = np.sqrt(np.maximum(0.0, (diff ** 2).sum(axis=2)))\n    # exclude self by setting diagonal to +inf\n    np.fill_diagonal(dmat, np.inf)\n    nearest = np.min(dmat, axis=1)\n\n    radii = np.minimum(border_dists, 0.5 * nearest)\n    radii = np.maximum(0.0, radii)\n    return radii\n\ndef make_fallback_centers(n):\n    \"\"\"Fallback symmetric hex-like layout summing to n (used only in degenerate case).\"\"\"\n    # pattern 4,6,6,6,4 = 26\n    pattern = [4,6,6,6,4]\n    max_k = max(pattern)\n    a_x = 1.0 / (max_k + 1.0)\n    a_y = a_x * np.sqrt(3.0) / 2.0\n    rows = len(pattern)\n    centers = []\n    y0 = 0.5 - (rows - 1) / 2.0 * a_y\n    for r, k in enumerate(pattern):\n        y = y0 + r * a_y\n        x_start = 0.5 - (k - 1) / 2.0 * a_x\n        for c in range(k):\n            x = x_start + c * a_x\n            centers.append([x, y])\n    centers = np.array(centers[:n])\n    centers = np.clip(centers, 1e-8, 1 - 1e-8)\n    return centers\n\n# EVOLVE-BLOCK-END\n\n# Fixed external interface\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n# Optional simple visualizer (not used in automated eval)\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(7,7))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect('equal')\n    for c, r in zip(centers, radii):\n        ax.add_patch(Circle(c, r, alpha=0.6, edgecolor='k', linewidth=0.5))\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, s = run_packing()\n    print(f\"Sum of radii: {s:.6f}\")\n    # visualize(centers, radii)\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.3206), Performs well on target_ratio (0.8807), Performs well on combined_score (0.8807), Performs well on eval_time (0.0627)\n\n\n\n## Diverse Programs\n\n### Program D1 (Score: 0.8807)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for n=26 circle packing in the unit square.\n\nStrategy:\n- Build several dense hex-like patches (varying lattice spacing).\n- For each patch take the 26 centers closest to the square center and recentre.\n- For each center set compute maximal radii by a Gauss-Seidel coordinate-wise\n  maximizer enforcing r_i <= wall_i and r_i + r_j <= d_ij.\n- Return the best (highest sum of radii) layout.\nThis is an explicit constructor (no stochastic search).\n\"\"\"\nimport numpy as np\n\ndef hex_patch(spacing, origin=(0.0, 0.0), bbox=(0.0, 1.0, 0.0, 1.0)):\n    \"\"\"Generate hex-lattice points covering bbox with given horizontal spacing.\"\"\"\n    x0, y0 = origin\n    x_min, x_max, y_min, y_max = bbox\n    v = spacing * np.sqrt(3) / 2.0\n    pts = []\n    row = 0\n    y = y_min - 2 * v\n    # generate enough rows to cover bbox (extend a bit)\n    while y <= y_max + 2 * v:\n        offset = 0.5 * spacing if (row % 2 == 1) else 0.0\n        x = x_min - 2 * spacing + offset\n        while x <= x_max + 2 * spacing:\n            pts.append([x, y])\n            x += spacing\n        row += 1\n        y += v\n    return np.array(pts)\n\ndef pick_cluster(pts, n):\n    \"\"\"Pick n points closest to center (0.5,0.5), shift their centroid to center and clip.\"\"\"\n    center = np.array([0.5, 0.5])\n    d2 = np.sum((pts - center)**2, axis=1)\n    idx = np.argsort(d2)[:n]\n    centers = pts[idx].astype(float)\n    # shift so centroid is exactly at center (helps symmetry)\n    avg = centers.mean(axis=0)\n    shift = center - avg\n    centers += shift\n    centers = np.clip(centers, 1e-8, 1.0 - 1e-8)\n    return centers\n\ndef compute_max_radii(centers, tol=1e-9, max_iter=5000):\n    \"\"\"Gauss-Seidel coordinate-wise maximizer for radii under linear constraints.\"\"\"\n    n = centers.shape[0]\n    diff = centers[:, None, :] - centers[None, :, :]\n    D = np.sqrt(np.maximum(1e-12, (diff**2).sum(axis=2)))\n    np.fill_diagonal(D, 1e9)\n    wall = np.minimum.reduce([centers[:,0], centers[:,1], 1.0-centers[:,0], 1.0-centers[:,1]])\n    # initial guess: min(wall, half nearest neighbor) (conservative but good)\n    nearest = np.min(D, axis=1)\n    r = np.minimum(wall, 0.5 * nearest)\n    # iterate Gauss-Seidel\n    for _ in range(max_iter):\n        r_old = r.copy()\n        for i in range(n):\n            limit = np.min(D[i] - r)  # r_i <= d_ij - r_j for all j\n            newr = min(wall[i], limit)\n            r[i] = max(0.0, newr)\n        if np.max(np.abs(r - r_old)) < tol:\n            break\n    # final safety pass: repair any remaining tiny overlaps by proportional scaling\n    for i in range(n):\n        for j in range(i+1, n):\n            dij = D[i,j]\n            if dij <= 0:\n                r[i] = r[j] = 0.0\n            else:\n                s = r[i] + r[j]\n                if s > dij:\n                    scale = dij / s\n                    r[i] *= scale\n                    r[j] *= scale\n    # enforce wall bounds\n    r = np.minimum(r, wall)\n    r = np.maximum(r, 0.0)\n    return r\n\ndef construct_packing():\n    n = 26\n    # candidate spacings chosen to explore slightly denser and slightly looser hex packs\n    spacings = [0.165, 0.18, 0.195, 0.21]\n    candidates = []\n    for s in spacings:\n        pts = hex_patch(s, bbox=(0.0,1.0,0.0,1.0))\n        centers = pick_cluster(pts, n)\n        candidates.append(centers)\n        # also try the same lattice but with a half-cell horizontal shift to vary parity\n        pts2 = pts + np.array([0.5*s, 0.0])\n        centers2 = pick_cluster(pts2, n)\n        candidates.append(centers2)\n\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n    for centers in candidates:\n        radii = compute_max_radii(centers)\n        s = float(radii.sum())\n        if s > best_sum:\n            best_sum = s\n            best_centers = centers.copy()\n            best_radii = radii.copy()\n\n    # Defensive: if for some reason we have < n centers (shouldn't), pad tiny circles\n    if best_centers is None or best_centers.shape[0] < n:\n        best_centers = np.zeros((n,2))\n        best_radii = np.zeros(n)\n        # place 26 tiny circles in a coarse grid\n        m = int(np.ceil(np.sqrt(n)))\n        xs = np.linspace(0.05, 0.95, m)\n        ys = np.linspace(0.05, 0.95, m)\n        grid = np.array([[x,y] for y in ys for x in xs])[:n]\n        best_centers[:grid.shape[0]] = grid\n        best_radii[:grid.shape[0]] = 1e-4\n        best_sum = float(best_radii.sum())\n\n    return best_centers, best_radii, float(best_sum)\n# EVOLVE-BLOCK-END\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(7,7))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect(\"equal\")\n    for c,r in zip(centers, radii):\n        ax.add_patch(Circle(c, float(r), alpha=0.6, edgecolor='k'))\n    plt.title(f\"n={len(centers)}, sum={radii.sum():.6f}\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # visualize(centers, radii)\n```\nKey features: Alternative approach to validity, Alternative approach to sum_radii\n\n### Program D2 (Score: 0.8116)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for n=26 circles: take a compact hexagonal patch\nof a hex lattice, center it, and set each radius = min(dist_to_wall, 0.5*nearest_neighbor).\nThis simple explicit constructor is fast and produces a dense near-hexagonal cluster.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    # Hex lattice spacing tuned for a compact dense patch with reasonably large radii\n    s = 0.20\n    v = s * np.sqrt(3.0) / 2.0\n\n    pts = []\n    row = 0\n    y = 0.0\n    # generate hex lattice covering the unit square (include slight overshoot to ensure enough points)\n    while y <= 1.0 + 1e-12:\n        xoff = (s / 2.0) if (row % 2 == 1) else 0.0\n        x = xoff\n        while x <= 1.0 + 1e-12:\n            pts.append((min(1.0, max(0.0, x)), min(1.0, max(0.0, y))))\n            x += s\n        row += 1\n        y += v\n\n    pts = np.array(pts)\n    # pick the n points closest to the square center to form a compact cluster\n    center = np.array([0.5, 0.5])\n    d2 = np.sum((pts - center)**2, axis=1)\n    idx = np.argsort(d2)[:n]\n    centers = pts[idx].astype(float)\n\n    # Re-center cluster so centroid sits exactly at (0.5,0.5) (helps symmetry and border distance)\n    avg = centers.mean(axis=0)\n    centers += (center - avg)\n    centers = np.clip(centers, 0.0, 1.0)\n\n    radii = compute_max_radii(centers)\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\ndef compute_max_radii(centers):\n    \"\"\"For fixed centers assign radius = min(distance-to-border, 0.5*nearest-neighbor-distance).\"\"\"\n    n = centers.shape[0]\n    # distance to borders\n    border = np.minimum.reduce([centers[:,0], centers[:,1], 1.0 - centers[:,0], 1.0 - centers[:,1]])\n    # pairwise distances\n    diff = centers[:, np.newaxis, :] - centers[np.newaxis, :, :]\n    D = np.sqrt(np.maximum(0.0, (diff**2).sum(axis=2)))\n    np.fill_diagonal(D, np.inf)\n    nearest = np.min(D, axis=1)\n    radii = np.minimum(border, 0.5 * nearest)\n    radii = np.maximum(radii, 0.0)\n    return radii\n# EVOLVE-BLOCK-END\n\n# Fixed interface\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(6,6))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect(\"equal\")\n    ax.add_patch(plt.Rectangle((0,0),1,1,fill=False,linewidth=1))\n    for i,(c,r) in enumerate(zip(centers, radii)):\n        ax.add_patch(Circle(c, float(r), alpha=0.6, edgecolor='k'))\n        ax.text(c[0], c[1], str(i), ha='center', va='center', fontsize=8)\n    plt.title(f\"n={len(centers)}, sum_radii={sum(radii):.6f}\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # visualize(centers, radii)\n```\nKey features: Alternative approach to validity, Alternative approach to sum_radii\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 0.8807, Type: High-Performer)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for packing n=26 circles in the unit square.\n\nStrategy:\n- Generate truncated hexagonal lattices for a small set of horizontal spacings s.\n- For each spacing, take the 26 lattice points closest to the square center to form a dense patch.\n- For the chosen patch compute radii by r_i = min(dist_to_border, 0.5*nearest_neighbor_dist).\n- Pick the spacing that yields the largest sum of radii and return those centers/radii.\n\nThis explicit constructor is deterministic and aims to reproduce the compact,\nnear-hexagonal cluster that yields high total radius sum for n=26.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    center = np.array([0.5, 0.5])\n    # candidate horizontal spacings to try (covers a range around good hex spacings)\n    spacings = np.linspace(0.15, 0.24, 10)  # tuned range; deterministic\n\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    sqrt3 = np.sqrt(3.0)\n    for s in spacings:\n        v = s * sqrt3 / 2.0  # vertical spacing for hex lattice\n        pts = []\n        # Generate enough rows/columns so that we have many lattice points around the unit square\n        # Row index range chosen to cover slightly beyond [0,1]\n        # Start y slightly negative to capture staggered rows\n        y = -v\n        row = 0\n        while y <= 1.0 + v:\n            x_offset = (s / 2.0) if (row % 2 == 1) else 0.0\n            x = -s + x_offset\n            while x <= 1.0 + s:\n                if 0.0 <= x <= 1.0 and 0.0 <= y <= 1.0:\n                    pts.append((x, y))\n                x += s\n            row += 1\n            y += v\n\n        pts = np.array(pts)\n        if pts.shape[0] < n:\n            # fallback regular grid if too few points (shouldn't happen for chosen spacings)\n            m = int(np.ceil(np.sqrt(n)))\n            xs = np.linspace(0.0, 1.0, m)\n            ys = np.linspace(0.0, 1.0, m)\n            pts = np.array([[x, y] for y in ys for x in xs])\n\n        # choose the n points whose squared distance to center is smallest\n        d2 = np.sum((pts - center) ** 2, axis=1)\n        idx = np.argsort(d2)[:n]\n        centers = pts[idx].copy()\n\n        # Shift cluster so its centroid matches the square center (helps symmetry)\n        avg = centers.mean(axis=0)\n        shift = center - avg\n        centers += shift\n        # Clip to remain inside the unit square (numerical safety)\n        centers = np.clip(centers, 0.0, 1.0)\n\n        radii = compute_radii_centers(centers)\n        ssum = float(np.sum(radii))\n        if ssum > best_sum:\n            best_sum = ssum\n            best_centers = centers.copy()\n            best_radii = radii.copy()\n\n    # final safety: ensure shapes and sum returned\n    if best_centers is None:\n        # degrade gracefully: use a simple symmetric hex-like pattern\n        best_centers = make_fallback_centers(n)\n        best_radii = compute_radii_centers(best_centers)\n        best_sum = float(np.sum(best_radii))\n\n    return best_centers, best_radii, float(best_sum)\n\ndef compute_radii_centers(centers):\n    \"\"\"Compute radii as min(distance to border, 0.5*nearest_neighbor_distance).\"\"\"\n    n = centers.shape[0]\n    xs = centers[:, 0]; ys = centers[:, 1]\n    border_dists = np.minimum.reduce([xs, ys, 1.0 - xs, 1.0 - ys])\n\n    # pairwise distances\n    diff = centers[:, np.newaxis, :] - centers[np.newaxis, :, :]\n    dmat = np.sqrt(np.maximum(0.0, (diff ** 2).sum(axis=2)))\n    # exclude self by setting diagonal to +inf\n    np.fill_diagonal(dmat, np.inf)\n    nearest = np.min(dmat, axis=1)\n\n    radii = np.minimum(border_dists, 0.5 * nearest)\n    radii = np.maximum(0.0, radii)\n    return radii\n\ndef make_fallback_centers(n):\n    \"\"\"Fallback symmetric hex-like layout summing to n (used only in degenerate case).\"\"\"\n    # pattern 4,6,6,6,4 = 26\n    pattern = [4,6,6,6,4]\n    max_k = max(pattern)\n    a_x = 1.0 / (max_k + 1.0)\n    a_y = a_x * np.sqrt(3.0) / 2.0\n    rows = len(pattern)\n    centers = []\n    y0 = 0.5 - (rows - 1) / 2.0 * a_y\n    for r, k in enumerate(pattern):\n        y = y0 + r * a_y\n        x_start = 0.5 - (k - 1) / 2.0 * a_x\n        for c in range(k):\n            x = x_start + c * a_x\n            centers.append([x, y])\n    centers = np.array(centers[:n])\n    centers = np.clip(centers, 1e-8, 1 - 1e-8)\n    return centers\n\n# EVOLVE-BLOCK-END\n\n# Fixed external interface\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n# Optional simple visualizer (not used in automated eval)\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(7,7))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect('equal')\n    for c, r in zip(centers, radii):\n        ax.add_patch(Circle(c, r, alpha=0.6, edgecolor='k', linewidth=0.5))\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, s = run_packing()\n    print(f\"Sum of radii: {s:.6f}\")\n    # visualize(centers, radii)\n```\nUnique approach: Modification: Full rewrite, Excellent validity (1.000), Excellent sum_radii (2.321)\n\n### Inspiration 2 (Score: 0.8807, Type: High-Performer)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for n=26 circles in the unit square.\n\nStrategy:\n- Generate hexagonal lattices for a small set of horizontal spacings s.\n- For each spacing take the 26 lattice points closest to the square center,\n  shift their centroid to the center and compute radii.\n- Radii are refined by a deterministic Gauss-Seidel style maximizer that\n  enforces r_i <= wall_i and r_i + r_j <= d_ij (non-overlap).\n- Pick the spacing that yields the largest total radius sum.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    center = np.array([0.5, 0.5])\n    # Search spacings around values known to produce dense central hex patches\n    spacings = np.linspace(0.16, 0.22, 13)\n\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    for s in spacings:\n        v = s * np.sqrt(3.0) / 2.0\n        pts = []\n        # generate hex lattice over a slightly extended region to capture center patch\n        y = -v\n        row = 0\n        while y <= 1.0 + v:\n            xoff = (s / 2.0) if (row % 2 == 1) else 0.0\n            x = -s + xoff\n            while x <= 1.0 + s:\n                if 0.0 <= x <= 1.0 and 0.0 <= y <= 1.0:\n                    pts.append((x, y))\n                x += s\n            row += 1\n            y += v\n        pts = np.array(pts)\n        if pts.shape[0] < n:\n            # fallback regular grid\n            m = int(np.ceil(np.sqrt(n)))\n            xs = np.linspace(0.0, 1.0, m)\n            ys = np.linspace(0.0, 1.0, m)\n            pts = np.array([[x, y] for y in ys for x in xs])\n\n        # select n points closest to center\n        d2 = np.sum((pts - center) ** 2, axis=1)\n        idx = np.argsort(d2)[:n]\n        centers = pts[idx].astype(float)\n\n        # center the cluster\n        avg = centers.mean(axis=0)\n        centers += (center - avg)\n        centers = np.clip(centers, 1e-10, 1 - 1e-10)\n\n        # compute radii and refine via Gauss-Seidel\n        radii = maximize_radii_gs(centers)\n        ssum = float(np.sum(radii))\n        if ssum > best_sum:\n            best_sum = ssum\n            best_centers = centers.copy()\n            best_radii = radii.copy()\n\n    # safety fallback\n    if best_centers is None:\n        # simple centered hex-like fallback (pattern sums to 26)\n        best_centers = make_fallback_centers(n)\n        best_radii = maximize_radii_gs(best_centers)\n        best_sum = float(np.sum(best_radii))\n\n    return best_centers, best_radii, float(best_sum)\n\ndef maximize_radii_gs(centers, tol=1e-9, max_iter=2000):\n    \"\"\"Gauss-Seidel coordinate-wise maximizer for linear packing constraints.\"\"\"\n    n = centers.shape[0]\n    # distances between centers\n    diff = centers[:, None, :] - centers[None, :, :]\n    D = np.sqrt(np.maximum(0.0, np.sum(diff * diff, axis=2)))\n    np.fill_diagonal(D, np.inf)  # ignore self\n\n    # wall distances\n    w = np.minimum.reduce([centers[:,0], centers[:,1], 1.0 - centers[:,0], 1.0 - centers[:,1]])\n\n    # initial radii: conservative estimate\n    # use min(w, 0.5*nearest) to be feasible\n    nearest = np.min(D, axis=1)\n    r = np.minimum(w, 0.5 * nearest)\n\n    for it in range(max_iter):\n        r_old = r.copy()\n        # update each variable in sequence (deterministic)\n        for i in range(n):\n            # r_i <= w[i]\n            # and r_i <= min_j (D[i,j] - r_j)\n            limits = D[i] - r  # for all j, includes inf on diagonal\n            new_r = np.minimum(w[i], np.min(limits))\n            if new_r < 0.0:\n                new_r = 0.0\n            r[i] = new_r\n        if np.max(np.abs(r - r_old)) < tol:\n            break\n\n    # final safety clamp\n    r = np.clip(r, 0.0, w)\n    # enforce pairwise non-overlap strictly (tiny numerical correction)\n    for i in range(n):\n        for j in range(i+1, n):\n            dij = D[i, j]\n            if r[i] + r[j] > dij:\n                if dij <= 0:\n                    r[i] = r[j] = 0.0\n                else:\n                    factor = dij / (r[i] + r[j])\n                    r[i] *= factor\n                    r[j] *= factor\n    return r\n\ndef make_fallback_centers(n):\n    \"\"\"Fallback symmetric hex-like layout summing to 26: rows [4,6,6,6,4].\"\"\"\n    pattern = [4,6,6,6,4]\n    sqrt3 = np.sqrt(3.0)\n    max_k = max(pattern)\n    a_x = 1.0 / (max_k + 1.0)\n    a_y = a_x * sqrt3 / 2.0\n    rows = len(pattern)\n    centers = []\n    y0 = 0.5 - (rows - 1) / 2.0 * a_y\n    for r, k in enumerate(pattern):\n        y = y0 + r * a_y\n        x_start = 0.5 - (k - 1) / 2.0 * a_x\n        for c in range(k):\n            x = x_start + c * a_x\n            centers.append([x, y])\n    centers = np.array(centers[:n])\n    centers = np.clip(centers, 1e-8, 1 - 1e-8)\n    return centers\n\n# EVOLVE-BLOCK-END\n\n# Fixed external interface\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n# Optional visualizer (not used in automated eval)\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(6,6))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect('equal')\n    for c,r in zip(centers, radii):\n        ax.add_patch(Circle(c, r, alpha=0.6, edgecolor='k', linewidth=0.4))\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, s = run_packing()\n    print(f\"Sum of radii: {s:.6f}\")\n```\nUnique approach: Modification: Full rewrite, Excellent validity (1.000), Excellent sum_radii (2.321)\n\n### Inspiration 3 (Score: 0.8116, Type: High-Performer)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles using a truncated hexagonal lattice.\nPlaces a dense cluster of 26 centers (closest to square center) from a hex grid\nwith spacing chosen to produce compact packing, then assigns each radius as the\nminimum of distance to the unit-square border and half the nearest-neighbor distance.\nThis explicit constructor maximizes radii for fixed centers (no iterative search).\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n\n    # Hex lattice spacing chosen to give a dense cluster whose per-circle radius ~ spacing/2 ~ 0.1\n    s = 0.20  # horizontal spacing\n    v = s * np.sqrt(3) / 2.0  # vertical spacing for hex packing\n\n    pts = []\n    y = 0.0\n    row = 0\n    # generate hex lattice points inside [0,1] x [0,1]\n    while y <= 1.0 + 1e-12:\n        x_offset = (s / 2.0) if (row % 2 == 1) else 0.0\n        x = x_offset\n        while x <= 1.0 + 1e-12:\n            # clamp numerical border overshoot\n            if -1e-12 <= x <= 1 + 1e-12 and -1e-12 <= y <= 1 + 1e-12:\n                pts.append((min(1.0, max(0.0, x)), min(1.0, max(0.0, y))))\n            x += s\n        row += 1\n        y += v\n\n    pts = np.array(pts)\n    if pts.shape[0] < n:\n        # fallback: regular grid if hex grid too sparse\n        m = int(np.ceil(np.sqrt(n)))\n        xs = np.linspace(0.0, 1.0, m)\n        ys = np.linspace(0.0, 1.0, m)\n        grid = np.array([[x, y] for y in ys for x in xs])\n        pts = grid\n\n    # choose the n points closest to the center (densest patch)\n    center = np.array([0.5, 0.5])\n    dists = np.sum((pts - center) ** 2, axis=1)\n    idx = np.argsort(dists)[:n]\n    centers = pts[idx]\n\n    # Slight jitter removal: re-center cluster by shifting average to exact center\n    avg = centers.mean(axis=0)\n    shift = center - avg\n    centers = centers + shift\n    centers = np.clip(centers, 0.0, 1.0)  # ensure inside unit square\n\n    radii = compute_max_radii(centers)\n\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\ndef compute_max_radii(centers):\n    \"\"\"Assign radius to each center = min(dist to border, 0.5 * nearest neighbor distance).\"\"\"\n    n = centers.shape[0]\n    radii = np.zeros(n)\n    # distance to borders\n    border_dists = np.minimum.reduce([centers[:,0], centers[:,1], 1.0 - centers[:,0], 1.0 - centers[:,1]])\n    # pairwise distances\n    dmat = np.sqrt(np.maximum(0.0, ((centers[:,np.newaxis,:] - centers[np.newaxis,:,:])**2).sum(axis=2)))\n    # replace diagonal with +inf so min excludes self\n    np.fill_diagonal(dmat, np.inf)\n    nearest = np.min(dmat, axis=1)\n    # radius is min(border_dist, half nearest neighbor distance)\n    radii = np.minimum(border_dists, 0.5 * nearest)\n    # ensure non-negative\n    radii = np.maximum(0.0, radii)\n    return radii\n# EVOLVE-BLOCK-END\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor='k', linewidth=0.5)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nUnique approach: Modification: Full rewrite, Excellent validity (1.000), Excellent sum_radii (2.139)\n\n# Current Program\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Improved constructor-based circle packing for n=26 circles (deterministic)\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square.\n    Returns:\n        centers: np.array (26,2)\n        radii: np.array (26,)\n        sum_of_radii: float\n    Strategy:\n        - Place 26 centers in a near-hexagonal layered layout: column counts [6,5,4,5,6].\n        - Assign radii greedily in order from center-most to edge, taking the maximum\n          allowed by the border and already-assigned neighbors.\n    \"\"\"\n    n = 26\n    # Column counts symmetric to total 26\n    col_counts = [6, 5, 4, 5, 6]\n    nx = len(col_counts)\n    # horizontal positions (leave margins)\n    xs = np.linspace(0.12, 0.88, nx)\n    centers = []\n    for ci, cnt in enumerate(col_counts):\n        x = xs[ci]\n        if cnt == 1:\n            ys = np.array([0.5])\n        else:\n            ys = np.linspace(0.12, 0.88, cnt)\n            # offset every other column to approximate hex packing\n            if ci % 2 == 1:\n                # shift by half spacing while keeping inside bounds\n                s = ys[1] - ys[0]\n                ys = ys + 0.5 * s\n                ys = np.clip(ys, 0.06, 0.94)\n        for y in ys:\n            centers.append([x, y])\n    centers = np.array(centers[:n])  # ensure 26 in case of rounding\n    # Order by proximity to center (central circles first)\n    center_point = np.array([0.5, 0.5])\n    dists_to_center = np.linalg.norm(centers - center_point, axis=1)\n    order = np.argsort(dists_to_center)\n\n    # Greedy radius assignment:\n    radii = np.zeros(n)\n    border_dists = np.minimum.reduce([centers[:, 0], centers[:, 1], 1 - centers[:, 0], 1 - centers[:, 1]])\n    for idx in order:\n        # maximum allowed by border\n        rmax = border_dists[idx]\n        # reduce by already placed neighbors\n        if np.any(radii > 0):\n            # distances to already placed centers\n            placed = radii > 0\n            dists = np.linalg.norm(centers[idx] - centers[placed], axis=1)\n            # for each placed j, r_i <= dist(i,j) - r_j\n            r_allowed = np.min(dists - radii[placed])\n            rmax = min(rmax, r_allowed)\n        # guard\n        radii[idx] = max(0.0, rmax)\n\n    # Minor safety: small numerical relaxation to ensure non-overlap (clamp positives)\n    radii = np.maximum(radii - 1e-12, 0.0)\n\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.5)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\n\n# Task\nRewrite the program to improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: complexity, diversity\nDifferent solutions with similar fitness but different features are valuable.\nProvide the complete new program code.\n\nIMPORTANT: Make sure your rewritten program maintains the same inputs and outputs\nas the original program, but with improved internal implementation.\n\n```python\n# Your rewritten program here\n```", "responses": ["```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for n=26 circles in the unit square.\n\nStrategy (explicit constructor):\n- Generate truncated hexagonal lattices for a small set of horizontal spacings s.\n- For each spacing take the 26 lattice points closest to the square center to form a dense patch.\n- Recentre the patch and compute radii by a deterministic Gauss-Seidel maximizer\n  enforcing r_i <= wall_i and r_i + r_j <= d_ij (pairwise non-overlap).\n- Return the best (largest sum of radii) layout found.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    center = np.array([0.5, 0.5])\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    # Candidate horizontal spacings near values that produce dense hex patches\n    spacings = np.linspace(0.155, 0.225, 15)\n    sqrt3 = np.sqrt(3.0)\n\n    for s in spacings:\n        v = s * sqrt3 / 2.0\n        pts = []\n        # generate a hex lattice covering slightly beyond the unit square\n        y = -v\n        row = 0\n        while y <= 1.0 + v:\n            xoff = (s / 2.0) if (row % 2 == 1) else 0.0\n            x = -s + xoff\n            while x <= 1.0 + s:\n                if 0.0 <= x <= 1.0 and 0.0 <= y <= 1.0:\n                    pts.append((x, y))\n                x += s\n            row += 1\n            y += v\n        pts = np.array(pts)\n        if pts.shape[0] < n:\n            # fallback regular grid\n            m = int(np.ceil(np.sqrt(n)))\n            xs = np.linspace(0.0, 1.0, m)\n            ys = np.linspace(0.0, 1.0, m)\n            pts = np.array([[x, y] for y in ys for x in xs])\n\n        # choose the n points closest to the square center\n        d2 = np.sum((pts - center)**2, axis=1)\n        idx = np.argsort(d2)[:n]\n        centers = pts[idx].astype(float)\n\n        # shift cluster centroid to the square center\n        centers += (center - centers.mean(axis=0))\n        centers = np.clip(centers, 1e-10, 1.0 - 1e-10)\n\n        radii = maximize_radii_gs(centers)\n        ssum = float(np.sum(radii))\n        if ssum > best_sum:\n            best_sum = ssum\n            best_centers = centers.copy()\n            best_radii = radii.copy()\n\n    # safety fallback: symmetric hex-like pattern [4,6,6,6,4]\n    if best_centers is None:\n        best_centers = make_fallback_centers(n)\n        best_radii = maximize_radii_gs(best_centers)\n        best_sum = float(np.sum(best_radii))\n\n    return best_centers, best_radii, float(best_sum)\n\ndef maximize_radii_gs(centers, tol=1e-9, max_iter=3000):\n    \"\"\"Gauss-Seidel coordinate-wise maximizer enforcing linear non-overlap constraints.\"\"\"\n    n = centers.shape[0]\n    # pairwise distances\n    diff = centers[:, None, :] - centers[None, :, :]\n    D = np.sqrt(np.maximum(0.0, (diff**2).sum(axis=2)))\n    np.fill_diagonal(D, np.inf)\n\n    # wall distances\n    w = np.minimum.reduce([centers[:,0], centers[:,1], 1.0 - centers[:,0], 1.0 - centers[:,1]])\n\n    # initial feasible radii: conservative\n    nearest = np.min(D, axis=1)\n    r = np.minimum(w, 0.5 * nearest)\n    r = np.clip(r, 0.0, w)\n\n    for _ in range(max_iter):\n        r_old = r.copy()\n        # update each radius deterministically\n        for i in range(n):\n            # r_i <= w[i] and r_i <= D[i,j] - r_j for all j\n            limits = D[i] - r\n            newr = min(w[i], np.min(limits))\n            r[i] = max(0.0, newr)\n        if np.max(np.abs(r - r_old)) < tol:\n            break\n\n    # final overlap repair (proportional scaling for violating pairs)\n    for i in range(n):\n        for j in range(i+1, n):\n            dij = D[i, j]\n            if dij <= 0:\n                r[i] = r[j] = 0.0\n            else:\n                s = r[i] + r[j]\n                if s > dij:\n                    factor = dij / s\n                    r[i] *= factor\n                    r[j] *= factor\n\n    r = np.clip(r, 0.0, w)\n    return r\n\ndef make_fallback_centers(n):\n    \"\"\"Pattern rows [4,6,6,6,4] centered in unit square (total 26).\"\"\"\n    pattern = [4,6,6,6,4]\n    max_k = max(pattern)\n    a_x = 1.0 / (max_k + 1.0)\n    a_y = a_x * np.sqrt(3.0) / 2.0\n    rows = len(pattern)\n    centers = []\n    y0 = 0.5 - (rows - 1) / 2.0 * a_y\n    for r, k in enumerate(pattern):\n        y = y0 + r * a_y\n        x_start = 0.5 - (k - 1) / 2.0 * a_x\n        for c in range(k):\n            x = x_start + c * a_x\n            centers.append([x, y])\n    centers = np.array(centers[:n])\n    return np.clip(centers, 1e-8, 1 - 1e-8)\n\n# EVOLVE-BLOCK-END\n\n# Fixed external interface\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\nif __name__ == \"__main__\":\n    c, r, s = run_packing()\n    print(f\"Sum of radii: {s:.6f}\")\n```"]}}, "artifacts_json": "{\"stdout\": \"Running in subprocess, Python version: 3.12.1 (v3.12.1:2305ca5144, Dec  7 2023, 17:23:38) [Clang 13.0.0 (clang-1300.0.29.30)]\\nProgram path: /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmpjni21gov.py\\nCalling run_packing()...\\nrun_packing() returned successfully: sum_radii = 2.32062153171662\\nResults saved to /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmph4utsovg.py.results\\n\", \"stderr\": \"\"}", "artifact_dir": null}