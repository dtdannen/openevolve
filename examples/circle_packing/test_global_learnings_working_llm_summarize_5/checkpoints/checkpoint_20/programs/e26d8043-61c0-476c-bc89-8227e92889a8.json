{"id": "e26d8043-61c0-476c-bc89-8227e92889a8", "code": "# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for n=26 circle packing in the unit square.\nBuilds dense hexagonal clusters at several scales, selects the best 26-center\ncluster (closest to center) and maximizes radii under linear constraints\nr_i <= wall_i, r_i + r_j <= d_ij using a Gauss-Seidel style coordinate-wise\nmaximization. This explicit constructor is fast and yields high sum of radii.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    center = np.array([0.5, 0.5])\n\n    def make_hex_points(s, margin=1e-8):\n        v = s * np.sqrt(3) / 2.0\n        pts = []\n        # cover a bit beyond [0,1] to ensure enough candidates\n        y = -s\n        row = 0\n        while y <= 1.0 + s:\n            xoff = (s/2.0) if (row % 2 == 1) else 0.0\n            x = -s + xoff\n            while x <= 1.0 + s:\n                if -margin <= x <= 1.0 + margin and -margin <= y <= 1.0 + margin:\n                    pts.append((min(1.0, max(0.0, x)), min(1.0, max(0.0, y))))\n                x += s\n            y += v\n            row += 1\n        return np.array(pts)\n\n    def compute_max_radii(centers, tol=1e-9, max_iter=2000):\n        m = centers.shape[0]\n        # wall distances\n        w = np.minimum.reduce([centers[:,0], centers[:,1], 1.0-centers[:,0], 1.0-centers[:,1]])\n        # pairwise distances\n        diff = centers[:, None, :] - centers[None, :, :]\n        D = np.sqrt(np.maximum(0.0, np.sum(diff*diff, axis=2)))\n        np.fill_diagonal(D, np.inf)\n        r = w.copy()  # start at wall bounds (generous)\n        for it in range(max_iter):\n            r_old = r.copy()\n            # Gauss-Seidel updates: r_i = min(w_i, min_j (D_ij - r_j))\n            for i in range(m):\n                limit = np.min(D[i] - r)  # uses current r (including recently updated)\n                newr = min(w[i], limit)\n                r[i] = max(0.0, newr)\n            if np.max(np.abs(r - r_old)) < tol:\n                break\n        # final safety: enforce pairwise by proportional scaling if any tiny violations remain\n        for i in range(m):\n            for j in range(i+1, m):\n                dij = D[i,j]\n                if r[i] + r[j] > dij:\n                    if dij <= 0:\n                        r[i] = r[j] = 0.0\n                    else:\n                        scale = dij / (r[i] + r[j])\n                        r[i] *= scale\n                        r[j] *= scale\n        # clamp to wall again\n        r = np.minimum(r, w)\n        r = np.maximum(r, 0.0)\n        return r\n\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    # sweep a range of hex spacings to find the best local scale for 26-point cluster\n    for s in np.linspace(0.13, 0.24, 46):\n        pts = make_hex_points(s)\n        # choose n points closest to the square center\n        d2 = np.sum((pts - center)**2, axis=1)\n        idx = np.argsort(d2)[:n]\n        centers = pts[idx].copy()\n        # center the cluster exactly at (0.5,0.5) to avoid asymmetric edge losses\n        shift = center - centers.mean(axis=0)\n        centers += shift\n        centers = np.clip(centers, 1e-9, 1.0-1e-9)\n        radii = compute_max_radii(centers)\n        ssum = float(np.sum(radii))\n        if ssum > best_sum:\n            best_sum = ssum\n            best_centers = centers.copy()\n            best_radii = radii.copy()\n\n    # Fallback: if something went wrong, construct a modest default hex cluster\n    if best_centers is None:\n        pts = make_hex_points(0.18)\n        d2 = np.sum((pts - center)**2, axis=1)\n        idx = np.argsort(d2)[:n]\n        best_centers = pts[idx].copy()\n        best_radii = compute_max_radii(best_centers)\n        best_sum = float(np.sum(best_radii))\n\n    return best_centers, best_radii, float(best_sum)\n# EVOLVE-BLOCK-END\n\n# Fixed interface\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(7,7))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect(\"equal\")\n    ax.add_patch(plt.Rectangle((0,0),1,1,fill=False))\n    for c,r in zip(centers, radii):\n        ax.add_patch(Circle(c, r, alpha=0.6, edgecolor='k'))\n    plt.title(f\"n={len(centers)}, sum radii={np.sum(radii):.6f}\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # visualize(centers, radii)", "language": "python", "parent_id": "6b4cd051-4ff6-40f9-b561-1ebd90f879dc", "generation": 2, "timestamp": 1759959862.761548, "iteration_found": 15, "metrics": {"validity": 0.0, "combined_score": 0.0}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Full rewrite", "parent_metrics": {"validity": 1.0, "sum_radii": 0.6550362729456909, "target_ratio": 0.2485906159186683, "combined_score": 0.2485906159186683}, "island": 0}, "prompts": {"full_rewrite_user": {"system": "You are an expert mathematician specializing in circle packing problems and computational geometry. Your task is to improve a constructor function that directly produces a specific arrangement of 26 circles in a unit square, maximizing the sum of their radii. The AlphaEvolve paper achieved a sum of 2.635 for n=26.\n\nKey geometric insights:\n- Circle packings often follow hexagonal patterns in the densest regions\n- Maximum density for infinite circle packing is pi/(2*sqrt(3)) \u2248 0.9069\n- Edge effects make square container packing harder than infinite packing\n- Circles can be placed in layers or shells when confined to a square\n- Similar radius circles often form regular patterns, while varied radii allow better space utilization\n- Perfect symmetry may not yield the optimal packing due to edge effects\n\nFocus on designing an explicit constructor that places each circle in a specific position, rather than an iterative search algorithm.\n\n\n## Evolution Insights (Global Learnings)\n\n### Common Pitfalls:\n\u26a0\ufe0f sum_radii decreased by 31.8% (0.960 \u2192 0.655) (seen 3x)\n\u26a0\ufe0f target_ratio decreased by 31.8% (0.364 \u2192 0.249) (seen 3x)\n\u26a0\ufe0f combined_score decreased by 31.8% (0.364 \u2192 0.249) (seen 3x)\n\u26a0\ufe0f validity decreased by 100.0% (1.000 \u2192 0.000) (seen 2x)", "user": "# Current Program Information\n- Fitness: 0.2486\n- Feature coordinates: No feature coordinates\n- Focus areas: - Fitness declined: 0.6023 \u2192 0.2486. Consider revising recent changes.\n- Consider simplifying - code length exceeds 500 characters\n\n## Last Execution Output\n\n### stdout\n```\nRunning in subprocess, Python version: 3.12.1 (v3.12.1:2305ca5144, Dec  7 2023, 17:23:38) [Clang 13.0.0 (clang-1300.0.29.30)]\nProgram path: /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmpqmndojcr.py\nCalling run_packing()...\nrun_packing() returned successfully: sum_radii = 0.6550362729456909\nResults saved to /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmpxgh2b019.py.results\n\n```\n\n### stderr\n```\n\n```\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 3\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 1.5871, target_ratio: 0.6023, combined_score: 0.6023, eval_time: 0.0566\n- Outcome: Mixed results\n\n### Attempt 2\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 1.6762, target_ratio: 0.6361, combined_score: 0.6361, eval_time: 0.0580\n- Outcome: Mixed results\n\n### Attempt 1\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.1386, target_ratio: 0.8116, combined_score: 0.8116, eval_time: 0.0558\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.8116)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles using a truncated hexagonal lattice.\nPlaces a dense cluster of 26 centers (closest to square center) from a hex grid\nwith spacing chosen to produce compact packing, then assigns each radius as the\nminimum of distance to the unit-square border and half the nearest-neighbor distance.\nThis explicit constructor maximizes radii for fixed centers (no iterative search).\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n\n    # Hex lattice spacing chosen to give a dense cluster whose per-circle radius ~ spacing/2 ~ 0.1\n    s = 0.20  # horizontal spacing\n    v = s * np.sqrt(3) / 2.0  # vertical spacing for hex packing\n\n    pts = []\n    y = 0.0\n    row = 0\n    # generate hex lattice points inside [0,1] x [0,1]\n    while y <= 1.0 + 1e-12:\n        x_offset = (s / 2.0) if (row % 2 == 1) else 0.0\n        x = x_offset\n        while x <= 1.0 + 1e-12:\n            # clamp numerical border overshoot\n            if -1e-12 <= x <= 1 + 1e-12 and -1e-12 <= y <= 1 + 1e-12:\n                pts.append((min(1.0, max(0.0, x)), min(1.0, max(0.0, y))))\n            x += s\n        row += 1\n        y += v\n\n    pts = np.array(pts)\n    if pts.shape[0] < n:\n        # fallback: regular grid if hex grid too sparse\n        m = int(np.ceil(np.sqrt(n)))\n        xs = np.linspace(0.0, 1.0, m)\n        ys = np.linspace(0.0, 1.0, m)\n        grid = np.array([[x, y] for y in ys for x in xs])\n        pts = grid\n\n    # choose the n points closest to the center (densest patch)\n    center = np.array([0.5, 0.5])\n    dists = np.sum((pts - center) ** 2, axis=1)\n    idx = np.argsort(dists)[:n]\n    centers = pts[idx]\n\n    # Slight jitter removal: re-center cluster by shifting average to exact center\n    avg = centers.mean(axis=0)\n    shift = center - avg\n    centers = centers + shift\n    centers = np.clip(centers, 0.0, 1.0)  # ensure inside unit square\n\n    radii = compute_max_radii(centers)\n\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\ndef compute_max_radii(centers):\n    \"\"\"Assign radius to each center = min(dist to border, 0.5 * nearest neighbor distance).\"\"\"\n    n = centers.shape[0]\n    radii = np.zeros(n)\n    # distance to borders\n    border_dists = np.minimum.reduce([centers[:,0], centers[:,1], 1.0 - centers[:,0], 1.0 - centers[:,1]])\n    # pairwise distances\n    dmat = np.sqrt(np.maximum(0.0, ((centers[:,np.newaxis,:] - centers[np.newaxis,:,:])**2).sum(axis=2)))\n    # replace diagonal with +inf so min excludes self\n    np.fill_diagonal(dmat, np.inf)\n    nearest = np.min(dmat, axis=1)\n    # radius is min(border_dist, half nearest neighbor distance)\n    radii = np.minimum(border_dists, 0.5 * nearest)\n    # ensure non-negative\n    radii = np.maximum(0.0, radii)\n    return radii\n# EVOLVE-BLOCK-END\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor='k', linewidth=0.5)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.1386), Performs well on target_ratio (0.8116), Performs well on combined_score (0.8116), Performs well on eval_time (0.0558)\n\n### Program 2 (Score: 0.6361)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (deterministic hex-like layout +\ngreedy LP-style radius maximizer)\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square\n    that attempts to maximize the sum of their radii using an explicit\n    center layout (hex-like rows) and a coordinate-wise maximization\n    of radii subject to r_i + r_j <= dist(i,j) and r_i <= wall_distance_i.\n    Returns (centers, radii, sum_of_radii).\n    \"\"\"\n    n = 26\n\n    # Row pattern that sums to 26: hex-like layering to fit the square\n    rows = [5, 6, 4, 6, 5]  # sums to 26\n    num_rows = len(rows)\n\n    margin = 0.02  # keep all centers a bit inside the unit square\n    height_avail = 1.0 - 2 * margin\n\n    # Vertical coordinates (evenly spaced)\n    ys = np.linspace(margin + height_avail / (num_rows + 1),\n                     1 - margin - height_avail / (num_rows + 1),\n                     num_rows)\n\n    centers = []\n    for ri, m in enumerate(rows):\n        # horizontal available span\n        span = 1.0 - 2 * margin\n        # spacing: place m points with equal gaps, use offset for alternate rows\n        hspace = span / (m + 1)\n        # offset to create hex-like staggering: rows with 6 get half-step offset\n        offset = 0.5 * hspace if (m % 2 == 0) else 0.0\n        y = ys[ri]\n        for k in range(m):\n            x = margin + (k + 1) * hspace + offset\n            centers.append([x, y])\n\n    centers = np.array(centers[:n])\n    centers = np.clip(centers, margin, 1.0 - margin)\n\n    radii = compute_max_radii(centers)\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers, tol=1e-9, max_iter=2000):\n    \"\"\"\n    Coordinate-wise maximization:\n    r_i <= w_i (distance to walls)\n    r_i + r_j <= d_ij (pairwise non-overlap)\n    We iterate updating each r_i := min(w_i, min_j (d_ij - r_j)) (clamped >=0)\n    in Gauss-Seidel fashion until convergence.\n    This tends to produce near-optimal sum of radii for the linear constraints.\n    \"\"\"\n    n = centers.shape[0]\n    # distances between centers\n    diff = centers[:, None, :] - centers[None, :, :]\n    dmat = np.sqrt(np.sum(diff * diff, axis=2))\n    # avoid self constraints by setting huge values on diagonal\n    np.fill_diagonal(dmat, 1e6)\n\n    # wall distances\n    w = np.minimum.reduce([centers[:, 0], centers[:, 1], 1.0 - centers[:, 0], 1.0 - centers[:, 1]])\n\n    # initialize radii conservatively at wall distances\n    r = w.copy()\n\n    for it in range(max_iter):\n        r_old = r.copy()\n        # Gauss-Seidel updates: update in sequence to accelerate convergence\n        for i in range(n):\n            # allowable by neighbors given current neighbor radii\n            # compute d_ij - r_j for all j; r_i must be <= min_j (d_ij - r_j)\n            # ensure we don't include self (dmat diag large)\n            limit_by_neighbors = np.min(dmat[i] - r)\n            new_r = min(w[i], limit_by_neighbors)\n            if new_r < 0.0:\n                new_r = 0.0\n            r[i] = new_r\n        # check convergence\n        if np.max(np.abs(r - r_old)) < tol:\n            break\n\n    return r\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, float(radius), alpha=0.6, edgecolor=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={np.sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (1.6762), Performs well on target_ratio (0.6361), Performs well on combined_score (0.6361), Performs well on eval_time (0.0580)\n\n### Program 3 (Score: 0.6023)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor-based circle packing for n=26 circles (improved)\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    \"\"\"\n    Construct an explicit arrangement of 26 circle centers (two hex-like layouts tried)\n    and compute the maximal feasible radii for each layout using an iterative\n    Gauss-Seidel style expansion. Return the best layout (centers, radii, sum).\n    \"\"\"\n    n = 26\n\n    def make_hex_layout(row_counts):\n        # horizontal spacing chosen to safely fit the widest row with a margin\n        max_k = max(row_counts)\n        a_x = 1.0 / (max_k + 1.0)        # spacing between columns\n        a_y = a_x * np.sqrt(3) / 2.0    # hex vertical offset\n        rows = len(row_counts)\n        centers = []\n        y0 = 0.5 - (rows - 1) / 2.0 * a_y\n        for r, k in enumerate(row_counts):\n            y = y0 + r * a_y\n            # center the row horizontally\n            x_start = 0.5 - (k - 1) / 2.0 * a_x\n            for c in range(k):\n                x = x_start + c * a_x\n                centers.append([x, y])\n        centers = np.array(centers)\n        # if more than n points (shouldn't), trim; if fewer, pad (shouldn't)\n        if centers.shape[0] > n:\n            centers = centers[:n]\n        return centers\n\n    # Two candidate hex-like patterns summing to 26\n    pattern_A = [4, 6, 6, 6, 4]   # symmetric: 4+6+6+6+4 = 26\n    pattern_B = [5, 6, 5, 5, 5]   # less symmetric but slightly different packing\n\n    candidates = [make_hex_layout(pattern_A), make_hex_layout(pattern_B)]\n\n    # compute maximal radii for given centers using iterative expansion\n    def compute_max_radii_iter(centers, tol=1e-6, max_iter=2000):\n        m = centers.shape[0]\n        # distances between centers\n        D = np.sqrt(((centers[:, None, :] - centers[None, :, :]) ** 2).sum(axis=2))\n        np.fill_diagonal(D, np.inf)\n        # distance to border\n        border = np.minimum.reduce([centers[:, 0], centers[:, 1], 1 - centers[:, 0], 1 - centers[:, 1]])\n        # start from zeros and expand each circle to meet constraints r_i <= border_i and r_i <= d_ij - r_j\n        r = np.zeros(m)\n        for it in range(max_iter):\n            max_change = 0.0\n            # Gauss-Seidel style: update in sequence using latest values\n            for i in range(m):\n                # allowable by border\n                ri_new = border[i]\n                # allowable by each neighbor given current neighbor radii\n                # (d_ij - r_j) may be negative; cap at zero\n                neighbors_allow = D[i] - r\n                # ignore self (inf)\n                ri_new = min(ri_new, np.min(neighbors_allow))\n                if ri_new < 0.0:\n                    ri_new = 0.0\n                change = abs(ri_new - r[i])\n                if change > 0:\n                    r[i] = ri_new\n                    if change > max_change:\n                        max_change = change\n            if max_change < tol:\n                break\n        # final safety pass to enforce pairwise non-overlap by proportional scaling if needed\n        for i in range(m):\n            for j in range(i + 1, m):\n                dij = D[i, j]\n                if r[i] + r[j] > dij:\n                    if dij <= 0:\n                        r[i] = r[j] = 0.0\n                    else:\n                        scale = dij / (r[i] + r[j])\n                        r[i] *= scale\n                        r[j] *= scale\n        return r\n\n    # Evaluate candidates and pick the one with larger sum of radii\n    best = None\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n    for centers in candidates:\n        # ensure centers lie strictly inside (numerical safety)\n        centers = np.clip(centers, 1e-6, 1 - 1e-6)\n        radii = compute_max_radii_iter(centers)\n        s = radii.sum()\n        if s > best_sum:\n            best_sum = s\n            best = (centers.copy(), radii.copy(), s)\n            best_centers, best_radii, _ = best\n\n    # If somehow we have fewer than 26 points (defensive), pad with tiny circles near corners\n    if best_centers.shape[0] < n:\n        pad = n - best_centers.shape[0]\n        extras = np.array([[0.02, 0.02],\n                           [0.98, 0.02],\n                           [0.02, 0.98],\n                           [0.98, 0.98]])[:pad]\n        best_centers = np.vstack([best_centers, extras])\n        best_r = np.concatenate([best_radii, np.full(pad, 1e-4)])\n        best_radii = best_r\n        best_sum = best_radii.sum()\n\n    return best_centers, best_radii, best_sum\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.5)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (1.5871), Performs well on target_ratio (0.6023), Performs well on combined_score (0.6023), Performs well on eval_time (0.0566)\n\n\n\n## Diverse Programs\n\n### Program D1 (Score: 0.3642)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square\n    that attempts to maximize the sum of their radii.\n\n    Returns:\n        Tuple of (centers, radii, sum_of_radii)\n        centers: np.array of shape (26, 2) with (x, y) coordinates\n        radii: np.array of shape (26) with radius of each circle\n        sum_of_radii: Sum of all radii\n    \"\"\"\n    # Initialize arrays for 26 circles\n    n = 26\n    centers = np.zeros((n, 2))\n\n    # Place circles in a structured pattern\n    # This is a simple pattern - evolution will improve this\n\n    # First, place a large circle in the center\n    centers[0] = [0.5, 0.5]\n\n    # Place 8 circles around it in a ring\n    for i in range(8):\n        angle = 2 * np.pi * i / 8\n        centers[i + 1] = [0.5 + 0.3 * np.cos(angle), 0.5 + 0.3 * np.sin(angle)]\n\n    # Place 16 more circles in an outer ring\n    for i in range(16):\n        angle = 2 * np.pi * i / 16\n        centers[i + 9] = [0.5 + 0.7 * np.cos(angle), 0.5 + 0.7 * np.sin(angle)]\n\n    # Additional positioning adjustment to make sure all circles\n    # are inside the square and don't overlap\n    # Clip to ensure everything is inside the unit square\n    centers = np.clip(centers, 0.01, 0.99)\n\n    # Compute maximum valid radii for this configuration\n    radii = compute_max_radii(centers)\n\n    # Calculate the sum of radii\n    sum_radii = np.sum(radii)\n\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers):\n    \"\"\"\n    Compute the maximum possible radii for each circle position\n    such that they don't overlap and stay within the unit square.\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n\n    Returns:\n        np.array of shape (n) with radius of each circle\n    \"\"\"\n    n = centers.shape[0]\n    radii = np.ones(n)\n\n    # First, limit by distance to square borders\n    for i in range(n):\n        x, y = centers[i]\n        # Distance to borders\n        radii[i] = min(x, y, 1 - x, 1 - y)\n\n    # Then, limit by distance to other circles\n    # Each pair of circles with centers at distance d can have\n    # sum of radii at most d to avoid overlap\n    for i in range(n):\n        for j in range(i + 1, n):\n            dist = np.sqrt(np.sum((centers[i] - centers[j]) ** 2))\n\n            # If current radii would cause overlap\n            if radii[i] + radii[j] > dist:\n                # Scale both radii proportionally\n                scale = dist / (radii[i] + radii[j])\n                radii[i] *= scale\n                radii[j] *= scale\n\n    return radii\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.5)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\")\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii}\")\n    # AlphaEvolve improved this to 2.635\n\n    # Uncomment to visualize:\n    visualize(centers, radii)\n\n```\nKey features: Alternative approach to validity, Alternative approach to sum_radii\n\n### Program D2 (Score: 0.3922)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles using a compact hexagonal cluster\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    \"\"\"\n    Construct 26 circle centers by sampling a hexagonal lattice inside the unit square,\n    selecting the 26 points nearest the square center to form a dense cluster,\n    then computing maximum non-overlapping radii by iterative pairwise relaxation.\n    Returns (centers, radii, sum_of_radii).\n    \"\"\"\n    n = 26\n    # hex lattice parameters\n    s = 0.13                      # nominal horizontal spacing\n    v = s * np.sqrt(3) / 2        # vertical spacing between rows\n    margin = 0.005\n\n    pts = []\n    # generate grid rows covering the square with small margin\n    y = margin\n    row = 0\n    while y <= 1 - margin + 1e-12:\n        offset = 0.0 if (row % 2 == 0) else s / 2\n        x = margin + offset\n        while x <= 1 - margin + 1e-12:\n            pts.append((x, y))\n            x += s\n        row += 1\n        y += v\n\n    pts = np.array(pts)\n    # choose n centers closest to the square center for a compact cluster\n    center = np.array([0.5, 0.5])\n    dists = np.sum((pts - center)**2, axis=1)\n    idx = np.argsort(dists)[:n]\n    centers = pts[idx].copy()\n\n    # small random jitter to break perfect symmetry (helps edge placement)\n    rng = np.random.default_rng(12345)\n    centers += (rng.random(centers.shape) - 0.5) * 1e-4\n    centers = np.clip(centers, margin, 1 - margin)\n\n    radii = compute_max_radii(centers)\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\ndef compute_max_radii(centers):\n    \"\"\"\n    Iteratively compute radii constrained by box borders and pairwise non-overlap.\n    Uses repeated proportional scaling on violating pairs until convergence.\n    \"\"\"\n    n = centers.shape[0]\n    # initial radii: distance to nearest border\n    xs = centers[:, 0]; ys = centers[:, 1]\n    radii = np.minimum.reduce([xs, ys, 1 - xs, 1 - ys])\n\n    # iterative relaxation: scale any violating pair down proportionally\n    max_iters = 400\n    tol = 1e-7\n    for it in range(max_iters):\n        prev = radii.copy()\n        for i in range(n):\n            ci = centers[i]\n            ri = radii[i]\n            for j in range(i + 1, n):\n                cj = centers[j]\n                rj = radii[j]\n                d = np.hypot(ci[0] - cj[0], ci[1] - cj[1])\n                if d <= 1e-12:\n                    # collapse tiny separation: push one to zero\n                    radii[j] = 0.0\n                    continue\n                sumr = ri + rj\n                if sumr > d:\n                    # scale both radii down proportionally to their current sizes\n                    scale = d / sumr\n                    # apply to current stored radii (not stale ri/rj)\n                    radii[i] *= scale\n                    radii[j] *= scale\n                    ri = radii[i]; rj = radii[j]\n        if np.max(prev - radii) < tol:\n            break\n\n    # final clamp to non-negative and border constraints (numerical safety)\n    radii = np.clip(radii, 0.0, None)\n    xs = centers[:, 0]; ys = centers[:, 1]\n    radii = np.minimum(radii, np.minimum.reduce([xs, ys, 1 - xs, 1 - ys]))\n    return radii\n\n# EVOLVE-BLOCK-END\n\n# Fixed external interface\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(7, 7))\n    ax.set_xlim(0, 1); ax.set_ylim(0, 1); ax.set_aspect(\"equal\")\n    for i, (c, r) in enumerate(zip(centers, radii)):\n        ax.add_patch(Circle(c, r, alpha=0.6))\n        ax.text(c[0], c[1], str(i), ha=\"center\", va=\"center\", fontsize=6)\n    plt.title(f\"n={len(centers)}, sum radii={sum(radii):.6f}\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # visualize(centers, radii)\n```\nKey features: Alternative approach to validity, Alternative approach to sum_radii\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 0.8116, Type: High-Performer)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles using a truncated hexagonal lattice.\nPlaces a dense cluster of 26 centers (closest to square center) from a hex grid\nwith spacing chosen to produce compact packing, then assigns each radius as the\nminimum of distance to the unit-square border and half the nearest-neighbor distance.\nThis explicit constructor maximizes radii for fixed centers (no iterative search).\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n\n    # Hex lattice spacing chosen to give a dense cluster whose per-circle radius ~ spacing/2 ~ 0.1\n    s = 0.20  # horizontal spacing\n    v = s * np.sqrt(3) / 2.0  # vertical spacing for hex packing\n\n    pts = []\n    y = 0.0\n    row = 0\n    # generate hex lattice points inside [0,1] x [0,1]\n    while y <= 1.0 + 1e-12:\n        x_offset = (s / 2.0) if (row % 2 == 1) else 0.0\n        x = x_offset\n        while x <= 1.0 + 1e-12:\n            # clamp numerical border overshoot\n            if -1e-12 <= x <= 1 + 1e-12 and -1e-12 <= y <= 1 + 1e-12:\n                pts.append((min(1.0, max(0.0, x)), min(1.0, max(0.0, y))))\n            x += s\n        row += 1\n        y += v\n\n    pts = np.array(pts)\n    if pts.shape[0] < n:\n        # fallback: regular grid if hex grid too sparse\n        m = int(np.ceil(np.sqrt(n)))\n        xs = np.linspace(0.0, 1.0, m)\n        ys = np.linspace(0.0, 1.0, m)\n        grid = np.array([[x, y] for y in ys for x in xs])\n        pts = grid\n\n    # choose the n points closest to the center (densest patch)\n    center = np.array([0.5, 0.5])\n    dists = np.sum((pts - center) ** 2, axis=1)\n    idx = np.argsort(dists)[:n]\n    centers = pts[idx]\n\n    # Slight jitter removal: re-center cluster by shifting average to exact center\n    avg = centers.mean(axis=0)\n    shift = center - avg\n    centers = centers + shift\n    centers = np.clip(centers, 0.0, 1.0)  # ensure inside unit square\n\n    radii = compute_max_radii(centers)\n\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\ndef compute_max_radii(centers):\n    \"\"\"Assign radius to each center = min(dist to border, 0.5 * nearest neighbor distance).\"\"\"\n    n = centers.shape[0]\n    radii = np.zeros(n)\n    # distance to borders\n    border_dists = np.minimum.reduce([centers[:,0], centers[:,1], 1.0 - centers[:,0], 1.0 - centers[:,1]])\n    # pairwise distances\n    dmat = np.sqrt(np.maximum(0.0, ((centers[:,np.newaxis,:] - centers[np.newaxis,:,:])**2).sum(axis=2)))\n    # replace diagonal with +inf so min excludes self\n    np.fill_diagonal(dmat, np.inf)\n    nearest = np.min(dmat, axis=1)\n    # radius is min(border_dist, half nearest neighbor distance)\n    radii = np.minimum(border_dists, 0.5 * nearest)\n    # ensure non-negative\n    radii = np.maximum(0.0, radii)\n    return radii\n# EVOLVE-BLOCK-END\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor='k', linewidth=0.5)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nUnique approach: Modification: Full rewrite, Excellent validity (1.000), Excellent sum_radii (2.139)\n\n### Inspiration 2 (Score: 0.0000, Type: Exploratory)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (improved)\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a deterministic arrangement of 26 circle centers using a\n    trimmed hexagonal (close-packed) layout and compute maximal radii\n    by enforcing border and pairwise non-overlap constraints.\n\n    Returns:\n        centers: np.array (26,2)\n        radii: np.array (26,)\n        sum_of_radii: float\n    \"\"\"\n    n = 26\n\n    # We use 5 rows with counts summing to 26: [5,5,6,5,5]\n    rows = [5, 5, 6, 5, 5]\n    m = len(rows)\n\n    # Vertical spacing factor for hex packing\n    vfactor = np.sqrt(3) / 2.0\n\n    def build_layout(s):\n        \"\"\"Place points on a centered hex-like grid with horizontal spacing s.\"\"\"\n        centers = []\n        # total vertical span\n        v = s * vfactor\n        y0 = 0.5 - (m - 1) * v / 2.0\n        for ri, k in enumerate(rows):\n            y = y0 + ri * v\n            # horizontally center each row around 0.5\n            x0 = 0.5 - (k - 1) * s / 2.0\n            # stagger every other row (hex packing)\n            if ri % 2 == 1:\n                x0 += s / 2.0\n            for j in range(k):\n                x = x0 + j * s\n                centers.append([x, y])\n        return np.array(centers)\n\n    def compute_max_radii(centers, tol=1e-6, max_iter=500):\n        \"\"\"\n        Iterative enforcement:\n          r_i <= dist_to_border_i\n          r_i <= min_j (dist_ij - r_j)\n        Repeated until convergence (this yields a feasible maximal set).\n        \"\"\"\n        n = centers.shape[0]\n        # distance to borders\n        d_border = np.minimum.reduce([centers[:, 0], centers[:, 1], 1 - centers[:, 0], 1 - centers[:, 1]])\n        radii = d_border.copy()\n        # pairwise distances matrix\n        diff = centers[:, None, :] - centers[None, :, :]\n        dists = np.sqrt(np.sum(diff ** 2, axis=2))\n        # avoid self distances being zero causing issues\n        np.fill_diagonal(dists, np.inf)\n\n        for _ in range(max_iter):\n            r_prev = radii.copy()\n            # for each circle, the pairwise constraint gives an upper bound:\n            # r_i <= min_j (d_ij - r_j)\n            pair_bounds = dists - radii[None, :]\n            # ensure non-negative bounds; if negative then radius will become <= 0\n            pair_bounds = np.maximum(pair_bounds, 0.0)\n            min_pair = np.min(pair_bounds, axis=1)\n            radii = np.minimum(d_border, min_pair)\n            # tiny numerical stabilization\n            radii = np.maximum(radii, 0.0)\n            if np.max(np.abs(radii - r_prev)) < tol:\n                break\n        return radii\n\n    # Sweep a small range of horizontal spacing s to pick the layout that\n    # maximizes sum(radii). This is a simple 1D search over the intuitive\n    # packing scale rather than a full optimizer.\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    # reasonable s range: from fairly tight (0.12) to looser (0.24)\n    s_values = np.linspace(0.12, 0.24, 61)\n    for s in s_values:\n        centers = build_layout(s)\n        # If any center lies outside [0,1], skip\n        if np.any(centers < 0.0) or np.any(centers > 1.0):\n            continue\n        radii = compute_max_radii(centers)\n        total = float(np.sum(radii))\n        if total > best_sum:\n            best_sum = total\n            best_centers = centers.copy()\n            best_radii = radii.copy()\n\n    # Fallback: if none in range fit (shouldn't happen), use a modest fixed s\n    if best_centers is None:\n        best_centers = build_layout(0.16)\n        best_radii = compute_max_radii(best_centers)\n        best_sum = float(np.sum(best_radii))\n\n    return best_centers, best_radii, best_sum\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nUnique approach: Modification: Full rewrite, Alternative validity approach, Alternative sum_radii approach\n\n### Inspiration 3 (Score: 0.3642, Type: Exploratory)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square\n    that attempts to maximize the sum of their radii.\n\n    Returns:\n        Tuple of (centers, radii, sum_of_radii)\n        centers: np.array of shape (26, 2) with (x, y) coordinates\n        radii: np.array of shape (26) with radius of each circle\n        sum_of_radii: Sum of all radii\n    \"\"\"\n    # Initialize arrays for 26 circles\n    n = 26\n    centers = np.zeros((n, 2))\n\n    # Place circles in a structured pattern\n    # This is a simple pattern - evolution will improve this\n\n    # First, place a large circle in the center\n    centers[0] = [0.5, 0.5]\n\n    # Place 8 circles around it in a ring\n    for i in range(8):\n        angle = 2 * np.pi * i / 8\n        centers[i + 1] = [0.5 + 0.3 * np.cos(angle), 0.5 + 0.3 * np.sin(angle)]\n\n    # Place 16 more circles in an outer ring\n    for i in range(16):\n        angle = 2 * np.pi * i / 16\n        centers[i + 9] = [0.5 + 0.7 * np.cos(angle), 0.5 + 0.7 * np.sin(angle)]\n\n    # Additional positioning adjustment to make sure all circles\n    # are inside the square and don't overlap\n    # Clip to ensure everything is inside the unit square\n    centers = np.clip(centers, 0.01, 0.99)\n\n    # Compute maximum valid radii for this configuration\n    radii = compute_max_radii(centers)\n\n    # Calculate the sum of radii\n    sum_radii = np.sum(radii)\n\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers):\n    \"\"\"\n    Compute the maximum possible radii for each circle position\n    such that they don't overlap and stay within the unit square.\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n\n    Returns:\n        np.array of shape (n) with radius of each circle\n    \"\"\"\n    n = centers.shape[0]\n    radii = np.ones(n)\n\n    # First, limit by distance to square borders\n    for i in range(n):\n        x, y = centers[i]\n        # Distance to borders\n        radii[i] = min(x, y, 1 - x, 1 - y)\n\n    # Then, limit by distance to other circles\n    # Each pair of circles with centers at distance d can have\n    # sum of radii at most d to avoid overlap\n    for i in range(n):\n        for j in range(i + 1, n):\n            dist = np.sqrt(np.sum((centers[i] - centers[j]) ** 2))\n\n            # If current radii would cause overlap\n            if radii[i] + radii[j] > dist:\n                # Scale both radii proportionally\n                scale = dist / (radii[i] + radii[j])\n                radii[i] *= scale\n                radii[j] *= scale\n\n    return radii\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.5)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\")\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii}\")\n    # AlphaEvolve improved this to 2.635\n\n    # Uncomment to visualize:\n    visualize(centers, radii)\n\n```\nUnique approach: Excellent validity (1.000), Excellent sum_radii (0.960), NumPy-based implementation\n\n# Current Program\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (improved)\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct an explicit arrangement of 26 circle centers in a unit square\n    with a hexagonal-style layout (rows: 6-5-6-5-4). Radii are computed with\n    an iterative pairwise relaxation to enforce non-overlap and boundary limits.\n    Returns: centers (26,2), radii (26,), sum_of_radii\n    \"\"\"\n    n = 26\n\n    # Hex-like row counts (sums to 26): centered pattern to utilize interior space\n    rows = [6, 5, 6, 5, 4]\n    num_rows = len(rows)\n\n    # Choose horizontal spacing s and corresponding vertical spacing for hex packing\n    s = 0.12  # center-to-center horizontal spacing\n    vy = s * np.sqrt(3) / 2.0\n\n    # compute y positions so rows are vertically centered in unit square\n    total_height = (num_rows - 1) * vy\n    y0 = 0.5 - total_height / 2.0\n    centers = np.zeros((n, 2))\n    idx = 0\n    for r, count in enumerate(rows):\n        y = y0 + r * vy\n        width = (count - 1) * s\n        x_start = 0.5 - width / 2.0\n        # alternate rows are offset by half spacing to produce hex packing\n        if r % 2 == 1:\n            x_start += s / 2.0\n        for k in range(count):\n            x = x_start + k * s\n            centers[idx, 0] = x\n            centers[idx, 1] = y\n            idx += 1\n\n    # Tiny random jitter to break perfect symmetry (helps iterative relaxation)\n    rng = np.random.RandomState(1)\n    centers += (rng.uniform(-1e-4, 1e-4, centers.shape))\n\n    # Clip to remain strictly inside unit square (small margin)\n    centers = np.clip(centers, 1e-6, 1 - 1e-6)\n\n    radii = compute_max_radii(centers)\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers, max_iter=500, tol=1e-8):\n    \"\"\"\n    Iterative proportional relaxation to find feasible radii:\n    - Start with radii = distance to nearest border (upper bound).\n    - Repeatedly enforce pairwise constraints r_i + r_j <= d_ij by scaling\n      offending pairs proportionally until convergence.\n    This tends to produce a large feasible sum while keeping the result stable.\n    \"\"\"\n    n = centers.shape[0]\n    # initial radii limited by borders\n    radii = np.empty(n)\n    for i in range(n):\n        x, y = centers[i]\n        radii[i] = min(x, y, 1 - x, 1 - y)\n\n    # Precompute pairwise distances\n    diff = centers[:, None, :] - centers[None, :, :]\n    D = np.sqrt(np.maximum(1e-12, np.sum(diff ** 2, axis=2)))\n\n    # Iteratively enforce r_i + r_j <= D_ij\n    for it in range(max_iter):\n        max_violation = 0.0\n        # Loop over upper triangle pairs\n        for i in range(n):\n            for j in range(i + 1, n):\n                dij = D[i, j]\n                if dij <= 0:\n                    # coincident centers: collapse both radii to zero\n                    if radii[i] + radii[j] > 0:\n                        radii[i] = 0.0\n                        radii[j] = 0.0\n                        max_violation = max(max_violation, radii[i] + radii[j])\n                    continue\n                s = radii[i] + radii[j]\n                if s > dij:\n                    # scale proportionally so r_i + r_j = dij\n                    if s > 0:\n                        factor = dij / s\n                        # to favor larger total sum, scale minimally: keep average distribution\n                        radii[i] *= factor\n                        radii[j] *= factor\n                    else:\n                        # both zero, nothing to do\n                        pass\n                    max_violation = max(max_violation, s - dij)\n        if max_violation < tol:\n            break\n\n    # Ensure non-negative and respect border limits once more (numerical safety)\n    for i in range(n):\n        x, y = centers[i]\n        radii[i] = min(radii[i], x, y, 1 - x, 1 - y)\n        if radii[i] < 0:\n            radii[i] = 0.0\n\n    return radii\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={np.sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\n\n# Task\nRewrite the program to improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: complexity, diversity\nDifferent solutions with similar fitness but different features are valuable.\nProvide the complete new program code.\n\nIMPORTANT: Make sure your rewritten program maintains the same inputs and outputs\nas the original program, but with improved internal implementation.\n\n```python\n# Your rewritten program here\n```", "responses": ["```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for n=26 circle packing in the unit square.\nBuilds dense hexagonal clusters at several scales, selects the best 26-center\ncluster (closest to center) and maximizes radii under linear constraints\nr_i <= wall_i, r_i + r_j <= d_ij using a Gauss-Seidel style coordinate-wise\nmaximization. This explicit constructor is fast and yields high sum of radii.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    center = np.array([0.5, 0.5])\n\n    def make_hex_points(s, margin=1e-8):\n        v = s * np.sqrt(3) / 2.0\n        pts = []\n        # cover a bit beyond [0,1] to ensure enough candidates\n        y = -s\n        row = 0\n        while y <= 1.0 + s:\n            xoff = (s/2.0) if (row % 2 == 1) else 0.0\n            x = -s + xoff\n            while x <= 1.0 + s:\n                if -margin <= x <= 1.0 + margin and -margin <= y <= 1.0 + margin:\n                    pts.append((min(1.0, max(0.0, x)), min(1.0, max(0.0, y))))\n                x += s\n            y += v\n            row += 1\n        return np.array(pts)\n\n    def compute_max_radii(centers, tol=1e-9, max_iter=2000):\n        m = centers.shape[0]\n        # wall distances\n        w = np.minimum.reduce([centers[:,0], centers[:,1], 1.0-centers[:,0], 1.0-centers[:,1]])\n        # pairwise distances\n        diff = centers[:, None, :] - centers[None, :, :]\n        D = np.sqrt(np.maximum(0.0, np.sum(diff*diff, axis=2)))\n        np.fill_diagonal(D, np.inf)\n        r = w.copy()  # start at wall bounds (generous)\n        for it in range(max_iter):\n            r_old = r.copy()\n            # Gauss-Seidel updates: r_i = min(w_i, min_j (D_ij - r_j))\n            for i in range(m):\n                limit = np.min(D[i] - r)  # uses current r (including recently updated)\n                newr = min(w[i], limit)\n                r[i] = max(0.0, newr)\n            if np.max(np.abs(r - r_old)) < tol:\n                break\n        # final safety: enforce pairwise by proportional scaling if any tiny violations remain\n        for i in range(m):\n            for j in range(i+1, m):\n                dij = D[i,j]\n                if r[i] + r[j] > dij:\n                    if dij <= 0:\n                        r[i] = r[j] = 0.0\n                    else:\n                        scale = dij / (r[i] + r[j])\n                        r[i] *= scale\n                        r[j] *= scale\n        # clamp to wall again\n        r = np.minimum(r, w)\n        r = np.maximum(r, 0.0)\n        return r\n\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    # sweep a range of hex spacings to find the best local scale for 26-point cluster\n    for s in np.linspace(0.13, 0.24, 46):\n        pts = make_hex_points(s)\n        # choose n points closest to the square center\n        d2 = np.sum((pts - center)**2, axis=1)\n        idx = np.argsort(d2)[:n]\n        centers = pts[idx].copy()\n        # center the cluster exactly at (0.5,0.5) to avoid asymmetric edge losses\n        shift = center - centers.mean(axis=0)\n        centers += shift\n        centers = np.clip(centers, 1e-9, 1.0-1e-9)\n        radii = compute_max_radii(centers)\n        ssum = float(np.sum(radii))\n        if ssum > best_sum:\n            best_sum = ssum\n            best_centers = centers.copy()\n            best_radii = radii.copy()\n\n    # Fallback: if something went wrong, construct a modest default hex cluster\n    if best_centers is None:\n        pts = make_hex_points(0.18)\n        d2 = np.sum((pts - center)**2, axis=1)\n        idx = np.argsort(d2)[:n]\n        best_centers = pts[idx].copy()\n        best_radii = compute_max_radii(best_centers)\n        best_sum = float(np.sum(best_radii))\n\n    return best_centers, best_radii, float(best_sum)\n# EVOLVE-BLOCK-END\n\n# Fixed interface\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(7,7))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect(\"equal\")\n    ax.add_patch(plt.Rectangle((0,0),1,1,fill=False))\n    for c,r in zip(centers, radii):\n        ax.add_patch(Circle(c, r, alpha=0.6, edgecolor='k'))\n    plt.title(f\"n={len(centers)}, sum radii={np.sum(radii):.6f}\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # visualize(centers, radii)\n```"]}}, "artifacts_json": "{\"stdout\": \"Running in subprocess, Python version: 3.12.1 (v3.12.1:2305ca5144, Dec  7 2023, 17:23:38) [Clang 13.0.0 (clang-1300.0.29.30)]\\nProgram path: /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmpu8l_w4ld.py\\nCalling run_packing()...\\nrun_packing() returned successfully: sum_radii = 2.137419735825923\\nResults saved to /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmpst4huv5q.py.results\\n\", \"stderr\": \"\\nInvalid shapes\"}", "artifact_dir": null}