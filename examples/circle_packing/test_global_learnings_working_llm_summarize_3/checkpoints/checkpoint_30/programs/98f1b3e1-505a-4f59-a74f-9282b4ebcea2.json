{"id": "98f1b3e1-505a-4f59-a74f-9282b4ebcea2", "code": "# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for packing n=26 circles in the unit square.\n\nStrategy (explicit constructor):\n- Build an offset hexagonal (triangular) lattice of candidate centers parameterized by spacing s.\n- Binary-search the largest s that still yields at least 26 candidate centers.\n- From the candidate centers keep the 26 closest to the square center (reduces edge effects).\n- For robustness try the found s and a slightly smaller s (to allow tighter central packing) and\n  pick whichever yields the larger sum of radii.\n- Radii for a fixed set of centers are computed by the safe rule:\n    r_i = min(dist_to_border(i), 0.5 * min_{j!=i} dist(center_i, center_j))\n  (touching allowed, no overlaps). This simple explicit constructor is fast,\n  deterministic and yields a dense near-hexagonal packing in the middle with high total radius.\n\"\"\"\nimport numpy as np\n\n\ndef make_hex_centers(s):\n    \"\"\"Generate hexagonal-lattice centers with spacing s, keeping centers at least s/2 from borders.\"\"\"\n    if s <= 0:\n        return np.zeros((0, 2))\n    margin = s * 0.5\n    pts = []\n    y = margin\n    y_step = s * np.sqrt(3.0) / 2.0\n    row = 0\n    # generate rows while y in allowable range\n    while y <= 1.0 - margin + 1e-12:\n        x_offset = (row % 2) * (s / 2.0)\n        x = margin + x_offset\n        while x <= 1.0 - margin + 1e-12:\n            pts.append((x, y))\n            x += s\n        row += 1\n        y += y_step\n    if len(pts) == 0:\n        return np.zeros((0, 2))\n    return np.array(pts, dtype=float)\n\n\ndef compute_max_radii(centers):\n    \"\"\"Compute safe maximal radii for a given set of centers (no overlaps, inside unit square).\"\"\"\n    if centers.size == 0:\n        return np.array([], dtype=float)\n    # border distance\n    dist_border = np.minimum.reduce([centers[:, 0], centers[:, 1], 1.0 - centers[:, 0], 1.0 - centers[:, 1]])\n    # pairwise distances\n    diff = centers[:, None, :] - centers[None, :, :]\n    dists = np.sqrt(np.sum(diff ** 2, axis=2))\n    np.fill_diagonal(dists, np.inf)\n    min_pair = np.min(dists, axis=1)\n    radii = np.minimum(dist_border, 0.5 * min_pair)\n    radii = np.maximum(radii, 0.0)\n    return radii\n\n\ndef construct_packing():\n    n_target = 26\n\n    # binary search for largest spacing s that yields at least n_target centers\n    low, high = 0.01, 0.6\n    best_s = low\n    for _ in range(40):\n        mid = 0.5 * (low + high)\n        pts = make_hex_centers(mid)\n        if pts.shape[0] >= n_target:\n            best_s = mid\n            low = mid\n        else:\n            high = mid\n\n    # evaluate two candidate spacings: best_s and a slightly smaller alternative\n    candidates = []\n    for s_try in (best_s, max(0.01, best_s * 0.98)):\n        pts = make_hex_centers(s_try)\n        if pts.shape[0] < n_target:\n            continue\n        center_sq = np.array([0.5, 0.5])\n        d = np.linalg.norm(pts - center_sq, axis=1)\n        idx = np.argsort(d)[:n_target]\n        centers = pts[idx]\n        # deterministic ordering\n        centers = centers[np.lexsort((centers[:, 1], centers[:, 0]))]\n        radii = compute_max_radii(centers)\n        ssum = float(np.sum(radii))\n        candidates.append((ssum, centers, radii))\n\n    if len(candidates) == 0:\n        # fallback: use a small centered hex with s = 1/6\n        centers = make_hex_centers(1.0 / 6.0)[:n_target]\n        centers = centers[np.lexsort((centers[:, 1], centers[:, 0]))]\n        radii = compute_max_radii(centers)\n        return centers, radii, float(np.sum(radii))\n\n    # pick best candidate by sum of radii\n    candidates.sort(key=lambda x: x[0], reverse=True)\n    sum_radii, centers, radii = candidates[0]\n    return centers, radii, float(sum_radii)\n\n\n# EVOLVE-BLOCK-END\n\n\n# Fixed interface (unchanged)\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(6, 6))\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    for i, (c, r) in enumerate(zip(centers, radii)):\n        ax.add_patch(Circle(c, r, alpha=0.6, edgecolor=\"k\"))\n        ax.text(c[0], c[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"n={len(centers)}, sum={sum(radii):.6f}\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, s = run_packing()\n    print(f\"Sum of radii: {s:.6f}\")\n    # visualize(centers, radii)", "language": "python", "parent_id": "f2079945-297e-4fa5-9a5c-0ce067a0b12a", "generation": 2, "timestamp": 1759959007.61277, "iteration_found": 26, "metrics": {"validity": 1.0, "sum_radii": 2.4389662673793744, "target_ratio": 0.925603896538662, "combined_score": 0.925603896538662, "eval_time": 0.058725833892822266}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Full rewrite", "parent_metrics": {"validity": 1.0, "sum_radii": 1.4948716593053937, "target_ratio": 0.5673137226965441, "combined_score": 0.5673137226965441, "eval_time": 0.05732989311218262}, "island": 0}, "prompts": {"full_rewrite_user": {"system": "You are an expert mathematician specializing in circle packing problems and computational geometry. Your task is to improve a constructor function that directly produces a specific arrangement of 26 circles in a unit square, maximizing the sum of their radii. The AlphaEvolve paper achieved a sum of 2.635 for n=26.\n\nKey geometric insights:\n- Circle packings often follow hexagonal patterns in the densest regions\n- Maximum density for infinite circle packing is pi/(2*sqrt(3)) \u2248 0.9069\n- Edge effects make square container packing harder than infinite packing\n- Circles can be placed in layers or shells when confined to a square\n- Similar radius circles often form regular patterns, while varied radii allow better space utilization\n- Perfect symmetry may not yield the optimal packing due to edge effects\n\nFocus on designing an explicit constructor that places each circle in a specific position, rather than an iterative search algorithm.\n\n\n## Evolution Insights (Global Learnings)\n\n### Common Pitfalls:\n\u26a0\ufe0f eval_time decreased by 42.8% (0.108 \u2192 0.062) (seen 1x)\n\u26a0\ufe0f validity decreased by 100.0% (1.000 \u2192 0.000) (seen 1x)\n\u26a0\ufe0f sum_radii decreased by 100.0% (2.439 \u2192 0.000) (seen 1x)\n\u26a0\ufe0f target_ratio decreased by 100.0% (0.926 \u2192 0.000) (seen 1x)\n\u26a0\ufe0f combined_score decreased by 100.0% (0.926 \u2192 0.000) (seen 1x)", "user": "# Current Program Information\n- Fitness: 0.5673\n- Feature coordinates: No feature coordinates\n- Focus areas: - Fitness declined: 0.9256 \u2192 0.5673. Consider revising recent changes.\n- Consider simplifying - code length exceeds 500 characters\n\n## Last Execution Output\n\n### stdout\n```\nRunning in subprocess, Python version: 3.12.1 (v3.12.1:2305ca5144, Dec  7 2023, 17:23:38) [Clang 13.0.0 (clang-1300.0.29.30)]\nProgram path: /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmpp61hsotm.py\nCalling run_packing()...\nrun_packing() returned successfully: sum_radii = 1.4948716593053937\nResults saved to /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmp2jce12wz.py.results\n\n```\n\n### stderr\n```\n\n```\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 3\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.4390, target_ratio: 0.9256, combined_score: 0.9256, eval_time: 0.0597\n- Outcome: Mixed results\n\n### Attempt 2\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.4390, target_ratio: 0.9256, combined_score: 0.9256, eval_time: 0.0598\n- Outcome: Mixed results\n\n### Attempt 1\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.4390, target_ratio: 0.9256, combined_score: 0.9256, eval_time: 0.0571\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.9256)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles using a hexagonal lattice constructor.\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a deterministic arrangement of 26 circle centers in [0,1]^2\n    using a hexagonal (offset-grid) lattice. We choose the largest lattice\n    spacing s that still produces at least 26 candidate centers, then\n    keep the 26 centers closest to the square center to mitigate edge effects.\n    Radii are set to the maximal non-overlapping values given these centers:\n        r_i = min(dist_to_border(i), 0.5 * min_j dist(center_i, center_j))\n    This yields a valid non-overlapping packing and tends to produce dense,\n    hexagonal-like local arrangements that maximize the sum of radii.\n    Returns:\n        (centers (26,2), radii (26,), sum_of_radii)\n    \"\"\"\n    n_target = 26\n\n    def make_hex_centers(s):\n        # margin so circles can have radius up to s/2 without crossing border\n        margin = s / 2.0\n        pts = []\n        y = margin\n        row = 0\n        y_step = s * np.sqrt(3) / 2.0\n        while y <= 1.0 - margin + 1e-12:\n            x_offset = (row % 2) * (s / 2.0)\n            x = margin + x_offset\n            while x <= 1.0 - margin + 1e-12:\n                pts.append((x, y))\n                x += s\n            row += 1\n            y += y_step\n        return np.array(pts)\n\n    # Binary search for the largest s that still yields at least n_target centers.\n    low, high = 0.01, 0.6  # high > possible spacing\n    best_s = low\n    for _ in range(40):\n        mid = 0.5 * (low + high)\n        pts = make_hex_centers(mid)\n        if pts.shape[0] >= n_target:\n            best_s = mid\n            low = mid  # we can try larger spacing\n        else:\n            high = mid\n    centers_all = make_hex_centers(best_s)\n\n    # If we have more than needed, keep the n_target centers closest to square center\n    if centers_all.shape[0] > n_target:\n        center_of_square = np.array([0.5, 0.5])\n        dists = np.linalg.norm(centers_all - center_of_square, axis=1)\n        idx = np.argsort(dists)[:n_target]\n        centers = centers_all[idx]\n    else:\n        centers = centers_all.copy()\n\n    # As a small enhancement, ensure we always return exactly n_target centers\n    # (should be guaranteed by construction/trimming above)\n    if centers.shape[0] < n_target:\n        # fallback: fill with evenly spaced points along a small spiral near center\n        extra = n_target - centers.shape[0]\n        spiral = []\n        for k in range(extra):\n            t = 0.05 * (k + 1)\n            angle = 2 * np.pi * (k + 1) / extra\n            spiral.append([0.5 + t * np.cos(angle), 0.5 + t * np.sin(angle)])\n        centers = np.vstack([centers, np.array(spiral)])\n\n    # Sort centers for determinism (by x then y)\n    centers = centers[np.lexsort((centers[:, 1], centers[:, 0]))]\n\n    # Compute maximal valid radii:\n    radii = compute_max_radii(centers)\n\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers):\n    \"\"\"\n    For a fixed set of centers, compute a safe maximal radius assignment\n    that guarantees no overlaps and containment in the unit square.\n    We use the conservative but tight rule:\n        r_i = min(dist_to_border(i), 0.5 * min_j dist(i,j))\n    which makes touching allowed but not overlapping.\n    \"\"\"\n    n = centers.shape[0]\n    radii = np.zeros(n)\n    # distance to borders\n    dist_border = np.minimum.reduce([centers[:, 0], centers[:, 1], 1 - centers[:, 0], 1 - centers[:, 1]])\n    # pairwise distances (compute efficiently)\n    diff = centers[:, None, :] - centers[None, :, :]  # n x n x 2\n    dists = np.sqrt(np.sum(diff ** 2, axis=2))\n    # set diagonal to a large number to ignore self-distance\n    np.fill_diagonal(dists, np.inf)\n    min_pair = np.min(dists, axis=1)\n    # radii limited by half the nearest neighbor distance and by borders\n    radii = np.minimum(dist_border, 0.5 * min_pair)\n    # Ensure non-negative and tiny numerical floor\n    radii = np.maximum(radii, 0.0)\n    return radii\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor='k', linewidth=0.5)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Hex-lattice Constructor Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.4390), Performs well on target_ratio (0.9256), Performs well on combined_score (0.9256), Performs well on eval_time (0.0571)\n\n### Program 2 (Score: 0.9256)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for n=26 circle packing in the unit square.\nBuild a hexagonal (offset) lattice with the largest feasible spacing that\nyields at least 26 candidate centers, keep the 26 closest to the square\ncenter (reduces edge effects), and set radii to the tight safe values:\n    r_i = min(dist_to_border(i), 0.5 * min_{j!=i} dist(center_i, center_j))\nThis simple explicit constructor yields dense, hex-like local packing\nand produces a high sum of radii without iterative search.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n_target = 26\n\n    def make_hex_centers(s):\n        # margin so centers are at least s/2 from borders (allow radius up to s/2)\n        margin = s * 0.5\n        pts = []\n        y = margin\n        y_step = s * np.sqrt(3.0) / 2.0\n        row = 0\n        while y <= 1.0 - margin + 1e-12:\n            x_offset = (row % 2) * (s / 2.0)\n            x = margin + x_offset\n            while x <= 1.0 - margin + 1e-12:\n                pts.append((x, y))\n                x += s\n            row += 1\n            y += y_step\n        return np.array(pts, dtype=float)\n\n    # Binary search largest spacing s that still produces >= n_target centers\n    low, high = 0.01, 0.6\n    best_s = low\n    for _ in range(40):\n        mid = 0.5 * (low + high)\n        pts = make_hex_centers(mid)\n        if pts.shape[0] >= n_target:\n            best_s = mid\n            low = mid\n        else:\n            high = mid\n    centers_all = make_hex_centers(best_s)\n\n    # Pick the n_target centers closest to the square center (reduce edge effects)\n    if centers_all.shape[0] > n_target:\n        center_sq = np.array([0.5, 0.5])\n        d = np.linalg.norm(centers_all - center_sq, axis=1)\n        idx = np.argsort(d)[:n_target]\n        centers = centers_all[idx]\n    else:\n        centers = centers_all.copy()\n\n    # Ensure deterministic ordering\n    centers = centers[np.lexsort((centers[:,1], centers[:,0]))]\n\n    # Compute safe maximal radii for fixed centers\n    radii = compute_max_radii(centers)\n    return centers, radii, float(np.sum(radii))\n\ndef compute_max_radii(centers):\n    # r_i <= distance to border\n    dist_border = np.minimum.reduce([centers[:,0], centers[:,1], 1.0 - centers[:,0], 1.0 - centers[:,1]])\n    # pairwise distances\n    diff = centers[:, None, :] - centers[None, :, :]\n    dists = np.sqrt(np.sum(diff**2, axis=2))\n    np.fill_diagonal(dists, np.inf)\n    min_pair = np.min(dists, axis=1)\n    radii = np.minimum(dist_border, 0.5 * min_pair)\n    radii = np.maximum(radii, 0.0)\n    return radii\n\n# EVOLVE-BLOCK-END\n\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(6,6))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect(\"equal\")\n    for i,(c,r) in enumerate(zip(centers,radii)):\n        ax.add_patch(Circle(c,r,alpha=0.6, edgecolor='k'))\n        ax.text(c[0], c[1], str(i), ha='center', va='center', fontsize=8)\n    plt.title(f\"n={len(centers)}, sum={sum(radii):.6f}\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, s = run_packing()\n    print(f\"Sum of radii: {s:.6f}\")\n    # visualize(centers, radii)\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.4390), Performs well on target_ratio (0.9256), Performs well on combined_score (0.9256), Performs well on eval_time (0.0598)\n\n### Program 3 (Score: 0.9256)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for n=26 circle packing in the unit square.\nBuild a hexagonal (offset) lattice, pick the 26 centers closest to the square\ncenter for best edge-effect mitigation, and set radii by the tight safe rule\nr_i = min(dist_to_border(i), 0.5 * min_{j!=i} dist(i,j)).\nThis simple constructor yields very compact local hexagonal arrangements\nand produces a high total sum of radii.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n_target = 26\n\n    def hex_centers(s):\n        # margin ensures a circle of radius s/2 around a center stays inside square\n        margin = s / 2.0\n        pts = []\n        y = margin\n        row = 0\n        y_step = s * np.sqrt(3) / 2.0\n        # generate rows while centers remain within [margin, 1-margin]\n        while y <= 1.0 - margin + 1e-12:\n            x_off = (row % 2) * (s / 2.0)\n            x = margin + x_off\n            while x <= 1.0 - margin + 1e-12:\n                pts.append((x, y))\n                x += s\n            row += 1\n            y += y_step\n        return np.array(pts)\n\n    # binary search largest spacing s that yields at least n_target centers\n    lo, hi = 0.01, 0.6\n    best_s = lo\n    for _ in range(40):\n        mid = 0.5 * (lo + hi)\n        pts = hex_centers(mid)\n        if pts.shape[0] >= n_target:\n            best_s = mid\n            lo = mid\n        else:\n            hi = mid\n    candidates = hex_centers(best_s)\n\n    # choose the n_target centers closest to center (mitigate edge effects)\n    if candidates.shape[0] > n_target:\n        center = np.array([0.5, 0.5])\n        d = np.linalg.norm(candidates - center, axis=1)\n        idx = np.argsort(d)[:n_target]\n        centers = candidates[idx]\n    else:\n        centers = candidates.copy()\n\n    # deterministic ordering\n    centers = centers[np.lexsort((centers[:,1], centers[:,0]))]\n\n    # compute maximal safe radii: min(border_dist, 0.5 * nearest_neighbor)\n    dist_border = np.minimum.reduce([centers[:,0], centers[:,1], 1 - centers[:,0], 1 - centers[:,1]])\n    diff = centers[:, None, :] - centers[None, :, :]\n    dists = np.sqrt(np.sum(diff**2, axis=2))\n    np.fill_diagonal(dists, np.inf)\n    min_pair = np.min(dists, axis=1)\n    radii = np.minimum(dist_border, 0.5 * min_pair)\n    radii = np.maximum(radii, 0.0)\n\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n# EVOLVE-BLOCK-END\n\n# Fixed runner and visualizer (kept for compatibility)\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(8,8))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect(\"equal\")\n    for c,r in zip(centers, radii):\n        ax.add_patch(Circle(c, r, alpha=0.6, edgecolor='k'))\n    ax.grid(True)\n    plt.title(f\"n={len(centers)}, sum={sum(radii):.6f}\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, s = run_packing()\n    print(f\"Sum of radii: {s:.6f}\")\n    # visualize(centers, radii)\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.4390), Performs well on target_ratio (0.9256), Performs well on combined_score (0.9256), Performs well on eval_time (0.0597)\n\n\n\n## Diverse Programs\n\n### Program D1 (Score: 0.9053)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Improved constructor for packing n=26 circles in the unit square.\n\nStrategy:\n- Build offset hexagonal lattices parametrized by spacing s and small offsets.\n- For each lattice, pick the 26 centers closest to the square center (mitigates edge effects).\n- Compute feasible radii starting from a generous estimate (min(border, 0.5*nearest))\n  then perform pairwise iterative corrections that only shrink the larger circle when a\n  pair overlaps. This preserves small circles and yields a large total radius sum.\n- Search over a grid of s and small offsets to find the best deterministic layout.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n_target = 26\n\n    def make_hex_centers(s, ox=0.0, oy=0.0):\n        # ox, oy are offsets (in [0,s)) to shift the lattice deterministically\n        pts = []\n        y_step = s * np.sqrt(3) / 2.0\n        # margin so points are at least tiny distance from border\n        margin = 1e-8\n        # start at oy - ensure within [0,1]\n        y = oy\n        row = 0\n        while y <= 1.0 + margin:\n            x_offset = (row % 2) * (s / 2.0)\n            x = ox + x_offset\n            while x <= 1.0 + margin:\n                if 0.0 <= x <= 1.0 and 0.0 <= y <= 1.0:\n                    pts.append((min(max(x, 1e-9), 1.0 - 1e-9), min(max(y, 1e-9), 1.0 - 1e-9)))\n                x += s\n            row += 1\n            y += y_step\n        return np.array(pts)\n\n    def compute_max_radii(centers, tol=1e-8, max_iter=500):\n        n = centers.shape[0]\n        if n == 0:\n            return np.array([], dtype=float)\n        # border-limited radii\n        dist_border = np.minimum.reduce([centers[:,0], centers[:,1], 1-centers[:,0], 1-centers[:,1]])\n        # pairwise distances\n        diff = centers[:, None, :] - centers[None, :, :]\n        dists = np.sqrt(np.sum(diff**2, axis=2))\n        np.fill_diagonal(dists, np.inf)\n        # initial generous radii: min(border, half nearest neighbor)\n        min_pair = np.min(dists, axis=1)\n        r = np.minimum(dist_border, 0.5 * min_pair)\n        r = np.maximum(r, 0.0)\n        # iterative pairwise correction: when ri + rj > dij, reduce the larger so equality holds\n        for it in range(max_iter):\n            max_change = 0.0\n            # loop pairs (explicit double loop is fine for n=26)\n            for i in range(n):\n                for j in range(i+1, n):\n                    dij = dists[i,j]\n                    if dij == np.inf:\n                        continue\n                    ri, rj = r[i], r[j]\n                    if ri + rj > dij + 1e-12:\n                        # reduce the larger radius (preserve the smaller)\n                        if ri > rj:\n                            new_ri = max(0.0, dij - rj)\n                            change = abs(new_ri - ri)\n                            if change > 0:\n                                r[i] = new_ri\n                                if change > max_change: max_change = change\n                        else:\n                            new_rj = max(0.0, dij - ri)\n                            change = abs(new_rj - rj)\n                            if change > 0:\n                                r[j] = new_rj\n                                if change > max_change: max_change = change\n            # enforce border limits again\n            over = r - dist_border\n            if np.any(over > 0):\n                change = float(np.max(np.maximum(over, 0.0)))\n                r = np.minimum(r, dist_border)\n                if change > max_change: max_change = change\n            if max_change < tol:\n                break\n        return r\n\n    # Search grid for best spacing and offsets\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    # plausible spacing range: from tight (~0.08) to loose (~0.22)\n    s_vals = np.linspace(0.07, 0.22, 70)\n    # small deterministic offsets as fractions of s (3x3 grid)\n    frac_offsets = [0.0, 0.33, 0.66]\n\n    center_sq = np.array([0.5, 0.5])\n\n    for s in s_vals:\n        for fx in frac_offsets:\n            for fy in frac_offsets:\n                ox = (fx * s) % s\n                oy = (fy * s) % s\n                pts = make_hex_centers(s, ox, oy)\n                if pts.shape[0] < n_target:\n                    continue\n                # choose the n_target centers closest to square center\n                dists_center = np.linalg.norm(pts - center_sq, axis=1)\n                idx = np.argsort(dists_center)[:n_target]\n                centers_try = pts[idx]\n                # sort deterministically (x then y)\n                centers_try = centers_try[np.lexsort((centers_try[:,1], centers_try[:,0]))]\n                radii_try = compute_max_radii(centers_try)\n                ssum = float(np.sum(radii_try))\n                if ssum > best_sum + 1e-12:\n                    best_sum = ssum\n                    best_centers = centers_try.copy()\n                    best_radii = radii_try.copy()\n\n    # As a final local refinement: try tiny symmetric inward shift of outer centers\n    if best_centers is not None:\n        # attempt shifting each center slightly toward the square center if it increases sum\n        for scale in [0.0, 0.002, 0.004, 0.006]:\n            if scale == 0.0:\n                centers_try = best_centers.copy()\n            else:\n                directions = center_sq - best_centers\n                norms = np.linalg.norm(directions, axis=1, keepdims=True)\n                norms = np.maximum(norms, 1e-12)\n                shift = (directions / norms) * scale\n                centers_try = np.clip(best_centers + shift, 1e-9, 1.0 - 1e-9)\n            radii_try = compute_max_radii(centers_try)\n            ssum = float(np.sum(radii_try))\n            if ssum > best_sum + 1e-12:\n                best_sum = ssum\n                best_centers = centers_try.copy()\n                best_radii = radii_try.copy()\n\n    # Fallback if search failed\n    if best_centers is None:\n        # use a simple centered 5-row hex pattern (guaranteed)\n        s = 1.0/6.0\n        best_centers = make_hex_centers(s, 0.0, 0.0)[:n_target]\n        best_centers = best_centers[np.lexsort((best_centers[:,1], best_centers[:,0]))]\n        best_radii = compute_max_radii(best_centers)\n        best_sum = float(np.sum(best_radii))\n\n    return best_centers, best_radii, float(best_sum)\n\n# EVOLVE-BLOCK-END\n\n# Fixed runner & visualizer (unchanged interface)\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(8,8))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect(\"equal\")\n    for i,(c,r) in enumerate(zip(centers,radii)):\n        ax.add_patch(Circle(c, r, alpha=0.6, edgecolor='k', linewidth=0.4))\n        ax.text(c[0], c[1], str(i), ha='center', va='center', fontsize=7)\n    plt.title(f\"n={len(centers)} sum radii={sum(radii):.6f}\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # visualize(centers, radii)\n```\nKey features: Alternative approach to validity, Alternative approach to sum_radii\n\n### Program D2 (Score: 0.9256)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 using a centered hexagonal-lattice constructor.\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    \"\"\"\n    Deterministic constructor that builds a hexagonal (offset-grid) lattice of candidate\n    centers, chooses the 26 centers closest to the square center (to reduce edge effects),\n    and assigns each circle the tightest safe radius:\n        r_i = min(distance_to_border(i), 0.5 * min_{j!=i} dist(center_i, center_j))\n    This simple, explicit constructor tends to produce dense, near-hexagonal packings\n    in the middle of the square and gives a high sum of radii for n=26.\n    Returns: (centers (26,2), radii (26,), sum_of_radii)\n    \"\"\"\n    n_target = 26\n\n    def make_hex_centers(s):\n        # margin ensures centers are at least s/2 from border so circles of radius s/2 fit\n        margin = s * 0.5\n        pts = []\n        y = margin\n        row = 0\n        y_step = s * np.sqrt(3) / 2.0\n        # generate rows until we exceed the unit square\n        while y <= 1.0 - margin + 1e-12:\n            x_offset = (row % 2) * (s / 2.0)\n            x = margin + x_offset\n            while x <= 1.0 - margin + 1e-12:\n                pts.append((x, y))\n                x += s\n            row += 1\n            y += y_step\n        return np.array(pts)\n\n    # Binary search for the largest spacing s that yields at least n_target centers.\n    low, high = 0.01, 0.6\n    best_s = low\n    for _ in range(45):\n        mid = 0.5 * (low + high)\n        pts = make_hex_centers(mid)\n        if pts.shape[0] >= n_target:\n            best_s = mid\n            low = mid\n        else:\n            high = mid\n    centers_all = make_hex_centers(best_s)\n\n    # If more centers than needed, keep those closest to square center\n    if centers_all.shape[0] > n_target:\n        center_sq = np.array([0.5, 0.5])\n        dists = np.linalg.norm(centers_all - center_sq, axis=1)\n        idx = np.argsort(dists)[:n_target]\n        centers = centers_all[idx]\n    else:\n        # If exactly or fewer (rare), pad/fill deterministically near center\n        centers = centers_all.copy()\n        if centers.shape[0] < n_target:\n            extra = n_target - centers.shape[0]\n            # deterministic small spiral placements\n            for k in range(extra):\n                t = 0.04 * (k + 1)\n                ang = 2 * np.pi * (k + 1) / extra\n                centers = np.vstack([centers, [0.5 + t * np.cos(ang), 0.5 + t * np.sin(ang)]])\n    # Sort for determinism (by x then y)\n    centers = centers[np.lexsort((centers[:,1], centers[:,0]))][:n_target]\n\n    # Compute maximal safe radii: limited by border and half nearest-neighbor distance\n    def compute_max_radii(centers):\n        n = centers.shape[0]\n        # distance to border\n        dist_border = np.minimum.reduce([centers[:,0], centers[:,1], 1.0 - centers[:,0], 1.0 - centers[:,1]])\n        # pairwise distances\n        diff = centers[:, None, :] - centers[None, :, :]  # n x n x 2\n        dists = np.sqrt(np.sum(diff**2, axis=2))\n        np.fill_diagonal(dists, np.inf)\n        min_pair = np.min(dists, axis=1)\n        radii = np.minimum(dist_border, 0.5 * min_pair)\n        radii = np.maximum(radii, 0.0)\n        return radii\n\n    radii = compute_max_radii(centers)\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n# EVOLVE-BLOCK-END\n\n# Runner and visualizer (unchanged interface)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(6,6))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect(\"equal\"); ax.grid(True)\n    for i, (c, r) in enumerate(zip(centers, radii)):\n        circ = Circle(c, r, alpha=0.6, edgecolor='k')\n        ax.add_patch(circ)\n        ax.text(c[0], c[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n    plt.title(f\"Hex-lattice Constructor (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # visualize(centers, radii)\n```\nKey features: Alternative approach to validity, Alternative approach to sum_radii\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 0.9256, Type: High-Performer)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles using a hexagonal lattice constructor.\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a deterministic arrangement of 26 circle centers in [0,1]^2\n    using a hexagonal (offset-grid) lattice. We choose the largest lattice\n    spacing s that still produces at least 26 candidate centers, then\n    keep the 26 centers closest to the square center to mitigate edge effects.\n    Radii are set to the maximal non-overlapping values given these centers:\n        r_i = min(dist_to_border(i), 0.5 * min_j dist(center_i, center_j))\n    This yields a valid non-overlapping packing and tends to produce dense,\n    hexagonal-like local arrangements that maximize the sum of radii.\n    Returns:\n        (centers (26,2), radii (26,), sum_of_radii)\n    \"\"\"\n    n_target = 26\n\n    def make_hex_centers(s):\n        # margin so circles can have radius up to s/2 without crossing border\n        margin = s / 2.0\n        pts = []\n        y = margin\n        row = 0\n        y_step = s * np.sqrt(3) / 2.0\n        while y <= 1.0 - margin + 1e-12:\n            x_offset = (row % 2) * (s / 2.0)\n            x = margin + x_offset\n            while x <= 1.0 - margin + 1e-12:\n                pts.append((x, y))\n                x += s\n            row += 1\n            y += y_step\n        return np.array(pts)\n\n    # Binary search for the largest s that still yields at least n_target centers.\n    low, high = 0.01, 0.6  # high > possible spacing\n    best_s = low\n    for _ in range(40):\n        mid = 0.5 * (low + high)\n        pts = make_hex_centers(mid)\n        if pts.shape[0] >= n_target:\n            best_s = mid\n            low = mid  # we can try larger spacing\n        else:\n            high = mid\n    centers_all = make_hex_centers(best_s)\n\n    # If we have more than needed, keep the n_target centers closest to square center\n    if centers_all.shape[0] > n_target:\n        center_of_square = np.array([0.5, 0.5])\n        dists = np.linalg.norm(centers_all - center_of_square, axis=1)\n        idx = np.argsort(dists)[:n_target]\n        centers = centers_all[idx]\n    else:\n        centers = centers_all.copy()\n\n    # As a small enhancement, ensure we always return exactly n_target centers\n    # (should be guaranteed by construction/trimming above)\n    if centers.shape[0] < n_target:\n        # fallback: fill with evenly spaced points along a small spiral near center\n        extra = n_target - centers.shape[0]\n        spiral = []\n        for k in range(extra):\n            t = 0.05 * (k + 1)\n            angle = 2 * np.pi * (k + 1) / extra\n            spiral.append([0.5 + t * np.cos(angle), 0.5 + t * np.sin(angle)])\n        centers = np.vstack([centers, np.array(spiral)])\n\n    # Sort centers for determinism (by x then y)\n    centers = centers[np.lexsort((centers[:, 1], centers[:, 0]))]\n\n    # Compute maximal valid radii:\n    radii = compute_max_radii(centers)\n\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers):\n    \"\"\"\n    For a fixed set of centers, compute a safe maximal radius assignment\n    that guarantees no overlaps and containment in the unit square.\n    We use the conservative but tight rule:\n        r_i = min(dist_to_border(i), 0.5 * min_j dist(i,j))\n    which makes touching allowed but not overlapping.\n    \"\"\"\n    n = centers.shape[0]\n    radii = np.zeros(n)\n    # distance to borders\n    dist_border = np.minimum.reduce([centers[:, 0], centers[:, 1], 1 - centers[:, 0], 1 - centers[:, 1]])\n    # pairwise distances (compute efficiently)\n    diff = centers[:, None, :] - centers[None, :, :]  # n x n x 2\n    dists = np.sqrt(np.sum(diff ** 2, axis=2))\n    # set diagonal to a large number to ignore self-distance\n    np.fill_diagonal(dists, np.inf)\n    min_pair = np.min(dists, axis=1)\n    # radii limited by half the nearest neighbor distance and by borders\n    radii = np.minimum(dist_border, 0.5 * min_pair)\n    # Ensure non-negative and tiny numerical floor\n    radii = np.maximum(radii, 0.0)\n    return radii\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor='k', linewidth=0.5)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Hex-lattice Constructor Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nUnique approach: Excellent validity (1.000), Excellent sum_radii (2.439), Excellent target_ratio (0.926)\n\n# Current Program\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (improved heuristic constructor)\n\nThis version builds an explicit hex-like layered layout of 26 centers inside the unit square\nand computes maximal non-overlapping radii by solving the pairwise linear constraints\nr_i + r_j <= dist(i,j), r_i <= dist_to_border(i) with a greedy iterative adjustment\nthat reduces the larger radius in any violating pair. This greedy policy typically\nyields a larger total sum of radii than scaling both radii proportionally.\n\nOutputs:\n    centers: (26,2) numpy array of center coordinates in [0,1]^2\n    radii: (26,) numpy array of radii that do not overlap and stay in the square\n    sum_radii: sum(radii)\n\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    n = 26\n\n    # Design a layered hex-like pattern (rows with offsets) that fits well in the unit square.\n    # Choose row counts so sum = 26\n    rows = [5, 6, 5, 5, 5]  # total 26\n    max_in_row = max(rows)\n\n    # horizontal spacing chosen to fit the row with most circles comfortably\n    s_x = 1.0 / (max_in_row + 1)  # base horizontal spacing\n    s_y = (np.sqrt(3) / 2.0) * s_x  # hex vertical spacing\n\n    # vertical margin so the rows are centered in the unit square\n    total_height = (len(rows) - 1) * s_y\n    y_margin = (1.0 - total_height) / 2.0\n\n    centers = np.zeros((n, 2))\n    idx = 0\n    for r_idx, k in enumerate(rows):\n        # compute y for this row\n        y = y_margin + r_idx * s_y\n\n        # center horizontally: compute start x so row is centered\n        row_width = (k - 1) * s_x\n        start_x = (1.0 - row_width) / 2.0\n\n        # For a hex-like pattern, offset alternate rows by s_x/2\n        offset = (s_x / 2.0) if (r_idx % 2 == 1) else 0.0\n        for j in range(k):\n            x = start_x + j * s_x + offset\n            # clip to tiny margin to avoid exact border\n            x = np.clip(x, 1e-6, 1.0 - 1e-6)\n            y_clipped = np.clip(y, 1e-6, 1.0 - 1e-6)\n            centers[idx, 0] = x\n            centers[idx, 1] = y_clipped\n            idx += 1\n\n    # Sanity: ensure we placed exactly n centers\n    assert idx == n\n\n    # Compute maximal radii satisfying border and pairwise non-overlap constraints\n    radii = compute_max_radii(centers)\n\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers, tol=1e-9, max_iters=5000):\n    \"\"\"\n    Greedy iterative solver for the LP:\n        maximize sum r_i\n        s.t. 0 <= r_i <= d_border_i\n             r_i + r_j <= dist(i,j)  for all i != j\n\n    Approach:\n    - Initialize r_i = distance to nearest border.\n    - Repeatedly scan pairs (i,j). If r_i + r_j > dist_ij, reduce the larger of r_i,r_j\n      to (dist_ij - smaller). Continue until no violations (within tolerance).\n    This heuristic tends to preserve small radii while trimming larger ones, increasing\n    the overall sum compared to proportional scaling in many configurations.\n    \"\"\"\n    n = centers.shape[0]\n    # initial radii limited by distance to borders\n    radii = np.minimum(centers[:, 0], 1.0 - centers[:, 0])\n    radii = np.minimum(radii, np.minimum(centers[:, 1], 1.0 - centers[:, 1]))\n\n    # precompute pairwise distances\n    diff = centers[:, None, :] - centers[None, :, :]\n    dists = np.sqrt(np.sum(diff ** 2, axis=2))\n    # set self-distances to a large number to ignore\n    np.fill_diagonal(dists, 1e9)\n\n    # Greedy iterative enforcement\n    it = 0\n    while it < max_iters:\n        max_delta = 0.0\n        it += 1\n\n        # We'll scan all pairs; to be deterministic, scan i<j\n        for i in range(n):\n            ri = radii[i]\n            for j in range(i + 1, n):\n                rj = radii[j]\n                dij = dists[i, j]\n                # numerical tolerance\n                if ri + rj > dij + tol:\n                    # reduce the larger radius to satisfy the constraint\n                    if ri >= rj:\n                        new_ri = max(0.0, dij - rj)\n                        delta = ri - new_ri\n                        if delta > 0:\n                            radii[i] = new_ri\n                            ri = new_ri\n                            if delta > max_delta:\n                                max_delta = delta\n                    else:\n                        new_rj = max(0.0, dij - ri)\n                        delta = rj - new_rj\n                        if delta > 0:\n                            radii[j] = new_rj\n                            rj = new_rj\n                            if delta > max_delta:\n                                max_delta = delta\n\n        # Enforce border constraints (should already hold, but after adjustments ensure)\n        border_dist = np.minimum(centers[:, 0], 1.0 - centers[:, 0])\n        border_dist = np.minimum(border_dist, np.minimum(centers[:, 1], 1.0 - centers[:, 1]))\n        # If any radius exceeds border distance, reduce it\n        diffs = radii - border_dist\n        over = diffs > tol\n        if np.any(over):\n            delta = np.max(diffs[over])\n            radii[over] = border_dist[over]\n            if delta > max_delta:\n                max_delta = delta\n\n        if max_delta <= tol:\n            break\n\n    # Final small numerical clamp\n    radii = np.maximum(0.0, radii)\n    return radii\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\n\n# Task\nRewrite the program to improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: complexity, diversity\nDifferent solutions with similar fitness but different features are valuable.\nProvide the complete new program code.\n\nIMPORTANT: Make sure your rewritten program maintains the same inputs and outputs\nas the original program, but with improved internal implementation.\n\n```python\n# Your rewritten program here\n```", "responses": ["```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for packing n=26 circles in the unit square.\n\nStrategy (explicit constructor):\n- Build an offset hexagonal (triangular) lattice of candidate centers parameterized by spacing s.\n- Binary-search the largest s that still yields at least 26 candidate centers.\n- From the candidate centers keep the 26 closest to the square center (reduces edge effects).\n- For robustness try the found s and a slightly smaller s (to allow tighter central packing) and\n  pick whichever yields the larger sum of radii.\n- Radii for a fixed set of centers are computed by the safe rule:\n    r_i = min(dist_to_border(i), 0.5 * min_{j!=i} dist(center_i, center_j))\n  (touching allowed, no overlaps). This simple explicit constructor is fast,\n  deterministic and yields a dense near-hexagonal packing in the middle with high total radius.\n\"\"\"\nimport numpy as np\n\n\ndef make_hex_centers(s):\n    \"\"\"Generate hexagonal-lattice centers with spacing s, keeping centers at least s/2 from borders.\"\"\"\n    if s <= 0:\n        return np.zeros((0, 2))\n    margin = s * 0.5\n    pts = []\n    y = margin\n    y_step = s * np.sqrt(3.0) / 2.0\n    row = 0\n    # generate rows while y in allowable range\n    while y <= 1.0 - margin + 1e-12:\n        x_offset = (row % 2) * (s / 2.0)\n        x = margin + x_offset\n        while x <= 1.0 - margin + 1e-12:\n            pts.append((x, y))\n            x += s\n        row += 1\n        y += y_step\n    if len(pts) == 0:\n        return np.zeros((0, 2))\n    return np.array(pts, dtype=float)\n\n\ndef compute_max_radii(centers):\n    \"\"\"Compute safe maximal radii for a given set of centers (no overlaps, inside unit square).\"\"\"\n    if centers.size == 0:\n        return np.array([], dtype=float)\n    # border distance\n    dist_border = np.minimum.reduce([centers[:, 0], centers[:, 1], 1.0 - centers[:, 0], 1.0 - centers[:, 1]])\n    # pairwise distances\n    diff = centers[:, None, :] - centers[None, :, :]\n    dists = np.sqrt(np.sum(diff ** 2, axis=2))\n    np.fill_diagonal(dists, np.inf)\n    min_pair = np.min(dists, axis=1)\n    radii = np.minimum(dist_border, 0.5 * min_pair)\n    radii = np.maximum(radii, 0.0)\n    return radii\n\n\ndef construct_packing():\n    n_target = 26\n\n    # binary search for largest spacing s that yields at least n_target centers\n    low, high = 0.01, 0.6\n    best_s = low\n    for _ in range(40):\n        mid = 0.5 * (low + high)\n        pts = make_hex_centers(mid)\n        if pts.shape[0] >= n_target:\n            best_s = mid\n            low = mid\n        else:\n            high = mid\n\n    # evaluate two candidate spacings: best_s and a slightly smaller alternative\n    candidates = []\n    for s_try in (best_s, max(0.01, best_s * 0.98)):\n        pts = make_hex_centers(s_try)\n        if pts.shape[0] < n_target:\n            continue\n        center_sq = np.array([0.5, 0.5])\n        d = np.linalg.norm(pts - center_sq, axis=1)\n        idx = np.argsort(d)[:n_target]\n        centers = pts[idx]\n        # deterministic ordering\n        centers = centers[np.lexsort((centers[:, 1], centers[:, 0]))]\n        radii = compute_max_radii(centers)\n        ssum = float(np.sum(radii))\n        candidates.append((ssum, centers, radii))\n\n    if len(candidates) == 0:\n        # fallback: use a small centered hex with s = 1/6\n        centers = make_hex_centers(1.0 / 6.0)[:n_target]\n        centers = centers[np.lexsort((centers[:, 1], centers[:, 0]))]\n        radii = compute_max_radii(centers)\n        return centers, radii, float(np.sum(radii))\n\n    # pick best candidate by sum of radii\n    candidates.sort(key=lambda x: x[0], reverse=True)\n    sum_radii, centers, radii = candidates[0]\n    return centers, radii, float(sum_radii)\n\n\n# EVOLVE-BLOCK-END\n\n\n# Fixed interface (unchanged)\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(6, 6))\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    for i, (c, r) in enumerate(zip(centers, radii)):\n        ax.add_patch(Circle(c, r, alpha=0.6, edgecolor=\"k\"))\n        ax.text(c[0], c[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"n={len(centers)}, sum={sum(radii):.6f}\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, s = run_packing()\n    print(f\"Sum of radii: {s:.6f}\")\n    # visualize(centers, radii)\n```"]}}, "artifacts_json": "{\"stdout\": \"Running in subprocess, Python version: 3.12.1 (v3.12.1:2305ca5144, Dec  7 2023, 17:23:38) [Clang 13.0.0 (clang-1300.0.29.30)]\\nProgram path: /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmp8jzjq_9c.py\\nCalling run_packing()...\\nrun_packing() returned successfully: sum_radii = 2.4389662673793744\\nResults saved to /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmplyittdby.py.results\\n\", \"stderr\": \"\"}", "artifact_dir": null}