{"id": "9d380db2-9d53-4d35-9c13-32eae7783305", "code": "# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 using a centered hexagonal-lattice constructor.\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    \"\"\"\n    Deterministic constructor that builds a hexagonal (offset-grid) lattice of candidate\n    centers, chooses the 26 centers closest to the square center (to reduce edge effects),\n    and assigns each circle the tightest safe radius:\n        r_i = min(distance_to_border(i), 0.5 * min_{j!=i} dist(center_i, center_j))\n    This simple, explicit constructor tends to produce dense, near-hexagonal packings\n    in the middle of the square and gives a high sum of radii for n=26.\n    Returns: (centers (26,2), radii (26,), sum_of_radii)\n    \"\"\"\n    n_target = 26\n\n    def make_hex_centers(s):\n        # margin ensures centers are at least s/2 from border so circles of radius s/2 fit\n        margin = s * 0.5\n        pts = []\n        y = margin\n        row = 0\n        y_step = s * np.sqrt(3) / 2.0\n        # generate rows until we exceed the unit square\n        while y <= 1.0 - margin + 1e-12:\n            x_offset = (row % 2) * (s / 2.0)\n            x = margin + x_offset\n            while x <= 1.0 - margin + 1e-12:\n                pts.append((x, y))\n                x += s\n            row += 1\n            y += y_step\n        return np.array(pts)\n\n    # Binary search for the largest spacing s that yields at least n_target centers.\n    low, high = 0.01, 0.6\n    best_s = low\n    for _ in range(45):\n        mid = 0.5 * (low + high)\n        pts = make_hex_centers(mid)\n        if pts.shape[0] >= n_target:\n            best_s = mid\n            low = mid\n        else:\n            high = mid\n    centers_all = make_hex_centers(best_s)\n\n    # If more centers than needed, keep those closest to square center\n    if centers_all.shape[0] > n_target:\n        center_sq = np.array([0.5, 0.5])\n        dists = np.linalg.norm(centers_all - center_sq, axis=1)\n        idx = np.argsort(dists)[:n_target]\n        centers = centers_all[idx]\n    else:\n        # If exactly or fewer (rare), pad/fill deterministically near center\n        centers = centers_all.copy()\n        if centers.shape[0] < n_target:\n            extra = n_target - centers.shape[0]\n            # deterministic small spiral placements\n            for k in range(extra):\n                t = 0.04 * (k + 1)\n                ang = 2 * np.pi * (k + 1) / extra\n                centers = np.vstack([centers, [0.5 + t * np.cos(ang), 0.5 + t * np.sin(ang)]])\n    # Sort for determinism (by x then y)\n    centers = centers[np.lexsort((centers[:,1], centers[:,0]))][:n_target]\n\n    # Compute maximal safe radii: limited by border and half nearest-neighbor distance\n    def compute_max_radii(centers):\n        n = centers.shape[0]\n        # distance to border\n        dist_border = np.minimum.reduce([centers[:,0], centers[:,1], 1.0 - centers[:,0], 1.0 - centers[:,1]])\n        # pairwise distances\n        diff = centers[:, None, :] - centers[None, :, :]  # n x n x 2\n        dists = np.sqrt(np.sum(diff**2, axis=2))\n        np.fill_diagonal(dists, np.inf)\n        min_pair = np.min(dists, axis=1)\n        radii = np.minimum(dist_border, 0.5 * min_pair)\n        radii = np.maximum(radii, 0.0)\n        return radii\n\n    radii = compute_max_radii(centers)\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n# EVOLVE-BLOCK-END\n\n# Runner and visualizer (unchanged interface)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(6,6))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect(\"equal\"); ax.grid(True)\n    for i, (c, r) in enumerate(zip(centers, radii)):\n        circ = Circle(c, r, alpha=0.6, edgecolor='k')\n        ax.add_patch(circ)\n        ax.text(c[0], c[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n    plt.title(f\"Hex-lattice Constructor (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # visualize(centers, radii)", "language": "python", "parent_id": "c5cccb37-3e61-46ed-9f58-8ddc4dd92db6", "generation": 2, "timestamp": 1759958906.6618948, "iteration_found": 13, "metrics": {"validity": 1.0, "sum_radii": 2.4389662673781265, "target_ratio": 0.9256038965381885, "combined_score": 0.9256038965381885, "eval_time": 0.05936622619628906}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Full rewrite", "parent_metrics": {"validity": 1.0, "sum_radii": 0.9847011147744233, "target_ratio": 0.373700612817618, "combined_score": 0.373700612817618}, "island": 0}, "prompts": {"full_rewrite_user": {"system": "You are an expert mathematician specializing in circle packing problems and computational geometry. Your task is to improve a constructor function that directly produces a specific arrangement of 26 circles in a unit square, maximizing the sum of their radii. The AlphaEvolve paper achieved a sum of 2.635 for n=26.\n\nKey geometric insights:\n- Circle packings often follow hexagonal patterns in the densest regions\n- Maximum density for infinite circle packing is pi/(2*sqrt(3)) \u2248 0.9069\n- Edge effects make square container packing harder than infinite packing\n- Circles can be placed in layers or shells when confined to a square\n- Similar radius circles often form regular patterns, while varied radii allow better space utilization\n- Perfect symmetry may not yield the optimal packing due to edge effects\n\nFocus on designing an explicit constructor that places each circle in a specific position, rather than an iterative search algorithm.\n", "user": "# Current Program Information\n- Fitness: 0.3737\n- Feature coordinates: No feature coordinates\n- Focus areas: - Fitness declined: 0.6434 \u2192 0.3737. Consider revising recent changes.\n- Consider simplifying - code length exceeds 500 characters\n\n## Last Execution Output\n\n### stdout\n```\nRunning in subprocess, Python version: 3.12.1 (v3.12.1:2305ca5144, Dec  7 2023, 17:23:38) [Clang 13.0.0 (clang-1300.0.29.30)]\nProgram path: /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmpyqmbljop.py\nCalling run_packing()...\nrun_packing() returned successfully: sum_radii = 0.9847011147744233\nResults saved to /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmp1so2tmrb.py.results\n\n```\n\n### stderr\n```\n\n```\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 3\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 1.6952, target_ratio: 0.6434, combined_score: 0.6434, eval_time: 0.1077\n- Outcome: Mixed results\n\n### Attempt 2\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 1.9434, target_ratio: 0.7375, combined_score: 0.7375, eval_time: 0.0576\n- Outcome: Mixed results\n\n### Attempt 1\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.4390, target_ratio: 0.9256, combined_score: 0.9256, eval_time: 0.0571\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.9256)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles using a hexagonal lattice constructor.\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a deterministic arrangement of 26 circle centers in [0,1]^2\n    using a hexagonal (offset-grid) lattice. We choose the largest lattice\n    spacing s that still produces at least 26 candidate centers, then\n    keep the 26 centers closest to the square center to mitigate edge effects.\n    Radii are set to the maximal non-overlapping values given these centers:\n        r_i = min(dist_to_border(i), 0.5 * min_j dist(center_i, center_j))\n    This yields a valid non-overlapping packing and tends to produce dense,\n    hexagonal-like local arrangements that maximize the sum of radii.\n    Returns:\n        (centers (26,2), radii (26,), sum_of_radii)\n    \"\"\"\n    n_target = 26\n\n    def make_hex_centers(s):\n        # margin so circles can have radius up to s/2 without crossing border\n        margin = s / 2.0\n        pts = []\n        y = margin\n        row = 0\n        y_step = s * np.sqrt(3) / 2.0\n        while y <= 1.0 - margin + 1e-12:\n            x_offset = (row % 2) * (s / 2.0)\n            x = margin + x_offset\n            while x <= 1.0 - margin + 1e-12:\n                pts.append((x, y))\n                x += s\n            row += 1\n            y += y_step\n        return np.array(pts)\n\n    # Binary search for the largest s that still yields at least n_target centers.\n    low, high = 0.01, 0.6  # high > possible spacing\n    best_s = low\n    for _ in range(40):\n        mid = 0.5 * (low + high)\n        pts = make_hex_centers(mid)\n        if pts.shape[0] >= n_target:\n            best_s = mid\n            low = mid  # we can try larger spacing\n        else:\n            high = mid\n    centers_all = make_hex_centers(best_s)\n\n    # If we have more than needed, keep the n_target centers closest to square center\n    if centers_all.shape[0] > n_target:\n        center_of_square = np.array([0.5, 0.5])\n        dists = np.linalg.norm(centers_all - center_of_square, axis=1)\n        idx = np.argsort(dists)[:n_target]\n        centers = centers_all[idx]\n    else:\n        centers = centers_all.copy()\n\n    # As a small enhancement, ensure we always return exactly n_target centers\n    # (should be guaranteed by construction/trimming above)\n    if centers.shape[0] < n_target:\n        # fallback: fill with evenly spaced points along a small spiral near center\n        extra = n_target - centers.shape[0]\n        spiral = []\n        for k in range(extra):\n            t = 0.05 * (k + 1)\n            angle = 2 * np.pi * (k + 1) / extra\n            spiral.append([0.5 + t * np.cos(angle), 0.5 + t * np.sin(angle)])\n        centers = np.vstack([centers, np.array(spiral)])\n\n    # Sort centers for determinism (by x then y)\n    centers = centers[np.lexsort((centers[:, 1], centers[:, 0]))]\n\n    # Compute maximal valid radii:\n    radii = compute_max_radii(centers)\n\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers):\n    \"\"\"\n    For a fixed set of centers, compute a safe maximal radius assignment\n    that guarantees no overlaps and containment in the unit square.\n    We use the conservative but tight rule:\n        r_i = min(dist_to_border(i), 0.5 * min_j dist(i,j))\n    which makes touching allowed but not overlapping.\n    \"\"\"\n    n = centers.shape[0]\n    radii = np.zeros(n)\n    # distance to borders\n    dist_border = np.minimum.reduce([centers[:, 0], centers[:, 1], 1 - centers[:, 0], 1 - centers[:, 1]])\n    # pairwise distances (compute efficiently)\n    diff = centers[:, None, :] - centers[None, :, :]  # n x n x 2\n    dists = np.sqrt(np.sum(diff ** 2, axis=2))\n    # set diagonal to a large number to ignore self-distance\n    np.fill_diagonal(dists, np.inf)\n    min_pair = np.min(dists, axis=1)\n    # radii limited by half the nearest neighbor distance and by borders\n    radii = np.minimum(dist_border, 0.5 * min_pair)\n    # Ensure non-negative and tiny numerical floor\n    radii = np.maximum(radii, 0.0)\n    return radii\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor='k', linewidth=0.5)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Hex-lattice Constructor Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.4390), Performs well on target_ratio (0.9256), Performs well on combined_score (0.9256), Performs well on eval_time (0.0571)\n\n### Program 2 (Score: 0.7375)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (improved)\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square\n    using a compact hexagonal-like layout (explicit positions).\n    Returns:\n        Tuple of (centers, radii, sum_of_radii)\n        centers: np.array of shape (26, 2)\n        radii: np.array of shape (26,)\n        sum_of_radii: float\n    \"\"\"\n    n = 26\n\n    # Hexagonal-style row counts that sum to 26\n    # We place one wider row (6) and four rows of 5 each -> 6 + 5*4 = 26\n    row_counts = [6, 5, 5, 5, 5]\n    rows = len(row_counts)\n\n    # choose horizontal spacing so 6 points fit evenly across [0,1]\n    s = 1.0 / 6.0  # center-to-center spacing horizontally for the densest row\n    v_spacing = s * np.sqrt(3) / 2.0  # vertical spacing for hex packing\n\n    centers_list = []\n    # vertical offset to keep points nicely within the unit square\n    y0 = s / 2.0\n\n    for r in range(rows):\n        m = row_counts[r]\n        # center the row horizontally\n        total_width = (m - 1) * s\n        x_start = 0.5 - total_width / 2.0\n        # apply the hex offset for alternate rows to get closer packing\n        # alternate rows shifted by s/2\n        if (r % 2) == 1:\n            x_start += s / 2.0\n        y = y0 + r * v_spacing\n        for k in range(m):\n            x = x_start + k * s\n            # ensure numerical safety inside (0,1)\n            x = min(max(x, 1e-6), 1.0 - 1e-6)\n            yy = min(max(y, 1e-6), 1.0 - 1e-6)\n            centers_list.append([x, yy])\n\n    centers = np.array(centers_list[:n], dtype=float)\n    assert centers.shape == (n, 2), \"constructed centers must be shape (26,2)\"\n\n    # compute maximal feasible radii for these centers\n    radii = compute_max_radii(centers)\n\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers, tol=1e-8, max_iter=2000):\n    \"\"\"\n    Compute maximal non-overlapping radii for given centers within the unit square.\n    This uses a monotone iterative reduction algorithm:\n      - start from border-limited radii\n      - repeatedly fix any pairwise overlap by reducing the larger radius\n      - re-enforce border limits until convergence\n    Args:\n        centers: np.array of shape (n,2)\n        tol: convergence tolerance (maximum change per iteration)\n        max_iter: cap on iterations\n    Returns:\n        radii: np.array of shape (n,)\n    \"\"\"\n    n = centers.shape[0]\n    # initial radii limited by distance to borders\n    radii = np.empty(n, dtype=float)\n    for i in range(n):\n        x, y = centers[i]\n        radii[i] = min(x, y, 1.0 - x, 1.0 - y)\n\n    # iterative enforcement of pairwise non-overlap constraints\n    for it in range(max_iter):\n        max_change = 0.0\n\n        # pairwise pass: for any violating pair, reduce the larger radius\n        for i in range(n):\n            ci = centers[i]\n            ri = radii[i]\n            for j in range(i + 1, n):\n                cj = centers[j]\n                rj = radii[j]\n                dx = ci[0] - cj[0]\n                dy = ci[1] - cj[1]\n                dist = np.hypot(dx, dy)\n                if dist <= 1e-12:\n                    # extremely close centers: collapse both to zero to be safe\n                    if ri > 0 or rj > 0:\n                        max_change = max(max_change, ri, rj)\n                    radii[i] = 0.0\n                    radii[j] = 0.0\n                    ri = radii[i]\n                    rj = radii[j]\n                    continue\n\n                if ri + rj > dist:\n                    # reduce the larger radius so ri + rj == dist (keep smaller fixed)\n                    if ri > rj:\n                        new_ri = max(0.0, dist - rj)\n                        change = abs(new_ri - ri)\n                        if change > 0:\n                            radii[i] = new_ri\n                            ri = new_ri\n                            max_change = max(max_change, change)\n                    else:\n                        new_rj = max(0.0, dist - ri)\n                        change = abs(new_rj - rj)\n                        if change > 0:\n                            radii[j] = new_rj\n                            rj = new_rj\n                            max_change = max(max_change, change)\n\n        # enforce border constraints again and track any changes\n        for i in range(n):\n            x, y = centers[i]\n            border_limit = min(x, y, 1.0 - x, 1.0 - y)\n            if radii[i] > border_limit:\n                change = radii[i] - border_limit\n                radii[i] = border_limit\n                if change > max_change:\n                    max_change = change\n\n        if max_change < tol:\n            break\n\n    # final safety clamp\n    radii = np.maximum(radii, 0.0)\n    return radii\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (1.9434), Performs well on target_ratio (0.7375), Performs well on combined_score (0.7375), Performs well on eval_time (0.0576)\n\n### Program 3 (Score: 0.6434)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (improved)\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square\n    using a hexagonal-like layered layout (rows: 5,6,5,5,5). The routine\n    searches over the lattice spacing parameter to maximize the sum of\n    feasible radii (subject to non-overlap and border constraints).\n\n    Returns:\n        Tuple of (centers, radii, sum_of_radii)\n        centers: np.array of shape (26, 2) with (x, y) coordinates\n        radii: np.array of shape (26) with radius of each circle\n        sum_of_radii: Sum of all radii\n    \"\"\"\n    n = 26\n    row_counts = [5, 6, 5, 5, 5]  # sums to 26\n    rows = len(row_counts)\n\n    def build_centers(s):\n        \"\"\"Build hex-like grid centers for spacing s.\"\"\"\n        h = s\n        v = s * np.sqrt(3) / 2.0\n        # horizontal extents for largest row to center the whole pattern\n        max_count = max(row_counts)\n        max_width = (max_count - 1) * h\n        x_origin = 0.5 - max_width / 2.0\n        total_height = (rows - 1) * v\n        y_origin = 0.5 - total_height / 2.0\n\n        centers = []\n        for ri, count in enumerate(row_counts):\n            row_width = (count - 1) * h\n            # center row relative to the largest row (so pattern is centered)\n            x_start = 0.5 - row_width / 2.0\n            # alternate shift for hex packing\n            shift = (h / 2.0) if (ri % 2 == 1) else 0.0\n            y = y_origin + ri * v\n            for ci in range(count):\n                x = x_start + shift + ci * h\n                centers.append([x, y])\n        centers = np.array(centers)\n        # keep within unit square (should be by construction if s chosen appropriately)\n        centers = np.clip(centers, 1e-6, 1.0 - 1e-6)\n        return centers\n\n    def compute_max_radii(centers, max_iter=400, tol=1e-6):\n        \"\"\"\n        Iterative relaxation to find a feasible set of radii satisfying:\n         - r_i <= border_distance(i)\n         - r_i + r_j <= dist(i,j)\n\n        Starts with border distances and repeatedly enforces pairwise constraints\n        by proportional scaling; iterates until convergence.\n        \"\"\"\n        nloc = centers.shape[0]\n        radii = np.zeros(nloc)\n        # initial limit by borders\n        for i in range(nloc):\n            x, y = centers[i]\n            radii[i] = min(x, y, 1 - x, 1 - y)\n        if nloc <= 1:\n            return radii\n\n        # Precompute pairwise distances\n        diffs = centers[:, None, :] - centers[None, :, :]\n        dists = np.sqrt(np.maximum(0.0, np.sum(diffs * diffs, axis=2)))\n        # Ensure nonzero diagonal for numeric safety\n        np.fill_diagonal(dists, 1.0)\n\n        for it in range(max_iter):\n            max_change = 0.0\n            # enforce pairwise non-overlap constraints\n            for i in range(nloc):\n                for j in range(i + 1, nloc):\n                    dij = dists[i, j]\n                    if dij <= 1e-12:\n                        # coincident centers: force tiny radii split\n                        new_ri = min(radii[i], 1e-6)\n                        new_rj = min(radii[j], 1e-6)\n                        max_change = max(max_change, abs(radii[i] - new_ri), abs(radii[j] - new_rj))\n                        radii[i], radii[j] = new_ri, new_rj\n                        continue\n                    if radii[i] + radii[j] > dij:\n                        # scale proportionally to satisfy equality r_i + r_j = dij\n                        total = radii[i] + radii[j]\n                        if total <= 0:\n                            # both zero: nothing to do\n                            continue\n                        factor = dij / total\n                        new_ri = radii[i] * factor\n                        new_rj = radii[j] * factor\n                        max_change = max(max_change, abs(radii[i] - new_ri), abs(radii[j] - new_rj))\n                        radii[i], radii[j] = new_ri, new_rj\n            # enforce border constraints (may reduce radii)\n            for i in range(nloc):\n                x, y = centers[i]\n                border = min(x, y, 1 - x, 1 - y)\n                if radii[i] > border:\n                    max_change = max(max_change, radii[i] - border)\n                    radii[i] = border\n            if max_change < tol:\n                break\n        return radii\n\n    # Search for the best spacing s that maximizes sum of radii\n    # s must be small enough so centers lie inside the unit square without clipping.\n    # For our largest row_count=6, an upper bound s_max <= 1/(6-2) = 0.25 (safe).\n    s_min, s_max = 0.06, 0.25\n    # coarse search\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    s_vals = np.linspace(s_min, s_max, 48)\n    for s in s_vals:\n        centers_try = build_centers(s)\n        radii_try = compute_max_radii(centers_try, max_iter=300)\n        ssum = radii_try.sum()\n        if ssum > best_sum:\n            best_sum = ssum\n            best_centers = centers_try.copy()\n            best_radii = radii_try.copy()\n\n    # refine around best s\n    if best_centers is not None:\n        # infer best s from centers approximate horizontal spacing between first two different x's\n        # Fallback: sample narrower range around s that produced best_centers\n        # We approximate s_best by median of nearest neighbor x-differences\n        xs = np.sort(best_centers[:, 0])\n        diffs_x = np.diff(xs)\n        # pick a robust estimate of s from small positive diffs (exclude near-zero)\n        cand = diffs_x[diffs_x > 1e-4]\n        if cand.size > 0:\n            s_est = np.median(cand)\n        else:\n            s_est = (s_min + s_max) / 2.0\n\n        r_low = max(s_min, s_est * 0.85)\n        r_high = min(s_max, s_est * 1.15)\n        s_vals2 = np.linspace(r_low, r_high, 48)\n        for s in s_vals2:\n            centers_try = build_centers(s)\n            radii_try = compute_max_radii(centers_try, max_iter=500)\n            ssum = radii_try.sum()\n            if ssum > best_sum:\n                best_sum = ssum\n                best_centers = centers_try.copy()\n                best_radii = radii_try.copy()\n\n    # Final tightening: slight local jitter of positions (small random shifts) to possibly increase sum\n    # Try a few random perturbations to escape symmetric local optima\n    rng = np.random.default_rng(42)\n    for scale in [0.0, 0.0025, 0.005, 0.01]:\n        for _ in range(6):\n            if scale == 0.0:\n                pert = np.zeros_like(best_centers)\n            else:\n                pert = rng.normal(scale=scale, size=best_centers.shape)\n            centers_try = np.clip(best_centers + pert, 1e-6, 1.0 - 1e-6)\n            radii_try = compute_max_radii(centers_try, max_iter=600)\n            ssum = radii_try.sum()\n            if ssum > best_sum:\n                best_sum = ssum\n                best_centers = centers_try.copy()\n                best_radii = radii_try.copy()\n\n    # Ensure shapes and return\n    centers_final = np.array(best_centers)[:n].reshape((n, 2))\n    radii_final = np.array(best_radii)[:n].reshape((n,))\n    sum_radii = float(np.sum(radii_final))\n    return centers_final, radii_final, sum_radii\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.5)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (1.6952), Performs well on target_ratio (0.6434), Performs well on combined_score (0.6434), Performs well on eval_time (0.1077)\n\n\n\n## Diverse Programs\n\n### Program D1 (Score: 0.6068)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (explicit hex-like layout + iterative radius projection)\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    \"\"\"\n    Place 26 circle centers in a hex-like 4-row arrangement (7,6,7,6)\n    and compute maximal non-overlapping radii via iterative projection.\n    Returns (centers, radii, sum_of_radii).\n    \"\"\"\n    n = 26\n    counts = [7, 6, 7, 6]  # rows from top to bottom\n    rows = len(counts)\n\n    # Choose horizontal spacing s and derived vertical spacing v (hex packing)\n    s = 0.15\n    v = s * np.sqrt(3) / 2.0\n\n    # vertical placement: center the block vertically\n    total_v_span = (rows - 1) * v\n    y_start = 0.5 - total_v_span / 2.0\n\n    centers = np.zeros((n, 2))\n    idx = 0\n    for r in range(rows):\n        k = counts[r]\n        # alternate offset for hex pattern: even rows (0,2,...) aligned, odd rows offset by s/2\n        offset = 0.0 if (r % 2 == 0) else s / 2.0\n        row_width = (k - 1) * s\n        x_start = 0.5 - row_width / 2.0 + offset\n        y = y_start + r * v\n        for j in range(k):\n            x = x_start + j * s\n            # Clip very slightly inside [0,1] to avoid zero border distance\n            centers[idx, 0] = np.clip(x, 1e-6, 1 - 1e-6)\n            centers[idx, 1] = np.clip(y, 1e-6, 1 - 1e-6)\n            idx += 1\n\n    # Safety: ensure exactly n centers\n    centers = centers[:n]\n\n    radii = compute_max_radii(centers)\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\ndef compute_max_radii(centers, tol=1e-7, max_iter=2000):\n    \"\"\"\n    Iterative projection to maximize radii given fixed centers.\n    Initialization: radii = distance to nearest border.\n    Repeatedly enforce pairwise non-overlap by shrinking the larger of two radii\n    when ri + rj > dist. Iterate until convergence.\n    \"\"\"\n    n = centers.shape[0]\n    # distance to borders sets an upper bound\n    radii = np.minimum(centers[:, 0], centers[:, 1])\n    radii = np.minimum(radii, 1 - centers[:, 0])\n    radii = np.minimum(radii, 1 - centers[:, 1])\n\n    # Precompute distances between centers\n    D = np.sqrt(((centers[:, None, :] - centers[None, :, :]) ** 2).sum(axis=2))\n    # Avoid zeros on diagonal\n    np.fill_diagonal(D, 0.0)\n\n    for it in range(max_iter):\n        max_change = 0.0\n\n        # Enforce pairwise non-overlap\n        for i in range(n):\n            for j in range(i + 1, n):\n                dij = D[i, j]\n                if dij <= 1e-12:\n                    continue\n                ri, rj = radii[i], radii[j]\n                if ri + rj > dij:\n                    # shrink the larger radius; keep smaller as is (greedy projection)\n                    if ri > rj:\n                        new_ri = max(0.0, dij - rj)\n                        change = abs(new_ri - ri)\n                        if change:\n                            radii[i] = new_ri\n                            max_change = max(max_change, change)\n                    else:\n                        new_rj = max(0.0, dij - ri)\n                        change = abs(new_rj - rj)\n                        if change:\n                            radii[j] = new_rj\n                            max_change = max(max_change, change)\n\n        # Re-enforce border constraints (they may have been violated by numeric)\n        border_limits = np.minimum.reduce([centers[:, 0], centers[:, 1], 1 - centers[:, 0], 1 - centers[:, 1]])\n        over = radii > border_limits\n        if np.any(over):\n            change = np.max(np.abs(radii[over] - border_limits[over]))\n            radii[over] = border_limits[over]\n            max_change = max(max_change, float(change))\n\n        if max_change < tol:\n            break\n\n    return radii\n\n# EVOLVE-BLOCK-END\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.5)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nKey features: Alternative approach to validity, Alternative approach to sum_radii\n\n### Program D2 (Score: 0.3737)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles using an explicit hex-like lattice\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square\n    following a compact hexagonal-like layout (rows: 7,6,7,6).\n    Returns centers (26,2), radii (26,), sum_of_radii.\n    \"\"\"\n    n = 26\n    # hex spacing: choose s to pack 7 across (max row length)\n    max_len = 7\n    s = 1.0 / max_len  # center-to-center horizontal spacing\n    h = s * np.sqrt(3) / 2.0  # vertical spacing for hex rows\n    r = s / 2.0  # target radius if touching neighbors horizontally\n\n    # Row lengths and offsets (alternate shorter rows offset by s/2)\n    row_lengths = [7, 6, 7, 6]\n    offsets = [0.0, s / 2.0, 0.0, s / 2.0]\n    R = len(row_lengths)\n\n    # Compute vertical start so pattern is centered in the unit square\n    total_height = (R - 1) * h + 2 * r\n    y0 = (1.0 - total_height) / 2.0 + r\n\n    centers = []\n    for i in range(R):\n        L = row_lengths[i]\n        offset = offsets[i]\n        row_span = (L - 1) * s + 2 * r\n        # horizontal start to center the row\n        x0 = (1.0 - row_span) / 2.0 + r + offset\n        for j in range(L):\n            x = x0 + j * s\n            y = y0 + i * h\n            # clip to avoid numerical border violation\n            x = min(max(x, 0.0), 1.0)\n            y = min(max(y, 0.0), 1.0)\n            centers.append((x, y))\n\n    centers = np.array(centers[:n])  # ensure exactly n=26\n    radii = compute_max_radii(centers)\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\ndef compute_max_radii(centers, tol=1e-8, max_iter=2000):\n    \"\"\"\n    Iteratively compute maximal non-overlapping radii given centers inside unit square.\n    Start from border-limited radii and repeatedly enforce pairwise non-overlap constraints\n    by proportionally shrinking offending pairs until convergence.\n    \"\"\"\n    n = centers.shape[0]\n    # initial radii limited by distance to border\n    radii = np.minimum(centers[:,0], centers[:,1])\n    radii = np.minimum(radii, 1.0 - centers[:,0])\n    radii = np.minimum(radii, 1.0 - centers[:,1])\n\n    # Precompute pairwise distances\n    dx = centers[:, 0][:, None] - centers[:, 0][None, :]\n    dy = centers[:, 1][:, None] - centers[:, 1][None, :]\n    dist = np.sqrt(np.maximum(0.0, dx * dx + dy * dy))\n    # Avoid zeros on diagonal\n    for i in range(n):\n        dist[i, i] = 1.0\n\n    # Iteratively enforce radii[i] + radii[j] <= dist[i,j]\n    for iteration in range(max_iter):\n        max_change = 0.0\n        # Loop over pairs\n        for i in range(n):\n            for j in range(i + 1, n):\n                d = dist[i, j]\n                if d <= 0:\n                    continue\n                ssum = radii[i] + radii[j]\n                if ssum > d:\n                    # proportional shrink to meet equality\n                    if ssum == 0:\n                        continue\n                    scale = d / ssum\n                    new_i = radii[i] * scale\n                    new_j = radii[j] * scale\n                    # apply change and track max absolute change\n                    max_change = max(max_change, abs(radii[i] - new_i), abs(radii[j] - new_j))\n                    radii[i] = new_i\n                    radii[j] = new_j\n        if max_change < tol:\n            break\n\n    # Ensure border constraints still satisfied (numerical safety)\n    border_limits = np.minimum.reduce([centers[:,0], centers[:,1], 1.0-centers[:,0], 1.0-centers[:,1]])\n    radii = np.minimum(radii, border_limits)\n    return radii\n\n# EVOLVE-BLOCK-END\n\n# Fixed runner and visualizer (unchanged interface)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(8, 8))\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n    for i, (c, r) in enumerate(zip(centers, radii)):\n        circ = Circle(c, r, alpha=0.6)\n        ax.add_patch(circ)\n        ax.text(c[0], c[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nKey features: Alternative approach to validity, Alternative approach to sum_radii\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 0.9256, Type: High-Performer)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles using a hexagonal lattice constructor.\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a deterministic arrangement of 26 circle centers in [0,1]^2\n    using a hexagonal (offset-grid) lattice. We choose the largest lattice\n    spacing s that still produces at least 26 candidate centers, then\n    keep the 26 centers closest to the square center to mitigate edge effects.\n    Radii are set to the maximal non-overlapping values given these centers:\n        r_i = min(dist_to_border(i), 0.5 * min_j dist(center_i, center_j))\n    This yields a valid non-overlapping packing and tends to produce dense,\n    hexagonal-like local arrangements that maximize the sum of radii.\n    Returns:\n        (centers (26,2), radii (26,), sum_of_radii)\n    \"\"\"\n    n_target = 26\n\n    def make_hex_centers(s):\n        # margin so circles can have radius up to s/2 without crossing border\n        margin = s / 2.0\n        pts = []\n        y = margin\n        row = 0\n        y_step = s * np.sqrt(3) / 2.0\n        while y <= 1.0 - margin + 1e-12:\n            x_offset = (row % 2) * (s / 2.0)\n            x = margin + x_offset\n            while x <= 1.0 - margin + 1e-12:\n                pts.append((x, y))\n                x += s\n            row += 1\n            y += y_step\n        return np.array(pts)\n\n    # Binary search for the largest s that still yields at least n_target centers.\n    low, high = 0.01, 0.6  # high > possible spacing\n    best_s = low\n    for _ in range(40):\n        mid = 0.5 * (low + high)\n        pts = make_hex_centers(mid)\n        if pts.shape[0] >= n_target:\n            best_s = mid\n            low = mid  # we can try larger spacing\n        else:\n            high = mid\n    centers_all = make_hex_centers(best_s)\n\n    # If we have more than needed, keep the n_target centers closest to square center\n    if centers_all.shape[0] > n_target:\n        center_of_square = np.array([0.5, 0.5])\n        dists = np.linalg.norm(centers_all - center_of_square, axis=1)\n        idx = np.argsort(dists)[:n_target]\n        centers = centers_all[idx]\n    else:\n        centers = centers_all.copy()\n\n    # As a small enhancement, ensure we always return exactly n_target centers\n    # (should be guaranteed by construction/trimming above)\n    if centers.shape[0] < n_target:\n        # fallback: fill with evenly spaced points along a small spiral near center\n        extra = n_target - centers.shape[0]\n        spiral = []\n        for k in range(extra):\n            t = 0.05 * (k + 1)\n            angle = 2 * np.pi * (k + 1) / extra\n            spiral.append([0.5 + t * np.cos(angle), 0.5 + t * np.sin(angle)])\n        centers = np.vstack([centers, np.array(spiral)])\n\n    # Sort centers for determinism (by x then y)\n    centers = centers[np.lexsort((centers[:, 1], centers[:, 0]))]\n\n    # Compute maximal valid radii:\n    radii = compute_max_radii(centers)\n\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers):\n    \"\"\"\n    For a fixed set of centers, compute a safe maximal radius assignment\n    that guarantees no overlaps and containment in the unit square.\n    We use the conservative but tight rule:\n        r_i = min(dist_to_border(i), 0.5 * min_j dist(i,j))\n    which makes touching allowed but not overlapping.\n    \"\"\"\n    n = centers.shape[0]\n    radii = np.zeros(n)\n    # distance to borders\n    dist_border = np.minimum.reduce([centers[:, 0], centers[:, 1], 1 - centers[:, 0], 1 - centers[:, 1]])\n    # pairwise distances (compute efficiently)\n    diff = centers[:, None, :] - centers[None, :, :]  # n x n x 2\n    dists = np.sqrt(np.sum(diff ** 2, axis=2))\n    # set diagonal to a large number to ignore self-distance\n    np.fill_diagonal(dists, np.inf)\n    min_pair = np.min(dists, axis=1)\n    # radii limited by half the nearest neighbor distance and by borders\n    radii = np.minimum(dist_border, 0.5 * min_pair)\n    # Ensure non-negative and tiny numerical floor\n    radii = np.maximum(radii, 0.0)\n    return radii\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor='k', linewidth=0.5)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Hex-lattice Constructor Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nUnique approach: Modification: Full rewrite, Excellent validity (1.000), Excellent sum_radii (2.439)\n\n### Inspiration 2 (Score: 0.3642, Type: Exploratory)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square\n    that attempts to maximize the sum of their radii.\n\n    Returns:\n        Tuple of (centers, radii, sum_of_radii)\n        centers: np.array of shape (26, 2) with (x, y) coordinates\n        radii: np.array of shape (26) with radius of each circle\n        sum_of_radii: Sum of all radii\n    \"\"\"\n    # Initialize arrays for 26 circles\n    n = 26\n    centers = np.zeros((n, 2))\n\n    # Place circles in a structured pattern\n    # This is a simple pattern - evolution will improve this\n\n    # First, place a large circle in the center\n    centers[0] = [0.5, 0.5]\n\n    # Place 8 circles around it in a ring\n    for i in range(8):\n        angle = 2 * np.pi * i / 8\n        centers[i + 1] = [0.5 + 0.3 * np.cos(angle), 0.5 + 0.3 * np.sin(angle)]\n\n    # Place 16 more circles in an outer ring\n    for i in range(16):\n        angle = 2 * np.pi * i / 16\n        centers[i + 9] = [0.5 + 0.7 * np.cos(angle), 0.5 + 0.7 * np.sin(angle)]\n\n    # Additional positioning adjustment to make sure all circles\n    # are inside the square and don't overlap\n    # Clip to ensure everything is inside the unit square\n    centers = np.clip(centers, 0.01, 0.99)\n\n    # Compute maximum valid radii for this configuration\n    radii = compute_max_radii(centers)\n\n    # Calculate the sum of radii\n    sum_radii = np.sum(radii)\n\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers):\n    \"\"\"\n    Compute the maximum possible radii for each circle position\n    such that they don't overlap and stay within the unit square.\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n\n    Returns:\n        np.array of shape (n) with radius of each circle\n    \"\"\"\n    n = centers.shape[0]\n    radii = np.ones(n)\n\n    # First, limit by distance to square borders\n    for i in range(n):\n        x, y = centers[i]\n        # Distance to borders\n        radii[i] = min(x, y, 1 - x, 1 - y)\n\n    # Then, limit by distance to other circles\n    # Each pair of circles with centers at distance d can have\n    # sum of radii at most d to avoid overlap\n    for i in range(n):\n        for j in range(i + 1, n):\n            dist = np.sqrt(np.sum((centers[i] - centers[j]) ** 2))\n\n            # If current radii would cause overlap\n            if radii[i] + radii[j] > dist:\n                # Scale both radii proportionally\n                scale = dist / (radii[i] + radii[j])\n                radii[i] *= scale\n                radii[j] *= scale\n\n    return radii\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.5)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\")\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii}\")\n    # AlphaEvolve improved this to 2.635\n\n    # Uncomment to visualize:\n    visualize(centers, radii)\n\n```\nUnique approach: Excellent validity (1.000), Excellent sum_radii (0.960), NumPy-based implementation\n\n### Inspiration 3 (Score: 0.6068, Type: Alternative)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (explicit hex-like layout + iterative radius projection)\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    \"\"\"\n    Place 26 circle centers in a hex-like 4-row arrangement (7,6,7,6)\n    and compute maximal non-overlapping radii via iterative projection.\n    Returns (centers, radii, sum_of_radii).\n    \"\"\"\n    n = 26\n    counts = [7, 6, 7, 6]  # rows from top to bottom\n    rows = len(counts)\n\n    # Choose horizontal spacing s and derived vertical spacing v (hex packing)\n    s = 0.15\n    v = s * np.sqrt(3) / 2.0\n\n    # vertical placement: center the block vertically\n    total_v_span = (rows - 1) * v\n    y_start = 0.5 - total_v_span / 2.0\n\n    centers = np.zeros((n, 2))\n    idx = 0\n    for r in range(rows):\n        k = counts[r]\n        # alternate offset for hex pattern: even rows (0,2,...) aligned, odd rows offset by s/2\n        offset = 0.0 if (r % 2 == 0) else s / 2.0\n        row_width = (k - 1) * s\n        x_start = 0.5 - row_width / 2.0 + offset\n        y = y_start + r * v\n        for j in range(k):\n            x = x_start + j * s\n            # Clip very slightly inside [0,1] to avoid zero border distance\n            centers[idx, 0] = np.clip(x, 1e-6, 1 - 1e-6)\n            centers[idx, 1] = np.clip(y, 1e-6, 1 - 1e-6)\n            idx += 1\n\n    # Safety: ensure exactly n centers\n    centers = centers[:n]\n\n    radii = compute_max_radii(centers)\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\ndef compute_max_radii(centers, tol=1e-7, max_iter=2000):\n    \"\"\"\n    Iterative projection to maximize radii given fixed centers.\n    Initialization: radii = distance to nearest border.\n    Repeatedly enforce pairwise non-overlap by shrinking the larger of two radii\n    when ri + rj > dist. Iterate until convergence.\n    \"\"\"\n    n = centers.shape[0]\n    # distance to borders sets an upper bound\n    radii = np.minimum(centers[:, 0], centers[:, 1])\n    radii = np.minimum(radii, 1 - centers[:, 0])\n    radii = np.minimum(radii, 1 - centers[:, 1])\n\n    # Precompute distances between centers\n    D = np.sqrt(((centers[:, None, :] - centers[None, :, :]) ** 2).sum(axis=2))\n    # Avoid zeros on diagonal\n    np.fill_diagonal(D, 0.0)\n\n    for it in range(max_iter):\n        max_change = 0.0\n\n        # Enforce pairwise non-overlap\n        for i in range(n):\n            for j in range(i + 1, n):\n                dij = D[i, j]\n                if dij <= 1e-12:\n                    continue\n                ri, rj = radii[i], radii[j]\n                if ri + rj > dij:\n                    # shrink the larger radius; keep smaller as is (greedy projection)\n                    if ri > rj:\n                        new_ri = max(0.0, dij - rj)\n                        change = abs(new_ri - ri)\n                        if change:\n                            radii[i] = new_ri\n                            max_change = max(max_change, change)\n                    else:\n                        new_rj = max(0.0, dij - ri)\n                        change = abs(new_rj - rj)\n                        if change:\n                            radii[j] = new_rj\n                            max_change = max(max_change, change)\n\n        # Re-enforce border constraints (they may have been violated by numeric)\n        border_limits = np.minimum.reduce([centers[:, 0], centers[:, 1], 1 - centers[:, 0], 1 - centers[:, 1]])\n        over = radii > border_limits\n        if np.any(over):\n            change = np.max(np.abs(radii[over] - border_limits[over]))\n            radii[over] = border_limits[over]\n            max_change = max(max_change, float(change))\n\n        if max_change < tol:\n            break\n\n    return radii\n\n# EVOLVE-BLOCK-END\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.5)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nUnique approach: Modification: Full rewrite, Excellent validity (1.000), Excellent sum_radii (1.599)\n\n# Current Program\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles using an explicit hex-like lattice\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square\n    following a compact hexagonal-like layout (rows: 7,6,7,6).\n    Returns centers (26,2), radii (26,), sum_of_radii.\n    \"\"\"\n    n = 26\n    # hex spacing: choose s to pack 7 across (max row length)\n    max_len = 7\n    s = 1.0 / max_len  # center-to-center horizontal spacing\n    h = s * np.sqrt(3) / 2.0  # vertical spacing for hex rows\n    r = s / 2.0  # target radius if touching neighbors horizontally\n\n    # Row lengths and offsets (alternate shorter rows offset by s/2)\n    row_lengths = [7, 6, 7, 6]\n    offsets = [0.0, s / 2.0, 0.0, s / 2.0]\n    R = len(row_lengths)\n\n    # Compute vertical start so pattern is centered in the unit square\n    total_height = (R - 1) * h + 2 * r\n    y0 = (1.0 - total_height) / 2.0 + r\n\n    centers = []\n    for i in range(R):\n        L = row_lengths[i]\n        offset = offsets[i]\n        row_span = (L - 1) * s + 2 * r\n        # horizontal start to center the row\n        x0 = (1.0 - row_span) / 2.0 + r + offset\n        for j in range(L):\n            x = x0 + j * s\n            y = y0 + i * h\n            # clip to avoid numerical border violation\n            x = min(max(x, 0.0), 1.0)\n            y = min(max(y, 0.0), 1.0)\n            centers.append((x, y))\n\n    centers = np.array(centers[:n])  # ensure exactly n=26\n    radii = compute_max_radii(centers)\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\ndef compute_max_radii(centers, tol=1e-8, max_iter=2000):\n    \"\"\"\n    Iteratively compute maximal non-overlapping radii given centers inside unit square.\n    Start from border-limited radii and repeatedly enforce pairwise non-overlap constraints\n    by proportionally shrinking offending pairs until convergence.\n    \"\"\"\n    n = centers.shape[0]\n    # initial radii limited by distance to border\n    radii = np.minimum(centers[:,0], centers[:,1])\n    radii = np.minimum(radii, 1.0 - centers[:,0])\n    radii = np.minimum(radii, 1.0 - centers[:,1])\n\n    # Precompute pairwise distances\n    dx = centers[:, 0][:, None] - centers[:, 0][None, :]\n    dy = centers[:, 1][:, None] - centers[:, 1][None, :]\n    dist = np.sqrt(np.maximum(0.0, dx * dx + dy * dy))\n    # Avoid zeros on diagonal\n    for i in range(n):\n        dist[i, i] = 1.0\n\n    # Iteratively enforce radii[i] + radii[j] <= dist[i,j]\n    for iteration in range(max_iter):\n        max_change = 0.0\n        # Loop over pairs\n        for i in range(n):\n            for j in range(i + 1, n):\n                d = dist[i, j]\n                if d <= 0:\n                    continue\n                ssum = radii[i] + radii[j]\n                if ssum > d:\n                    # proportional shrink to meet equality\n                    if ssum == 0:\n                        continue\n                    scale = d / ssum\n                    new_i = radii[i] * scale\n                    new_j = radii[j] * scale\n                    # apply change and track max absolute change\n                    max_change = max(max_change, abs(radii[i] - new_i), abs(radii[j] - new_j))\n                    radii[i] = new_i\n                    radii[j] = new_j\n        if max_change < tol:\n            break\n\n    # Ensure border constraints still satisfied (numerical safety)\n    border_limits = np.minimum.reduce([centers[:,0], centers[:,1], 1.0-centers[:,0], 1.0-centers[:,1]])\n    radii = np.minimum(radii, border_limits)\n    return radii\n\n# EVOLVE-BLOCK-END\n\n# Fixed runner and visualizer (unchanged interface)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(8, 8))\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n    for i, (c, r) in enumerate(zip(centers, radii)):\n        circ = Circle(c, r, alpha=0.6)\n        ax.add_patch(circ)\n        ax.text(c[0], c[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\n\n# Task\nRewrite the program to improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: complexity, diversity\nDifferent solutions with similar fitness but different features are valuable.\nProvide the complete new program code.\n\nIMPORTANT: Make sure your rewritten program maintains the same inputs and outputs\nas the original program, but with improved internal implementation.\n\n```python\n# Your rewritten program here\n```", "responses": ["```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 using a centered hexagonal-lattice constructor.\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    \"\"\"\n    Deterministic constructor that builds a hexagonal (offset-grid) lattice of candidate\n    centers, chooses the 26 centers closest to the square center (to reduce edge effects),\n    and assigns each circle the tightest safe radius:\n        r_i = min(distance_to_border(i), 0.5 * min_{j!=i} dist(center_i, center_j))\n    This simple, explicit constructor tends to produce dense, near-hexagonal packings\n    in the middle of the square and gives a high sum of radii for n=26.\n    Returns: (centers (26,2), radii (26,), sum_of_radii)\n    \"\"\"\n    n_target = 26\n\n    def make_hex_centers(s):\n        # margin ensures centers are at least s/2 from border so circles of radius s/2 fit\n        margin = s * 0.5\n        pts = []\n        y = margin\n        row = 0\n        y_step = s * np.sqrt(3) / 2.0\n        # generate rows until we exceed the unit square\n        while y <= 1.0 - margin + 1e-12:\n            x_offset = (row % 2) * (s / 2.0)\n            x = margin + x_offset\n            while x <= 1.0 - margin + 1e-12:\n                pts.append((x, y))\n                x += s\n            row += 1\n            y += y_step\n        return np.array(pts)\n\n    # Binary search for the largest spacing s that yields at least n_target centers.\n    low, high = 0.01, 0.6\n    best_s = low\n    for _ in range(45):\n        mid = 0.5 * (low + high)\n        pts = make_hex_centers(mid)\n        if pts.shape[0] >= n_target:\n            best_s = mid\n            low = mid\n        else:\n            high = mid\n    centers_all = make_hex_centers(best_s)\n\n    # If more centers than needed, keep those closest to square center\n    if centers_all.shape[0] > n_target:\n        center_sq = np.array([0.5, 0.5])\n        dists = np.linalg.norm(centers_all - center_sq, axis=1)\n        idx = np.argsort(dists)[:n_target]\n        centers = centers_all[idx]\n    else:\n        # If exactly or fewer (rare), pad/fill deterministically near center\n        centers = centers_all.copy()\n        if centers.shape[0] < n_target:\n            extra = n_target - centers.shape[0]\n            # deterministic small spiral placements\n            for k in range(extra):\n                t = 0.04 * (k + 1)\n                ang = 2 * np.pi * (k + 1) / extra\n                centers = np.vstack([centers, [0.5 + t * np.cos(ang), 0.5 + t * np.sin(ang)]])\n    # Sort for determinism (by x then y)\n    centers = centers[np.lexsort((centers[:,1], centers[:,0]))][:n_target]\n\n    # Compute maximal safe radii: limited by border and half nearest-neighbor distance\n    def compute_max_radii(centers):\n        n = centers.shape[0]\n        # distance to border\n        dist_border = np.minimum.reduce([centers[:,0], centers[:,1], 1.0 - centers[:,0], 1.0 - centers[:,1]])\n        # pairwise distances\n        diff = centers[:, None, :] - centers[None, :, :]  # n x n x 2\n        dists = np.sqrt(np.sum(diff**2, axis=2))\n        np.fill_diagonal(dists, np.inf)\n        min_pair = np.min(dists, axis=1)\n        radii = np.minimum(dist_border, 0.5 * min_pair)\n        radii = np.maximum(radii, 0.0)\n        return radii\n\n    radii = compute_max_radii(centers)\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n# EVOLVE-BLOCK-END\n\n# Runner and visualizer (unchanged interface)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(6,6))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect(\"equal\"); ax.grid(True)\n    for i, (c, r) in enumerate(zip(centers, radii)):\n        circ = Circle(c, r, alpha=0.6, edgecolor='k')\n        ax.add_patch(circ)\n        ax.text(c[0], c[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n    plt.title(f\"Hex-lattice Constructor (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # visualize(centers, radii)\n```"]}}, "artifacts_json": "{\"stdout\": \"Running in subprocess, Python version: 3.12.1 (v3.12.1:2305ca5144, Dec  7 2023, 17:23:38) [Clang 13.0.0 (clang-1300.0.29.30)]\\nProgram path: /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmpuz9phg2b.py\\nCalling run_packing()...\\nrun_packing() returned successfully: sum_radii = 2.4389662673781265\\nResults saved to /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmpge3dyfri.py.results\\n\", \"stderr\": \"\"}", "artifact_dir": null}