{"id": "5981223d-7f0d-4f91-b94d-58bbb254a09a", "code": "# EVOLVE-BLOCK-START\n\"\"\"Improved deterministic constructor for n=26 circle packing in the unit square.\n\nApproach:\n- Build many hexagonal (offset) lattice candidates by sweeping spacing s and small\n  lattice offsets. For each candidate with at least 26 centers, keep the 26\n  centers closest to the square center (mitigates edge effects).\n- For fixed centers compute a guaranteed non-overlapping radius assignment using\n  the tight conservative formula:\n      r_i = min(distance_to_border(i), 0.5 * min_{j!=i} dist(center_i, center_j))\n  (This is a fast, reliable upper bound for fixed centers; choosing the best\n  candidate lattice usually gives very good total radius sums.)\n- Return the best candidate found (highest sum of radii). Deterministic.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n_target = 26\n    center_sq = np.array([0.5, 0.5])\n\n    def make_hex_centers(s, x_offset=0.0, y_offset=0.0):\n        \"\"\"Generate hex-lattice centers with horizontal spacing s and offsets.\n        The lattice generation ensures centers lie in [0,1]^2.\n        \"\"\"\n        margin = 1e-12\n        y_step = s * np.sqrt(3.0) / 2.0\n        pts = []\n        # start y so that adding y_offset recenters possibilities deterministically\n        y = y_offset\n        # generate rows while inside [0,1]\n        row = 0\n        while y <= 1.0 + margin:\n            x_off = (row % 2) * (s / 2.0) + x_offset\n            x = x_off\n            while x <= 1.0 + margin:\n                if 0.0 <= x <= 1.0 and 0.0 <= y <= 1.0:\n                    pts.append((x, y))\n                x += s\n            row += 1\n            y += y_step\n        return np.array(pts, dtype=float)\n\n    def radii_from_centers(centers):\n        \"\"\"Fast conservative max radii for fixed centers.\"\"\"\n        # distance to border\n        db = np.minimum.reduce([centers[:,0], centers[:,1], 1.0 - centers[:,0], 1.0 - centers[:,1]])\n        # pairwise distances\n        diff = centers[:, None, :] - centers[None, :, :]\n        dists = np.sqrt(np.sum(diff**2, axis=2))\n        np.fill_diagonal(dists, np.inf)\n        min_pair = np.min(dists, axis=1)\n        radii = np.minimum(db, 0.5 * min_pair)\n        radii = np.maximum(radii, 0.0)\n        return radii\n\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    # Sweep plausible spacings. Range chosen to allow fairly dense hex blocks\n    s_vals = np.linspace(0.09, 0.25, 48)  # denser sampling\n    # Small deterministic offsets (three values each) to shift lattice relative to square\n    # improves chance to align a dense central patch.\n    offs = [0.0, 0.25, 0.5]\n\n    for s in s_vals:\n        v = s * np.sqrt(3.0) / 2.0\n        # choose x,y offsets in [0,s) and [0,v) via scaled offs\n        for xo in offs:\n            for yo in offs:\n                x_offset = (xo * s) % s\n                y_offset = (yo * v) % v\n                pts = make_hex_centers(s, x_offset, y_offset)\n                if pts.shape[0] < n_target:\n                    continue\n                # pick the n_target centers closest to the square center\n                d = np.linalg.norm(pts - center_sq, axis=1)\n                idx = np.argsort(d)[:n_target]\n                centers = pts[idx]\n                # sort deterministically\n                centers = centers[np.lexsort((centers[:,1], centers[:,0]))]\n                radii = radii_from_centers(centers)\n                ssum = float(np.sum(radii))\n                if ssum > best_sum + 1e-12:\n                    best_sum = ssum\n                    best_centers = centers.copy()\n                    best_radii = radii.copy()\n\n    # Fallback: if no lattice candidate produced 26 centers (unlikely), use a small spiral\n    if best_centers is None:\n        centers = []\n        for k in range(n_target):\n            t = 0.02 * (k + 1)\n            ang = 2.0 * np.pi * k / max(1, n_target)\n            centers.append([0.5 + t * np.cos(ang), 0.5 + t * np.sin(ang)])\n        centers = np.array(centers, dtype=float)\n        centers = np.clip(centers, 1e-12, 1.0 - 1e-12)\n        best_centers = centers\n        best_radii = radii_from_centers(centers)\n        best_sum = float(np.sum(best_radii))\n\n    return best_centers, best_radii, float(best_sum)\n# EVOLVE-BLOCK-END\n\n# Runner and visualizer (unchanged interface)\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(7,7))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect(\"equal\")\n    for c,r in zip(centers, radii):\n        ax.add_patch(Circle(c, r, alpha=0.6, edgecolor='k'))\n    ax.grid(True)\n    plt.title(f\"n={len(centers)}, sum={sum(radii):.6f}\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, s = run_packing()\n    print(f\"Sum of radii: {s:.6f}\")\n    # visualize(centers, radii)", "language": "python", "parent_id": "ebc4ef5c-ee6d-4d26-a237-3e1fdc2b74f1", "generation": 2, "timestamp": 1759958978.736224, "iteration_found": 22, "metrics": {"validity": 1.0, "sum_radii": 2.3774993190925944, "target_ratio": 0.9022767814393148, "combined_score": 0.9022767814393148, "eval_time": 0.07756805419921875}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Full rewrite", "parent_metrics": {"validity": 1.0, "sum_radii": 2.083333343333333, "target_ratio": 0.7906388399746995, "combined_score": 0.7906388399746995, "eval_time": 0.05682015419006348}, "island": 0}, "prompts": {"full_rewrite_user": {"system": "You are an expert mathematician specializing in circle packing problems and computational geometry. Your task is to improve a constructor function that directly produces a specific arrangement of 26 circles in a unit square, maximizing the sum of their radii. The AlphaEvolve paper achieved a sum of 2.635 for n=26.\n\nKey geometric insights:\n- Circle packings often follow hexagonal patterns in the densest regions\n- Maximum density for infinite circle packing is pi/(2*sqrt(3)) \u2248 0.9069\n- Edge effects make square container packing harder than infinite packing\n- Circles can be placed in layers or shells when confined to a square\n- Similar radius circles often form regular patterns, while varied radii allow better space utilization\n- Perfect symmetry may not yield the optimal packing due to edge effects\n\nFocus on designing an explicit constructor that places each circle in a specific position, rather than an iterative search algorithm.\n\n\n## Evolution Insights (Global Learnings)\n\n### Common Pitfalls:\n\u26a0\ufe0f eval_time decreased by 42.8% (0.108 \u2192 0.062) (seen 1x)", "user": "# Current Program Information\n- Fitness: 0.7906\n- Feature coordinates: No feature coordinates\n- Focus areas: - Fitness declined: 0.9256 \u2192 0.7906. Consider revising recent changes.\n- Consider simplifying - code length exceeds 500 characters\n\n## Last Execution Output\n\n### stdout\n```\nRunning in subprocess, Python version: 3.12.1 (v3.12.1:2305ca5144, Dec  7 2023, 17:23:38) [Clang 13.0.0 (clang-1300.0.29.30)]\nProgram path: /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmpdtez046y.py\nCalling run_packing()...\nrun_packing() returned successfully: sum_radii = 2.083333343333333\nResults saved to /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmpp7me_24c.py.results\n\n```\n\n### stderr\n```\n\n```\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 3\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.4390, target_ratio: 0.9256, combined_score: 0.9256, eval_time: 0.0594\n- Outcome: Mixed results\n\n### Attempt 2\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.4390, target_ratio: 0.9256, combined_score: 0.9256, eval_time: 0.0571\n- Outcome: Mixed results\n\n### Attempt 1\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.4390, target_ratio: 0.9256, combined_score: 0.9256, eval_time: 0.0597\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.9256)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for n=26 circle packing in the unit square.\nBuild a hexagonal (offset) lattice, pick the 26 centers closest to the square\ncenter for best edge-effect mitigation, and set radii by the tight safe rule\nr_i = min(dist_to_border(i), 0.5 * min_{j!=i} dist(i,j)).\nThis simple constructor yields very compact local hexagonal arrangements\nand produces a high total sum of radii.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n_target = 26\n\n    def hex_centers(s):\n        # margin ensures a circle of radius s/2 around a center stays inside square\n        margin = s / 2.0\n        pts = []\n        y = margin\n        row = 0\n        y_step = s * np.sqrt(3) / 2.0\n        # generate rows while centers remain within [margin, 1-margin]\n        while y <= 1.0 - margin + 1e-12:\n            x_off = (row % 2) * (s / 2.0)\n            x = margin + x_off\n            while x <= 1.0 - margin + 1e-12:\n                pts.append((x, y))\n                x += s\n            row += 1\n            y += y_step\n        return np.array(pts)\n\n    # binary search largest spacing s that yields at least n_target centers\n    lo, hi = 0.01, 0.6\n    best_s = lo\n    for _ in range(40):\n        mid = 0.5 * (lo + hi)\n        pts = hex_centers(mid)\n        if pts.shape[0] >= n_target:\n            best_s = mid\n            lo = mid\n        else:\n            hi = mid\n    candidates = hex_centers(best_s)\n\n    # choose the n_target centers closest to center (mitigate edge effects)\n    if candidates.shape[0] > n_target:\n        center = np.array([0.5, 0.5])\n        d = np.linalg.norm(candidates - center, axis=1)\n        idx = np.argsort(d)[:n_target]\n        centers = candidates[idx]\n    else:\n        centers = candidates.copy()\n\n    # deterministic ordering\n    centers = centers[np.lexsort((centers[:,1], centers[:,0]))]\n\n    # compute maximal safe radii: min(border_dist, 0.5 * nearest_neighbor)\n    dist_border = np.minimum.reduce([centers[:,0], centers[:,1], 1 - centers[:,0], 1 - centers[:,1]])\n    diff = centers[:, None, :] - centers[None, :, :]\n    dists = np.sqrt(np.sum(diff**2, axis=2))\n    np.fill_diagonal(dists, np.inf)\n    min_pair = np.min(dists, axis=1)\n    radii = np.minimum(dist_border, 0.5 * min_pair)\n    radii = np.maximum(radii, 0.0)\n\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n# EVOLVE-BLOCK-END\n\n# Fixed runner and visualizer (kept for compatibility)\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(8,8))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect(\"equal\")\n    for c,r in zip(centers, radii):\n        ax.add_patch(Circle(c, r, alpha=0.6, edgecolor='k'))\n    ax.grid(True)\n    plt.title(f\"n={len(centers)}, sum={sum(radii):.6f}\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, s = run_packing()\n    print(f\"Sum of radii: {s:.6f}\")\n    # visualize(centers, radii)\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.4390), Performs well on target_ratio (0.9256), Performs well on combined_score (0.9256), Performs well on eval_time (0.0597)\n\n### Program 2 (Score: 0.9256)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles using a hexagonal lattice constructor.\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a deterministic arrangement of 26 circle centers in [0,1]^2\n    using a hexagonal (offset-grid) lattice. We choose the largest lattice\n    spacing s that still produces at least 26 candidate centers, then\n    keep the 26 centers closest to the square center to mitigate edge effects.\n    Radii are set to the maximal non-overlapping values given these centers:\n        r_i = min(dist_to_border(i), 0.5 * min_j dist(center_i, center_j))\n    This yields a valid non-overlapping packing and tends to produce dense,\n    hexagonal-like local arrangements that maximize the sum of radii.\n    Returns:\n        (centers (26,2), radii (26,), sum_of_radii)\n    \"\"\"\n    n_target = 26\n\n    def make_hex_centers(s):\n        # margin so circles can have radius up to s/2 without crossing border\n        margin = s / 2.0\n        pts = []\n        y = margin\n        row = 0\n        y_step = s * np.sqrt(3) / 2.0\n        while y <= 1.0 - margin + 1e-12:\n            x_offset = (row % 2) * (s / 2.0)\n            x = margin + x_offset\n            while x <= 1.0 - margin + 1e-12:\n                pts.append((x, y))\n                x += s\n            row += 1\n            y += y_step\n        return np.array(pts)\n\n    # Binary search for the largest s that still yields at least n_target centers.\n    low, high = 0.01, 0.6  # high > possible spacing\n    best_s = low\n    for _ in range(40):\n        mid = 0.5 * (low + high)\n        pts = make_hex_centers(mid)\n        if pts.shape[0] >= n_target:\n            best_s = mid\n            low = mid  # we can try larger spacing\n        else:\n            high = mid\n    centers_all = make_hex_centers(best_s)\n\n    # If we have more than needed, keep the n_target centers closest to square center\n    if centers_all.shape[0] > n_target:\n        center_of_square = np.array([0.5, 0.5])\n        dists = np.linalg.norm(centers_all - center_of_square, axis=1)\n        idx = np.argsort(dists)[:n_target]\n        centers = centers_all[idx]\n    else:\n        centers = centers_all.copy()\n\n    # As a small enhancement, ensure we always return exactly n_target centers\n    # (should be guaranteed by construction/trimming above)\n    if centers.shape[0] < n_target:\n        # fallback: fill with evenly spaced points along a small spiral near center\n        extra = n_target - centers.shape[0]\n        spiral = []\n        for k in range(extra):\n            t = 0.05 * (k + 1)\n            angle = 2 * np.pi * (k + 1) / extra\n            spiral.append([0.5 + t * np.cos(angle), 0.5 + t * np.sin(angle)])\n        centers = np.vstack([centers, np.array(spiral)])\n\n    # Sort centers for determinism (by x then y)\n    centers = centers[np.lexsort((centers[:, 1], centers[:, 0]))]\n\n    # Compute maximal valid radii:\n    radii = compute_max_radii(centers)\n\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers):\n    \"\"\"\n    For a fixed set of centers, compute a safe maximal radius assignment\n    that guarantees no overlaps and containment in the unit square.\n    We use the conservative but tight rule:\n        r_i = min(dist_to_border(i), 0.5 * min_j dist(i,j))\n    which makes touching allowed but not overlapping.\n    \"\"\"\n    n = centers.shape[0]\n    radii = np.zeros(n)\n    # distance to borders\n    dist_border = np.minimum.reduce([centers[:, 0], centers[:, 1], 1 - centers[:, 0], 1 - centers[:, 1]])\n    # pairwise distances (compute efficiently)\n    diff = centers[:, None, :] - centers[None, :, :]  # n x n x 2\n    dists = np.sqrt(np.sum(diff ** 2, axis=2))\n    # set diagonal to a large number to ignore self-distance\n    np.fill_diagonal(dists, np.inf)\n    min_pair = np.min(dists, axis=1)\n    # radii limited by half the nearest neighbor distance and by borders\n    radii = np.minimum(dist_border, 0.5 * min_pair)\n    # Ensure non-negative and tiny numerical floor\n    radii = np.maximum(radii, 0.0)\n    return radii\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor='k', linewidth=0.5)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Hex-lattice Constructor Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.4390), Performs well on target_ratio (0.9256), Performs well on combined_score (0.9256), Performs well on eval_time (0.0571)\n\n### Program 3 (Score: 0.9256)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 using a centered hexagonal-lattice constructor.\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    \"\"\"\n    Deterministic constructor that builds a hexagonal (offset-grid) lattice of candidate\n    centers, chooses the 26 centers closest to the square center (to reduce edge effects),\n    and assigns each circle the tightest safe radius:\n        r_i = min(distance_to_border(i), 0.5 * min_{j!=i} dist(center_i, center_j))\n    This simple, explicit constructor tends to produce dense, near-hexagonal packings\n    in the middle of the square and gives a high sum of radii for n=26.\n    Returns: (centers (26,2), radii (26,), sum_of_radii)\n    \"\"\"\n    n_target = 26\n\n    def make_hex_centers(s):\n        # margin ensures centers are at least s/2 from border so circles of radius s/2 fit\n        margin = s * 0.5\n        pts = []\n        y = margin\n        row = 0\n        y_step = s * np.sqrt(3) / 2.0\n        # generate rows until we exceed the unit square\n        while y <= 1.0 - margin + 1e-12:\n            x_offset = (row % 2) * (s / 2.0)\n            x = margin + x_offset\n            while x <= 1.0 - margin + 1e-12:\n                pts.append((x, y))\n                x += s\n            row += 1\n            y += y_step\n        return np.array(pts)\n\n    # Binary search for the largest spacing s that yields at least n_target centers.\n    low, high = 0.01, 0.6\n    best_s = low\n    for _ in range(45):\n        mid = 0.5 * (low + high)\n        pts = make_hex_centers(mid)\n        if pts.shape[0] >= n_target:\n            best_s = mid\n            low = mid\n        else:\n            high = mid\n    centers_all = make_hex_centers(best_s)\n\n    # If more centers than needed, keep those closest to square center\n    if centers_all.shape[0] > n_target:\n        center_sq = np.array([0.5, 0.5])\n        dists = np.linalg.norm(centers_all - center_sq, axis=1)\n        idx = np.argsort(dists)[:n_target]\n        centers = centers_all[idx]\n    else:\n        # If exactly or fewer (rare), pad/fill deterministically near center\n        centers = centers_all.copy()\n        if centers.shape[0] < n_target:\n            extra = n_target - centers.shape[0]\n            # deterministic small spiral placements\n            for k in range(extra):\n                t = 0.04 * (k + 1)\n                ang = 2 * np.pi * (k + 1) / extra\n                centers = np.vstack([centers, [0.5 + t * np.cos(ang), 0.5 + t * np.sin(ang)]])\n    # Sort for determinism (by x then y)\n    centers = centers[np.lexsort((centers[:,1], centers[:,0]))][:n_target]\n\n    # Compute maximal safe radii: limited by border and half nearest-neighbor distance\n    def compute_max_radii(centers):\n        n = centers.shape[0]\n        # distance to border\n        dist_border = np.minimum.reduce([centers[:,0], centers[:,1], 1.0 - centers[:,0], 1.0 - centers[:,1]])\n        # pairwise distances\n        diff = centers[:, None, :] - centers[None, :, :]  # n x n x 2\n        dists = np.sqrt(np.sum(diff**2, axis=2))\n        np.fill_diagonal(dists, np.inf)\n        min_pair = np.min(dists, axis=1)\n        radii = np.minimum(dist_border, 0.5 * min_pair)\n        radii = np.maximum(radii, 0.0)\n        return radii\n\n    radii = compute_max_radii(centers)\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n# EVOLVE-BLOCK-END\n\n# Runner and visualizer (unchanged interface)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(6,6))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect(\"equal\"); ax.grid(True)\n    for i, (c, r) in enumerate(zip(centers, radii)):\n        circ = Circle(c, r, alpha=0.6, edgecolor='k')\n        ax.add_patch(circ)\n        ax.text(c[0], c[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n    plt.title(f\"Hex-lattice Constructor (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # visualize(centers, radii)\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.4390), Performs well on target_ratio (0.9256), Performs well on combined_score (0.9256), Performs well on eval_time (0.0594)\n\n\n\n## Diverse Programs\n\n### Program D1 (Score: 0.7906)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (explicit hex-like layout).\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square using an\n    explicit hexagonal-like layering. Returns (centers, radii, sum_of_radii).\n    \"\"\"\n    n = 26\n\n    # Row counts that sum to 26\n    # A compact block with three dense rows of 6 and two shorter rows helps\n    # utilize interior space while allowing larger border circles.\n    row_counts = [6, 6, 6, 4, 4]\n\n    # choose horizontal spacing s so a 6-wide row fits comfortably in [0,1]\n    s = 1.0 / 6.0\n    v = s * np.sqrt(3) / 2.0  # hexagonal vertical spacing\n\n    centers_list = []\n    # vertical start so block is nicely centered vertically but inside borders\n    total_v_span = (len(row_counts) - 1) * v\n    y_start = 0.5 - total_v_span / 2.0\n\n    for r, m in enumerate(row_counts):\n        # alternate rows offset by s/2 to realize hex packing\n        offset = (s / 2.0) if (r % 2 == 1) else 0.0\n        row_width = (m - 1) * s\n        x_start = 0.5 - row_width / 2.0 + offset\n        y = y_start + r * v\n        for k in range(m):\n            x = x_start + k * s\n            # clip slightly inside to avoid zero border distance numerically\n            x = np.clip(x, 1e-8, 1.0 - 1e-8)\n            yy = np.clip(y, 1e-8, 1.0 - 1e-8)\n            centers_list.append([x, yy])\n            if len(centers_list) >= n:\n                break\n        if len(centers_list) >= n:\n            break\n\n    centers = np.array(centers_list[:n], dtype=float)\n    assert centers.shape == (n, 2)\n\n    radii = compute_max_radii(centers)\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers, tol=1e-8, max_iter=2000):\n    \"\"\"\n    Monotone iterative radius projection:\n    - initialize radii to distance to nearest border\n    - repeatedly resolve any pair overlap by shrinking the larger radius so ri + rj = dist\n      (keeps the smaller radius fixed). Re-enforce border limits each pass.\n    \"\"\"\n    n = centers.shape[0]\n    radii = np.minimum.reduce([centers[:, 0], centers[:, 1],\n                               1.0 - centers[:, 0], 1.0 - centers[:, 1]]).copy()\n\n    for it in range(max_iter):\n        max_change = 0.0\n        # pairwise pass\n        for i in range(n):\n            ci = centers[i]\n            ri = radii[i]\n            for j in range(i + 1, n):\n                cj = centers[j]\n                rj = radii[j]\n                dx = ci[0] - cj[0]\n                dy = ci[1] - cj[1]\n                d = np.hypot(dx, dy)\n                if d <= 1e-12:\n                    # coincident centers: set both to zero to be safe\n                    if ri > 0.0:\n                        max_change = max(max_change, ri)\n                        radii[i] = 0.0\n                        ri = 0.0\n                    if rj > 0.0:\n                        max_change = max(max_change, rj)\n                        radii[j] = 0.0\n                        rj = 0.0\n                    continue\n                if ri + rj > d:\n                    # shrink the larger radius\n                    if ri > rj:\n                        new_ri = max(0.0, d - rj)\n                        change = abs(new_ri - ri)\n                        if change > 0:\n                            radii[i] = new_ri\n                            ri = new_ri\n                            max_change = max(max_change, change)\n                    else:\n                        new_rj = max(0.0, d - ri)\n                        change = abs(new_rj - rj)\n                        if change > 0:\n                            radii[j] = new_rj\n                            rj = new_rj\n                            max_change = max(max_change, change)\n\n        # enforce border limits\n        border_limits = np.minimum.reduce([centers[:, 0], centers[:, 1],\n                                           1.0 - centers[:, 0], 1.0 - centers[:, 1]])\n        over = radii > border_limits\n        if np.any(over):\n            change = np.max(radii[over] - border_limits[over])\n            radii[over] = border_limits[over]\n            max_change = max(max_change, float(change))\n\n        if max_change < tol:\n            break\n\n    radii = np.maximum(radii, 0.0)\n    return radii\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nKey features: Alternative approach to validity, Alternative approach to sum_radii\n\n### Program D2 (Score: 0.7838)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (parameterized layouts + projection)\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    \"\"\"\n    Try a small family of explicit hex-like row patterns and a short 1D sweep of\n    horizontal spacing s to pick the layout that yields the largest sum of maximal\n    radii (centers fixed, radii computed by projection).\n    Returns centers (26,2), radii (26,), sum_radii.\n    \"\"\"\n    n = 26\n    patterns = [\n        [6,5,5,5,5],   # 5 rows\n        [7,6,7,6],     # 4 rows\n        [5,6,5,5,5]    # alternative 5 rows\n    ]\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n    sqrt3 = np.sqrt(3.0)\n\n    # For each pattern, sweep plausible s values\n    for pat in patterns:\n        max_row = max(pat)\n        # choose plausible s range so row fits: s*(max_row-1) <= 0.9 approx leave margin\n        s_min = 0.10\n        s_max = min(0.22, 0.9 / max(1, (max_row - 1)))\n        if s_max <= s_min:\n            s_max = s_min + 0.05\n        for s in np.linspace(s_min, s_max, 36):\n            v = s * sqrt3 / 2.0\n            rows = len(pat)\n            # vertical span of centers\n            v_span = (rows - 1) * v\n            # center block vertically in unit square with small margin\n            y0 = 0.5 - v_span / 2.0\n            centers = []\n            for r, m in enumerate(pat):\n                total_w = (m - 1) * s\n                x_start = 0.5 - total_w / 2.0\n                if r % 2 == 1:\n                    x_start += s / 2.0\n                y = y0 + r * v\n                for k in range(m):\n                    x = x_start + k * s\n                    # clip a touch inside\n                    centers.append([min(max(x, 1e-6), 1.0 - 1e-6), min(max(y, 1e-6), 1.0 - 1e-6)])\n            centers = np.array(centers[:n], dtype=float)\n            if centers.shape[0] != n:\n                continue\n            radii = compute_max_radii(centers)\n            ssum = radii.sum()\n            if ssum > best_sum:\n                best_sum = ssum\n                best_centers = centers.copy()\n                best_radii = radii.copy()\n\n    # Fallback if none found\n    if best_centers is None:\n        best_centers = np.array([[0.5,0.5]] + [[0.5+0.01*i,0.5+0.01*j] for i in range(n-1) for j in range(1)][:n-1])\n        best_radii = compute_max_radii(best_centers)\n        best_sum = float(best_radii.sum())\n\n    return best_centers, best_radii, float(best_sum)\n\ndef compute_max_radii(centers, tol=1e-8, max_iter=2000):\n    \"\"\"\n    Given fixed centers in unit square compute maximal radii by iterative projection:\n    - start from border-limited radii\n    - repeatedly fix pairwise overlaps by shrinking the larger circle of each offending pair\n    \"\"\"\n    n = centers.shape[0]\n    # initial radii limited by distance to border\n    radii = np.minimum.reduce([centers[:,0], centers[:,1], 1.0-centers[:,0], 1.0-centers[:,1]]).copy()\n    # pairwise distances\n    dx = centers[:,0][:,None] - centers[:,0][None,:]\n    dy = centers[:,1][:,None] - centers[:,1][None,:]\n    D = np.sqrt(np.maximum(0.0, dx*dx + dy*dy))\n    # small iterative greedy correction (shrink larger when ri+rj>d)\n    for _ in range(max_iter):\n        max_change = 0.0\n        # find violating pairs\n        I,J = np.triu_indices(n, k=1)\n        dij = D[I,J]\n        rij = radii[I] + radii[J]\n        mask = rij > dij\n        if not np.any(mask):\n            break\n        for i,j,d in zip(I[mask], J[mask], dij[mask]):\n            ri = radii[i]; rj = radii[j]\n            if ri + rj <= d + 1e-12:\n                continue\n            if ri > rj:\n                new_ri = max(0.0, d - rj)\n                ch = abs(new_ri - ri)\n                if ch>0:\n                    radii[i] = new_ri\n                    if ch>max_change: max_change=ch\n            else:\n                new_rj = max(0.0, d - ri)\n                ch = abs(new_rj - rj)\n                if ch>0:\n                    radii[j] = new_rj\n                    if ch>max_change: max_change=ch\n        # enforce border limits\n        border = np.minimum.reduce([centers[:,0], centers[:,1], 1.0-centers[:,0], 1.0-centers[:,1]])\n        over = radii > border\n        if np.any(over):\n            diff = np.max(radii[over] - border[over])\n            radii[over] = border[over]\n            if diff > max_change: max_change = diff\n        if max_change < tol:\n            break\n    radii = np.maximum(radii, 0.0)\n    return radii\n\n# EVOLVE-BLOCK-END\n\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(6,6))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect(\"equal\")\n    for c,r in zip(centers,radii):\n        ax.add_patch(Circle(c,r,alpha=0.6,edgecolor='k'))\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, s = run_packing()\n    print(f\"Sum of radii: {s:.6f}\")\n    # visualize(centers, radii)\n```\nKey features: Alternative approach to validity, Alternative approach to sum_radii\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 0.9256, Type: High-Performer)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles using a hexagonal lattice constructor.\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a deterministic arrangement of 26 circle centers in [0,1]^2\n    using a hexagonal (offset-grid) lattice. We choose the largest lattice\n    spacing s that still produces at least 26 candidate centers, then\n    keep the 26 centers closest to the square center to mitigate edge effects.\n    Radii are set to the maximal non-overlapping values given these centers:\n        r_i = min(dist_to_border(i), 0.5 * min_j dist(center_i, center_j))\n    This yields a valid non-overlapping packing and tends to produce dense,\n    hexagonal-like local arrangements that maximize the sum of radii.\n    Returns:\n        (centers (26,2), radii (26,), sum_of_radii)\n    \"\"\"\n    n_target = 26\n\n    def make_hex_centers(s):\n        # margin so circles can have radius up to s/2 without crossing border\n        margin = s / 2.0\n        pts = []\n        y = margin\n        row = 0\n        y_step = s * np.sqrt(3) / 2.0\n        while y <= 1.0 - margin + 1e-12:\n            x_offset = (row % 2) * (s / 2.0)\n            x = margin + x_offset\n            while x <= 1.0 - margin + 1e-12:\n                pts.append((x, y))\n                x += s\n            row += 1\n            y += y_step\n        return np.array(pts)\n\n    # Binary search for the largest s that still yields at least n_target centers.\n    low, high = 0.01, 0.6  # high > possible spacing\n    best_s = low\n    for _ in range(40):\n        mid = 0.5 * (low + high)\n        pts = make_hex_centers(mid)\n        if pts.shape[0] >= n_target:\n            best_s = mid\n            low = mid  # we can try larger spacing\n        else:\n            high = mid\n    centers_all = make_hex_centers(best_s)\n\n    # If we have more than needed, keep the n_target centers closest to square center\n    if centers_all.shape[0] > n_target:\n        center_of_square = np.array([0.5, 0.5])\n        dists = np.linalg.norm(centers_all - center_of_square, axis=1)\n        idx = np.argsort(dists)[:n_target]\n        centers = centers_all[idx]\n    else:\n        centers = centers_all.copy()\n\n    # As a small enhancement, ensure we always return exactly n_target centers\n    # (should be guaranteed by construction/trimming above)\n    if centers.shape[0] < n_target:\n        # fallback: fill with evenly spaced points along a small spiral near center\n        extra = n_target - centers.shape[0]\n        spiral = []\n        for k in range(extra):\n            t = 0.05 * (k + 1)\n            angle = 2 * np.pi * (k + 1) / extra\n            spiral.append([0.5 + t * np.cos(angle), 0.5 + t * np.sin(angle)])\n        centers = np.vstack([centers, np.array(spiral)])\n\n    # Sort centers for determinism (by x then y)\n    centers = centers[np.lexsort((centers[:, 1], centers[:, 0]))]\n\n    # Compute maximal valid radii:\n    radii = compute_max_radii(centers)\n\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers):\n    \"\"\"\n    For a fixed set of centers, compute a safe maximal radius assignment\n    that guarantees no overlaps and containment in the unit square.\n    We use the conservative but tight rule:\n        r_i = min(dist_to_border(i), 0.5 * min_j dist(i,j))\n    which makes touching allowed but not overlapping.\n    \"\"\"\n    n = centers.shape[0]\n    radii = np.zeros(n)\n    # distance to borders\n    dist_border = np.minimum.reduce([centers[:, 0], centers[:, 1], 1 - centers[:, 0], 1 - centers[:, 1]])\n    # pairwise distances (compute efficiently)\n    diff = centers[:, None, :] - centers[None, :, :]  # n x n x 2\n    dists = np.sqrt(np.sum(diff ** 2, axis=2))\n    # set diagonal to a large number to ignore self-distance\n    np.fill_diagonal(dists, np.inf)\n    min_pair = np.min(dists, axis=1)\n    # radii limited by half the nearest neighbor distance and by borders\n    radii = np.minimum(dist_border, 0.5 * min_pair)\n    # Ensure non-negative and tiny numerical floor\n    radii = np.maximum(radii, 0.0)\n    return radii\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor='k', linewidth=0.5)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Hex-lattice Constructor Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nUnique approach: Modification: Full rewrite, Excellent validity (1.000), Excellent sum_radii (2.439)\n\n### Inspiration 2 (Score: 0.9256, Type: High-Performer)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for n=26 circle packing in the unit square.\nBuild a hexagonal (offset) lattice, pick the 26 centers closest to the square\ncenter for best edge-effect mitigation, and set radii by the tight safe rule\nr_i = min(dist_to_border(i), 0.5 * min_{j!=i} dist(i,j)).\nThis simple constructor yields very compact local hexagonal arrangements\nand produces a high total sum of radii.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n_target = 26\n\n    def hex_centers(s):\n        # margin ensures a circle of radius s/2 around a center stays inside square\n        margin = s / 2.0\n        pts = []\n        y = margin\n        row = 0\n        y_step = s * np.sqrt(3) / 2.0\n        # generate rows while centers remain within [margin, 1-margin]\n        while y <= 1.0 - margin + 1e-12:\n            x_off = (row % 2) * (s / 2.0)\n            x = margin + x_off\n            while x <= 1.0 - margin + 1e-12:\n                pts.append((x, y))\n                x += s\n            row += 1\n            y += y_step\n        return np.array(pts)\n\n    # binary search largest spacing s that yields at least n_target centers\n    lo, hi = 0.01, 0.6\n    best_s = lo\n    for _ in range(40):\n        mid = 0.5 * (lo + hi)\n        pts = hex_centers(mid)\n        if pts.shape[0] >= n_target:\n            best_s = mid\n            lo = mid\n        else:\n            hi = mid\n    candidates = hex_centers(best_s)\n\n    # choose the n_target centers closest to center (mitigate edge effects)\n    if candidates.shape[0] > n_target:\n        center = np.array([0.5, 0.5])\n        d = np.linalg.norm(candidates - center, axis=1)\n        idx = np.argsort(d)[:n_target]\n        centers = candidates[idx]\n    else:\n        centers = candidates.copy()\n\n    # deterministic ordering\n    centers = centers[np.lexsort((centers[:,1], centers[:,0]))]\n\n    # compute maximal safe radii: min(border_dist, 0.5 * nearest_neighbor)\n    dist_border = np.minimum.reduce([centers[:,0], centers[:,1], 1 - centers[:,0], 1 - centers[:,1]])\n    diff = centers[:, None, :] - centers[None, :, :]\n    dists = np.sqrt(np.sum(diff**2, axis=2))\n    np.fill_diagonal(dists, np.inf)\n    min_pair = np.min(dists, axis=1)\n    radii = np.minimum(dist_border, 0.5 * min_pair)\n    radii = np.maximum(radii, 0.0)\n\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n# EVOLVE-BLOCK-END\n\n# Fixed runner and visualizer (kept for compatibility)\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(8,8))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect(\"equal\")\n    for c,r in zip(centers, radii):\n        ax.add_patch(Circle(c, r, alpha=0.6, edgecolor='k'))\n    ax.grid(True)\n    plt.title(f\"n={len(centers)}, sum={sum(radii):.6f}\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, s = run_packing()\n    print(f\"Sum of radii: {s:.6f}\")\n    # visualize(centers, radii)\n```\nUnique approach: Modification: Full rewrite, Excellent validity (1.000), Excellent sum_radii (2.439)\n\n### Inspiration 3 (Score: 0.7375, Type: Alternative)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (explicit symmetric layout)\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Build an explicit symmetric arrangement of 26 circle centers in the unit square\n    and compute maximal non-overlapping radii by iterative projection.\n    Returns (centers, radii, sum_of_radii).\n    \"\"\"\n    n = 26\n    # Symmetric row pattern (outer rows wider, taper to middle): sums to 26\n    row_counts = [6, 5, 4, 5, 6]\n    rows = len(row_counts)\n\n    # Use a dense hex-like horizontal spacing tuned for 6-across rows\n    s = 1.0 / 6.0  # horizontal center-to-center spacing for the widest rows\n    v = s * np.sqrt(3) / 2.0  # vertical spacing for hex packing\n\n    centers = []\n    # center the block within the unit square vertically and horizontally\n    total_height = (rows - 1) * v\n    y0 = 0.5 - total_height / 2.0\n    for r, count in enumerate(row_counts):\n        row_width = (count - 1) * s\n        x_start = 0.5 - row_width / 2.0\n        # alternate rows offset by s/2 to achieve hex-like compactness\n        if r % 2 == 1:\n            x_start += s / 2.0\n        y = y0 + r * v\n        for k in range(count):\n            x = x_start + k * s\n            centers.append([min(max(x, 1e-8), 1.0 - 1e-8), min(max(y, 1e-8), 1.0 - 1e-8)])\n\n    centers = np.array(centers[:n], dtype=float)\n    assert centers.shape == (n, 2)\n\n    radii = _max_radii_for_centers(centers)\n    return centers, radii, float(radii.sum())\n\n\ndef _max_radii_for_centers(centers, tol=1e-8, max_iter=2000):\n    \"\"\"\n    Iteratively enforce constraints:\n      r_i <= distance to border and r_i + r_j <= dist(i,j)\n    by repeatedly shrinking the larger radius in any violating pair.\n    \"\"\"\n    n = centers.shape[0]\n    # initial radii limited by border distances\n    radii = np.minimum.reduce([centers[:, 0], centers[:, 1], 1.0 - centers[:, 0], 1.0 - centers[:, 1]]).astype(float)\n\n    # precompute pairwise distances\n    diffs = centers[:, None, :] - centers[None, :, :]\n    D = np.sqrt(np.maximum(0.0, np.sum(diffs * diffs, axis=2)))\n    # avoid self-zero problems\n    for i in range(n):\n        D[i, i] = 0.0\n\n    for _ in range(max_iter):\n        max_change = 0.0\n        # pairwise pass: shrink the larger radius when ri + rj > dij\n        for i in range(n):\n            ci = centers[i]\n            ri = radii[i]\n            for j in range(i + 1, n):\n                dij = D[i, j]\n                if dij <= 1e-12:\n                    # nearly coincident centers -> collapse both\n                    if ri > 0 or radii[j] > 0:\n                        max_change = max(max_change, ri, radii[j])\n                    radii[i] = 0.0\n                    radii[j] = 0.0\n                    ri = radii[i]\n                    continue\n                rj = radii[j]\n                if ri + rj > dij:\n                    # keep the smaller one, reduce the larger so sum == dij\n                    if ri >= rj:\n                        new_ri = max(0.0, dij - rj)\n                        change = abs(new_ri - ri)\n                        if change > 0:\n                            radii[i] = new_ri\n                            ri = new_ri\n                            max_change = max(max_change, change)\n                    else:\n                        new_rj = max(0.0, dij - ri)\n                        change = abs(new_rj - rj)\n                        if change > 0:\n                            radii[j] = new_rj\n                            max_change = max(max_change, change)\n        # re-enforce border constraints\n        border_limits = np.minimum.reduce([centers[:, 0], centers[:, 1], 1.0 - centers[:, 0], 1.0 - centers[:, 1]])\n        over = radii > border_limits\n        if np.any(over):\n            change = np.max(radii[over] - border_limits[over])\n            radii[over] = border_limits[over]\n            max_change = max(max_change, float(change))\n        if max_change < tol:\n            break\n\n    radii = np.maximum(radii, 0.0)\n    return radii\n\n\n# EVOLVE-BLOCK-END\n\n\n# Runner (unchanged interface)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"Optional visualizer for debugging.\"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(6, 6))\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    for c, r in zip(centers, radii):\n        ax.add_patch(Circle(c, r, alpha=0.6, edgecolor=\"k\"))\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, s = run_packing()\n    print(f\"Sum of radii: {s:.6f}\")\n```\nUnique approach: Modification: Full rewrite, Excellent validity (1.000), Excellent sum_radii (1.943)\n\n# Current Program\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (explicit hex-like layout).\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square using an\n    explicit hexagonal-like layering. Returns (centers, radii, sum_of_radii).\n    \"\"\"\n    n = 26\n\n    # Row counts that sum to 26\n    # A compact block with three dense rows of 6 and two shorter rows helps\n    # utilize interior space while allowing larger border circles.\n    row_counts = [6, 6, 6, 4, 4]\n\n    # choose horizontal spacing s so a 6-wide row fits comfortably in [0,1]\n    s = 1.0 / 6.0\n    v = s * np.sqrt(3) / 2.0  # hexagonal vertical spacing\n\n    centers_list = []\n    # vertical start so block is nicely centered vertically but inside borders\n    total_v_span = (len(row_counts) - 1) * v\n    y_start = 0.5 - total_v_span / 2.0\n\n    for r, m in enumerate(row_counts):\n        # alternate rows offset by s/2 to realize hex packing\n        offset = (s / 2.0) if (r % 2 == 1) else 0.0\n        row_width = (m - 1) * s\n        x_start = 0.5 - row_width / 2.0 + offset\n        y = y_start + r * v\n        for k in range(m):\n            x = x_start + k * s\n            # clip slightly inside to avoid zero border distance numerically\n            x = np.clip(x, 1e-8, 1.0 - 1e-8)\n            yy = np.clip(y, 1e-8, 1.0 - 1e-8)\n            centers_list.append([x, yy])\n            if len(centers_list) >= n:\n                break\n        if len(centers_list) >= n:\n            break\n\n    centers = np.array(centers_list[:n], dtype=float)\n    assert centers.shape == (n, 2)\n\n    radii = compute_max_radii(centers)\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers, tol=1e-8, max_iter=2000):\n    \"\"\"\n    Monotone iterative radius projection:\n    - initialize radii to distance to nearest border\n    - repeatedly resolve any pair overlap by shrinking the larger radius so ri + rj = dist\n      (keeps the smaller radius fixed). Re-enforce border limits each pass.\n    \"\"\"\n    n = centers.shape[0]\n    radii = np.minimum.reduce([centers[:, 0], centers[:, 1],\n                               1.0 - centers[:, 0], 1.0 - centers[:, 1]]).copy()\n\n    for it in range(max_iter):\n        max_change = 0.0\n        # pairwise pass\n        for i in range(n):\n            ci = centers[i]\n            ri = radii[i]\n            for j in range(i + 1, n):\n                cj = centers[j]\n                rj = radii[j]\n                dx = ci[0] - cj[0]\n                dy = ci[1] - cj[1]\n                d = np.hypot(dx, dy)\n                if d <= 1e-12:\n                    # coincident centers: set both to zero to be safe\n                    if ri > 0.0:\n                        max_change = max(max_change, ri)\n                        radii[i] = 0.0\n                        ri = 0.0\n                    if rj > 0.0:\n                        max_change = max(max_change, rj)\n                        radii[j] = 0.0\n                        rj = 0.0\n                    continue\n                if ri + rj > d:\n                    # shrink the larger radius\n                    if ri > rj:\n                        new_ri = max(0.0, d - rj)\n                        change = abs(new_ri - ri)\n                        if change > 0:\n                            radii[i] = new_ri\n                            ri = new_ri\n                            max_change = max(max_change, change)\n                    else:\n                        new_rj = max(0.0, d - ri)\n                        change = abs(new_rj - rj)\n                        if change > 0:\n                            radii[j] = new_rj\n                            rj = new_rj\n                            max_change = max(max_change, change)\n\n        # enforce border limits\n        border_limits = np.minimum.reduce([centers[:, 0], centers[:, 1],\n                                           1.0 - centers[:, 0], 1.0 - centers[:, 1]])\n        over = radii > border_limits\n        if np.any(over):\n            change = np.max(radii[over] - border_limits[over])\n            radii[over] = border_limits[over]\n            max_change = max(max_change, float(change))\n\n        if max_change < tol:\n            break\n\n    radii = np.maximum(radii, 0.0)\n    return radii\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\n\n# Task\nRewrite the program to improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: complexity, diversity\nDifferent solutions with similar fitness but different features are valuable.\nProvide the complete new program code.\n\nIMPORTANT: Make sure your rewritten program maintains the same inputs and outputs\nas the original program, but with improved internal implementation.\n\n```python\n# Your rewritten program here\n```", "responses": ["```python\n# EVOLVE-BLOCK-START\n\"\"\"Improved deterministic constructor for n=26 circle packing in the unit square.\n\nApproach:\n- Build many hexagonal (offset) lattice candidates by sweeping spacing s and small\n  lattice offsets. For each candidate with at least 26 centers, keep the 26\n  centers closest to the square center (mitigates edge effects).\n- For fixed centers compute a guaranteed non-overlapping radius assignment using\n  the tight conservative formula:\n      r_i = min(distance_to_border(i), 0.5 * min_{j!=i} dist(center_i, center_j))\n  (This is a fast, reliable upper bound for fixed centers; choosing the best\n  candidate lattice usually gives very good total radius sums.)\n- Return the best candidate found (highest sum of radii). Deterministic.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n_target = 26\n    center_sq = np.array([0.5, 0.5])\n\n    def make_hex_centers(s, x_offset=0.0, y_offset=0.0):\n        \"\"\"Generate hex-lattice centers with horizontal spacing s and offsets.\n        The lattice generation ensures centers lie in [0,1]^2.\n        \"\"\"\n        margin = 1e-12\n        y_step = s * np.sqrt(3.0) / 2.0\n        pts = []\n        # start y so that adding y_offset recenters possibilities deterministically\n        y = y_offset\n        # generate rows while inside [0,1]\n        row = 0\n        while y <= 1.0 + margin:\n            x_off = (row % 2) * (s / 2.0) + x_offset\n            x = x_off\n            while x <= 1.0 + margin:\n                if 0.0 <= x <= 1.0 and 0.0 <= y <= 1.0:\n                    pts.append((x, y))\n                x += s\n            row += 1\n            y += y_step\n        return np.array(pts, dtype=float)\n\n    def radii_from_centers(centers):\n        \"\"\"Fast conservative max radii for fixed centers.\"\"\"\n        # distance to border\n        db = np.minimum.reduce([centers[:,0], centers[:,1], 1.0 - centers[:,0], 1.0 - centers[:,1]])\n        # pairwise distances\n        diff = centers[:, None, :] - centers[None, :, :]\n        dists = np.sqrt(np.sum(diff**2, axis=2))\n        np.fill_diagonal(dists, np.inf)\n        min_pair = np.min(dists, axis=1)\n        radii = np.minimum(db, 0.5 * min_pair)\n        radii = np.maximum(radii, 0.0)\n        return radii\n\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    # Sweep plausible spacings. Range chosen to allow fairly dense hex blocks\n    s_vals = np.linspace(0.09, 0.25, 48)  # denser sampling\n    # Small deterministic offsets (three values each) to shift lattice relative to square\n    # improves chance to align a dense central patch.\n    offs = [0.0, 0.25, 0.5]\n\n    for s in s_vals:\n        v = s * np.sqrt(3.0) / 2.0\n        # choose x,y offsets in [0,s) and [0,v) via scaled offs\n        for xo in offs:\n            for yo in offs:\n                x_offset = (xo * s) % s\n                y_offset = (yo * v) % v\n                pts = make_hex_centers(s, x_offset, y_offset)\n                if pts.shape[0] < n_target:\n                    continue\n                # pick the n_target centers closest to the square center\n                d = np.linalg.norm(pts - center_sq, axis=1)\n                idx = np.argsort(d)[:n_target]\n                centers = pts[idx]\n                # sort deterministically\n                centers = centers[np.lexsort((centers[:,1], centers[:,0]))]\n                radii = radii_from_centers(centers)\n                ssum = float(np.sum(radii))\n                if ssum > best_sum + 1e-12:\n                    best_sum = ssum\n                    best_centers = centers.copy()\n                    best_radii = radii.copy()\n\n    # Fallback: if no lattice candidate produced 26 centers (unlikely), use a small spiral\n    if best_centers is None:\n        centers = []\n        for k in range(n_target):\n            t = 0.02 * (k + 1)\n            ang = 2.0 * np.pi * k / max(1, n_target)\n            centers.append([0.5 + t * np.cos(ang), 0.5 + t * np.sin(ang)])\n        centers = np.array(centers, dtype=float)\n        centers = np.clip(centers, 1e-12, 1.0 - 1e-12)\n        best_centers = centers\n        best_radii = radii_from_centers(centers)\n        best_sum = float(np.sum(best_radii))\n\n    return best_centers, best_radii, float(best_sum)\n# EVOLVE-BLOCK-END\n\n# Runner and visualizer (unchanged interface)\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(7,7))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect(\"equal\")\n    for c,r in zip(centers, radii):\n        ax.add_patch(Circle(c, r, alpha=0.6, edgecolor='k'))\n    ax.grid(True)\n    plt.title(f\"n={len(centers)}, sum={sum(radii):.6f}\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, s = run_packing()\n    print(f\"Sum of radii: {s:.6f}\")\n    # visualize(centers, radii)\n```"]}}, "artifacts_json": "{\"stdout\": \"Running in subprocess, Python version: 3.12.1 (v3.12.1:2305ca5144, Dec  7 2023, 17:23:38) [Clang 13.0.0 (clang-1300.0.29.30)]\\nProgram path: /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmpeukqhrqs.py\\nCalling run_packing()...\\nrun_packing() returned successfully: sum_radii = 2.3774993190925944\\nResults saved to /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmpfnu7pbs3.py.results\\n\", \"stderr\": \"\"}", "artifact_dir": null}