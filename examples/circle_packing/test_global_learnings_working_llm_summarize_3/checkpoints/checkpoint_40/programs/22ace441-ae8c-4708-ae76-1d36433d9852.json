{"id": "22ace441-ae8c-4708-ae76-1d36433d9852", "code": "# EVOLVE-BLOCK-START\n\"\"\"Improved deterministic constructor for n=26 circle packing in the unit square.\n\nStrategy:\n- Build hexagonal (offset) lattices with the largest feasible spacing s that\n  yields candidate centers.\n- For several phase offsets (shifting the lattice within one cell) and a few\n  nearby spacings, take the 26 centers closest to the square center (reducing\n  edge effects), compute tight safe radii r_i = min(dist_to_border, 0.5*min_j dist(i,j))\n  and keep the configuration with the largest sum of radii found.\nThis explicit (non-iterative) constructor explores a small, deterministic family\nof hex-like placements and picks the best, yielding dense center packings\nand improved total radius sum.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n_target = 26\n\n    def make_hex_centers(s, ox=0.0, oy=0.0):\n        # margin so a circle of radius up to s/2 around a center stays inside\n        margin = s * 0.5\n        pts = []\n        y = margin + oy\n        y_step = s * np.sqrt(3.0) / 2.0\n        row = 0\n        eps = 1e-12\n        while y <= 1.0 - margin + eps:\n            x_offset = (row % 2) * (s / 2.0) + ox\n            x = margin + x_offset\n            while x <= 1.0 - margin + eps:\n                if 0.0 <= x <= 1.0 and 0.0 <= y <= 1.0:\n                    pts.append((x, y))\n                x += s\n            row += 1\n            y += y_step\n        return np.array(pts, dtype=float)\n\n    # binary search largest s that yields at least n_target centers (default phase)\n    low, high = 0.01, 0.6\n    best_s = low\n    for _ in range(40):\n        mid = 0.5 * (low + high)\n        if make_hex_centers(mid).shape[0] >= n_target:\n            best_s = mid\n            low = mid\n        else:\n            high = mid\n\n    # explore a small grid of offsets within one lattice cell and a few nearby s-values\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    s_candidates = [best_s * 0.98, best_s, best_s * 1.02]\n    # ensure candidates are within sensible bounds\n    s_candidates = [max(0.005, min(0.6, sc)) for sc in s_candidates]\n\n    # sample deterministic offsets (5x5 grid) within [0, s)\n    offs = np.linspace(0.0, 1.0, 5, endpoint=False)  # fraction of s\n    for s_val in s_candidates:\n        for fx in offs:\n            for fy in offs:\n                ox = fx * s_val\n                oy = fy * s_val * np.sqrt(3.0) / 2.0  # align vertical phase\n                pts = make_hex_centers(s_val, ox=ox, oy=oy)\n                if pts.shape[0] < n_target:\n                    continue\n                # pick n_target centers closest to square center\n                center_sq = np.array([0.5, 0.5])\n                d = np.linalg.norm(pts - center_sq, axis=1)\n                idx = np.argsort(d)[:n_target]\n                centers = pts[idx]\n\n                # compute tight safe radii vectorized\n                dist_border = np.minimum.reduce([centers[:,0], centers[:,1], 1.0-centers[:,0], 1.0-centers[:,1]])\n                diff = centers[:, None, :] - centers[None, :, :]\n                dists = np.sqrt(np.maximum(0.0, (diff**2).sum(axis=2)))\n                np.fill_diagonal(dists, np.inf)\n                min_pair = np.min(dists, axis=1)\n                radii = np.minimum(dist_border, 0.5 * min_pair)\n                radii = np.maximum(radii, 0.0)\n                ssum = float(np.sum(radii))\n                if ssum > best_sum + 1e-12:\n                    best_sum = ssum\n                    best_centers = centers.copy()\n                    best_radii = radii.copy()\n\n    # Fallback: if nothing found (very unlikely), build the default lattice and trim\n    if best_centers is None:\n        pts = make_hex_centers(best_s)\n        center_sq = np.array([0.5, 0.5])\n        d = np.linalg.norm(pts - center_sq, axis=1)\n        idx = np.argsort(d)[:n_target]\n        best_centers = pts[idx]\n        dist_border = np.minimum.reduce([best_centers[:,0], best_centers[:,1], 1.0-best_centers[:,0], 1.0-best_centers[:,1]])\n        diff = best_centers[:, None, :] - best_centers[None, :, :]\n        dists = np.sqrt(np.maximum(0.0, (diff**2).sum(axis=2)))\n        np.fill_diagonal(dists, np.inf)\n        min_pair = np.min(dists, axis=1)\n        best_radii = np.minimum(dist_border, 0.5 * min_pair)\n        best_radii = np.maximum(best_radii, 0.0)\n        best_sum = float(np.sum(best_radii))\n\n    # deterministic ordering\n    order = np.lexsort((best_centers[:,1], best_centers[:,0]))\n    best_centers = best_centers[order]\n    best_radii = best_radii[order]\n\n    return best_centers, best_radii, float(np.sum(best_radii))\n\n\n# EVOLVE-BLOCK-END\n\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(6,6))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect('equal')\n    for i,(c,r) in enumerate(zip(centers,radii)):\n        ax.add_patch(Circle(c,r,alpha=0.6,edgecolor='k'))\n        ax.text(c[0], c[1], str(i), ha='center', va='center', fontsize=8)\n    plt.title(f\"n={len(centers)}, sum={sum(radii):.6f}\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, s = run_packing()\n    print(f\"Sum of radii: {s:.6f}\")\n    # visualize(centers, radii)", "language": "python", "parent_id": "69119e78-9a92-4015-911d-caa5b3d198f7", "generation": 2, "timestamp": 1759959037.683876, "iteration_found": 28, "metrics": {"validity": 1.0, "sum_radii": 2.4389662673793744, "target_ratio": 0.925603896538662, "combined_score": 0.925603896538662, "eval_time": 0.057813167572021484}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Full rewrite", "parent_metrics": {"validity": 1.0, "sum_radii": 1.9433756729740652, "target_ratio": 0.7375239745632126, "combined_score": 0.7375239745632126, "eval_time": 0.057639122009277344}, "island": 0}, "prompts": {"full_rewrite_user": {"system": "You are an expert mathematician specializing in circle packing problems and computational geometry. Your task is to improve a constructor function that directly produces a specific arrangement of 26 circles in a unit square, maximizing the sum of their radii. The AlphaEvolve paper achieved a sum of 2.635 for n=26.\n\nKey geometric insights:\n- Circle packings often follow hexagonal patterns in the densest regions\n- Maximum density for infinite circle packing is pi/(2*sqrt(3)) \u2248 0.9069\n- Edge effects make square container packing harder than infinite packing\n- Circles can be placed in layers or shells when confined to a square\n- Similar radius circles often form regular patterns, while varied radii allow better space utilization\n- Perfect symmetry may not yield the optimal packing due to edge effects\n\nFocus on designing an explicit constructor that places each circle in a specific position, rather than an iterative search algorithm.\n\n\n## Evolution Insights (Global Learnings)\n\n### Common Pitfalls:\n\u26a0\ufe0f eval_time decreased by 42.8% (0.108 \u2192 0.062) (seen 2x)\n\u26a0\ufe0f validity decreased by 100.0% (1.000 \u2192 0.000) (seen 1x)\n\u26a0\ufe0f sum_radii decreased by 100.0% (2.439 \u2192 0.000) (seen 1x)\n\u26a0\ufe0f target_ratio decreased by 100.0% (0.926 \u2192 0.000) (seen 1x)\n\u26a0\ufe0f combined_score decreased by 100.0% (0.926 \u2192 0.000) (seen 1x)", "user": "# Current Program Information\n- Fitness: 0.7375\n- Feature coordinates: No feature coordinates\n- Focus areas: - Fitness declined: 0.9256 \u2192 0.7375. Consider revising recent changes.\n- Consider simplifying - code length exceeds 500 characters\n\n## Last Execution Output\n\n### stdout\n```\nRunning in subprocess, Python version: 3.12.1 (v3.12.1:2305ca5144, Dec  7 2023, 17:23:38) [Clang 13.0.0 (clang-1300.0.29.30)]\nProgram path: /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmp9dbd_mw_.py\nCalling run_packing()...\nrun_packing() returned successfully: sum_radii = 1.9433756729740652\nResults saved to /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmp4c76bxhq.py.results\n\n```\n\n### stderr\n```\n\n```\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 3\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.4390, target_ratio: 0.9256, combined_score: 0.9256, eval_time: 0.0597\n- Outcome: Mixed results\n\n### Attempt 2\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.4390, target_ratio: 0.9256, combined_score: 0.9256, eval_time: 0.0598\n- Outcome: Mixed results\n\n### Attempt 1\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.4390, target_ratio: 0.9256, combined_score: 0.9256, eval_time: 0.0571\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.9256)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles using a hexagonal lattice constructor.\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a deterministic arrangement of 26 circle centers in [0,1]^2\n    using a hexagonal (offset-grid) lattice. We choose the largest lattice\n    spacing s that still produces at least 26 candidate centers, then\n    keep the 26 centers closest to the square center to mitigate edge effects.\n    Radii are set to the maximal non-overlapping values given these centers:\n        r_i = min(dist_to_border(i), 0.5 * min_j dist(center_i, center_j))\n    This yields a valid non-overlapping packing and tends to produce dense,\n    hexagonal-like local arrangements that maximize the sum of radii.\n    Returns:\n        (centers (26,2), radii (26,), sum_of_radii)\n    \"\"\"\n    n_target = 26\n\n    def make_hex_centers(s):\n        # margin so circles can have radius up to s/2 without crossing border\n        margin = s / 2.0\n        pts = []\n        y = margin\n        row = 0\n        y_step = s * np.sqrt(3) / 2.0\n        while y <= 1.0 - margin + 1e-12:\n            x_offset = (row % 2) * (s / 2.0)\n            x = margin + x_offset\n            while x <= 1.0 - margin + 1e-12:\n                pts.append((x, y))\n                x += s\n            row += 1\n            y += y_step\n        return np.array(pts)\n\n    # Binary search for the largest s that still yields at least n_target centers.\n    low, high = 0.01, 0.6  # high > possible spacing\n    best_s = low\n    for _ in range(40):\n        mid = 0.5 * (low + high)\n        pts = make_hex_centers(mid)\n        if pts.shape[0] >= n_target:\n            best_s = mid\n            low = mid  # we can try larger spacing\n        else:\n            high = mid\n    centers_all = make_hex_centers(best_s)\n\n    # If we have more than needed, keep the n_target centers closest to square center\n    if centers_all.shape[0] > n_target:\n        center_of_square = np.array([0.5, 0.5])\n        dists = np.linalg.norm(centers_all - center_of_square, axis=1)\n        idx = np.argsort(dists)[:n_target]\n        centers = centers_all[idx]\n    else:\n        centers = centers_all.copy()\n\n    # As a small enhancement, ensure we always return exactly n_target centers\n    # (should be guaranteed by construction/trimming above)\n    if centers.shape[0] < n_target:\n        # fallback: fill with evenly spaced points along a small spiral near center\n        extra = n_target - centers.shape[0]\n        spiral = []\n        for k in range(extra):\n            t = 0.05 * (k + 1)\n            angle = 2 * np.pi * (k + 1) / extra\n            spiral.append([0.5 + t * np.cos(angle), 0.5 + t * np.sin(angle)])\n        centers = np.vstack([centers, np.array(spiral)])\n\n    # Sort centers for determinism (by x then y)\n    centers = centers[np.lexsort((centers[:, 1], centers[:, 0]))]\n\n    # Compute maximal valid radii:\n    radii = compute_max_radii(centers)\n\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers):\n    \"\"\"\n    For a fixed set of centers, compute a safe maximal radius assignment\n    that guarantees no overlaps and containment in the unit square.\n    We use the conservative but tight rule:\n        r_i = min(dist_to_border(i), 0.5 * min_j dist(i,j))\n    which makes touching allowed but not overlapping.\n    \"\"\"\n    n = centers.shape[0]\n    radii = np.zeros(n)\n    # distance to borders\n    dist_border = np.minimum.reduce([centers[:, 0], centers[:, 1], 1 - centers[:, 0], 1 - centers[:, 1]])\n    # pairwise distances (compute efficiently)\n    diff = centers[:, None, :] - centers[None, :, :]  # n x n x 2\n    dists = np.sqrt(np.sum(diff ** 2, axis=2))\n    # set diagonal to a large number to ignore self-distance\n    np.fill_diagonal(dists, np.inf)\n    min_pair = np.min(dists, axis=1)\n    # radii limited by half the nearest neighbor distance and by borders\n    radii = np.minimum(dist_border, 0.5 * min_pair)\n    # Ensure non-negative and tiny numerical floor\n    radii = np.maximum(radii, 0.0)\n    return radii\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor='k', linewidth=0.5)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Hex-lattice Constructor Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.4390), Performs well on target_ratio (0.9256), Performs well on combined_score (0.9256), Performs well on eval_time (0.0571)\n\n### Program 2 (Score: 0.9256)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for n=26 circle packing in the unit square.\nBuild a hexagonal (offset) lattice with the largest feasible spacing that\nyields at least 26 candidate centers, keep the 26 closest to the square\ncenter (reduces edge effects), and set radii to the tight safe values:\n    r_i = min(dist_to_border(i), 0.5 * min_{j!=i} dist(center_i, center_j))\nThis simple explicit constructor yields dense, hex-like local packing\nand produces a high sum of radii without iterative search.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n_target = 26\n\n    def make_hex_centers(s):\n        # margin so centers are at least s/2 from borders (allow radius up to s/2)\n        margin = s * 0.5\n        pts = []\n        y = margin\n        y_step = s * np.sqrt(3.0) / 2.0\n        row = 0\n        while y <= 1.0 - margin + 1e-12:\n            x_offset = (row % 2) * (s / 2.0)\n            x = margin + x_offset\n            while x <= 1.0 - margin + 1e-12:\n                pts.append((x, y))\n                x += s\n            row += 1\n            y += y_step\n        return np.array(pts, dtype=float)\n\n    # Binary search largest spacing s that still produces >= n_target centers\n    low, high = 0.01, 0.6\n    best_s = low\n    for _ in range(40):\n        mid = 0.5 * (low + high)\n        pts = make_hex_centers(mid)\n        if pts.shape[0] >= n_target:\n            best_s = mid\n            low = mid\n        else:\n            high = mid\n    centers_all = make_hex_centers(best_s)\n\n    # Pick the n_target centers closest to the square center (reduce edge effects)\n    if centers_all.shape[0] > n_target:\n        center_sq = np.array([0.5, 0.5])\n        d = np.linalg.norm(centers_all - center_sq, axis=1)\n        idx = np.argsort(d)[:n_target]\n        centers = centers_all[idx]\n    else:\n        centers = centers_all.copy()\n\n    # Ensure deterministic ordering\n    centers = centers[np.lexsort((centers[:,1], centers[:,0]))]\n\n    # Compute safe maximal radii for fixed centers\n    radii = compute_max_radii(centers)\n    return centers, radii, float(np.sum(radii))\n\ndef compute_max_radii(centers):\n    # r_i <= distance to border\n    dist_border = np.minimum.reduce([centers[:,0], centers[:,1], 1.0 - centers[:,0], 1.0 - centers[:,1]])\n    # pairwise distances\n    diff = centers[:, None, :] - centers[None, :, :]\n    dists = np.sqrt(np.sum(diff**2, axis=2))\n    np.fill_diagonal(dists, np.inf)\n    min_pair = np.min(dists, axis=1)\n    radii = np.minimum(dist_border, 0.5 * min_pair)\n    radii = np.maximum(radii, 0.0)\n    return radii\n\n# EVOLVE-BLOCK-END\n\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(6,6))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect(\"equal\")\n    for i,(c,r) in enumerate(zip(centers,radii)):\n        ax.add_patch(Circle(c,r,alpha=0.6, edgecolor='k'))\n        ax.text(c[0], c[1], str(i), ha='center', va='center', fontsize=8)\n    plt.title(f\"n={len(centers)}, sum={sum(radii):.6f}\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, s = run_packing()\n    print(f\"Sum of radii: {s:.6f}\")\n    # visualize(centers, radii)\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.4390), Performs well on target_ratio (0.9256), Performs well on combined_score (0.9256), Performs well on eval_time (0.0598)\n\n### Program 3 (Score: 0.9256)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for n=26 circle packing in the unit square.\nBuild a hexagonal (offset) lattice, pick the 26 centers closest to the square\ncenter for best edge-effect mitigation, and set radii by the tight safe rule\nr_i = min(dist_to_border(i), 0.5 * min_{j!=i} dist(i,j)).\nThis simple constructor yields very compact local hexagonal arrangements\nand produces a high total sum of radii.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n_target = 26\n\n    def hex_centers(s):\n        # margin ensures a circle of radius s/2 around a center stays inside square\n        margin = s / 2.0\n        pts = []\n        y = margin\n        row = 0\n        y_step = s * np.sqrt(3) / 2.0\n        # generate rows while centers remain within [margin, 1-margin]\n        while y <= 1.0 - margin + 1e-12:\n            x_off = (row % 2) * (s / 2.0)\n            x = margin + x_off\n            while x <= 1.0 - margin + 1e-12:\n                pts.append((x, y))\n                x += s\n            row += 1\n            y += y_step\n        return np.array(pts)\n\n    # binary search largest spacing s that yields at least n_target centers\n    lo, hi = 0.01, 0.6\n    best_s = lo\n    for _ in range(40):\n        mid = 0.5 * (lo + hi)\n        pts = hex_centers(mid)\n        if pts.shape[0] >= n_target:\n            best_s = mid\n            lo = mid\n        else:\n            hi = mid\n    candidates = hex_centers(best_s)\n\n    # choose the n_target centers closest to center (mitigate edge effects)\n    if candidates.shape[0] > n_target:\n        center = np.array([0.5, 0.5])\n        d = np.linalg.norm(candidates - center, axis=1)\n        idx = np.argsort(d)[:n_target]\n        centers = candidates[idx]\n    else:\n        centers = candidates.copy()\n\n    # deterministic ordering\n    centers = centers[np.lexsort((centers[:,1], centers[:,0]))]\n\n    # compute maximal safe radii: min(border_dist, 0.5 * nearest_neighbor)\n    dist_border = np.minimum.reduce([centers[:,0], centers[:,1], 1 - centers[:,0], 1 - centers[:,1]])\n    diff = centers[:, None, :] - centers[None, :, :]\n    dists = np.sqrt(np.sum(diff**2, axis=2))\n    np.fill_diagonal(dists, np.inf)\n    min_pair = np.min(dists, axis=1)\n    radii = np.minimum(dist_border, 0.5 * min_pair)\n    radii = np.maximum(radii, 0.0)\n\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n# EVOLVE-BLOCK-END\n\n# Fixed runner and visualizer (kept for compatibility)\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(8,8))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect(\"equal\")\n    for c,r in zip(centers, radii):\n        ax.add_patch(Circle(c, r, alpha=0.6, edgecolor='k'))\n    ax.grid(True)\n    plt.title(f\"n={len(centers)}, sum={sum(radii):.6f}\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, s = run_packing()\n    print(f\"Sum of radii: {s:.6f}\")\n    # visualize(centers, radii)\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.4390), Performs well on target_ratio (0.9256), Performs well on combined_score (0.9256), Performs well on eval_time (0.0597)\n\n\n\n## Diverse Programs\n\n### Program D1 (Score: 0.9256)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 using a centered hexagonal-lattice constructor.\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    \"\"\"\n    Deterministic constructor that builds a hexagonal (offset-grid) lattice of candidate\n    centers, chooses the 26 centers closest to the square center (to reduce edge effects),\n    and assigns each circle the tightest safe radius:\n        r_i = min(distance_to_border(i), 0.5 * min_{j!=i} dist(center_i, center_j))\n    This simple, explicit constructor tends to produce dense, near-hexagonal packings\n    in the middle of the square and gives a high sum of radii for n=26.\n    Returns: (centers (26,2), radii (26,), sum_of_radii)\n    \"\"\"\n    n_target = 26\n\n    def make_hex_centers(s):\n        # margin ensures centers are at least s/2 from border so circles of radius s/2 fit\n        margin = s * 0.5\n        pts = []\n        y = margin\n        row = 0\n        y_step = s * np.sqrt(3) / 2.0\n        # generate rows until we exceed the unit square\n        while y <= 1.0 - margin + 1e-12:\n            x_offset = (row % 2) * (s / 2.0)\n            x = margin + x_offset\n            while x <= 1.0 - margin + 1e-12:\n                pts.append((x, y))\n                x += s\n            row += 1\n            y += y_step\n        return np.array(pts)\n\n    # Binary search for the largest spacing s that yields at least n_target centers.\n    low, high = 0.01, 0.6\n    best_s = low\n    for _ in range(45):\n        mid = 0.5 * (low + high)\n        pts = make_hex_centers(mid)\n        if pts.shape[0] >= n_target:\n            best_s = mid\n            low = mid\n        else:\n            high = mid\n    centers_all = make_hex_centers(best_s)\n\n    # If more centers than needed, keep those closest to square center\n    if centers_all.shape[0] > n_target:\n        center_sq = np.array([0.5, 0.5])\n        dists = np.linalg.norm(centers_all - center_sq, axis=1)\n        idx = np.argsort(dists)[:n_target]\n        centers = centers_all[idx]\n    else:\n        # If exactly or fewer (rare), pad/fill deterministically near center\n        centers = centers_all.copy()\n        if centers.shape[0] < n_target:\n            extra = n_target - centers.shape[0]\n            # deterministic small spiral placements\n            for k in range(extra):\n                t = 0.04 * (k + 1)\n                ang = 2 * np.pi * (k + 1) / extra\n                centers = np.vstack([centers, [0.5 + t * np.cos(ang), 0.5 + t * np.sin(ang)]])\n    # Sort for determinism (by x then y)\n    centers = centers[np.lexsort((centers[:,1], centers[:,0]))][:n_target]\n\n    # Compute maximal safe radii: limited by border and half nearest-neighbor distance\n    def compute_max_radii(centers):\n        n = centers.shape[0]\n        # distance to border\n        dist_border = np.minimum.reduce([centers[:,0], centers[:,1], 1.0 - centers[:,0], 1.0 - centers[:,1]])\n        # pairwise distances\n        diff = centers[:, None, :] - centers[None, :, :]  # n x n x 2\n        dists = np.sqrt(np.sum(diff**2, axis=2))\n        np.fill_diagonal(dists, np.inf)\n        min_pair = np.min(dists, axis=1)\n        radii = np.minimum(dist_border, 0.5 * min_pair)\n        radii = np.maximum(radii, 0.0)\n        return radii\n\n    radii = compute_max_radii(centers)\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n# EVOLVE-BLOCK-END\n\n# Runner and visualizer (unchanged interface)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(6,6))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect(\"equal\"); ax.grid(True)\n    for i, (c, r) in enumerate(zip(centers, radii)):\n        circ = Circle(c, r, alpha=0.6, edgecolor='k')\n        ax.add_patch(circ)\n        ax.text(c[0], c[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n    plt.title(f\"Hex-lattice Constructor (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # visualize(centers, radii)\n```\nKey features: Alternative approach to validity, Alternative approach to sum_radii\n\n### Program D2 (Score: 0.9256)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for n=26 circle packing in the unit square.\nUses a hexagonal (offset) lattice: choose the largest lattice spacing s that\nstill yields at least 26 candidate centers, keep the 26 centers closest to\nthe square center (mitigating edge effects) and assign radii by the tight\nlocal rule r_i = min(dist_to_border(i), 0.5*min_j dist(i,j)).\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n\n    def make_hex_centers(s):\n        eps = 1e-12\n        margin = s / 2.0\n        pts = []\n        y = margin\n        row = 0\n        y_step = s * np.sqrt(3) / 2.0\n        while y <= 1.0 - margin + eps:\n            x_offset = (row % 2) * (s / 2.0)\n            x = margin + x_offset\n            while x <= 1.0 - margin + eps:\n                pts.append((x, y))\n                x += s\n            row += 1\n            y += y_step\n        return np.array(pts, dtype=float)\n\n    # binary search for largest spacing producing >= n centers\n    low, high = 0.01, 0.6\n    best_s = low\n    for _ in range(40):\n        mid = 0.5 * (low + high)\n        pts = make_hex_centers(mid)\n        if pts.shape[0] >= n:\n            best_s = mid\n            low = mid\n        else:\n            high = mid\n\n    centers_all = make_hex_centers(best_s)\n    if centers_all.shape[0] > n:\n        center_sq = np.array([0.5, 0.5])\n        d = np.linalg.norm(centers_all - center_sq, axis=1)\n        idx = np.argsort(d)[:n]\n        centers = centers_all[idx]\n    else:\n        centers = centers_all.copy()\n\n    # deterministic ordering\n    centers = centers[np.lexsort((centers[:,1], centers[:,0]))]\n\n    radii = compute_max_radii(centers)\n    return centers, radii, float(radii.sum())\n\ndef compute_max_radii(centers):\n    \"\"\"Vectorized tight local rule: r_i = min(dist_to_border, 0.5*min_pair_dist).\"\"\"\n    if centers is None or len(centers) == 0:\n        return np.array([])\n    n = centers.shape[0]\n    dist_border = np.minimum.reduce([centers[:,0], centers[:,1], 1.0-centers[:,0], 1.0-centers[:,1]])\n    diff = centers[:,None,:] - centers[None,:,:]\n    dists = np.sqrt(np.maximum(0.0, (diff**2).sum(axis=2)))\n    np.fill_diagonal(dists, np.inf)\n    min_pair = np.min(dists, axis=1)\n    radii = np.minimum(dist_border, 0.5 * min_pair)\n    radii = np.maximum(radii, 0.0)\n    return radii\n\n# EVOLVE-BLOCK-END\n\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(6,6))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect(\"equal\")\n    for c,r in zip(centers,radii):\n        ax.add_patch(Circle(c,r,alpha=0.6,edgecolor='k'))\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, s = run_packing()\n    print(f\"Sum of radii: {s:.6f}\")\n    # visualize(centers, radii)\n```\nKey features: Alternative approach to validity, Alternative approach to sum_radii\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 0.6333, Type: Alternative)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (deterministic hex-like layout)\nImproved radii computation via iterative relaxation of pairwise constraints.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square.\n    Returns:\n        centers: np.array (26,2)\n        radii: np.array (26,)\n        sum_of_radii: float\n    \"\"\"\n    n = 26\n\n    # Row counts chosen to create a compact hex-like layout with edge effects\n    row_counts = [4, 5, 6, 6, 4, 1]  # sums to 26\n    rows = len(row_counts)\n    max_count = max(row_counts)\n\n    # Margins to keep centers away from exact border (allows positive radii)\n    margin = 0.05\n    xs = []\n    ys = []\n\n    # Vertical spacing approximating hex packing: sqrt(3)/2 factor\n    # Spread rows evenly between margin and 1-margin\n    y_positions = np.linspace(margin, 1 - margin, rows)\n\n    # base horizontal step to align with max_count\n    if max_count > 1:\n        base_step = (1 - 2 * margin) / (max_count - 1)\n    else:\n        base_step = 0.5\n\n    for ri, c in enumerate(row_counts):\n        y = y_positions[ri]\n        if c == 1:\n            x_line = np.array([0.5])\n        else:\n            x_line = np.linspace(margin, 1 - margin, c)\n            # offset alternate rows by half a base step to mimic hex staggering\n            if (ri % 2) == 1:\n                x_line = x_line + 0.5 * base_step\n                # clip back into allowed range\n                x_line = np.clip(x_line, margin, 1 - margin)\n        for x in x_line:\n            xs.append(x)\n            ys.append(y)\n\n    centers = np.vstack([xs, ys]).T\n    assert centers.shape == (n, 2)\n\n    radii = compute_max_radii(centers)\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\ndef compute_max_radii(centers, tol=1e-6, max_iter=1000):\n    \"\"\"\n    Iterative relaxation to find the largest radii per center such that:\n      r_i <= distance_to_border(i)\n      r_i + r_j <= dist(center_i, center_j) for all i != j\n\n    This returns a stable feasible vector of radii (not necessarily globally optimal\n    for repositioning centers, but much better than naive proportional scaling).\n    \"\"\"\n    n = centers.shape[0]\n    # distance to border limits\n    border_limit = np.minimum.reduce([centers[:,0], centers[:,1], 1-centers[:,0], 1-centers[:,1]])\n    border_limit = np.maximum(border_limit, 0.0)\n\n    # Precompute distance matrix\n    dif = centers[:, None, :] - centers[None, :, :]\n    dist = np.linalg.norm(dif, axis=2)\n    # Avoid zeros on diagonal\n    np.fill_diagonal(dist, np.inf)\n\n    # Initialize radii as border limits (optimistic)\n    r = border_limit.copy()\n\n    # Iteratively enforce pairwise constraints:\n    # For each i, r[i] must be <= min_j (dist[i,j] - r[j])\n    # so set r[i] = min(border_limit[i], min_j (dist[i,j] - r[j])) clipped to >=0.\n    for it in range(max_iter):\n        r_old = r.copy()\n        # For vectorized update compute min_allowed for each i\n        # dist - r_j (broadcast)\n        allowed = dist - r[None, :]  # shape (n,n)\n        # For each i take minimal across j\n        min_allowed = np.min(allowed, axis=1)\n        # New radii\n        r = np.minimum(border_limit, np.maximum(0.0, min_allowed))\n        # If some min_allowed are +inf (no other circles), r becomes border_limit (handled)\n        # Convergence check\n        if np.max(np.abs(r - r_old)) < tol:\n            break\n    else:\n        # If not converged, perform one final sweep with a stabilizing damping\n        r = 0.5 * (r + r_old)\n\n    return r\n\n# EVOLVE-BLOCK-END\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nUnique approach: Modification: Full rewrite, Excellent validity (1.000), Excellent sum_radii (1.669)\n\n### Inspiration 2 (Score: 0.3642, Type: Exploratory)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square\n    that attempts to maximize the sum of their radii.\n\n    Returns:\n        Tuple of (centers, radii, sum_of_radii)\n        centers: np.array of shape (26, 2) with (x, y) coordinates\n        radii: np.array of shape (26) with radius of each circle\n        sum_of_radii: Sum of all radii\n    \"\"\"\n    # Initialize arrays for 26 circles\n    n = 26\n    centers = np.zeros((n, 2))\n\n    # Place circles in a structured pattern\n    # This is a simple pattern - evolution will improve this\n\n    # First, place a large circle in the center\n    centers[0] = [0.5, 0.5]\n\n    # Place 8 circles around it in a ring\n    for i in range(8):\n        angle = 2 * np.pi * i / 8\n        centers[i + 1] = [0.5 + 0.3 * np.cos(angle), 0.5 + 0.3 * np.sin(angle)]\n\n    # Place 16 more circles in an outer ring\n    for i in range(16):\n        angle = 2 * np.pi * i / 16\n        centers[i + 9] = [0.5 + 0.7 * np.cos(angle), 0.5 + 0.7 * np.sin(angle)]\n\n    # Additional positioning adjustment to make sure all circles\n    # are inside the square and don't overlap\n    # Clip to ensure everything is inside the unit square\n    centers = np.clip(centers, 0.01, 0.99)\n\n    # Compute maximum valid radii for this configuration\n    radii = compute_max_radii(centers)\n\n    # Calculate the sum of radii\n    sum_radii = np.sum(radii)\n\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers):\n    \"\"\"\n    Compute the maximum possible radii for each circle position\n    such that they don't overlap and stay within the unit square.\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n\n    Returns:\n        np.array of shape (n) with radius of each circle\n    \"\"\"\n    n = centers.shape[0]\n    radii = np.ones(n)\n\n    # First, limit by distance to square borders\n    for i in range(n):\n        x, y = centers[i]\n        # Distance to borders\n        radii[i] = min(x, y, 1 - x, 1 - y)\n\n    # Then, limit by distance to other circles\n    # Each pair of circles with centers at distance d can have\n    # sum of radii at most d to avoid overlap\n    for i in range(n):\n        for j in range(i + 1, n):\n            dist = np.sqrt(np.sum((centers[i] - centers[j]) ** 2))\n\n            # If current radii would cause overlap\n            if radii[i] + radii[j] > dist:\n                # Scale both radii proportionally\n                scale = dist / (radii[i] + radii[j])\n                radii[i] *= scale\n                radii[j] *= scale\n\n    return radii\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.5)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\")\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii}\")\n    # AlphaEvolve improved this to 2.635\n\n    # Uncomment to visualize:\n    visualize(centers, radii)\n\n```\nUnique approach: Excellent validity (1.000), Excellent sum_radii (0.960), NumPy-based implementation\n\n### Inspiration 3 (Score: 0.9256, Type: High-Performer)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles using a hexagonal lattice constructor.\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a deterministic arrangement of 26 circle centers in [0,1]^2\n    using a hexagonal (offset-grid) lattice. We choose the largest lattice\n    spacing s that still produces at least 26 candidate centers, then\n    keep the 26 centers closest to the square center to mitigate edge effects.\n    Radii are set to the maximal non-overlapping values given these centers:\n        r_i = min(dist_to_border(i), 0.5 * min_j dist(center_i, center_j))\n    This yields a valid non-overlapping packing and tends to produce dense,\n    hexagonal-like local arrangements that maximize the sum of radii.\n    Returns:\n        (centers (26,2), radii (26,), sum_of_radii)\n    \"\"\"\n    n_target = 26\n\n    def make_hex_centers(s):\n        # margin so circles can have radius up to s/2 without crossing border\n        margin = s / 2.0\n        pts = []\n        y = margin\n        row = 0\n        y_step = s * np.sqrt(3) / 2.0\n        while y <= 1.0 - margin + 1e-12:\n            x_offset = (row % 2) * (s / 2.0)\n            x = margin + x_offset\n            while x <= 1.0 - margin + 1e-12:\n                pts.append((x, y))\n                x += s\n            row += 1\n            y += y_step\n        return np.array(pts)\n\n    # Binary search for the largest s that still yields at least n_target centers.\n    low, high = 0.01, 0.6  # high > possible spacing\n    best_s = low\n    for _ in range(40):\n        mid = 0.5 * (low + high)\n        pts = make_hex_centers(mid)\n        if pts.shape[0] >= n_target:\n            best_s = mid\n            low = mid  # we can try larger spacing\n        else:\n            high = mid\n    centers_all = make_hex_centers(best_s)\n\n    # If we have more than needed, keep the n_target centers closest to square center\n    if centers_all.shape[0] > n_target:\n        center_of_square = np.array([0.5, 0.5])\n        dists = np.linalg.norm(centers_all - center_of_square, axis=1)\n        idx = np.argsort(dists)[:n_target]\n        centers = centers_all[idx]\n    else:\n        centers = centers_all.copy()\n\n    # As a small enhancement, ensure we always return exactly n_target centers\n    # (should be guaranteed by construction/trimming above)\n    if centers.shape[0] < n_target:\n        # fallback: fill with evenly spaced points along a small spiral near center\n        extra = n_target - centers.shape[0]\n        spiral = []\n        for k in range(extra):\n            t = 0.05 * (k + 1)\n            angle = 2 * np.pi * (k + 1) / extra\n            spiral.append([0.5 + t * np.cos(angle), 0.5 + t * np.sin(angle)])\n        centers = np.vstack([centers, np.array(spiral)])\n\n    # Sort centers for determinism (by x then y)\n    centers = centers[np.lexsort((centers[:, 1], centers[:, 0]))]\n\n    # Compute maximal valid radii:\n    radii = compute_max_radii(centers)\n\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers):\n    \"\"\"\n    For a fixed set of centers, compute a safe maximal radius assignment\n    that guarantees no overlaps and containment in the unit square.\n    We use the conservative but tight rule:\n        r_i = min(dist_to_border(i), 0.5 * min_j dist(i,j))\n    which makes touching allowed but not overlapping.\n    \"\"\"\n    n = centers.shape[0]\n    radii = np.zeros(n)\n    # distance to borders\n    dist_border = np.minimum.reduce([centers[:, 0], centers[:, 1], 1 - centers[:, 0], 1 - centers[:, 1]])\n    # pairwise distances (compute efficiently)\n    diff = centers[:, None, :] - centers[None, :, :]  # n x n x 2\n    dists = np.sqrt(np.sum(diff ** 2, axis=2))\n    # set diagonal to a large number to ignore self-distance\n    np.fill_diagonal(dists, np.inf)\n    min_pair = np.min(dists, axis=1)\n    # radii limited by half the nearest neighbor distance and by borders\n    radii = np.minimum(dist_border, 0.5 * min_pair)\n    # Ensure non-negative and tiny numerical floor\n    radii = np.maximum(radii, 0.0)\n    return radii\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor='k', linewidth=0.5)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Hex-lattice Constructor Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nUnique approach: Modification: Full rewrite, Excellent validity (1.000), Excellent sum_radii (2.439)\n\n# Current Program\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (improved)\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square\n    using a compact hexagonal-like layout (explicit positions).\n    Returns:\n        Tuple of (centers, radii, sum_of_radii)\n        centers: np.array of shape (26, 2)\n        radii: np.array of shape (26,)\n        sum_of_radii: float\n    \"\"\"\n    n = 26\n\n    # Hexagonal-style row counts that sum to 26\n    # We place one wider row (6) and four rows of 5 each -> 6 + 5*4 = 26\n    row_counts = [6, 5, 5, 5, 5]\n    rows = len(row_counts)\n\n    # choose horizontal spacing so 6 points fit evenly across [0,1]\n    s = 1.0 / 6.0  # center-to-center spacing horizontally for the densest row\n    v_spacing = s * np.sqrt(3) / 2.0  # vertical spacing for hex packing\n\n    centers_list = []\n    # vertical offset to keep points nicely within the unit square\n    y0 = s / 2.0\n\n    for r in range(rows):\n        m = row_counts[r]\n        # center the row horizontally\n        total_width = (m - 1) * s\n        x_start = 0.5 - total_width / 2.0\n        # apply the hex offset for alternate rows to get closer packing\n        # alternate rows shifted by s/2\n        if (r % 2) == 1:\n            x_start += s / 2.0\n        y = y0 + r * v_spacing\n        for k in range(m):\n            x = x_start + k * s\n            # ensure numerical safety inside (0,1)\n            x = min(max(x, 1e-6), 1.0 - 1e-6)\n            yy = min(max(y, 1e-6), 1.0 - 1e-6)\n            centers_list.append([x, yy])\n\n    centers = np.array(centers_list[:n], dtype=float)\n    assert centers.shape == (n, 2), \"constructed centers must be shape (26,2)\"\n\n    # compute maximal feasible radii for these centers\n    radii = compute_max_radii(centers)\n\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers, tol=1e-8, max_iter=2000):\n    \"\"\"\n    Compute maximal non-overlapping radii for given centers within the unit square.\n    This uses a monotone iterative reduction algorithm:\n      - start from border-limited radii\n      - repeatedly fix any pairwise overlap by reducing the larger radius\n      - re-enforce border limits until convergence\n    Args:\n        centers: np.array of shape (n,2)\n        tol: convergence tolerance (maximum change per iteration)\n        max_iter: cap on iterations\n    Returns:\n        radii: np.array of shape (n,)\n    \"\"\"\n    n = centers.shape[0]\n    # initial radii limited by distance to borders\n    radii = np.empty(n, dtype=float)\n    for i in range(n):\n        x, y = centers[i]\n        radii[i] = min(x, y, 1.0 - x, 1.0 - y)\n\n    # iterative enforcement of pairwise non-overlap constraints\n    for it in range(max_iter):\n        max_change = 0.0\n\n        # pairwise pass: for any violating pair, reduce the larger radius\n        for i in range(n):\n            ci = centers[i]\n            ri = radii[i]\n            for j in range(i + 1, n):\n                cj = centers[j]\n                rj = radii[j]\n                dx = ci[0] - cj[0]\n                dy = ci[1] - cj[1]\n                dist = np.hypot(dx, dy)\n                if dist <= 1e-12:\n                    # extremely close centers: collapse both to zero to be safe\n                    if ri > 0 or rj > 0:\n                        max_change = max(max_change, ri, rj)\n                    radii[i] = 0.0\n                    radii[j] = 0.0\n                    ri = radii[i]\n                    rj = radii[j]\n                    continue\n\n                if ri + rj > dist:\n                    # reduce the larger radius so ri + rj == dist (keep smaller fixed)\n                    if ri > rj:\n                        new_ri = max(0.0, dist - rj)\n                        change = abs(new_ri - ri)\n                        if change > 0:\n                            radii[i] = new_ri\n                            ri = new_ri\n                            max_change = max(max_change, change)\n                    else:\n                        new_rj = max(0.0, dist - ri)\n                        change = abs(new_rj - rj)\n                        if change > 0:\n                            radii[j] = new_rj\n                            rj = new_rj\n                            max_change = max(max_change, change)\n\n        # enforce border constraints again and track any changes\n        for i in range(n):\n            x, y = centers[i]\n            border_limit = min(x, y, 1.0 - x, 1.0 - y)\n            if radii[i] > border_limit:\n                change = radii[i] - border_limit\n                radii[i] = border_limit\n                if change > max_change:\n                    max_change = change\n\n        if max_change < tol:\n            break\n\n    # final safety clamp\n    radii = np.maximum(radii, 0.0)\n    return radii\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\n\n# Task\nRewrite the program to improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: complexity, diversity\nDifferent solutions with similar fitness but different features are valuable.\nProvide the complete new program code.\n\nIMPORTANT: Make sure your rewritten program maintains the same inputs and outputs\nas the original program, but with improved internal implementation.\n\n```python\n# Your rewritten program here\n```", "responses": ["```python\n# EVOLVE-BLOCK-START\n\"\"\"Improved deterministic constructor for n=26 circle packing in the unit square.\n\nStrategy:\n- Build hexagonal (offset) lattices with the largest feasible spacing s that\n  yields candidate centers.\n- For several phase offsets (shifting the lattice within one cell) and a few\n  nearby spacings, take the 26 centers closest to the square center (reducing\n  edge effects), compute tight safe radii r_i = min(dist_to_border, 0.5*min_j dist(i,j))\n  and keep the configuration with the largest sum of radii found.\nThis explicit (non-iterative) constructor explores a small, deterministic family\nof hex-like placements and picks the best, yielding dense center packings\nand improved total radius sum.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n_target = 26\n\n    def make_hex_centers(s, ox=0.0, oy=0.0):\n        # margin so a circle of radius up to s/2 around a center stays inside\n        margin = s * 0.5\n        pts = []\n        y = margin + oy\n        y_step = s * np.sqrt(3.0) / 2.0\n        row = 0\n        eps = 1e-12\n        while y <= 1.0 - margin + eps:\n            x_offset = (row % 2) * (s / 2.0) + ox\n            x = margin + x_offset\n            while x <= 1.0 - margin + eps:\n                if 0.0 <= x <= 1.0 and 0.0 <= y <= 1.0:\n                    pts.append((x, y))\n                x += s\n            row += 1\n            y += y_step\n        return np.array(pts, dtype=float)\n\n    # binary search largest s that yields at least n_target centers (default phase)\n    low, high = 0.01, 0.6\n    best_s = low\n    for _ in range(40):\n        mid = 0.5 * (low + high)\n        if make_hex_centers(mid).shape[0] >= n_target:\n            best_s = mid\n            low = mid\n        else:\n            high = mid\n\n    # explore a small grid of offsets within one lattice cell and a few nearby s-values\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    s_candidates = [best_s * 0.98, best_s, best_s * 1.02]\n    # ensure candidates are within sensible bounds\n    s_candidates = [max(0.005, min(0.6, sc)) for sc in s_candidates]\n\n    # sample deterministic offsets (5x5 grid) within [0, s)\n    offs = np.linspace(0.0, 1.0, 5, endpoint=False)  # fraction of s\n    for s_val in s_candidates:\n        for fx in offs:\n            for fy in offs:\n                ox = fx * s_val\n                oy = fy * s_val * np.sqrt(3.0) / 2.0  # align vertical phase\n                pts = make_hex_centers(s_val, ox=ox, oy=oy)\n                if pts.shape[0] < n_target:\n                    continue\n                # pick n_target centers closest to square center\n                center_sq = np.array([0.5, 0.5])\n                d = np.linalg.norm(pts - center_sq, axis=1)\n                idx = np.argsort(d)[:n_target]\n                centers = pts[idx]\n\n                # compute tight safe radii vectorized\n                dist_border = np.minimum.reduce([centers[:,0], centers[:,1], 1.0-centers[:,0], 1.0-centers[:,1]])\n                diff = centers[:, None, :] - centers[None, :, :]\n                dists = np.sqrt(np.maximum(0.0, (diff**2).sum(axis=2)))\n                np.fill_diagonal(dists, np.inf)\n                min_pair = np.min(dists, axis=1)\n                radii = np.minimum(dist_border, 0.5 * min_pair)\n                radii = np.maximum(radii, 0.0)\n                ssum = float(np.sum(radii))\n                if ssum > best_sum + 1e-12:\n                    best_sum = ssum\n                    best_centers = centers.copy()\n                    best_radii = radii.copy()\n\n    # Fallback: if nothing found (very unlikely), build the default lattice and trim\n    if best_centers is None:\n        pts = make_hex_centers(best_s)\n        center_sq = np.array([0.5, 0.5])\n        d = np.linalg.norm(pts - center_sq, axis=1)\n        idx = np.argsort(d)[:n_target]\n        best_centers = pts[idx]\n        dist_border = np.minimum.reduce([best_centers[:,0], best_centers[:,1], 1.0-best_centers[:,0], 1.0-best_centers[:,1]])\n        diff = best_centers[:, None, :] - best_centers[None, :, :]\n        dists = np.sqrt(np.maximum(0.0, (diff**2).sum(axis=2)))\n        np.fill_diagonal(dists, np.inf)\n        min_pair = np.min(dists, axis=1)\n        best_radii = np.minimum(dist_border, 0.5 * min_pair)\n        best_radii = np.maximum(best_radii, 0.0)\n        best_sum = float(np.sum(best_radii))\n\n    # deterministic ordering\n    order = np.lexsort((best_centers[:,1], best_centers[:,0]))\n    best_centers = best_centers[order]\n    best_radii = best_radii[order]\n\n    return best_centers, best_radii, float(np.sum(best_radii))\n\n\n# EVOLVE-BLOCK-END\n\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(6,6))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect('equal')\n    for i,(c,r) in enumerate(zip(centers,radii)):\n        ax.add_patch(Circle(c,r,alpha=0.6,edgecolor='k'))\n        ax.text(c[0], c[1], str(i), ha='center', va='center', fontsize=8)\n    plt.title(f\"n={len(centers)}, sum={sum(radii):.6f}\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, s = run_packing()\n    print(f\"Sum of radii: {s:.6f}\")\n    # visualize(centers, radii)\n```"]}}, "artifacts_json": "{\"stdout\": \"Running in subprocess, Python version: 3.12.1 (v3.12.1:2305ca5144, Dec  7 2023, 17:23:38) [Clang 13.0.0 (clang-1300.0.29.30)]\\nProgram path: /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmp6qpbp9m4.py\\nCalling run_packing()...\\nrun_packing() returned successfully: sum_radii = 2.4389662673793744\\nResults saved to /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmp7u8i7s6x.py.results\\n\", \"stderr\": \"\"}", "artifact_dir": null}