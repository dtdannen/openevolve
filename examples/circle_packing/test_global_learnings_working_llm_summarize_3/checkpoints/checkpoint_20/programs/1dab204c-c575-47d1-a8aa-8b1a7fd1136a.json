{"id": "1dab204c-c575-47d1-a8aa-8b1a7fd1136a", "code": "# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for n=26 circle packing in the unit square.\nUses a hexagonal (offset) lattice: choose the largest lattice spacing s that\nstill yields at least 26 candidate centers, keep the 26 centers closest to\nthe square center (mitigating edge effects) and assign radii by the tight\nlocal rule r_i = min(dist_to_border(i), 0.5*min_j dist(i,j)).\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n\n    def make_hex_centers(s):\n        eps = 1e-12\n        margin = s / 2.0\n        pts = []\n        y = margin\n        row = 0\n        y_step = s * np.sqrt(3) / 2.0\n        while y <= 1.0 - margin + eps:\n            x_offset = (row % 2) * (s / 2.0)\n            x = margin + x_offset\n            while x <= 1.0 - margin + eps:\n                pts.append((x, y))\n                x += s\n            row += 1\n            y += y_step\n        return np.array(pts, dtype=float)\n\n    # binary search for largest spacing producing >= n centers\n    low, high = 0.01, 0.6\n    best_s = low\n    for _ in range(40):\n        mid = 0.5 * (low + high)\n        pts = make_hex_centers(mid)\n        if pts.shape[0] >= n:\n            best_s = mid\n            low = mid\n        else:\n            high = mid\n\n    centers_all = make_hex_centers(best_s)\n    if centers_all.shape[0] > n:\n        center_sq = np.array([0.5, 0.5])\n        d = np.linalg.norm(centers_all - center_sq, axis=1)\n        idx = np.argsort(d)[:n]\n        centers = centers_all[idx]\n    else:\n        centers = centers_all.copy()\n\n    # deterministic ordering\n    centers = centers[np.lexsort((centers[:,1], centers[:,0]))]\n\n    radii = compute_max_radii(centers)\n    return centers, radii, float(radii.sum())\n\ndef compute_max_radii(centers):\n    \"\"\"Vectorized tight local rule: r_i = min(dist_to_border, 0.5*min_pair_dist).\"\"\"\n    if centers is None or len(centers) == 0:\n        return np.array([])\n    n = centers.shape[0]\n    dist_border = np.minimum.reduce([centers[:,0], centers[:,1], 1.0-centers[:,0], 1.0-centers[:,1]])\n    diff = centers[:,None,:] - centers[None,:,:]\n    dists = np.sqrt(np.maximum(0.0, (diff**2).sum(axis=2)))\n    np.fill_diagonal(dists, np.inf)\n    min_pair = np.min(dists, axis=1)\n    radii = np.minimum(dist_border, 0.5 * min_pair)\n    radii = np.maximum(radii, 0.0)\n    return radii\n\n# EVOLVE-BLOCK-END\n\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(6,6))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect(\"equal\")\n    for c,r in zip(centers,radii):\n        ax.add_patch(Circle(c,r,alpha=0.6,edgecolor='k'))\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, s = run_packing()\n    print(f\"Sum of radii: {s:.6f}\")\n    # visualize(centers, radii)", "language": "python", "parent_id": "3b8526ee-297f-4942-8e9b-525497ffd9e2", "generation": 3, "timestamp": 1759958948.4732852, "iteration_found": 19, "metrics": {"validity": 1.0, "sum_radii": 2.4389662673793744, "target_ratio": 0.925603896538662, "combined_score": 0.925603896538662, "eval_time": 0.05725693702697754}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Full rewrite", "parent_metrics": {"validity": 1.0, "sum_radii": 2.0652831878705595, "target_ratio": 0.7837886860988842, "combined_score": 0.7837886860988842, "eval_time": 0.07875490188598633}, "island": 0}, "prompts": {"full_rewrite_user": {"system": "You are an expert mathematician specializing in circle packing problems and computational geometry. Your task is to improve a constructor function that directly produces a specific arrangement of 26 circles in a unit square, maximizing the sum of their radii. The AlphaEvolve paper achieved a sum of 2.635 for n=26.\n\nKey geometric insights:\n- Circle packings often follow hexagonal patterns in the densest regions\n- Maximum density for infinite circle packing is pi/(2*sqrt(3)) \u2248 0.9069\n- Edge effects make square container packing harder than infinite packing\n- Circles can be placed in layers or shells when confined to a square\n- Similar radius circles often form regular patterns, while varied radii allow better space utilization\n- Perfect symmetry may not yield the optimal packing due to edge effects\n\nFocus on designing an explicit constructor that places each circle in a specific position, rather than an iterative search algorithm.\n", "user": "# Current Program Information\n- Fitness: 0.7838\n- Feature coordinates: No feature coordinates\n- Focus areas: - Fitness unchanged at 0.7838\n- Consider simplifying - code length exceeds 500 characters\n\n## Last Execution Output\n\n### stdout\n```\nRunning in subprocess, Python version: 3.12.1 (v3.12.1:2305ca5144, Dec  7 2023, 17:23:38) [Clang 13.0.0 (clang-1300.0.29.30)]\nProgram path: /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmpy79g3ccv.py\nCalling run_packing()...\nrun_packing() returned successfully: sum_radii = 2.0652831878705595\nResults saved to /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmppezh0t4p.py.results\n\n```\n\n### stderr\n```\n\n```\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 3\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.0653, target_ratio: 0.7838, combined_score: 0.7838, eval_time: 0.0788\n- Outcome: Mixed results\n\n### Attempt 2\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.0833, target_ratio: 0.7906, combined_score: 0.7906, eval_time: 0.0568\n- Outcome: Mixed results\n\n### Attempt 1\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.4390, target_ratio: 0.9256, combined_score: 0.9256, eval_time: 0.0571\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.9256)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles using a hexagonal lattice constructor.\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a deterministic arrangement of 26 circle centers in [0,1]^2\n    using a hexagonal (offset-grid) lattice. We choose the largest lattice\n    spacing s that still produces at least 26 candidate centers, then\n    keep the 26 centers closest to the square center to mitigate edge effects.\n    Radii are set to the maximal non-overlapping values given these centers:\n        r_i = min(dist_to_border(i), 0.5 * min_j dist(center_i, center_j))\n    This yields a valid non-overlapping packing and tends to produce dense,\n    hexagonal-like local arrangements that maximize the sum of radii.\n    Returns:\n        (centers (26,2), radii (26,), sum_of_radii)\n    \"\"\"\n    n_target = 26\n\n    def make_hex_centers(s):\n        # margin so circles can have radius up to s/2 without crossing border\n        margin = s / 2.0\n        pts = []\n        y = margin\n        row = 0\n        y_step = s * np.sqrt(3) / 2.0\n        while y <= 1.0 - margin + 1e-12:\n            x_offset = (row % 2) * (s / 2.0)\n            x = margin + x_offset\n            while x <= 1.0 - margin + 1e-12:\n                pts.append((x, y))\n                x += s\n            row += 1\n            y += y_step\n        return np.array(pts)\n\n    # Binary search for the largest s that still yields at least n_target centers.\n    low, high = 0.01, 0.6  # high > possible spacing\n    best_s = low\n    for _ in range(40):\n        mid = 0.5 * (low + high)\n        pts = make_hex_centers(mid)\n        if pts.shape[0] >= n_target:\n            best_s = mid\n            low = mid  # we can try larger spacing\n        else:\n            high = mid\n    centers_all = make_hex_centers(best_s)\n\n    # If we have more than needed, keep the n_target centers closest to square center\n    if centers_all.shape[0] > n_target:\n        center_of_square = np.array([0.5, 0.5])\n        dists = np.linalg.norm(centers_all - center_of_square, axis=1)\n        idx = np.argsort(dists)[:n_target]\n        centers = centers_all[idx]\n    else:\n        centers = centers_all.copy()\n\n    # As a small enhancement, ensure we always return exactly n_target centers\n    # (should be guaranteed by construction/trimming above)\n    if centers.shape[0] < n_target:\n        # fallback: fill with evenly spaced points along a small spiral near center\n        extra = n_target - centers.shape[0]\n        spiral = []\n        for k in range(extra):\n            t = 0.05 * (k + 1)\n            angle = 2 * np.pi * (k + 1) / extra\n            spiral.append([0.5 + t * np.cos(angle), 0.5 + t * np.sin(angle)])\n        centers = np.vstack([centers, np.array(spiral)])\n\n    # Sort centers for determinism (by x then y)\n    centers = centers[np.lexsort((centers[:, 1], centers[:, 0]))]\n\n    # Compute maximal valid radii:\n    radii = compute_max_radii(centers)\n\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers):\n    \"\"\"\n    For a fixed set of centers, compute a safe maximal radius assignment\n    that guarantees no overlaps and containment in the unit square.\n    We use the conservative but tight rule:\n        r_i = min(dist_to_border(i), 0.5 * min_j dist(i,j))\n    which makes touching allowed but not overlapping.\n    \"\"\"\n    n = centers.shape[0]\n    radii = np.zeros(n)\n    # distance to borders\n    dist_border = np.minimum.reduce([centers[:, 0], centers[:, 1], 1 - centers[:, 0], 1 - centers[:, 1]])\n    # pairwise distances (compute efficiently)\n    diff = centers[:, None, :] - centers[None, :, :]  # n x n x 2\n    dists = np.sqrt(np.sum(diff ** 2, axis=2))\n    # set diagonal to a large number to ignore self-distance\n    np.fill_diagonal(dists, np.inf)\n    min_pair = np.min(dists, axis=1)\n    # radii limited by half the nearest neighbor distance and by borders\n    radii = np.minimum(dist_border, 0.5 * min_pair)\n    # Ensure non-negative and tiny numerical floor\n    radii = np.maximum(radii, 0.0)\n    return radii\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor='k', linewidth=0.5)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Hex-lattice Constructor Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.4390), Performs well on target_ratio (0.9256), Performs well on combined_score (0.9256), Performs well on eval_time (0.0571)\n\n### Program 2 (Score: 0.7906)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (explicit hex-like layout).\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square using an\n    explicit hexagonal-like layering. Returns (centers, radii, sum_of_radii).\n    \"\"\"\n    n = 26\n\n    # Row counts that sum to 26\n    # A compact block with three dense rows of 6 and two shorter rows helps\n    # utilize interior space while allowing larger border circles.\n    row_counts = [6, 6, 6, 4, 4]\n\n    # choose horizontal spacing s so a 6-wide row fits comfortably in [0,1]\n    s = 1.0 / 6.0\n    v = s * np.sqrt(3) / 2.0  # hexagonal vertical spacing\n\n    centers_list = []\n    # vertical start so block is nicely centered vertically but inside borders\n    total_v_span = (len(row_counts) - 1) * v\n    y_start = 0.5 - total_v_span / 2.0\n\n    for r, m in enumerate(row_counts):\n        # alternate rows offset by s/2 to realize hex packing\n        offset = (s / 2.0) if (r % 2 == 1) else 0.0\n        row_width = (m - 1) * s\n        x_start = 0.5 - row_width / 2.0 + offset\n        y = y_start + r * v\n        for k in range(m):\n            x = x_start + k * s\n            # clip slightly inside to avoid zero border distance numerically\n            x = np.clip(x, 1e-8, 1.0 - 1e-8)\n            yy = np.clip(y, 1e-8, 1.0 - 1e-8)\n            centers_list.append([x, yy])\n            if len(centers_list) >= n:\n                break\n        if len(centers_list) >= n:\n            break\n\n    centers = np.array(centers_list[:n], dtype=float)\n    assert centers.shape == (n, 2)\n\n    radii = compute_max_radii(centers)\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers, tol=1e-8, max_iter=2000):\n    \"\"\"\n    Monotone iterative radius projection:\n    - initialize radii to distance to nearest border\n    - repeatedly resolve any pair overlap by shrinking the larger radius so ri + rj = dist\n      (keeps the smaller radius fixed). Re-enforce border limits each pass.\n    \"\"\"\n    n = centers.shape[0]\n    radii = np.minimum.reduce([centers[:, 0], centers[:, 1],\n                               1.0 - centers[:, 0], 1.0 - centers[:, 1]]).copy()\n\n    for it in range(max_iter):\n        max_change = 0.0\n        # pairwise pass\n        for i in range(n):\n            ci = centers[i]\n            ri = radii[i]\n            for j in range(i + 1, n):\n                cj = centers[j]\n                rj = radii[j]\n                dx = ci[0] - cj[0]\n                dy = ci[1] - cj[1]\n                d = np.hypot(dx, dy)\n                if d <= 1e-12:\n                    # coincident centers: set both to zero to be safe\n                    if ri > 0.0:\n                        max_change = max(max_change, ri)\n                        radii[i] = 0.0\n                        ri = 0.0\n                    if rj > 0.0:\n                        max_change = max(max_change, rj)\n                        radii[j] = 0.0\n                        rj = 0.0\n                    continue\n                if ri + rj > d:\n                    # shrink the larger radius\n                    if ri > rj:\n                        new_ri = max(0.0, d - rj)\n                        change = abs(new_ri - ri)\n                        if change > 0:\n                            radii[i] = new_ri\n                            ri = new_ri\n                            max_change = max(max_change, change)\n                    else:\n                        new_rj = max(0.0, d - ri)\n                        change = abs(new_rj - rj)\n                        if change > 0:\n                            radii[j] = new_rj\n                            rj = new_rj\n                            max_change = max(max_change, change)\n\n        # enforce border limits\n        border_limits = np.minimum.reduce([centers[:, 0], centers[:, 1],\n                                           1.0 - centers[:, 0], 1.0 - centers[:, 1]])\n        over = radii > border_limits\n        if np.any(over):\n            change = np.max(radii[over] - border_limits[over])\n            radii[over] = border_limits[over]\n            max_change = max(max_change, float(change))\n\n        if max_change < tol:\n            break\n\n    radii = np.maximum(radii, 0.0)\n    return radii\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.0833), Performs well on target_ratio (0.7906), Performs well on combined_score (0.7906), Performs well on eval_time (0.0568)\n\n### Program 3 (Score: 0.7838)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (parameterized layouts + projection)\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    \"\"\"\n    Try a small family of explicit hex-like row patterns and a short 1D sweep of\n    horizontal spacing s to pick the layout that yields the largest sum of maximal\n    radii (centers fixed, radii computed by projection).\n    Returns centers (26,2), radii (26,), sum_radii.\n    \"\"\"\n    n = 26\n    patterns = [\n        [6,5,5,5,5],   # 5 rows\n        [7,6,7,6],     # 4 rows\n        [5,6,5,5,5]    # alternative 5 rows\n    ]\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n    sqrt3 = np.sqrt(3.0)\n\n    # For each pattern, sweep plausible s values\n    for pat in patterns:\n        max_row = max(pat)\n        # choose plausible s range so row fits: s*(max_row-1) <= 0.9 approx leave margin\n        s_min = 0.10\n        s_max = min(0.22, 0.9 / max(1, (max_row - 1)))\n        if s_max <= s_min:\n            s_max = s_min + 0.05\n        for s in np.linspace(s_min, s_max, 36):\n            v = s * sqrt3 / 2.0\n            rows = len(pat)\n            # vertical span of centers\n            v_span = (rows - 1) * v\n            # center block vertically in unit square with small margin\n            y0 = 0.5 - v_span / 2.0\n            centers = []\n            for r, m in enumerate(pat):\n                total_w = (m - 1) * s\n                x_start = 0.5 - total_w / 2.0\n                if r % 2 == 1:\n                    x_start += s / 2.0\n                y = y0 + r * v\n                for k in range(m):\n                    x = x_start + k * s\n                    # clip a touch inside\n                    centers.append([min(max(x, 1e-6), 1.0 - 1e-6), min(max(y, 1e-6), 1.0 - 1e-6)])\n            centers = np.array(centers[:n], dtype=float)\n            if centers.shape[0] != n:\n                continue\n            radii = compute_max_radii(centers)\n            ssum = radii.sum()\n            if ssum > best_sum:\n                best_sum = ssum\n                best_centers = centers.copy()\n                best_radii = radii.copy()\n\n    # Fallback if none found\n    if best_centers is None:\n        best_centers = np.array([[0.5,0.5]] + [[0.5+0.01*i,0.5+0.01*j] for i in range(n-1) for j in range(1)][:n-1])\n        best_radii = compute_max_radii(best_centers)\n        best_sum = float(best_radii.sum())\n\n    return best_centers, best_radii, float(best_sum)\n\ndef compute_max_radii(centers, tol=1e-8, max_iter=2000):\n    \"\"\"\n    Given fixed centers in unit square compute maximal radii by iterative projection:\n    - start from border-limited radii\n    - repeatedly fix pairwise overlaps by shrinking the larger circle of each offending pair\n    \"\"\"\n    n = centers.shape[0]\n    # initial radii limited by distance to border\n    radii = np.minimum.reduce([centers[:,0], centers[:,1], 1.0-centers[:,0], 1.0-centers[:,1]]).copy()\n    # pairwise distances\n    dx = centers[:,0][:,None] - centers[:,0][None,:]\n    dy = centers[:,1][:,None] - centers[:,1][None,:]\n    D = np.sqrt(np.maximum(0.0, dx*dx + dy*dy))\n    # small iterative greedy correction (shrink larger when ri+rj>d)\n    for _ in range(max_iter):\n        max_change = 0.0\n        # find violating pairs\n        I,J = np.triu_indices(n, k=1)\n        dij = D[I,J]\n        rij = radii[I] + radii[J]\n        mask = rij > dij\n        if not np.any(mask):\n            break\n        for i,j,d in zip(I[mask], J[mask], dij[mask]):\n            ri = radii[i]; rj = radii[j]\n            if ri + rj <= d + 1e-12:\n                continue\n            if ri > rj:\n                new_ri = max(0.0, d - rj)\n                ch = abs(new_ri - ri)\n                if ch>0:\n                    radii[i] = new_ri\n                    if ch>max_change: max_change=ch\n            else:\n                new_rj = max(0.0, d - ri)\n                ch = abs(new_rj - rj)\n                if ch>0:\n                    radii[j] = new_rj\n                    if ch>max_change: max_change=ch\n        # enforce border limits\n        border = np.minimum.reduce([centers[:,0], centers[:,1], 1.0-centers[:,0], 1.0-centers[:,1]])\n        over = radii > border\n        if np.any(over):\n            diff = np.max(radii[over] - border[over])\n            radii[over] = border[over]\n            if diff > max_change: max_change = diff\n        if max_change < tol:\n            break\n    radii = np.maximum(radii, 0.0)\n    return radii\n\n# EVOLVE-BLOCK-END\n\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(6,6))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect(\"equal\")\n    for c,r in zip(centers,radii):\n        ax.add_patch(Circle(c,r,alpha=0.6,edgecolor='k'))\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, s = run_packing()\n    print(f\"Sum of radii: {s:.6f}\")\n    # visualize(centers, radii)\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.0653), Performs well on target_ratio (0.7838), Performs well on combined_score (0.7838), Performs well on eval_time (0.0788)\n\n\n\n## Diverse Programs\n\n### Program D1 (Score: 0.7375)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (improved)\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square\n    using a compact hexagonal-like layout (explicit positions).\n    Returns:\n        Tuple of (centers, radii, sum_of_radii)\n        centers: np.array of shape (26, 2)\n        radii: np.array of shape (26,)\n        sum_of_radii: float\n    \"\"\"\n    n = 26\n\n    # Hexagonal-style row counts that sum to 26\n    # We place one wider row (6) and four rows of 5 each -> 6 + 5*4 = 26\n    row_counts = [6, 5, 5, 5, 5]\n    rows = len(row_counts)\n\n    # choose horizontal spacing so 6 points fit evenly across [0,1]\n    s = 1.0 / 6.0  # center-to-center spacing horizontally for the densest row\n    v_spacing = s * np.sqrt(3) / 2.0  # vertical spacing for hex packing\n\n    centers_list = []\n    # vertical offset to keep points nicely within the unit square\n    y0 = s / 2.0\n\n    for r in range(rows):\n        m = row_counts[r]\n        # center the row horizontally\n        total_width = (m - 1) * s\n        x_start = 0.5 - total_width / 2.0\n        # apply the hex offset for alternate rows to get closer packing\n        # alternate rows shifted by s/2\n        if (r % 2) == 1:\n            x_start += s / 2.0\n        y = y0 + r * v_spacing\n        for k in range(m):\n            x = x_start + k * s\n            # ensure numerical safety inside (0,1)\n            x = min(max(x, 1e-6), 1.0 - 1e-6)\n            yy = min(max(y, 1e-6), 1.0 - 1e-6)\n            centers_list.append([x, yy])\n\n    centers = np.array(centers_list[:n], dtype=float)\n    assert centers.shape == (n, 2), \"constructed centers must be shape (26,2)\"\n\n    # compute maximal feasible radii for these centers\n    radii = compute_max_radii(centers)\n\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers, tol=1e-8, max_iter=2000):\n    \"\"\"\n    Compute maximal non-overlapping radii for given centers within the unit square.\n    This uses a monotone iterative reduction algorithm:\n      - start from border-limited radii\n      - repeatedly fix any pairwise overlap by reducing the larger radius\n      - re-enforce border limits until convergence\n    Args:\n        centers: np.array of shape (n,2)\n        tol: convergence tolerance (maximum change per iteration)\n        max_iter: cap on iterations\n    Returns:\n        radii: np.array of shape (n,)\n    \"\"\"\n    n = centers.shape[0]\n    # initial radii limited by distance to borders\n    radii = np.empty(n, dtype=float)\n    for i in range(n):\n        x, y = centers[i]\n        radii[i] = min(x, y, 1.0 - x, 1.0 - y)\n\n    # iterative enforcement of pairwise non-overlap constraints\n    for it in range(max_iter):\n        max_change = 0.0\n\n        # pairwise pass: for any violating pair, reduce the larger radius\n        for i in range(n):\n            ci = centers[i]\n            ri = radii[i]\n            for j in range(i + 1, n):\n                cj = centers[j]\n                rj = radii[j]\n                dx = ci[0] - cj[0]\n                dy = ci[1] - cj[1]\n                dist = np.hypot(dx, dy)\n                if dist <= 1e-12:\n                    # extremely close centers: collapse both to zero to be safe\n                    if ri > 0 or rj > 0:\n                        max_change = max(max_change, ri, rj)\n                    radii[i] = 0.0\n                    radii[j] = 0.0\n                    ri = radii[i]\n                    rj = radii[j]\n                    continue\n\n                if ri + rj > dist:\n                    # reduce the larger radius so ri + rj == dist (keep smaller fixed)\n                    if ri > rj:\n                        new_ri = max(0.0, dist - rj)\n                        change = abs(new_ri - ri)\n                        if change > 0:\n                            radii[i] = new_ri\n                            ri = new_ri\n                            max_change = max(max_change, change)\n                    else:\n                        new_rj = max(0.0, dist - ri)\n                        change = abs(new_rj - rj)\n                        if change > 0:\n                            radii[j] = new_rj\n                            rj = new_rj\n                            max_change = max(max_change, change)\n\n        # enforce border constraints again and track any changes\n        for i in range(n):\n            x, y = centers[i]\n            border_limit = min(x, y, 1.0 - x, 1.0 - y)\n            if radii[i] > border_limit:\n                change = radii[i] - border_limit\n                radii[i] = border_limit\n                if change > max_change:\n                    max_change = change\n\n        if max_change < tol:\n            break\n\n    # final safety clamp\n    radii = np.maximum(radii, 0.0)\n    return radii\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor=\"k\")\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nKey features: Alternative approach to validity, Alternative approach to sum_radii\n\n### Program D2 (Score: 0.6434)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (improved)\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square\n    using a hexagonal-like layered layout (rows: 5,6,5,5,5). The routine\n    searches over the lattice spacing parameter to maximize the sum of\n    feasible radii (subject to non-overlap and border constraints).\n\n    Returns:\n        Tuple of (centers, radii, sum_of_radii)\n        centers: np.array of shape (26, 2) with (x, y) coordinates\n        radii: np.array of shape (26) with radius of each circle\n        sum_of_radii: Sum of all radii\n    \"\"\"\n    n = 26\n    row_counts = [5, 6, 5, 5, 5]  # sums to 26\n    rows = len(row_counts)\n\n    def build_centers(s):\n        \"\"\"Build hex-like grid centers for spacing s.\"\"\"\n        h = s\n        v = s * np.sqrt(3) / 2.0\n        # horizontal extents for largest row to center the whole pattern\n        max_count = max(row_counts)\n        max_width = (max_count - 1) * h\n        x_origin = 0.5 - max_width / 2.0\n        total_height = (rows - 1) * v\n        y_origin = 0.5 - total_height / 2.0\n\n        centers = []\n        for ri, count in enumerate(row_counts):\n            row_width = (count - 1) * h\n            # center row relative to the largest row (so pattern is centered)\n            x_start = 0.5 - row_width / 2.0\n            # alternate shift for hex packing\n            shift = (h / 2.0) if (ri % 2 == 1) else 0.0\n            y = y_origin + ri * v\n            for ci in range(count):\n                x = x_start + shift + ci * h\n                centers.append([x, y])\n        centers = np.array(centers)\n        # keep within unit square (should be by construction if s chosen appropriately)\n        centers = np.clip(centers, 1e-6, 1.0 - 1e-6)\n        return centers\n\n    def compute_max_radii(centers, max_iter=400, tol=1e-6):\n        \"\"\"\n        Iterative relaxation to find a feasible set of radii satisfying:\n         - r_i <= border_distance(i)\n         - r_i + r_j <= dist(i,j)\n\n        Starts with border distances and repeatedly enforces pairwise constraints\n        by proportional scaling; iterates until convergence.\n        \"\"\"\n        nloc = centers.shape[0]\n        radii = np.zeros(nloc)\n        # initial limit by borders\n        for i in range(nloc):\n            x, y = centers[i]\n            radii[i] = min(x, y, 1 - x, 1 - y)\n        if nloc <= 1:\n            return radii\n\n        # Precompute pairwise distances\n        diffs = centers[:, None, :] - centers[None, :, :]\n        dists = np.sqrt(np.maximum(0.0, np.sum(diffs * diffs, axis=2)))\n        # Ensure nonzero diagonal for numeric safety\n        np.fill_diagonal(dists, 1.0)\n\n        for it in range(max_iter):\n            max_change = 0.0\n            # enforce pairwise non-overlap constraints\n            for i in range(nloc):\n                for j in range(i + 1, nloc):\n                    dij = dists[i, j]\n                    if dij <= 1e-12:\n                        # coincident centers: force tiny radii split\n                        new_ri = min(radii[i], 1e-6)\n                        new_rj = min(radii[j], 1e-6)\n                        max_change = max(max_change, abs(radii[i] - new_ri), abs(radii[j] - new_rj))\n                        radii[i], radii[j] = new_ri, new_rj\n                        continue\n                    if radii[i] + radii[j] > dij:\n                        # scale proportionally to satisfy equality r_i + r_j = dij\n                        total = radii[i] + radii[j]\n                        if total <= 0:\n                            # both zero: nothing to do\n                            continue\n                        factor = dij / total\n                        new_ri = radii[i] * factor\n                        new_rj = radii[j] * factor\n                        max_change = max(max_change, abs(radii[i] - new_ri), abs(radii[j] - new_rj))\n                        radii[i], radii[j] = new_ri, new_rj\n            # enforce border constraints (may reduce radii)\n            for i in range(nloc):\n                x, y = centers[i]\n                border = min(x, y, 1 - x, 1 - y)\n                if radii[i] > border:\n                    max_change = max(max_change, radii[i] - border)\n                    radii[i] = border\n            if max_change < tol:\n                break\n        return radii\n\n    # Search for the best spacing s that maximizes sum of radii\n    # s must be small enough so centers lie inside the unit square without clipping.\n    # For our largest row_count=6, an upper bound s_max <= 1/(6-2) = 0.25 (safe).\n    s_min, s_max = 0.06, 0.25\n    # coarse search\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n\n    s_vals = np.linspace(s_min, s_max, 48)\n    for s in s_vals:\n        centers_try = build_centers(s)\n        radii_try = compute_max_radii(centers_try, max_iter=300)\n        ssum = radii_try.sum()\n        if ssum > best_sum:\n            best_sum = ssum\n            best_centers = centers_try.copy()\n            best_radii = radii_try.copy()\n\n    # refine around best s\n    if best_centers is not None:\n        # infer best s from centers approximate horizontal spacing between first two different x's\n        # Fallback: sample narrower range around s that produced best_centers\n        # We approximate s_best by median of nearest neighbor x-differences\n        xs = np.sort(best_centers[:, 0])\n        diffs_x = np.diff(xs)\n        # pick a robust estimate of s from small positive diffs (exclude near-zero)\n        cand = diffs_x[diffs_x > 1e-4]\n        if cand.size > 0:\n            s_est = np.median(cand)\n        else:\n            s_est = (s_min + s_max) / 2.0\n\n        r_low = max(s_min, s_est * 0.85)\n        r_high = min(s_max, s_est * 1.15)\n        s_vals2 = np.linspace(r_low, r_high, 48)\n        for s in s_vals2:\n            centers_try = build_centers(s)\n            radii_try = compute_max_radii(centers_try, max_iter=500)\n            ssum = radii_try.sum()\n            if ssum > best_sum:\n                best_sum = ssum\n                best_centers = centers_try.copy()\n                best_radii = radii_try.copy()\n\n    # Final tightening: slight local jitter of positions (small random shifts) to possibly increase sum\n    # Try a few random perturbations to escape symmetric local optima\n    rng = np.random.default_rng(42)\n    for scale in [0.0, 0.0025, 0.005, 0.01]:\n        for _ in range(6):\n            if scale == 0.0:\n                pert = np.zeros_like(best_centers)\n            else:\n                pert = rng.normal(scale=scale, size=best_centers.shape)\n            centers_try = np.clip(best_centers + pert, 1e-6, 1.0 - 1e-6)\n            radii_try = compute_max_radii(centers_try, max_iter=600)\n            ssum = radii_try.sum()\n            if ssum > best_sum:\n                best_sum = ssum\n                best_centers = centers_try.copy()\n                best_radii = radii_try.copy()\n\n    # Ensure shapes and return\n    centers_final = np.array(best_centers)[:n].reshape((n, 2))\n    radii_final = np.array(best_radii)[:n].reshape((n,))\n    sum_radii = float(np.sum(radii_final))\n    return centers_final, radii_final, sum_radii\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.5)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nKey features: Alternative approach to validity, Alternative approach to sum_radii\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 0.9256, Type: High-Performer)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles using a hexagonal lattice constructor.\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a deterministic arrangement of 26 circle centers in [0,1]^2\n    using a hexagonal (offset-grid) lattice. We choose the largest lattice\n    spacing s that still produces at least 26 candidate centers, then\n    keep the 26 centers closest to the square center to mitigate edge effects.\n    Radii are set to the maximal non-overlapping values given these centers:\n        r_i = min(dist_to_border(i), 0.5 * min_j dist(center_i, center_j))\n    This yields a valid non-overlapping packing and tends to produce dense,\n    hexagonal-like local arrangements that maximize the sum of radii.\n    Returns:\n        (centers (26,2), radii (26,), sum_of_radii)\n    \"\"\"\n    n_target = 26\n\n    def make_hex_centers(s):\n        # margin so circles can have radius up to s/2 without crossing border\n        margin = s / 2.0\n        pts = []\n        y = margin\n        row = 0\n        y_step = s * np.sqrt(3) / 2.0\n        while y <= 1.0 - margin + 1e-12:\n            x_offset = (row % 2) * (s / 2.0)\n            x = margin + x_offset\n            while x <= 1.0 - margin + 1e-12:\n                pts.append((x, y))\n                x += s\n            row += 1\n            y += y_step\n        return np.array(pts)\n\n    # Binary search for the largest s that still yields at least n_target centers.\n    low, high = 0.01, 0.6  # high > possible spacing\n    best_s = low\n    for _ in range(40):\n        mid = 0.5 * (low + high)\n        pts = make_hex_centers(mid)\n        if pts.shape[0] >= n_target:\n            best_s = mid\n            low = mid  # we can try larger spacing\n        else:\n            high = mid\n    centers_all = make_hex_centers(best_s)\n\n    # If we have more than needed, keep the n_target centers closest to square center\n    if centers_all.shape[0] > n_target:\n        center_of_square = np.array([0.5, 0.5])\n        dists = np.linalg.norm(centers_all - center_of_square, axis=1)\n        idx = np.argsort(dists)[:n_target]\n        centers = centers_all[idx]\n    else:\n        centers = centers_all.copy()\n\n    # As a small enhancement, ensure we always return exactly n_target centers\n    # (should be guaranteed by construction/trimming above)\n    if centers.shape[0] < n_target:\n        # fallback: fill with evenly spaced points along a small spiral near center\n        extra = n_target - centers.shape[0]\n        spiral = []\n        for k in range(extra):\n            t = 0.05 * (k + 1)\n            angle = 2 * np.pi * (k + 1) / extra\n            spiral.append([0.5 + t * np.cos(angle), 0.5 + t * np.sin(angle)])\n        centers = np.vstack([centers, np.array(spiral)])\n\n    # Sort centers for determinism (by x then y)\n    centers = centers[np.lexsort((centers[:, 1], centers[:, 0]))]\n\n    # Compute maximal valid radii:\n    radii = compute_max_radii(centers)\n\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers):\n    \"\"\"\n    For a fixed set of centers, compute a safe maximal radius assignment\n    that guarantees no overlaps and containment in the unit square.\n    We use the conservative but tight rule:\n        r_i = min(dist_to_border(i), 0.5 * min_j dist(i,j))\n    which makes touching allowed but not overlapping.\n    \"\"\"\n    n = centers.shape[0]\n    radii = np.zeros(n)\n    # distance to borders\n    dist_border = np.minimum.reduce([centers[:, 0], centers[:, 1], 1 - centers[:, 0], 1 - centers[:, 1]])\n    # pairwise distances (compute efficiently)\n    diff = centers[:, None, :] - centers[None, :, :]  # n x n x 2\n    dists = np.sqrt(np.sum(diff ** 2, axis=2))\n    # set diagonal to a large number to ignore self-distance\n    np.fill_diagonal(dists, np.inf)\n    min_pair = np.min(dists, axis=1)\n    # radii limited by half the nearest neighbor distance and by borders\n    radii = np.minimum(dist_border, 0.5 * min_pair)\n    # Ensure non-negative and tiny numerical floor\n    radii = np.maximum(radii, 0.0)\n    return radii\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6, edgecolor='k', linewidth=0.5)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Hex-lattice Constructor Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nUnique approach: Modification: Full rewrite, Excellent validity (1.000), Excellent sum_radii (2.439)\n\n### Inspiration 2 (Score: 0.4222, Type: Experimental)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Improved constructor-based circle packing for n=26 circles.\n\nThis constructs an explicit hex-like layout (5 rows: 5,6,5,5,5 = 26)\nand then computes near-optimal radii for the fixed centers by an\niterative relaxation that enforces border & pairwise non-overlap\nconstraints.  A short scan over initial spacing scales picks the best\nconfiguration found.\n\"\"\"\nimport math\nimport numpy as np\n\n\ndef construct_packing():\n    n = 26\n\n    # row pattern (5 + 6 + 5 + 5 + 5 = 26)\n    rows = [5, 6, 5, 5, 5]\n\n    def centers_for_r(r):\n        # spacing for hex-like packing\n        vx = 2.0 * r\n        vy = math.sqrt(3.0) * r\n        total_height = vy * (len(rows) - 1)\n        y0 = 0.5 - total_height / 2.0\n        centers = []\n        for i, m in enumerate(rows):\n            # center a row horizontally; alternate rows offset by r (hex pattern)\n            span = (m - 1) * vx\n            x0 = 0.5 - span / 2.0\n            if i % 2 == 1:\n                x0 += r  # offset\n            for k in range(m):\n                x = x0 + k * vx\n                y = y0 + i * vy\n                centers.append([x, y])\n        return np.array(centers)\n\n    def max_radii_relax(centers, iters=1000, tol=1e-9):\n        nloc = centers.shape[0]\n        # initial radii: distance to border\n        radii = np.minimum.reduce([\n            centers[:, 0], centers[:, 1], 1 - centers[:, 0], 1 - centers[:, 1]\n        ]).copy()\n        # iterative pairwise relaxation: scale offending pairs\n        for _ in range(iters):\n            changed = 0.0\n            for i in range(nloc):\n                for j in range(i + 1, nloc):\n                    dx = centers[i, 0] - centers[j, 0]\n                    dy = centers[i, 1] - centers[j, 1]\n                    d = math.hypot(dx, dy)\n                    if d <= 0.0:\n                        # coincide: force one to zero to avoid division by zero\n                        if radii[j] > radii[i]:\n                            radii[j] = 0.0\n                        else:\n                            radii[i] = 0.0\n                        changed = 1.0\n                        continue\n                    sumr = radii[i] + radii[j]\n                    if sumr > d:\n                        # scale proportionally to keep ratio but satisfy r_i + r_j = d\n                        if sumr > 0:\n                            factor = d / sumr\n                            ri_new = radii[i] * factor\n                            rj_new = radii[j] * factor\n                        else:\n                            # both zero - nothing\n                            ri_new, rj_new = 0.0, 0.0\n                        changed += abs(radii[i] - ri_new) + abs(radii[j] - rj_new)\n                        radii[i], radii[j] = ri_new, rj_new\n            # enforce border caps each pass (in case floating drift)\n            border_caps = np.minimum.reduce([\n                centers[:, 0], centers[:, 1], 1 - centers[:, 0], 1 - centers[:, 1]\n            ])\n            # also ensure non-negative\n            new_radii = np.minimum(radii, border_caps)\n            changed += np.sum(np.abs(radii - new_radii))\n            radii = new_radii\n            if changed <= tol:\n                break\n        return radii\n\n    # Scan initial spacing parameter (r_target) to generate centers.\n    # For each candidate, compute relaxed radii and keep best sum.\n    best = None\n    best_sum = -1.0\n\n    # plausible r range: small to an upper bound (0.02 .. 0.13)\n    candidates = np.concatenate([\n        np.linspace(0.06, 0.12, 25),\n        np.linspace(0.04, 0.08, 12)\n    ])\n    candidates = np.unique(np.clip(candidates, 0.01, 0.5))\n\n    for r_try in candidates:\n        centers = centers_for_r(r_try)\n        # discard configurations with centers outside [0,1]\n        if centers.min() < -1e-8 or centers.max() > 1 + 1e-8:\n            continue\n        radii = max_radii_relax(centers)\n        s = radii.sum()\n        if s > best_sum:\n            best_sum = s\n            best = (centers.copy(), radii.copy(), s)\n\n    # Fallback (shouldn't happen) - create a tiny grid\n    if best is None:\n        centers = centers_for_r(0.05)\n        radii = max_radii_relax(centers)\n        best = (centers, radii, radii.sum())\n\n    centers, radii, sum_radii = best\n    # ensure returned arrays have correct shapes (26,2) and (26,)\n    centers = np.asarray(centers).reshape((n, 2))\n    radii = np.asarray(radii).reshape((n,))\n    return centers, radii, float(sum_radii)\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.6)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nUnique approach: Modification: Full rewrite, Excellent validity (1.000), Excellent sum_radii (1.113)\n\n### Inspiration 3 (Score: 0.5622, Type: Experimental)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26: hex-lattice center-biased selection\nwith proportional pairwise scaling to maximize sum of radii.\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n    center = np.array([0.5, 0.5])\n    # hex lattice spacing (tuned)\n    s = 0.125\n    a = np.array([s, 0.0])\n    b = np.array([s * 0.5, s * np.sqrt(3) / 2.0])\n\n    # generate lattice around center\n    pts = []\n    R = 8  # index range, large enough to cover square\n    eps = 1e-6\n    for i in range(-R, R + 1):\n        for j in range(-R, R + 1):\n            p = center + i * a + j * b\n            if eps <= p[0] <= 1 - eps and eps <= p[1] <= 1 - eps:\n                pts.append(p)\n    pts = np.array(pts)\n    if pts.shape[0] < n:\n        # fallback: uniform grid\n        m = int(np.ceil(np.sqrt(n)))\n        xs = np.linspace(0.05, 0.95, m)\n        ys = np.linspace(0.05, 0.95, m)\n        grid = np.array([[x, y] for y in ys for x in xs])\n        pts = grid\n\n    # pick n points closest to center to concentrate large circles centrally\n    d = np.sum((pts - center) ** 2, axis=1)\n    order = np.argsort(d)\n    centers = pts[order[:n]]\n\n    radii = compute_max_radii_proportional(centers)\n    sum_radii = float(np.sum(radii))\n    return centers, radii, sum_radii\n\ndef compute_max_radii_proportional(centers, tol=1e-8, max_iter=5000):\n    n = centers.shape[0]\n    # initial limits by borders\n    border_limits = np.minimum.reduce([centers[:,0], centers[:,1], 1 - centers[:,0], 1 - centers[:,1]])\n    radii = border_limits.copy()\n\n    # pairwise distances\n    diff = centers[:, None, :] - centers[None, :, :]\n    D = np.sqrt((diff ** 2).sum(axis=2))\n    np.fill_diagonal(D, 1.0)  # avoid zero on diagonal for divisions\n\n    for it in range(max_iter):\n        max_change = 0.0\n        # Enforce pairwise constraints by proportional scaling of both radii\n        for i in range(n):\n            ri = radii[i]\n            # vectorized over j>i to reduce python overhead\n            j_idx = np.arange(i+1, n)\n            if j_idx.size == 0:\n                continue\n            rj = radii[j_idx]\n            dij = D[i, j_idx]\n            sumr = ri + rj\n            # where overlap occurs\n            overlap = sumr > dij\n            if not np.any(overlap):\n                continue\n            # compute scale factors where needed\n            scale = np.ones_like(sumr)\n            scale[overlap] = dij[overlap] / sumr[overlap]\n            # apply proportional scaling: ri' = ri * min_scale over all overlapping pairs involving i\n            # To be fair across multiple overlaps, compute minimal scale for each involved variable\n            min_scale_i = np.min(scale[overlap]) if np.any(overlap) else 1.0\n            if min_scale_i < 1.0:\n                new_ri = ri * min_scale_i\n                change = abs(new_ri - ri)\n                if change > 0:\n                    radii[i] = new_ri\n                    max_change = max(max_change, change)\n            # apply scaling to each rj individually\n            for idx, j in enumerate(j_idx):\n                if overlap[idx]:\n                    new_rj = radii[j] * scale[idx]\n                    change = abs(new_rj - radii[j])\n                    if change > 0:\n                        radii[j] = new_rj\n                        max_change = max(max_change, change)\n\n        # enforce border limits\n        over = radii > border_limits\n        if np.any(over):\n            change = np.max(np.abs(radii[over] - border_limits[over]))\n            radii[over] = border_limits[over]\n            max_change = max(max_change, float(change))\n\n        if max_change < tol:\n            break\n\n    # numerical sanitize\n    radii[radii < 0] = 0.0\n    return radii\n\n# EVOLVE-BLOCK-END\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.5)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nUnique approach: Modification: Full rewrite, Excellent validity (1.000), Excellent sum_radii (1.481)\n\n# Current Program\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles (parameterized layouts + projection)\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    \"\"\"\n    Try a small family of explicit hex-like row patterns and a short 1D sweep of\n    horizontal spacing s to pick the layout that yields the largest sum of maximal\n    radii (centers fixed, radii computed by projection).\n    Returns centers (26,2), radii (26,), sum_radii.\n    \"\"\"\n    n = 26\n    patterns = [\n        [6,5,5,5,5],   # 5 rows\n        [7,6,7,6],     # 4 rows\n        [5,6,5,5,5]    # alternative 5 rows\n    ]\n    best_sum = -1.0\n    best_centers = None\n    best_radii = None\n    sqrt3 = np.sqrt(3.0)\n\n    # For each pattern, sweep plausible s values\n    for pat in patterns:\n        max_row = max(pat)\n        # choose plausible s range so row fits: s*(max_row-1) <= 0.9 approx leave margin\n        s_min = 0.10\n        s_max = min(0.22, 0.9 / max(1, (max_row - 1)))\n        if s_max <= s_min:\n            s_max = s_min + 0.05\n        for s in np.linspace(s_min, s_max, 36):\n            v = s * sqrt3 / 2.0\n            rows = len(pat)\n            # vertical span of centers\n            v_span = (rows - 1) * v\n            # center block vertically in unit square with small margin\n            y0 = 0.5 - v_span / 2.0\n            centers = []\n            for r, m in enumerate(pat):\n                total_w = (m - 1) * s\n                x_start = 0.5 - total_w / 2.0\n                if r % 2 == 1:\n                    x_start += s / 2.0\n                y = y0 + r * v\n                for k in range(m):\n                    x = x_start + k * s\n                    # clip a touch inside\n                    centers.append([min(max(x, 1e-6), 1.0 - 1e-6), min(max(y, 1e-6), 1.0 - 1e-6)])\n            centers = np.array(centers[:n], dtype=float)\n            if centers.shape[0] != n:\n                continue\n            radii = compute_max_radii(centers)\n            ssum = radii.sum()\n            if ssum > best_sum:\n                best_sum = ssum\n                best_centers = centers.copy()\n                best_radii = radii.copy()\n\n    # Fallback if none found\n    if best_centers is None:\n        best_centers = np.array([[0.5,0.5]] + [[0.5+0.01*i,0.5+0.01*j] for i in range(n-1) for j in range(1)][:n-1])\n        best_radii = compute_max_radii(best_centers)\n        best_sum = float(best_radii.sum())\n\n    return best_centers, best_radii, float(best_sum)\n\ndef compute_max_radii(centers, tol=1e-8, max_iter=2000):\n    \"\"\"\n    Given fixed centers in unit square compute maximal radii by iterative projection:\n    - start from border-limited radii\n    - repeatedly fix pairwise overlaps by shrinking the larger circle of each offending pair\n    \"\"\"\n    n = centers.shape[0]\n    # initial radii limited by distance to border\n    radii = np.minimum.reduce([centers[:,0], centers[:,1], 1.0-centers[:,0], 1.0-centers[:,1]]).copy()\n    # pairwise distances\n    dx = centers[:,0][:,None] - centers[:,0][None,:]\n    dy = centers[:,1][:,None] - centers[:,1][None,:]\n    D = np.sqrt(np.maximum(0.0, dx*dx + dy*dy))\n    # small iterative greedy correction (shrink larger when ri+rj>d)\n    for _ in range(max_iter):\n        max_change = 0.0\n        # find violating pairs\n        I,J = np.triu_indices(n, k=1)\n        dij = D[I,J]\n        rij = radii[I] + radii[J]\n        mask = rij > dij\n        if not np.any(mask):\n            break\n        for i,j,d in zip(I[mask], J[mask], dij[mask]):\n            ri = radii[i]; rj = radii[j]\n            if ri + rj <= d + 1e-12:\n                continue\n            if ri > rj:\n                new_ri = max(0.0, d - rj)\n                ch = abs(new_ri - ri)\n                if ch>0:\n                    radii[i] = new_ri\n                    if ch>max_change: max_change=ch\n            else:\n                new_rj = max(0.0, d - ri)\n                ch = abs(new_rj - rj)\n                if ch>0:\n                    radii[j] = new_rj\n                    if ch>max_change: max_change=ch\n        # enforce border limits\n        border = np.minimum.reduce([centers[:,0], centers[:,1], 1.0-centers[:,0], 1.0-centers[:,1]])\n        over = radii > border\n        if np.any(over):\n            diff = np.max(radii[over] - border[over])\n            radii[over] = border[over]\n            if diff > max_change: max_change = diff\n        if max_change < tol:\n            break\n    radii = np.maximum(radii, 0.0)\n    return radii\n\n# EVOLVE-BLOCK-END\n\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(6,6))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect(\"equal\")\n    for c,r in zip(centers,radii):\n        ax.add_patch(Circle(c,r,alpha=0.6,edgecolor='k'))\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, s = run_packing()\n    print(f\"Sum of radii: {s:.6f}\")\n    # visualize(centers, radii)\n```\n\n# Task\nRewrite the program to improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: complexity, diversity\nDifferent solutions with similar fitness but different features are valuable.\nProvide the complete new program code.\n\nIMPORTANT: Make sure your rewritten program maintains the same inputs and outputs\nas the original program, but with improved internal implementation.\n\n```python\n# Your rewritten program here\n```", "responses": ["```python\n# EVOLVE-BLOCK-START\n\"\"\"Deterministic constructor for n=26 circle packing in the unit square.\nUses a hexagonal (offset) lattice: choose the largest lattice spacing s that\nstill yields at least 26 candidate centers, keep the 26 centers closest to\nthe square center (mitigating edge effects) and assign radii by the tight\nlocal rule r_i = min(dist_to_border(i), 0.5*min_j dist(i,j)).\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    n = 26\n\n    def make_hex_centers(s):\n        eps = 1e-12\n        margin = s / 2.0\n        pts = []\n        y = margin\n        row = 0\n        y_step = s * np.sqrt(3) / 2.0\n        while y <= 1.0 - margin + eps:\n            x_offset = (row % 2) * (s / 2.0)\n            x = margin + x_offset\n            while x <= 1.0 - margin + eps:\n                pts.append((x, y))\n                x += s\n            row += 1\n            y += y_step\n        return np.array(pts, dtype=float)\n\n    # binary search for largest spacing producing >= n centers\n    low, high = 0.01, 0.6\n    best_s = low\n    for _ in range(40):\n        mid = 0.5 * (low + high)\n        pts = make_hex_centers(mid)\n        if pts.shape[0] >= n:\n            best_s = mid\n            low = mid\n        else:\n            high = mid\n\n    centers_all = make_hex_centers(best_s)\n    if centers_all.shape[0] > n:\n        center_sq = np.array([0.5, 0.5])\n        d = np.linalg.norm(centers_all - center_sq, axis=1)\n        idx = np.argsort(d)[:n]\n        centers = centers_all[idx]\n    else:\n        centers = centers_all.copy()\n\n    # deterministic ordering\n    centers = centers[np.lexsort((centers[:,1], centers[:,0]))]\n\n    radii = compute_max_radii(centers)\n    return centers, radii, float(radii.sum())\n\ndef compute_max_radii(centers):\n    \"\"\"Vectorized tight local rule: r_i = min(dist_to_border, 0.5*min_pair_dist).\"\"\"\n    if centers is None or len(centers) == 0:\n        return np.array([])\n    n = centers.shape[0]\n    dist_border = np.minimum.reduce([centers[:,0], centers[:,1], 1.0-centers[:,0], 1.0-centers[:,1]])\n    diff = centers[:,None,:] - centers[None,:,:]\n    dists = np.sqrt(np.maximum(0.0, (diff**2).sum(axis=2)))\n    np.fill_diagonal(dists, np.inf)\n    min_pair = np.min(dists, axis=1)\n    radii = np.minimum(dist_border, 0.5 * min_pair)\n    radii = np.maximum(radii, 0.0)\n    return radii\n\n# EVOLVE-BLOCK-END\n\ndef run_packing():\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n    fig, ax = plt.subplots(figsize=(6,6))\n    ax.set_xlim(0,1); ax.set_ylim(0,1); ax.set_aspect(\"equal\")\n    for c,r in zip(centers,radii):\n        ax.add_patch(Circle(c,r,alpha=0.6,edgecolor='k'))\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, s = run_packing()\n    print(f\"Sum of radii: {s:.6f}\")\n    # visualize(centers, radii)\n```"]}}, "artifacts_json": "{\"stdout\": \"Running in subprocess, Python version: 3.12.1 (v3.12.1:2305ca5144, Dec  7 2023, 17:23:38) [Clang 13.0.0 (clang-1300.0.29.30)]\\nProgram path: /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmpuqp8x_6l.py\\nCalling run_packing()...\\nrun_packing() returned successfully: sum_radii = 2.4389662673793744\\nResults saved to /var/folders/mk/x70_g2zd66sbqw_vz5g2fgh00000gn/T/tmpaud0ksmy.py.results\\n\", \"stderr\": \"\"}", "artifact_dir": null}